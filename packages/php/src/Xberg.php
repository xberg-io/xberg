<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
declare(strict_types=1);

namespace Xberg;

final class Xberg
{
    /**
     * Extract content from a single bytes or URI input.
     *
     * @param ExtractInput $input
     * @param ExtractionConfig $config
     * @return ExtractionOutput
     * @throws \Xberg\XbergException
     */
    public static function extract(
ExtractInput $input, ExtractionConfig $config): ExtractionOutput
    {
        return \Xberg\XbergApi::extract($input, $config); // delegate to native extension class
    }
    /**
     * Extract content from multiple bytes or URI inputs.
     *
     * @param array<ExtractInput> $inputs
     * @param ExtractionConfig $config
     * @return ExtractionOutput
     * @throws \Xberg\XbergException
     */
    public static function extractBatch(
array $inputs, ExtractionConfig $config): ExtractionOutput
    {
        return \Xberg\XbergApi::extractBatch($inputs, $config); // delegate to native extension class
    }
    /**
     * Detect MIME type from raw file bytes.
     *
     * Uses magic byte signatures to detect file type from content.
     * Falls back to `infer` crate for comprehensive detection.
     *
     * For ZIP-based files, inspects contents to distinguish Office Open XML
     * formats (DOCX, XLSX, PPTX) from plain ZIP archives.
     *
     * @param string $content
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function detectMimeTypeFromBytes(
string $content): string
    {
        return \Xberg\XbergApi::detectMimeTypeFromBytes($content); // delegate to native extension class
    }
    /**
     * Get file extensions for a given MIME type.
     *
     * Returns all known file extensions that map to the specified MIME type.
     *
     * @param string $mime_type
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function getExtensionsForMime(
string $mime_type): array
    {
        return \Xberg\XbergApi::getExtensionsForMime($mime_type); // delegate to native extension class
    }
    /**
     * List all supported document formats.
     *
     * Returns every file extension Xberg recognizes together with its
     * corresponding MIME type, derived from the central format registry.
     * Formats that have no registered file extension (such as source code,
     * which is detected dynamically) are not included.
     *
     * The list is sorted alphabetically by file extension.
     *
     * @return array<SupportedFormat>
     */
    public static function listSupportedFormats(): array {
        return \Xberg\XbergApi::listSupportedFormats(); // delegate to native extension class
    }
    /**
     * Detect QR codes in the bytes of an `ExtractedImage`.
     *
     * `format_hint` is currently unused — the `image` crate auto-detects the
     * container format from magic bytes — but the parameter is retained so future
     * backends (e.g. a WebP-via-`webp-decoder` variant) can use it without an API
     * break.
     *
     * Returns an empty vector on any of:
     *
     * - Empty input.
     * - Image-decode failure.
     * - No QR grids detected.
     * - All detected grids fail to decode.
     *
     * Successfully decoded QR codes carry their payload, a confidence of `1.0`
     * (rqrr does not expose per-grid confidence; a successful decode is treated
     * as high-confidence by convention), and the pixel-space bounding box derived
     * from the four corner points of the grid.
     *
     * @param string $image_bytes
     * @param ?string $_format_hint
     * @return array<QrCode>
     */
    public static function detectQrCodes(
string $image_bytes, ?string $_format_hint = null): array
    {
        return \Xberg\XbergApi::detectQrCodes($image_bytes, $_format_hint); // delegate to native extension class
    }
    /**
     * List the names of all registered embedding backends.
     *
     * Used by `xberg-cli`, the api/mcp endpoints, and generated language
     * bindings.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listEmbeddingBackends(): array {
        return \Xberg\XbergApi::listEmbeddingBackends(); // delegate to native extension class
    }
    /**
     * List all registered OCR backends.
     *
     * Returns the names of all OCR backends currently registered in the global registry.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listOcrBackends(): array {
        return \Xberg\XbergApi::listOcrBackends(); // delegate to native extension class
    }
    /**
     * Register every built-in post-processor enabled by the active feature set.
     *
     * This is the single entry point that callers (including
     * `register_default_post_processors`) use to populate the global
     * post-processor registry with the in-tree built-ins. Each submodule's own
     * `register` function is gated by its feature flag so this aggregate stays
     * safe to call on any target.
     *
     * @return void
     * @throws \Xberg\XbergException
     */
    public static function registerBuiltin(): void {
        \Xberg\XbergApi::registerBuiltin(); // delegate to native extension class
    }
    /**
     * List all registered post-processor names.
     *
     * Returns a vector of all post-processor names currently registered in the
     * global registry.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listPostProcessors(): array {
        return \Xberg\XbergApi::listPostProcessors(); // delegate to native extension class
    }
    /**
     * List names of all registered renderers.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listRenderers(): array {
        return \Xberg\XbergApi::listRenderers(); // delegate to native extension class
    }
    /**
     * List the names of all registered reranker backends.
     *
     * Used by `xberg-cli`, the api/mcp endpoints, and generated language
     * bindings.
     *
     * Since v5.0.0.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listRerankerBackends(): array {
        return \Xberg\XbergApi::listRerankerBackends(); // delegate to native extension class
    }
    /**
     * List names of all registered validators.
     *
     * @return array<string>
     * @throws \Xberg\XbergException
     */
    public static function listValidators(): array {
        return \Xberg\XbergApi::listValidators(); // delegate to native extension class
    }
    /**
     * Run page classification against an extraction result.
     *
     * Mutates `result.page_classifications` with one entry per non-empty page and
     * appends every LLM call's usage to `result.llm_usage`.
     *
     * @param ExtractionResult $result
     * @param PageClassificationConfig $config
     * @return void
     * @throws \Xberg\XbergException
     */
    public static function classifyPages(
ExtractionResult $result, PageClassificationConfig $config): void
    {
        \Xberg\XbergApi::classifyPages($result, $config); // delegate to native extension class
    }
    /**
     * Classify a single piece of text without requiring an `ExtractionResult`.
     *
     * Use this when the caller already has plain text (e.g. a RAG ingest pipeline
     * receiving documents off a queue) and wants a label list back without
     * manufacturing extractor-side metadata.
     *
     * @param string $text
     * @param PageClassificationConfig $config
     * @return array<ClassificationLabel>
     * @throws \Xberg\XbergException
     */
    public static function classifyText(
string $text, PageClassificationConfig $config): array
    {
        return \Xberg\XbergApi::classifyText($text, $config); // delegate to native extension class
    }
    /**
     * Classify a single document (as multiple pages or a single text block).
     *
     * Aggregates classifications across all pages in the provided text, returning
     * a combined label set that represents the document as a whole.
     *
     * @param array<string> $pages
     * @param PageClassificationConfig $config
     * @return array<ClassificationLabel>
     * @throws \Xberg\XbergException
     */
    public static function classifyDocument(
array $pages, PageClassificationConfig $config): array
    {
        return \Xberg\XbergApi::classifyDocument($pages, $config); // delegate to native extension class
    }
    /**
     * Eagerly download a NER model into the xberg cache.
     *
     * `name` is a supported xberg GLiNER alias or catalog id. The CLI flag
     * `xberg cache warm --ner` delegates here.
     *
     * @param string $name
     * @param ?string $cache_dir
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function downloadModel(
string $name, ?string $cache_dir = null): string
    {
        return \Xberg\XbergApi::downloadModel($name, $cache_dir); // delegate to native extension class
    }
    /**
     * Pinned default NER model identifier.
     *
     * @return string
     */
    public static function defaultModelName(): string {
        return \Xberg\XbergApi::defaultModelName(); // delegate to native extension class
    }
    /**
     * All NER models xberg knows about (used by `--all-ner-models`).
     *
     * @return array<string>
     */
    public static function knownModels(): array {
        return \Xberg\XbergApi::knownModels(); // delegate to native extension class
    }
    /**
     * Run pattern redaction (and optional NER-driven redaction) over `result` and
     * rewrite every textual field. Populates `result.redaction_report`.
     *
     * @param ExtractionResult $result
     * @param RedactionConfig $config
     * @return void
     * @throws \Xberg\XbergException
     */
    public static function redact(
ExtractionResult $result, RedactionConfig $config): void
    {
        \Xberg\XbergApi::redact($result, $config); // delegate to native extension class
    }
    /**
     * Find all US Social Security Number spans in `text` (format: NNN-NN-NNNN).
     *
     * @param string $text
     * @return array<PatternMatch>
     */
    public static function findAll(
string $text): array
    {
        return \Xberg\XbergApi::findAll($text); // delegate to native extension class
    }
    /**
     * Scan `text` for every PII category in `categories` and return all matches
     * in source-byte order.
     *
     * When `categories` is empty every supported regex-detectable category fires.
     * Person / Organization / Location are *not* covered by the pattern engine —
     * they must be supplied by a NER backend through the redaction engine.
     *
     * @param string $text
     * @param array<PiiCategory> $categories
     * @return array<PatternMatch>
     */
    public static function scanText(
string $text, array $categories): array
    {
        return \Xberg\XbergApi::scanText($text, $categories); // delegate to native extension class
    }
    /**
     * Score and return the top-N sentences from `text`, joined in original order.
     *
     * `language` is an ISO 639 (or locale) code used to pick a stopword list;
     * pass `None` (or an unknown code) to fall back to English.
     * `max_tokens` bounds the summary length by whitespace-separated tokens;
     * `None` falls back to [`DEFAULT_MAX_TOKENS`].
     *
     * @param string $text
     * @param ?string $language
     * @param ?int $max_tokens
     * @return ?string
     */
    public static function summarize(
string $text, ?string $language = null, ?int $max_tokens = null): ?string
    {
        return \Xberg\XbergApi::summarize($text, $language, $max_tokens); // delegate to native extension class
    }
    /**
     * Count whitespace-separated tokens (used for token-budget bookkeeping by
     * callers).
     *
     * @param string $text
     * @return int
     */
    public static function tokenCount(
string $text): int
    {
        return \Xberg\XbergApi::tokenCount($text); // delegate to native extension class
    }
    /**
     * Translate the extraction result in place.
     *
     * Populates `result.translation` with the translated `content`, optionally the
     * translated `formatted_content` (when `preserve_markup = true`), and rewrites
     * every chunk's `content` field. Every LLM call's usage is appended to
     * `result.llm_usage`.
     *
     * @param ExtractionResult $result
     * @param TranslationConfig $config
     * @return void
     * @throws \Xberg\XbergException
     */
    public static function translateResult(
ExtractionResult $result, TranslationConfig $config): void
    {
        \Xberg\XbergApi::translateResult($result, $config); // delegate to native extension class
    }
    /**
     * Find all footnote anchor references in markdown text.
     *
     * Returns a vector of footnote anchors (`[^label]` use-sites), including byte offsets.
     * Footnote definitions (`[^label]: ...`) are NOT included in the results.
     *
     * @param string $markdown
     * @return array<FootnoteAnchor>
     */
    public static function findFootnoteAnchors(
string $markdown): array
    {
        return \Xberg\XbergApi::findFootnoteAnchors($markdown); // delegate to native extension class
    }
    /**
     * Parse footnote definitions from markdown text.
     *
     * Returns a vector of footnote definitions found in the markdown.
     * Handles multi-line definitions with continuation/indented lines (CommonMark format).
     *
     * @param string $markdown
     * @return array<FootnoteDefinition>
     */
    public static function parseFootnoteDefinitions(
string $markdown): array
    {
        return \Xberg\XbergApi::parseFootnoteDefinitions($markdown); // delegate to native extension class
    }
    /**
     * Find inference markers in markdown text.
     *
     * Returns byte offsets of every `[*inference*]` marker found in the text.
     *
     * @param string $markdown
     * @return array<int>
     */
    public static function findInferenceMarkers(
string $markdown): array
    {
        return \Xberg\XbergApi::findInferenceMarkers($markdown); // delegate to native extension class
    }
    /**
     * Find unmarked claims in markdown text.
     *
     * Returns lines that assert a claim but carry neither a footnote citation anchor (`[^...]`)
     * nor an inference marker (`[*inference*]`).
     *
     * The heuristic is simple: a line that contains alphabetic words, ends with sentence punctuation,
     * and is not a heading, blank line, or markup-only line is considered a claim.
     * Exclude lines that appear in the citation block (after `---` + `<!-- citations ... -->`).
     *
     * @param string $markdown
     * @return array<string>
     */
    public static function findUnmarkedClaims(
string $markdown): array
    {
        return \Xberg\XbergApi::findUnmarkedClaims($markdown); // delegate to native extension class
    }
    /**
     * Parse the structured citation block from markdown.
     *
     * Extracts citations from the block after a `---` thematic break followed by
     * `<!-- citations ... -->` comment. Parses each entry as:
     * `[^srcN]: <source>, <optional-locator>, excerpt: "<text>"`
     *
     * Returns parsed citations with source, optional locator, and optional excerpt.
     *
     * @param string $markdown
     * @return array<Citation>
     */
    public static function parseCitations(
string $markdown): array
    {
        return \Xberg\XbergApi::parseCitations($markdown); // delegate to native extension class
    }
    /**
     * Verify that an excerpt appears verbatim in source text.
     *
     * Performs exact matching by default. Also tries whitespace-normalized matching
     * (collapsing runs of whitespace on both sides) since PDF-extracted text often
     * has irregular spacing.
     *
     * @param string $excerpt
     * @param string $source_text
     * @return bool
     */
    public static function verifyExcerpt(
string $excerpt, string $source_text): bool
    {
        return \Xberg\XbergApi::verifyExcerpt($excerpt, $source_text); // delegate to native extension class
    }
    /**
     * Chunk text for RAG retrieval, ensuring every chunk carries a `heading_path`.
     *
     * Delegates to [`chunk_text`] using the caller's config (defaulting to
     * `ChunkerType::Markdown` when the config uses the default `Text` type, so that
     * heading hierarchy is resolved).  After chunking, derives
     * [`ChunkMetadata::heading_path`](crate::types::ChunkMetadata::heading_path) from each chunk's `heading_context`.
     *
     * @param string $text
     * @param ChunkingConfig $config
     * @return ChunkingResult
     * @throws \Xberg\XbergException
     */
    public static function chunkForRag(
string $text, ChunkingConfig $config): ChunkingResult
    {
        return \Xberg\XbergApi::chunkForRag($text, $config); // delegate to native extension class
    }
    /**
     * Compare two extraction results and return a structured diff.
     *
     * The comparison is purely structural — no I/O, no side effects. All fields
     * of [`ExtractionDiff`] are populated according to the provided [`DiffOptions`].
     *
     * @param ExtractionResult $a
     * @param ExtractionResult $b
     * @param DiffOptions $opts
     * @return ExtractionDiff
     */
    public static function compare(
ExtractionResult $a, ExtractionResult $b, DiffOptions $opts): ExtractionDiff
    {
        return \Xberg\XbergApi::compare($a, $b, $opts); // delegate to native extension class
    }
    /**
     * Extract content from a pre-cropped image region using a VLM.
     *
     * The caller is responsible for cropping the page image to the region's bounding
     * box before calling this function. The `image_bytes` parameter must contain the
     * raw bytes of the **cropped** region image (JPEG, PNG, WebP, etc.).
     *
     * @param string $image_bytes
     * @param string $image_mime
     * @param RegionKind $region_kind
     * @param LlmConfig $llm_config
     * @param ?string $custom_prompt
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function extractRegionWithVlm(
string $image_bytes, string $image_mime, RegionKind $region_kind, LlmConfig $llm_config, ?string $custom_prompt = null): string
    {
        return \Xberg\XbergApi::extractRegionWithVlm($image_bytes, $image_mime, $region_kind, $llm_config, $custom_prompt); // delegate to native extension class
    }
    /**
     * Rerank documents asynchronously.
     *
     * Async counterpart to [`rerank`]. Offloads blocking ONNX inference to a
     * dedicated blocking thread pool via Tokio's `spawn_blocking`, keeping the
     * async executor free.
     *
     * Since v5.0.0.
     *
     * @param string $query
     * @param array<string> $documents
     * @param RerankerConfig $config
     * @return array<RerankedDocument>
     * @throws \Xberg\XbergException
     */
    public static function rerankAsync(
string $query, array $documents, RerankerConfig $config): array
    {
        return \Xberg\XbergApi::rerankAsync($query, $documents, $config); // delegate to native extension class
    }
    /**
     * Extract keywords from text using the specified algorithm.
     *
     * This is the unified entry point for keyword extraction. The algorithm
     * used is determined by `config.algorithm`.
     *
     * @param string $text
     * @param KeywordConfig $config
     * @return array<Keyword>
     * @throws \Xberg\XbergException
     */
    public static function extractKeywords(
string $text, KeywordConfig $config): array
    {
        return \Xberg\XbergApi::extractKeywords($text, $config); // delegate to native extension class
    }
    /**
     * Analyze a document and determine the optimal chunking strategy.
     *
     * Decision logic (in priority order):
     *
     * 1. If user provides `disable_chunking` → no chunking
     * 2. If user provides page_ranges → use user overrides
     * 3. If chunking is not enabled → no chunking
     * 4. If format doesn't support chunking → no chunking
     * 5. If file is small (below both thresholds) and not force_chunking → no chunking
     * 6. If PDF has a substantial text layer AND !force_ocr → no chunking
     *    *(only when `heuristics-pdf` feature is enabled; otherwise skipped)*
     * 7. Otherwise → chunk the document
     *
     * @param DocumentMetadata $metadata
     * @param HeuristicsConfig $config
     * @param ?string $document_bytes
     * @return ChunkingDecision
     * @throws \Xberg\XbergException
     */
    public static function analyzeDocument(
DocumentMetadata $metadata, HeuristicsConfig $config, ?string $document_bytes = null): ChunkingDecision
    {
        return \Xberg\XbergApi::analyzeDocument($metadata, $config, $document_bytes); // delegate to native extension class
    }
    /**
     * Analyze a document with user-specified chunk ranges.
     *
     * Creates a chunk plan based on user-provided page ranges.
     *
     * @param array<PageRange> $user_ranges
     * @param int $total_pages
     * @param int $size_bytes
     * @param HeuristicsConfig $config
     * @return ChunkingDecision
     */
    public static function analyzeWithUserChunks(
array $user_ranges, int $total_pages, int $size_bytes, HeuristicsConfig $config): ChunkingDecision
    {
        return \Xberg\XbergApi::analyzeWithUserChunks($user_ranges, $total_pages, $size_bytes, $config); // delegate to native extension class
    }
    /**
     * Score a [`ConfidenceSignals`] triple into an [`ExtractionConfidence`] using
     * the supplied weights.
     *
     * When `signals.ocr_aggregate` is `None`, the OCR weight folds into
     * `text_coverage` so the weighted sum still totals 1.0.
     *
     * @param ConfidenceSignals $signals
     * @param ConfidenceWeights $weights
     * @return ExtractionConfidence
     */
    public static function scoreConfidence(
ConfidenceSignals $signals, ConfidenceWeights $weights): ExtractionConfidence
    {
        return \Xberg\XbergApi::scoreConfidence($signals, $weights); // delegate to native extension class
    }
    /**
     * Decision returned for pre-extraction rejection based on XLSX/PPTX-specific
     * resource bounds. Returns `Some(reason)` to reject; `None` to proceed.
     *
     * Callers must provide counts from a pre-extraction peek (e.g. parsing
     * `xl/workbook.xml` for sheet count).
     *
     * @param string $mime_type
     * @param ?int $sheet_count
     * @param ?int $workbook_cells
     * @param ?int $embedded_count
     * @param HeuristicsConfig $config
     * @return ?string
     */
    public static function checkFormatLimits(
string $mime_type, ?int $sheet_count, ?int $workbook_cells, ?int $embedded_count, HeuristicsConfig $config): ?string
    {
        return \Xberg\XbergApi::checkFormatLimits($mime_type, $sheet_count, $workbook_cells, $embedded_count, $config); // delegate to native extension class
    }
    /**
     * Derive document boundaries from an already-produced [`ExtractionResult`](crate::types::ExtractionResult).
     *
     * Builds a [`MultidocInput`] from `result.pages` (one [`PageSignals`] per
     * `PageContent` entry), then delegates to [`detect_boundaries`].
     *
     * # Fallback behaviour
     *
     * - If `result.pages` is `None` or empty the whole document is treated as a
     *   single document: returns `[Start(1), End(1)]`, matching the contract of
     *   [`detect_boundaries`] for a one-page input.
     *
     * # Text density
     *
     * `PageContent` does not carry a pre-computed density score.
     * This function approximates density as
     * `non_whitespace_chars / total_chars` (clamped to `[0.0, 1.0]`), which is a
     * reasonable proxy for how text-dense a page is relative to itself.  Pass a
     * custom [`MultidocInput`] to [`detect_boundaries`] directly when you need a
     * higher-fidelity density measurement (e.g. chars-per-pt² from a PDF extractor).
     *
     * @param ExtractionResult $result
     * @param MultidocThresholds $thresholds
     * @return array<DocumentBoundary>
     */
    public static function boundariesFromExtractionResult(
ExtractionResult $result, MultidocThresholds $thresholds): array
    {
        return \Xberg\XbergApi::boundariesFromExtractionResult($result, $thresholds); // delegate to native extension class
    }
    /**
     * Detect document boundaries in a multi-document PDF.
     *
     * Returns a list of detected boundaries, always including implicit boundaries
     * at start (page 1) and end (page_count).  Boundaries are returned in ascending
     * order of `start_page`.
     *
     * @param MultidocInput $input
     * @param MultidocThresholds $thresholds
     * @return array<DocumentBoundary>
     */
    public static function detectBoundaries(
MultidocInput $input, MultidocThresholds $thresholds): array
    {
        return \Xberg\XbergApi::detectBoundaries($input, $thresholds); // delegate to native extension class
    }
    /**
     * Decide which call mode best fits this document.
     *
     * Rules applied in order:
     *
     * 1. `image/*` → [`StructuredCallMode::VisionOnly`] (no text layer to start from).
     * 2. `application/pdf` → [`StructuredCallMode::TextOnly`] regardless of
     *    `text_coverage` or embedded image count.  Xberg's OCR + text-layer
     *    extraction produces text for scanned PDFs; the orchestrator's
     *    post-call confidence gate handles any vision escalation actually needed.
     * 3. DOCX / `text/html` / `text/*` / `application/json` / `application/xml` /
     *    `application/rtf` with `avg_chars_per_page > docx_text_min_density`
     *    → [`StructuredCallMode::TextOnly`].
     * 4. Anything else → [`StructuredCallMode::Skip`].
     *
     * After rule selection two post-rule promotions apply (in order):
     *
     * - `user_force_vision` promotes `TextOnly` → `TextPlusVision`
     *   (`Skip` stays `Skip` — caller meant to opt out).
     * - `enable_vision_fallback` promotes `TextOnly` →
     *   `TextOnlyWithVisionFallback` (does **not** upgrade `TextPlusVision` or
     *   `Skip`).
     *
     * @param StructuredInput $input
     * @param StructuredThresholds $t
     * @return StructuredCallMode
     */
    public static function chooseCallMode(
StructuredInput $input, StructuredThresholds $t): StructuredCallMode
    {
        return \Xberg\XbergApi::chooseCallMode($input, $t); // delegate to native extension class
    }
    /**
     * Calculate a chunking plan for a document.
     *
     * @param int $page_count
     * @param int $size_bytes
     * @param bool $needs_ocr
     * @param HeuristicsConfig $config
     * @return ChunkPlan
     */
    public static function calculateChunkPlan(
int $page_count, int $size_bytes, bool $needs_ocr, HeuristicsConfig $config): ChunkPlan
    {
        return \Xberg\XbergApi::calculateChunkPlan($page_count, $size_bytes, $needs_ocr, $config); // delegate to native extension class
    }
    /**
     * Calculate a chunk plan from user-specified page ranges.
     *
     * Validates and processes user overrides into a proper chunk plan.
     *
     * @param array<PageRange> $user_chunks
     * @param int $total_pages
     * @param int $size_bytes
     * @param HeuristicsConfig $config
     * @return ChunkPlan
     */
    public static function calculatePlanFromOverrides(
array $user_chunks, int $total_pages, int $size_bytes, HeuristicsConfig $config): ChunkPlan
    {
        return \Xberg\XbergApi::calculatePlanFromOverrides($user_chunks, $total_pages, $size_bytes, $config); // delegate to native extension class
    }
    /**
     * Stable sha256 fingerprint of `raw`, formatted as `sha256:<hex>`.
     *
     * @param string $raw
     * @return string
     */
    public static function fingerprint(
string $raw): string
    {
        return \Xberg\XbergApi::fingerprint($raw); // delegate to native extension class
    }
    /**
     * Resolve `(preset, custom_schema_override, context)` into a [`ResolvedPreset`].
     *
     * - `custom_schema` overrides `preset.schema` when set.
     * - `context` substitutes `{{key}}` tokens in `preset.context_template`; the
     *   rendered string is appended to `system_prompt` so the model sees it.
     *
     * @param Preset $preset
     * @param ?string $custom_schema
     * @param array<string, string> $context
     * @return ResolvedPreset
     * @throws \Xberg\XbergException
     */
    public static function resolve(
Preset $preset, ?string $custom_schema, array $context): ResolvedPreset
    {
        return \Xberg\XbergApi::resolve($preset, $custom_schema, $context); // delegate to native extension class
    }
    /**
     * Extract structured JSON from a document using JSON-encoded preset spec and options.
     *
     * This is the synchronous JSON-in / JSON-out entry point suitable for FFI and
     * language-binding call paths.
     *
     * @param string $bytes
     * @param string $mime
     * @param string $preset_spec_json
     * @param string $options_json
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function extractStructuredJson(
string $bytes, string $mime, string $preset_spec_json, string $options_json): string
    {
        return \Xberg\XbergApi::extractStructuredJson($bytes, $mime, $preset_spec_json, $options_json); // delegate to native extension class
    }
    /**
     * Split a multi-document PDF and extract structured JSON from each segment,
     * returning a JSON array of `StructuredOutput` objects.
     *
     * Non-PDF documents are passed through as a single-element array.
     *
     * @param string $bytes
     * @param string $mime
     * @param string $preset_spec_json
     * @param string $options_json
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function splitAndExtractJson(
string $bytes, string $mime, string $preset_spec_json, string $options_json): string
    {
        return \Xberg\XbergApi::splitAndExtractJson($bytes, $mime, $preset_spec_json, $options_json); // delegate to native extension class
    }
    /**
     * Render a single PDF page to PNG bytes.
     *
     * Returns raw PNG-encoded bytes for the specified page at the given DPI.
     * Uses pdf_oxide with tiny-skia for pure-Rust rendering.
     *
     * For pages with extreme dimensions (very wide vector diagrams, etc.) the
     * effective DPI may be automatically reduced to avoid rasterizer failure.
     * A warning is logged when this happens.
     *
     * @param string $pdf_bytes
     * @param int $page_index
     * @param ?int $dpi
     * @param ?string $password
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function renderPdfPageToPng(
string $pdf_bytes, int $page_index, ?int $dpi = null, ?string $password = null): string
    {
        return \Xberg\XbergApi::renderPdfPageToPng($pdf_bytes, $page_index, $dpi, $password); // delegate to native extension class
    }
    /**
     * Count the pages in a PDF without rendering any of them.
     *
     * Opens the document and returns its page count from the PDF structure. No page
     * is rasterized, so this is cheap relative to `render_pdf_page_to_png` — use it
     * when you only need the count (e.g. to drive a render loop over the pages).
     *
     * @param string $pdf_bytes
     * @param ?string $password
     * @return int
     * @throws \Xberg\XbergException
     */
    public static function pdfPageCount(
string $pdf_bytes, ?string $password = null): int
    {
        return \Xberg\XbergApi::pdfPageCount($pdf_bytes, $password); // delegate to native extension class
    }
    /**
     * Caption a single image from bytes.
     *
     * @param string $image_bytes
     * @param LlmConfig $llm_config
     * @param ?string $custom_prompt
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function captionImage(
string $image_bytes, LlmConfig $llm_config, ?string $custom_prompt = null): string
    {
        return \Xberg\XbergApi::captionImage($image_bytes, $llm_config, $custom_prompt); // delegate to native extension class
    }
    /**
     * Caption a single image from a file path.
     *
     * @param string $path
     * @param LlmConfig $llm_config
     * @param ?string $custom_prompt
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function captionImageFile(
string $path, LlmConfig $llm_config, ?string $custom_prompt = null): string
    {
        return \Xberg\XbergApi::captionImageFile($path, $llm_config, $custom_prompt); // delegate to native extension class
    }
    /**
     * Detect the MIME type of a file at the given path.
     *
     * Uses the file extension and optionally the file content to determine the MIME type.
     * Set `check_exists` to `true` to verify the file exists before detection.
     *
     * @param string $path
     * @param bool $check_exists
     * @return string
     * @throws \Xberg\XbergException
     */
    public static function detectMimeType(
string $path, bool $check_exists): string
    {
        return \Xberg\XbergApi::detectMimeType($path, $check_exists); // delegate to native extension class
    }
    /**
     * embedTextsAsync.
     *
     * @param array<string> $_texts
     * @param EmbeddingConfig $_config
     * @return array<array<float>>
     * @throws \Xberg\XbergException
     */
    public static function embedTextsAsync(
array $_texts, EmbeddingConfig $_config): array
    {
        return \Xberg\XbergApi::embedTextsAsync($_texts, $_config); // delegate to native extension class
    }
    /**
     * Get an embedding preset by name.
     *
     * Returns `None` if no preset with the given name exists. Returns an owned
     * clone so the value is safe to pass across FFI boundaries.
     *
     * @param string $name
     * @return ?EmbeddingPreset
     */
    public static function getEmbeddingPreset(
string $name): ?EmbeddingPreset
    {
        return \Xberg\XbergApi::getEmbeddingPreset($name); // delegate to native extension class
    }
    /**
     * List the names of all available embedding presets.
     *
     * Returns owned `String`s so the values are safe to pass across FFI boundaries.
     *
     * @return array<string>
     */
    public static function listEmbeddingPresets(): array {
        return \Xberg\XbergApi::listEmbeddingPresets(); // delegate to native extension class
    }
    /**
     * Rerank a list of documents by relevance to a query.
     *
     * Returns documents sorted descending by score. Applies `top_k` truncation if
     * configured.
     *
     * @param string $query
     * @param array<string> $documents
     * @param RerankerConfig $config
     * @return array<RerankedDocument>
     * @throws \Xberg\XbergException
     */
    public static function rerank(
string $query, array $documents, RerankerConfig $config): array
    {
        return \Xberg\XbergApi::rerank($query, $documents, $config); // delegate to native extension class
    }
    /**
     * Get a reranker preset by name.
     *
     * Returns `None` if no preset with the given name exists. Returns an owned
     * clone so the value is safe to pass across FFI boundaries.
     *
     * Since v5.0.0.
     *
     * @param string $name
     * @return ?RerankerPreset
     */
    public static function getRerankerPreset(
string $name): ?RerankerPreset
    {
        return \Xberg\XbergApi::getRerankerPreset($name); // delegate to native extension class
    }
    /**
     * List the names of all available reranker presets.
     *
     * Returns owned `String`s so the values are safe to pass across FFI boundaries.
     *
     * Since v5.0.0.
     *
     * @return array<string>
     */
    public static function listRerankerPresets(): array {
        return \Xberg\XbergApi::listRerankerPresets(); // delegate to native extension class
    }
    /**
     * registerOcrBackend.
     *
     * @param OcrBackend $backend
     * @return void
     */
    public static function registerOcrBackend(
OcrBackend $backend) : void
    {
        \Xberg\XbergApi::registerOcrBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterOcrBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterOcrBackend(
string $name) : void
    {
        \Xberg\XbergApi::unregisterOcrBackend($name); // delegate to native extension class
    }
    /**
     * clearOcrBackends.
     *
     * @return void
     */
    public static function clearOcrBackends(
) : void
    {
        \Xberg\XbergApi::clearOcrBackends(); // delegate to native extension class
    }
    /**
     * registerPostProcessor.
     *
     * @param PostProcessor $backend
     * @return void
     */
    public static function registerPostProcessor(
PostProcessor $backend) : void
    {
        \Xberg\XbergApi::registerPostProcessor($backend); // delegate to native extension class
    }
    /**
     * unregisterPostProcessor.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterPostProcessor(
string $name) : void
    {
        \Xberg\XbergApi::unregisterPostProcessor($name); // delegate to native extension class
    }
    /**
     * clearPostProcessors.
     *
     * @return void
     */
    public static function clearPostProcessors(
) : void
    {
        \Xberg\XbergApi::clearPostProcessors(); // delegate to native extension class
    }
    /**
     * registerValidator.
     *
     * @param Validator $backend
     * @return void
     */
    public static function registerValidator(
Validator $backend) : void
    {
        \Xberg\XbergApi::registerValidator($backend); // delegate to native extension class
    }
    /**
     * unregisterValidator.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterValidator(
string $name) : void
    {
        \Xberg\XbergApi::unregisterValidator($name); // delegate to native extension class
    }
    /**
     * clearValidators.
     *
     * @return void
     */
    public static function clearValidators(
) : void
    {
        \Xberg\XbergApi::clearValidators(); // delegate to native extension class
    }
    /**
     * registerEmbeddingBackend.
     *
     * @param EmbeddingBackend $backend
     * @return void
     */
    public static function registerEmbeddingBackend(
EmbeddingBackend $backend) : void
    {
        \Xberg\XbergApi::registerEmbeddingBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterEmbeddingBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterEmbeddingBackend(
string $name) : void
    {
        \Xberg\XbergApi::unregisterEmbeddingBackend($name); // delegate to native extension class
    }
    /**
     * clearEmbeddingBackends.
     *
     * @return void
     */
    public static function clearEmbeddingBackends(
) : void
    {
        \Xberg\XbergApi::clearEmbeddingBackends(); // delegate to native extension class
    }
    /**
     * registerRenderer.
     *
     * @param Renderer $backend
     * @return void
     */
    public static function registerRenderer(
Renderer $backend) : void
    {
        \Xberg\XbergApi::registerRenderer($backend); // delegate to native extension class
    }
    /**
     * unregisterRenderer.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterRenderer(
string $name) : void
    {
        \Xberg\XbergApi::unregisterRenderer($name); // delegate to native extension class
    }
    /**
     * clearRenderers.
     *
     * @return void
     */
    public static function clearRenderers(
) : void
    {
        \Xberg\XbergApi::clearRenderers(); // delegate to native extension class
    }
    /**
     * registerRerankerBackend.
     *
     * @param RerankerBackend $backend
     * @return void
     */
    public static function registerRerankerBackend(
RerankerBackend $backend) : void
    {
        \Xberg\XbergApi::registerRerankerBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterRerankerBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterRerankerBackend(
string $name) : void
    {
        \Xberg\XbergApi::unregisterRerankerBackend($name); // delegate to native extension class
    }
    /**
     * clearRerankerBackends.
     *
     * @return void
     */
    public static function clearRerankerBackends(
) : void
    {
        \Xberg\XbergApi::clearRerankerBackends(); // delegate to native extension class
    }
}
