<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
declare(strict_types=1);

namespace Kreuzberg;

final class Kreuzberg
{
    /**
     * Extract content from a byte array.
     *
     * This is the main entry point for in-memory extraction. It performs the following steps:
     * 1. Validate MIME type
     * 2. Handle legacy format conversion if needed
     * 3. Select appropriate extractor from registry
     * 4. Extract content
     * 5. Run post-processing pipeline
     *
     * @param string $content
     * @param string $mime_type
     * @param ExtractionConfig $config
     * @return ExtractionResult
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractBytes(
string $content, string $mime_type, ExtractionConfig $config): ExtractionResult
    {
        return \Kreuzberg\KreuzbergApi::extractBytes($content, $mime_type, $config); // delegate to native extension class
    }
    /**
     * Extract content from a file.
     *
     * This is the main entry point for file-based extraction. It performs the following steps:
     * 1. Check cache for existing result (if caching enabled)
     * 2. Detect or validate MIME type
     * 3. Select appropriate extractor from registry
     * 4. Extract content
     * 5. Run post-processing pipeline
     * 6. Store result in cache (if caching enabled)
     *
     * @param string $path
     * @param ?string $mime_type
     * @param ExtractionConfig $config
     * @return ExtractionResult
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractFile(
string $path, ?string $mime_type, ExtractionConfig $config): ExtractionResult
    {
        return \Kreuzberg\KreuzbergApi::extractFile($path, $mime_type, $config); // delegate to native extension class
    }
    /**
     * Synchronous wrapper for `extract_file`.
     *
     * This is a convenience function that blocks the current thread until extraction completes.
     * For async code, use `extract_file` directly.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating
     * a new runtime per call. Always uses the global runtime to avoid nested runtime issues.
     *
     * This function is only available with the `tokio-runtime` feature. For WASM targets,
     * use a truly synchronous extraction approach instead.
     *
     * @param string $path
     * @param ?string $mime_type
     * @param ExtractionConfig $config
     * @return ExtractionResult
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractFileSync(
string $path, ?string $mime_type, ExtractionConfig $config): ExtractionResult
    {
        return \Kreuzberg\KreuzbergApi::extractFileSync($path, $mime_type, $config); // delegate to native extension class
    }
    /**
     * Synchronous wrapper for `extract_bytes`.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating
     * a new runtime per call.
     *
     * With the `tokio-runtime` feature, this blocks the current thread using the global
     * Tokio runtime. Without it (WASM), this calls a truly synchronous implementation.
     *
     * @param string $content
     * @param string $mime_type
     * @param ExtractionConfig $config
     * @return ExtractionResult
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractBytesSync(
string $content, string $mime_type, ExtractionConfig $config): ExtractionResult
    {
        return \Kreuzberg\KreuzbergApi::extractBytesSync($content, $mime_type, $config); // delegate to native extension class
    }
    /**
     * Synchronous wrapper for `batch_extract_files`.
     *
     * Uses the global Tokio runtime for optimal performance.
     * Only available with `tokio-runtime` (WASM has no filesystem).
     *
     * @param array<BatchFileItem> $items
     * @param ExtractionConfig $config
     * @return array<ExtractionResult>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function batchExtractFilesSync(
array $items, ExtractionConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::batchExtractFilesSync($items, $config); // delegate to native extension class
    }
    /**
     * Synchronous wrapper for `batch_extract_bytes`.
     *
     * Uses the global Tokio runtime for optimal performance.
     * With the `tokio-runtime` feature, this blocks the current thread using the global
     * Tokio runtime. Without it (WASM), this calls a truly synchronous implementation
     * that iterates through items and calls `extract_bytes_sync()`.
     *
     * @param array<BatchBytesItem> $items
     * @param ExtractionConfig $config
     * @return array<ExtractionResult>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function batchExtractBytesSync(
array $items, ExtractionConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::batchExtractBytesSync($items, $config); // delegate to native extension class
    }
    /**
     * Extract content from multiple files concurrently.
     *
     * This function processes multiple files in parallel, automatically managing
     * concurrency to prevent resource exhaustion. The concurrency limit can be
     * configured via `ExtractionConfig::max_concurrent_extractions` or defaults
     * to `(num_cpus * 1.5).ceil()`.
     *
     * Each file can optionally specify a [`FileExtractionConfig`] that overrides specific
     * fields from the batch-level `config`. Pass `None` for a file to use the batch defaults.
     * Batch-level settings like `max_concurrent_extractions` and `use_cache` are always
     * taken from the batch-level `config`.
     *
     * @param array<BatchFileItem> $items
     * @param ExtractionConfig $config
     * @return array<ExtractionResult>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function batchExtractFiles(
array $items, ExtractionConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::batchExtractFiles($items, $config); // delegate to native extension class
    }
    /**
     * Extract content from multiple byte arrays concurrently.
     *
     * This function processes multiple byte arrays in parallel, automatically managing
     * concurrency to prevent resource exhaustion. The concurrency limit can be
     * configured via `ExtractionConfig::max_concurrent_extractions` or defaults
     * to `(num_cpus * 1.5).ceil()`.
     *
     * Each item can optionally specify a [`FileExtractionConfig`] that overrides specific
     * fields from the batch-level `config`. Pass `None` as the config to use
     * the batch-level defaults for that item.
     *
     * @param array<BatchBytesItem> $items
     * @param ExtractionConfig $config
     * @return array<ExtractionResult>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function batchExtractBytes(
array $items, ExtractionConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::batchExtractBytes($items, $config); // delegate to native extension class
    }
    /**
     * Detect MIME type from raw file bytes.
     *
     * Uses magic byte signatures to detect file type from content.
     * Falls back to `infer` crate for comprehensive detection.
     *
     * For ZIP-based files, inspects contents to distinguish Office Open XML
     * formats (DOCX, XLSX, PPTX) from plain ZIP archives.
     *
     * @param string $content
     * @return string
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function detectMimeTypeFromBytes(
string $content): string
    {
        return \Kreuzberg\KreuzbergApi::detectMimeTypeFromBytes($content); // delegate to native extension class
    }
    /**
     * Get file extensions for a given MIME type.
     *
     * Returns all known file extensions that map to the specified MIME type.
     *
     * @param string $mime_type
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function getExtensionsForMime(
string $mime_type): array
    {
        return \Kreuzberg\KreuzbergApi::getExtensionsForMime($mime_type); // delegate to native extension class
    }
    /**
     * List all supported document formats.
     *
     * Returns every file extension Kreuzberg recognizes together with its
     * corresponding MIME type, derived from the central format registry.
     * Formats that have no registered file extension (such as source code,
     * which is detected dynamically) are not included.
     *
     * The list is sorted alphabetically by file extension.
     *
     * @return array<SupportedFormat>
     */
    public static function listSupportedFormats(): array {
        return \Kreuzberg\KreuzbergApi::listSupportedFormats(); // delegate to native extension class
    }
    /**
     * Detect QR codes in the bytes of an `ExtractedImage`.
     *
     * `format_hint` is currently unused — the `image` crate auto-detects the
     * container format from magic bytes — but the parameter is retained so future
     * backends (e.g. a WebP-via-`webp-decoder` variant) can use it without an API
     * break.
     *
     * Returns an empty vector on any of:
     *
     * - Empty input.
     * - Image-decode failure.
     * - No QR grids detected.
     * - All detected grids fail to decode.
     *
     * Successfully decoded QR codes carry their payload, a confidence of `1.0`
     * (rqrr does not expose per-grid confidence; a successful decode is treated
     * as high-confidence by convention), and the pixel-space bounding box derived
     * from the four corner points of the grid.
     *
     * @param string $image_bytes
     * @param ?string $_format_hint
     * @return array<QrCode>
     */
    public static function detectQrCodes(
string $image_bytes, ?string $_format_hint = null): array
    {
        return \Kreuzberg\KreuzbergApi::detectQrCodes($image_bytes, $_format_hint); // delegate to native extension class
    }
    /**
     * List the names of all registered embedding backends.
     *
     * Used by `kreuzberg-cli`, the api/mcp endpoints, and generated language
     * bindings.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listEmbeddingBackends(): array {
        return \Kreuzberg\KreuzbergApi::listEmbeddingBackends(); // delegate to native extension class
    }
    /**
     * List names of all registered document extractors.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listDocumentExtractors(): array {
        return \Kreuzberg\KreuzbergApi::listDocumentExtractors(); // delegate to native extension class
    }
    /**
     * List all registered OCR backends.
     *
     * Returns the names of all OCR backends currently registered in the global registry.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listOcrBackends(): array {
        return \Kreuzberg\KreuzbergApi::listOcrBackends(); // delegate to native extension class
    }
    /**
     * Register every built-in post-processor enabled by the active feature set.
     *
     * This is the single entry point that callers (including
     * `register_default_post_processors`) use to populate the global
     * post-processor registry with the in-tree built-ins. Each submodule's own
     * `register` function is gated by its feature flag so this aggregate stays
     * safe to call on any target.
     *
     * @return void
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function registerBuiltin(): void {
        \Kreuzberg\KreuzbergApi::registerBuiltin(); // delegate to native extension class
    }
    /**
     * List all registered post-processor names.
     *
     * Returns a vector of all post-processor names currently registered in the
     * global registry.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listPostProcessors(): array {
        return \Kreuzberg\KreuzbergApi::listPostProcessors(); // delegate to native extension class
    }
    /**
     * List names of all registered renderers.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listRenderers(): array {
        return \Kreuzberg\KreuzbergApi::listRenderers(); // delegate to native extension class
    }
    /**
     * List the names of all registered reranker backends.
     *
     * Used by `kreuzberg-cli`, the api/mcp endpoints, and generated language
     * bindings.
     *
     * Since v5.0.0.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listRerankerBackends(): array {
        return \Kreuzberg\KreuzbergApi::listRerankerBackends(); // delegate to native extension class
    }
    /**
     * List names of all registered validators.
     *
     * @return array<string>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function listValidators(): array {
        return \Kreuzberg\KreuzbergApi::listValidators(); // delegate to native extension class
    }
    /**
     * Run page classification against an extraction result.
     *
     * Mutates `result.page_classifications` with one entry per non-empty page and
     * appends every LLM call's usage to `result.llm_usage`.
     *
     * @param ExtractionResult $result
     * @param PageClassificationConfig $config
     * @return void
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function classifyPages(
ExtractionResult $result, PageClassificationConfig $config): void
    {
        \Kreuzberg\KreuzbergApi::classifyPages($result, $config); // delegate to native extension class
    }
    /**
     * Classify a single piece of text without requiring an `ExtractionResult`.
     *
     * Use this when the caller already has plain text (e.g. a RAG ingest pipeline
     * receiving documents off a queue) and wants a label list back without
     * manufacturing extractor-side metadata.
     *
     * @param string $text
     * @param PageClassificationConfig $config
     * @return array<ClassificationLabel>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function classifyText(
string $text, PageClassificationConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::classifyText($text, $config); // delegate to native extension class
    }
    /**
     * Eagerly download a NER model into the kreuzberg cache.
     *
     * `name` is a HuggingFace repo id (e.g. `urchade/gliner_multi-v2.1`). The
     * CLI flag `kreuzberg warm --ner` delegates here.
     *
     * @param string $name
     * @param ?string $cache_dir
     * @return string
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function downloadModel(
string $name, ?string $cache_dir = null): string
    {
        return \Kreuzberg\KreuzbergApi::downloadModel($name, $cache_dir); // delegate to native extension class
    }
    /**
     * Pinned default NER model identifier.
     *
     * @return string
     */
    public static function defaultModelName(): string {
        return \Kreuzberg\KreuzbergApi::defaultModelName(); // delegate to native extension class
    }
    /**
     * All NER models kreuzberg knows about (used by `--all-ner-models`).
     *
     * @return array<string>
     */
    public static function knownModels(): array {
        return \Kreuzberg\KreuzbergApi::knownModels(); // delegate to native extension class
    }
    /**
     * Run pattern redaction (and optional NER-driven redaction) over `result` and
     * rewrite every textual field. Populates `result.redaction_report`.
     *
     * @param ExtractionResult $result
     * @param RedactionConfig $config
     * @return void
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function redact(
ExtractionResult $result, RedactionConfig $config): void
    {
        \Kreuzberg\KreuzbergApi::redact($result, $config); // delegate to native extension class
    }
    /**
     * Find all US Social Security Number spans in `text` (format: NNN-NN-NNNN).
     *
     * @param string $text
     * @return array<PatternMatch>
     */
    public static function findAll(
string $text): array
    {
        return \Kreuzberg\KreuzbergApi::findAll($text); // delegate to native extension class
    }
    /**
     * Scan `text` for every PII category in `categories` and return all matches
     * in source-byte order.
     *
     * When `categories` is empty every supported regex-detectable category fires.
     * Person / Organization / Location are *not* covered by the pattern engine —
     * they must be supplied by a NER backend through the redaction engine.
     *
     * @param string $text
     * @param array<PiiCategory> $categories
     * @return array<PatternMatch>
     */
    public static function scanText(
string $text, array $categories): array
    {
        return \Kreuzberg\KreuzbergApi::scanText($text, $categories); // delegate to native extension class
    }
    /**
     * Score and return the top-N sentences from `text`, joined in original order.
     *
     * `language` is an ISO 639 (or locale) code used to pick a stopword list;
     * pass `None` (or an unknown code) to fall back to English.
     * `max_tokens` bounds the summary length by whitespace-separated tokens;
     * `None` falls back to [`DEFAULT_MAX_TOKENS`].
     *
     * @param string $text
     * @param ?string $language
     * @param ?int $max_tokens
     * @return ?string
     */
    public static function summarize(
string $text, ?string $language = null, ?int $max_tokens = null): ?string
    {
        return \Kreuzberg\KreuzbergApi::summarize($text, $language, $max_tokens); // delegate to native extension class
    }
    /**
     * Count whitespace-separated tokens (used for token-budget bookkeeping by
     * callers).
     *
     * @param string $text
     * @return int
     */
    public static function tokenCount(
string $text): int
    {
        return \Kreuzberg\KreuzbergApi::tokenCount($text); // delegate to native extension class
    }
    /**
     * Translate the extraction result in place.
     *
     * Populates `result.translation` with the translated `content`, optionally the
     * translated `formatted_content` (when `preserve_markup = true`), and rewrites
     * every chunk's `content` field. Every LLM call's usage is appended to
     * `result.llm_usage`.
     *
     * @param ExtractionResult $result
     * @param TranslationConfig $config
     * @return void
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function translateResult(
ExtractionResult $result, TranslationConfig $config): void
    {
        \Kreuzberg\KreuzbergApi::translateResult($result, $config); // delegate to native extension class
    }
    /**
     * Compare two extraction results and return a structured diff.
     *
     * The comparison is purely structural — no I/O, no side effects. All fields
     * of [`ExtractionDiff`] are populated according to the provided [`DiffOptions`].
     *
     * @param ExtractionResult $a
     * @param ExtractionResult $b
     * @param DiffOptions $opts
     * @return ExtractionDiff
     */
    public static function compare(
ExtractionResult $a, ExtractionResult $b, DiffOptions $opts): ExtractionDiff
    {
        return \Kreuzberg\KreuzbergApi::compare($a, $b, $opts); // delegate to native extension class
    }
    /**
     * Extract content from a pre-cropped image region using a VLM.
     *
     * The caller is responsible for cropping the page image to the region's bounding
     * box before calling this function. The `image_bytes` parameter must contain the
     * raw bytes of the **cropped** region image (JPEG, PNG, WebP, etc.).
     *
     * @param string $image_bytes
     * @param string $image_mime
     * @param RegionKind $region_kind
     * @param LlmConfig $llm_config
     * @param ?string $custom_prompt
     * @return string
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractRegionWithVlm(
string $image_bytes, string $image_mime, RegionKind $region_kind, LlmConfig $llm_config, ?string $custom_prompt = null): string
    {
        return \Kreuzberg\KreuzbergApi::extractRegionWithVlm($image_bytes, $image_mime, $region_kind, $llm_config, $custom_prompt); // delegate to native extension class
    }
    /**
     * Extract keywords from text using the specified algorithm.
     *
     * This is the unified entry point for keyword extraction. The algorithm
     * used is determined by `config.algorithm`.
     *
     * @param string $text
     * @param KeywordConfig $config
     * @return array<Keyword>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function extractKeywords(
string $text, KeywordConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::extractKeywords($text, $config); // delegate to native extension class
    }
    /**
     * Render a single PDF page to PNG bytes.
     *
     * Returns raw PNG-encoded bytes for the specified page at the given DPI.
     * Uses pdf_oxide with tiny-skia for pure-Rust rendering.
     *
     * For pages with extreme dimensions (very wide vector diagrams, etc.) the
     * effective DPI may be automatically reduced to avoid rasterizer failure.
     * A warning is logged when this happens.
     *
     * @param string $pdf_bytes
     * @param int $page_index
     * @param ?int $dpi
     * @param ?string $password
     * @return string
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function renderPdfPageToPng(
string $pdf_bytes, int $page_index, ?int $dpi = null, ?string $password = null): string
    {
        return \Kreuzberg\KreuzbergApi::renderPdfPageToPng($pdf_bytes, $page_index, $dpi, $password); // delegate to native extension class
    }
    /**
     * Detect the MIME type of a file at the given path.
     *
     * Uses the file extension and optionally the file content to determine the MIME type.
     * Set `check_exists` to `true` to verify the file exists before detection.
     *
     * @param string $path
     * @param bool $check_exists
     * @return string
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function detectMimeType(
string $path, bool $check_exists): string
    {
        return \Kreuzberg\KreuzbergApi::detectMimeType($path, $check_exists); // delegate to native extension class
    }
    /**
     * Embed a list of texts using the configured embedding model.
     *
     * Returns a 2D vector where each inner vector is the embedding for the corresponding text.
     *
     * @param array<string> $texts
     * @param EmbeddingConfig $config
     * @return array<array<float>>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function embedTexts(
array $texts, EmbeddingConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::embedTexts($texts, $config); // delegate to native extension class
    }
    /**
     * embedTextsAsync.
     *
     * @param array<string> $_texts
     * @param EmbeddingConfig $_config
     * @return array<array<float>>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function embedTextsAsync(
array $_texts, EmbeddingConfig $_config): array
    {
        return \Kreuzberg\KreuzbergApi::embedTextsAsync($_texts, $_config); // delegate to native extension class
    }
    /**
     * Get an embedding preset by name.
     *
     * Returns `None` if no preset with the given name exists. Returns an owned
     * clone so the value is safe to pass across FFI boundaries.
     *
     * @param string $name
     * @return ?EmbeddingPreset
     */
    public static function getEmbeddingPreset(
string $name): ?EmbeddingPreset
    {
        return \Kreuzberg\KreuzbergApi::getEmbeddingPreset($name); // delegate to native extension class
    }
    /**
     * List the names of all available embedding presets.
     *
     * Returns owned `String`s so the values are safe to pass across FFI boundaries.
     *
     * @return array<string>
     */
    public static function listEmbeddingPresets(): array {
        return \Kreuzberg\KreuzbergApi::listEmbeddingPresets(); // delegate to native extension class
    }
    /**
     * Rerank a list of documents by relevance to a query.
     *
     * Returns documents sorted descending by score. Applies `top_k` truncation if
     * configured.
     *
     * @param string $query
     * @param array<string> $documents
     * @param RerankerConfig $config
     * @return array<RerankedDocument>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function rerank(
string $query, array $documents, RerankerConfig $config): array
    {
        return \Kreuzberg\KreuzbergApi::rerank($query, $documents, $config); // delegate to native extension class
    }
    /**
     * Stub for builds without the `reranker` feature.
     *
     * Since v5.0.0.
     *
     * @param string $_query
     * @param array<string> $_documents
     * @param RerankerConfig $_config
     * @return array<RerankedDocument>
     * @throws \Kreuzberg\KreuzbergException
     */
    public static function rerankAsync(
string $_query, array $_documents, RerankerConfig $_config): array
    {
        return \Kreuzberg\KreuzbergApi::rerankAsync($_query, $_documents, $_config); // delegate to native extension class
    }
    /**
     * Get a reranker preset by name.
     *
     * Returns `None` if no preset with the given name exists. Returns an owned
     * clone so the value is safe to pass across FFI boundaries.
     *
     * Since v5.0.0.
     *
     * @param string $name
     * @return ?RerankerPreset
     */
    public static function getRerankerPreset(
string $name): ?RerankerPreset
    {
        return \Kreuzberg\KreuzbergApi::getRerankerPreset($name); // delegate to native extension class
    }
    /**
     * List the names of all available reranker presets.
     *
     * Returns owned `String`s so the values are safe to pass across FFI boundaries.
     *
     * Since v5.0.0.
     *
     * @return array<string>
     */
    public static function listRerankerPresets(): array {
        return \Kreuzberg\KreuzbergApi::listRerankerPresets(); // delegate to native extension class
    }
    /**
     * registerOcrBackend.
     *
     * @param OcrBackend $backend
     * @return void
     */
    public static function registerOcrBackend(
OcrBackend $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerOcrBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterOcrBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterOcrBackend(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterOcrBackend($name); // delegate to native extension class
    }
    /**
     * clearOcrBackends.
     *
     * @return void
     */
    public static function clearOcrBackends(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearOcrBackends(); // delegate to native extension class
    }
    /**
     * registerPostProcessor.
     *
     * @param PostProcessor $backend
     * @return void
     */
    public static function registerPostProcessor(
PostProcessor $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerPostProcessor($backend); // delegate to native extension class
    }
    /**
     * unregisterPostProcessor.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterPostProcessor(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterPostProcessor($name); // delegate to native extension class
    }
    /**
     * clearPostProcessors.
     *
     * @return void
     */
    public static function clearPostProcessors(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearPostProcessors(); // delegate to native extension class
    }
    /**
     * registerValidator.
     *
     * @param Validator $backend
     * @return void
     */
    public static function registerValidator(
Validator $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerValidator($backend); // delegate to native extension class
    }
    /**
     * unregisterValidator.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterValidator(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterValidator($name); // delegate to native extension class
    }
    /**
     * clearValidators.
     *
     * @return void
     */
    public static function clearValidators(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearValidators(); // delegate to native extension class
    }
    /**
     * registerEmbeddingBackend.
     *
     * @param EmbeddingBackend $backend
     * @return void
     */
    public static function registerEmbeddingBackend(
EmbeddingBackend $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerEmbeddingBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterEmbeddingBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterEmbeddingBackend(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterEmbeddingBackend($name); // delegate to native extension class
    }
    /**
     * clearEmbeddingBackends.
     *
     * @return void
     */
    public static function clearEmbeddingBackends(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearEmbeddingBackends(); // delegate to native extension class
    }
    /**
     * registerDocumentExtractor.
     *
     * @param DocumentExtractor $backend
     * @return void
     */
    public static function registerDocumentExtractor(
DocumentExtractor $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerDocumentExtractor($backend); // delegate to native extension class
    }
    /**
     * unregisterDocumentExtractor.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterDocumentExtractor(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterDocumentExtractor($name); // delegate to native extension class
    }
    /**
     * clearDocumentExtractors.
     *
     * @return void
     */
    public static function clearDocumentExtractors(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearDocumentExtractors(); // delegate to native extension class
    }
    /**
     * registerRenderer.
     *
     * @param Renderer $backend
     * @return void
     */
    public static function registerRenderer(
Renderer $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerRenderer($backend); // delegate to native extension class
    }
    /**
     * unregisterRenderer.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterRenderer(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterRenderer($name); // delegate to native extension class
    }
    /**
     * clearRenderers.
     *
     * @return void
     */
    public static function clearRenderers(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearRenderers(); // delegate to native extension class
    }
    /**
     * registerRerankerBackend.
     *
     * @param RerankerBackend $backend
     * @return void
     */
    public static function registerRerankerBackend(
RerankerBackend $backend) : void
    {
        \Kreuzberg\KreuzbergApi::registerRerankerBackend($backend); // delegate to native extension class
    }
    /**
     * unregisterRerankerBackend.
     *
     * @param string $name
     * @return void
     */
    public static function unregisterRerankerBackend(
string $name) : void
    {
        \Kreuzberg\KreuzbergApi::unregisterRerankerBackend($name); // delegate to native extension class
    }
    /**
     * clearRerankerBackends.
     *
     * @return void
     */
    public static function clearRerankerBackends(
) : void
    {
        \Kreuzberg\KreuzbergApi::clearRerankerBackends(); // delegate to native extension class
    }
}
