<?php

// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
declare(strict_types=1);

namespace Xberg;

/**
 * Wraps `Gliner2Candle` behind the `NerBackend` trait.
 *
 * `Gliner2Candle` holds candle tensors which are not `Send`, so we wrap it
 * in a `Mutex` to satisfy the `Send + Sync` requirement of `NerBackend`.
 */
final class CandleBackend
{
    /**
     * @param array<EntityCategory> $categories
     * @return array<Entity>
     */
    public function detect(string $text, array $categories): array
    {
        throw new \RuntimeException('Not implemented — provided by the native extension.');
    }
    /**
     * Load from a local model directory. Applies `lora_adapter_dir` if provided.
     */
    public static function fromLocal(string $model_dir, ?string $lora_adapter_dir = null): CandleBackend
    {
        throw new \RuntimeException('Not implemented — provided by the native extension.');
    }
    /**
     * Load from in-memory model bytes (no filesystem access — required on `wasm32`,
     */
    public static function fromBytes(string $safetensors, string $tokenizer_json, string $encoder_config_json): CandleBackend
    {
        throw new \RuntimeException('Not implemented — provided by the native extension.');
    }
}
