// Generated by alef. Do not edit by hand.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// swift-format-ignore-file
// This file contains generated FFI glue for plugin Box classes.

import Foundation
import RustBridge

/// Opaque box class retained by Rust via `Unmanaged<SwiftRerankerBackendBox>.passRetained`.
/// Wraps `any SwiftRerankerBackendBridge` and exposes `alef_*` FFI shim methods.
/// swift-bridge @_cdecl shims call these methods directly.
public final class SwiftRerankerBackendBox {
    private let bridge: any SwiftRerankerBackendBridge
    public init(_ bridge: any SwiftRerankerBackendBridge) { self.bridge = bridge }
    // MARK: Plugin super-trait shims

    public func alef_name() -> RustString {
        return RustString(bridge.name)
    }

    public func alef_version() -> RustString {
        return RustString(bridge.version())
    }

    public func alef_initialize() -> String {
        do {
            try bridge.initialize()
            return encodeOkVoidEnvelope()
        } catch { return encodeErrEnvelope("\(error)") }
    }

    public func alef_shutdown() -> String {
        do {
            try bridge.shutdown()
            return encodeOkVoidEnvelope()
        } catch { return encodeErrEnvelope("\(error)") }
    }

    // MARK: Trait-specific shims
    public func alef_rerank(query: RustString, documents: RustVec<RustString>) -> String {
        var documents_list: [String] = []
let documents_count = documents.len()
var documents_idx: UInt = 0
while documents_idx < documents_count {
    documents_list.append(documents.get(index: documents_idx)!.as_str().toString())
    documents_idx += 1
}
        do {
          let result = try bridge.rerank(query: query.toString(), documents: documents_list)
          return encodeOkEnvelope(result)
        } catch { return encodeErrEnvelope("\(error)") }
    }
}
