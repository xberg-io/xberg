# Generated by alef. Do not edit by hand.
# alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
[package]
name = "xberg-swift"
version = "1.0.0-rc.1"
edition = "2024"
license = "MIT"

# `ahash`, `async-trait`, `libc`, `serde`, `serde_json`, and `tokio` are all
# conditionally referenced by alef-emitted code: `ahash` only when the
# umbrella crate exposes `AHashMap<Cow<str>, _>` parameters (the conditional
# `__*_ahash` shim rebuilds), `async-trait` and `tokio` only when the API
# surface includes async streaming adapters and runtime spawn, `libc` only
# when service API C callback functions are emitted, `serde` and
# `serde_json` only when JSON DTO conversions are emitted. They are listed
# unconditionally in `[dependencies]` so the manifest is stable across
# regens, and ignored here so cargo-machete does not flag downstream crates
# whose API surface does not trigger those paths as unused.
[package.metadata.cargo-machete]
ignored = ["ahash", "async-trait", "libc", "serde", "serde_json", "tokio"]

[lib]
crate-type = ["cdylib", "staticlib"]
# The `extern "Swift"` block emits linker references that are only resolvable
# when the crate is linked into a Swift target. `cargo test --workspace` on
# pure-Rust runners (e.g. windows-latest) would otherwise fail with
# undefined `__swift_bridge__$*$alef_visit_*` symbols.
test = false
doctest = false
bench = false

[features]
default = ["api", "api-types", "auto-rotate-types", "captioning", "chunking", "chunking-tokenizers", "classification", "diff", "embedding-presets", "embeddings", "enrichment", "heuristics", "html", "keywords-rake", "keywords-yake", "layout-types", "liter-llm", "markdown-footnotes", "ner-llm", "ocr", "ocr-wasm", "office", "paddle-ocr-types", "pdf", "presets", "quality", "redaction", "reranker", "reranker-presets", "structured", "svg", "tokio-runtime", "transcription-types", "tree-sitter", "url-ingestion", "xml"]
api = ["xberg/api"]
api-types = ["xberg/api-types"]
auto-rotate-types = ["xberg/auto-rotate-types"]
captioning = ["xberg/captioning"]
chunking = ["xberg/chunking"]
chunking-tokenizers = ["xberg/chunking-tokenizers"]
classification = ["xberg/classification"]
diff = ["xberg/diff"]
embedding-presets = ["xberg/embedding-presets"]
embeddings = ["xberg/embeddings"]
enrichment = ["xberg/enrichment"]
heic = ["xberg/heic"]
heuristics = ["xberg/heuristics"]
html = ["xberg/html"]
keywords-rake = ["xberg/keywords-rake"]
keywords-yake = ["xberg/keywords-yake"]
layout-types = ["xberg/layout-types"]
liter-llm = ["xberg/liter-llm"]
markdown-footnotes = ["xberg/markdown-footnotes"]
ner-llm = ["xberg/ner-llm"]
ocr = ["xberg/ocr"]
ocr-wasm = ["xberg/ocr-wasm"]
office = ["xberg/office"]
paddle-ocr-types = ["xberg/paddle-ocr-types"]
pdf = ["xberg/pdf"]
presets = ["xberg/presets"]
quality = ["xberg/quality"]
redaction = ["xberg/redaction"]
reranker = ["xberg/reranker"]
reranker-presets = ["xberg/reranker-presets"]
structured = ["xberg/structured"]
svg = ["xberg/svg"]
tokio-runtime = ["xberg/tokio-runtime"]
transcription-types = ["xberg/transcription-types"]
tree-sitter = ["xberg/tree-sitter"]
url-ingestion = ["xberg/url-ingestion"]
xml = ["xberg/xml"]

[dependencies]
ahash = "0.8"
async-trait = "0.1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-bridge = "0.1.59"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[target.'cfg(not(any(target_os = "ios", target_os = "android", target_os = "windows")))'.dependencies]
xberg = { version = "1.0.0-rc.1", path = "../../../crates/xberg", features = [
    "pdf",
    "excel",
    "office",
    "hwp",
    "hwpx",
    "iwork",
    "email",
    "html",
    "xml",
    "archives",
    "mdx",
    "svg",
    "language-detection",
    "chunking",
    "chunking-tokenizers",
    "quality",
    "keywords",
    "diff",
    "api",
    "api-types",
    "mcp",
    "ocr",
    "paddle-ocr",
    "paddle-ocr-types",
    "layout-detection",
    "layout-types",
    "embeddings",
    "embedding-presets",
    "reranker",
    "reranker-presets",
    "transcription-types",
    "tree-sitter",
    "liter-llm",
    "tokio-runtime",
    "ner-llm",
    "ner-onnx",
    "redaction",
    "summarization",
    "classification",
    "captioning",
    "qr-codes",
    "auto-rotate-types",
    "stopwords",
    "ocr-wasm",
] }

[target.'cfg(target_os = "ios")'.dependencies]
xberg = { version = "1.0.0-rc.1", path = "../../../crates/xberg", features = ["android-target"], default-features = false }

[target.'cfg(target_os = "android")'.dependencies]
xberg = { version = "1.0.0-rc.1", path = "../../../crates/xberg", features = ["android-target"], default-features = false }

[target.'cfg(target_os = "windows")'.dependencies]
xberg = { version = "1.0.0-rc.1", path = "../../../crates/xberg", features = ["windows-target"], default-features = false }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }

[build-dependencies]
swift-bridge-build = "0.1.59"
