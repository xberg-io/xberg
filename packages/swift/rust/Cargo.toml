# Generated by alef. Do not edit by hand.
# alef:hash:8de16b0b46f51cd658ead6a50d5957ed715c71d82c2bedde98c64a0ef2666b1f
[package]
name = "xberg-swift"
version = "1.1.6"
edition = "2024"
license = "MIT"

# `ahash`, `async-trait`, `libc`, `serde`, `serde_json`, and `tokio` are all
# conditionally referenced by alef-emitted code: `ahash` only when the
# umbrella crate exposes `AHashMap<Cow<str>, _>` parameters (the conditional
# `__*_ahash` shim rebuilds), `async-trait` and `tokio` only when the API
# surface includes async streaming adapters and runtime spawn, `libc` only
# when service API C callback functions are emitted, `serde` and
# `serde_json` only when JSON DTO conversions are emitted. They are listed
# unconditionally in `[dependencies]` so the manifest is stable across
# regens, and ignored here so cargo-machete does not flag downstream crates
# whose API surface does not trigger those paths as unused.
[package.metadata.cargo-machete]
ignored = ["ahash", "async-trait", "libc", "serde", "serde_json", "tokio"]

[lib]
crate-type = ["cdylib", "staticlib"]
# The `extern "Swift"` block emits linker references that are only resolvable
# when the crate is linked into a Swift target. `cargo test --workspace` on
# pure-Rust runners (e.g. windows-latest) would otherwise fail with
# undefined `__swift_bridge__$*$alef_visit_*` symbols.
test = false
doctest = false
bench = false

[features]
default = ["api", "api-types", "auto-rotate-types", "candle-ocr", "chunking-tokenizers", "classification", "diff", "embedding-presets", "heuristics", "html", "keywords-rake", "keywords-yake", "late-interaction", "late-interaction-presets", "layout-types", "markdown-footnotes", "ocr", "ocr-wasm", "office", "paddle-ocr-types", "pdf", "presets", "quality", "redaction", "reranker", "reranker-presets", "sparse-embedding-presets", "svg", "transcription-types", "tree-sitter", "url-config-types", "url-ingestion", "xml"]
api = ["xberg/api"]
api-types = ["xberg/api-types"]
auto-rotate-types = ["xberg/auto-rotate-types"]
candle-ocr = ["xberg/candle-ocr"]
chunking-tokenizers = ["xberg/chunking-tokenizers"]
classification = ["xberg/classification"]
diff = ["xberg/diff"]
embedding-presets = ["xberg/embedding-presets"]
heic = ["xberg/heic"]
heuristics = ["xberg/heuristics"]
html = ["xberg/html"]
keywords-rake = ["xberg/keywords-rake"]
keywords-yake = ["xberg/keywords-yake"]
late-interaction = ["xberg/late-interaction"]
late-interaction-presets = ["xberg/late-interaction-presets"]
layout-types = ["xberg/layout-types"]
markdown-footnotes = ["xberg/markdown-footnotes"]
ocr = ["xberg/ocr"]
ocr-wasm = ["xberg/ocr-wasm"]
office = ["xberg/office"]
paddle-ocr-types = ["xberg/paddle-ocr-types"]
pdf = ["xberg/pdf"]
presets = ["xberg/presets"]
quality = ["xberg/quality"]
redaction = ["xberg/redaction"]
reranker = ["xberg/reranker"]
reranker-presets = ["xberg/reranker-presets"]
sparse-embedding-presets = ["xberg/sparse-embedding-presets"]
svg = ["xberg/svg"]
transcription-types = ["xberg/transcription-types"]
tree-sitter = ["xberg/tree-sitter"]
url-config-types = ["xberg/url-config-types"]
url-ingestion = ["xberg/url-ingestion"]
xml = ["xberg/xml"]

[dependencies]
ahash = "0.8"
async-trait = "0.1"
crawlberg = { default-features = false, version = "1.5.2" }
html-to-markdown-rs = { default-features = false, version = "3.12" }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-bridge = "0.1.59"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[target.'cfg(all(target_os = "macos", target_arch = "x86_64"))'.dependencies]
xberg = { version = "1.1.6", path = "../../../crates/xberg", features = ["macos-intel-target", "simd-utf8"], default-features = false }

[target.'cfg(any(target_os = "ios", target_os = "android"))'.dependencies]
xberg-ffi = { version = "1.1.6", path = "../../../crates/xberg-ffi", default-features = false, features = ["android-target"] }

[target.'cfg(not(any(target_os = "ios", target_os = "android")))'.dependencies]
xberg-ffi = { version = "1.1.6", path = "../../../crates/xberg-ffi", default-features = false, features = ["full-no-heic"] }

[target.'cfg(not(any(target_os = "ios", target_os = "android", target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))))'.dependencies]
xberg = { version = "1.1.6", path = "../../../crates/xberg", features = [
    "api",
    "api-types",
    "archives",
    "auto-rotate-types",
    "captioning",
    "chunking",
    "chunking-tokenizers",
    "classification",
    "diff",
    "email",
    "embedding-presets",
    "embeddings",
    "excel",
    "html",
    "hwp",
    "hwpx",
    "iwork",
    "keywords",
    "language-detection",
    "late-interaction",
    "late-interaction-presets",
    "layout-detection",
    "layout-types",
    "liter-llm",
    "mcp",
    "mdx",
    "ner-llm",
    "ner-onnx",
    "ocr",
    "ocr-wasm",
    "office",
    "paddle-ocr",
    "paddle-ocr-types",
    "pdf",
    "qr-codes",
    "quality",
    "redaction",
    "reranker",
    "reranker-presets",
    "sparse-embedding-presets",
    "sparse-embeddings",
    "stopwords",
    "summarization",
    "svg",
    "tokio-runtime",
    "transcription-types",
    "tree-sitter",
    "wordperfect",
    "xml",
] }

[target.'cfg(target_os = "android")'.dependencies]
xberg = { version = "1.1.6", path = "../../../crates/xberg", features = ["android-target", "simd-utf8"], default-features = false }

[target.'cfg(target_os = "ios")'.dependencies]
xberg = { version = "1.1.6", path = "../../../crates/xberg", features = ["android-target", "simd-utf8"], default-features = false }

[target.'cfg(target_os = "windows")'.dependencies]
xberg = { version = "1.1.6", path = "../../../crates/xberg", features = ["windows-target", "simd-utf8"], default-features = false }

[build-dependencies]
swift-bridge-build = "0.1.59"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }

[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
