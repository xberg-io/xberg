# Generated by alef. Do not edit by hand.
# alef:hash:0912795d137244d47ca46093261df585cc70f719f15f01c0a00970e77e266da2
[package]
name = "kreuzberg-swift"
version = "5.0.0-rc.33"
edition = "2024"
license = "Elastic-2.0"

# `ahash`, `async-trait`, `libc`, `serde`, `serde_json`, and `tokio` are all
# conditionally referenced by alef-emitted code: `ahash` only when the
# umbrella crate exposes `AHashMap<Cow<str>, _>` parameters (the conditional
# `__*_ahash` shim rebuilds), `async-trait` and `tokio` only when the API
# surface includes async streaming adapters and runtime spawn, `libc` only
# when service API C callback functions are emitted, `serde` and
# `serde_json` only when JSON DTO conversions are emitted. They are listed
# unconditionally in `[dependencies]` so the manifest is stable across
# regens, and ignored here so cargo-machete does not flag downstream crates
# whose API surface does not trigger those paths as unused.
[package.metadata.cargo-machete]
ignored = ["ahash", "async-trait", "libc", "serde", "serde_json", "tokio"]

[lib]
crate-type = ["cdylib", "staticlib"]
# The `extern "Swift"` block emits linker references that are only resolvable
# when the crate is linked into a Swift target. `cargo test --workspace` on
# pure-Rust runners (e.g. windows-latest) would otherwise fail with
# undefined `__swift_bridge__$*$alef_visit_*` symbols.
test = false
doctest = false
bench = false

[features]
default = ["api", "api-types", "auto-rotate-types", "captioning", "chunking", "chunking-tokenizers", "classification", "diff", "embedding-presets", "embeddings", "enrichment", "heuristics", "html", "keywords-rake", "keywords-yake", "layout-types", "liter-llm", "markdown-footnotes", "ner-llm", "ocr", "ocr-wasm", "office", "paddle-ocr-types", "pdf", "presets", "quality", "redaction", "reranker", "reranker-presets", "structured", "svg", "tokio-runtime", "transcription-types", "tree-sitter", "xml"]
api = ["kreuzberg/api"]
api-types = ["kreuzberg/api-types"]
auto-rotate-types = ["kreuzberg/auto-rotate-types"]
captioning = ["kreuzberg/captioning"]
chunking = ["kreuzberg/chunking"]
chunking-tokenizers = ["kreuzberg/chunking-tokenizers"]
classification = ["kreuzberg/classification"]
diff = ["kreuzberg/diff"]
embedding-presets = ["kreuzberg/embedding-presets"]
embeddings = ["kreuzberg/embeddings"]
enrichment = ["kreuzberg/enrichment"]
heic = ["kreuzberg/heic"]
heuristics = ["kreuzberg/heuristics"]
html = ["kreuzberg/html"]
keywords-rake = ["kreuzberg/keywords-rake"]
keywords-yake = ["kreuzberg/keywords-yake"]
layout-types = ["kreuzberg/layout-types"]
liter-llm = ["kreuzberg/liter-llm"]
markdown-footnotes = ["kreuzberg/markdown-footnotes"]
ner-llm = ["kreuzberg/ner-llm"]
ocr = ["kreuzberg/ocr"]
ocr-wasm = ["kreuzberg/ocr-wasm"]
office = ["kreuzberg/office"]
paddle-ocr-types = ["kreuzberg/paddle-ocr-types"]
pdf = ["kreuzberg/pdf"]
presets = ["kreuzberg/presets"]
quality = ["kreuzberg/quality"]
redaction = ["kreuzberg/redaction"]
reranker = ["kreuzberg/reranker"]
reranker-presets = ["kreuzberg/reranker-presets"]
structured = ["kreuzberg/structured"]
svg = ["kreuzberg/svg"]
tokio-runtime = ["kreuzberg/tokio-runtime"]
transcription-types = ["kreuzberg/transcription-types"]
tree-sitter = ["kreuzberg/tree-sitter"]
xml = ["kreuzberg/xml"]

[dependencies]
ahash = "0.8"
async-trait = "0.1"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-bridge = "0.1.59"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[target.'cfg(not(any(target_os = "ios", target_os = "android", target_os = "windows")))'.dependencies]
kreuzberg = { version = "5.0.0-rc.33", path = "../../../crates/kreuzberg", features = [
    "pdf",
    "excel",
    "office",
    "hwp",
    "hwpx",
    "iwork",
    "email",
    "html",
    "xml",
    "archives",
    "mdx",
    "svg",
    "language-detection",
    "chunking",
    "chunking-tokenizers",
    "quality",
    "keywords",
    "diff",
    "api",
    "api-types",
    "mcp",
    "ocr",
    "paddle-ocr",
    "paddle-ocr-types",
    "layout-detection",
    "layout-types",
    "embeddings",
    "embedding-presets",
    "reranker",
    "reranker-presets",
    "transcription-types",
    "tree-sitter",
    "liter-llm",
    "tokio-runtime",
    "ner-llm",
    "ner-onnx",
    "redaction",
    "summarization",
    "classification",
    "captioning",
    "qr-codes",
    "auto-rotate-types",
    "stopwords",
    "ocr-wasm",
] }

[target.'cfg(target_os = "ios")'.dependencies]
kreuzberg = { version = "5.0.0-rc.33", path = "../../../crates/kreuzberg", features = ["android-target"], default-features = false }

[target.'cfg(target_os = "android")'.dependencies]
kreuzberg = { version = "5.0.0-rc.33", path = "../../../crates/kreuzberg", features = ["android-target"], default-features = false }

[target.'cfg(target_os = "windows")'.dependencies]
kreuzberg = { version = "5.0.0-rc.33", path = "../../../crates/kreuzberg", features = ["windows-target"], default-features = false }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }

[build-dependencies]
swift-bridge-build = "0.1.59"
