// Generated by alef. Do not edit by hand.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg

/**
 * Output format for extraction results.
 *
 * Controls the format of the `content` field in `ExtractedDocument`.
 * When set to `Markdown`, `Djot`, or `Html`, the output uses that format.
 * `Plain` returns the raw extracted text.
 * `Structured` returns JSON with full OCR element data including bounding
 * boxes and confidence scores.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = OutputFormatDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = OutputFormatSerializer::class)
sealed class OutputFormat {
    /** Plain text content only (default) */
    object Plain : OutputFormat()
    /** Markdown format */
    object Markdown : OutputFormat()
    /** Djot markup format */
    object Djot : OutputFormat()
    /** HTML format */
    object Html : OutputFormat()
    /** JSON tree format with heading-driven sections. */
    object Json : OutputFormat()
    /** Structured JSON format with full OCR element metadata. */
    object Structured : OutputFormat()
    /**
     * Custom renderer registered via the RendererRegistry.
     * The string is the renderer name (e.g., "docx", "latex").
     */
    data class Custom(val value: String) : OutputFormat()
}

private class OutputFormatDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<OutputFormat>(OutputFormat::class.java) {
    @Suppress("LongMethod", "CyclomaticComplexMethod", "ReturnCount")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): OutputFormat {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.JsonNode>(parser)
        if (node.isTextual) {
            return when (node.asText()) {
                "plain" -> OutputFormat.Plain
                "markdown" -> OutputFormat.Markdown
                "djot" -> OutputFormat.Djot
                "html" -> OutputFormat.Html
                "json" -> OutputFormat.Json
                "structured" -> OutputFormat.Structured
                else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                    parser, "Unknown OutputFormat unit variant", node.asText(), OutputFormat::class.java,
                )
            }
        }
        if (node.isObject) {
            val obj = node as com.fasterxml.jackson.databind.node.ObjectNode
            val it = obj.fields()
            if (it.hasNext()) {
                val entry = it.next()
                if (!it.hasNext()) {
                    val payload = entry.value
                    return when (entry.key) {
                        "custom" -> OutputFormat.Custom(ctx.readTreeAsValue<String>(payload, String::class.java))
                        else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                            parser, "Unknown OutputFormat data variant", entry.key, OutputFormat::class.java,
                        )
                    }
                }
            }
        }
        throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
            parser, "Cannot deserialize OutputFormat: expected string or single-field object", null, OutputFormat::class.java,
        )
    }
}

private class OutputFormatSerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<OutputFormat>(OutputFormat::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: OutputFormat,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        when (value) {
            is OutputFormat.Plain -> gen.writeString("plain")
            is OutputFormat.Markdown -> gen.writeString("markdown")
            is OutputFormat.Djot -> gen.writeString("djot")
            is OutputFormat.Html -> gen.writeString("html")
            is OutputFormat.Json -> gen.writeString("json")
            is OutputFormat.Structured -> gen.writeString("structured")
            is OutputFormat.Custom -> {
                gen.writeStartObject()
                gen.writeFieldName("custom")
                mapper.writeValue(gen, value.value)
                gen.writeEndObject()
            }
        }
    }
}
