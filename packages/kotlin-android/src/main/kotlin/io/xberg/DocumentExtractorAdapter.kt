// Generated by alef. Do not edit by hand.
// alef:hash:9b6c82fdc18c3106bdec2c926b44f0262b7dcc51189a52c181c04410d16a2fd9

package io.xberg

/// Bridge adapter for IDocumentExtractor implementations.
///
/// Wraps a user-supplied IDocumentExtractor instance and delegates all method calls.
/// This adapter is used internally by the trait bridge registration layer to ensure
/// proper handling of impl instances through JNI boundaries.
internal class DocumentExtractorAdapter(private val impl: IDocumentExtractor) : IDocumentExtractor {
    // Delegate all interface methods to the wrapped implementation
    override fun name(): String = impl.name()

    override fun version(): String = impl.version()

    override fun initialize() = impl.initialize()

    override fun shutdown() = impl.shutdown()

    override fun description(): String = impl.description()

    override fun author(): String = impl.author()

    override suspend fun extract(input: ExtractInput, config: ExtractionConfig): ExtractedDocument {
        return impl.extract(input, config)
    }

    override fun supportedMimeTypes(): List<String> {
        return impl.supportedMimeTypes()
    }

    override fun priority(): Int {
        return impl.priority()
    }

    override fun canHandle(path: String, mimeType: String): Boolean {
        return impl.canHandle(path, mimeType)
    }


}
