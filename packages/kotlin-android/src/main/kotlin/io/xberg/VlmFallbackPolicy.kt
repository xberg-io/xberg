// Generated by alef. Do not edit by hand.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg

/**
 * Policy controlling when VLM (Vision Language Model) OCR is used as a fallback.
 *
 * This knob is syntactic sugar over the explicit `OcrPipelineConfig` stage
 * ordering. When `vlm_fallback` is set and `pipeline` is `null`, an equivalent
 * pipeline is synthesised at extraction time:
 *
 * - `VlmFallbackPolicy.Disabled` — no synthesis; single-backend mode (default).
 * - `VlmFallbackPolicy.OnLowQuality` — tries the classical backend first; if the
 *   result scores below `quality_threshold`, tries VLM.
 *
 * - `VlmFallbackPolicy.Always` — skips the classical backend and sends every page
 *   to the VLM.
 *
 * When `OcrConfig.pipeline` is explicitly set, `vlm_fallback` is ignored — the
 * explicit pipeline takes precedence.
 *
 * **Errors:**
 *
 * Both `OnLowQuality` and `Always` require `OcrConfig.vlm_config` to be `Some`.
 * Constructing an `OcrConfig` with one of these policies but no `vlm_config` is
 * detected by `OcrConfig.validate` and will surface as a
 * `Validation` error at extraction time, not a panic.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = VlmFallbackPolicyDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = VlmFallbackPolicySerializer::class)
sealed class VlmFallbackPolicy {
    /** No VLM fallback (default). Behaves identically to the pre-policy single-backend mode. */
    object Disabled : VlmFallbackPolicy()
    /**
     * Try the classical OCR backend first. If the quality score is below
     * `quality_threshold`, send the page to the VLM.
     *
     * `quality_threshold` is in the `[0.0, 1.0]` range produced by
     * `calculate_quality_score`. A value of `0.5` is a
     * reasonable starting point; calibrate with the Stage 0 benchmark harness.
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class OnLowQuality(
        val qualityThreshold: Double,
    ) : VlmFallbackPolicy()
    /** Skip the classical OCR backend entirely. Every page is sent to the VLM. */
    object Always : VlmFallbackPolicy()
}

private class VlmFallbackPolicyDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<VlmFallbackPolicy>(VlmFallbackPolicy::class.java) {
    @Suppress("LongMethod")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): VlmFallbackPolicy {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.node.ObjectNode>(parser)
        val tag = node.get("mode")?.asText()
        @Suppress("UNCHECKED_CAST")
        val payload = (node.deepCopy() as com.fasterxml.jackson.databind.node.ObjectNode).apply { remove("mode") }
        return when (tag) {
            "disabled" -> VlmFallbackPolicy.Disabled
            "on_low_quality" -> ctx.readTreeAsValue<VlmFallbackPolicy.OnLowQuality>(payload, VlmFallbackPolicy.OnLowQuality::class.java)
            "always" -> VlmFallbackPolicy.Always
            else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                parser, "Unknown VlmFallbackPolicy tag", tag, VlmFallbackPolicy::class.java,
            )
        }
    }
}

private class VlmFallbackPolicySerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<VlmFallbackPolicy>(VlmFallbackPolicy::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: VlmFallbackPolicy,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        val node: com.fasterxml.jackson.databind.node.ObjectNode = when (value) {
            is VlmFallbackPolicy.Disabled -> {
                val n = mapper.createObjectNode()
                n.put("mode", "disabled")
                n
            }
            is VlmFallbackPolicy.OnLowQuality -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as VlmFallbackPolicy.OnLowQuality) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("mode", "on_low_quality")
                n
            }
            is VlmFallbackPolicy.Always -> {
                val n = mapper.createObjectNode()
                n.put("mode", "always")
                n
            }
        }
        mapper.writeTree(gen, node)
    }
}
