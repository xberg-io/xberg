// Generated by alef. Do not edit by hand.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg

/**
 * Target format for re-encoding extracted images.
 *
 * Controls whether and how extracted images are normalised to a uniform
 * container format before being returned in `ExtractionResult.images`.
 * The default (`Native`) preserves the format produced by each extractor
 * without any additional encode pass.
 *
 * Callers that need uniform output — e.g. cloud pipelines that always store
 * WebP thumbnails — set this once on `ImageExtractionConfig.output_format`
 * rather than re-encoding downstream.
 *
 * # Serde shape
 *
 * Uses a tagged enum: `{"type": "native"}`, `{"type": "png"}`,
 * `{"type": "jpeg", "quality": 90}`, etc.
 */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = ImageOutputFormatDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = ImageOutputFormatSerializer::class)
sealed class ImageOutputFormat {
    /**
     * Preserve whatever format the extractor produced (default).
     *
     * No re-encode pass is performed. `ExtractedImage.format` reflects the
     * source format: JPEG for embedded PDF images, PNG for rasterised content,
     * or the native container format from office documents.
     */
    object Native : ImageOutputFormat()
    /** Re-encode all extracted images as PNG (lossless). */
    object Png : ImageOutputFormat()
    /**
     * Re-encode all extracted images as JPEG at the given quality level.
     *
     * `quality` must be in `1..=100`. Values outside this range are clamped
     * and a warning is emitted. Higher values produce larger files with less
     * artefacting; 85 is a reasonable default.
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Jpeg(
        val quality: Byte,
    ) : ImageOutputFormat()
    /**
     * Re-encode all extracted images as WebP at the given quality level.
     *
     * `quality` must be in `1..=100`. Values outside this range are clamped
     * and a warning is emitted. 80 is a reasonable default.
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Webp(
        val quality: Byte,
    ) : ImageOutputFormat()
    /**
     * Re-encode all extracted images as HEIF/HEIC at the given quality level.
     *
     * Requires the `heic` feature. `quality` must be in `1..=100`. Values
     * outside this range are clamped and a warning is emitted. 80 is a
     * reasonable default.
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Heif(
        val quality: Byte,
    ) : ImageOutputFormat()
    /**
     * Output pure-vector SVG. Lossless. Raster sources are not re-encoded
     * (a warning is emitted and the image bytes are left untouched).
     *
     * When the source is already SVG, the bytes are passed through the
     * `usvg` sanitizer (strips external hrefs, JS event handlers, and
     * `foreignObject` elements) when `SvgOptions.sanitize` is `true`.
     *
     * Requires the `svg` feature.
     */
    object Svg : ImageOutputFormat()
}

private class ImageOutputFormatDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<ImageOutputFormat>(ImageOutputFormat::class.java) {
    @Suppress("LongMethod")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): ImageOutputFormat {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.node.ObjectNode>(parser)
        val tag = node.get("type")?.asText()
        @Suppress("UNCHECKED_CAST")
        val payload = (node.deepCopy() as com.fasterxml.jackson.databind.node.ObjectNode).apply { remove("type") }
        return when (tag) {
            "native" -> ImageOutputFormat.Native
            "png" -> ImageOutputFormat.Png
            "jpeg" -> ctx.readTreeAsValue<ImageOutputFormat.Jpeg>(payload, ImageOutputFormat.Jpeg::class.java)
            "webp" -> ctx.readTreeAsValue<ImageOutputFormat.Webp>(payload, ImageOutputFormat.Webp::class.java)
            "heif" -> ctx.readTreeAsValue<ImageOutputFormat.Heif>(payload, ImageOutputFormat.Heif::class.java)
            "svg" -> ImageOutputFormat.Svg
            else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                parser, "Unknown ImageOutputFormat tag", tag, ImageOutputFormat::class.java,
            )
        }
    }
}

private class ImageOutputFormatSerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<ImageOutputFormat>(ImageOutputFormat::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: ImageOutputFormat,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        val node: com.fasterxml.jackson.databind.node.ObjectNode = when (value) {
            is ImageOutputFormat.Native -> {
                val n = mapper.createObjectNode()
                n.put("type", "native")
                n
            }
            is ImageOutputFormat.Png -> {
                val n = mapper.createObjectNode()
                n.put("type", "png")
                n
            }
            is ImageOutputFormat.Jpeg -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as ImageOutputFormat.Jpeg) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("type", "jpeg")
                n
            }
            is ImageOutputFormat.Webp -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as ImageOutputFormat.Webp) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("type", "webp")
                n
            }
            is ImageOutputFormat.Heif -> {
                @Suppress("UNCHECKED_CAST")
                val n = mapper.valueToTree<com.fasterxml.jackson.databind.node.ObjectNode>(value as ImageOutputFormat.Heif) as com.fasterxml.jackson.databind.node.ObjectNode
                n.put("type", "heif")
                n
            }
            is ImageOutputFormat.Svg -> {
                val n = mapper.createObjectNode()
                n.put("type", "svg")
                n
            }
        }
        mapper.writeTree(gen, node)
    }
}
