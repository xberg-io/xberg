// Generated by alef. Do not edit by hand.
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg

/** Outcome of a single validator on a single candidate match. */
@com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = ValidationResultDeserializer::class)
@com.fasterxml.jackson.databind.annotation.JsonSerialize(using = ValidationResultSerializer::class)
sealed class ValidationResult {
    /** Pass-through, no change. */
    object Accept : ValidationResult()
    /**
     * Drop the candidate. `reason` is a static identifier used as a
     * `RejectionCounts` key (e.g. `"iban_checksum_failed"`).
     */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = com.fasterxml.jackson.databind.JsonDeserializer.None::class)
    @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.JsonSerializer.None::class)
    data class Reject(
        val reason: String,
    ) : ValidationResult()
}

private class ValidationResultDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<ValidationResult>(ValidationResult::class.java) {
    @Suppress("LongMethod", "CyclomaticComplexMethod", "ReturnCount")
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): ValidationResult {
        val node = parser.codec.readTree<com.fasterxml.jackson.databind.JsonNode>(parser)
        if (node.isTextual) {
            return when (node.asText()) {
                "Accept" -> ValidationResult.Accept
                else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                    parser, "Unknown ValidationResult unit variant", node.asText(), ValidationResult::class.java,
                )
            }
        }
        if (node.isObject) {
            val obj = node as com.fasterxml.jackson.databind.node.ObjectNode
            val it = obj.fields()
            if (it.hasNext()) {
                val entry = it.next()
                if (!it.hasNext()) {
                    val payload = entry.value
                    return when (entry.key) {
                        "Reject" -> ctx.readTreeAsValue<ValidationResult.Reject>(payload, ValidationResult.Reject::class.java)
                        else -> throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
                            parser, "Unknown ValidationResult data variant", entry.key, ValidationResult::class.java,
                        )
                    }
                }
            }
        }
        throw com.fasterxml.jackson.databind.exc.InvalidFormatException(
            parser, "Cannot deserialize ValidationResult: expected string or single-field object", null, ValidationResult::class.java,
        )
    }
}

private class ValidationResultSerializer : com.fasterxml.jackson.databind.ser.std.StdSerializer<ValidationResult>(ValidationResult::class.java) {
    @Suppress("LongMethod")
    override fun serialize(
        value: ValidationResult,
        gen: com.fasterxml.jackson.core.JsonGenerator,
        provider: com.fasterxml.jackson.databind.SerializerProvider,
    ) {
        @Suppress("UNCHECKED_CAST")
        val mapper = (gen.codec as? com.fasterxml.jackson.databind.ObjectMapper) ?: com.fasterxml.jackson.databind.ObjectMapper().findAndRegisterModules()
        when (value) {
            is ValidationResult.Accept -> gen.writeString("Accept")
            is ValidationResult.Reject -> {
                gen.writeStartObject()
                gen.writeFieldName("Reject")
                mapper.writeValue(gen, value as ValidationResult.Reject)
                gen.writeEndObject()
            }
        }
    }
}
