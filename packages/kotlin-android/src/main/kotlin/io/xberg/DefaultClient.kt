// Generated by alef. Do not edit by hand.
@file:Suppress(
    "ktlint:standard:max-line-length",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:annotation",
    "MaxLineLength",
    "TooManyFunctions",
    "LongParameterList",
    "LongMethod",
)

package io.xberg

import com.fasterxml.jackson.core.type.TypeReference
import kotlin.time.Duration
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

@Suppress("TooManyFunctions")
class NoopCache internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    suspend fun get(key: String): ByteArray? {
        return withContext(Dispatchers.IO) { XbergBridge.nativeNoopCacheGet(handle, MAPPER.writeValueAsString(key)) }
    }

    suspend fun put(key: String, value: ByteArray, ttl: Duration? = null): Unit {
        withContext(Dispatchers.IO) { XbergBridge.nativeNoopCachePut(handle, MAPPER.writeValueAsString(mapOf("key" to key, "value" to value, "ttl" to ttl))) }
    }

    override fun close() { XbergBridge.nativeFreeNoopCache(handle) }
}
@Suppress("TooManyFunctions")
class NoopProgressSink internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    fun emit(event: ProgressEvent): Unit {
        XbergBridge.nativeNoopProgressSinkEmit(handle, MAPPER.writeValueAsString(event))
    }

    override fun close() { XbergBridge.nativeFreeNoopProgressSink(handle) }
}
@Suppress("TooManyFunctions")
class DefaultModelProvider internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    suspend fun ensureModel(model: ModelId): String {
        return withContext(Dispatchers.IO) { XbergBridge.nativeDefaultModelProviderEnsureModel(handle, MAPPER.writeValueAsString(model)) }
    }

    override fun close() { XbergBridge.nativeFreeDefaultModelProvider(handle) }
}
@Suppress("TooManyFunctions")
class ParsedDocument internal constructor(internal val handle: Long) : AutoCloseable {
    // The detected MIME type.
    fun mimeType(): String {
        return XbergBridge.nativeParsedDocumentMimeType(handle)
    }

    // The page count (1 for non-PDF inputs).
    fun pageCount(): Int {
        return XbergBridge.nativeParsedDocumentPageCount(handle)
    }

    // The shared source bytes.
    fun source(): ByteArray {
        return XbergBridge.nativeParsedDocumentSource(handle)
    }

    override fun close() { XbergBridge.nativeFreeParsedDocument(handle) }
}
@Suppress("TooManyFunctions")
class Engine internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Extract content from a single bytes or URI input.
    suspend fun extract(input: ExtractInput, config: ExtractionConfig): ExtractionResult {
        return withContext(Dispatchers.IO) {
            val responseJson = XbergBridge.nativeEngineExtract(handle, MAPPER.writeValueAsString(mapOf("input" to input, "config" to config)))
            MAPPER.readValue(responseJson, ExtractionResult::class.java)
        }
    }

    // Extract content from multiple bytes or URI inputs.
    suspend fun extractBatch(inputs: List<ExtractInput>, config: ExtractionConfig): ExtractionResult {
        return withContext(Dispatchers.IO) {
            val responseJson = XbergBridge.nativeEngineExtractBatch(handle, MAPPER.writeValueAsString(mapOf("inputs" to inputs, "config" to config)))
            MAPPER.readValue(responseJson, ExtractionResult::class.java)
        }
    }

    // The injected [`CacheBackend`] seam (default: [`NoopCache`]).
    fun cacheBackend(): CacheBackend {
        val responseJson = XbergBridge.nativeEngineCacheBackend(handle)
        return MAPPER.readValue(responseJson, CacheBackend::class.java)
    }

    // The injected [`ProgressSink`] seam (default: [`NoopProgressSink`]).
    fun progressSink(): ProgressSink {
        val responseJson = XbergBridge.nativeEngineProgressSink(handle)
        return MAPPER.readValue(responseJson, ProgressSink::class.java)
    }

    // The injected [`ModelProvider`] seam (default: [`DefaultModelProvider`]).
    fun modelProvider(): ModelProvider {
        val responseJson = XbergBridge.nativeEngineModelProvider(handle)
        return MAPPER.readValue(responseJson, ModelProvider::class.java)
    }

    override fun close() { XbergBridge.nativeFreeEngine(handle) }
}
@Suppress("TooManyFunctions")
class EngineBuilder internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Inject a [`CacheBackend`], overriding the [`NoopCache`] default.
    fun withCacheBackend(cache: CacheBackend): EngineBuilder {
        val handle = XbergBridge.nativeEngineBuilderWithCacheBackend(handle, MAPPER.writeValueAsString(cache))
        return EngineBuilder(handle)
    }

    // Inject a [`ProgressSink`], overriding the [`NoopProgressSink`] default.
    fun withProgressSink(progress: ProgressSink): EngineBuilder {
        val handle = XbergBridge.nativeEngineBuilderWithProgressSink(handle, MAPPER.writeValueAsString(progress))
        return EngineBuilder(handle)
    }

    // Inject a [`ModelProvider`], overriding the [`DefaultModelProvider`] default.
    fun withModelProvider(provider: ModelProvider): EngineBuilder {
        val handle = XbergBridge.nativeEngineBuilderWithModelProvider(handle, MAPPER.writeValueAsString(provider))
        return EngineBuilder(handle)
    }

    // Finalize the builder into an [`Engine`], filling every unset seam with
    // its in-core default.
    fun build(): Engine {
        val handle = XbergBridge.nativeEngineBuilderBuild(handle)
        return Engine(handle)
    }

    override fun close() { XbergBridge.nativeFreeEngineBuilder(handle) }
}
@Suppress("TooManyFunctions")
class CandleBackend internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    suspend fun detect(text: String, categories: List<EntityCategory>): List<Entity> {
        return withContext(Dispatchers.IO) {
            val responseJson = XbergBridge.nativeCandleBackendDetect(handle, MAPPER.writeValueAsString(mapOf("text" to text, "categories" to categories)))
            MAPPER.readValue(responseJson, object : TypeReference<List<Entity>>() {})
        }
    }

    override fun close() { XbergBridge.nativeFreeCandleBackend(handle) }
}
@Suppress("TooManyFunctions")
class MetaSchema internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Validate `raw` against the meta-schema and deserialize into a [`Preset`],
    // stamping the fingerprint over the canonical file bytes.
    fun parsePreset(path: String, raw: ByteArray): Preset {
        val responseJson = XbergBridge.nativeMetaSchemaParsePreset(handle, MAPPER.writeValueAsString(mapOf("path" to path, "raw" to raw)))
        return MAPPER.readValue(responseJson, Preset::class.java)
    }

    override fun close() { XbergBridge.nativeFreeMetaSchema(handle) }
}
@Suppress("TooManyFunctions")
class Registry internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Look up a preset by its identifier.
    fun get(id: String): Preset? {
        val responseJson = XbergBridge.nativeRegistryGet(handle, MAPPER.writeValueAsString(id))
        return MAPPER.readValue(responseJson, Preset::class.java)
    }

    // Materialize a [`PresetSummary`] list for the public registry endpoint.
    fun summaries(): List<PresetSummary> {
        val responseJson = XbergBridge.nativeRegistrySummaries(handle)
        return MAPPER.readValue(responseJson, object : TypeReference<List<PresetSummary>>() {})
    }

    // Number of presets currently loaded.
    fun len(): Long {
        return XbergBridge.nativeRegistryLen(handle)
    }

    // Whether the registry contains zero presets.
    fun isEmpty(): Boolean {
        return XbergBridge.nativeRegistryIsEmpty(handle)
    }

    // Read raw sample bytes for `<preset_id>` from
    // `library/<id>/samples/<name>`. Returns `None` when the file is absent.
    fun sampleBytes(presetId: String, name: String): ByteArray? {
        return XbergBridge.nativeRegistrySampleBytes(handle, MAPPER.writeValueAsString(mapOf("presetId" to presetId, "name" to name)))
    }

    // Load additional preset files from a runtime directory and insert them
    // into this registry.
    //
    // Reads every `*.json` file directly under `dir` (non-recursive),
    // validates each against the meta-schema, and inserts it. Files that fail
    // validation are rejected — the error is returned immediately and the
    // registry is left in a partially-updated state. Existing entries with the
    // same id are overwritten.
    //
    // Returns the number of presets successfully loaded from `dir`.
    //
    // # Use case
    //
    // This is the injection point for downstream catalogs that add curated
    // presets on top of the single embedded OSS preset.
    fun extendFromDir(dir: String): Long {
        return XbergBridge.nativeRegistryExtendFromDir(handle, MAPPER.writeValueAsString(dir))
    }

    override fun close() { XbergBridge.nativeFreeRegistry(handle) }
}
