# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
"""Configuration options for the conversion API."""

from __future__ import annotations

from dataclasses import dataclass, field
from enum import Enum
from typing import TYPE_CHECKING, Any, TypedDict

from ._xberg import (
    AssetCategory,  # noqa: TC001
    AuthConfig,  # noqa: TC001
    BrowserBackend,  # noqa: TC001
    BrowserMode,  # noqa: TC001
    BrowserWait,  # noqa: TC001
    CallMode,  # noqa: TC001
    ChunkSizing,  # noqa: TC001
    ChunkerType,  # noqa: TC001
    CodeContentMode,  # noqa: TC001
    DiffLine,  # noqa: TC001
    EmbeddingModelType,  # noqa: TC001
    EntityCategory,  # noqa: TC001
    ExecutionProviderType,  # noqa: TC001
    ExtractInputKind,  # noqa: TC001
    ExtractionMethod,  # noqa: TC001
    FormatMetadata,  # noqa: TC001
    HtmlTheme,  # noqa: TC001
    ImageKind,  # noqa: TC001
    ImageOutputFormat,  # noqa: TC001
    JupyterCellRendering,  # noqa: TC001
    KeywordAlgorithm,  # noqa: TC001
    LateInteractionModelType,  # noqa: TC001
    MergeMode,  # noqa: TC001
    NerBackendKind,  # noqa: TC001
    OcrBoundingGeometry,  # noqa: TC001
    OcrElementLevel,  # noqa: TC001
    OutputFormat,  # noqa: TC001
    PiiCategory,  # noqa: TC001
    RedactionStrategy,  # noqa: TC001
    ReductionLevel,  # noqa: TC001
    RerankerHead,  # noqa: TC001
    RerankerModelType,  # noqa: TC001
    ResultFormat,  # noqa: TC001
    SchemaCompliance,  # noqa: TC001
    SparseEmbeddingModelType,  # noqa: TC001
    SummaryStrategy,  # noqa: TC001
    TableChunkingMode,  # noqa: TC001
    TableModel,  # noqa: TC001
    TableOverlapPreference,  # noqa: TC001
    TextDirection,  # noqa: TC001
    UrlExtractionMode,  # noqa: TC001
    VlmFallbackPolicy,  # noqa: TC001
    WhisperModel,  # noqa: TC001
)

if TYPE_CHECKING:
    from ._xberg import (
        ArchiveEntry,  # noqa: F401
        CaptioningConfig,  # noqa: F401
        CellChange,  # noqa: F401
        Chunk,  # noqa: F401
        CodeChunkInfo,  # noqa: F401
        ContributorRole,  # noqa: F401
        DbfFieldInfo,  # noqa: F401
        DiffHunk,  # noqa: F401
        DjotContent,  # noqa: F401
        DocumentNode,  # noqa: F401
        DocumentRelationship,  # noqa: F401
        DocumentRevision,  # noqa: F401
        DocumentSummary,  # noqa: F401
        Element,  # noqa: F401
        EmbeddedDiff,  # noqa: F401
        Entity,  # noqa: F401
        ErrorMetadata,  # noqa: F401
        ExtractedDocument,  # noqa: F401
        ExtractedUri,  # noqa: F401
        ExtractionConfidence,  # noqa: F401
        ExtractionErrorItem,  # noqa: F401
        Formula,  # noqa: F401
        GridCell,  # noqa: F401
        HeaderMetadata,  # noqa: F401
        ImageMetadataType,  # noqa: F401
        ImagePreprocessingMetadata,  # noqa: F401
        Keyword,  # noqa: F401
        LinkMetadata,  # noqa: F401
        OcrPipelineConfig,  # noqa: F401
        OcrRotation,  # noqa: F401
        OcrTable,  # noqa: F401
        PageClassification,  # noqa: F401
        PageClassificationConfig,  # noqa: F401
        PageContent,  # noqa: F401
        PageRange,  # noqa: F401
        PageStructure,  # noqa: F401
        PdfAnnotation,  # noqa: F401
        PdfFormField,  # noqa: F401
        ProcessingWarning,  # noqa: F401
        PropertyChange,  # noqa: F401
        QrCode,  # noqa: F401
        RedactionPattern,  # noqa: F401
        RedactionReport,  # noqa: F401
        RedactionTerm,  # noqa: F401
        StructuredData,  # noqa: F401
        StructuredExtractionConfig,  # noqa: F401
        TableDiff,  # noqa: F401
        Translation,  # noqa: F401
        TranslationConfig,  # noqa: F401
        YearRange,  # noqa: F401
    )


class ListType(str, Enum):
    """Type of list detection."""

    bullet = "bullet"

    numbered = "numbered"

    lettered = "lettered"

    indented = "indented"


class OcrBackendType(str, Enum):
    """OCR backend types."""

    tesseract = "tesseract"

    paddle_ocr = "paddle_ocr"

    candle = "candle"

    custom = "custom"


class ProcessingStage(str, Enum):
    """Processing stages for post-processors."""

    early = "early"

    middle = "middle"

    late = "late"


class PdfAnnotationType(str, Enum):
    """Type of PDF annotation."""

    text = "text"

    highlight = "highlight"

    link = "link"

    stamp = "stamp"

    underline = "underline"

    strike_out = "strike_out"

    other = "other"


class BlockType(str, Enum):
    """Types of block-level elements in Djot."""

    paragraph = "paragraph"

    heading = "heading"

    blockquote = "blockquote"

    code_block = "code_block"

    list_item = "list_item"

    ordered_list = "ordered_list"

    bullet_list = "bullet_list"

    task_list = "task_list"

    definition_list = "definition_list"

    definition_term = "definition_term"

    definition_description = "definition_description"

    div = "div"

    section = "section"

    thematic_break = "thematic_break"

    raw_block = "raw_block"

    math_display = "math_display"


class InlineType(str, Enum):
    """Types of inline elements in Djot."""

    text = "text"

    strong = "strong"

    emphasis = "emphasis"

    highlight = "highlight"

    subscript = "subscript"

    superscript = "superscript"

    insert = "insert"

    delete = "delete"

    code = "code"

    link = "link"

    image = "image"

    span = "span"

    math = "math"

    raw_inline = "raw_inline"

    footnote_ref = "footnote_ref"

    symbol = "symbol"


class RelationshipKind(str, Enum):
    """Semantic kind of a relationship between document elements."""

    footnote_reference = "footnote_reference"

    citation_reference = "citation_reference"

    internal_link = "internal_link"

    caption = "caption"

    label = "label"

    toc_entry = "toc_entry"

    cross_reference = "cross_reference"


class ContentLayer(str, Enum):
    """Content layer classification for document nodes."""

    body = "body"

    header = "header"

    footer = "footer"

    footnote = "footnote"


class ChunkType(str, Enum):
    """Semantic structural classification of a text chunk."""

    heading = "heading"

    party_list = "party_list"

    definitions = "definitions"

    operative_clause = "operative_clause"

    signature_block = "signature_block"

    schedule = "schedule"

    table_like = "table_like"

    formula = "formula"

    code_block = "code_block"

    function = "function"

    class_ = "class"

    module = "module"

    image = "image"

    org_chart = "org_chart"

    diagram = "diagram"

    unknown = "unknown"


class ElementType(str, Enum):
    """Semantic element type classification."""

    title_ = "title"

    narrative_text = "narrative_text"

    heading = "heading"

    list_item = "list_item"

    table = "table"

    image = "image"

    page_break = "page_break"

    code_block = "code_block"

    block_quote = "block_quote"

    footer = "footer"

    header = "header"


class FormFieldType(str, Enum):
    """Kind of a PDF form field."""

    text = "text"

    checkbox = "checkbox"

    radio = "radio"

    choice = "choice"

    signature = "signature"

    button = "button"

    unknown = "unknown"


class LinkType(str, Enum):
    """Link type classification."""

    anchor = "anchor"

    internal = "internal"

    external = "external"

    email = "email"

    phone = "phone"

    other = "other"


class ImageType(str, Enum):
    """Image type classification."""

    data_uri = "data-uri"

    inline_svg = "inline-svg"

    external = "external"

    relative = "relative"


class StructuredDataType(str, Enum):
    """Structured data type classification."""

    json_ld = "json-ld"

    microdata = "microdata"

    rd_fa = "rdfa"


class PageUnitType(str, Enum):
    """Type of paginated unit in a document."""

    page = "page"

    slide = "slide"

    sheet = "sheet"


class RevisionKind(str, Enum):
    """Semantic classification of a tracked change."""

    insertion = "insertion"

    deletion = "deletion"

    format_change = "format_change"

    comment = "comment"


class UriKind(str, Enum):
    """Semantic classification of an extracted URI."""

    hyperlink = "hyperlink"

    image = "image"

    anchor = "anchor"

    citation = "citation"

    reference = "reference"

    email = "email"


class RegionKind(str, Enum):
    """Classification of a detected layout region that warrants VLM extraction."""

    figure = "figure"

    dense_table = "dense_table"

    complex_layout = "complex_layout"

    caption = "caption"


class EmbeddingsEmbeddingBackend(str, Enum):
    """Inference backend that an [`EmbeddingPreset`] runs on."""

    onnx = "onnx"

    static = "static"


class BoundaryReason(str, Enum):
    """Reason for boundary detection."""

    start = "start"

    page_one_marker = "page_one_marker"

    letterhead_reset = "letterhead_reset"

    density_shift = "density_shift"

    end = "end"


class PresetCategory(str, Enum):
    """High-level category used to group presets in the registry UI."""

    finance = "finance"

    identity = "identity"

    legal = "legal"

    logistics = "logistics"

    medical = "medical"

    hr = "hr"

    other = "other"


class PSMMode(str, Enum):
    """Page Segmentation Mode for Tesseract OCR."""

    osd_only = "osd_only"

    auto_osd = "auto_osd"

    auto_only = "auto_only"

    auto = "auto"

    single_column = "single_column"

    single_block_vertical = "single_block_vertical"

    single_block = "single_block"

    single_line = "single_line"

    single_word = "single_word"

    circle_word = "circle_word"

    single_char = "single_char"


class PaddleLanguage(str, Enum):
    """Supported languages in PaddleOCR."""

    english = "english"

    chinese = "chinese"

    japanese = "japanese"

    korean = "korean"

    german = "german"

    french = "french"

    latin = "latin"

    cyrillic = "cyrillic"

    traditional_chinese = "traditional_chinese"

    thai = "thai"

    greek = "greek"

    east_slavic = "east_slavic"

    arabic = "arabic"

    devanagari = "devanagari"

    tamil = "tamil"

    telugu = "telugu"


class LayoutClass(str, Enum):
    """The 18 canonical document layout classes."""

    caption = "caption"

    chart = "chart"

    footnote = "footnote"

    formula = "formula"

    list_item = "list_item"

    page_footer = "page_footer"

    page_header = "page_header"

    picture = "picture"

    section_header = "section_header"

    table = "table"

    text = "text"

    title_ = "title"

    document_index = "document_index"

    code = "code"

    checkbox_selected = "checkbox_selected"

    checkbox_unselected = "checkbox_unselected"

    form = "form"

    key_value_region = "key_value_region"


@dataclass(frozen=True, slots=True)
class AccelerationConfig:
    """Hardware acceleration configuration for ONNX Runtime models."""

    provider: ExecutionProviderType | str = "auto"

    """Execution provider to use for ONNX inference."""

    device_id: int = 0

    """GPU device ID (for CUDA/TensorRT). Ignored for CPU/CoreML/Auto."""


@dataclass(frozen=True, slots=True)
class ContentFilterConfig:
    """Cross-extractor content filtering configuration."""

    include_headers: bool = False

    """Include running headers in extraction output."""

    include_footers: bool = False

    """Include running footers in extraction output."""

    strip_repeating_text: bool = True

    """Enable the heuristic cross-page repeating text detector."""

    include_watermarks: bool = False

    """Include watermark text in extraction output."""


@dataclass(frozen=True, slots=True)
class EmailConfig:
    """Configuration for email extraction."""

    msg_fallback_codepage: int | None = None

    """Windows codepage number to use when an MSG file contains no codepage property. Defaults to `None`, which falls back to windows-1252."""


class ExtractionConfig(TypedDict, total=False):
    """Main extraction configuration."""

    use_cache: bool

    """Enable caching of extraction results"""
    enable_quality_processing: bool

    """Enable quality post-processing"""
    ocr: OcrConfig | None

    """OCR configuration (None = OCR disabled)"""
    force_ocr: bool

    """Force OCR even for searchable PDFs"""
    force_ocr_pages: list[int] | None

    """Force OCR on specific pages only (1-indexed page numbers, must be >= 1)."""
    disable_ocr: bool

    """Disable OCR entirely, even for images."""
    chunking: ChunkingConfig | None

    """Text chunking configuration (None = chunking disabled)"""
    content_filter: ContentFilterConfig | None

    """Content filtering configuration (None = use extractor defaults)."""
    images: ImageExtractionConfig | None

    """Image extraction configuration (None = no image extraction)"""
    pdf_options: PdfConfig | None

    """PDF-specific options (None = use defaults)"""
    token_reduction: TokenReductionOptions | None

    """Token reduction configuration (None = no token reduction)"""
    language_detection: LanguageDetectionConfig | None

    """Language detection configuration (None = no language detection)"""
    pages: PageConfig | None

    """Page extraction configuration (None = no page tracking)"""
    keywords: KeywordConfig | None

    """Keyword extraction configuration (None = no keyword extraction)"""
    postprocessor: PostProcessorConfig | None

    """Post-processor configuration (None = use defaults)"""
    html_output: HtmlOutputConfig | None

    """Styled HTML output configuration."""
    extraction_timeout_secs: int | None

    """Default per-file timeout in seconds for batch extraction."""
    max_concurrent_extractions: int | None

    """Maximum concurrent extractions in batch operations (None = (num_cpus x 1.5).ceil())."""
    result_format: ResultFormat | str

    """Result structure format"""
    security_limits: SecurityLimits | None

    """Security limits for archive extraction."""
    max_embedded_file_bytes: int | None

    """Maximum uncompressed size in bytes for a single embedded file before recursive extraction is attempted (default: 50 MiB)."""
    output_format: OutputFormat | str

    """Content text format (default: Plain)."""
    jupyter_cell_rendering: JupyterCellRendering | str

    """Controls how Jupyter notebook (`.ipynb`) code cells are rendered."""
    layout: LayoutDetectionConfig | None

    """Layout detection configuration (None = layout detection disabled)."""
    transcription: TranscriptionConfig | None

    """Transcription (speech-to-text) configuration for audio/video files."""
    use_layout_for_markdown: bool

    """Run layout detection on the non-OCR PDF markdown path."""
    include_document_structure: bool

    """Enable structured document tree output."""
    acceleration: AccelerationConfig | None

    """Hardware acceleration configuration for ONNX Runtime models."""
    cache_namespace: str | None

    """Cache namespace for tenant isolation."""
    cache_ttl_secs: int | None

    """Per-request cache TTL in seconds."""
    email: EmailConfig | None

    """Email extraction configuration (None = use defaults)."""
    url: UrlExtractionConfig

    """URL ingestion and crawl configuration."""
    max_archive_depth: int

    """Maximum recursion depth for archive extraction (default: 3). Set to 0 to disable recursive extraction (legacy behavior)."""
    tree_sitter: TreeSitterConfig | None

    """Tree-sitter language pack configuration (None = tree-sitter disabled)."""
    structured_extraction: StructuredExtractionConfig | None

    """Structured extraction via LLM (None = disabled)."""
    ner: NerConfig | None

    """Named-entity recognition configuration. When set, the NER post-processor runs at the Middle stage and populates `ExtractedDocument::entities`."""
    redaction: RedactionConfig | None

    """Redaction / anonymisation configuration. When set, the redaction post-processor runs at the Late stage and rewrites every textual field in `ExtractedDocument`, emitting an audit trail in `ExtractedDocument::redaction_report`."""
    summarization: SummarizationConfig | None

    """Summarisation configuration. When set, the summarisation post-processor runs at the Middle stage and populates `ExtractedDocument::summary`."""
    translation: TranslationConfig | None

    """Translation configuration. When set, the translation post-processor runs at the Middle stage and populates `ExtractedDocument::translation`."""
    page_classification: PageClassificationConfig | None

    """Per-page classification configuration. When set, the classification post-processor runs at the Middle stage and populates `ExtractedDocument::page_classifications`."""
    captioning: CaptioningConfig | None

    """VLM captioning configuration for extracted images. When set, the captioning post-processor runs at the Middle stage and writes a caption into each `ExtractedImage::caption`."""
    qr_codes: bool | None

    """Enable QR-code detection in extracted images. When `true`, the QR post-processor runs at the Middle stage and populates `ExtractedImage::qr_codes`."""


@dataclass(frozen=True, slots=True)
class FileExtractionConfig:
    """Per-file extraction configuration overrides for batch processing."""

    enable_quality_processing: bool | None = None

    """Override quality post-processing for this file."""

    ocr: OcrConfig | None = None

    """Override OCR configuration for this file (None in the Option = use batch default)."""

    force_ocr: bool | None = None

    """Override force OCR for this file."""

    force_ocr_pages: list[int] | None = None

    """Override force OCR pages for this file (1-indexed page numbers)."""

    disable_ocr: bool | None = None

    """Override disable OCR for this file."""

    chunking: ChunkingConfig | None = None

    """Override chunking configuration for this file."""

    content_filter: ContentFilterConfig | None = None

    """Override content filtering configuration for this file."""

    images: ImageExtractionConfig | None = None

    """Override image extraction configuration for this file."""

    pdf_options: PdfConfig | None = None

    """Override PDF options for this file."""

    token_reduction: TokenReductionOptions | None = None

    """Override token reduction for this file."""

    language_detection: LanguageDetectionConfig | None = None

    """Override language detection for this file."""

    pages: PageConfig | None = None

    """Override page extraction for this file."""

    keywords: KeywordConfig | None = None

    """Override keyword extraction for this file."""

    postprocessor: PostProcessorConfig | None = None

    """Override post-processor for this file."""

    html_output: HtmlOutputConfig | None = None

    """Override styled HTML output configuration for this file."""

    result_format: ResultFormat | str | None = None

    """Override result format for this file."""

    output_format: OutputFormat | str | None = None

    """Override output content format for this file."""

    include_document_structure: bool | None = None

    """Override document structure output for this file."""

    layout: LayoutDetectionConfig | None = None

    """Override layout detection for this file."""

    transcription: TranscriptionConfig | None = None

    """Transcription configuration (see ExtractionConfig for docs)."""

    timeout_secs: int | None = None

    """Override per-file extraction timeout in seconds."""

    tree_sitter: TreeSitterConfig | None = None

    """Override tree-sitter configuration for this file."""

    structured_extraction: StructuredExtractionConfig | None = None

    """Override structured extraction configuration for this file."""

    url: UrlExtractionConfig | None = None

    """Override URL ingestion and crawl configuration for this file."""

    ner: NerConfig | None = None

    """Override named-entity recognition configuration for this file."""

    redaction: RedactionConfig | None = None

    """Override redaction configuration for this file."""

    summarization: SummarizationConfig | None = None

    """Override summarization configuration for this file."""

    translation: TranslationConfig | None = None

    """Override translation configuration for this file."""

    page_classification: PageClassificationConfig | None = None

    """Override per-page classification configuration for this file."""

    captioning: CaptioningConfig | None = None

    """Override VLM captioning configuration for this file."""

    qr_codes: bool | None = None

    """Override QR-code detection for this file."""


@dataclass(frozen=True, slots=True)
class SvgOptions:
    """SVG-specific configuration for the image-encode pipeline."""

    sanitize: bool = True

    """Run SVG bytes through `usvg` sanitization (strips external `href` attributes, JavaScript event handlers, and `foreignObject` elements) even when the output format is `Native`.  Defaults to `true`."""

    render_dpi: float = 96

    """Target DPI when rasterizing SVG to a pixel-based format (PNG, JPEG, WebP, HEIF).  The tree's viewBox is scaled by `render_dpi / 96.0` before the pixel buffer is allocated.  Defaults to `96.0` (1x CSS pixel density)."""


@dataclass(frozen=True, slots=True)
class ExtractInput:
    """Unified extraction input for all public extraction entry points."""

    kind: ExtractInputKind | str = "uri"

    """Source kind. `bytes` requires `bytes`; `uri` requires `uri`."""

    bytes: bytes | None = None

    """Raw bytes for `kind = "bytes"`."""

    uri: str | None = None

    """Local path, `file://` URI, or HTTP(S) URL for `kind = "uri"`."""

    mime_type: str | None = None

    """MIME type hint."""

    filename: str | None = None

    """Filename hint used for MIME detection and metadata."""

    config: FileExtractionConfig | None = None

    """Per-input extraction overrides."""


@dataclass(frozen=True, slots=True)
class ExtractionSummary:
    """Summary for a unified extraction call."""

    inputs: int = 0

    """Number of inputs submitted by the caller."""

    results: int = 0

    """Number of extraction results produced."""

    errors: int = 0

    """Number of per-input errors."""

    remote_urls: int = 0

    """Number of URI inputs that resolved to remote HTTP(S) URLs."""

    pages_crawled: int = 0

    """Number of HTML pages crawled or scraped."""

    documents_downloaded: int = 0

    """Number of downloaded non-HTML documents extracted from URLs."""


@dataclass(frozen=True, slots=True)
class UrlExtractionConfig:
    """URL ingestion and crawl configuration."""

    mode: UrlExtractionMode | str = "auto"

    """URL extraction mode."""

    crawl: CrawlConfig | None = None

    """Crawlberg crawl configuration used for HTTP(S) URL extraction."""

    document_url_pattern: str | None = None

    """Optional regex filter for document-discovered URLs."""

    max_document_urls_per_result: int | None = 100

    """Maximum URLs to follow per extraction result."""

    max_total_urls: int | None = 1000

    """Maximum URLs followed across the whole extraction call."""

    allow_local_file_inputs: bool = True

    """Allow bare local filesystem path inputs."""

    allow_file_uris: bool = True

    """Allow local `file://` URI inputs."""


@dataclass(frozen=True, slots=True)
class ImageExtractionConfig:
    """Image extraction configuration."""

    extract_images: bool = True

    """Extract images from documents"""

    target_dpi: int = 300

    """Target DPI for image normalization"""

    max_image_dimension: int = 4096

    """Maximum dimension for images (width or height)"""

    inject_placeholders: bool = True

    """Whether to inject image reference placeholders into markdown output. When `true` (default), image references like `![Image 1](embedded:p1_i0)` are appended to the markdown. Set to `false` to extract images as data without polluting the markdown output."""

    auto_adjust_dpi: bool = True

    """Automatically adjust DPI based on image content"""

    min_dpi: int = 72

    """Minimum DPI threshold"""

    max_dpi: int = 600

    """Maximum DPI threshold"""

    max_images_per_page: int | None = None

    """Maximum number of image objects to extract per PDF page."""

    classify: bool = False

    """When `true`, extracted images are classified by kind and grouped into clusters where they appear to belong to one figure. Defaults to `false` -- opt in explicitly to avoid unexpected ML overhead."""

    include_page_rasters: bool = False

    """When `true`, full-page renders produced during OCR preprocessing are captured and returned as `ImageKind::PageRaster` entries in `ExtractedDocument.images`."""

    run_ocr_on_images: bool = True

    """Run OCR on extracted images and include the recognized text in the document content."""

    ocr_text_only: bool = False

    """When `true`, image OCR results are rendered as plain text without the `![...](...)` markdown placeholder. Only takes effect when `run_ocr_on_images` is also `true`."""

    append_ocr_text: bool = False

    """When `true` and `ocr_text_only` is `false`, append the OCR text after the image placeholder in the rendered output."""

    output_format: ImageOutputFormat | str = "native"

    """Target format for re-encoding extracted images."""

    svg: SvgOptions | None = None

    """SVG-specific knobs for the image-encode pipeline."""

    include_data_base64: bool = False

    """When `true`, populate `ExtractedImage::data_base64` with a Base64-encoded copy of the raw image bytes."""


@dataclass(frozen=True, slots=True)
class TokenReductionOptions:
    """Token reduction configuration."""

    mode: str = ""

    """Reduction mode: "off", "light", "moderate", "aggressive", "maximum" """

    preserve_important_words: bool = True

    """Preserve important words (capitalized, technical terms)"""


@dataclass(frozen=True, slots=True)
class LanguageDetectionConfig:
    """Language detection configuration."""

    enabled: bool = True

    """Enable language detection"""

    min_confidence: float = 0.8

    """Minimum confidence threshold (0.0-1.0)"""

    detect_multiple: bool = False

    """Detect multiple languages in the document"""


@dataclass(frozen=True, slots=True)
class HtmlOutputConfig:
    """Configuration for styled HTML output."""

    css: str | None = None

    """Inline CSS string injected into the output after the theme stylesheet. Concatenated after `css_file` content when both are set."""

    css_file: str | None = None

    """Path to a CSS file loaded once at renderer construction time. Concatenated before `css` when both are set."""

    theme: HtmlTheme | str = "unstyled"

    """Built-in colour/typography theme. Default: [`HtmlTheme::Unstyled`]."""

    class_prefix: str = ""

    """CSS class prefix applied to every emitted class name."""

    embed_css: bool = True

    """When `true` (default), write the resolved CSS into a `<style>` block immediately after the opening `<div class="{prefix}doc">`."""


@dataclass(frozen=True, slots=True)
class LateInteractionConfig:
    """Configuration for the late-interaction (ColBERT) pipeline."""

    model: LateInteractionModelType | None = None

    """The late-interaction model to use (defaults to the "gte-moderncolbert" preset)."""

    batch_size: int = 0

    """Batch size for local ONNX inference."""

    max_length: int = 0

    """Maximum token sequence length for the tokenizer (documents)."""

    query_max_length: int = 0

    """Fixed padded length for query augmentation."""

    show_download_progress: bool = False

    """Show model download progress (local ONNX path only)."""

    cache_dir: str | None = None

    """Custom cache directory for model files."""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for the late-interaction ONNX model."""

    max_embed_duration_secs: int | None = None

    """Maximum wall-clock duration (in seconds) for a single embed call when using [`LateInteractionModelType::Plugin`]. `None` disables the timeout."""


@dataclass(frozen=True, slots=True)
class LayoutDetectionConfig:
    """Layout detection configuration."""

    confidence_threshold: float | None = None

    """Confidence threshold override (None = use model default)."""

    apply_heuristics: bool = True

    """Whether to apply postprocessing heuristics (default: true)."""

    table_model: TableModel | str = "tatr"

    """Table structure recognition model."""

    table_overlap_preference: TableOverlapPreference | str = "content"

    """How to resolve overlapping native vs layout tables."""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for ONNX models (layout detection + table structure)."""

    enable_chart_understanding: bool = False

    """Route regions classified as charts to the chart-understanding OCR task."""


@dataclass(frozen=True, slots=True)
class LlmConfig:
    """Configuration for an LLM provider/model via liter-llm."""

    model: str = ""

    """Provider/model string using liter-llm routing format."""

    api_key: str | None = None

    """API key for the provider. When `None`, liter-llm falls back to the provider's standard environment variable (e.g., `OPENAI_API_KEY`)."""

    base_url: str | None = None

    """Custom base URL override for the provider endpoint."""

    timeout_secs: int | None = None

    """Request timeout in seconds (default: 60)."""

    max_retries: int | None = None

    """Maximum retry attempts (default: 3)."""

    temperature: float | None = None

    """Sampling temperature for generation tasks."""

    max_tokens: int | None = None

    """Maximum tokens to generate."""


@dataclass(frozen=True, slots=True)
class NerConfig:
    """Configuration for the NER post-processor."""

    backend: NerBackendKind | str = "onnx"

    """Backend that runs the entity detection."""

    categories: list[EntityCategory | str] = field(default_factory=list)

    """Entity categories to detect. Defaults to a sensible PERSON/ORG/LOCATION/EMAIL set when empty."""

    model: str | None = None

    """Override the default model -- only used by [`NerBackendKind::Onnx`]. `None` lets the backend pick its pinned default xberg GLiNER model alias."""

    llm: LlmConfig | None = None

    """Optional LLM configuration -- only used by [`NerBackendKind::Llm`]. Token usage for LLM backends is recorded in `ExtractedDocument::llm_usage`."""

    custom_labels: list[str] = field(default_factory=list)

    """Arbitrary user-supplied entity labels for zero-shot detection."""


@dataclass(frozen=True, slots=True)
class OcrQualityThresholds:
    """Quality thresholds for OCR fallback decisions and pipeline quality gating."""

    min_total_non_whitespace: int = 64

    """Minimum total non-whitespace characters to consider text substantive."""

    min_non_whitespace_per_page: float = 32

    """Minimum non-whitespace characters per page on average."""

    min_meaningful_word_len: int = 4

    """Minimum character count for a word to be "meaningful"."""

    min_meaningful_words: int = 3

    """Minimum count of meaningful words before text is accepted."""

    min_alnum_ratio: float = 0.3

    """Minimum alphanumeric ratio (non-whitespace chars that are alphanumeric)."""

    min_garbage_chars: int = 5

    """Minimum Unicode replacement characters (U+FFFD) to trigger OCR fallback."""

    max_fragmented_word_ratio: float = 0.6

    """Maximum fraction of short (1-2 char) words before text is considered fragmented."""

    critical_fragmented_word_ratio: float = 0.8

    """Critical fragmentation threshold -- triggers OCR regardless of meaningful words. Normal English text has ~20-30% short words. 80%+ is definitive garbage."""

    min_avg_word_length: float = 2

    """Minimum average word length. Below this with enough words indicates garbled extraction."""

    min_words_for_avg_length_check: int = 50

    """Minimum word count before average word length check applies."""

    min_consecutive_repeat_ratio: float = 0.08

    """Minimum consecutive word repetition ratio to detect column scrambling."""

    min_words_for_repeat_check: int = 50

    """Minimum word count before consecutive repetition check is applied."""

    substantive_min_chars: int = 100

    """Minimum character count for "substantive markdown" OCR skip gate."""

    non_text_min_chars: int = 20

    """Minimum character count for "non-text content" OCR skip gate."""

    alnum_ws_ratio_threshold: float = 0.4

    """Alphanumeric+whitespace ratio threshold for skip decisions."""

    pipeline_min_quality: float = 0.5

    """Minimum quality score (0.0-1.0) for a pipeline stage result to be accepted. If the result from a backend scores below this, try the next backend."""


@dataclass(frozen=True, slots=True)
class OcrConfig:
    """OCR configuration."""

    enabled: bool = True

    """Whether OCR is enabled."""

    backend: str = ""

    """OCR backend: tesseract, paddleocr, paddle-ocr, or vlm"""

    language: list[str] = field(default_factory=list)

    """Language code(s) for OCR recognition. Accepts either a single language code ("eng") or a list (["eng", "deu"]). Defaults to ["eng"]. For Tesseract, languages are joined with "+"."""

    tesseract_config: TesseractConfig | None = None

    """Tesseract-specific configuration (optional)"""

    output_format: OutputFormat | str | None = None

    """Output format for OCR results (optional, for format conversion)"""

    paddle_ocr_config: dict[str, Any] | None = None

    """PaddleOCR-specific configuration (optional, JSON passthrough)"""

    backend_options: dict[str, Any] | None = None

    """Arbitrary per-call options passed through to the backend unchanged."""

    element_config: OcrElementConfig | None = None

    """OCR element extraction configuration"""

    quality_thresholds: OcrQualityThresholds | None = None

    """Quality thresholds for the native-text-to-OCR fallback decision. When None, uses compiled defaults (matching previous hardcoded behavior)."""

    pipeline: OcrPipelineConfig | None = None

    """Multi-backend OCR pipeline configuration. When set, enables weighted fallback across multiple OCR backends based on output quality. When None, uses the single `backend` field (same as today)."""

    auto_rotate: bool = False

    """Enable automatic page rotation based on orientation detection."""

    vlm_fallback: VlmFallbackPolicy | str = "disabled"

    """Ergonomic VLM fallback policy."""

    vlm_config: LlmConfig | None = None

    """VLM (Vision Language Model) OCR configuration."""

    vlm_prompt: str | None = None

    """Custom Jinja2 prompt template for VLM OCR."""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for ONNX Runtime models (e.g. PaddleOCR, layout detection)."""

    tessdata_bytes: dict[str, bytes] | None = None

    """Caller-supplied Tesseract `traineddata` bytes per language code."""

    tessdata_path: str | None = None

    """Runtime override for tessdata directory path."""


@dataclass(frozen=True, slots=True)
class PageConfig:
    """Page extraction and tracking configuration."""

    extract_pages: bool = False

    """Extract pages as separate array (ExtractedDocument.pages)"""

    insert_page_markers: bool = False

    """Insert page markers in main content string"""

    marker_format: str = "\n\n<!-- PAGE {page_num} -->\n\n"

    """Page marker format (use {page_num} placeholder) Default: "\n\n<!-- PAGE {page_num} -->\n\n" """


@dataclass(frozen=True, slots=True)
class PdfConfig:
    """PDF-specific configuration."""

    extract_images: bool = False

    """Extract images from PDF"""

    extract_tables: bool = True

    """Extract tables from PDF."""

    passwords: list[str] | None = None

    """List of passwords to try when opening encrypted PDFs"""

    extract_metadata: bool = True

    """Extract PDF metadata"""

    hierarchy: HierarchyConfig | None = None

    """Hierarchy extraction configuration (None = hierarchy extraction disabled)"""

    extract_annotations: bool = False

    """Extract PDF annotations (text notes, highlights, links, stamps). Default: false"""

    top_margin_fraction: float | None = None

    """Top margin fraction (0.0-1.0) of page height to exclude headers/running heads. Default: 0.06 (6%)"""

    bottom_margin_fraction: float | None = None

    """Bottom margin fraction (0.0-1.0) of page height to exclude footers/page numbers. Default: 0.05 (5%)"""

    allow_single_column_tables: bool = False

    """Allow single-column pseudo tables in extraction results."""

    ocr_inline_images: bool = False

    """Perform OCR on inline images extracted from PDF pages and attach the recognized text to each `ExtractedImage.ocr_result`. Requires Tesseract to be available; if `ExtractionConfig.ocr` is `None` the extractor falls back to `TesseractConfig::default()`. Per-image failures degrade gracefully (the image is returned without OCR text rather than failing the whole extraction). Default: `false`."""

    extract_form_fields: bool = True

    """Extract AcroForm and XFA form fields into `ExtractedDocument.form_fields`."""

    reading_order: bool = False

    """Reorder extracted text by layout-detected reading order."""


@dataclass(frozen=True, slots=True)
class HierarchyConfig:
    """Hierarchy extraction configuration for PDF text structure analysis."""

    enabled: bool = True

    """Enable hierarchy extraction"""

    k_clusters: int = 3

    """Number of font size clusters to use for hierarchy levels (1-7)"""

    include_bbox: bool = True

    """Include bounding box information in hierarchy blocks"""

    ocr_coverage_threshold: float | None = None

    """OCR coverage threshold for smart OCR triggering (0.0-1.0)"""


@dataclass(frozen=True, slots=True)
class PostProcessorConfig:
    """Post-processor configuration."""

    enabled: bool = True

    """Enable post-processors"""

    enabled_processors: list[str] | None = None

    """Whitelist of processor names to run (None = all enabled)"""

    disabled_processors: list[str] | None = None

    """Blacklist of processor names to skip (None = none disabled)"""

    enabled_set: list[str] | None = None

    """Pre-computed AHashSet for O(1) enabled processor lookup"""

    disabled_set: list[str] | None = None

    """Pre-computed AHashSet for O(1) disabled processor lookup"""


@dataclass(frozen=True, slots=True)
class ChunkingConfig:
    """Chunking configuration."""

    max_characters: int = 1000

    """Maximum size per chunk (in units determined by `sizing`)."""

    overlap: int = 200

    """Overlap between chunks (in units determined by `sizing`)."""

    trim: bool = True

    """Whether to trim whitespace from chunk boundaries."""

    chunker_type: ChunkerType | str = "text"

    """Type of chunker to use (Text or Markdown)."""

    embedding: EmbeddingConfig | None = None

    """Optional embedding configuration for chunk embeddings."""

    preset: str | None = None

    """Use a preset configuration (overrides individual settings if provided)."""

    sizing: ChunkSizing | str | None = None

    """How to measure chunk size."""

    prepend_heading_context: bool = False

    """When `true` and `chunker_type` is `Markdown`, prepend the heading hierarchy path (e.g. `"# Title > ## Section\n\n"`) to each chunk's content string."""

    topic_threshold: float | None = None

    """Optional cosine similarity threshold for semantic topic boundary detection."""

    table_chunking: TableChunkingMode | str = "split"

    """How to handle markdown tables that exceed the chunk size limit."""


@dataclass(frozen=True, slots=True)
class EmbeddingConfig:
    """Embedding configuration for text chunks."""

    model: EmbeddingModelType | None = None

    """The embedding model to use (defaults to "gte-modernbert-base" preset if not specified)"""

    normalize: bool = True

    """Whether to normalize embedding vectors (recommended for cosine similarity)"""

    batch_size: int = 32

    """Batch size for embedding generation"""

    show_download_progress: bool = False

    """Show model download progress"""

    cache_dir: str | None = None

    """Custom cache directory for model files"""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for the embedding ONNX model."""

    max_embed_duration_secs: int | None = 60

    """Maximum wall-clock duration (in seconds) for a single `embed()` call when using [`EmbeddingModelType::Plugin`]."""

    max_sequence_length: int | None = None

    """Maximum number of tokens fed to the tokenizer before truncation when embedding a chunk with a local ONNX model (Preset/Custom)."""


@dataclass(frozen=True, slots=True)
class RedactionConfig:
    """Configuration for the redaction post-processor."""

    categories: list[PiiCategory | str] = field(default_factory=list)

    """Categories to redact. Empty means "every category supported by the engine." """

    strategy: RedactionStrategy | str = "mask"

    """Strategy applied to every match."""

    ner: NerConfig | None = None

    """Optional NER backend -- required to redact PERSON / ORGANIZATION / LOCATION categories (the pure-Rust pattern engine only covers regex-detectable PII)."""

    preserve_offsets: bool = True

    """When `true`, chunk byte ranges are kept consistent with the rewritten content by adjusting `byte_start` / `byte_end` after replacement. When `false`, chunk byte ranges still refer to the *original* content offsets -- useful when downstream consumers want to map findings back to the original document."""

    custom_terms: list[RedactionTerm] = field(default_factory=list)

    """Arbitrary user-supplied literal terms to redact."""

    custom_patterns: list[RedactionPattern] = field(default_factory=list)

    """Arbitrary user-supplied regex patterns to redact."""


@dataclass(frozen=True, slots=True)
class RerankerConfig:
    """Configuration for the reranking pipeline."""

    model: RerankerModelType | None = None

    """The reranker model to use (defaults to "balanced" preset if not specified)."""

    top_k: int | None = None

    """Return at most this many documents. `None` returns all."""

    batch_size: int = 32

    """Batch size for local ONNX cross-encoder inference."""

    show_download_progress: bool = False

    """Show model download progress (local ONNX path only)."""

    cache_dir: str | None = None

    """Custom cache directory for model files."""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for the reranker ONNX model."""

    max_rerank_duration_secs: int | None = 60

    """Maximum wall-clock duration (in seconds) for a single `rerank()` call when using [`RerankerModelType::Plugin`]."""


@dataclass(frozen=True, slots=True)
class SparseEmbeddingConfig:
    """Configuration for the sparse-embedding pipeline."""

    model: SparseEmbeddingModelType | None = None

    """The sparse-embedding model to use (defaults to the "opensearch-v3-distill" preset)."""

    batch_size: int = 0

    """Batch size for local ONNX inference."""

    max_length: int = 0

    """Maximum token sequence length for the tokenizer."""

    show_download_progress: bool = False

    """Show model download progress (local ONNX path only)."""

    cache_dir: str | None = None

    """Custom cache directory for model files."""

    acceleration: AccelerationConfig | None = None

    """Hardware acceleration for the sparse-embedding ONNX model."""

    max_embed_duration_secs: int | None = None

    """Maximum wall-clock duration (in seconds) for a single embed call when using [`SparseEmbeddingModelType::Plugin`]. `None` disables the timeout."""


@dataclass(frozen=True, slots=True)
class SummarizationConfig:
    """Configuration for the summarisation post-processor."""

    strategy: SummaryStrategy | str = "extractive"

    """Summarisation strategy."""

    max_tokens: int | None = None

    """Maximum summary length in tokens. `None` lets the backend pick a default."""

    llm: LlmConfig | None = None

    """LLM configuration for the abstractive backend. Ignored when `strategy = Extractive`. Required when `strategy = Abstractive`."""


@dataclass(frozen=True, slots=True)
class TranscriptionConfig:
    """Configuration for audio/video transcription (speech-to-text)."""

    enabled: bool = True

    """Master switch. When false the block is ignored and audio files fall back to the normal "unsupported format" path."""

    model: WhisperModel | str = "tiny"

    """Whisper model size to use."""

    language: str | None = None

    """Optional language hint (ISO-639-1 code, e.g. "en", "de")."""

    timestamps: bool = False

    """Whether to request segment-level timestamps."""

    max_duration_ms: int | None = None

    """Hard safety limit on input duration (milliseconds)."""

    max_bytes: int | None = None

    """Hard safety limit on input size (bytes)."""

    timeout_ms: int | None = None

    """Wall-clock timeout for the entire transcription operation (ms)."""

    model_cache_dir: str | None = None

    """Override the directory used for Whisper model cache."""

    allow_network: bool = True

    """Allow network access to download models from Hugging Face Hub."""

    verify_hash: bool = False

    """Request SHA256 verification of downloaded model files."""


@dataclass(frozen=True, slots=True)
class TreeSitterConfig:
    """Configuration for tree-sitter language pack integration."""

    enabled: bool = True

    """Enable code intelligence processing (default: true)."""

    cache_dir: str | None = None

    """Custom cache directory for downloaded grammars."""

    languages: list[str] | None = None

    """Languages to pre-download on init (e.g., `["python", "rust"]`)."""

    groups: list[str] | None = None

    """Language groups to pre-download (e.g., `["web", "systems", "scripting"]`)."""

    process: TreeSitterProcessConfig | None = None

    """Processing options for code analysis."""


@dataclass(frozen=True, slots=True)
class TreeSitterProcessConfig:
    """Processing options for tree-sitter code analysis."""

    structure: bool = True

    """Extract structural items (functions, classes, structs, etc.). Default: true."""

    imports: bool = True

    """Extract import statements. Default: true."""

    exports: bool = True

    """Extract export statements. Default: true."""

    comments: bool = False

    """Extract comments. Default: false."""

    docstrings: bool = False

    """Extract docstrings. Default: false."""

    symbols: bool = False

    """Extract symbol definitions. Default: false."""

    diagnostics: bool = False

    """Include parse diagnostics. Default: false."""

    chunk_max_size: int | None = None

    """Maximum chunk size in bytes. `None` disables chunking."""

    content_mode: CodeContentMode | str = "chunks"

    """Content rendering mode for code extraction."""


@dataclass(frozen=True, slots=True)
class ServerConfig:
    """API server configuration."""

    host: str = ""

    """Server host address (e.g., "127.0.0.1", "0.0.0.0")"""

    port: int = 0

    """Server port number"""

    cors_origins: list[str] = field(default_factory=list)

    """CORS allowed origins. Empty vector means allow all origins."""

    max_request_body_bytes: int = 0

    """Maximum size of request body in bytes (default: 100 MB)"""

    max_multipart_field_bytes: int = 0

    """Maximum size of multipart fields in bytes (default: 100 MB)"""


@dataclass(frozen=True, slots=True)
class DocxAppProperties:
    """Application properties from docProps/app.xml for DOCX."""

    application: str | None = None

    """Application name (e.g., "Microsoft Office Word")"""

    app_version: str | None = None

    """Application version"""

    template: str | None = None

    """Template filename"""

    total_time: int | None = None

    """Total editing time in minutes"""

    pages: int | None = None

    """Number of pages"""

    words: int | None = None

    """Number of words"""

    characters: int | None = None

    """Number of characters (excluding spaces)"""

    characters_with_spaces: int | None = None

    """Number of characters (including spaces)"""

    lines: int | None = None

    """Number of lines"""

    paragraphs: int | None = None

    """Number of paragraphs"""

    company: str | None = None

    """Company name"""

    doc_security: int | None = None

    """Document security level"""

    scale_crop: bool | None = None

    """Scale crop flag"""

    links_up_to_date: bool | None = None

    """Links up to date flag"""

    shared_doc: bool | None = None

    """Shared document flag"""

    hyperlinks_changed: bool | None = None

    """Hyperlinks changed flag"""


@dataclass(frozen=True, slots=True)
class XlsxAppProperties:
    """Application properties from docProps/app.xml for XLSX."""

    application: str | None = None

    """Application name (e.g., "Microsoft Excel")"""

    app_version: str | None = None

    """Application version"""

    doc_security: int | None = None

    """Document security level"""

    scale_crop: bool | None = None

    """Scale crop flag"""

    links_up_to_date: bool | None = None

    """Links up to date flag"""

    shared_doc: bool | None = None

    """Shared document flag"""

    hyperlinks_changed: bool | None = None

    """Hyperlinks changed flag"""

    company: str | None = None

    """Company name"""

    worksheet_names: list[str] = field(default_factory=list)

    """Worksheet names"""


@dataclass(frozen=True, slots=True)
class PptxAppProperties:
    """Application properties from docProps/app.xml for PPTX."""

    application: str | None = None

    """Application name (e.g., "Microsoft Office PowerPoint")"""

    app_version: str | None = None

    """Application version"""

    total_time: int | None = None

    """Total editing time in minutes"""

    company: str | None = None

    """Company name"""

    doc_security: int | None = None

    """Document security level"""

    scale_crop: bool | None = None

    """Scale crop flag"""

    links_up_to_date: bool | None = None

    """Links up to date flag"""

    shared_doc: bool | None = None

    """Shared document flag"""

    hyperlinks_changed: bool | None = None

    """Hyperlinks changed flag"""

    slides: int | None = None

    """Number of slides"""

    notes: int | None = None

    """Number of notes"""

    hidden_slides: int | None = None

    """Number of hidden slides"""

    multimedia_clips: int | None = None

    """Number of multimedia clips"""

    presentation_format: str | None = None

    """Presentation format (e.g., "Widescreen", "Standard")"""

    slide_titles: list[str] = field(default_factory=list)

    """Slide titles"""


@dataclass(frozen=True, slots=True)
class CoreProperties:
    """Dublin Core metadata from docProps/core.xml."""

    title: str | None = None

    """Document title"""

    subject: str | None = None

    """Document subject/topic"""

    creator: str | None = None

    """Document creator/author"""

    keywords: str | None = None

    """Keywords or tags"""

    description: str | None = None

    """Document description/abstract"""

    last_modified_by: str | None = None

    """User who last modified the document"""

    revision: str | None = None

    """Revision number"""

    created: str | None = None

    """Creation timestamp (ISO 8601)"""

    modified: str | None = None

    """Last modification timestamp (ISO 8601)"""

    category: str | None = None

    """Document category"""

    content_status: str | None = None

    """Content status (Draft, Final, etc.)"""

    language: str | None = None

    """Document language"""

    identifier: str | None = None

    """Unique identifier"""

    version: str | None = None

    """Document version"""

    last_printed: str | None = None

    """Last print timestamp (ISO 8601)"""


@dataclass(frozen=True, slots=True)
class SecurityLimits:
    """Configuration for security limits across extractors."""

    max_archive_size: int = 524288000

    """Maximum uncompressed size for archives (500 MB)"""

    max_compression_ratio: int = 100

    """Maximum compression ratio before flagging as potential bomb (100:1)"""

    max_files_in_archive: int = 10000

    """Maximum number of files in archive (10,000)"""

    max_nesting_depth: int = 1024

    """Maximum nesting depth for structures (100)"""

    max_entity_length: int = 1048576

    """Maximum length of any single XML entity / attribute / token (1 MiB). This is a per-token cap, NOT a total cap -- billion-laughs class attacks where a single entity expands to hundreds of MB are caught here, while normal long text content (a paragraph, a CDATA block) is caught by `max_content_size` instead."""

    max_content_size: int = 104857600

    """Maximum string growth per document (100 MB)"""

    max_iterations: int = 10000000

    """Maximum iterations per operation"""

    max_xml_depth: int = 1024

    """Maximum XML depth (100 levels)"""

    max_table_cells: int = 100000

    """Maximum cells per table (100,000)"""


@dataclass(frozen=True, slots=True)
class TokenReductionConfig:
    """Configuration for the token-reduction pipeline."""

    level: ReductionLevel | str = "moderate"

    """Reduction intensity level."""

    language_hint: str | None = None

    """ISO 639-1 language code hint for stopword selection (e.g. `"en"`, `"de"`)."""

    preserve_markdown: bool = False

    """Preserve Markdown formatting tokens during reduction."""

    preserve_code: bool = True

    """Preserve code block contents unchanged."""

    semantic_threshold: float = 0.3

    """Cosine similarity threshold below which sentences are considered dissimilar."""

    enable_parallel: bool = True

    """Use Rayon parallel iterators for multi-core processing."""

    use_simd: bool = True

    """Use SIMD-optimized text scanning where available."""

    custom_stopwords: dict[str, list[str]] | None = None

    """Per-language custom stopword lists (`language_code → stopword_list`)."""

    preserve_patterns: list[str] = field(default_factory=list)

    """Regex patterns whose matched text is always preserved unchanged."""

    target_reduction: float | None = None

    """Target fraction of text to retain (0.0-1.0); `None` = no fixed target."""

    enable_semantic_clustering: bool = False

    """Group semantically similar sentences and emit only one per cluster."""


@dataclass(frozen=True, slots=True)
class TokenCounter:
    """Per-category running counter for [`RedactionStrategy::TokenReplace`]."""


@dataclass(frozen=True, slots=True)
class FootnoteConfig:
    """Configuration for markdown footnote and citation parsing."""

    parse_citations: bool = True

    """Whether to parse the structured citation block (default: true)."""


@dataclass(frozen=True, slots=True)
class DocumentStructure:
    """Top-level structured document representation."""

    nodes: list[DocumentNode] = field(default_factory=list)

    """All nodes in document/reading order."""

    source_format: str | None = None

    """Origin format identifier (e.g. "docx", "pptx", "html", "pdf")."""

    relationships: list[DocumentRelationship] = field(default_factory=list)

    """Resolved relationships between nodes (footnote refs, citations, anchor links, etc.)."""

    node_types: list[str] = field(default_factory=list)

    """Sorted, deduplicated list of node type names present in this document."""


@dataclass(frozen=True, slots=True)
class TableGrid:
    """Structured table grid with cell-level metadata."""

    rows: int = 0

    """Number of rows in the table."""

    cols: int = 0

    """Number of columns in the table."""

    cells: list[GridCell] = field(default_factory=list)

    """All cells in row-major order."""


@dataclass(frozen=True, slots=True)
class DocumentCounts:
    """Cheap structural counts for an extracted document."""

    pages: int = 0

    """Total pages in the source document (`0` when not page-addressable)."""

    tables: int = 0

    """Tables detected in the document."""

    images: int = 0

    """Images detected in the document."""


@dataclass(frozen=True, slots=True)
class LlmUsage:
    """Token usage and cost data for a single LLM call made during extraction."""

    model: str = ""

    """The LLM model identifier (e.g. "openai/gpt-4o", "anthropic/claude-sonnet-4-20250514")."""

    source: str = ""

    """The pipeline stage that triggered this LLM call (e.g. "vlm_ocr", "structured_extraction", "embeddings")."""

    input_tokens: int | None = None

    """Number of input/prompt tokens consumed."""

    output_tokens: int | None = None

    """Number of output/completion tokens generated."""

    total_tokens: int | None = None

    """Total tokens (input + output)."""

    estimated_cost: float | None = None

    """Estimated cost in USD based on the provider's published pricing."""

    finish_reason: str | None = None

    """Why the model stopped generating (e.g. "stop", "length", "content_filter")."""


@dataclass(frozen=True, slots=True)
class ExtractedImage:
    """Extracted image from a document."""

    data: bytes = b""

    """Raw image data (PNG, JPEG, WebP, etc. bytes). Uses `bytes::Bytes` for cheap cloning of large buffers."""

    format: str = ""

    """Image format (e.g., "jpeg", "png", "webp") Uses Cow<'static, str> to avoid allocation for static literals."""

    image_index: int = 0

    """Zero-indexed position of this image in the document/page"""

    page_number: int | None = None

    """Page/slide number where image was found (1-indexed)"""

    width: int | None = None

    """Image width in pixels"""

    height: int | None = None

    """Image height in pixels"""

    colorspace: str | None = None

    """Colorspace information (e.g., "RGB", "CMYK", "Gray")"""

    bits_per_component: int | None = None

    """Bits per color component (e.g., 8, 16)"""

    is_mask: bool = False

    """Whether this image is a mask image"""

    description: str | None = None

    """Optional description of the image"""

    ocr_result: ExtractedDocument | None = None

    """Nested OCR extraction result (if image was OCRed)"""

    bounding_box: BoundingBox | None = None

    """Bounding box of the image on the page (PDF coordinates: x0=left, y0=bottom, x1=right, y1=top). Only populated for PDF-extracted images when position data is available from the PDF extractor."""

    source_path: str | None = None

    """Original source path of the image within the document archive (e.g., "media/image1.png" in DOCX). Used for rendering image references when the binary data is not extracted."""

    image_kind: ImageKind | str | None = None

    """Heuristic classification of what this image likely depicts. `None` if classification was disabled or inconclusive."""

    kind_confidence: float | None = None

    """Confidence score for `image_kind`, in the range 0.0 to 1.0."""

    cluster_id: int | None = None

    """Identifier shared across images that form a single logical figure (e.g. all raster tiles of one technical drawing). `None` for singletons."""

    caption: str | None = None

    """VLM-generated caption describing the image, when captioning is configured."""

    qr_codes: list[QrCode] | None = None

    """QR codes decoded from this image, when QR detection is enabled."""

    data_base64: str | None = None

    """Base64-encoded copy of `data`; populated when `ImageExtractionConfig::include_data_base64` is `true`. Omitted from JSON by default; use instead of `data` in JSON-only clients."""


@dataclass(frozen=True, slots=True)
class BoundingBox:
    """Bounding box coordinates for element positioning."""

    x0: float = 0.0

    """Left x-coordinate"""

    y0: float = 0.0

    """Bottom y-coordinate"""

    x1: float = 0.0

    """Right x-coordinate"""

    y1: float = 0.0

    """Top y-coordinate"""


@dataclass(frozen=True, slots=True)
class OcrExtractionResult:
    """OCR extraction result."""

    content: str = ""

    """Recognized text content"""

    mime_type: str = ""

    """Original MIME type of the processed image"""

    metadata: dict[str, dict[str, Any]] = field(default_factory=dict)

    """OCR processing metadata (confidence scores, language, etc.)"""

    tables: list[OcrTable] = field(default_factory=list)

    """Tables detected and extracted via OCR"""

    ocr_elements: list[OcrElement] | None = None

    """Structured OCR elements with bounding boxes and confidence scores. Available when TSV output is requested or table detection is enabled."""


@dataclass(frozen=True, slots=True)
class ImagePreprocessingConfig:
    """Image preprocessing configuration for OCR."""

    target_dpi: int = 300

    """Target DPI for the image (300 is standard, 600 for small text)."""

    auto_rotate: bool = False

    """Auto-detect and correct image rotation."""

    deskew: bool = True

    """Correct skew (tilted images)."""

    denoise: bool = False

    """Remove noise from the image."""

    contrast_enhance: bool = False

    """Enhance contrast for better text visibility."""

    binarization_method: str = "otsu"

    """Binarization method: "otsu", "sauvola", "adaptive"."""

    invert_colors: bool = False

    """Invert colors (white text on black → black on white)."""


@dataclass(frozen=True, slots=True)
class TesseractConfig:
    """Tesseract OCR configuration."""

    language: list[str] = field(default_factory=list)

    """Language code(s) for OCR recognition. Accepts either a single language code ("eng") or a list (["eng", "deu"]). For Tesseract backend, languages are joined with "+"."""

    psm: int = 3

    """Page Segmentation Mode (0-13)."""

    output_format: str = "markdown"

    """Output format ("text" or "markdown")"""

    oem: int = 3

    """OCR Engine Mode (0-3)."""

    min_confidence: float = 0

    """Minimum confidence threshold (0.0-100.0)."""

    preprocessing: ImagePreprocessingConfig | None = None

    """Image preprocessing configuration."""

    enable_table_detection: bool = True

    """Enable automatic table detection and reconstruction"""

    table_min_confidence: float = 0

    """Minimum confidence threshold for table detection (0.0-1.0)"""

    table_column_threshold: int = 50

    """Column threshold for table detection (pixels)"""

    table_row_threshold_ratio: float = 0.5

    """Row threshold ratio for table detection (0.0-1.0)"""

    use_cache: bool = True

    """Enable OCR result caching"""

    classify_use_pre_adapted_templates: bool = True

    """Use pre-adapted templates for character classification"""

    language_model_ngram_on: bool = False

    """Enable N-gram language model"""

    tessedit_dont_blkrej_good_wds: bool = True

    """Don't reject good words during block-level processing"""

    tessedit_dont_rowrej_good_wds: bool = True

    """Don't reject good words during row-level processing"""

    tessedit_enable_dict_correction: bool = True

    """Enable dictionary correction"""

    tessedit_char_whitelist: str = ""

    """Whitelist of allowed characters (empty = all allowed)"""

    tessedit_char_blacklist: str = ""

    """Blacklist of forbidden characters (empty = none forbidden)"""

    tessedit_use_primary_params_model: bool = True

    """Use primary language params model"""

    textord_space_size_is_variable: bool = True

    """Variable-width space detection"""

    thresholding_method: bool = False

    """Use adaptive thresholding method"""


@dataclass(frozen=True, slots=True)
class CodeMetadata:
    """Code-format metadata: the structural chunks produced by tree-sitter parsing."""

    chunks: list[CodeChunkInfo] = field(default_factory=list)

    """Structural code chunks (function/class/module boundaries)."""


@dataclass(frozen=True, slots=True)
class Metadata:
    """Extraction result metadata."""

    title: str | None = None

    """Document title"""

    subject: str | None = None

    """Document subject or description"""

    authors: list[str] | None = None

    """Primary author(s) - always Vec for consistency"""

    keywords: list[str] | None = None

    """Keywords/tags - always Vec for consistency"""

    language: str | None = None

    """Primary language (ISO 639 code)"""

    created_at: str | None = None

    """Creation timestamp (ISO 8601 format)"""

    modified_at: str | None = None

    """Last modification timestamp (ISO 8601 format)"""

    created_by: str | None = None

    """User who created the document"""

    modified_by: str | None = None

    """User who last modified the document"""

    pages: PageStructure | None = None

    """Page/slide/sheet structure with boundaries"""

    format: FormatMetadata | None = None

    """Format-specific metadata (discriminated union)"""

    image_preprocessing: ImagePreprocessingMetadata | None = None

    """Image preprocessing metadata (when OCR preprocessing was applied)"""

    json_schema: dict[str, Any] | None = None

    """JSON schema (for structured data extraction)"""

    error: ErrorMetadata | None = None

    """Error metadata (for batch operations)"""

    extraction_duration_ms: int | None = None

    """Extraction duration in milliseconds (for benchmarking)."""

    category: str | None = None

    """Document category (from frontmatter or classification)."""

    tags: list[str] | None = None

    """Document tags (from frontmatter)."""

    document_version: str | None = None

    """Document version string (from frontmatter)."""

    abstract_text: str | None = None

    """Abstract or summary text (from frontmatter)."""

    output_format: str | None = None

    """Output format identifier (e.g., "markdown", "html", "text")."""

    ocr_used: bool = False

    """Whether OCR was used during extraction."""

    additional: dict[str, dict[str, Any]] = field(default_factory=dict)

    """Additional custom fields from postprocessors."""


@dataclass(frozen=True, slots=True)
class ExcelMetadata:
    """Excel/spreadsheet format metadata."""

    sheet_count: int | None = None

    """Number of sheets in the workbook."""

    sheet_names: list[str] | None = None

    """Names of all sheets in the workbook."""


@dataclass(frozen=True, slots=True)
class EmailMetadata:
    """Email metadata extracted from .eml and .msg files."""

    from_email: str | None = None

    """Sender's email address"""

    from_name: str | None = None

    """Sender's display name"""

    to_emails: list[str] = field(default_factory=list)

    """Primary recipients"""

    cc_emails: list[str] = field(default_factory=list)

    """CC recipients"""

    bcc_emails: list[str] = field(default_factory=list)

    """BCC recipients"""

    message_id: str | None = None

    """Message-ID header value"""

    attachments: list[str] = field(default_factory=list)

    """List of attachment filenames"""


@dataclass(frozen=True, slots=True)
class ArchiveMetadata:
    """Archive (ZIP/TAR/7Z) metadata."""

    format: str = ""

    """Archive format ("ZIP", "TAR", "7Z", etc.)"""

    file_count: int = 0

    """Total number of files in the archive"""

    file_list: list[str] = field(default_factory=list)

    """List of file paths within the archive"""

    total_size: int = 0

    """Total uncompressed size in bytes"""

    compressed_size: int | None = None

    """Compressed size in bytes (if available)"""


@dataclass(frozen=True, slots=True)
class ImageMetadata:
    """Image metadata extracted from image files."""

    width: int = 0

    """Image width in pixels"""

    height: int = 0

    """Image height in pixels"""

    format: str = ""

    """Image format (e.g., "PNG", "JPEG", "TIFF")"""

    exif: dict[str, str] = field(default_factory=dict)

    """EXIF metadata tags"""


@dataclass(frozen=True, slots=True)
class XmlMetadata:
    """XML metadata extracted during XML parsing."""

    element_count: int = 0

    """Total number of XML elements processed"""

    unique_elements: list[str] = field(default_factory=list)

    """List of unique element tag names (sorted)"""


@dataclass(frozen=True, slots=True)
class TextMetadata:
    """Text/Markdown metadata."""

    line_count: int = 0

    """Number of lines in the document"""

    word_count: int = 0

    """Number of words"""

    character_count: int = 0

    """Number of characters"""

    headers: list[str] | None = None

    """Markdown headers (headings text only, for Markdown files)"""


@dataclass(frozen=True, slots=True)
class HtmlMetadata:
    """HTML metadata extracted from HTML documents."""

    title: str | None = None

    """Document title from `<title>` tag"""

    description: str | None = None

    """Document description from `<meta name="description">` tag"""

    keywords: list[str] = field(default_factory=list)

    """Document keywords from `<meta name="keywords">` tag, split on commas"""

    author: str | None = None

    """Document author from `<meta name="author">` tag"""

    canonical_url: str | None = None

    """Canonical URL from `<link rel="canonical">` tag"""

    base_href: str | None = None

    """Base URL from `<base href="">` tag for resolving relative URLs"""

    language: str | None = None

    """Document language from `lang` attribute"""

    text_direction: TextDirection | str | None = None

    """Document text direction from `dir` attribute"""

    open_graph: dict[str, str] = field(default_factory=dict)

    """Open Graph metadata (og:* properties) for social media Keys like "title", "description", "image", "url", etc."""

    twitter_card: dict[str, str] = field(default_factory=dict)

    """Twitter Card metadata (twitter:* properties) Keys like "card", "site", "creator", "title", "description", "image", etc."""

    meta_tags: dict[str, str] = field(default_factory=dict)

    """Additional meta tags not covered by specific fields Keys are meta name/property attributes, values are content"""

    headers: list[HeaderMetadata] = field(default_factory=list)

    """Extracted header elements with hierarchy"""

    links: list[LinkMetadata] = field(default_factory=list)

    """Extracted hyperlinks with type classification"""

    images: list[ImageMetadataType] = field(default_factory=list)

    """Extracted images with source and dimensions"""

    structured_data: list[StructuredData] = field(default_factory=list)

    """Extracted structured data blocks"""


@dataclass(frozen=True, slots=True)
class OcrMetadata:
    """OCR processing metadata."""

    language: str = ""

    """OCR language code(s) used"""

    psm: int = 0

    """Tesseract Page Segmentation Mode (PSM)"""

    output_format: str = ""

    """Output format (e.g., "text", "hocr")"""

    table_count: int = 0

    """Number of tables detected"""

    table_rows: int | None = None

    """Number of rows in the detected table (if a single table was found)."""

    table_cols: int | None = None

    """Number of columns in the detected table (if a single table was found)."""


@dataclass(frozen=True, slots=True)
class PptxMetadata:
    """PowerPoint presentation metadata."""

    slide_count: int = 0

    """Total number of slides in the presentation"""

    slide_names: list[str] = field(default_factory=list)

    """Names of slides (if available)"""

    image_count: int | None = None

    """Number of embedded images"""

    table_count: int | None = None

    """Number of tables"""


@dataclass(frozen=True, slots=True)
class DocxMetadata:
    """Word document metadata."""

    core_properties: CoreProperties | None = None

    """Core properties from docProps/core.xml (Dublin Core metadata)"""

    app_properties: DocxAppProperties | None = None

    """Application properties from docProps/app.xml (Word-specific statistics)"""

    custom_properties: dict[str, dict[str, Any]] | None = None

    """Custom properties from docProps/custom.xml (user-defined properties)"""


@dataclass(frozen=True, slots=True)
class CsvMetadata:
    """CSV/TSV file metadata."""

    row_count: int = 0

    """Total number of data rows (excluding the header row if present)."""

    column_count: int = 0

    """Number of columns detected."""

    delimiter: str | None = None

    """Field delimiter character (e.g. `","` or `"\t"`)."""

    has_header: bool = False

    """Whether the first row was treated as a header."""

    column_types: list[str] | None = None

    """Inferred data type for each column (e.g. `"string"`, `"integer"`, `"float"`)."""


@dataclass(frozen=True, slots=True)
class BibtexMetadata:
    """BibTeX bibliography metadata."""

    entry_count: int = 0

    """Number of entries in the bibliography."""

    citation_keys: list[str] = field(default_factory=list)

    """BibTeX citation keys (e.g. `"knuth1984"`) for all entries."""

    authors: list[str] = field(default_factory=list)

    """Author names collected across all bibliography entries."""

    year_range: YearRange | None = None

    """Earliest and latest publication years found in the bibliography."""

    entry_types: dict[str, int] | None = None

    """Count of entries grouped by BibTeX entry type (e.g. `"article"` → 5)."""


@dataclass(frozen=True, slots=True)
class CitationMetadata:
    """Citation file metadata (RIS, PubMed, EndNote)."""

    citation_count: int = 0

    """Total number of citation records in the file."""

    format: str | None = None

    """Detected citation file format (e.g. `"ris"`, `"pubmed"`, `"endnote"`)."""

    authors: list[str] = field(default_factory=list)

    """Author names collected across all citation records."""

    year_range: YearRange | None = None

    """Earliest and latest publication years found in the file."""

    dois: list[str] = field(default_factory=list)

    """DOI identifiers found in the citation records."""

    keywords: list[str] = field(default_factory=list)

    """Keywords collected from all citation records."""


@dataclass(frozen=True, slots=True)
class FictionBookMetadata:
    """FictionBook (FB2) metadata."""

    genres: list[str] = field(default_factory=list)

    """Genre tags as declared in the FB2 `<genre>` elements."""

    sequences: list[str] = field(default_factory=list)

    """Book series (sequence) names, if any."""

    annotation: str | None = None

    """Short annotation / summary from the FB2 `<annotation>` element."""


@dataclass(frozen=True, slots=True)
class DbfMetadata:
    """dBASE (DBF) file metadata."""

    record_count: int = 0

    """Total number of data records in the DBF file."""

    field_count: int = 0

    """Number of field (column) definitions."""

    fields: list[DbfFieldInfo] = field(default_factory=list)

    """Descriptor for each field in the table schema."""


@dataclass(frozen=True, slots=True)
class JatsMetadata:
    """JATS (Journal Article Tag Suite) metadata."""

    copyright: str | None = None

    """Copyright statement from the article's `<permissions>` element."""

    license: str | None = None

    """Open-access license URI from the article's `<license>` element."""

    history_dates: dict[str, str] = field(default_factory=dict)

    """Publication history dates keyed by event type (e.g. `"received"`, `"accepted"`)."""

    contributor_roles: list[ContributorRole] = field(default_factory=list)

    """Authors and contributors with their stated roles."""


@dataclass(frozen=True, slots=True)
class EpubMetadata:
    """EPUB metadata (Dublin Core extensions)."""

    coverage: str | None = None

    """Dublin Core `coverage` field (geographic or temporal scope)."""

    dc_format: str | None = None

    """Dublin Core `format` field (media type of the resource)."""

    relation: str | None = None

    """Dublin Core `relation` field (related resource identifier)."""

    source: str | None = None

    """Dublin Core `source` field (origin resource identifier)."""

    dc_type: str | None = None

    """Dublin Core `type` field (nature or genre of the resource)."""

    cover_image: str | None = None

    """Path or identifier of the cover image within the EPUB container."""


@dataclass(frozen=True, slots=True)
class PstMetadata:
    """Outlook PST archive metadata."""

    message_count: int = 0

    """Total number of email messages found in the PST archive."""


@dataclass(frozen=True, slots=True)
class AudioMetadata:
    """Audio/video file metadata."""

    duration_ms: int | None = None

    """Duration in milliseconds derived from the decoded audio stream."""

    codec: str | None = None

    """Audio codec (e.g. "mp3", "aac", "opus", "flac")."""

    container: str | None = None

    """Container format (e.g. "mpeg", "mp4", "ogg", "wav")."""

    sample_rate_hz: int | None = None

    """Sample rate in Hz after decode (always 16000 when resampled for Whisper)."""

    channels: int | None = None

    """Number of audio channels (1 = mono, 2 = stereo)."""

    bitrate: int | None = None

    """Audio bitrate in kbps from the source file tags/properties."""


@dataclass(frozen=True, slots=True)
class OcrConfidence:
    """Confidence scores for an OCR element."""

    detection: float | None = None

    """Detection confidence: how confident the OCR engine is that text exists here."""

    recognition: float = 0.0

    """Recognition confidence: how confident about the text content."""


@dataclass(frozen=True, slots=True)
class OcrElement:
    """A unified OCR element representing detected text with full metadata."""

    text: str = ""

    """The recognized text content."""

    geometry: OcrBoundingGeometry | None = None

    """Bounding geometry (rectangle or quadrilateral)."""

    confidence: OcrConfidence | None = None

    """Confidence scores for detection and recognition."""

    level: OcrElementLevel | str = "line"

    """Hierarchical level (word, line, block, page)."""

    rotation: OcrRotation | None = None

    """Rotation information (if detected)."""

    page_number: int = 0

    """Page number (1-indexed)."""

    parent_id: str | None = None

    """Parent element ID for hierarchical relationships."""

    backend_metadata: dict[str, dict[str, Any]] = field(default_factory=dict)

    """Backend-specific metadata that doesn't fit the unified schema."""


@dataclass(frozen=True, slots=True)
class OcrElementConfig:
    """Configuration for OCR element extraction."""

    include_elements: bool = False

    """Whether to include OCR elements in the extraction result."""

    min_level: OcrElementLevel | str = "line"

    """Minimum hierarchical level to include."""

    min_confidence: float = 0.0

    """Minimum recognition confidence threshold (0.0-1.0)."""

    build_hierarchy: bool = False

    """Whether to build hierarchical relationships between elements."""


@dataclass(frozen=True, slots=True)
class LayoutRegion:
    """A detected layout region on a page."""

    class_name: str = ""

    """Layout class name (e.g. "picture", "table", "text", "section_header")."""

    confidence: float = 0.0

    """Confidence score from the layout detection model (0.0 to 1.0)."""

    bounding_box: BoundingBox | None = None

    """Bounding box in document coordinate space."""

    area_fraction: float = 0.0

    """Fraction of the page area covered by this region (0.0 to 1.0)."""


@dataclass(frozen=True, slots=True)
class RevisionDelta:
    """The content changes that make up a single revision."""

    content: list[DiffLine] = field(default_factory=list)

    """Line-level content changes for this revision."""

    table_changes: list[CellChange] = field(default_factory=list)

    """Cell-level table changes for this revision."""

    property_changes: list[PropertyChange] = field(default_factory=list)

    """Formatting or metadata property changes for this revision."""


@dataclass(frozen=True, slots=True)
class Table:
    """Extracted table structure."""

    cells: list[list[str]] = field(default_factory=list)

    """Table cells as a 2D vector (rows x columns)"""

    markdown: str = ""

    """Markdown representation of the table"""

    page_number: int = 0

    """Page number where the table was found (1-indexed)"""

    bounding_box: BoundingBox | None = None

    """Bounding box of the table on the page (PDF coordinates: x0=left, y0=bottom, x1=right, y1=top). Only populated for PDF-extracted tables when position data is available."""


@dataclass(frozen=True, slots=True)
class TableCell:
    """Individual table cell with content and optional styling."""

    content: str = ""

    """Cell content as text"""

    row_span: int = 0

    """Row span (number of rows this cell spans)"""

    col_span: int = 0

    """Column span (number of columns this cell spans)"""

    is_header: bool = False

    """Whether this is a header cell"""


@dataclass(frozen=True, slots=True)
class DiffOptions:
    """Options controlling how two `ExtractedDocument` values are compared."""

    include_metadata: bool = True

    """Include metadata changes in the diff. Default: `true`."""

    include_embedded: bool = True

    """Include embedded-children changes in the diff. Default: `true`."""

    max_content_chars: int | None = None

    """Truncate content to this many characters before diffing."""


@dataclass(frozen=True, slots=True)
class EmbeddedChanges:
    """Changes to embedded archive children between two results."""

    added: list[ArchiveEntry] = field(default_factory=list)

    """Children present in `b` but not in `a` (matched by `path`)."""

    removed: list[ArchiveEntry] = field(default_factory=list)

    """Children present in `a` but not in `b` (matched by `path`)."""

    changed: list[EmbeddedDiff] = field(default_factory=list)

    """Children present in both but with differing content (matched by `path`)."""


@dataclass(frozen=True, slots=True)
class YakeParams:
    """YAKE-specific parameters."""

    window_size: int = 2

    """Window size for co-occurrence analysis (default: 2)."""


@dataclass(frozen=True, slots=True)
class RakeParams:
    """RAKE-specific parameters."""

    min_word_length: int = 1

    """Minimum word length to consider (default: 1)."""

    max_words_per_phrase: int = 3

    """Maximum words in a keyword phrase (default: 3)."""


@dataclass(frozen=True, slots=True)
class KeywordConfig:
    """Keyword extraction configuration."""

    algorithm: KeywordAlgorithm | str = "yake"

    """Algorithm to use for extraction."""

    max_keywords: int = 10

    """Maximum number of keywords to extract (default: 10)."""

    min_score: float = 0

    """Minimum score threshold (0.0-1.0, default: 0.0)."""

    language: str | None = "en"

    """Language code for stopword filtering (e.g., "en", "de", "fr")."""

    yake_params: YakeParams | None = None

    """YAKE-specific tuning parameters."""

    rake_params: RakeParams | None = None

    """RAKE-specific tuning parameters."""


@dataclass(frozen=True, slots=True)
class UserChunkConfig:
    """User-provided chunk configuration."""

    page_ranges: list[PageRange] | None = None

    """User-specified page ranges (overrides automatic chunking)."""

    pages_per_chunk: int | None = None

    """User-specified pages per chunk (overrides automatic calculation)."""

    force_chunking: bool = False

    """Force chunking even for small documents."""

    disable_chunking: bool = False

    """Disable chunking even for large documents."""


@dataclass(frozen=True, slots=True)
class HeuristicsConfig:
    """Configuration for document chunking and analysis heuristics."""

    enable_pdf_text_heuristics: bool = True

    """Enable PDF text-layer detection heuristics."""

    text_layer_threshold: float = 0.7

    """Minimum fraction of pages that must have text to skip chunking."""

    file_size_threshold_bytes: int = 10485760

    """File size threshold in bytes for considering chunking."""

    page_count_threshold: int = 50

    """Page count threshold for considering chunking."""

    target_pages_per_chunk: int = 10

    """Target number of pages per chunk for optimal parallel processing."""

    max_pages_per_chunk: int = 25

    """Hard cap on pages per chunk."""

    disk_processing_threshold_bytes: int = 52428800

    """File size threshold for disk-based processing."""

    min_chars_per_page: int = 50

    """Minimum characters per page to consider a page as having text."""

    max_xlsx_sheet_count: int = 200

    """Maximum sheet count allowed in an XLSX workbook."""

    max_xlsx_workbook_cells: int = 5000000

    """Maximum cell count (sheets x rows x columns approximation) in an XLSX workbook."""

    max_pptx_embedded_count: int = 50

    """Maximum number of OLE-embedded objects extractable from a single PPTX or DOCX."""


@dataclass(frozen=True, slots=True)
class MultidocThresholds:
    """Thresholds for multi-document boundary detection."""

    density_shift_threshold: float = 0.3

    """Text density difference threshold for `DensityShift` detection. Default: 0.3."""

    bigram_overlap_min: float = 0.1

    """Minimum bigram-overlap ratio below which a density shift is promoted to a `DensityShift` boundary.  Default: 0.1 (10 % overlap)."""


@dataclass(frozen=True, slots=True)
class PaddleOcrConfig:
    """Configuration for PaddleOCR backend."""

    language: str = ""

    """Language code (e.g., "en", "ch", "jpn", "kor", "deu", "fra")"""

    cache_dir: str | None = None

    """Optional custom cache directory for model files"""

    use_angle_cls: bool = False

    """Enable angle classification for rotated text (default: false). Can misfire on short text regions, rotating crops incorrectly before recognition."""

    enable_table_detection: bool = False

    """Enable table structure detection (default: false)"""

    det_db_thresh: float = 0.0

    """Database threshold for text detection (default: 0.3) Range: 0.0-1.0, higher values require more confident detections"""

    det_db_box_thresh: float = 0.0

    """Box threshold for text bounding box refinement (default: 0.5) Range: 0.0-1.0"""

    det_db_unclip_ratio: float = 0.0

    """Unclip ratio for expanding text bounding boxes (default: 1.6) Controls the expansion of detected text regions"""

    det_limit_side_len: int = 0

    """Maximum side length for detection image (default: 960) Larger images may be resized to this limit for faster inference"""

    rec_batch_num: int = 0

    """Batch size for recognition inference (default: 6) Number of text regions to process simultaneously"""

    padding: int = 0

    """Padding in pixels added around the image before detection (default: 10). Large values can include surrounding content like table gridlines."""

    drop_score: float = 0.0

    """Minimum recognition confidence score for text lines (default: 0.5). Text regions with recognition confidence below this threshold are discarded. Matches PaddleOCR Python's `drop_score` parameter. Range: 0.0-1.0"""

    model_tier: str = ""

    """Model tier controlling detection/recognition model size and accuracy trade-off."""

    model_version: str = ""

    """Model generation: `"pp-ocrv6"` (default) or `"pp-ocrv5"`."""


@dataclass(frozen=True, slots=True)
class PdfMetadata:
    """PDF-specific metadata."""

    pdf_version: str | None = None

    """PDF version (e.g., "1.7", "2.0")"""

    producer: str | None = None

    """PDF producer (application that created the PDF)"""

    is_encrypted: bool | None = None

    """Whether the PDF is encrypted/password-protected"""

    width: int | None = None

    """First page width in points (1/72 inch)"""

    height: int | None = None

    """First page height in points (1/72 inch)"""

    page_count: int | None = None

    """Total number of pages in the PDF document"""


@dataclass(frozen=True, slots=True)
class ProxyConfig:
    """Proxy configuration for HTTP requests."""

    url: str = ""

    """Proxy URL (e.g. "http://proxy:8080", "socks5://proxy:1080")."""

    username: str | None = None

    """Optional username for proxy authentication."""

    password: str | None = None

    """Optional password for proxy authentication."""


@dataclass(frozen=True, slots=True)
class ContentConfig:
    """Content extraction and conversion configuration."""

    output_format: str = "markdown"

    """Output format: `"markdown"` (default), `"plain"`, `"djot"`."""

    preprocessing_preset: str = "standard"

    """Preprocessing aggressiveness: `"minimal"`, `"standard"` (default), `"aggressive"`."""

    remove_navigation: bool = True

    """Remove navigation elements (nav, breadcrumbs, menus). Default: `true`."""

    remove_forms: bool = True

    """Remove form elements. Default: `true`."""

    strip_tags: list[str] = field(default_factory=list)

    """HTML tag names to strip (render children only, remove the tag wrapper). Default: `["noscript"]`."""

    preserve_tags: list[str] = field(default_factory=list)

    """HTML tag names to preserve as raw HTML in output."""

    exclude_selectors: list[str] = field(default_factory=list)

    """CSS selectors for elements to exclude entirely (element + all content)."""

    skip_images: bool = False

    """Skip image elements in output. Default: `false`."""

    max_depth: int | None = None

    """Max DOM traversal depth. Prevents stack overflow on deeply nested HTML."""

    wrap: bool = False

    """Enable line wrapping. Default: `false`."""

    wrap_width: int = 80

    """Wrap width when `wrap` is enabled. Default: `80`."""

    include_document_structure: bool = True

    """Include document structure tree in output. Default: `true`."""


@dataclass(frozen=True, slots=True)
class BrowserConfig:
    """Browser fallback configuration."""

    mode: BrowserMode | str = "auto"

    """When to use the headless browser fallback."""

    backend: BrowserBackend | str = "chromiumoxide"

    """Browser backend used to render JavaScript-heavy pages."""

    endpoint: str | None = None

    """CDP WebSocket endpoint for connecting to an external browser instance."""

    timeout: int = 30000

    """Timeout for browser page load and rendering (in milliseconds when serialized)."""

    wait: BrowserWait | str = "network_idle"

    """Wait strategy after browser navigation."""

    wait_selector: str | None = None

    """CSS selector to wait for when `wait` is `Selector`."""

    extra_wait: int | None = None

    """Extra time to wait after the wait condition is met."""

    proxy: ProxyConfig | None = None

    """Proxy for browser fetches. Overrides `CrawlConfig.proxy` when set. Native backend supports http/https only (no SOCKS5)."""

    block_url_patterns: list[str] = field(default_factory=list)

    """URL patterns to block before the network request fires. Supports `*` wildcards. Useful for skipping ads/analytics/large images. Honored by `BrowserBackend::Native`; chromiumoxide ignores this field today."""

    eval_script: str | None = None

    """JavaScript snippet evaluated after navigation completes."""

    robots_user_agent: str | None = None

    """User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent` (or crawlberg's default) if unset. Native only."""

    capture_network_events: bool = False

    """Capture the full network event stream into the result. Default false (only the document event is captured). Native only."""

    session_affinity: bool = True

    """Enable session affinity: reuse chromiumoxide Pages for same-domain requests so cookies + fingerprint + solved challenges persist. Default: true. When false, each request gets a fresh Page."""


@dataclass(frozen=True, slots=True)
class CrawlConfig:
    """Configuration for crawl, scrape, and map operations."""

    max_depth: int | None = None

    """Maximum crawl depth (number of link hops from the start URL)."""

    max_pages: int | None = None

    """Maximum number of pages to crawl."""

    max_concurrent: int | None = None

    """Maximum number of concurrent requests."""

    respect_robots_txt: bool = False

    """Whether to respect robots.txt directives."""

    soft_http_errors: bool = False

    """When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF blocks) are surfaced as `ScrapeResult` records with the matching `status_code` rather than raised as `CrawlError`. Default `false` preserves the historical throw-on-error contract for direct fetches. Independently of this flag, 404s reached at the end of a redirect chain are *always* surfaced softly -- the user opted into redirect-following, so receiving a 404 there is part of the normal flow rather than an unexpected error."""

    user_agent: str | None = None

    """Custom user-agent string."""

    stay_on_domain: bool = False

    """Whether to restrict crawling to the same domain."""

    allow_subdomains: bool = False

    """Whether to allow subdomains when `stay_on_domain` is true."""

    include_paths: list[str] = field(default_factory=list)

    """Regex patterns for paths to include during crawling."""

    exclude_paths: list[str] = field(default_factory=list)

    """Regex patterns for paths to exclude during crawling."""

    custom_headers: dict[str, str] = field(default_factory=dict)

    """Custom HTTP headers to send with each request."""

    request_timeout: int = 30000

    """Timeout for individual HTTP requests (in milliseconds when serialized)."""

    rate_limit_ms: int | None = None

    """Per-domain rate limit in milliseconds. When set, enforces a minimum delay between requests to the same domain. Defaults to 200ms when `None`."""

    max_redirects: int = 10

    """Maximum number of redirects to follow."""

    retry_count: int = 0

    """Number of retry attempts for failed requests."""

    retry_codes: list[int] = field(default_factory=list)

    """HTTP status codes that should trigger a retry."""

    cookies_enabled: bool = False

    """Whether to enable cookie handling."""

    auth: AuthConfig | None = None

    """Authentication configuration."""

    max_body_size: int | None = None

    """Maximum response body size in bytes."""

    remove_tags: list[str] = field(default_factory=list)

    """CSS selectors for tags to remove from HTML before processing."""

    content: ContentConfig | None = None

    """Content extraction and conversion configuration."""

    map_limit: int | None = None

    """Maximum number of URLs to return from a map operation."""

    map_search: str | None = None

    """Search filter for map results (case-insensitive substring match on URLs)."""

    download_assets: bool = False

    """Whether to download assets (CSS, JS, images, etc.) from the page."""

    asset_types: list[AssetCategory | str] = field(default_factory=list)

    """Filter for asset categories to download."""

    max_asset_size: int | None = None

    """Maximum size in bytes for individual asset downloads."""

    browser: BrowserConfig | None = None

    """Browser configuration."""

    proxy: ProxyConfig | None = None

    """Proxy configuration for HTTP requests."""

    user_agents: list[str] = field(default_factory=list)

    """List of user-agent strings for rotation. If non-empty, overrides `user_agent`."""

    capture_screenshot: bool = False

    """Whether to capture a screenshot when using the browser."""

    follow_document_urls: bool = False

    """Re-enqueue discovered `LinkType::Document` URLs into the crawl frontier so the crawl follows links *from* document pages (PDFs, etc.) as it would from HTML pages. Default: `false` (documents terminate at materialisation)."""

    document_url_depth: int | None = None

    """Maximum document-depth (from the seed URL through document links only) when `follow_document_urls` is true. `None` means inherit `max_depth`. Independent of `max_depth`: a document URL is enqueued only if BOTH the outer `max_depth` and (if set) `document_url_depth` permit it."""

    download_documents: bool = True

    """Whether to download non-HTML documents (PDF, DOCX, images, code, etc.) instead of skipping them."""

    document_max_size: int | None = 52428800

    """Maximum size in bytes for document downloads. Defaults to 50 MB."""

    document_mime_types: list[str] = field(default_factory=list)

    """Allowlist of MIME types to download. If empty, uses built-in defaults."""

    warc_output: str | None = None

    """Path to write WARC output. If `None`, WARC output is disabled."""

    browser_profile: str | None = None

    """Named browser profile for persistent sessions (cookies, localStorage)."""

    save_browser_profile: bool = False

    """Whether to save changes back to the browser profile on exit."""

    ssrf: SsrfPolicy | None = None

    """SSRF policy for outbound network requests. Default: deny private networks, allow http/https only, max 5 redirects."""


@dataclass(frozen=True, slots=True)
class SitemapUrl:
    """A URL entry from a sitemap."""

    url: str = ""

    """The URL."""

    lastmod: str | None = None

    """The last modification date, if present."""

    changefreq: str | None = None

    """The change frequency, if present."""

    priority: str | None = None

    """The priority, if present."""


@dataclass(frozen=True, slots=True)
class MapResult:
    """The result of a map operation, containing discovered URLs."""

    urls: list[SitemapUrl] = field(default_factory=list)

    """The list of discovered URLs."""


@dataclass(frozen=True, slots=True)
class SsrfPolicy:
    """SSRF policy configuration."""

    deny_private: bool = True

    """If true, reject URLs that resolve to private/metadata IP ranges."""

    max_redirects: int = 5

    """Maximum number of HTTP redirects to follow during validation."""


def _from_native_acceleration_config(native: Any) -> AccelerationConfig:
    """Build the public `AccelerationConfig` dataclass from the native binding object."""
    return AccelerationConfig(
        provider=native.provider,
        device_id=native.device_id,
    )


def _from_native_archive_metadata(native: Any) -> ArchiveMetadata:
    """Build the public `ArchiveMetadata` dataclass from the native binding object."""
    return ArchiveMetadata(
        format=native.format,
        file_count=native.file_count,
        file_list=native.file_list,
        total_size=native.total_size,
        compressed_size=native.compressed_size,
    )


def _from_native_audio_metadata(native: Any) -> AudioMetadata:
    """Build the public `AudioMetadata` dataclass from the native binding object."""
    return AudioMetadata(
        duration_ms=native.duration_ms,
        codec=native.codec,
        container=native.container,
        sample_rate_hz=native.sample_rate_hz,
        channels=native.channels,
        bitrate=native.bitrate,
    )


def _from_native_bibtex_metadata(native: Any) -> BibtexMetadata:
    """Build the public `BibtexMetadata` dataclass from the native binding object."""
    return BibtexMetadata(
        entry_count=native.entry_count,
        citation_keys=native.citation_keys,
        authors=native.authors,
        year_range=native.year_range,
        entry_types=native.entry_types,
    )


def _from_native_bounding_box(native: Any) -> BoundingBox:
    """Build the public `BoundingBox` dataclass from the native binding object."""
    return BoundingBox(
        x0=native.x0,
        y0=native.y0,
        x1=native.x1,
        y1=native.y1,
    )


def _from_native_browser_config(native: Any) -> BrowserConfig:
    """Build the public `BrowserConfig` dataclass from the native binding object."""
    return BrowserConfig(
        mode=native.mode,
        backend=native.backend,
        endpoint=native.endpoint,
        timeout=native.timeout,
        wait=native.wait,
        wait_selector=native.wait_selector,
        extra_wait=native.extra_wait,
        proxy=(None if native.proxy is None else _from_native_proxy_config(native.proxy)),
        block_url_patterns=native.block_url_patterns,
        eval_script=native.eval_script,
        robots_user_agent=native.robots_user_agent,
        capture_network_events=native.capture_network_events,
        session_affinity=native.session_affinity,
    )


def _from_native_chunking_config(native: Any) -> ChunkingConfig:
    """Build the public `ChunkingConfig` dataclass from the native binding object."""
    return ChunkingConfig(
        max_characters=native.max_characters,
        overlap=native.overlap,
        trim=native.trim,
        chunker_type=native.chunker_type,
        embedding=(None if native.embedding is None else _from_native_embedding_config(native.embedding)),
        preset=native.preset,
        sizing=native.sizing,
        prepend_heading_context=native.prepend_heading_context,
        topic_threshold=native.topic_threshold,
        table_chunking=native.table_chunking,
    )


def _from_native_citation_metadata(native: Any) -> CitationMetadata:
    """Build the public `CitationMetadata` dataclass from the native binding object."""
    return CitationMetadata(
        citation_count=native.citation_count,
        format=native.format,
        authors=native.authors,
        year_range=native.year_range,
        dois=native.dois,
        keywords=native.keywords,
    )


def _from_native_code_metadata(native: Any) -> CodeMetadata:
    """Build the public `CodeMetadata` dataclass from the native binding object."""
    return CodeMetadata(
        chunks=native.chunks,
    )


def _from_native_content_config(native: Any) -> ContentConfig:
    """Build the public `ContentConfig` dataclass from the native binding object."""
    return ContentConfig(
        output_format=native.output_format,
        preprocessing_preset=native.preprocessing_preset,
        remove_navigation=native.remove_navigation,
        remove_forms=native.remove_forms,
        strip_tags=native.strip_tags,
        preserve_tags=native.preserve_tags,
        exclude_selectors=native.exclude_selectors,
        skip_images=native.skip_images,
        max_depth=native.max_depth,
        wrap=native.wrap,
        wrap_width=native.wrap_width,
        include_document_structure=native.include_document_structure,
    )


def _from_native_content_filter_config(native: Any) -> ContentFilterConfig:
    """Build the public `ContentFilterConfig` dataclass from the native binding object."""
    return ContentFilterConfig(
        include_headers=native.include_headers,
        include_footers=native.include_footers,
        strip_repeating_text=native.strip_repeating_text,
        include_watermarks=native.include_watermarks,
    )


def _from_native_core_properties(native: Any) -> CoreProperties:
    """Build the public `CoreProperties` dataclass from the native binding object."""
    return CoreProperties(
        title=native.title,
        subject=native.subject,
        creator=native.creator,
        keywords=native.keywords,
        description=native.description,
        last_modified_by=native.last_modified_by,
        revision=native.revision,
        created=native.created,
        modified=native.modified,
        category=native.category,
        content_status=native.content_status,
        language=native.language,
        identifier=native.identifier,
        version=native.version,
        last_printed=native.last_printed,
    )


def _from_native_crawl_config(native: Any) -> CrawlConfig:
    """Build the public `CrawlConfig` dataclass from the native binding object."""
    return CrawlConfig(
        max_depth=native.max_depth,
        max_pages=native.max_pages,
        max_concurrent=native.max_concurrent,
        respect_robots_txt=native.respect_robots_txt,
        soft_http_errors=native.soft_http_errors,
        user_agent=native.user_agent,
        stay_on_domain=native.stay_on_domain,
        allow_subdomains=native.allow_subdomains,
        include_paths=native.include_paths,
        exclude_paths=native.exclude_paths,
        custom_headers=native.custom_headers,
        request_timeout=native.request_timeout,
        rate_limit_ms=native.rate_limit_ms,
        max_redirects=native.max_redirects,
        retry_count=native.retry_count,
        retry_codes=native.retry_codes,
        cookies_enabled=native.cookies_enabled,
        auth=native.auth,
        max_body_size=native.max_body_size,
        remove_tags=native.remove_tags,
        content=_from_native_content_config(native.content),
        map_limit=native.map_limit,
        map_search=native.map_search,
        download_assets=native.download_assets,
        asset_types=native.asset_types,
        max_asset_size=native.max_asset_size,
        browser=_from_native_browser_config(native.browser),
        proxy=(None if native.proxy is None else _from_native_proxy_config(native.proxy)),
        user_agents=native.user_agents,
        capture_screenshot=native.capture_screenshot,
        follow_document_urls=native.follow_document_urls,
        document_url_depth=native.document_url_depth,
        download_documents=native.download_documents,
        document_max_size=native.document_max_size,
        document_mime_types=native.document_mime_types,
        warc_output=native.warc_output,
        browser_profile=native.browser_profile,
        save_browser_profile=native.save_browser_profile,
        ssrf=_from_native_ssrf_policy(native.ssrf),
    )


def _from_native_csv_metadata(native: Any) -> CsvMetadata:
    """Build the public `CsvMetadata` dataclass from the native binding object."""
    return CsvMetadata(
        row_count=native.row_count,
        column_count=native.column_count,
        delimiter=native.delimiter,
        has_header=native.has_header,
        column_types=native.column_types,
    )


def _from_native_dbf_metadata(native: Any) -> DbfMetadata:
    """Build the public `DbfMetadata` dataclass from the native binding object."""
    return DbfMetadata(
        record_count=native.record_count,
        field_count=native.field_count,
        fields=native.fields,
    )


def _from_native_diff_options(native: Any) -> DiffOptions:
    """Build the public `DiffOptions` dataclass from the native binding object."""
    return DiffOptions(
        include_metadata=native.include_metadata,
        include_embedded=native.include_embedded,
        max_content_chars=native.max_content_chars,
    )


def _from_native_document_counts(native: Any) -> DocumentCounts:
    """Build the public `DocumentCounts` dataclass from the native binding object."""
    return DocumentCounts(
        pages=native.pages,
        tables=native.tables,
        images=native.images,
    )


def _from_native_document_structure(native: Any) -> DocumentStructure:
    """Build the public `DocumentStructure` dataclass from the native binding object."""
    return DocumentStructure(
        nodes=native.nodes,
        source_format=native.source_format,
        relationships=native.relationships,
        node_types=native.node_types,
    )


def _from_native_docx_app_properties(native: Any) -> DocxAppProperties:
    """Build the public `DocxAppProperties` dataclass from the native binding object."""
    return DocxAppProperties(
        application=native.application,
        app_version=native.app_version,
        template=native.template,
        total_time=native.total_time,
        pages=native.pages,
        words=native.words,
        characters=native.characters,
        characters_with_spaces=native.characters_with_spaces,
        lines=native.lines,
        paragraphs=native.paragraphs,
        company=native.company,
        doc_security=native.doc_security,
        scale_crop=native.scale_crop,
        links_up_to_date=native.links_up_to_date,
        shared_doc=native.shared_doc,
        hyperlinks_changed=native.hyperlinks_changed,
    )


def _from_native_docx_metadata(native: Any) -> DocxMetadata:
    """Build the public `DocxMetadata` dataclass from the native binding object."""
    return DocxMetadata(
        core_properties=(
            None if native.core_properties is None else _from_native_core_properties(native.core_properties)
        ),
        app_properties=(
            None if native.app_properties is None else _from_native_docx_app_properties(native.app_properties)
        ),
        custom_properties=native.custom_properties,
    )


def _from_native_email_config(native: Any) -> EmailConfig:
    """Build the public `EmailConfig` dataclass from the native binding object."""
    return EmailConfig(
        msg_fallback_codepage=native.msg_fallback_codepage,
    )


def _from_native_email_metadata(native: Any) -> EmailMetadata:
    """Build the public `EmailMetadata` dataclass from the native binding object."""
    return EmailMetadata(
        from_email=native.from_email,
        from_name=native.from_name,
        to_emails=native.to_emails,
        cc_emails=native.cc_emails,
        bcc_emails=native.bcc_emails,
        message_id=native.message_id,
        attachments=native.attachments,
    )


def _from_native_embedded_changes(native: Any) -> EmbeddedChanges:
    """Build the public `EmbeddedChanges` dataclass from the native binding object."""
    return EmbeddedChanges(
        added=native.added,
        removed=native.removed,
        changed=native.changed,
    )


def _from_native_embedding_config(native: Any) -> EmbeddingConfig:
    """Build the public `EmbeddingConfig` dataclass from the native binding object."""
    return EmbeddingConfig(
        model=native.model,
        normalize=native.normalize,
        batch_size=native.batch_size,
        show_download_progress=native.show_download_progress,
        cache_dir=native.cache_dir,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        max_embed_duration_secs=native.max_embed_duration_secs,
        max_sequence_length=native.max_sequence_length,
    )


def _from_native_epub_metadata(native: Any) -> EpubMetadata:
    """Build the public `EpubMetadata` dataclass from the native binding object."""
    return EpubMetadata(
        coverage=native.coverage,
        dc_format=native.dc_format,
        relation=native.relation,
        source=native.source,
        dc_type=native.dc_type,
        cover_image=native.cover_image,
    )


def _from_native_excel_metadata(native: Any) -> ExcelMetadata:
    """Build the public `ExcelMetadata` dataclass from the native binding object."""
    return ExcelMetadata(
        sheet_count=native.sheet_count,
        sheet_names=native.sheet_names,
    )


def _from_native_extract_input(native: Any) -> ExtractInput:
    """Build the public `ExtractInput` dataclass from the native binding object."""
    return ExtractInput(
        kind=native.kind,
        bytes=native.bytes,
        uri=native.uri,
        mime_type=native.mime_type,
        filename=native.filename,
        config=(None if native.config is None else _from_native_file_extraction_config(native.config)),
    )


def _from_native_extracted_image(native: Any) -> ExtractedImage:
    """Build the public `ExtractedImage` dataclass from the native binding object."""
    return ExtractedImage(
        data=native.data,
        format=native.format,
        image_index=native.image_index,
        page_number=native.page_number,
        width=native.width,
        height=native.height,
        colorspace=native.colorspace,
        bits_per_component=native.bits_per_component,
        is_mask=native.is_mask,
        description=native.description,
        ocr_result=native.ocr_result,
        bounding_box=(None if native.bounding_box is None else _from_native_bounding_box(native.bounding_box)),
        source_path=native.source_path,
        image_kind=native.image_kind,
        kind_confidence=native.kind_confidence,
        cluster_id=native.cluster_id,
        caption=native.caption,
        qr_codes=native.qr_codes,
        data_base64=native.data_base64,
    )


def _from_native_extraction_summary(native: Any) -> ExtractionSummary:
    """Build the public `ExtractionSummary` dataclass from the native binding object."""
    return ExtractionSummary(
        inputs=native.inputs,
        results=native.results,
        errors=native.errors,
        remote_urls=native.remote_urls,
        pages_crawled=native.pages_crawled,
        documents_downloaded=native.documents_downloaded,
    )


def _from_native_fiction_book_metadata(native: Any) -> FictionBookMetadata:
    """Build the public `FictionBookMetadata` dataclass from the native binding object."""
    return FictionBookMetadata(
        genres=native.genres,
        sequences=native.sequences,
        annotation=native.annotation,
    )


def _from_native_file_extraction_config(native: Any) -> FileExtractionConfig:
    """Build the public `FileExtractionConfig` dataclass from the native binding object."""
    return FileExtractionConfig(
        enable_quality_processing=native.enable_quality_processing,
        ocr=(None if native.ocr is None else _from_native_ocr_config(native.ocr)),
        force_ocr=native.force_ocr,
        force_ocr_pages=native.force_ocr_pages,
        disable_ocr=native.disable_ocr,
        chunking=(None if native.chunking is None else _from_native_chunking_config(native.chunking)),
        content_filter=(
            None if native.content_filter is None else _from_native_content_filter_config(native.content_filter)
        ),
        images=(None if native.images is None else _from_native_image_extraction_config(native.images)),
        pdf_options=(None if native.pdf_options is None else _from_native_pdf_config(native.pdf_options)),
        token_reduction=(
            None if native.token_reduction is None else _from_native_token_reduction_options(native.token_reduction)
        ),
        language_detection=(
            None
            if native.language_detection is None
            else _from_native_language_detection_config(native.language_detection)
        ),
        pages=(None if native.pages is None else _from_native_page_config(native.pages)),
        keywords=(None if native.keywords is None else _from_native_keyword_config(native.keywords)),
        postprocessor=(
            None if native.postprocessor is None else _from_native_post_processor_config(native.postprocessor)
        ),
        html_output=(None if native.html_output is None else _from_native_html_output_config(native.html_output)),
        result_format=native.result_format,
        output_format=native.output_format,
        include_document_structure=native.include_document_structure,
        layout=(None if native.layout is None else _from_native_layout_detection_config(native.layout)),
        transcription=(
            None if native.transcription is None else _from_native_transcription_config(native.transcription)
        ),
        timeout_secs=native.timeout_secs,
        tree_sitter=(None if native.tree_sitter is None else _from_native_tree_sitter_config(native.tree_sitter)),
        structured_extraction=native.structured_extraction,
        url=(None if native.url is None else _from_native_url_extraction_config(native.url)),
        ner=(None if native.ner is None else _from_native_ner_config(native.ner)),
        redaction=(None if native.redaction is None else _from_native_redaction_config(native.redaction)),
        summarization=(
            None if native.summarization is None else _from_native_summarization_config(native.summarization)
        ),
        translation=native.translation,
        page_classification=native.page_classification,
        captioning=native.captioning,
        qr_codes=native.qr_codes,
    )


def _from_native_footnote_config(native: Any) -> FootnoteConfig:
    """Build the public `FootnoteConfig` dataclass from the native binding object."""
    return FootnoteConfig(
        parse_citations=native.parse_citations,
    )


def _from_native_heuristics_config(native: Any) -> HeuristicsConfig:
    """Build the public `HeuristicsConfig` dataclass from the native binding object."""
    return HeuristicsConfig(
        enable_pdf_text_heuristics=native.enable_pdf_text_heuristics,
        text_layer_threshold=native.text_layer_threshold,
        file_size_threshold_bytes=native.file_size_threshold_bytes,
        page_count_threshold=native.page_count_threshold,
        target_pages_per_chunk=native.target_pages_per_chunk,
        max_pages_per_chunk=native.max_pages_per_chunk,
        disk_processing_threshold_bytes=native.disk_processing_threshold_bytes,
        min_chars_per_page=native.min_chars_per_page,
        max_xlsx_sheet_count=native.max_xlsx_sheet_count,
        max_xlsx_workbook_cells=native.max_xlsx_workbook_cells,
        max_pptx_embedded_count=native.max_pptx_embedded_count,
    )


def _from_native_hierarchy_config(native: Any) -> HierarchyConfig:
    """Build the public `HierarchyConfig` dataclass from the native binding object."""
    return HierarchyConfig(
        enabled=native.enabled,
        k_clusters=native.k_clusters,
        include_bbox=native.include_bbox,
        ocr_coverage_threshold=native.ocr_coverage_threshold,
    )


def _from_native_html_metadata(native: Any) -> HtmlMetadata:
    """Build the public `HtmlMetadata` dataclass from the native binding object."""
    return HtmlMetadata(
        title=native.title,
        description=native.description,
        keywords=native.keywords,
        author=native.author,
        canonical_url=native.canonical_url,
        base_href=native.base_href,
        language=native.language,
        text_direction=native.text_direction,
        open_graph=native.open_graph,
        twitter_card=native.twitter_card,
        meta_tags=native.meta_tags,
        headers=native.headers,
        links=native.links,
        images=native.images,
        structured_data=native.structured_data,
    )


def _from_native_html_output_config(native: Any) -> HtmlOutputConfig:
    """Build the public `HtmlOutputConfig` dataclass from the native binding object."""
    return HtmlOutputConfig(
        css=native.css,
        css_file=native.css_file,
        theme=native.theme,
        class_prefix=native.class_prefix,
        embed_css=native.embed_css,
    )


def _from_native_image_extraction_config(native: Any) -> ImageExtractionConfig:
    """Build the public `ImageExtractionConfig` dataclass from the native binding object."""
    return ImageExtractionConfig(
        extract_images=native.extract_images,
        target_dpi=native.target_dpi,
        max_image_dimension=native.max_image_dimension,
        inject_placeholders=native.inject_placeholders,
        auto_adjust_dpi=native.auto_adjust_dpi,
        min_dpi=native.min_dpi,
        max_dpi=native.max_dpi,
        max_images_per_page=native.max_images_per_page,
        classify=native.classify,
        include_page_rasters=native.include_page_rasters,
        run_ocr_on_images=native.run_ocr_on_images,
        ocr_text_only=native.ocr_text_only,
        append_ocr_text=native.append_ocr_text,
        output_format=native.output_format,
        svg=_from_native_svg_options(native.svg),
        include_data_base64=native.include_data_base64,
    )


def _from_native_image_metadata(native: Any) -> ImageMetadata:
    """Build the public `ImageMetadata` dataclass from the native binding object."""
    return ImageMetadata(
        width=native.width,
        height=native.height,
        format=native.format,
        exif=native.exif,
    )


def _from_native_image_preprocessing_config(native: Any) -> ImagePreprocessingConfig:
    """Build the public `ImagePreprocessingConfig` dataclass from the native binding object."""
    return ImagePreprocessingConfig(
        target_dpi=native.target_dpi,
        auto_rotate=native.auto_rotate,
        deskew=native.deskew,
        denoise=native.denoise,
        contrast_enhance=native.contrast_enhance,
        binarization_method=native.binarization_method,
        invert_colors=native.invert_colors,
    )


def _from_native_jats_metadata(native: Any) -> JatsMetadata:
    """Build the public `JatsMetadata` dataclass from the native binding object."""
    return JatsMetadata(
        copyright=native.copyright,
        license=native.license,
        history_dates=native.history_dates,
        contributor_roles=native.contributor_roles,
    )


def _from_native_keyword_config(native: Any) -> KeywordConfig:
    """Build the public `KeywordConfig` dataclass from the native binding object."""
    return KeywordConfig(
        algorithm=native.algorithm,
        max_keywords=native.max_keywords,
        min_score=native.min_score,
        language=native.language,
        yake_params=(None if native.yake_params is None else _from_native_yake_params(native.yake_params)),
        rake_params=(None if native.rake_params is None else _from_native_rake_params(native.rake_params)),
    )


def _from_native_language_detection_config(native: Any) -> LanguageDetectionConfig:
    """Build the public `LanguageDetectionConfig` dataclass from the native binding object."""
    return LanguageDetectionConfig(
        enabled=native.enabled,
        min_confidence=native.min_confidence,
        detect_multiple=native.detect_multiple,
    )


def _from_native_late_interaction_config(native: Any) -> LateInteractionConfig:
    """Build the public `LateInteractionConfig` dataclass from the native binding object."""
    return LateInteractionConfig(
        model=native.model,
        batch_size=native.batch_size,
        max_length=native.max_length,
        query_max_length=native.query_max_length,
        show_download_progress=native.show_download_progress,
        cache_dir=native.cache_dir,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        max_embed_duration_secs=native.max_embed_duration_secs,
    )


def _from_native_layout_detection_config(native: Any) -> LayoutDetectionConfig:
    """Build the public `LayoutDetectionConfig` dataclass from the native binding object."""
    return LayoutDetectionConfig(
        confidence_threshold=native.confidence_threshold,
        apply_heuristics=native.apply_heuristics,
        table_model=native.table_model,
        table_overlap_preference=native.table_overlap_preference,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        enable_chart_understanding=native.enable_chart_understanding,
    )


def _from_native_layout_region(native: Any) -> LayoutRegion:
    """Build the public `LayoutRegion` dataclass from the native binding object."""
    return LayoutRegion(
        class_name=native.class_name,
        confidence=native.confidence,
        bounding_box=_from_native_bounding_box(native.bounding_box),
        area_fraction=native.area_fraction,
    )


def _from_native_llm_config(native: Any) -> LlmConfig:
    """Build the public `LlmConfig` dataclass from the native binding object."""
    return LlmConfig(
        model=native.model,
        api_key=native.api_key,
        base_url=native.base_url,
        timeout_secs=native.timeout_secs,
        max_retries=native.max_retries,
        temperature=native.temperature,
        max_tokens=native.max_tokens,
    )


def _from_native_llm_usage(native: Any) -> LlmUsage:
    """Build the public `LlmUsage` dataclass from the native binding object."""
    return LlmUsage(
        model=native.model,
        source=native.source,
        input_tokens=native.input_tokens,
        output_tokens=native.output_tokens,
        total_tokens=native.total_tokens,
        estimated_cost=native.estimated_cost,
        finish_reason=native.finish_reason,
    )


def _from_native_map_result(native: Any) -> MapResult:
    """Build the public `MapResult` dataclass from the native binding object."""
    return MapResult(
        urls=[_from_native_sitemap_url(__v) for __v in native.urls],
    )


def _from_native_metadata(native: Any) -> Metadata:
    """Build the public `Metadata` dataclass from the native binding object."""
    return Metadata(
        title=native.title,
        subject=native.subject,
        authors=native.authors,
        keywords=native.keywords,
        language=native.language,
        created_at=native.created_at,
        modified_at=native.modified_at,
        created_by=native.created_by,
        modified_by=native.modified_by,
        pages=native.pages,
        format=native.format,
        image_preprocessing=native.image_preprocessing,
        json_schema=native.json_schema,
        error=native.error,
        extraction_duration_ms=native.extraction_duration_ms,
        category=native.category,
        tags=native.tags,
        document_version=native.document_version,
        abstract_text=native.abstract_text,
        output_format=native.output_format,
        ocr_used=native.ocr_used,
        additional=native.additional,
    )


def _from_native_multidoc_thresholds(native: Any) -> MultidocThresholds:
    """Build the public `MultidocThresholds` dataclass from the native binding object."""
    return MultidocThresholds(
        density_shift_threshold=native.density_shift_threshold,
        bigram_overlap_min=native.bigram_overlap_min,
    )


def _from_native_ner_config(native: Any) -> NerConfig:
    """Build the public `NerConfig` dataclass from the native binding object."""
    return NerConfig(
        backend=native.backend,
        categories=native.categories,
        model=native.model,
        llm=(None if native.llm is None else _from_native_llm_config(native.llm)),
        custom_labels=native.custom_labels,
    )


def _from_native_ocr_confidence(native: Any) -> OcrConfidence:
    """Build the public `OcrConfidence` dataclass from the native binding object."""
    return OcrConfidence(
        detection=native.detection,
        recognition=native.recognition,
    )


def _from_native_ocr_config(native: Any) -> OcrConfig:
    """Build the public `OcrConfig` dataclass from the native binding object."""
    return OcrConfig(
        enabled=native.enabled,
        backend=native.backend,
        language=native.language,
        tesseract_config=(
            None if native.tesseract_config is None else _from_native_tesseract_config(native.tesseract_config)
        ),
        output_format=native.output_format,
        paddle_ocr_config=native.paddle_ocr_config,
        backend_options=native.backend_options,
        element_config=(
            None if native.element_config is None else _from_native_ocr_element_config(native.element_config)
        ),
        quality_thresholds=(
            None
            if native.quality_thresholds is None
            else _from_native_ocr_quality_thresholds(native.quality_thresholds)
        ),
        pipeline=native.pipeline,
        auto_rotate=native.auto_rotate,
        vlm_fallback=native.vlm_fallback,
        vlm_config=(None if native.vlm_config is None else _from_native_llm_config(native.vlm_config)),
        vlm_prompt=native.vlm_prompt,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        tessdata_bytes=native.tessdata_bytes,
        tessdata_path=native.tessdata_path,
    )


def _from_native_ocr_element(native: Any) -> OcrElement:
    """Build the public `OcrElement` dataclass from the native binding object."""
    return OcrElement(
        text=native.text,
        geometry=native.geometry,
        confidence=_from_native_ocr_confidence(native.confidence),
        level=native.level,
        rotation=native.rotation,
        page_number=native.page_number,
        parent_id=native.parent_id,
        backend_metadata=native.backend_metadata,
    )


def _from_native_ocr_element_config(native: Any) -> OcrElementConfig:
    """Build the public `OcrElementConfig` dataclass from the native binding object."""
    return OcrElementConfig(
        include_elements=native.include_elements,
        min_level=native.min_level,
        min_confidence=native.min_confidence,
        build_hierarchy=native.build_hierarchy,
    )


def _from_native_ocr_extraction_result(native: Any) -> OcrExtractionResult:
    """Build the public `OcrExtractionResult` dataclass from the native binding object."""
    return OcrExtractionResult(
        content=native.content,
        mime_type=native.mime_type,
        metadata=native.metadata,
        tables=native.tables,
        ocr_elements=(
            None if native.ocr_elements is None else [_from_native_ocr_element(__v) for __v in native.ocr_elements]
        ),
    )


def _from_native_ocr_metadata(native: Any) -> OcrMetadata:
    """Build the public `OcrMetadata` dataclass from the native binding object."""
    return OcrMetadata(
        language=native.language,
        psm=native.psm,
        output_format=native.output_format,
        table_count=native.table_count,
        table_rows=native.table_rows,
        table_cols=native.table_cols,
    )


def _from_native_ocr_quality_thresholds(native: Any) -> OcrQualityThresholds:
    """Build the public `OcrQualityThresholds` dataclass from the native binding object."""
    return OcrQualityThresholds(
        min_total_non_whitespace=native.min_total_non_whitespace,
        min_non_whitespace_per_page=native.min_non_whitespace_per_page,
        min_meaningful_word_len=native.min_meaningful_word_len,
        min_meaningful_words=native.min_meaningful_words,
        min_alnum_ratio=native.min_alnum_ratio,
        min_garbage_chars=native.min_garbage_chars,
        max_fragmented_word_ratio=native.max_fragmented_word_ratio,
        critical_fragmented_word_ratio=native.critical_fragmented_word_ratio,
        min_avg_word_length=native.min_avg_word_length,
        min_words_for_avg_length_check=native.min_words_for_avg_length_check,
        min_consecutive_repeat_ratio=native.min_consecutive_repeat_ratio,
        min_words_for_repeat_check=native.min_words_for_repeat_check,
        substantive_min_chars=native.substantive_min_chars,
        non_text_min_chars=native.non_text_min_chars,
        alnum_ws_ratio_threshold=native.alnum_ws_ratio_threshold,
        pipeline_min_quality=native.pipeline_min_quality,
    )


def _from_native_paddle_ocr_config(native: Any) -> PaddleOcrConfig:
    """Build the public `PaddleOcrConfig` dataclass from the native binding object."""
    return PaddleOcrConfig(
        language=native.language,
        cache_dir=native.cache_dir,
        use_angle_cls=native.use_angle_cls,
        enable_table_detection=native.enable_table_detection,
        det_db_thresh=native.det_db_thresh,
        det_db_box_thresh=native.det_db_box_thresh,
        det_db_unclip_ratio=native.det_db_unclip_ratio,
        det_limit_side_len=native.det_limit_side_len,
        rec_batch_num=native.rec_batch_num,
        padding=native.padding,
        drop_score=native.drop_score,
        model_tier=native.model_tier,
        model_version=native.model_version,
    )


def _from_native_page_config(native: Any) -> PageConfig:
    """Build the public `PageConfig` dataclass from the native binding object."""
    return PageConfig(
        extract_pages=native.extract_pages,
        insert_page_markers=native.insert_page_markers,
        marker_format=native.marker_format,
    )


def _from_native_pdf_config(native: Any) -> PdfConfig:
    """Build the public `PdfConfig` dataclass from the native binding object."""
    return PdfConfig(
        extract_images=native.extract_images,
        extract_tables=native.extract_tables,
        passwords=native.passwords,
        extract_metadata=native.extract_metadata,
        hierarchy=(None if native.hierarchy is None else _from_native_hierarchy_config(native.hierarchy)),
        extract_annotations=native.extract_annotations,
        top_margin_fraction=native.top_margin_fraction,
        bottom_margin_fraction=native.bottom_margin_fraction,
        allow_single_column_tables=native.allow_single_column_tables,
        ocr_inline_images=native.ocr_inline_images,
        extract_form_fields=native.extract_form_fields,
        reading_order=native.reading_order,
    )


def _from_native_pdf_metadata(native: Any) -> PdfMetadata:
    """Build the public `PdfMetadata` dataclass from the native binding object."""
    return PdfMetadata(
        pdf_version=native.pdf_version,
        producer=native.producer,
        is_encrypted=native.is_encrypted,
        width=native.width,
        height=native.height,
        page_count=native.page_count,
    )


def _from_native_post_processor_config(native: Any) -> PostProcessorConfig:
    """Build the public `PostProcessorConfig` dataclass from the native binding object."""
    return PostProcessorConfig(
        enabled=native.enabled,
        enabled_processors=native.enabled_processors,
        disabled_processors=native.disabled_processors,
        enabled_set=native.enabled_set,
        disabled_set=native.disabled_set,
    )


def _from_native_pptx_app_properties(native: Any) -> PptxAppProperties:
    """Build the public `PptxAppProperties` dataclass from the native binding object."""
    return PptxAppProperties(
        application=native.application,
        app_version=native.app_version,
        total_time=native.total_time,
        company=native.company,
        doc_security=native.doc_security,
        scale_crop=native.scale_crop,
        links_up_to_date=native.links_up_to_date,
        shared_doc=native.shared_doc,
        hyperlinks_changed=native.hyperlinks_changed,
        slides=native.slides,
        notes=native.notes,
        hidden_slides=native.hidden_slides,
        multimedia_clips=native.multimedia_clips,
        presentation_format=native.presentation_format,
        slide_titles=native.slide_titles,
    )


def _from_native_pptx_metadata(native: Any) -> PptxMetadata:
    """Build the public `PptxMetadata` dataclass from the native binding object."""
    return PptxMetadata(
        slide_count=native.slide_count,
        slide_names=native.slide_names,
        image_count=native.image_count,
        table_count=native.table_count,
    )


def _from_native_proxy_config(native: Any) -> ProxyConfig:
    """Build the public `ProxyConfig` dataclass from the native binding object."""
    return ProxyConfig(
        url=native.url,
        username=native.username,
        password=native.password,
    )


def _from_native_pst_metadata(native: Any) -> PstMetadata:
    """Build the public `PstMetadata` dataclass from the native binding object."""
    return PstMetadata(
        message_count=native.message_count,
    )


def _from_native_rake_params(native: Any) -> RakeParams:
    """Build the public `RakeParams` dataclass from the native binding object."""
    return RakeParams(
        min_word_length=native.min_word_length,
        max_words_per_phrase=native.max_words_per_phrase,
    )


def _from_native_redaction_config(native: Any) -> RedactionConfig:
    """Build the public `RedactionConfig` dataclass from the native binding object."""
    return RedactionConfig(
        categories=native.categories,
        strategy=native.strategy,
        ner=(None if native.ner is None else _from_native_ner_config(native.ner)),
        preserve_offsets=native.preserve_offsets,
        custom_terms=native.custom_terms,
        custom_patterns=native.custom_patterns,
    )


def _from_native_reranker_config(native: Any) -> RerankerConfig:
    """Build the public `RerankerConfig` dataclass from the native binding object."""
    return RerankerConfig(
        model=native.model,
        top_k=native.top_k,
        batch_size=native.batch_size,
        show_download_progress=native.show_download_progress,
        cache_dir=native.cache_dir,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        max_rerank_duration_secs=native.max_rerank_duration_secs,
    )


def _from_native_revision_delta(native: Any) -> RevisionDelta:
    """Build the public `RevisionDelta` dataclass from the native binding object."""
    return RevisionDelta(
        content=native.content,
        table_changes=native.table_changes,
        property_changes=native.property_changes,
    )


def _from_native_security_limits(native: Any) -> SecurityLimits:
    """Build the public `SecurityLimits` dataclass from the native binding object."""
    return SecurityLimits(
        max_archive_size=native.max_archive_size,
        max_compression_ratio=native.max_compression_ratio,
        max_files_in_archive=native.max_files_in_archive,
        max_nesting_depth=native.max_nesting_depth,
        max_entity_length=native.max_entity_length,
        max_content_size=native.max_content_size,
        max_iterations=native.max_iterations,
        max_xml_depth=native.max_xml_depth,
        max_table_cells=native.max_table_cells,
    )


def _from_native_server_config(native: Any) -> ServerConfig:
    """Build the public `ServerConfig` dataclass from the native binding object."""
    return ServerConfig(
        host=native.host,
        port=native.port,
        cors_origins=native.cors_origins,
        max_request_body_bytes=native.max_request_body_bytes,
        max_multipart_field_bytes=native.max_multipart_field_bytes,
    )


def _from_native_sitemap_url(native: Any) -> SitemapUrl:
    """Build the public `SitemapUrl` dataclass from the native binding object."""
    return SitemapUrl(
        url=native.url,
        lastmod=native.lastmod,
        changefreq=native.changefreq,
        priority=native.priority,
    )


def _from_native_sparse_embedding_config(native: Any) -> SparseEmbeddingConfig:
    """Build the public `SparseEmbeddingConfig` dataclass from the native binding object."""
    return SparseEmbeddingConfig(
        model=native.model,
        batch_size=native.batch_size,
        max_length=native.max_length,
        show_download_progress=native.show_download_progress,
        cache_dir=native.cache_dir,
        acceleration=(None if native.acceleration is None else _from_native_acceleration_config(native.acceleration)),
        max_embed_duration_secs=native.max_embed_duration_secs,
    )


def _from_native_ssrf_policy(native: Any) -> SsrfPolicy:
    """Build the public `SsrfPolicy` dataclass from the native binding object."""
    return SsrfPolicy(
        deny_private=native.deny_private,
        max_redirects=native.max_redirects,
    )


def _from_native_summarization_config(native: Any) -> SummarizationConfig:
    """Build the public `SummarizationConfig` dataclass from the native binding object."""
    return SummarizationConfig(
        strategy=native.strategy,
        max_tokens=native.max_tokens,
        llm=(None if native.llm is None else _from_native_llm_config(native.llm)),
    )


def _from_native_svg_options(native: Any) -> SvgOptions:
    """Build the public `SvgOptions` dataclass from the native binding object."""
    return SvgOptions(
        sanitize=native.sanitize,
        render_dpi=native.render_dpi,
    )


def _from_native_table(native: Any) -> Table:
    """Build the public `Table` dataclass from the native binding object."""
    return Table(
        cells=native.cells,
        markdown=native.markdown,
        page_number=native.page_number,
        bounding_box=(None if native.bounding_box is None else _from_native_bounding_box(native.bounding_box)),
    )


def _from_native_table_cell(native: Any) -> TableCell:
    """Build the public `TableCell` dataclass from the native binding object."""
    return TableCell(
        content=native.content,
        row_span=native.row_span,
        col_span=native.col_span,
        is_header=native.is_header,
    )


def _from_native_table_grid(native: Any) -> TableGrid:
    """Build the public `TableGrid` dataclass from the native binding object."""
    return TableGrid(
        rows=native.rows,
        cols=native.cols,
        cells=native.cells,
    )


def _from_native_tesseract_config(native: Any) -> TesseractConfig:
    """Build the public `TesseractConfig` dataclass from the native binding object."""
    return TesseractConfig(
        language=native.language,
        psm=native.psm,
        output_format=native.output_format,
        oem=native.oem,
        min_confidence=native.min_confidence,
        preprocessing=(
            None if native.preprocessing is None else _from_native_image_preprocessing_config(native.preprocessing)
        ),
        enable_table_detection=native.enable_table_detection,
        table_min_confidence=native.table_min_confidence,
        table_column_threshold=native.table_column_threshold,
        table_row_threshold_ratio=native.table_row_threshold_ratio,
        use_cache=native.use_cache,
        classify_use_pre_adapted_templates=native.classify_use_pre_adapted_templates,
        language_model_ngram_on=native.language_model_ngram_on,
        tessedit_dont_blkrej_good_wds=native.tessedit_dont_blkrej_good_wds,
        tessedit_dont_rowrej_good_wds=native.tessedit_dont_rowrej_good_wds,
        tessedit_enable_dict_correction=native.tessedit_enable_dict_correction,
        tessedit_char_whitelist=native.tessedit_char_whitelist,
        tessedit_char_blacklist=native.tessedit_char_blacklist,
        tessedit_use_primary_params_model=native.tessedit_use_primary_params_model,
        textord_space_size_is_variable=native.textord_space_size_is_variable,
        thresholding_method=native.thresholding_method,
    )


def _from_native_text_metadata(native: Any) -> TextMetadata:
    """Build the public `TextMetadata` dataclass from the native binding object."""
    return TextMetadata(
        line_count=native.line_count,
        word_count=native.word_count,
        character_count=native.character_count,
        headers=native.headers,
    )


def _from_native_token_counter(_native: Any) -> TokenCounter:
    """Build the public `TokenCounter` dataclass from the native binding object."""
    return TokenCounter()


def _from_native_token_reduction_config(native: Any) -> TokenReductionConfig:
    """Build the public `TokenReductionConfig` dataclass from the native binding object."""
    return TokenReductionConfig(
        level=native.level,
        language_hint=native.language_hint,
        preserve_markdown=native.preserve_markdown,
        preserve_code=native.preserve_code,
        semantic_threshold=native.semantic_threshold,
        enable_parallel=native.enable_parallel,
        use_simd=native.use_simd,
        custom_stopwords=native.custom_stopwords,
        preserve_patterns=native.preserve_patterns,
        target_reduction=native.target_reduction,
        enable_semantic_clustering=native.enable_semantic_clustering,
    )


def _from_native_token_reduction_options(native: Any) -> TokenReductionOptions:
    """Build the public `TokenReductionOptions` dataclass from the native binding object."""
    return TokenReductionOptions(
        mode=native.mode,
        preserve_important_words=native.preserve_important_words,
    )


def _from_native_transcription_config(native: Any) -> TranscriptionConfig:
    """Build the public `TranscriptionConfig` dataclass from the native binding object."""
    return TranscriptionConfig(
        enabled=native.enabled,
        model=native.model,
        language=native.language,
        timestamps=native.timestamps,
        max_duration_ms=native.max_duration_ms,
        max_bytes=native.max_bytes,
        timeout_ms=native.timeout_ms,
        model_cache_dir=native.model_cache_dir,
        allow_network=native.allow_network,
        verify_hash=native.verify_hash,
    )


def _from_native_tree_sitter_config(native: Any) -> TreeSitterConfig:
    """Build the public `TreeSitterConfig` dataclass from the native binding object."""
    return TreeSitterConfig(
        enabled=native.enabled,
        cache_dir=native.cache_dir,
        languages=native.languages,
        groups=native.groups,
        process=_from_native_tree_sitter_process_config(native.process),
    )


def _from_native_tree_sitter_process_config(native: Any) -> TreeSitterProcessConfig:
    """Build the public `TreeSitterProcessConfig` dataclass from the native binding object."""
    return TreeSitterProcessConfig(
        structure=native.structure,
        imports=native.imports,
        exports=native.exports,
        comments=native.comments,
        docstrings=native.docstrings,
        symbols=native.symbols,
        diagnostics=native.diagnostics,
        chunk_max_size=native.chunk_max_size,
        content_mode=native.content_mode,
    )


def _from_native_url_extraction_config(native: Any) -> UrlExtractionConfig:
    """Build the public `UrlExtractionConfig` dataclass from the native binding object."""
    return UrlExtractionConfig(
        mode=native.mode,
        crawl=_from_native_crawl_config(native.crawl),
        document_url_pattern=native.document_url_pattern,
        max_document_urls_per_result=native.max_document_urls_per_result,
        max_total_urls=native.max_total_urls,
        allow_local_file_inputs=native.allow_local_file_inputs,
        allow_file_uris=native.allow_file_uris,
    )


def _from_native_user_chunk_config(native: Any) -> UserChunkConfig:
    """Build the public `UserChunkConfig` dataclass from the native binding object."""
    return UserChunkConfig(
        page_ranges=native.page_ranges,
        pages_per_chunk=native.pages_per_chunk,
        force_chunking=native.force_chunking,
        disable_chunking=native.disable_chunking,
    )


def _from_native_xlsx_app_properties(native: Any) -> XlsxAppProperties:
    """Build the public `XlsxAppProperties` dataclass from the native binding object."""
    return XlsxAppProperties(
        application=native.application,
        app_version=native.app_version,
        doc_security=native.doc_security,
        scale_crop=native.scale_crop,
        links_up_to_date=native.links_up_to_date,
        shared_doc=native.shared_doc,
        hyperlinks_changed=native.hyperlinks_changed,
        company=native.company,
        worksheet_names=native.worksheet_names,
    )


def _from_native_xml_metadata(native: Any) -> XmlMetadata:
    """Build the public `XmlMetadata` dataclass from the native binding object."""
    return XmlMetadata(
        element_count=native.element_count,
        unique_elements=native.unique_elements,
    )


def _from_native_yake_params(native: Any) -> YakeParams:
    """Build the public `YakeParams` dataclass from the native binding object."""
    return YakeParams(
        window_size=native.window_size,
    )
