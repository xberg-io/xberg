// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// One rejection-reason tally emitted by the redaction engine's
/// post-detection validators (see
/// `EntityValidator`).
/// </summary>
public sealed record RejectionCount
{
    /// <summary>
    /// Static reason identifier reported by the validator (e.g.
    /// `"iban_checksum_failed"`). Never contains the underlying PII text.
    /// </summary>
    [JsonPropertyName("reason")]
    public required string Reason { get; init; }

    /// <summary>
    /// Number of candidates rejected for this reason.
    /// </summary>
    [JsonPropertyName("count")]
    public uint Count { get; init; } = 0;


    /// <summary>
    /// Parse a <see cref="RejectionCount"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static RejectionCount FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<RejectionCount>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse RejectionCount from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse RejectionCount from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
