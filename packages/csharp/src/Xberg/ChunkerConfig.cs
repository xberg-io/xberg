// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for batch packing.
///
/// These are plain parameters with no environment or policy coupling.
/// </summary>
public sealed record ChunkerConfig
{
    /// <summary>
    /// Maximum input tokens per batch.
    /// </summary>
    [JsonPropertyName("max_input_tokens")]
    public uint MaxInputTokens { get; init; } = 0;

    /// <summary>
    /// Estimated tokens contributed by each image.
    /// </summary>
    [JsonPropertyName("avg_tokens_per_image")]
    public uint AvgTokensPerImage { get; init; } = 0;

    /// <summary>
    /// Characters-per-token divisor used to estimate text/image byte token counts.
    /// </summary>
    [JsonPropertyName("chars_per_token")]
    public ulong CharsPerToken { get; init; } = 0;


    /// <summary>
    /// Parse a <see cref="ChunkerConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static ChunkerConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ChunkerConfig>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse ChunkerConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse ChunkerConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static ChunkerConfig Default()
    {
        var nativeResult = NativeMethods.ChunkerConfigDefault();
        var jsonPtr = NativeMethods.ChunkerConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ChunkerConfigFree(nativeResult);
        return JsonSerializer.Deserialize<ChunkerConfig>(json ?? "null", JsonOptions)!;
    }
}
