// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Unified extraction input for all public extraction entry points.
/// </summary>
public sealed record ExtractInput
{
    /// <summary>
    /// Source kind. `bytes` requires `bytes`; `uri` requires `uri`.
    /// </summary>
    [JsonPropertyName("kind")]
    public ExtractInputKind Kind { get; init; } = ExtractInputKind.Uri;

    /// <summary>
    /// Raw bytes for `kind = "bytes"`.
    /// </summary>
    [JsonPropertyName("bytes")]
    public byte[]? Bytes { get; init; } = null;

    /// <summary>
    /// Local path, `file://` URI, or HTTP(S) URL for `kind = "uri"`.
    /// </summary>
    [JsonPropertyName("uri")]
    public string? Uri { get; init; } = null;

    /// <summary>
    /// MIME type hint.
    /// </summary>
    [JsonPropertyName("mime_type")]
    public string? MimeType { get; init; } = null;

    /// <summary>
    /// Filename hint used for MIME detection and metadata.
    /// </summary>
    [JsonPropertyName("filename")]
    public string? Filename { get; init; } = null;

    /// <summary>
    /// Per-input extraction overrides.
    /// </summary>
    [JsonPropertyName("config")]
    public FileExtractionConfig? Config { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="ExtractInput"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static ExtractInput FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ExtractInput>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse ExtractInput from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse ExtractInput from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    public static ExtractInput Default()
    {
        var nativeResult = NativeMethods.ExtractInputDefault();
        var jsonPtr = NativeMethods.ExtractInputToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ExtractInputFree(nativeResult);
        return JsonSerializer.Deserialize<ExtractInput>(json ?? "null", JsonOptions)!;
    }

    /// <summary>
    /// Build a bytes input with a MIME type and optional filename hint.
    /// </summary>
    public static ExtractInput Bytes(byte[] bytes, string mimeType, string? filename)
    {
        var bytesHandle = GCHandle.Alloc(bytes, GCHandleType.Pinned);
        try
        {
            var nativeResult = NativeMethods.ExtractInputBytes(bytesHandle.AddrOfPinnedObject(), mimeType, filename!);
            var jsonPtr = NativeMethods.ExtractInputToJson(nativeResult);
            var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ExtractInputFree(nativeResult);
            return JsonSerializer.Deserialize<ExtractInput>(json ?? "null", JsonOptions)!;
        }
        finally
        {
            if (bytesHandle.IsAllocated) bytesHandle.Free();
        }
    }

    /// <summary>
    /// Build a URI input from a local path, `file://` URI, or HTTP(S) URL.
    /// </summary>
    public static ExtractInput Uri(string uri)
    {
        var nativeResult = NativeMethods.ExtractInputUri(uri);
        var jsonPtr = NativeMethods.ExtractInputToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ExtractInputFree(nativeResult);
        return JsonSerializer.Deserialize<ExtractInput>(json ?? "null", JsonOptions)!;
    }
}
