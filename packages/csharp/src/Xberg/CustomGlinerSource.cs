// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Caller-supplied override pointing GLiNER at an arbitrary Hugging Face repo
/// instead of the pinned `xberg-io/gliner-models` catalog.
///
/// Files downloaded from a custom repo are **not** checksum-verified — the
/// catalog's `checksums.sha256` only covers the pinned models xberg publishes.
/// Callers choosing a custom repo are trusting that source directly.
/// </summary>
public sealed record CustomGlinerSource
{
    /// <summary>
    /// Hugging Face repo id, e.g. `"gliner-community/gliner_small-v2.5"`.
    /// </summary>
    [JsonPropertyName("repo")]
    public required string Repo { get; init; }

    /// <summary>
    /// Path to the ONNX model file within `repo`.
    /// </summary>
    [JsonPropertyName("model_file")]
    public required string ModelFile { get; init; }

    /// <summary>
    /// Path to the tokenizer file within `repo`.
    /// </summary>
    [JsonPropertyName("tokenizer_file")]
    public required string TokenizerFile { get; init; }

    /// <summary>
    /// Which GLiNER tensor I/O contract `model_file` uses.
    /// </summary>
    [JsonPropertyName("architecture")]
    public required GlinerArchitecture Architecture { get; init; }


    /// <summary>
    /// Parse a <see cref="CustomGlinerSource"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static CustomGlinerSource FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<CustomGlinerSource>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse CustomGlinerSource from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse CustomGlinerSource from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
