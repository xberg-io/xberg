// This file is auto-generated by alef. DO NOT EDIT.
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

internal sealed class EngineBuilderSafeHandle : SafeHandle
{
    internal EngineBuilderSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.EngineBuilderFree(handle);
        return true;
    }
}
/// <summary>/// Builder for `Engine`./// /// Each extension seam left unset is filled with its in-core default by/// `build`, so `Engine.new_default` produces an/// engine whose seams are exactly those defaults./// </summary>
public sealed class EngineBuilder : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly EngineBuilderSafeHandle _safeHandle;

    internal EngineBuilder(IntPtr handle)
    {
        _safeHandle = new EngineBuilderSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Finalize the builder into an `Engine`, filling every unset seam with
    /// its in-core default.
    /// </summary>
    public Engine Build()
    {
        var nativeResult = NativeMethods.EngineBuilderBuild(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "EngineBuilderBuild failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = new Engine(nativeResult);
        return returnValue;
    }
}
