// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Thresholds for the structured-extraction call-mode heuristic.
///
/// All defaults are **conservative starting points**.  Deployments should
/// measure their own document corpus and override via their own config;
/// these values are chosen to be safe-by-default, not to be optimal for
/// any particular workload.
///
/// Construct custom thresholds with struct-update syntax:
/// </summary>
public sealed record StructuredThresholds
{
    /// <summary>
    /// PDFs with `text_coverage` strictly below this are treated as scanned.
    ///
    /// **Conservative default: 0.10** — deployments override via their own
    /// config after measuring their document corpus.
    /// </summary>
    [JsonPropertyName("scan_max_coverage")]
    public double ScanMaxCoverage { get; init; } = 0.1;

    /// <summary>
    /// PDFs with `text_coverage` at or above this AND zero embedded images
    /// route to `StructuredCallMode.TextOnly`.
    ///
    /// **Conservative default: 0.90** — deployments override via their own
    /// config after measuring their document corpus.
    /// </summary>
    [JsonPropertyName("digital_min_coverage")]
    public double DigitalMinCoverage { get; init; } = 0.9;

    /// <summary>
    /// DOCX / HTML / text documents with `avg_chars_per_page` above this
    /// route to `StructuredCallMode.TextOnly`.
    ///
    /// **Conservative default: 200.0** — deployments override via their own
    /// config after measuring their document corpus.
    /// </summary>
    [JsonPropertyName("docx_text_min_density")]
    public double DocxTextMinDensity { get; init; } = 200.0;

    /// <summary>
    /// When `true`, emit `StructuredCallMode.TextOnlyWithVisionFallback`
    /// instead of `StructuredCallMode.TextOnly` so the orchestrator can
    /// escalate to vision on low confidence.
    ///
    /// **Conservative default: `false`** — must be explicitly enabled per
    /// deployment after bench validation; deployments override via their own
    /// config.
    /// </summary>
    [JsonPropertyName("enable_vision_fallback")]
    public bool EnableVisionFallback { get; init; } = false;


    /// <summary>
    /// Parse a <see cref="StructuredThresholds"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static StructuredThresholds FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<StructuredThresholds>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse StructuredThresholds from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse StructuredThresholds from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    public static StructuredThresholds Default()
    {
        var nativeResult = NativeMethods.StructuredThresholdsDefault();
        var jsonPtr = NativeMethods.StructuredThresholdsToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.StructuredThresholdsFree(nativeResult);
        return JsonSerializer.Deserialize<StructuredThresholds>(json ?? "null", JsonOptions)!;
    }
}
