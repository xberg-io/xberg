// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for the late-interaction (ColBERT) pipeline.
///
/// Controls which model to use, batching, and download/cache behavior for the
/// local ONNX ColBERT model.
///
/// Since v5.0.0.
/// </summary>
public sealed record LateInteractionConfig
{
    /// <summary>
    /// The late-interaction model to use (defaults to the "gte-moderncolbert" preset).
    /// </summary>
    [JsonPropertyName("model")]
    public LateInteractionModelType? Model { get; init; } = null;

    /// <summary>
    /// Batch size for local ONNX inference.
    ///
    /// ColBERT emits a `[seq, dim]` multi-vector embedding per document, so
    /// memory scales with batch size — keep this modest.
    /// </summary>
    [JsonPropertyName("batch_size")]
    public ulong BatchSize { get; init; } = 0;

    /// <summary>
    /// Maximum token sequence length for the tokenizer (documents).
    /// </summary>
    [JsonPropertyName("max_length")]
    public ulong MaxLength { get; init; } = 0;

    /// <summary>
    /// Fixed padded length for query augmentation.
    ///
    /// ColBERT queries are padded (with the mask token, kept attention-live)
    /// to exactly this many tokens rather than truncated/left as-is — this is
    /// the "query augmentation" trick from the ColBERT paper.
    /// </summary>
    [JsonPropertyName("query_max_length")]
    public ulong QueryMaxLength { get; init; } = 0;

    /// <summary>
    /// Show model download progress (local ONNX path only).
    /// </summary>
    [JsonPropertyName("show_download_progress")]
    public bool ShowDownloadProgress { get; init; } = false;

    /// <summary>
    /// Custom cache directory for model files.
    ///
    /// Defaults to `~/.cache/xberg/late-interaction/` if not specified.
    /// </summary>
    [JsonPropertyName("cache_dir")]
    public string? CacheDir { get; init; } = null;

    /// <summary>
    /// Hardware acceleration for the late-interaction ONNX model.
    /// </summary>
    [JsonPropertyName("acceleration")]
    public AccelerationConfig? Acceleration { get; init; } = null;

    /// <summary>
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `LateInteractionModelType.Plugin`. `None` disables the timeout.
    /// </summary>
    [JsonPropertyName("max_embed_duration_secs")]
    public ulong? MaxEmbedDurationSecs { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="LateInteractionConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static LateInteractionConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<LateInteractionConfig>(json, JsonOptions)
            ?? throw new XbergException($"Failed to parse LateInteractionConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse LateInteractionConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static LateInteractionConfig Default()
    {
        var nativeResult = NativeMethods.LateInteractionConfigDefault();
        var jsonPtr = NativeMethods.LateInteractionConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.LateInteractionConfigFree(nativeResult);
        return JsonSerializer.Deserialize<LateInteractionConfig>(json ?? "null", JsonOptions)!;
    }
}
