// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Thresholds for multi-document boundary detection.
///
/// All fields are public; callers override any subset via struct-update syntax.
/// </summary>
public sealed record MultidocThresholds
{
    /// <summary>
    /// Text density difference threshold for `DensityShift` detection.
    /// Default: 0.3.
    /// </summary>
    [JsonPropertyName("density_shift_threshold")]
    public float DensityShiftThreshold { get; init; } = 0.3f;

    /// <summary>
    /// Minimum bigram-overlap ratio below which a density shift is promoted to
    /// a `DensityShift` boundary.  Default: 0.1 (10 % overlap).
    /// </summary>
    [JsonPropertyName("bigram_overlap_min")]
    public float BigramOverlapMin { get; init; } = 0.1f;


    /// <summary>
    /// Parse a <see cref="MultidocThresholds"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static MultidocThresholds FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<MultidocThresholds>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse MultidocThresholds from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse MultidocThresholds from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    public static MultidocThresholds Default()
    {
        var nativeResult = NativeMethods.MultidocThresholdsDefault();
        var jsonPtr = NativeMethods.MultidocThresholdsToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.MultidocThresholdsFree(nativeResult);
        return JsonSerializer.Deserialize<MultidocThresholds>(json ?? "null", JsonOptions)!;
    }
}
