// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for the sparse-embedding pipeline.
///
/// Controls which model to use, batching, and download/cache behavior for the
/// local ONNX SPLADE model.
///
/// Since v5.0.0.
/// </summary>
public sealed record SparseEmbeddingConfig
{
    /// <summary>
    /// The sparse-embedding model to use (defaults to the "opensearch-v3-distill" preset).
    /// </summary>
    [JsonPropertyName("model")]
    public SparseEmbeddingModelType? Model { get; init; } = null;

    /// <summary>
    /// Batch size for local ONNX inference.
    ///
    /// SPLADE emits a `[seq, vocab]` logit tensor per document, so memory scales
    /// with batch size — keep this modest.
    /// </summary>
    [JsonPropertyName("batch_size")]
    public ulong BatchSize { get; init; } = 0;

    /// <summary>
    /// Maximum token sequence length for the tokenizer.
    /// </summary>
    [JsonPropertyName("max_length")]
    public ulong MaxLength { get; init; } = 0;

    /// <summary>
    /// Show model download progress (local ONNX path only).
    /// </summary>
    [JsonPropertyName("show_download_progress")]
    public bool ShowDownloadProgress { get; init; } = false;

    /// <summary>
    /// Custom cache directory for model files.
    ///
    /// Defaults to `~/.cache/xberg/sparse-embeddings/` if not specified.
    /// </summary>
    [JsonPropertyName("cache_dir")]
    public string? CacheDir { get; init; } = null;

    /// <summary>
    /// Hardware acceleration for the sparse-embedding ONNX model.
    /// </summary>
    [JsonPropertyName("acceleration")]
    public AccelerationConfig? Acceleration { get; init; } = null;

    /// <summary>
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `SparseEmbeddingModelType.Plugin`. `None` disables the timeout.
    /// </summary>
    [JsonPropertyName("max_embed_duration_secs")]
    public ulong? MaxEmbedDurationSecs { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="SparseEmbeddingConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static SparseEmbeddingConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<SparseEmbeddingConfig>(json, JsonOptions)
            ?? throw new XbergException($"Failed to parse SparseEmbeddingConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse SparseEmbeddingConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static SparseEmbeddingConfig Default()
    {
        var nativeResult = NativeMethods.SparseEmbeddingConfigDefault();
        var jsonPtr = NativeMethods.SparseEmbeddingConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.SparseEmbeddingConfigFree(nativeResult);
        return JsonSerializer.Deserialize<SparseEmbeddingConfig>(json ?? "null", JsonOptions)!;
    }
}
