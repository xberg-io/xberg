// This file is auto-generated by alef. DO NOT EDIT.
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

internal sealed class ParsedDocumentSafeHandle : SafeHandle
{
    internal ParsedDocumentSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.ParsedDocumentFree(handle);
        return true;
    }
}
/// <summary>/// A single-operation memo of a document's parse facts and rendered pages./// /// Cheaply shareable: the source bytes are held behind an `Arc`, and the/// render cache is interior-mutable so `rendered_pages`/// can take `&amp;self`./// </summary>
public sealed class ParsedDocument : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly ParsedDocumentSafeHandle _safeHandle;

    internal ParsedDocument(IntPtr handle)
    {
        _safeHandle = new ParsedDocumentSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Build a memo from shared source bytes: detect the MIME type and count
    /// pages (PDF via `pdf_page_count`; every other type is
    /// treated as a single page). Does not render — rendering is lazy.
    /// </summary>
    public static ParsedDocument FromBytes(byte[] source)
    {
        var sourceHandle = GCHandle.Alloc(source, GCHandleType.Pinned);
        var nativeResult = NativeMethods.ParsedDocumentFromBytes(
            sourceHandle.AddrOfPinnedObject(),
            (nuint)source.Length
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "ParsedDocumentFromBytes failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = new ParsedDocument(nativeResult);
        if (sourceHandle.IsAllocated) sourceHandle.Free();
        return returnValue;
    }

    /// <summary>
    /// The detected MIME type.
    /// </summary>
    public string MimeType()
    {
        var nativeResult = NativeMethods.ParsedDocumentMimeType(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "ParsedDocumentMimeType failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
        NativeMethods.FreeString(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// The page count (1 for non-PDF inputs).
    /// </summary>
    public uint PageCount()
    {
        var nativeResult = NativeMethods.ParsedDocumentPageCount(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// The shared source bytes.
    /// </summary>
    public byte[] Source()
    {
        var nativeResult = NativeMethods.ParsedDocumentSource(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "ParsedDocumentSource failed";
            throw new XbergException(ec, msg);
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<byte[]>(json ?? "null", JsonOptions)!;
        return returnValue;
    }
}
