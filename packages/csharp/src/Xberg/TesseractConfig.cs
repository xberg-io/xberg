// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3dc5e750756a36771a7f6eef2a99e079420641dfc95ee0b10e584496d7864c97
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Tesseract OCR configuration.
///
/// Provides fine-grained control over Tesseract OCR engine parameters.
/// Most users can use the defaults, but these settings allow optimization
/// for specific document types (invoices, handwriting, etc.).
///
/// **This is the public-facing counterpart of `ocr.types.TesseractConfig`
/// (the internal, engine-facing representation with `u8`/`String` fields instead of
/// `i32`/`Vec&lt;String&gt;`).** They are two independent struct definitions bridged only by
/// an explicit `From&lt;&amp;TesseractConfig&gt; for crate.ocr.types.TesseractConfig` impl in
/// `ocr/types.rs` — that conversion carries field *values* across, but each struct keeps
/// its own `Default` impl, and the conversion does nothing to keep those two defaults in
/// sync. Several production call sites (`extractors.image.apply_default_tesseract_psm`,
/// `configured_region_ocr`, `sparse_image_ocr_fallback_config`) construct *this* struct's
/// default and convert it, bypassing the internal struct's `Default` entirely — so if the
/// two defaults disagree, standalone image OCR silently uses this struct's value while
/// PDF-embedded OCR (which can reach the internal `Default` directly when no
/// `tesseract_config` is set) uses the other. When changing a default here, also update
/// `ocr.types.TesseractConfig.default`, and vice versa.
/// </summary>
public sealed record TesseractConfig
{
    /// <summary>
    /// Language code(s) for OCR recognition. For Tesseract, languages are joined with "+".
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs (Python, Node, PHP, WASM, etc.): `["eng", "deu"]`. When
    /// deserializing from a config file, JSON body, or the REST/MCP API, a
    /// single string is also accepted, either as one code ("eng") or
    /// "+"-joined ("eng+deu").
    /// </summary>
    [JsonPropertyName("language")]
    public List<string> Language { get; init; } = ["eng"];

    /// <summary>
    /// Page Segmentation Mode (1-13).
    ///
    /// PSM 0 is rejected: Tesseract's `PSM_OSD_ONLY` performs orientation and script
    /// detection with no character recognition, so it cannot satisfy a text-extraction
    /// request and previously yielded an empty document (GH#1586).
    ///
    /// `None` (the default) means the caller made no explicit choice: the extraction
    /// pipeline applies its own context-appropriate PSM (whole-image PSM 11, vertical-
    /// language PSM 5, layout-region PSM 6, or the sparse-text retry's PSM 3) exactly as
    /// it would with no `TesseractConfig` at all — see issue #1573. Setting any other
    /// field on this struct no longer changes that behaviour.
    ///
    /// Common explicit values:
    /// - 3: Fully automatic page segmentation (native engine default)
    /// - 6: Assume a single uniform block of text (WASM engine default — avoids
    ///   layout-analysis hang)
    /// - 11: Sparse text with no particular order
    /// </summary>
    [JsonPropertyName("psm")]
    public int? Psm { get; init; } = null;

    /// <summary>
    /// Output format ("text" or "markdown")
    /// </summary>
    [JsonPropertyName("output_format")]
    public string OutputFormat { get; init; } = "markdown";

    /// <summary>
    /// OCR Engine Mode (0-3).
    ///
    /// - 0: Legacy engine only
    /// - 1: Neural nets (LSTM) only (usually best)
    /// - 2: Legacy + LSTM
    /// - 3: Default (based on what's available)
    /// </summary>
    [JsonPropertyName("oem")]
    public int Oem { get; init; } = 3;

    /// <summary>
    /// Minimum confidence threshold (0.0-100.0).
    ///
    /// Words with confidence below this threshold may be rejected or flagged.
    /// </summary>
    [JsonPropertyName("min_confidence")]
    public double MinConfidence { get; init; } = 0.0;

    /// <summary>
    /// Image preprocessing configuration.
    ///
    /// Controls how images are preprocessed before OCR. Can significantly
    /// improve quality for scanned documents or low-quality images.
    /// </summary>
    [JsonPropertyName("preprocessing")]
    public ImagePreprocessingConfig? Preprocessing { get; init; } = null;

    /// <summary>
    /// Enable automatic table detection and reconstruction
    /// </summary>
    [JsonPropertyName("enable_table_detection")]
    public bool EnableTableDetection { get; init; } = true;

    /// <summary>
    /// Minimum confidence threshold for table detection (0.0-1.0)
    /// </summary>
    [JsonPropertyName("table_min_confidence")]
    public double TableMinConfidence { get; init; } = 0.0;

    /// <summary>
    /// Column threshold for table detection (pixels)
    /// </summary>
    [JsonPropertyName("table_column_threshold")]
    public int TableColumnThreshold { get; init; } = 50;

    /// <summary>
    /// Row threshold ratio for table detection (0.0-1.0)
    /// </summary>
    [JsonPropertyName("table_row_threshold_ratio")]
    public double TableRowThresholdRatio { get; init; } = 0.5;

    /// <summary>
    /// Enable OCR result caching
    /// </summary>
    [JsonPropertyName("use_cache")]
    public bool UseCache { get; init; } = true;

    /// <summary>
    /// Use pre-adapted templates for character classification
    /// </summary>
    [JsonPropertyName("classify_use_pre_adapted_templates")]
    public bool ClassifyUsePreAdaptedTemplates { get; init; } = true;

    /// <summary>
    /// Enable N-gram language model.
    ///
    /// Kept on by default (see `Self.default` and
    /// `ocr.types.TesseractConfig.language_model_ngram_on` for the rationale);
    /// keep this field's default in sync with the internal struct's.
    /// </summary>
    [JsonPropertyName("language_model_ngram_on")]
    public bool LanguageModelNgramOn { get; init; } = true;

    /// <summary>
    /// Don't reject good words during block-level processing
    /// </summary>
    [JsonPropertyName("tessedit_dont_blkrej_good_wds")]
    public bool TesseditDontBlkrejGoodWds { get; init; } = true;

    /// <summary>
    /// Don't reject good words during row-level processing
    /// </summary>
    [JsonPropertyName("tessedit_dont_rowrej_good_wds")]
    public bool TesseditDontRowrejGoodWds { get; init; } = true;

    /// <summary>
    /// Enable dictionary correction
    /// </summary>
    [JsonPropertyName("tessedit_enable_dict_correction")]
    public bool TesseditEnableDictCorrection { get; init; } = true;

    /// <summary>
    /// Whitelist of allowed characters (empty = all allowed)
    /// </summary>
    [JsonPropertyName("tessedit_char_whitelist")]
    public string TesseditCharWhitelist { get; init; } = "";

    /// <summary>
    /// Blacklist of forbidden characters (empty = none forbidden)
    /// </summary>
    [JsonPropertyName("tessedit_char_blacklist")]
    public string TesseditCharBlacklist { get; init; } = "";

    /// <summary>
    /// Use primary language params model
    /// </summary>
    [JsonPropertyName("tessedit_use_primary_params_model")]
    public bool TesseditUsePrimaryParamsModel { get; init; } = true;

    /// <summary>
    /// Variable-width space detection
    /// </summary>
    [JsonPropertyName("textord_space_size_is_variable")]
    public bool TextordSpaceSizeIsVariable { get; init; } = true;

    /// <summary>
    /// Use adaptive thresholding method
    /// </summary>
    [JsonPropertyName("thresholding_method")]
    public string ThresholdingMethod { get; init; } = "otsu";


    /// <summary>
    /// Parse a <see cref="TesseractConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static TesseractConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<TesseractConfig>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse TesseractConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse TesseractConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static TesseractConfig Default()
    {
        var nativeResult = NativeMethods.TesseractConfigDefault();
        var jsonPtr = NativeMethods.TesseractConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.TesseractConfigFree(nativeResult);
        return JsonSerializer.Deserialize<TesseractConfig>(json ?? "null", JsonOptions)!;
    }
}
