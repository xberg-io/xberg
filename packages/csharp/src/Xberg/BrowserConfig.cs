// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Browser fallback configuration.
/// </summary>
public sealed record BrowserConfig
{
    /// <summary>
    /// When to use the headless browser fallback.
    /// </summary>
    [JsonPropertyName("mode")]
    public BrowserMode Mode { get; init; } = BrowserMode.Auto;

    /// <summary>
    /// Browser backend used to render JavaScript-heavy pages.
    /// </summary>
    [JsonPropertyName("backend")]
    public BrowserBackend Backend { get; init; } = BrowserBackend.Chromiumoxide;

    /// <summary>
    /// CDP WebSocket endpoint for connecting to an external browser instance.
    /// </summary>
    [JsonPropertyName("endpoint")]
    public string? Endpoint { get; init; } = null;

    /// <summary>
    /// Timeout for browser page load and rendering (in milliseconds when serialized).
    /// </summary>
    [JsonPropertyName("timeout")]
    public ulong? Timeout { get; init; } = null;

    /// <summary>
    /// Wait strategy after browser navigation.
    /// </summary>
    [JsonPropertyName("wait")]
    public BrowserWait? Wait { get; init; } = null;

    /// <summary>
    /// CSS selector to wait for when `wait` is `Selector`.
    /// </summary>
    [JsonPropertyName("wait_selector")]
    public string? WaitSelector { get; init; } = null;

    /// <summary>
    /// Extra time to wait after the wait condition is met.
    /// </summary>
    [JsonPropertyName("extra_wait")]
    public ulong? ExtraWait { get; init; } = null;

    /// <summary>
    /// Proxy for browser fetches. Overrides `CrawlConfig.proxy` when set.
    /// Native backend supports http/https only (no SOCKS5).
    /// </summary>
    [JsonPropertyName("proxy")]
    public ProxyConfig? Proxy { get; init; } = null;

    /// <summary>
    /// URL patterns to block before the network request fires. Supports `*`
    /// wildcards. Useful for skipping ads/analytics/large images. Honored by
    /// `BrowserBackend.Native`; chromiumoxide ignores this field today.
    /// </summary>
    [JsonPropertyName("block_url_patterns")]
    public List<string> BlockUrlPatterns { get; init; } = [];

    /// <summary>
    /// JavaScript snippet evaluated after navigation completes.
    ///
    /// Scraping captures the native backend result in `ScrapeResult.browser.eval_result`.
    /// Interactions run this script before page actions on both browser backends but do
    /// not include the script result in `InteractionResult`.
    /// </summary>
    [JsonPropertyName("eval_script")]
    public string? EvalScript { get; init; } = null;

    /// <summary>
    /// User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent`
    /// (or crawlberg's default) if unset. Native only.
    /// </summary>
    [JsonPropertyName("robots_user_agent")]
    public string? RobotsUserAgent { get; init; } = null;

    /// <summary>
    /// Capture the full network event stream into the result. Default false
    /// (only the document event is captured). Native only.
    /// </summary>
    [JsonPropertyName("capture_network_events")]
    public bool CaptureNetworkEvents { get; init; } = false;

    /// <summary>
    /// Enable session affinity: reuse chromiumoxide Pages for same-domain
    /// requests so cookies + fingerprint + solved challenges persist.
    /// Default: true. When false, each request gets a fresh Page.
    /// </summary>
    [JsonPropertyName("session_affinity")]
    public bool SessionAffinity { get; init; } = true;


    /// <summary>
    /// Parse a <see cref="BrowserConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static BrowserConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<BrowserConfig>(json, JsonOptions)
            ?? throw new XbergException($"Failed to parse BrowserConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse BrowserConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
