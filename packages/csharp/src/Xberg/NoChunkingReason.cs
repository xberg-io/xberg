// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Reason for not chunking a document.
/// </summary>
    [JsonConverter(typeof(NoChunkingReasonJsonConverter))]
public abstract record NoChunkingReason
{
    /// <summary>
    /// File is below size threshold.
    /// </summary>
    public sealed record SmallFile(
        [property: JsonPropertyName("size_bytes")] ulong SizeBytes,
        [property: JsonPropertyName("threshold_bytes")] ulong ThresholdBytes
    ) : NoChunkingReason;

    /// <summary>
    /// Document has fewer pages than threshold.
    /// </summary>
    public sealed record FewPages(
        [property: JsonPropertyName("page_count")] uint PageCount,
        [property: JsonPropertyName("threshold")] uint Threshold
    ) : NoChunkingReason;

    /// <summary>
    /// PDF has substantial text layer (OCR not needed).
    /// </summary>
    public sealed record TextLayerDetected(
        [property: JsonPropertyName("text_coverage")] float TextCoverage,
        [property: JsonPropertyName("avg_chars_per_page")] uint AvgCharsPerPage
    ) : NoChunkingReason;

    /// <summary>
    /// Document format does not support chunking.
    /// </summary>
    public sealed record FormatNotChunkable(
        [property: JsonPropertyName("mime_type")] string MimeType
    ) : NoChunkingReason;

    /// <summary>
    /// Chunking is disabled by configuration.
    /// </summary>
    public sealed record ChunkingDisabled() : NoChunkingReason;

    /// <summary>
    /// Force OCR is disabled and text extraction is fast.
    /// </summary>
    public sealed record FastTextExtraction() : NoChunkingReason;

}

/// <summary>
/// Custom converter for NoChunkingReason sealed union with flattened variant fields.
/// </summary>
/// <remarks>
/// Handles JSON objects with a discriminator field (type) and variant-specific
/// fields at the same level. System.Text.Json's [JsonPolymorphic] cannot handle
/// this layout, so we manually deserialize here.
/// </remarks>
public sealed class NoChunkingReasonJsonConverter : JsonConverter<NoChunkingReason>
{
    public override NoChunkingReason Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException($"Expected JSON object, got {reader.TokenType}");
        }

        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;

        if (!root.TryGetProperty("type", out var tagElement))
        {
            throw new JsonException($"Missing discriminator field: type");
        }

        var tagValue = tagElement.GetString();
        if (tagValue == null)
        {
            throw new JsonException("Discriminator field is null");
        }

        // Tuple-variant records (`Variant(InnerStruct value)`) expect a single
        // "Value" field holding the inner struct's JSON, so wrap the remaining
        // fields under "Value". Struct-variant records (`Variant { field1,
        // field2 }`) have positional record components annotated with
        // [JsonPropertyName(...)] for each named field, so pass the remaining
        // fields through directly without the wrap.
        using var ms = new MemoryStream();
        using var writer = new Utf8JsonWriter(ms);
        writer.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "type")
            {
                writer.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writer);
            }
        }
        writer.WriteEndObject();
        writer.Flush();
        ms.Position = 0;
        var flatJson = ms.ToArray();

        using var msWrapped = new MemoryStream();
        using var writerWrapped = new Utf8JsonWriter(msWrapped);
        writerWrapped.WriteStartObject();
        writerWrapped.WritePropertyName("Value");
        writerWrapped.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "type")
            {
                writerWrapped.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writerWrapped);
            }
        }
        writerWrapped.WriteEndObject();
        writerWrapped.WriteEndObject();
        writerWrapped.Flush();
        msWrapped.Position = 0;
        var wrappedJson = msWrapped.ToArray();

        return tagValue switch
        {            "SmallFile" => JsonSerializer.Deserialize<NoChunkingReason.SmallFile>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            "FewPages" => JsonSerializer.Deserialize<NoChunkingReason.FewPages>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            "TextLayerDetected" => JsonSerializer.Deserialize<NoChunkingReason.TextLayerDetected>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            "FormatNotChunkable" => JsonSerializer.Deserialize<NoChunkingReason.FormatNotChunkable>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),            "ChunkingDisabled" => new NoChunkingReason.ChunkingDisabled(),            "FastTextExtraction" => new NoChunkingReason.FastTextExtraction(),            // Unknown discriminator: forward compatibility for unknown variants
            _ => throw new JsonException($"Unknown NoChunkingReason discriminator: {tagValue}")
        };
    }

    public override void Write(Utf8JsonWriter writer, NoChunkingReason value, JsonSerializerOptions options)
    {
        // Emit the discriminator tag plus the inner variant's fields flattened at
        // the same level — mirrors the Java sealed-union serializer pattern. Turn
        // `Message.User(UserMessage value)` into `{"type":"user","content":...}`
        // not `{"value":{...}}`. Without this, sending a chat request to FFI fails
        // with "missing field type" inside Rust serde.
        string tag;
        object? inner;
        switch (value)
        {            case NoChunkingReason.SmallFile v_smallfile:
                tag = "SmallFile";                inner = v_smallfile;                break;            case NoChunkingReason.FewPages v_fewpages:
                tag = "FewPages";                inner = v_fewpages;                break;            case NoChunkingReason.TextLayerDetected v_textlayerdetected:
                tag = "TextLayerDetected";                inner = v_textlayerdetected;                break;            case NoChunkingReason.FormatNotChunkable v_formatnotchunkable:
                tag = "FormatNotChunkable";                inner = v_formatnotchunkable;                break;            case NoChunkingReason.ChunkingDisabled _:
                tag = "ChunkingDisabled";
                inner = null;
                break;            case NoChunkingReason.FastTextExtraction _:
                tag = "FastTextExtraction";
                inner = null;
                break;            default:
                throw new JsonException($"Unknown NoChunkingReason variant: {value.GetType().Name}");
        }

        writer.WriteStartObject();
        writer.WriteString("type", tag);
        if (inner != null)
        {
            using var doc = JsonSerializer.SerializeToDocument(inner, inner.GetType(), options);
            if (doc.RootElement.ValueKind == JsonValueKind.Object)
            {
                foreach (var prop in doc.RootElement.EnumerateObject())
                {
                    writer.WritePropertyName(prop.Name);
                    prop.Value.WriteTo(writer);
                }
            }
        }
        writer.WriteEndObject();
    }
}
