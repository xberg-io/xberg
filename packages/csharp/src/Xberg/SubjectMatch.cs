// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// One vault match — either direction of lookup.
/// </summary>
public sealed record SubjectMatch
{
    [JsonPropertyName("token")]
    public required string Token { get; init; }

    [JsonPropertyName("original")]
    public required string Original { get; init; }

    /// <summary>
    /// Category parsed from the token's bracket contents (e.g. `"EMAIL"`
    /// from `"[EMAIL_1]"`), or `None` if the token doesn't follow the
    /// `"[CATEGORY_N]"` convention.
    /// </summary>
    [JsonPropertyName("category")]
    public string? Category { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="SubjectMatch"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static SubjectMatch FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<SubjectMatch>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse SubjectMatch from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse SubjectMatch from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
