// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Collections.Generic;

namespace Xberg;

internal sealed class RegistrySafeHandle : SafeHandle
{
    internal RegistrySafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.RegistryFree(handle);
        return true;
    }
}
/// <summary>/// Sorted map of preset id → `Preset`./// </summary>
public sealed class Registry : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly RegistrySafeHandle _safeHandle;

    internal Registry(IntPtr handle)
    {
        _safeHandle = new RegistrySafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Build the registry from preset files embedded at compile time under
    /// `src/presets/library/`. Validates every file against the meta-schema.
    /// </summary>
    public static Registry LoadEmbedded()
    {
        var nativeResult = NativeMethods.RegistryLoadEmbedded(

        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "RegistryLoadEmbedded failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = new Registry(nativeResult);
        return returnValue;
    }

    /// <summary>
    /// Look up a preset by its identifier.
    /// </summary>
    public Preset? Get(string id)
    {
        var nativeResult = NativeMethods.RegistryGet(
            Handle,
            id
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var jsonPtr = NativeMethods.PresetToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.PresetFree(nativeResult);
        var returnValue = JsonSerializer.Deserialize<Preset?>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Materialize a `PresetSummary` list for the public registry endpoint.
    /// </summary>
    public List<PresetSummary> Summaries()
    {
        var nativeResult = NativeMethods.RegistrySummaries(
            Handle
        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "RegistrySummaries failed";
            throw new XbergException(ec, msg);
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<List<PresetSummary>>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Number of presets currently loaded.
    /// </summary>
    public ulong Len()
    {
        var nativeResult = NativeMethods.RegistryLen(
            Handle
        );
        var returnValue = nativeResult;
        return returnValue;
    }

    /// <summary>
    /// Whether the registry contains zero presets.
    /// </summary>
    public bool IsEmpty()
    {
        var nativeResult = NativeMethods.RegistryIsEmpty(
            Handle
        );
        var returnValue = nativeResult != 0;
        return returnValue;
    }

    /// <summary>
    /// Read raw sample bytes for `&lt;preset_id&gt;` from
    /// `library/&lt;id&gt;/samples/&lt;name&gt;`. Returns `None` when the file is absent.
    /// </summary>
    public byte[]? SampleBytes(string presetId, string name)
    {
        var nativeResult = NativeMethods.RegistrySampleBytes(
            Handle,
            presetId,
            name
        );
        if (nativeResult == IntPtr.Zero)
        {
            return null;
        }
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult);
        NativeMethods.FreeString(nativeResult);
        var returnValue = JsonSerializer.Deserialize<byte[]?>(json ?? "null", JsonOptions)!;
        return returnValue;
    }

    /// <summary>
    /// Load additional preset files from a runtime directory and insert them
    /// into this registry.
    ///
    /// Reads every `*.json` file directly under `dir` (non-recursive),
    /// validates each against the meta-schema, and inserts it. Files that fail
    /// validation are rejected — the error is returned immediately and the
    /// registry is left in a partially-updated state. Existing entries with the
    /// same id are overwritten.
    ///
    /// Returns the number of presets successfully loaded from `dir`.
    ///
    /// # Use case
    ///
    /// This is the injection point for downstream catalogs that add curated
    /// presets on top of the single embedded OSS preset.
    /// </summary>
    public ulong ExtendFromDir(string dir)
    {
        var nativeResult = NativeMethods.RegistryExtendFromDir(
            Handle,
            dir
        );
        if (NativeMethods.LastErrorCode() != 0)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "RegistryExtendFromDir failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = nativeResult;
        return returnValue;
    }
}
