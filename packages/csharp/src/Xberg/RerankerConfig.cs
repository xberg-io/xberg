// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for the reranking pipeline.
///
/// Controls which model to use, how many results to return, and download/cache
/// behavior for local ONNX models.
///
/// Since v5.0.0.
/// </summary>
public sealed record RerankerConfig
{
    /// <summary>
    /// The reranker model to use (defaults to "balanced" preset if not specified).
    /// </summary>
    [JsonPropertyName("model")]
    public RerankerModelType? Model { get; init; } = null;

    /// <summary>
    /// Return at most this many documents. `None` returns all.
    ///
    /// Applied after sorting by score, so the highest-scoring documents are kept.
    /// </summary>
    [JsonPropertyName("top_k")]
    public ulong? TopK { get; init; } = null;

    /// <summary>
    /// Batch size for local ONNX cross-encoder inference.
    /// </summary>
    [JsonPropertyName("batch_size")]
    public ulong BatchSize { get; init; } = 32;

    /// <summary>
    /// Show model download progress (local ONNX path only).
    /// </summary>
    [JsonPropertyName("show_download_progress")]
    public bool ShowDownloadProgress { get; init; } = false;

    /// <summary>
    /// Custom cache directory for model files.
    ///
    /// Defaults to `~/.cache/xberg/rerankers/` if not specified.
    /// </summary>
    [JsonPropertyName("cache_dir")]
    public string? CacheDir { get; init; } = null;

    /// <summary>
    /// Hardware acceleration for the reranker ONNX model.
    ///
    /// Controls which execution provider (CPU, CUDA, CoreML, TensorRT) is used for
    /// local inference. Defaults to `None` (auto-select per platform).
    /// </summary>
    [JsonPropertyName("acceleration")]
    public AccelerationConfig? Acceleration { get; init; } = null;

    /// <summary>
    /// Maximum wall-clock duration (in seconds) for a single `rerank()` call when
    /// using `RerankerModelType.Plugin`.
    ///
    /// Applies only to the in-process plugin path — protects against hung
    /// host-language backends. On timeout, the dispatcher returns
    /// `Plugin` instead of blocking forever.
    ///
    /// `None` disables the timeout. The default (60 seconds) is conservative
    /// for common in-process inference; increase for large document sets on slow
    /// hardware.
    /// </summary>
    [JsonPropertyName("max_rerank_duration_secs")]
    public ulong? MaxRerankDurationSecs { get; init; } = null;


    /// <summary>
    /// Parse a <see cref="RerankerConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static RerankerConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<RerankerConfig>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse RerankerConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse RerankerConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    public static RerankerConfig Default()
    {
        var nativeResult = NativeMethods.RerankerConfigDefault();
        var jsonPtr = NativeMethods.RerankerConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.RerankerConfigFree(nativeResult);
        return JsonSerializer.Deserialize<RerankerConfig>(json ?? "null", JsonOptions)!;
    }
}
