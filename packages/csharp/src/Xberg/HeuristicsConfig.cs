// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for document chunking and analysis heuristics.
///
/// Every threshold is a public field so callers can override any subset via
/// struct-update syntax: `HeuristicsConfig { text_layer_threshold: 0.5, ..Default.default() }`.
/// </summary>
public sealed record HeuristicsConfig
{
    /// <summary>
    /// Enable PDF text-layer detection heuristics.
    ///
    /// When `true`, PDFs with a substantial text layer will skip chunking.
    /// Default: `true`.
    /// </summary>
    [JsonPropertyName("enable_pdf_text_heuristics")]
    public bool EnablePdfTextHeuristics { get; init; } = true;

    /// <summary>
    /// Minimum fraction of pages that must have text to skip chunking.
    ///
    /// Range `0.0..=1.0`. Default: `0.7` (70 % of pages).
    /// </summary>
    [JsonPropertyName("text_layer_threshold")]
    public float TextLayerThreshold { get; init; } = 0.7f;

    /// <summary>
    /// File size threshold in bytes for considering chunking.
    ///
    /// Files smaller than this are processed without chunking.
    /// Default: 10 MiB (10 × 1 024 × 1 024).
    /// </summary>
    [JsonPropertyName("file_size_threshold_bytes")]
    public ulong FileSizeThresholdBytes { get; init; } = 10485760;

    /// <summary>
    /// Page count threshold for considering chunking.
    ///
    /// Documents with fewer pages are processed without chunking.
    /// Default: 50.
    /// </summary>
    [JsonPropertyName("page_count_threshold")]
    public uint PageCountThreshold { get; init; } = 50;

    /// <summary>
    /// Target number of pages per chunk for optimal parallel processing.
    ///
    /// Default: 10.
    /// </summary>
    [JsonPropertyName("target_pages_per_chunk")]
    public uint TargetPagesPerChunk { get; init; } = 10;

    /// <summary>
    /// Hard cap on pages per chunk.
    ///
    /// No chunk will exceed this limit. Must be ≥ `target_pages_per_chunk`.
    /// Default: 25.
    /// </summary>
    [JsonPropertyName("max_pages_per_chunk")]
    public uint MaxPagesPerChunk { get; init; } = 25;

    /// <summary>
    /// File size threshold for disk-based processing.
    ///
    /// Files larger than this are buffered to disk to prevent OOM.
    /// Default: 50 MiB (50 × 1 024 × 1 024).
    /// </summary>
    [JsonPropertyName("disk_processing_threshold_bytes")]
    public ulong DiskProcessingThresholdBytes { get; init; } = 52428800;

    /// <summary>
    /// Minimum characters per page to consider a page as having text.
    ///
    /// Default: 50.
    /// </summary>
    [JsonPropertyName("min_chars_per_page")]
    public uint MinCharsPerPage { get; init; } = 50;

    /// <summary>
    /// Maximum sheet count allowed in an XLSX workbook.
    ///
    /// Workbooks beyond this are rejected pre-extraction to avoid OOM /
    /// abusive billing inflation. Default: 200.
    /// </summary>
    [JsonPropertyName("max_xlsx_sheet_count")]
    public uint MaxXlsxSheetCount { get; init; } = 200;

    /// <summary>
    /// Maximum cell count (sheets × rows × columns approximation) in an XLSX workbook.
    ///
    /// Default: 5 000 000 (≈ 200 sheets × 25 k cells).
    /// </summary>
    [JsonPropertyName("max_xlsx_workbook_cells")]
    public ulong MaxXlsxWorkbookCells { get; init; } = 5000000;

    /// <summary>
    /// Maximum number of OLE-embedded objects extractable from a single PPTX or DOCX.
    ///
    /// Protects against zip-bomb-style nested-document abuse. Default: 50.
    /// </summary>
    [JsonPropertyName("max_pptx_embedded_count")]
    public uint MaxPptxEmbeddedCount { get; init; } = 50;


    /// <summary>
    /// Parse a <see cref="HeuristicsConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static HeuristicsConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<HeuristicsConfig>(json, JsonOptions)
            ?? throw new XbergException($"Failed to parse HeuristicsConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse HeuristicsConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static HeuristicsConfig Default()
    {
        var nativeResult = NativeMethods.HeuristicsConfigDefault();
        var jsonPtr = NativeMethods.HeuristicsConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.HeuristicsConfigFree(nativeResult);
        return JsonSerializer.Deserialize<HeuristicsConfig>(json ?? "null", JsonOptions)!;
    }
}
