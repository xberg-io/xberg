// This file is auto-generated by alef. DO NOT EDIT.
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;

namespace Xberg;

internal sealed class DefaultModelProviderSafeHandle : SafeHandle
{
    internal DefaultModelProviderSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.DefaultModelProviderFree(handle);
        return true;
    }
}
/// <summary>/// In-core default: resolution via `LayoutModelManager`'s on-demand path./// /// Wraps the same manager the extraction pipeline uses, so a resolved path is/// identical to the manager's existing on-demand download/cache behavior. The/// blocking download runs on a blocking thread to keep the async call non-blocking./// </summary>
public sealed class DefaultModelProvider : IDisposable
{    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly DefaultModelProviderSafeHandle _safeHandle;

    internal DefaultModelProvider(IntPtr handle)
    {
        _safeHandle = new DefaultModelProviderSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    public static DefaultModelProvider Default()
    {
        var nativeResult = NativeMethods.DefaultModelProviderDefault(

        );
        if (nativeResult == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "DefaultModelProviderDefault failed";
            throw new XbergException(ec, msg);
        }
        var returnValue = new DefaultModelProvider(nativeResult);
        return returnValue;
    }

    public async Task<string> EnsureModelAsync(ModelId model)
    {
        var modelJson = JsonSerializer.Serialize(model, JsonSerializationOptions);
        var modelHandle = NativeMethods.ModelIdFromJson(modelJson);
        if (modelHandle == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "ModelIdFromJson failed";
            throw new XbergException(ec, msg);
        }
        return await Task.Run(() =>
        {
            var nativeResult = NativeMethods.DefaultModelProviderEnsureModel(
                Handle,
                modelHandle
            );
            if (nativeResult == IntPtr.Zero)
            {
                var ec = NativeMethods.LastErrorCode();
                var ctxPtr = NativeMethods.LastErrorContext();
                var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "DefaultModelProviderEnsureModel failed";
                throw new XbergException(ec, msg);
            }
            var returnValue = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(nativeResult) ?? string.Empty;
            NativeMethods.FreeString(nativeResult);
            if (modelHandle != global::System.IntPtr.Zero) NativeMethods.ModelIdFree(modelHandle);
            return returnValue;
        });
    }
}
