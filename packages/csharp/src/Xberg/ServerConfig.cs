// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2b3889aab60471e45a5be52b4e5bae65511369a182e0c194fa844c1865bdb693
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// API server configuration.
///
/// This struct holds all configuration options for the Xberg API server,
/// including host/port settings, CORS configuration, and upload limits.
///
/// # Defaults
///
/// - `host`: "127.0.0.1" (localhost only)
/// - `port`: 8000
/// - `cors_origins`: empty vector (allows all origins)
/// - `max_request_body_bytes`: 104_857_600 (100 MB)
/// - `max_multipart_field_bytes`: 104_857_600 (100 MB)
/// - `job_timeout_secs`: 600 (10 minutes)
/// </summary>
public sealed record ServerConfig
{
    /// <summary>
    /// Server host address (e.g., "127.0.0.1", "0.0.0.0")
    /// </summary>
    [JsonPropertyName("host")]
    public string Host { get; init; } = "127.0.0.1";

    /// <summary>
    /// Server port number
    /// </summary>
    [JsonPropertyName("port")]
    public ushort Port { get; init; } = 8000;

    /// <summary>
    /// CORS allowed origins. Empty vector means allow all origins.
    ///
    /// If this is an empty vector, the server will accept requests from any origin.
    /// If populated with specific origins (e.g., `"https://example.com"`), only
    /// those origins will be allowed.
    /// </summary>
    [JsonPropertyName("cors_origins")]
    public List<string> CorsOrigins { get; init; } = [];

    /// <summary>
    /// Maximum size of request body in bytes (default: 100 MB)
    /// </summary>
    [JsonPropertyName("max_request_body_bytes")]
    public ulong MaxRequestBodyBytes { get; init; } = 104857600;

    /// <summary>
    /// Maximum size of multipart fields in bytes (default: 100 MB)
    /// </summary>
    [JsonPropertyName("max_multipart_field_bytes")]
    public ulong MaxMultipartFieldBytes { get; init; } = 104857600;

    /// <summary>
    /// Fallback timeout, in seconds, for `POST /extract-async` jobs whose request does not
    /// pin down `extraction_timeout_secs` (default: 600, 10 minutes).
    ///
    /// A per-request `extraction_timeout_secs: Some(n)` always overrides this value. An
    /// explicit `extraction_timeout_secs: null` deliberately does NOT mean "run unbounded" —
    /// it still falls back to this server-configured cap, because an unbounded job on a
    /// shared server is a denial-of-service risk.
    /// </summary>
    [JsonPropertyName("job_timeout_secs")]
    public ulong JobTimeoutSecs { get; init; } = 600;


    /// <summary>
    /// Parse a <see cref="ServerConfig"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static ServerConfig FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ServerConfig>(json, JsonOptions)
                ?? throw new XbergException($"Failed to parse ServerConfig from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse ServerConfig from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    public static ServerConfig Default()
    {
        var nativeResult = NativeMethods.ServerConfigDefault();
        var jsonPtr = NativeMethods.ServerConfigToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.ServerConfigFree(nativeResult);
        return JsonSerializer.Deserialize<ServerConfig>(json ?? "null", JsonOptions)!;
    }
}
