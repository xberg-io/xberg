// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Unified extraction result envelope.
/// </summary>
public sealed record ExtractionResult
{
    /// <summary>
    /// Extracted documents in discovery order.
    /// </summary>
    [JsonPropertyName("results")]
    public List<ExtractedDocument> Results { get; init; } = [];

    /// <summary>
    /// Non-fatal per-input errors.
    /// </summary>
    [JsonPropertyName("errors")]
    public List<ExtractionErrorItem> Errors { get; init; } = [];

    /// <summary>
    /// Aggregate counts for the operation.
    /// </summary>
    [JsonPropertyName("summary")]
    public ExtractionSummary Summary { get; init; } = default!;

    /// <summary>
    /// Final URLs reached after redirects during URL ingestion.
    /// </summary>
    [JsonPropertyName("crawl_final_urls")]
    public List<string> CrawlFinalUrls { get; init; } = [];

    /// <summary>
    /// Total redirects followed while fetching or crawling URLs.
    /// </summary>
    [JsonPropertyName("crawl_redirect_count")]
    public ulong CrawlRedirectCount { get; init; } = 0;

    /// <summary>
    /// Unique normalized URLs discovered by crawls.
    /// </summary>
    [JsonPropertyName("crawl_unique_normalized_urls")]
    public List<string> CrawlUniqueNormalizedUrls { get; init; } = [];


    /// <summary>
    /// Parse a <see cref="ExtractionResult"/> from a JSON string.
    /// </summary>
    /// <exception cref="XbergException">When the JSON cannot be deserialised.</exception>
    public static ExtractionResult FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<ExtractionResult>(json, JsonOptions)
            ?? throw new XbergException($"Failed to parse ExtractionResult from JSON: deserializer returned null");
        }
        catch (XbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new XbergException($"Failed to parse ExtractionResult from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>
    /// Build an output containing one successful result.
    /// </summary>
    public static ExtractionResult Single(ExtractedDocument result)
    {
        var resultJson = JsonSerializer.Serialize(result, JsonSerializationOptions);
        var resultHandle = NativeMethods.ExtractedDocumentFromJson(resultJson);
        if (resultHandle == IntPtr.Zero)
        {
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "ExtractedDocumentFromJson failed";
            throw new XbergException(ec, msg);
        }
        try
        {
            var nativeResult = NativeMethods.ExtractionResultSingle(resultHandle);
            var jsonPtr = NativeMethods.ExtractionResultToJson(nativeResult);
            var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
            NativeMethods.FreeString(jsonPtr);
            NativeMethods.ExtractionResultFree(nativeResult);
            return JsonSerializer.Deserialize<ExtractionResult>(json ?? "null", JsonOptions)!;
        }
        finally
        {
            if (resultHandle != global::System.IntPtr.Zero) NativeMethods.ExtractedDocumentFree(resultHandle);
        }
    }
}
