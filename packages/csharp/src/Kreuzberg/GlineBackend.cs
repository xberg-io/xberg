// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:30817b344f365228e91c2755e89eaa207ea820052ef54bbd351aec533228d105
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// kreuzberg-gliner-rs ONNX backend wrapper.
///
/// Holds an initialised `GLiNER&lt;SpanMode&gt;` behind an `Arc&lt;Mutex&lt;...&gt;&gt;` so the
/// model can be safely shared across async tasks (inference is synchronous and
/// serialised internally by the mutex).
/// </summary>
public sealed record GlineBackend
{
    [JsonPropertyName("repo_id")]
    public required string RepoId { get; init; }

    [JsonPropertyName("model_path")]
    public required string ModelPath { get; init; }

    [JsonPropertyName("tokenizer_path")]
    public required string TokenizerPath { get; init; }


    /// <summary>
    /// Parse a <see cref="GlineBackend"/> from a JSON string.
    /// </summary>
    /// <exception cref="KreuzbergException">When the JSON cannot be deserialised.</exception>
    public static GlineBackend FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<GlineBackend>(json, JsonOptions)
                ?? throw new KreuzbergException($"Failed to parse GlineBackend from JSON: deserializer returned null");
        }
        catch (KreuzbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new KreuzbergException($"Failed to parse GlineBackend from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>
    /// Build a backend for `repo_id` (or the default model if `None`).
    /// </summary>
    public static GlineBackend New(string? repoId)
    {
        var nativeResult = NativeMethods.GlineBackendNew(repoId!);
        if (nativeResult == global::System.IntPtr.Zero) throw new KreuzbergException("Method New failed");
        var jsonPtr = NativeMethods.GlineBackendToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.GlineBackendFree(nativeResult);
        return JsonSerializer.Deserialize<GlineBackend>(json ?? "null", JsonOptions)!;
    }
}
