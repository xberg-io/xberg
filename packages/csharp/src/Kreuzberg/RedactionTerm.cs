// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// One user-supplied literal term to redact.
///
/// Matched as a regex-escaped substring (so callers do not need to escape
/// metacharacters themselves). Case-insensitive by default — set
/// `Self.case_sensitive` to `true` for exact byte-match semantics.
/// </summary>
public sealed record RedactionTerm
{
    /// <summary>
    /// Custom category label surfaced in `RedactionFinding.category`(crate.types.redaction.RedactionFinding.category).
    /// </summary>
    [JsonPropertyName("label")]
    public required string Label { get; init; }

    /// <summary>
    /// Literal value to match. Regex metacharacters are escaped automatically.
    /// </summary>
    [JsonPropertyName("value")]
    public required string Value { get; init; }

    /// <summary>
    /// When `true`, match the value as-is; otherwise match ASCII-case-insensitively.
    /// </summary>
    [JsonPropertyName("case_sensitive")]
    public bool CaseSensitive { get; init; } = false;


    /// <summary>
    /// Parse a <see cref="RedactionTerm"/> from a JSON string.
    /// </summary>
    /// <exception cref="KreuzbergException">When the JSON cannot be deserialised.</exception>
    public static RedactionTerm FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<RedactionTerm>(json, JsonOptions)
                ?? throw new KreuzbergException($"Failed to parse RedactionTerm from JSON: deserializer returned null");
        }
        catch (KreuzbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new KreuzbergException($"Failed to parse RedactionTerm from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>
    /// Build a term whose label is the literal value itself (case-insensitive).
    /// </summary>
    public static RedactionTerm Literal(string value)
    {
        var nativeResult = NativeMethods.RedactionTermLiteral(value);
        var jsonPtr = NativeMethods.RedactionTermToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.RedactionTermFree(nativeResult);
        return JsonSerializer.Deserialize<RedactionTerm>(json ?? "null", JsonOptions)!;
    }

    /// <summary>
    /// Build a term with a custom label.
    /// </summary>
    public static RedactionTerm Labeled(string label, string value)
    {
        var nativeResult = NativeMethods.RedactionTermLabeled(label, value);
        var jsonPtr = NativeMethods.RedactionTermToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.RedactionTermFree(nativeResult);
        return JsonSerializer.Deserialize<RedactionTerm>(json ?? "null", JsonOptions)!;
    }
}
