// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// Metadata for a bundled reranker preset.
///
/// All string fields are owned `String` for FFI compatibility — instances are
/// safe to clone and pass across language boundaries.
///
/// Since v5.0.0.
/// </summary>
public sealed record RerankerPreset
{
    /// <summary>
    /// Short identifier (catalog name, e.g. `"bge-reranker-base"`).
    /// </summary>
    [JsonPropertyName("name")]
    public required string Name { get; init; }

    /// <summary>
    /// HuggingFace repository name for the model.
    /// </summary>
    [JsonPropertyName("model_repo")]
    public required string ModelRepo { get; init; }

    /// <summary>
    /// Path to the ONNX model file within the repo.
    /// </summary>
    [JsonPropertyName("model_file")]
    public required string ModelFile { get; init; }

    /// <summary>
    /// Sibling files that must be downloaded alongside `model_file`.
    ///
    /// Empty for most presets. Used by repos that split the weight blob —
    /// e.g. `rozgo/bge-reranker-v2-m3` ships the model in `model.onnx` plus a
    /// co-located `model.onnx.data` payload.
    /// </summary>
    [JsonPropertyName("additional_files")]
    public List<string> AdditionalFiles { get; init; } = [];

    /// <summary>
    /// Maximum token sequence length the model supports.
    /// </summary>
    [JsonPropertyName("max_length")]
    public ulong MaxLength { get; init; } = 0;

    /// <summary>
    /// Human-readable description of the preset's intended use case.
    /// </summary>
    [JsonPropertyName("description")]
    public required string Description { get; init; }


    /// <summary>
    /// Parse a <see cref="RerankerPreset"/> from a JSON string.
    /// </summary>
    /// <exception cref="KreuzbergException">When the JSON cannot be deserialised.</exception>
    public static RerankerPreset FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<RerankerPreset>(json, JsonOptions)
                ?? throw new KreuzbergException($"Failed to parse RerankerPreset from JSON: deserializer returned null");
        }
        catch (KreuzbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new KreuzbergException($"Failed to parse RerankerPreset from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
}
