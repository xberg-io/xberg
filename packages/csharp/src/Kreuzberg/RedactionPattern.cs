// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// One user-supplied regex pattern to redact.
///
/// The pattern is compiled with the Rust `regex` crate (no look-around). Case
/// sensitivity is encoded in the pattern via the `(?i)` inline flag when
/// `Self.case_sensitive` is `false`.
/// </summary>
public sealed record RedactionPattern
{
    /// <summary>
    /// Custom category label surfaced in `RedactionFinding.category`(crate.types.redaction.RedactionFinding.category).
    /// </summary>
    [JsonPropertyName("label")]
    public required string Label { get; init; }

    /// <summary>
    /// Regex pattern (Rust `regex` crate dialect — no look-around).
    /// </summary>
    [JsonPropertyName("pattern")]
    public required string Pattern { get; init; }

    /// <summary>
    /// When `true`, match case-sensitively; otherwise prepend `(?i)` to the regex.
    /// </summary>
    [JsonPropertyName("case_sensitive")]
    public bool CaseSensitive { get; init; } = false;


    /// <summary>
    /// Parse a <see cref="RedactionPattern"/> from a JSON string.
    /// </summary>
    /// <exception cref="KreuzbergException">When the JSON cannot be deserialised.</exception>
    public static RedactionPattern FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<RedactionPattern>(json, JsonOptions)
                ?? throw new KreuzbergException($"Failed to parse RedactionPattern from JSON: deserializer returned null");
        }
        catch (KreuzbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new KreuzbergException($"Failed to parse RedactionPattern from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };

    /// <summary>
    /// Build a pattern with the given label (case-insensitive by default).
    /// </summary>
    public static RedactionPattern Labeled(string label, string pattern)
    {
        var nativeResult = NativeMethods.RedactionPatternLabeled(label, pattern);
        var jsonPtr = NativeMethods.RedactionPatternToJson(nativeResult);
        var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
        NativeMethods.FreeString(jsonPtr);
        NativeMethods.RedactionPatternFree(nativeResult);
        return JsonSerializer.Deserialize<RedactionPattern>(json ?? "null", JsonOptions)!;
    }
}
