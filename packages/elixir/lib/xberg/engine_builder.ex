# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.EngineBuilder do
  @moduledoc """
  Builder for [`Engine`].

  Each extension seam left unset is filled with its in-core default by
  [`build`](EngineBuilder::build), so [`Engine::new_default`] produces an
  engine whose seams are exactly those defaults.
  """

  alias Xberg.Native
  defstruct [:ref]

  @typedoc "Builder for [`Engine`]."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Build a default instance."
  @spec new() :: t()
  def new do
    %__MODULE__{ref: Native.enginebuilder_default()}
  end

  @doc "Finalize the builder into an [`Engine`], filling every unset seam with"
  def build(obj) do
    Native.enginebuilder_build(obj.ref)
  end

  def default() do
    ref = Native.enginebuilder_default()
    %__MODULE__{ref: ref}
  end
end
