# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.DefaultModelProvider do
  @moduledoc """
  In-core default: resolution via [`LayoutModelManager`]'s on-demand path.

  Wraps the same manager the extraction pipeline uses, so a resolved path is
  identical to the manager's existing on-demand download/cache behavior. The
  blocking download runs on a blocking thread to keep the async call non-blocking.
  """

  alias Xberg.Native
  defstruct [:ref]

  @typedoc "In-core default: resolution via [`LayoutModelManager`]'s on-demand path."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Build a default instance."
  @spec new() :: t()
  def new do
    %__MODULE__{ref: Native.defaultmodelprovider_default()}
  end
  def ensure_model(obj, model) do
    Native.defaultmodelprovider_ensure_model_async(obj.ref, model)
  end

  def default() do
    ref = Native.defaultmodelprovider_default()
    %__MODULE__{ref: ref}
  end
end
