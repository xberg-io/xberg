# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.Engine do
  @moduledoc """
  A reusable, cheaply-cloneable extraction engine.

  Cloning an [`Engine`] shares the same underlying state via [`Arc`].
  """

  alias Xberg.Native
  defstruct [:ref]

  @typedoc "A reusable, cheaply-cloneable extraction engine."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Start building an [`Engine`]."
  def builder() do
    Native.engine_builder()
  end

  @doc "Construct an [`Engine`] with default configuration."
  def new_default() do
    ref = Native.engine_new_default()
    %__MODULE__{ref: ref}
  end

  @doc "Extract content from a single bytes or URI input."
  def extract(obj, input, config) do
    Native.engine_extract_async(obj.ref, input, config)
  end

  @doc "Extract content from multiple bytes or URI inputs."
  def extract_batch(obj, inputs, config) do
    Native.engine_extract_batch_async(obj.ref, inputs, config)
  end
end
