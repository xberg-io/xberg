# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.GlinerArchitecture do
  @moduledoc """
  GLiNER ONNX architecture family. Determines which tensor I/O contract and
  preprocessing pipeline xberg uses — only relevant when `hf_repo` is set,
  since the pinned `xberg-io/gliner-models` catalog is always [`GlinerArchitecture::Gliner1`].
  """

  @typedoc "GLiNER ONNX architecture family. Determines which tensor I/O contract and preprocessing pipeline xberg uses — only relevant when `hf_repo` is set, since the pinned `xberg-io/gliner-models` catalog is always [`GlinerArchitecture::Gliner1`]."
  @type t :: :gliner1 | :gliner2

  @gliner1 :gliner1
  @gliner2 :gliner2

  @doc "Span-mode GLiNER (`gliner-community`/`urchade` lineage, the pinned `xberg-io/gliner-models` catalog, and most GLiNER fine-tunes including the `knowledgator/gliner-pii-*` family)."
  @spec gliner1() :: t()
  def gliner1, do: @gliner1
  @doc "Schema-prompt GLiNER2 (`fastino/gliner2` lineage). Requires an ONNX export with `[P]`/`[E]`/`[SEP_TEXT]` special tokens in its tokenizer and the `input_ids`/`attention_mask`/`text_positions`/`schema_positions`/`span_idx` tensor contract. Most GLiNER2 model cards ship safetensors only and have no ONNX export — check the repo's file list for a `.onnx` file before pointing `hf_repo` at it."
  @spec gliner2() :: t()
  def gliner2, do: @gliner2
end
