# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.ChunkerConfig do
  @moduledoc """
  Configuration for batch packing.

  These are plain parameters with no environment or policy coupling.
  """

  @typedoc "Configuration for batch packing."
  @type t :: %__MODULE__{
          max_input_tokens: non_neg_integer(),
          avg_tokens_per_image: non_neg_integer(),
          chars_per_token: non_neg_integer()
        }

  defstruct max_input_tokens: 0,
            avg_tokens_per_image: 0,
            chars_per_token: 0

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
