# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.Registry do
  @moduledoc "Sorted map of preset id → [`Preset`]."

  alias Xberg.Native
  defstruct [:ref]

  @typedoc "Sorted map of preset id → [`Preset`]."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Build the registry from preset files embedded at compile time under"
  def load_embedded() do
    ref = Native.registry_load_embedded()
    %__MODULE__{ref: ref}
  end

  @doc "Return the global registry, loading it on first access."
  def global() do
    ref = Native.registry_global()
    %__MODULE__{ref: ref}
  end

  @doc "Look up a preset by its identifier."
  def get(obj, id) do
    Native.registry_get(obj.ref, id)
  end

  @doc "Materialize a [`PresetSummary`] list for the public registry endpoint."
  def summaries(obj) do
    Native.registry_summaries(obj.ref)
  end

  @doc "Number of presets currently loaded."
  def len(obj) do
    Native.registry_len(obj.ref)
  end

  @doc "Whether the registry contains zero presets."
  def is_empty(obj) do
    Native.registry_is_empty(obj.ref)
  end

  @doc "Read raw sample bytes for `<preset_id>` from"
  def sample_bytes(obj, preset_id, name) do
    Native.registry_sample_bytes(obj.ref, preset_id, name)
  end

  @doc "Load additional preset files from a runtime directory and insert them"
  def extend_from_dir(obj, dir) do
    Native.registry_extend_from_dir(obj.ref, dir)
  end
end
