# This file is auto-generated by alef — DO NOT EDIT.
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg.ParsedDocument do
  @moduledoc """
  A single-operation memo of a document's parse facts and rendered pages.

  Cheaply shareable: the source bytes are held behind an [`Arc`], and the
  render cache is interior-mutable so [`rendered_pages`](Self::rendered_pages)
  can take `&self`.
  """

  alias Xberg.Native
  defstruct [:ref]

  @typedoc "A single-operation memo of a document's parse facts and rendered pages."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Build a memo from shared source bytes: detect the MIME type and count"
  def from_bytes(source) do
    ref = Native.parseddocument_from_bytes(source)
    %__MODULE__{ref: ref}
  end

  @doc "The detected MIME type."
  def mime_type(obj) do
    Native.parseddocument_mime_type(obj.ref)
  end

  @doc "The page count (1 for non-PDF inputs)."
  def page_count(obj) do
    Native.parseddocument_page_count(obj.ref)
  end

  @doc "The shared source bytes."
  def source(obj) do
    Native.parseddocument_source(obj.ref)
  end
end
