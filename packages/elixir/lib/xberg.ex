# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Xberg do
  @moduledoc "High-level API for xberg"

  @doc "Extract content from a single bytes or URI input."
  @spec extract_async(keyword()) :: {:ok, map()} | {:error, atom, String.t()}
  def extract_async(opts \\ []) do
    Xberg.Native.extract_async(
      Keyword.get(opts, :input),
      Keyword.get(opts, :config)
    )
  end

  @doc "Extract content from multiple bytes or URI inputs."
  @spec extract_batch_async(keyword()) :: {:ok, map()} | {:error, atom, String.t()}
  def extract_batch_async(opts \\ []) do
    Xberg.Native.extract_batch_async(
      case Keyword.get(opts, :inputs) do nil -> nil; v -> Jason.encode!(v) end,
      Keyword.get(opts, :config)
    )
  end

  @doc "Detect MIME type from raw file bytes."
  @spec detect_mime_type_from_bytes(binary()) :: {:ok, String.t()} | {:error, atom, String.t()}
  def detect_mime_type_from_bytes(content) do
    Xberg.Native.detect_mime_type_from_bytes(content)
  end

  @doc "Get file extensions for a given MIME type."
  @spec get_extensions_for_mime(String.t()) :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def get_extensions_for_mime(mime_type) do
    Xberg.Native.get_extensions_for_mime(mime_type)
  end

  @doc "List all supported document formats."
  @spec list_supported_formats() :: [map()]
  def list_supported_formats do
    Xberg.Native.list_supported_formats()
  end

  @doc "Detect QR codes in the bytes of an `ExtractedImage`."
  @spec detect_qr_codes(binary()) :: [map()]
  def detect_qr_codes(image_bytes) do
    Xberg.Native.detect_qr_codes(image_bytes, nil)
  end

  @doc "Detect QR codes in the bytes of an `ExtractedImage`."
  @spec detect_qr_codes(binary(), String.t() | nil) :: [map()]
  def detect_qr_codes(image_bytes, format_hint) do
    Xberg.Native.detect_qr_codes(image_bytes, format_hint)
  end

  @doc "Clear all embedding backends from the global registry."
  @spec clear_embedding_backends() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_embedding_backends do
    Xberg.Native.clear_embedding_backends()
  end

  @doc "List the names of all registered embedding backends."
  @spec list_embedding_backends() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_embedding_backends do
    Xberg.Native.list_embedding_backends()
  end

  @doc "List all registered OCR backends."
  @spec list_ocr_backends() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_ocr_backends do
    Xberg.Native.list_ocr_backends()
  end

  @doc "Clear all OCR backends from the global registry."
  @spec clear_ocr_backends() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_ocr_backends do
    Xberg.Native.clear_ocr_backends()
  end

  @doc "Register every built-in post-processor enabled by the active feature set."
  @spec register_builtin() :: {:ok, nil} | {:error, atom, String.t()}
  def register_builtin do
    Xberg.Native.register_builtin()
  end

  @doc "List all registered post-processor names."
  @spec list_post_processors() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_post_processors do
    Xberg.Native.list_post_processors()
  end

  @doc "Remove all registered post-processors."
  @spec clear_post_processors() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_post_processors do
    Xberg.Native.clear_post_processors()
  end

  @doc "List names of all registered renderers."
  @spec list_renderers() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_renderers do
    Xberg.Native.list_renderers()
  end

  @doc "Clear all renderers from the global registry."
  @spec clear_renderers() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_renderers do
    Xberg.Native.clear_renderers()
  end

  @doc "Clear all reranker backends from the global registry."
  @spec clear_reranker_backends() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_reranker_backends do
    Xberg.Native.clear_reranker_backends()
  end

  @doc "List the names of all registered reranker backends."
  @spec list_reranker_backends() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_reranker_backends do
    Xberg.Native.list_reranker_backends()
  end

  @doc "List names of all registered validators."
  @spec list_validators() :: {:ok, [String.t()]} | {:error, atom, String.t()}
  def list_validators do
    Xberg.Native.list_validators()
  end

  @doc "Remove all registered validators."
  @spec clear_validators() :: {:ok, nil} | {:error, atom, String.t()}
  def clear_validators do
    Xberg.Native.clear_validators()
  end

  @doc "Run page classification against an extraction result."
  @spec classify_pages_async(String.t() | nil, map()) :: {:ok, nil} | {:error, atom, String.t()}
  def classify_pages_async(result \\ nil, config) do
    Xberg.Native.classify_pages_async(result, config)
  end

  @doc "Classify a single piece of text without requiring an `ExtractionResult`."
  @spec classify_text_async(String.t(), map()) :: {:ok, [map()]} | {:error, atom, String.t()}
  def classify_text_async(text, config) do
    Xberg.Native.classify_text_async(text, config)
  end

  @doc "Classify a single document (as multiple pages or a single text block)."
  @spec classify_document_async([String.t()], map()) ::
          {:ok, [map()]} | {:error, atom, String.t()}
  def classify_document_async(pages, config) do
    Xberg.Native.classify_document_async(pages, config)
  end

  @doc "Eagerly download a NER model into the xberg cache."
  @spec download_model(String.t()) :: {:ok, String.t()} | {:error, atom, String.t()}
  def download_model(name) do
    Xberg.Native.download_model(name, nil)
  end

  @doc "Eagerly download a NER model into the xberg cache."
  @spec download_model(String.t(), String.t() | nil) ::
          {:ok, String.t()} | {:error, atom, String.t()}
  def download_model(name, cache_dir) do
    Xberg.Native.download_model(name, cache_dir)
  end

  @doc "Pinned default NER model identifier."
  @spec default_model_name() :: String.t()
  def default_model_name do
    Xberg.Native.default_model_name()
  end

  @doc "All NER models xberg knows about (used by `--all-ner-models`)."
  @spec known_models() :: [String.t()]
  def known_models do
    Xberg.Native.known_models()
  end

  @doc "Run pattern redaction (and optional NER-driven redaction) over `result` and rewrite every textual field. Populates `result.redaction_report`."
  @spec redact_async(keyword()) :: {:ok, nil} | {:error, atom, String.t()}
  def redact_async(opts \\ []) do
    Xberg.Native.redact_async(
      Keyword.get(opts, :result),
      Keyword.get(opts, :config)
    )
  end

  @doc "Find all US Social Security Number spans in `text` (format: NNN-NN-NNNN)."
  @spec find_all(String.t()) :: [map()]
  def find_all(text) do
    Xberg.Native.find_all(text)
  end

  @doc "Scan `text` for every PII category in `categories` and return all matches in source-byte order."
  @spec scan_text(String.t(), [map()]) :: [map()]
  def scan_text(text, categories) do
    Xberg.Native.scan_text(text, Jason.encode!(categories))
  end

  @doc "Score and return the top-N sentences from `text`, joined in original order."
  @spec summarize(String.t(), keyword()) :: String.t() | nil
  def summarize(text, opts \\ []) do
    Xberg.Native.summarize(
      text,
      Keyword.get(opts, :language),
      Keyword.get(opts, :max_tokens)
    )
  end

  @doc "Count whitespace-separated tokens (used for token-budget bookkeeping by callers)."
  @spec token_count(String.t()) :: non_neg_integer()
  def token_count(text) do
    Xberg.Native.token_count(text)
  end

  @doc "Translate the extraction result in place."
  @spec translate_result_async(String.t() | nil, map()) :: {:ok, nil} | {:error, atom, String.t()}
  def translate_result_async(result \\ nil, config) do
    Xberg.Native.translate_result_async(result, config)
  end

  @doc "Find all footnote anchor references in markdown text."
  @spec find_footnote_anchors(String.t()) :: [map()]
  def find_footnote_anchors(markdown) do
    Xberg.Native.find_footnote_anchors(markdown)
  end

  @doc "Parse footnote definitions from markdown text."
  @spec parse_footnote_definitions(String.t()) :: [map()]
  def parse_footnote_definitions(markdown) do
    Xberg.Native.parse_footnote_definitions(markdown)
  end

  @doc "Find inference markers in markdown text."
  @spec find_inference_markers(String.t()) :: [non_neg_integer()]
  def find_inference_markers(markdown) do
    Xberg.Native.find_inference_markers(markdown)
  end

  @doc "Find unmarked claims in markdown text."
  @spec find_unmarked_claims(String.t()) :: [String.t()]
  def find_unmarked_claims(markdown) do
    Xberg.Native.find_unmarked_claims(markdown)
  end

  @doc "Parse the structured citation block from markdown."
  @spec parse_citations(String.t()) :: [map()]
  def parse_citations(markdown) do
    Xberg.Native.parse_citations(markdown)
  end

  @doc "Verify that an excerpt appears verbatim in source text."
  @spec verify_excerpt(String.t(), String.t()) :: boolean()
  def verify_excerpt(excerpt, source_text) do
    Xberg.Native.verify_excerpt(excerpt, source_text)
  end

  @doc "Chunk text for RAG retrieval, ensuring every chunk carries a `heading_path`."
  @spec chunk_for_rag(String.t()) :: {:ok, map()} | {:error, atom, String.t()}
  def chunk_for_rag(text) do
    Xberg.Native.chunk_for_rag(text, nil)
  end

  @doc "Chunk text for RAG retrieval, ensuring every chunk carries a `heading_path`."
  @spec chunk_for_rag(String.t(), String.t() | nil) :: {:ok, map()} | {:error, atom, String.t()}
  def chunk_for_rag(text, config) do
    Xberg.Native.chunk_for_rag(text, config)
  end

  @doc "Compare two extraction results and return a structured diff."
  @spec compare(keyword()) :: map()
  def compare(opts \\ []) do
    Xberg.Native.compare(
      Keyword.get(opts, :a),
      Keyword.get(opts, :b),
      Keyword.get(opts, :opts)
    )
  end

  @doc "Extract content from a pre-cropped image region using a VLM."
  @spec extract_region_with_vlm_async(binary(), String.t(), map(), keyword()) ::
          {:ok, String.t()} | {:error, atom, String.t()}
  def extract_region_with_vlm_async(image_bytes, image_mime, region_kind, opts \\ []) do
    Xberg.Native.extract_region_with_vlm_async(
      image_bytes,
      image_mime,
      region_kind,
      Keyword.get(opts, :llm_config),
      Keyword.get(opts, :custom_prompt)
    )
  end

  @doc "Rerank documents asynchronously."
  @spec rerank_async(String.t(), [String.t()]) :: {:ok, [map()]} | {:error, atom, String.t()}
  def rerank_async(query, documents) do
    Xberg.Native.rerank_async(query, documents, nil)
  end

  @doc "Rerank documents asynchronously."
  @spec rerank_async(String.t(), [String.t()], String.t() | nil) ::
          {:ok, [map()]} | {:error, atom, String.t()}
  def rerank_async(query, documents, config) do
    Xberg.Native.rerank_async(query, documents, config)
  end

  @doc "Extract keywords from text using the specified algorithm."
  @spec extract_keywords(String.t()) :: {:ok, [map()]} | {:error, atom, String.t()}
  def extract_keywords(text) do
    Xberg.Native.extract_keywords(text, nil)
  end

  @doc "Extract keywords from text using the specified algorithm."
  @spec extract_keywords(String.t(), String.t() | nil) ::
          {:ok, [map()]} | {:error, atom, String.t()}
  def extract_keywords(text, config) do
    Xberg.Native.extract_keywords(text, config)
  end

  @doc "Analyze a document and determine the optimal chunking strategy."
  @spec analyze_document(map(), keyword()) :: {:ok, map()} | {:error, atom, String.t()}
  def analyze_document(metadata, opts \\ []) do
    Xberg.Native.analyze_document(
      metadata,
      Keyword.get(opts, :config),
      Keyword.get(opts, :document_bytes)
    )
  end

  @doc "Analyze a document with user-specified chunk ranges."
  @spec analyze_with_user_chunks([map()], non_neg_integer(), non_neg_integer()) :: map()
  def analyze_with_user_chunks(user_ranges, total_pages, size_bytes) do
    Xberg.Native.analyze_with_user_chunks(Jason.encode!(user_ranges), total_pages, size_bytes, nil)
  end

  @doc "Analyze a document with user-specified chunk ranges."
  @spec analyze_with_user_chunks(
          [map()],
          non_neg_integer(),
          non_neg_integer(),
          String.t() | nil
        ) :: map()
  def analyze_with_user_chunks(user_ranges, total_pages, size_bytes, config) do
    Xberg.Native.analyze_with_user_chunks(Jason.encode!(user_ranges), total_pages, size_bytes, config)
  end

  @doc "Score a [`ConfidenceSignals`] triple into an [`ExtractionConfidence`] using the supplied weights."
  @spec score_confidence(map()) :: map()
  def score_confidence(signals) do
    Xberg.Native.score_confidence(signals, nil)
  end

  @doc "Score a [`ConfidenceSignals`] triple into an [`ExtractionConfidence`] using the supplied weights."
  @spec score_confidence(map(), String.t() | nil) :: map()
  def score_confidence(signals, weights) do
    Xberg.Native.score_confidence(signals, weights)
  end

  @doc "Decision returned for pre-extraction rejection based on XLSX/PPTX-specific resource bounds. Returns `Some(reason)` to reject; `None` to proceed."
  @spec check_format_limits(String.t(), keyword()) :: String.t() | nil
  def check_format_limits(mime_type, opts \\ []) do
    Xberg.Native.check_format_limits(
      mime_type,
      Keyword.get(opts, :sheet_count),
      Keyword.get(opts, :workbook_cells),
      Keyword.get(opts, :embedded_count),
      Keyword.get(opts, :config)
    )
  end

  @doc "Derive document boundaries from an already-produced [`ExtractionResult`](crate::types::ExtractionResult)."
  @spec boundaries_from_extraction_result(keyword()) :: [map()]
  def boundaries_from_extraction_result(opts \\ []) do
    Xberg.Native.boundaries_from_extraction_result(
      Keyword.get(opts, :result),
      Keyword.get(opts, :thresholds)
    )
  end

  @doc "Detect document boundaries in a multi-document PDF."
  @spec detect_boundaries(map()) :: [map()]
  def detect_boundaries(input) do
    Xberg.Native.detect_boundaries(input, nil)
  end

  @doc "Detect document boundaries in a multi-document PDF."
  @spec detect_boundaries(map(), String.t() | nil) :: [map()]
  def detect_boundaries(input, thresholds) do
    Xberg.Native.detect_boundaries(input, thresholds)
  end

  @doc "Decide which call mode best fits this document."
  @spec choose_call_mode(map()) :: map()
  def choose_call_mode(input) do
    Xberg.Native.choose_call_mode(input, nil)
  end

  @doc "Decide which call mode best fits this document."
  @spec choose_call_mode(map(), String.t() | nil) :: map()
  def choose_call_mode(input, t) do
    Xberg.Native.choose_call_mode(input, t)
  end

  @doc "Calculate a chunking plan for a document."
  @spec calculate_chunk_plan(non_neg_integer(), non_neg_integer(), boolean()) :: map()
  def calculate_chunk_plan(page_count, size_bytes, needs_ocr) do
    Xberg.Native.calculate_chunk_plan(page_count, size_bytes, needs_ocr, nil)
  end

  @doc "Calculate a chunking plan for a document."
  @spec calculate_chunk_plan(non_neg_integer(), non_neg_integer(), boolean(), String.t() | nil) ::
          map()
  def calculate_chunk_plan(page_count, size_bytes, needs_ocr, config) do
    Xberg.Native.calculate_chunk_plan(page_count, size_bytes, needs_ocr, config)
  end

  @doc "Calculate a chunk plan from user-specified page ranges."
  @spec calculate_plan_from_overrides([map()], non_neg_integer(), non_neg_integer()) :: map()
  def calculate_plan_from_overrides(user_chunks, total_pages, size_bytes) do
    Xberg.Native.calculate_plan_from_overrides(Jason.encode!(user_chunks), total_pages, size_bytes, nil)
  end

  @doc "Calculate a chunk plan from user-specified page ranges."
  @spec calculate_plan_from_overrides(
          [map()],
          non_neg_integer(),
          non_neg_integer(),
          String.t() | nil
        ) :: map()
  def calculate_plan_from_overrides(user_chunks, total_pages, size_bytes, config) do
    Xberg.Native.calculate_plan_from_overrides(Jason.encode!(user_chunks), total_pages, size_bytes, config)
  end

  @doc "Stable sha256 fingerprint of `raw`, formatted as `sha256:<hex>`."
  @spec fingerprint(binary()) :: String.t()
  def fingerprint(raw) do
    Xberg.Native.fingerprint(raw)
  end

  @doc "Resolve `(preset, custom_schema_override, context)` into a [`ResolvedPreset`]."
  @spec resolve(map(), String.t() | nil, map()) :: {:ok, map()} | {:error, atom, String.t()}
  def resolve(preset, custom_schema \\ nil, context) do
    Xberg.Native.resolve(preset, custom_schema, context)
  end

  @doc "Extract structured JSON from a document using JSON-encoded preset spec and options."
  @spec extract_structured_json(binary(), String.t(), String.t(), String.t()) ::
          {:ok, String.t()} | {:error, atom, String.t()}
  def extract_structured_json(bytes, mime, preset_spec_json, options_json) do
    Xberg.Native.extract_structured_json(bytes, mime, preset_spec_json, options_json)
  end

  @doc "Split a multi-document PDF and extract structured JSON from each segment, returning a JSON array of `StructuredOutput` objects."
  @spec split_and_extract_json(binary(), String.t(), String.t(), String.t()) ::
          {:ok, String.t()} | {:error, atom, String.t()}
  def split_and_extract_json(bytes, mime, preset_spec_json, options_json) do
    Xberg.Native.split_and_extract_json(bytes, mime, preset_spec_json, options_json)
  end

  @doc "Render a single PDF page to PNG bytes."
  @spec render_pdf_page_to_png(binary(), non_neg_integer(), keyword()) ::
          {:ok, binary()} | {:error, atom, String.t()}
  def render_pdf_page_to_png(pdf_bytes, page_index, opts \\ []) do
    Xberg.Native.render_pdf_page_to_png(
      pdf_bytes,
      page_index,
      Keyword.get(opts, :dpi),
      Keyword.get(opts, :password)
    )
  end

  @doc "Count the pages in a PDF without rendering any of them."
  @spec pdf_page_count(binary()) :: {:ok, non_neg_integer()} | {:error, atom, String.t()}
  def pdf_page_count(pdf_bytes) do
    Xberg.Native.pdf_page_count(pdf_bytes, nil)
  end

  @doc "Count the pages in a PDF without rendering any of them."
  @spec pdf_page_count(binary(), String.t() | nil) ::
          {:ok, non_neg_integer()} | {:error, atom, String.t()}
  def pdf_page_count(pdf_bytes, password) do
    Xberg.Native.pdf_page_count(pdf_bytes, password)
  end

  @doc "Caption a single image from bytes."
  @spec caption_image_async(binary(), keyword()) :: {:ok, String.t()} | {:error, atom, String.t()}
  def caption_image_async(image_bytes, opts \\ []) do
    Xberg.Native.caption_image_async(
      image_bytes,
      Keyword.get(opts, :llm_config),
      Keyword.get(opts, :custom_prompt)
    )
  end

  @doc "Caption a single image from a file path."
  @spec caption_image_file_async(String.t(), keyword()) ::
          {:ok, String.t()} | {:error, atom, String.t()}
  def caption_image_file_async(path, opts \\ []) do
    Xberg.Native.caption_image_file_async(
      path,
      Keyword.get(opts, :llm_config),
      Keyword.get(opts, :custom_prompt)
    )
  end

  @doc "Detect the MIME type of a file at the given path."
  @spec detect_mime_type(String.t(), boolean()) :: {:ok, String.t()} | {:error, atom, String.t()}
  def detect_mime_type(path, check_exists) do
    Xberg.Native.detect_mime_type(path, check_exists)
  end

  @doc "Function"
  @spec embed_texts_async([String.t()]) :: {:ok, [[float()]]} | {:error, atom, String.t()}
  def embed_texts_async(texts) do
    Xberg.Native.embed_texts_async(texts, nil)
  end

  @doc "Function"
  @spec embed_texts_async([String.t()], String.t() | nil) ::
          {:ok, [[float()]]} | {:error, atom, String.t()}
  def embed_texts_async(texts, config) do
    Xberg.Native.embed_texts_async(texts, config)
  end

  @doc "Get an embedding preset by name."
  @spec get_embedding_preset(String.t()) :: map() | nil
  def get_embedding_preset(name) do
    Xberg.Native.get_embedding_preset(name)
  end

  @doc "List the names of all available embedding presets."
  @spec list_embedding_presets() :: [String.t()]
  def list_embedding_presets do
    Xberg.Native.list_embedding_presets()
  end

  @doc "Rerank a list of documents by relevance to a query."
  @spec rerank(String.t(), [String.t()]) :: {:ok, [map()]} | {:error, atom, String.t()}
  def rerank(query, documents) do
    Xberg.Native.rerank(query, documents, nil)
  end

  @doc "Rerank a list of documents by relevance to a query."
  @spec rerank(String.t(), [String.t()], String.t() | nil) ::
          {:ok, [map()]} | {:error, atom, String.t()}
  def rerank(query, documents, config) do
    Xberg.Native.rerank(query, documents, config)
  end

  @doc "Get a reranker preset by name."
  @spec get_reranker_preset(String.t()) :: map() | nil
  def get_reranker_preset(name) do
    Xberg.Native.get_reranker_preset(name)
  end

  @doc "List the names of all available reranker presets."
  @spec list_reranker_presets() :: [String.t()]
  def list_reranker_presets do
    Xberg.Native.list_reranker_presets()
  end

  @doc "Create a new LLM-backed NER backend with the given LLM configuration."
  def llmbackend_new(config) do
    Xberg.Native.llmbackend_new(config)
  end

  def llmbackend_detect_async(obj, text, categories) do
    Xberg.Native.llmbackend_detect_async(obj, text, categories)
  end

  def llmbackend_detect_with_custom_async(obj, text, categories, custom_labels) do
    Xberg.Native.llmbackend_detect_with_custom_async(obj, text, categories, custom_labels)
  end

  @doc "Create a fresh counter with no previous state."
  def tokencounter_new do
    Xberg.Native.tokencounter_new()
  end

  @doc "Compile the given JSON text as a Draft 2020-12 meta-schema."
  def metaschema_compile(meta_schema_json) do
    Xberg.Native.metaschema_compile(meta_schema_json)
  end

  @doc "Validate `raw` against the meta-schema and deserialize into a [`Preset`],"
  def metaschema_parse_preset(obj, path, raw) do
    Xberg.Native.metaschema_parse_preset(obj, path, raw)
  end

  @doc "Build the registry from preset files embedded at compile time under"
  def registry_load_embedded do
    Xberg.Native.registry_load_embedded()
  end

  @doc "Return the global registry, loading it on first access."
  def registry_global do
    Xberg.Native.registry_global()
  end

  @doc "Look up a preset by its identifier."
  def registry_get(obj, id) do
    Xberg.Native.registry_get(obj, id)
  end

  @doc "Materialize a [`PresetSummary`] list for the public registry endpoint."
  def registry_summaries(obj) do
    Xberg.Native.registry_summaries(obj)
  end

  @doc "Number of presets currently loaded."
  def registry_len(obj) do
    Xberg.Native.registry_len(obj)
  end

  @doc "Whether the registry contains zero presets."
  def registry_is_empty(obj) do
    Xberg.Native.registry_is_empty(obj)
  end

  @doc "Read raw sample bytes for `<preset_id>` from"
  def registry_sample_bytes(obj, preset_id, name) do
    Xberg.Native.registry_sample_bytes(obj, preset_id, name)
  end

  @doc "Load additional preset files from a runtime directory and insert them"
  def registry_extend_from_dir(obj, dir) do
    Xberg.Native.registry_extend_from_dir(obj, dir)
  end

  defmodule Xberg.OcrBackend.Host do
    @moduledoc """
    Typed host interface for the `OcrBackend` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_ocr_backend/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `process_image` trait call."
    @callback process_image(binary(), String.t() | nil) :: {:ok, map()} | {:error, atom, String.t()}
    @doc "Handle the `process_image_file` trait call."
    @callback process_image_file(String.t(), String.t() | nil) :: {:ok, map()} | {:error, atom, String.t()}
    @doc "Handle the `supports_language` trait call."
    @callback supports_language(String.t()) :: boolean()
    @doc "Handle the `backend_type` trait call."
    @callback backend_type() :: map()
    @doc "Handle the `supported_languages` trait call."
    @callback supported_languages() :: [String.t()]
    @doc "Handle the `supports_table_detection` trait call."
    @callback supports_table_detection() :: boolean()
    @doc "Handle the `supports_document_processing` trait call."
    @callback supports_document_processing() :: boolean()
    @doc "Handle the `emits_structured_markdown` trait call."
    @callback emits_structured_markdown() :: boolean()
    @doc "Handle the `process_document` trait call."
    @callback process_document(String.t(), String.t() | nil) :: {:ok, map()} | {:error, atom, String.t()}  end

  @doc """
  Register a OcrBackend plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.OcrBackend.Host` behaviour.
  """
  @spec register_ocr_backend(pid(), String.t()) :: :ok | :error
  def register_ocr_backend(genserver_pid, plugin_name) do
    Xberg.Native.register_ocr_backend(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered OcrBackend plugin by name."
  @spec unregister_ocr_backend(String.t()) :: :ok | :error
  def unregister_ocr_backend(name) do
    Xberg.Native.unregister_ocr_backend(name)
  end
  defmodule Xberg.PostProcessor.Host do
    @moduledoc """
    Typed host interface for the `PostProcessor` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_post_processor/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `process` trait call."
    @callback process(String.t() | nil, String.t() | nil) :: {:ok, nil} | {:error, atom, String.t()}
    @doc "Handle the `processing_stage` trait call."
    @callback processing_stage() :: map()
    @doc "Handle the `should_process` trait call."
    @callback should_process(String.t() | nil, String.t() | nil) :: boolean()
    @doc "Handle the `estimated_duration_ms` trait call."
    @callback estimated_duration_ms(String.t() | nil) :: non_neg_integer()
    @doc "Handle the `priority` trait call."
    @callback priority() :: integer()  end

  @doc """
  Register a PostProcessor plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.PostProcessor.Host` behaviour.
  """
  @spec register_post_processor(pid(), String.t()) :: :ok | :error
  def register_post_processor(genserver_pid, plugin_name) do
    Xberg.Native.register_post_processor(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered PostProcessor plugin by name."
  @spec unregister_post_processor(String.t()) :: :ok | :error
  def unregister_post_processor(name) do
    Xberg.Native.unregister_post_processor(name)
  end
  defmodule Xberg.Validator.Host do
    @moduledoc """
    Typed host interface for the `Validator` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_validator/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `validate` trait call."
    @callback validate(String.t() | nil, String.t() | nil) :: {:ok, nil} | {:error, atom, String.t()}
    @doc "Handle the `should_validate` trait call."
    @callback should_validate(String.t() | nil, String.t() | nil) :: boolean()
    @doc "Handle the `priority` trait call."
    @callback priority() :: integer()  end

  @doc """
  Register a Validator plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.Validator.Host` behaviour.
  """
  @spec register_validator(pid(), String.t()) :: :ok | :error
  def register_validator(genserver_pid, plugin_name) do
    Xberg.Native.register_validator(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered Validator plugin by name."
  @spec unregister_validator(String.t()) :: :ok | :error
  def unregister_validator(name) do
    Xberg.Native.unregister_validator(name)
  end
  defmodule Xberg.EmbeddingBackend.Host do
    @moduledoc """
    Typed host interface for the `EmbeddingBackend` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_embedding_backend/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `dimensions` trait call."
    @callback dimensions() :: non_neg_integer()
    @doc "Handle the `embed` trait call."
    @callback embed([String.t()]) :: {:ok, [[float()]]} | {:error, atom, String.t()}  end

  @doc """
  Register a EmbeddingBackend plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.EmbeddingBackend.Host` behaviour.
  """
  @spec register_embedding_backend(pid(), String.t()) :: :ok | :error
  def register_embedding_backend(genserver_pid, plugin_name) do
    Xberg.Native.register_embedding_backend(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered EmbeddingBackend plugin by name."
  @spec unregister_embedding_backend(String.t()) :: :ok | :error
  def unregister_embedding_backend(name) do
    Xberg.Native.unregister_embedding_backend(name)
  end
  defmodule Xberg.Renderer.Host do
    @moduledoc """
    Typed host interface for the `Renderer` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_renderer/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `render` trait call."
    @callback render(map()) :: {:ok, String.t()} | {:error, atom, String.t()}  end

  @doc """
  Register a Renderer plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.Renderer.Host` behaviour.
  """
  @spec register_renderer(pid(), String.t()) :: :ok | :error
  def register_renderer(genserver_pid, plugin_name) do
    Xberg.Native.register_renderer(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered Renderer plugin by name."
  @spec unregister_renderer(String.t()) :: :ok | :error
  def unregister_renderer(name) do
    Xberg.Native.unregister_renderer(name)
  end
  defmodule Xberg.RerankerBackend.Host do
    @moduledoc """
    Typed host interface for the `RerankerBackend` plugin bridge.

    A plugin handler module implements this behaviour and is registered with
    `register_reranker_backend/2`. Each callback receives the decoded arguments for a
    trait method and returns the method's result. The dispatching GenServer
    passes the decoded `args` map to the callback for the corresponding method.
    """
    @doc "Handle the `rerank` trait call."
    @callback rerank(String.t(), [String.t()]) :: {:ok, [float()]} | {:error, atom, String.t()}  end

  @doc """
  Register a RerankerBackend plugin with a GenServer PID and name.

  The GenServer should dispatch `{:trait_call, ...}` messages to a module that
  implements the `Xberg.RerankerBackend.Host` behaviour.
  """
  @spec register_reranker_backend(pid(), String.t()) :: :ok | :error
  def register_reranker_backend(genserver_pid, plugin_name) do
    Xberg.Native.register_reranker_backend(genserver_pid, plugin_name)
  end
  @doc "Unregister a previously registered RerankerBackend plugin by name."
  @spec unregister_reranker_backend(String.t()) :: :ok | :error
  def unregister_reranker_backend(name) do
    Xberg.Native.unregister_reranker_backend(name)
  end
end
