# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
defmodule Kreuzberg.TranscriptionConfig do
  @moduledoc """
  Configuration for audio/video transcription (speech-to-text).

  When present and `enabled`, Kreuzberg will route audio and video files
  (mp3, mp4, m4a, wav, webm, etc.) through the transcription pipeline.

  The heavy dependencies (ORT, hf-hub, symphonia) are only pulled when the
  `transcription` feature is enabled. The config struct itself is available
  under `transcription-types` so that `ExtractionConfig` round-trips on all
  targets.

  All fields have sensible defaults. The recommended starting point is:

  ```toml
  [extraction.transcription]
  enabled = true
  model = "tiny"
  ```
  """

  @typedoc "Configuration for audio/video transcription (speech-to-text)."
  @type t :: %__MODULE__{
          enabled: boolean(),
          model: String.t() | nil,
          language: String.t() | nil,
          timestamps: boolean(),
          max_duration_ms: non_neg_integer() | nil,
          max_bytes: non_neg_integer() | nil,
          timeout_ms: non_neg_integer() | nil,
          model_cache_dir: String.t() | nil,
          allow_network: boolean(),
          verify_hash: boolean()
        }

  defstruct enabled: true,
            model: :tiny,
            language: nil,
            timestamps: false,
            max_duration_ms: nil,
            max_bytes: nil,
            timeout_ms: nil,
            model_cache_dir: nil,
            allow_network: true,
            verify_hash: true

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
