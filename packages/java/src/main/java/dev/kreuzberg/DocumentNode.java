// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6430e3cd2dc5a1615a5455a4632aab133ccdf7e466c03d88481b38d68c414758
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import java.util.Map;
import java.util.Optional;

/**
 * A single node in the document tree.
 *
 * <p>Each node has deterministic {@code id}, typed {@code content}, optional {@code parent}/{@code
 * children} for tree structure, and metadata like page number, bounding box, and content layer.
 */
public record DocumentNode(
    /** Deterministic identifier (hash of content + position). */
    String id,
    /** Node content — tagged enum, type-specific data only. */
    NodeContent content,
    /** Parent node index ({@code None} = root-level node). */
    Optional<Integer> parent,
    /** Child node indices in reading order. */
    List<Integer> children,
    /** Content layer classification. */
    @JsonProperty("content_layer") ContentLayer contentLayer,
    /** Page number where this node starts (1-indexed). */
    Optional<Integer> page,
    /** Page number where this node ends (for multi-page tables/sections). */
    @JsonProperty("page_end") Optional<Integer> pageEnd,
    /** Bounding box in document coordinates. */
    Optional<String> bbox,
    /** Inline annotations (formatting, links) on this node's text content. */
    List<TextAnnotation> annotations,
    /** Format-specific key-value attributes. */
    Optional<Map<String, String>> attributes) {}
