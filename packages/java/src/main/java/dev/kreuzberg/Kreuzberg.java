// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:20200ed43ace1f6db4d56a2b542a92753b994b29503c147917448a18fd002f53
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg;

import java.util.List;

public final class Kreuzberg {
    private Kreuzberg() {
    }

    /**
     * Extract content from a byte array.
     *
     * This is the main entry point for in-memory extraction. It performs the following steps: 1. Validate MIME type 2.
     * Handle legacy format conversion if needed 3. Select appropriate extractor from registry 4. Extract content 5. Run
     * post-processing pipeline
     *
     * # Arguments
     *
     * * `content` - The byte array to extract * `mime_type` - MIME type of the content * `config` - Extraction
     * configuration
     *
     * # Returns
     *
     * An `ExtractionResult` containing the extracted content and metadata.
     *
     * # Errors
     *
     * Returns `KreuzbergError::Validation` if MIME type is invalid. Returns `KreuzbergError::UnsupportedFormat` if MIME
     * type is not supported.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::extract_bytes; use kreuzberg::core::config::ExtractionConfig;
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let bytes = b"Hello,
     * world!"; let result = extract_bytes(bytes, "text/plain", &config).await?; println!("Content: {}",
     * result.content); # Ok(()) # } ```
     */
    public static ExtractionResult extractBytes(final byte[] content, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractBytes(content, mimeType, config);
    }

    /**
     * Extract content from a file.
     *
     * This is the main entry point for file-based extraction. It performs the following steps: 1. Check cache for
     * existing result (if caching enabled) 2. Detect or validate MIME type 3. Select appropriate extractor from
     * registry 4. Extract content 5. Run post-processing pipeline 6. Store result in cache (if caching enabled)
     *
     * # Arguments
     *
     * * `path` - Path to the file to extract * `mime_type` - Optional MIME type override. If None, will be
     * auto-detected * `config` - Extraction configuration
     *
     * # Returns
     *
     * An `ExtractionResult` containing the extracted content and metadata.
     *
     * # Errors
     *
     * Returns `KreuzbergError::Io` if the file doesn't exist (NotFound) or for other file I/O errors. Returns
     * `KreuzbergError::UnsupportedFormat` if MIME type is not supported.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::extract_file; use kreuzberg::core::config::ExtractionConfig;
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let result =
     * extract_file("document.pdf", None, &config).await?; println!("Content: {}", result.content); # Ok(()) # } ```
     */
    public static ExtractionResult extractFile(final java.nio.file.Path path, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractFile(path, mimeType, config);
    }

    public static ExtractionResult extractFile(final java.nio.file.Path path, final ExtractionConfig config)
            throws KreuzbergRsException {
        return KreuzbergRs.extractFile(path, null, config);
    }

    /**
     * Synchronous wrapper for `extract_file`.
     *
     * This is a convenience function that blocks the current thread until extraction completes. For async code, use
     * `extract_file` directly.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating a new runtime per call. Always uses
     * the global runtime to avoid nested runtime issues.
     *
     * This function is only available with the `tokio-runtime` feature. For WASM targets, use a truly synchronous
     * extraction approach instead.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::extract_file_sync; use kreuzberg::core::config::ExtractionConfig;
     *
     * let config = ExtractionConfig::default(); let result = extract_file_sync("document.pdf", None, &config)?;
     * println!("Content: {}", result.content); # Ok::<(), kreuzberg::KreuzbergError>(()) ```
     */
    public static ExtractionResult extractFileSync(final java.nio.file.Path path, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractFileSync(path, mimeType, config);
    }

    public static ExtractionResult extractFileSync(final java.nio.file.Path path, final ExtractionConfig config)
            throws KreuzbergRsException {
        return KreuzbergRs.extractFileSync(path, null, config);
    }

    /**
     * Synchronous wrapper for `extract_bytes`.
     *
     * Uses the global Tokio runtime for 100x+ performance improvement over creating a new runtime per call.
     *
     * With the `tokio-runtime` feature, this blocks the current thread using the global Tokio runtime. Without it
     * (WASM), this calls a truly synchronous implementation.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::extract_bytes_sync; use kreuzberg::core::config::ExtractionConfig;
     *
     * let config = ExtractionConfig::default(); let bytes = b"Hello, world!"; let result = extract_bytes_sync(bytes,
     * "text/plain", &config)?; println!("Content: {}", result.content); # Ok::<(), kreuzberg::KreuzbergError>(()) ```
     */
    public static ExtractionResult extractBytesSync(final byte[] content, final String mimeType,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.extractBytesSync(content, mimeType, config);
    }

    /**
     * Synchronous wrapper for `batch_extract_files`.
     *
     * Uses the global Tokio runtime for optimal performance. Only available with `tokio-runtime` (WASM has no
     * filesystem).
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_files_sync; use
     * kreuzberg::core::config::{ExtractionConfig, BatchFileItem, FileExtractionConfig};
     *
     * let config = ExtractionConfig::default(); let items = vec![ BatchFileItem { path: "doc1.pdf".into(), config:
     * Some(FileExtractionConfig { force_ocr: Some(true), ..Default::default() }), }, BatchFileItem { path:
     * "doc2.pdf".into(), config: None }, ]; let results = batch_extract_files_sync(items, &config)?; # Ok::<(),
     * kreuzberg::KreuzbergError>(()) ```
     */
    public static List<ExtractionResult> batchExtractFilesSync(final List<BatchFileItem> items,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractFilesSync(items, config);
    }

    /**
     * Synchronous wrapper for `batch_extract_bytes`.
     *
     * Uses the global Tokio runtime for optimal performance. With the `tokio-runtime` feature, this blocks the current
     * thread using the global Tokio runtime. Without it (WASM), this calls a truly synchronous implementation that
     * iterates through items and calls `extract_bytes_sync()`.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_bytes_sync; use
     * kreuzberg::core::config::{ExtractionConfig, BatchBytesItem, FileExtractionConfig};
     *
     * let config = ExtractionConfig::default(); let items = vec![ BatchBytesItem { content: b"content".to_vec(),
     * mime_type: "text/plain".to_string(), config: None }, BatchBytesItem { content: b"other".to_vec(), mime_type:
     * "text/plain".to_string(), config: Some(FileExtractionConfig { force_ocr: Some(true), ..Default::default() }), },
     * ]; let results = batch_extract_bytes_sync(items, &config)?; # Ok::<(), kreuzberg::KreuzbergError>(()) ```
     */
    public static List<ExtractionResult> batchExtractBytesSync(final List<BatchBytesItem> items,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractBytesSync(items, config);
    }

    /**
     * Extract content from multiple files concurrently.
     *
     * This function processes multiple files in parallel, automatically managing concurrency to prevent resource
     * exhaustion. The concurrency limit can be configured via `ExtractionConfig::max_concurrent_extractions` or
     * defaults to `(num_cpus * 1.5).ceil()`.
     *
     * Each file can optionally specify a [`FileExtractionConfig`] that overrides specific fields from the batch-level
     * `config`. Pass `None` for a file to use the batch defaults. Batch-level settings like
     * `max_concurrent_extractions` and `use_cache` are always taken from the batch-level `config`.
     *
     * # Arguments
     *
     * * `items` - Vector of [`BatchFileItem`] structs, each containing a path and optional per-file configuration
     * overrides. * `config` - Batch-level extraction configuration (provides defaults and batch settings)
     *
     * # Returns
     *
     * A vector of `ExtractionResult` in the same order as the input items.
     *
     * # Errors
     *
     * Individual file errors are captured in the result metadata. System errors (IO, RuntimeError equivalents) will
     * bubble up and fail the entire batch.
     *
     * # Examples
     *
     * Simple usage with no per-file overrides:
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_files; use
     * kreuzberg::core::config::{ExtractionConfig, BatchFileItem}; use std::path::PathBuf;
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let items = vec![
     * BatchFileItem { path: "doc1.pdf".into(), config: None }, BatchFileItem { path: "doc2.pdf".into(), config: None },
     * ]; let results = batch_extract_files(items, &config).await?; println!("Processed {} files", results.len()); #
     * Ok(()) # } ```
     *
     * Per-file configuration overrides:
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_files; use
     * kreuzberg::core::config::{ExtractionConfig, BatchFileItem, FileExtractionConfig}; use std::path::PathBuf;
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let items = vec![
     * BatchFileItem { path: "scan.pdf".into(), config: Some(FileExtractionConfig { force_ocr: Some(true),
     * ..Default::default() }), }, BatchFileItem { path: "notes.txt".into(), config: None }, ]; let results =
     * batch_extract_files(items, &config).await?; # Ok(()) # } ```
     */
    public static List<ExtractionResult> batchExtractFiles(final List<BatchFileItem> items,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractFiles(items, config);
    }

    /**
     * Extract content from multiple byte arrays concurrently.
     *
     * This function processes multiple byte arrays in parallel, automatically managing concurrency to prevent resource
     * exhaustion. The concurrency limit can be configured via `ExtractionConfig::max_concurrent_extractions` or
     * defaults to `(num_cpus * 1.5).ceil()`.
     *
     * Each item can optionally specify a [`FileExtractionConfig`] that overrides specific fields from the batch-level
     * `config`. Pass `None` as the config to use the batch-level defaults for that item.
     *
     * # Arguments
     *
     * * `items` - Vector of [`BatchBytesItem`] structs, each containing content bytes, MIME type, and optional per-item
     * configuration overrides. * `config` - Batch-level extraction configuration
     *
     * # Returns
     *
     * A vector of `ExtractionResult` in the same order as the input items.
     *
     * # Examples
     *
     * Simple usage with no per-item overrides:
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_bytes; use
     * kreuzberg::core::config::{ExtractionConfig, BatchBytesItem};
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let items = vec![
     * BatchBytesItem { content: b"content 1".to_vec(), mime_type: "text/plain".to_string(), config: None },
     * BatchBytesItem { content: b"content 2".to_vec(), mime_type: "text/plain".to_string(), config: None }, ]; let
     * results = batch_extract_bytes(items, &config).await?; println!("Processed {} items", results.len()); # Ok(()) # }
     * ```
     *
     * Per-item configuration overrides:
     *
     * ```rust,no_run use kreuzberg::core::extractor::batch_extract_bytes; use
     * kreuzberg::core::config::{ExtractionConfig, BatchBytesItem, FileExtractionConfig};
     *
     * # async fn example() -> kreuzberg::Result<()> { let config = ExtractionConfig::default(); let items = vec![
     * BatchBytesItem { content: b"content".to_vec(), mime_type: "text/plain".to_string(), config: None },
     * BatchBytesItem { content: b"<html>test</html>".to_vec(), mime_type: "text/html".to_string(), config:
     * Some(FileExtractionConfig { force_ocr: Some(true), ..Default::default() }), }, ]; let results =
     * batch_extract_bytes(items, &config).await?; # Ok(()) # } ```
     */
    public static List<ExtractionResult> batchExtractBytes(final List<BatchBytesItem> items,
            final ExtractionConfig config) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(items, "items must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        return KreuzbergRs.batchExtractBytes(items, config);
    }

    /**
     * Detect MIME type from raw file bytes.
     *
     * Uses magic byte signatures to detect file type from content. Falls back to `infer` crate for comprehensive
     * detection.
     *
     * For ZIP-based files, inspects contents to distinguish Office Open XML formats (DOCX, XLSX, PPTX) from plain ZIP
     * archives.
     *
     * # Arguments
     *
     * * `content` - Raw file bytes
     *
     * # Returns
     *
     * The detected MIME type string.
     *
     * # Errors
     *
     * Returns `KreuzbergError::UnsupportedFormat` if MIME type cannot be determined.
     */
    public static String detectMimeTypeFromBytes(final byte[] content) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(content, "content must not be null");
        return KreuzbergRs.detectMimeTypeFromBytes(content);
    }

    /**
     * Get file extensions for a given MIME type.
     *
     * Returns all known file extensions that map to the specified MIME type.
     *
     * # Arguments
     *
     * * `mime_type` - The MIME type to look up
     *
     * # Returns
     *
     * A vector of file extensions (without leading dot) for the MIME type.
     *
     * # Example
     *
     * ``` use kreuzberg::core::mime::get_extensions_for_mime;
     *
     * let extensions = get_extensions_for_mime("application/pdf").unwrap(); assert_eq!(extensions, vec!["pdf"]);
     *
     * let doc_extensions =
     * get_extensions_for_mime("application/vnd.openxmlformats-officedocument.wordprocessingml.document").unwrap();
     * assert!(doc_extensions.contains(&"docx".to_string())); ```
     */
    public static List<String> getExtensionsForMime(final String mimeType) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(mimeType, "mimeType must not be null");
        return KreuzbergRs.getExtensionsForMime(mimeType);
    }

    /**
     * List names of all registered document extractors.
     */
    public static List<String> listDocumentExtractors() throws KreuzbergRsException {
        return KreuzbergRs.listDocumentExtractors();
    }

    /**
     * List all registered OCR backends.
     *
     * Returns the names of all OCR backends currently registered in the global registry.
     *
     * # Returns
     *
     * A vector of OCR backend names.
     *
     * # Example
     *
     * ```rust use kreuzberg::plugins::list_ocr_backends;
     *
     * # tokio_test::block_on(async { let backends = list_ocr_backends()?; for name in backends { println!("Registered
     * OCR backend: {}", name); } # Ok::<(), kreuzberg::KreuzbergError>(()) # }); ```
     */
    public static List<String> listOcrBackends() throws KreuzbergRsException {
        return KreuzbergRs.listOcrBackends();
    }

    /**
     * Clear all OCR backends from the global registry.
     *
     * Removes all OCR backends and calls their `shutdown()` methods.
     *
     * # Returns
     *
     * - `Ok(())` if all backends were cleared successfully - `Err(...)` if any shutdown method failed
     *
     * # Example
     *
     * ```rust use kreuzberg::plugins::clear_ocr_backends;
     *
     * # tokio_test::block_on(async { clear_ocr_backends()?; # Ok::<(), kreuzberg::KreuzbergError>(()) # }); ```
     */
    public static void clearOcrBackends() throws KreuzbergRsException {
        KreuzbergRs.clearOcrBackends();
    }

    /**
     * List all registered post-processor names.
     *
     * Returns a vector of all post-processor names currently registered in the global registry.
     *
     * # Returns
     *
     * - `Ok(Vec<String>)` - Vector of post-processor names - `Err(...)` if the registry lock is poisoned
     *
     * # Example
     *
     * ```rust use kreuzberg::plugins::list_post_processors;
     *
     * # tokio_test::block_on(async { let processors = list_post_processors()?; for name in processors {
     * println!("Registered post-processor: {}", name); } # Ok::<(), kreuzberg::KreuzbergError>(()) # }); ```
     */
    public static List<String> listPostProcessors() throws KreuzbergRsException {
        return KreuzbergRs.listPostProcessors();
    }

    /**
     * Remove all registered post-processors.
     */
    public static void clearPostProcessors() throws KreuzbergRsException {
        KreuzbergRs.clearPostProcessors();
    }

    /**
     * List names of all registered validators.
     */
    public static List<String> listValidators() throws KreuzbergRsException {
        return KreuzbergRs.listValidators();
    }

    /**
     * Remove all registered validators.
     */
    public static void clearValidators() throws KreuzbergRsException {
        KreuzbergRs.clearValidators();
    }

    /**
     * Render a single PDF page to a PNG-encoded byte buffer.
     *
     * # Errors
     *
     * Returns an error if the PDF is invalid, the page index is out of bounds, or if the page fails to render.
     *
     * # Example
     *
     * ```rust,no_run use kreuzberg::pdf::render_pdf_page_to_png;
     *
     * # fn example() -> kreuzberg::pdf::error::Result<()> { let pdf_bytes = std::fs::read("document.pdf")?; let png =
     * render_pdf_page_to_png(&pdf_bytes, 0, Some(150), None)?; std::fs::write("page_0.png", png)?; # Ok(()) # } ```
     */
    public static byte[] renderPdfPageToPng(final byte[] pdfBytes, final long pageIndex, final Integer dpi,
            final String password) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(pdfBytes, "pdfBytes must not be null");
        java.util.Objects.requireNonNull(pageIndex, "pageIndex must not be null");
        return KreuzbergRs.renderPdfPageToPng(pdfBytes, pageIndex, dpi, password);
    }

    public static byte[] renderPdfPageToPng(final byte[] pdfBytes, final long pageIndex) throws KreuzbergRsException {
        return KreuzbergRs.renderPdfPageToPng(pdfBytes, pageIndex, 0, null);
    }

    /**
     * Detect the MIME type of a file at the given path.
     *
     * Uses the file extension and optionally the file content to determine the MIME type. Set `check_exists` to `true`
     * to verify the file exists before detection.
     */
    public static String detectMimeType(final String path, final boolean checkExists) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(checkExists, "checkExists must not be null");
        return KreuzbergRs.detectMimeType(path, checkExists);
    }

    /**
     * Embed a list of texts using the configured embedding model.
     *
     * Returns a 2D vector where each inner vector is the embedding for the corresponding text.
     */
    public static List<List<Float>> embedTexts(final List<String> texts, final EmbeddingConfig config)
            throws KreuzbergRsException {
        java.util.Objects.requireNonNull(texts, "texts must not be null");
        return KreuzbergRs.embedTexts(texts, config);
    }

    public static List<List<Float>> embedTexts(final List<String> texts) throws KreuzbergRsException {
        return KreuzbergRs.embedTexts(texts, null);
    }

    /**
     * Get an embedding preset by name.
     *
     * Returns `None` if no preset with the given name exists. Returns an owned clone so the value is safe to pass
     * across FFI boundaries.
     */
    public static EmbeddingPreset getEmbeddingPreset(final String name) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(name, "name must not be null");
        return KreuzbergRs.getEmbeddingPreset(name).orElseThrow();
    }

    /**
     * List the names of all available embedding presets.
     *
     * Returns owned `String`s so the values are safe to pass across FFI boundaries.
     */
    public static List<String> listEmbeddingPresets() throws KreuzbergRsException {
        return KreuzbergRs.listEmbeddingPresets();
    }

}
