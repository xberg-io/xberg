// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.node.ObjectNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import org.jspecify.annotations.Nullable;

/**
 * The chunking decision made by the analyzer.
 */
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
@JsonDeserialize(using = ChunkingDecisionDeserializer.class)
@JsonSerialize(using = ChunkingDecisionSerializer.class)
public sealed interface ChunkingDecision {

    /**
     * Process without chunking (small file, text layer detected, etc.)
     */
    record NoChunking(@JsonProperty("reason") NoChunkingReason reason) implements ChunkingDecision { }

    /**
     * Chunk according to plan.
     */
    record Chunk(ChunkPlan value) implements ChunkingDecision { }

    /**
     * Use user-provided chunk overrides.
     */
    record UseOverrides(@JsonProperty("user_chunks") List<PageRange> userChunks) implements ChunkingDecision { }

    /** Returns the Chunk data if this is a Chunk variant, otherwise null. */
    default @Nullable ChunkPlan chunk() {
        return this instanceof Chunk variant ? variant.value() : null;
    }

}

/** Custom deserializer for sealed interface with unwrapped variants. */
class ChunkingDecisionDeserializer extends StdDeserializer<ChunkingDecision> {
  private static final long serialVersionUID = 1L;

  ChunkingDecisionDeserializer() {
    super(ChunkingDecision.class);
  }

  @Override
  @SuppressWarnings("PMD.CyclomaticComplexity")
  public ChunkingDecision deserialize(final JsonParser parser, final DeserializationContext ctx)
      throws java.io.IOException {
    final ObjectNode node = parser.getCodec().readTree(parser);
    final com.fasterxml.jackson.databind.JsonNode tagNode = node.get("type");
    if (tagNode == null || tagNode.isNull()) {
      throw new com.fasterxml.jackson.databind.JsonMappingException(
          parser, "Missing discriminator field: type");
    }
    final String tagValue = tagNode.asText();
    node.remove("type");

    return switch (tagValue) {
      case "NoChunking" -> ctx.readTreeAsValue(node, ChunkingDecision.NoChunking.class);
      case "Chunk" -> new ChunkingDecision.Chunk(ctx.readTreeAsValue(node, ChunkPlan.class));
      case "UseOverrides" -> ctx.readTreeAsValue(node, ChunkingDecision.UseOverrides.class);
      default -> {
        throw new com.fasterxml.jackson.databind.JsonMappingException(
            parser, "Unknown ChunkingDecision discriminator: " + tagValue);
      }
    };
  }
}

/** Custom serializer for sealed interface with unwrapped variants. Emits the discriminator tag alongside the inner record's fields (flat object). */
class ChunkingDecisionSerializer extends StdSerializer<ChunkingDecision> {
  private static final long serialVersionUID = 1L;
  /** ObjectMapper for serializing inner variant values. */
  private static final com.fasterxml.jackson.databind.ObjectMapper MAPPER =
      new com.fasterxml.jackson.databind.ObjectMapper()
          .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
          .setPropertyNamingStrategy(
              com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
          .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL);

  /** Creates a new ChunkingDecisionSerializer. */
  ChunkingDecisionSerializer() {
    super(ChunkingDecision.class);
  }

  @Override
  @SuppressWarnings("PMD.CyclomaticComplexity")
  public void serialize(ChunkingDecision value, JsonGenerator gen, SerializerProvider provider)
      throws java.io.IOException {
    String tag;
    Object inner;if (value instanceof ChunkingDecision.NoChunking variantNoChunking) {
      tag = "NoChunking";      inner = variantNoChunking;    }else if (value instanceof ChunkingDecision.Chunk variantChunk) {
      tag = "Chunk";      inner = variantChunk.value();    }else if (value instanceof ChunkingDecision.UseOverrides variantUseOverrides) {
      tag = "UseOverrides";      inner = variantUseOverrides;    }    else {
      throw new com.fasterxml.jackson.databind.JsonMappingException(
          gen, "Unknown ChunkingDecision variant: " + value.getClass().getName());
    }

    gen.writeStartObject();
    gen.writeStringField("type", tag);
    if (inner != null) {
      com.fasterxml.jackson.databind.JsonNode tree = MAPPER.valueToTree(inner);
      if (tree.isObject()) {
        java.util.Iterator<java.util.Map.Entry<String, com.fasterxml.jackson.databind.JsonNode>>
            fieldIterator = tree.fields();
        while (fieldIterator.hasNext()) {
          java.util.Map.Entry<String, com.fasterxml.jackson.databind.JsonNode> field =
              fieldIterator.next();
          gen.writeFieldName(field.getKey());
          gen.writeTree(field.getValue());
        }
      }
    }
    gen.writeEndObject();
  }
}
