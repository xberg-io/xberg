// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;

/**
 * Validates credit-card-shaped candidates against the Luhn mod-10 checksum.
 */
@SuppressWarnings("PMD")
public class LuhnValidator implements AutoCloseable {
    private final MemorySegment handle;

    LuhnValidator(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    public String label() throws XbergRsException {
        try {
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_LUHN_VALIDATOR_LABEL.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.XBERG_FREE_STRING.invoke(resultPtr);
            }
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("label: failed", e);
        }
    }
    public ValidationResult validate(final PatternMatch entity, final String ctx) throws XbergRsException {
        java.util.Objects.requireNonNull(entity, "entity must not be null");
        java.util.Objects.requireNonNull(ctx, "ctx must not be null");
        try (Arena arena = Arena.ofShared()) {
            String cEntityJson = STREAM_MAPPER.writeValueAsString(entity);
            var cEntityJsonSeg = arena.allocateFrom(cEntityJson);
            MemorySegment cEntity = (MemorySegment) NativeLib.XBERG_PATTERN_MATCH_FROM_JSON.invoke(cEntityJsonSeg);
            if (cEntity.equals(MemorySegment.NULL)) { checkLastFfiError(); throw new XbergRsException("validate: failed to marshal entity", (Throwable) null); }
            var cCtx = arena.allocateFrom(ctx);
            if (!cEntity.equals(MemorySegment.NULL)) { try { NativeLib.XBERG_PATTERN_MATCH_FREE.invoke(cEntity); } catch (Throwable ignore) {} }
            // Unsupported return shape for validate
            throw new XbergRsException("validate: unsupported return shape", (Throwable) null);
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("validate: failed", e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_LUHN_VALIDATOR_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free LuhnValidator: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = createStreamMapper();

    private static ObjectMapper createStreamMapper() {
        return new ObjectMapper()
            .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
            .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
            .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
    }    // CPD-ON
}
