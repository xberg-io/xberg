// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.List;

/**
 * A reusable, cheaply-cloneable extraction engine.
 *
 * Cloning an Engine shares the same underlying state via Arc.
 */
@SuppressWarnings("PMD")
public class Engine implements AutoCloseable {
    private final MemorySegment handle;

    Engine(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    /**
     * Extract content from a single bytes or URI input.
     */
    public ExtractionResult extract(final ExtractInput input, final ExtractionConfig config) throws XbergRsException {
        java.util.Objects.requireNonNull(input, "input must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        try (Arena arena = Arena.ofShared()) {
            String cInputJson = STREAM_MAPPER.writeValueAsString(input);
            var cInputJsonSeg = arena.allocateFrom(cInputJson);
            MemorySegment cInput = (MemorySegment) NativeLib.XBERG_EXTRACT_INPUT_FROM_JSON.invoke(cInputJsonSeg);
            if (cInput.equals(MemorySegment.NULL)) { checkLastFfiError(); throw new XbergRsException("extract: failed to marshal input", (Throwable) null); }
            String cConfigJson = STREAM_MAPPER.writeValueAsString(config);
            var cConfigJsonSeg = arena.allocateFrom(cConfigJson);
            MemorySegment cConfig = (MemorySegment) NativeLib.XBERG_EXTRACTION_CONFIG_FROM_JSON.invoke(cConfigJsonSeg);
            if (cConfig.equals(MemorySegment.NULL)) { checkLastFfiError(); throw new XbergRsException("extract: failed to marshal config", (Throwable) null); }
            // CPD-OFF — FFI JSON-roundtrip body, structurally identical for every named-result method.
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_ENGINE_EXTRACT.invoke(this.handle, cInput, cConfig);
            if (!cInput.equals(MemorySegment.NULL)) { try { NativeLib.XBERG_EXTRACT_INPUT_FREE.invoke(cInput); } catch (Throwable ignore) {} }
            if (!cConfig.equals(MemorySegment.NULL)) { try { NativeLib.XBERG_EXTRACTION_CONFIG_FREE.invoke(cConfig); } catch (Throwable ignore) {} }
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.XBERG_EXTRACTION_RESULT_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new XbergRsException("extract: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.XBERG_FREE_STRING.invoke(jsonPtr);
                return STREAM_MAPPER.readValue(json, ExtractionResult.class);
            } finally {
                NativeLib.XBERG_EXTRACTION_RESULT_FREE.invoke(resultPtr);
            }
            // CPD-ON
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("extract: failed", e);
        }
    }
    /**
     * Extract content from multiple bytes or URI inputs.
     */
    public ExtractionResult extractBatch(final List<ExtractInput> inputs, final ExtractionConfig config) throws XbergRsException {
        java.util.Objects.requireNonNull(inputs, "inputs must not be null");
        java.util.Objects.requireNonNull(config, "config must not be null");
        try (Arena arena = Arena.ofShared()) {
            // Unsupported parameter type for inputs
            throw new XbergRsException("extractBatch: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("extractBatch: failed", e);
        }
    }
    /**
     * The injected CacheBackend seam (default: NoopCache).
     */
    public CacheBackend cacheBackend() throws XbergRsException {
        try {
            // CPD-OFF — FFI opaque-handle return, no JSON deserialization needed.
            // The returned pointer is owned by the new wrapper, which frees it in
            // close(); freeing it here would leave the wrapper holding a dangling
            // handle (use-after-free on the next native call).
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_ENGINE_CACHE_BACKEND.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            return new CacheBackend(resultPtr);
            // CPD-ON
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("cacheBackend: failed", e);
        }
    }
    /**
     * The injected ProgressSink seam (default: NoopProgressSink).
     */
    public ProgressSink progressSink() throws XbergRsException {
        try {
            // CPD-OFF — FFI opaque-handle return, no JSON deserialization needed.
            // The returned pointer is owned by the new wrapper, which frees it in
            // close(); freeing it here would leave the wrapper holding a dangling
            // handle (use-after-free on the next native call).
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_ENGINE_PROGRESS_SINK.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            return new ProgressSink(resultPtr);
            // CPD-ON
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("progressSink: failed", e);
        }
    }
    /**
     * The injected ModelProvider seam (default: DefaultModelProvider).
     */
    public ModelProvider modelProvider() throws XbergRsException {
        try {
            // CPD-OFF — FFI opaque-handle return, no JSON deserialization needed.
            // The returned pointer is owned by the new wrapper, which frees it in
            // close(); freeing it here would leave the wrapper holding a dangling
            // handle (use-after-free on the next native call).
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_ENGINE_MODEL_PROVIDER.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            return new ModelProvider(resultPtr);
            // CPD-ON
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("modelProvider: failed", e);
        }
    }
    /**
     * Start building an Engine.
     */
    public static Engine builder() throws XbergRsException {
        try {
            var handle = (MemorySegment) NativeLib.XBERG_ENGINE_BUILDER.invoke();
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new XbergRsException("builder returned null", (Throwable) null);
            }
            return new Engine(handle);
        }
        catch (Throwable e) {
            if (e instanceof XbergRsException) throw (XbergRsException) e;
            throw new RuntimeException("builder failed: " + e.getMessage(), e);
        }
    }
    /**
     * Construct an Engine with default configuration.
     */
    public static Engine newDefault() throws XbergRsException {
        try {
            var handle = (MemorySegment) NativeLib.XBERG_ENGINE_NEW_DEFAULT.invoke();
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new XbergRsException("newDefault returned null", (Throwable) null);
            }
            return new Engine(handle);
        }
        catch (Throwable e) {
            if (e instanceof XbergRsException) throw (XbergRsException) e;
            throw new RuntimeException("newDefault failed: " + e.getMessage(), e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_ENGINE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free Engine: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = createStreamMapper();

    private static ObjectMapper createStreamMapper() {
        return new ObjectMapper()
            .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
            .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
            .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
    }    // CPD-ON
}
