// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * GLiNER ONNX architecture family. Determines which tensor I/O contract and
 * preprocessing pipeline xberg uses — only relevant when {@code hf_repo} is set,
 * since the pinned {@code xberg-io/gliner-models} catalog is always GlinerArchitecture.Gliner1.
 */
@SuppressWarnings("PMD")
public enum GlinerArchitecture {
    /**
     * Span-mode GLiNER ({@code gliner-community}/{@code urchade} lineage, the pinned
     * {@code xberg-io/gliner-models} catalog, and most GLiNER fine-tunes including
     * the {@code knowledgator/gliner-pii-*} family).
     */
    Gliner1("gliner1"),
    /**
     * Schema-prompt GLiNER2 ({@code fastino/gliner2} lineage). Requires an ONNX export
     * with {@code [P]}/{@code [E]}/{@code [SEP_TEXT]} special tokens in its tokenizer and the
     * {@code input_ids}/{@code attention_mask}/{@code text_positions}/{@code schema_positions}/{@code span_idx}
     * tensor contract. Most GLiNER2 model cards ship safetensors only and have
     * no ONNX export — check the repo's file list for a {@code .onnx} file before
     * pointing {@code hf_repo} at it.
     */
    Gliner2("gliner2");


    /** The string value. */
    private final String value;

    GlinerArchitecture(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static GlinerArchitecture fromValue(final String value) {
        for (GlinerArchitecture e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown GlinerArchitecture value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
