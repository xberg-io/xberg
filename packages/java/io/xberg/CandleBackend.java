// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import java.util.List;

/**
 * Wraps Gliner2Candle behind the NerBackend trait.
 *
 * {@code Gliner2Candle} holds candle tensors which are not {@code Send}, so we wrap it
 * in a {@code Mutex} to satisfy the {@code Send + Sync} requirement of NerBackend.
 */
@SuppressWarnings("PMD")
public class CandleBackend implements AutoCloseable {
    private final MemorySegment handle;

    CandleBackend(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    public List<Entity> detect(final String text, final List<EntityCategory> categories) throws XbergRsException {
        java.util.Objects.requireNonNull(text, "text must not be null");
        java.util.Objects.requireNonNull(categories, "categories must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cText = arena.allocateFrom(text);
            // Unsupported parameter type for categories
            throw new XbergRsException("detect: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("detect: failed", e);
        }
    }
    /**
     * Load from a local model directory. Applies {@code lora_adapter_dir} if provided.
     *
     * {@code model_dir} must contain {@code tokenizer.json} and {@code model.safetensors}.
     * {@code lora_adapter_dir}, when set, must contain {@code adapter_config.json} and
     * {@code adapter_model.safetensors} — merged into the base weights at load time.
     *
     * Not available on {@code wasm32} — {@code Gliner2Candle.from_local} requires filesystem
     * access, which wasm32 does not have. Use Self.from_bytes there instead.
     */
    public static CandleBackend fromLocal(
        final java.nio.file.Path modelDir,
        final java.nio.file.Path loraAdapterDir
    ) throws XbergRsException {
        java.util.Objects.requireNonNull(modelDir, "modelDir must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cModelDir = arena.allocateFrom(modelDir.toString());
            var cLoraAdapterDir = arena.allocateFrom(loraAdapterDir.toString());
            var handle = (MemorySegment) NativeLib.XBERG_CANDLE_BACKEND_FROM_LOCAL.invoke(cModelDir, cLoraAdapterDir);
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new XbergRsException("fromLocal returned null", (Throwable) null);
            }
            return new CandleBackend(handle);
        }
        catch (Throwable e) {
            if (e instanceof XbergRsException) throw (XbergRsException) e;
            throw new RuntimeException("fromLocal failed: " + e.getMessage(), e);
        }
    }
    /**
     * Load from in-memory model bytes (no filesystem access — required on {@code wasm32},
     * also usable natively when the caller already has the model bytes in memory).
     */
    public static CandleBackend fromBytes(
        final byte[] safetensors,
        final byte[] tokenizerJson,
        final byte[] encoderConfigJson
    ) throws XbergRsException {
        java.util.Objects.requireNonNull(safetensors, "safetensors must not be null");
        java.util.Objects.requireNonNull(tokenizerJson, "tokenizerJson must not be null");
        java.util.Objects.requireNonNull(encoderConfigJson, "encoderConfigJson must not be null");
        try {
            // Unsupported parameter type for safetensors
            throw new XbergRsException("fromBytes: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("fromBytes: failed", e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_CANDLE_BACKEND_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CandleBackend: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }    // CPD-ON
}
