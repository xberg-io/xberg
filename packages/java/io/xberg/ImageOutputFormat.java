// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Target format for re-encoding extracted images.
 *
 * Controls whether and how extracted images are normalised to a uniform
 * container format before being returned in {@code ExtractionResult.images}.
 * The default ({@code Native}) preserves the format produced by each extractor
 * without any additional encode pass.
 *
 * Callers that need uniform output — e.g. cloud pipelines that always store
 * WebP thumbnails — set this once on {@code ImageExtractionConfig.output_format}
 * rather than re-encoding downstream.
 *
 * # Serde shape
 *
 * Uses a tagged enum: {@code {"type": "native"}}, {@code {"type": "png"}},
 * {@code {"type": "jpeg", "quality": 90}}, etc.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = false)
@JsonSubTypes({
    @JsonSubTypes.Type(value = ImageOutputFormat.Native.class, name = "native"),
    @JsonSubTypes.Type(value = ImageOutputFormat.Png.class, name = "png"),
    @JsonSubTypes.Type(value = ImageOutputFormat.Jpeg.class, name = "jpeg"),
    @JsonSubTypes.Type(value = ImageOutputFormat.Webp.class, name = "webp"),
    @JsonSubTypes.Type(value = ImageOutputFormat.Heif.class, name = "heif"),
    @JsonSubTypes.Type(value = ImageOutputFormat.Svg.class, name = "svg")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface ImageOutputFormat {

    /**
     * Preserve whatever format the extractor produced (default).
     *
     * No re-encode pass is performed. {@code ExtractedImage.format} reflects the
     * source format: JPEG for embedded PDF images, PNG for rasterised content,
     * or the native container format from office documents.
     */
    record Native() implements ImageOutputFormat {
    }

    /**
     * Re-encode all extracted images as PNG (lossless).
     */
    record Png() implements ImageOutputFormat {
    }

    /**
     * Re-encode all extracted images as JPEG at the given quality level.
     *
     * {@code quality} must be in {@code 1..=100}. Values outside this range are clamped
     * and a warning is emitted. Higher values produce larger files with less
     * artefacting; 85 is a reasonable default.
     */
    record Jpeg(@JsonProperty("quality") byte quality) implements ImageOutputFormat { }

    /**
     * Re-encode all extracted images as WebP at the given quality level.
     *
     * {@code quality} must be in {@code 1..=100}. Values outside this range are clamped
     * and a warning is emitted. 80 is a reasonable default.
     */
    record Webp(@JsonProperty("quality") byte quality) implements ImageOutputFormat { }

    /**
     * Re-encode all extracted images as HEIF/HEIC at the given quality level.
     *
     * Requires the {@code heic} feature. {@code quality} must be in {@code 1..=100}. Values
     * outside this range are clamped and a warning is emitted. 80 is a
     * reasonable default.
     */
    record Heif(@JsonProperty("quality") byte quality) implements ImageOutputFormat { }

    /**
     * Output pure-vector SVG. Lossless. Raster sources are not re-encoded
     * (a warning is emitted and the image bytes are left untouched).
     *
     * When the source is already SVG, the bytes are passed through the
     * {@code usvg} sanitizer (strips external hrefs, JS event handlers, and
     * {@code foreignObject} elements) when SvgOptions.sanitize is {@code true}.
     *
     * Requires the {@code svg} feature.
     */
    record Svg() implements ImageOutputFormat {
    }
}
