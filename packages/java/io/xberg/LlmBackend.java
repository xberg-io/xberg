// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.List;

/**
 * liter-llm-backed NER backend.
 */
@SuppressWarnings("PMD")
public class LlmBackend implements AutoCloseable {
    private final MemorySegment handle;

    LlmBackend(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    public List<Entity> detect(final String text, final List<EntityCategory> categories) throws XbergRsException {
        java.util.Objects.requireNonNull(text, "text must not be null");
        java.util.Objects.requireNonNull(categories, "categories must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cText = arena.allocateFrom(text);
            // Unsupported parameter type for categories
            throw new XbergRsException("detect: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("detect: failed", e);
        }
    }
    public List<Entity> detectWithCustom(
        final String text,
        final List<EntityCategory> categories,
        final List<String> customLabels
    ) throws XbergRsException {
        java.util.Objects.requireNonNull(text, "text must not be null");
        java.util.Objects.requireNonNull(categories, "categories must not be null");
        java.util.Objects.requireNonNull(customLabels, "customLabels must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cText = arena.allocateFrom(text);
            // Unsupported parameter type for categories
            throw new XbergRsException("detectWithCustom: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("detectWithCustom: failed", e);
        }
    }
    /**
     * Create a new LLM-backed NER backend with the given LLM configuration.
     */
    public static LlmBackend create(final LlmConfig config) throws XbergRsException {
        java.util.Objects.requireNonNull(config, "config must not be null");
        try (Arena arena = Arena.ofShared()) {
            String cConfigJson = STREAM_MAPPER.writeValueAsString(config);
            var cConfigJsonSeg = arena.allocateFrom(cConfigJson);
            MemorySegment cConfig = (MemorySegment) NativeLib.XBERG_LLM_CONFIG_FROM_JSON.invoke(cConfigJsonSeg);
            if (cConfig.equals(MemorySegment.NULL)) { checkLastFfiError(); throw new XbergRsException("create: failed to marshal config", (Throwable) null); }
            var handle = (MemorySegment) NativeLib.XBERG_LLM_BACKEND_NEW.invoke(cConfig);
            if (!cConfig.equals(MemorySegment.NULL)) { try { NativeLib.XBERG_LLM_CONFIG_FREE.invoke(cConfig); } catch (Throwable ignore) {} }
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new XbergRsException("create returned null", (Throwable) null);
            }
            return new LlmBackend(handle);
        }
        catch (Throwable e) {
            if (e instanceof XbergRsException) throw (XbergRsException) e;
            throw new RuntimeException("create failed: " + e.getMessage(), e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_LLM_BACKEND_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free LlmBackend: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = createStreamMapper();

    private static ObjectMapper createStreamMapper() {
        return new ObjectMapper()
            .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
            .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
            .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
    }    // CPD-ON
}
