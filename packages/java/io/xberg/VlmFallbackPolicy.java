// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Policy controlling when VLM (Vision Language Model) OCR is used as a fallback.
 *
 * This knob is syntactic sugar over the explicit OcrPipelineConfig stage
 * ordering. When {@code vlm_fallback} is set and {@code pipeline} is {@code None}, an equivalent
 * pipeline is synthesised at extraction time:
 *
 * - VlmFallbackPolicy.Disabled — no synthesis; single-backend mode (default).
 * - VlmFallbackPolicy.OnLowQuality — tries the classical backend first; if the
 *   result scores below {@code quality_threshold}, tries VLM.
 * - VlmFallbackPolicy.Always — skips the classical backend and sends every page
 *   to the VLM.
 *
 * When OcrConfig.pipeline is explicitly set, {@code vlm_fallback} is ignored — the
 * explicit pipeline takes precedence.
 * {@literal @}throws Exception Both {@code OnLowQuality} and {@code Always} require OcrConfig.vlm_config to be {@code Some}.
 * Constructing an OcrConfig with one of these policies but no {@code vlm_config} is
 * detected by {@code OcrConfig.validate} and will surface as a
 * {@code Validation} error at extraction time, not a panic.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "mode", visible = false)
@JsonSubTypes({
    @JsonSubTypes.Type(value = VlmFallbackPolicy.Disabled.class, name = "disabled"),
    @JsonSubTypes.Type(value = VlmFallbackPolicy.OnLowQuality.class, name = "on_low_quality"),
    @JsonSubTypes.Type(value = VlmFallbackPolicy.Always.class, name = "always")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface VlmFallbackPolicy {

    /**
     * No VLM fallback (default). Behaves identically to the pre-policy single-backend mode.
     */
    record Disabled() implements VlmFallbackPolicy {
    }

    /**
     * Try the classical OCR backend first. If the quality score is below
     * {@code quality_threshold}, send the page to the VLM.
     *
     * {@code quality_threshold} is in the {@code [0.0, 1.0]} range produced by
     * {@code calculate_quality_score}. A value of {@code 0.5} is a
     * reasonable starting point; calibrate with the Stage 0 benchmark harness.
     */
    record OnLowQuality(@JsonProperty("quality_threshold") double qualityThreshold) implements VlmFallbackPolicy { }

    /**
     * Skip the classical OCR backend entirely. Every page is sent to the VLM.
     */
    record Always() implements VlmFallbackPolicy {
    }
}
