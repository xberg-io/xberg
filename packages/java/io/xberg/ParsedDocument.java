// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;

/**
 * A single-operation memo of a document's parse facts and rendered pages.
 *
 * Cheaply shareable: the source bytes are held behind an Arc, and the
 * render cache is interior-mutable so rendered_pages(Self.rendered_pages)
 * can take {@code &amp;self}.
 */
@SuppressWarnings("PMD")
public class ParsedDocument implements AutoCloseable {
    private final MemorySegment handle;

    ParsedDocument(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    /**
     * The detected MIME type.
     */
    public String mimeType() throws XbergRsException {
        try {
            MemorySegment resultPtr = (MemorySegment) NativeLib.XBERG_PARSED_DOCUMENT_MIME_TYPE.invoke(this.handle);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return null;
            }
            try {
                String result = resultPtr.reinterpret(Long.MAX_VALUE).getString(0);
                return result;
            } finally {
                NativeLib.XBERG_FREE_STRING.invoke(resultPtr);
            }
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("mimeType: failed", e);
        }
    }
    /**
     * The page count (1 for non-PDF inputs).
     */
    public int pageCount() throws XbergRsException {
        try {
            var result = (int)(long) NativeLib.XBERG_PARSED_DOCUMENT_PAGE_COUNT.invoke(this.handle);
            return result;
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("pageCount: failed", e);
        }
    }
    /**
     * The shared source bytes.
     */
    public byte[] source() throws XbergRsException {
        try {
            // Unsupported return shape for source
            throw new XbergRsException("source: unsupported return shape", (Throwable) null);
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("source: failed", e);
        }
    }
    /**
     * Build a memo from shared source bytes: detect the MIME type and count
     * pages (PDF via {@code pdf_page_count}; every other type is
     * treated as a single page). Does not render — rendering is lazy.
     * {@literal @}throws Exception Propagates MIME-detection and PDF page-count failures.
     */
    public static ParsedDocument fromBytes(final byte[] source) throws XbergRsException {
        java.util.Objects.requireNonNull(source, "source must not be null");
        try {
            // Unsupported parameter type for source
            throw new XbergRsException("fromBytes: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("fromBytes: failed", e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_PARSED_DOCUMENT_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free ParsedDocument: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }    // CPD-ON
}
