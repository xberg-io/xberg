// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Outcome of a single validator on a single candidate match.
 */
@SuppressWarnings("PMD")
public enum ValidationResult {
    /**
     * Pass-through, no change.
     */
    Accept("accept"),
    /**
     * Drop the candidate. {@code reason} is a static identifier used as a
     * RejectionCounts key (e.g. {@code "iban_checksum_failed"}).
     */
    Reject("reject");


    /** The string value. */
    private final String value;

    ValidationResult(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static ValidationResult fromValue(final String value) {
        for (ValidationResult e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown ValidationResult value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
