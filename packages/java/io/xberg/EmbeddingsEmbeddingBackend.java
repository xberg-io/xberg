// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Inference backend that an EmbeddingPreset runs on.
 *
 * {@code Onnx} presets require the {@code embeddings} feature (ONNX Runtime, not available on
 * WASM/Android x86_64 emulator). {@code Static} presets require {@code static-embeddings}
 * (pure-Rust model2vec inference, no ORT — the only dense-embedding backend
 * available on {@code no-ort-target}).
 *
 * Defaults to {@code Onnx} via {@code #[serde(default)]} so every existing preset payload
 * (which predates this field) keeps deserializing without change.
 */
@SuppressWarnings("PMD")
public enum EmbeddingsEmbeddingBackend {
    /**
     * ONNX Runtime transformer inference (the historical, default backend).
     */
    Onnx("onnx"),
    /**
     * Pure-Rust static (model2vec) inference — no ONNX Runtime.
     */
    Static("static");


    /** The string value. */
    private final String value;

    EmbeddingsEmbeddingBackend(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static EmbeddingsEmbeddingBackend fromValue(final String value) {
        for (EmbeddingsEmbeddingBackend e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown EmbeddingsEmbeddingBackend value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
