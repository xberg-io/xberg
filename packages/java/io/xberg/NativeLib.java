// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                // Library loaded via System.loadLibrary() without absolute path
                LIB = LINKER.defaultLookup();
            }
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libxberg_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "xberg_ffi";
            libExt = ".dll";
        } else {
            libName = "libxberg_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
        if (extracted != null) {
            loadedLibraryPath = extracted;
            return loadedLibraryPath;
        }

        try {
            System.loadLibrary("xberg_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load xberg_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "or place it on the system library path (java.library.path).";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            System.err.println("[NativeLib] Failed to extract and load native library from resources: " + e.getMessage());
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for xberg_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("xberg_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle XBERG_EXTRACT = LINKER.downcallHandle(
        LIB.find("xberg_extract")
            .or(() -> LIB.find("_xberg_extract"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract" +
                " (tried: xberg_extract, _xberg_extract). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_BATCH = LINKER.downcallHandle(
        LIB.find("xberg_extract_batch")
            .or(() -> LIB.find("_xberg_extract_batch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_batch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_batch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract_batch" +
                " (tried: xberg_extract_batch, _xberg_extract_batch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_MIME_TYPE_FROM_BYTES = LINKER.downcallHandle(
        LIB.find("xberg_detect_mime_type_from_bytes")
            .or(() -> LIB.find("_xberg_detect_mime_type_from_bytes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_mime_type_from_bytes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_mime_type_from_bytes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_detect_mime_type_from_bytes" +
                " (tried: xberg_detect_mime_type_from_bytes, _xberg_detect_mime_type_from_bytes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_DETECT_MIME_TYPE_FROM_BYTES_LEN = LINKER.downcallHandle(
        LIB.find("xberg_detect_mime_type_from_bytes_len")
            .or(() -> LIB.find("_xberg_detect_mime_type_from_bytes_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_mime_type_from_bytes_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_mime_type_from_bytes_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_GET_EXTENSIONS_FOR_MIME = LINKER.downcallHandle(
        LIB.find("xberg_get_extensions_for_mime")
            .or(() -> LIB.find("_xberg_get_extensions_for_mime"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_get_extensions_for_mime"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_get_extensions_for_mime"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_get_extensions_for_mime" +
                " (tried: xberg_get_extensions_for_mime, _xberg_get_extensions_for_mime). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_SUPPORTED_FORMATS = LINKER.downcallHandle(
        LIB.find("xberg_list_supported_formats")
            .or(() -> LIB.find("_xberg_list_supported_formats"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_supported_formats"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_supported_formats"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_supported_formats" +
                " (tried: xberg_list_supported_formats, _xberg_list_supported_formats). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_QR_CODES = LINKER.downcallHandle(
        LIB.find("xberg_detect_qr_codes")
            .or(() -> LIB.find("_xberg_detect_qr_codes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_qr_codes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_qr_codes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_detect_qr_codes" +
                " (tried: xberg_detect_qr_codes, _xberg_detect_qr_codes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_EMBEDDING_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_embedding_backends")
            .or(() -> LIB.find("_xberg_list_embedding_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_embedding_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_embedding_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_embedding_backends" +
                " (tried: xberg_list_embedding_backends, _xberg_list_embedding_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_OCR_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_ocr_backends")
            .or(() -> LIB.find("_xberg_list_ocr_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_ocr_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_ocr_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_ocr_backends" +
                " (tried: xberg_list_ocr_backends, _xberg_list_ocr_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTER_BUILTIN = LINKER.downcallHandle(
        LIB.find("xberg_register_builtin")
            .or(() -> LIB.find("_xberg_register_builtin"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_register_builtin"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_register_builtin"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_register_builtin" +
                " (tried: xberg_register_builtin, _xberg_register_builtin). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle XBERG_LIST_POST_PROCESSORS = LINKER.downcallHandle(
        LIB.find("xberg_list_post_processors")
            .or(() -> LIB.find("_xberg_list_post_processors"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_post_processors"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_post_processors"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_post_processors" +
                " (tried: xberg_list_post_processors, _xberg_list_post_processors). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_RENDERERS = LINKER.downcallHandle(
        LIB.find("xberg_list_renderers")
            .or(() -> LIB.find("_xberg_list_renderers"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_renderers"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_renderers"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_renderers" +
                " (tried: xberg_list_renderers, _xberg_list_renderers). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_RERANKER_BACKENDS = LINKER.downcallHandle(
        LIB.find("xberg_list_reranker_backends")
            .or(() -> LIB.find("_xberg_list_reranker_backends"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_reranker_backends"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_reranker_backends"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_reranker_backends" +
                " (tried: xberg_list_reranker_backends, _xberg_list_reranker_backends). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_VALIDATORS = LINKER.downcallHandle(
        LIB.find("xberg_list_validators")
            .or(() -> LIB.find("_xberg_list_validators"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_validators"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_validators"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_validators" +
                " (tried: xberg_list_validators, _xberg_list_validators). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFY_PAGES = LINKER.downcallHandle(
        LIB.find("xberg_classify_pages")
            .or(() -> LIB.find("_xberg_classify_pages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classify_pages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classify_pages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_classify_pages" +
                " (tried: xberg_classify_pages, _xberg_classify_pages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFY_TEXT = LINKER.downcallHandle(
        LIB.find("xberg_classify_text")
            .or(() -> LIB.find("_xberg_classify_text"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classify_text"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classify_text"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_classify_text" +
                " (tried: xberg_classify_text, _xberg_classify_text). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CLASSIFY_DOCUMENT = LINKER.downcallHandle(
        LIB.find("xberg_classify_document")
            .or(() -> LIB.find("_xberg_classify_document"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_classify_document"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_classify_document"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_classify_document" +
                " (tried: xberg_classify_document, _xberg_classify_document). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOWNLOAD_MODEL = LINKER.downcallHandle(
        LIB.find("xberg_download_model")
            .or(() -> LIB.find("_xberg_download_model"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_download_model"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_download_model"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_download_model" +
                " (tried: xberg_download_model, _xberg_download_model). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle XBERG_DOWNLOAD_MODEL_LEN = LINKER.downcallHandle(
        LIB.find("xberg_download_model_len")
            .or(() -> LIB.find("_xberg_download_model_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_download_model_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_download_model_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DEFAULT_MODEL_NAME = LINKER.downcallHandle(
        LIB.find("xberg_default_model_name")
            .or(() -> LIB.find("_xberg_default_model_name"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_default_model_name"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_default_model_name"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_default_model_name" +
                " (tried: xberg_default_model_name, _xberg_default_model_name). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );

    static final MethodHandle XBERG_DEFAULT_MODEL_NAME_LEN = LINKER.downcallHandle(
        LIB.find("xberg_default_model_name_len")
            .or(() -> LIB.find("_xberg_default_model_name_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_default_model_name_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_default_model_name_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_KNOWN_MODELS = LINKER.downcallHandle(
        LIB.find("xberg_known_models")
            .or(() -> LIB.find("_xberg_known_models"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_known_models"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_known_models"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_known_models" +
                " (tried: xberg_known_models, _xberg_known_models). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACT = LINKER.downcallHandle(
        LIB.find("xberg_redact")
            .or(() -> LIB.find("_xberg_redact"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redact"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redact"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_redact" +
                " (tried: xberg_redact, _xberg_redact). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FIND_ALL = LIB.find("xberg_find_all")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_SCAN_TEXT = LIB.find("xberg_scan_text")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_SUMMARIZE = LINKER.downcallHandle(
        LIB.find("xberg_summarize")
            .or(() -> LIB.find("_xberg_summarize"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_summarize"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_summarize"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_summarize" +
                " (tried: xberg_summarize, _xberg_summarize). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_SUMMARIZE_LEN = LINKER.downcallHandle(
        LIB.find("xberg_summarize_len")
            .or(() -> LIB.find("_xberg_summarize_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_summarize_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_summarize_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_TOKEN_COUNT = LINKER.downcallHandle(
        LIB.find("xberg_token_count")
            .or(() -> LIB.find("_xberg_token_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_token_count" +
                " (tried: xberg_token_count, _xberg_token_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATE_RESULT = LINKER.downcallHandle(
        LIB.find("xberg_translate_result")
            .or(() -> LIB.find("_xberg_translate_result"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translate_result"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translate_result"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_translate_result" +
                " (tried: xberg_translate_result, _xberg_translate_result). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FIND_FOOTNOTE_ANCHORS = LINKER.downcallHandle(
        LIB.find("xberg_find_footnote_anchors")
            .or(() -> LIB.find("_xberg_find_footnote_anchors"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_find_footnote_anchors"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_find_footnote_anchors"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_find_footnote_anchors" +
                " (tried: xberg_find_footnote_anchors, _xberg_find_footnote_anchors). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PARSE_FOOTNOTE_DEFINITIONS = LINKER.downcallHandle(
        LIB.find("xberg_parse_footnote_definitions")
            .or(() -> LIB.find("_xberg_parse_footnote_definitions"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_parse_footnote_definitions"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_parse_footnote_definitions"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_parse_footnote_definitions" +
                " (tried: xberg_parse_footnote_definitions, _xberg_parse_footnote_definitions). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FIND_INFERENCE_MARKERS = LINKER.downcallHandle(
        LIB.find("xberg_find_inference_markers")
            .or(() -> LIB.find("_xberg_find_inference_markers"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_find_inference_markers"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_find_inference_markers"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_find_inference_markers" +
                " (tried: xberg_find_inference_markers, _xberg_find_inference_markers). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FIND_UNMARKED_CLAIMS = LINKER.downcallHandle(
        LIB.find("xberg_find_unmarked_claims")
            .or(() -> LIB.find("_xberg_find_unmarked_claims"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_find_unmarked_claims"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_find_unmarked_claims"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_find_unmarked_claims" +
                " (tried: xberg_find_unmarked_claims, _xberg_find_unmarked_claims). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PARSE_CITATIONS = LINKER.downcallHandle(
        LIB.find("xberg_parse_citations")
            .or(() -> LIB.find("_xberg_parse_citations"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_parse_citations"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_parse_citations"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_parse_citations" +
                " (tried: xberg_parse_citations, _xberg_parse_citations). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_VERIFY_EXCERPT = LINKER.downcallHandle(
        LIB.find("xberg_verify_excerpt")
            .or(() -> LIB.find("_xberg_verify_excerpt"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_verify_excerpt"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_verify_excerpt"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_verify_excerpt" +
                " (tried: xberg_verify_excerpt, _xberg_verify_excerpt). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_FOR_RAG = LINKER.downcallHandle(
        LIB.find("xberg_chunk_for_rag")
            .or(() -> LIB.find("_xberg_chunk_for_rag"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_for_rag"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_for_rag"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_chunk_for_rag" +
                " (tried: xberg_chunk_for_rag, _xberg_chunk_for_rag). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_COMPARE = LINKER.downcallHandle(
        LIB.find("xberg_compare")
            .or(() -> LIB.find("_xberg_compare"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_compare"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_compare"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_compare" +
                " (tried: xberg_compare, _xberg_compare). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_REGION_WITH_VLM = LINKER.downcallHandle(
        LIB.find("xberg_extract_region_with_vlm")
            .or(() -> LIB.find("_xberg_extract_region_with_vlm"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_region_with_vlm"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_region_with_vlm"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract_region_with_vlm" +
                " (tried: xberg_extract_region_with_vlm, _xberg_extract_region_with_vlm). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );

    static final MethodHandle XBERG_EXTRACT_REGION_WITH_VLM_LEN = LINKER.downcallHandle(
        LIB.find("xberg_extract_region_with_vlm_len")
            .or(() -> LIB.find("_xberg_extract_region_with_vlm_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_region_with_vlm_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_region_with_vlm_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle XBERG_RERANK_ASYNC = LINKER.downcallHandle(
        LIB.find("xberg_rerank_async")
            .or(() -> LIB.find("_xberg_rerank_async"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_rerank_async"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_rerank_async"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_rerank_async" +
                " (tried: xberg_rerank_async, _xberg_rerank_async). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_KEYWORDS = LINKER.downcallHandle(
        LIB.find("xberg_extract_keywords")
            .or(() -> LIB.find("_xberg_extract_keywords"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_keywords"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_keywords"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract_keywords" +
                " (tried: xberg_extract_keywords, _xberg_extract_keywords). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_ANALYZE_DOCUMENT = LINKER.downcallHandle(
        LIB.find("xberg_analyze_document")
            .or(() -> LIB.find("_xberg_analyze_document"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_analyze_document"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_analyze_document"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_analyze_document" +
                " (tried: xberg_analyze_document, _xberg_analyze_document). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_ANALYZE_WITH_USER_CHUNKS = LINKER.downcallHandle(
        LIB.find("xberg_analyze_with_user_chunks")
            .or(() -> LIB.find("_xberg_analyze_with_user_chunks"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_analyze_with_user_chunks"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_analyze_with_user_chunks"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_analyze_with_user_chunks" +
                " (tried: xberg_analyze_with_user_chunks, _xberg_analyze_with_user_chunks). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_SCORE_CONFIDENCE = LINKER.downcallHandle(
        LIB.find("xberg_score_confidence")
            .or(() -> LIB.find("_xberg_score_confidence"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_score_confidence"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_score_confidence"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_score_confidence" +
                " (tried: xberg_score_confidence, _xberg_score_confidence). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHECK_FORMAT_LIMITS = LINKER.downcallHandle(
        LIB.find("xberg_check_format_limits")
            .or(() -> LIB.find("_xberg_check_format_limits"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_check_format_limits"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_check_format_limits"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_check_format_limits" +
                " (tried: xberg_check_format_limits, _xberg_check_format_limits). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS
        )
    );

    static final MethodHandle XBERG_CHECK_FORMAT_LIMITS_LEN = LINKER.downcallHandle(
        LIB.find("xberg_check_format_limits_len")
            .or(() -> LIB.find("_xberg_check_format_limits_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_check_format_limits_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_check_format_limits_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle XBERG_BOUNDARIES_FROM_EXTRACTION_RESULT = LINKER.downcallHandle(
        LIB.find("xberg_boundaries_from_extraction_result")
            .or(() -> LIB.find("_xberg_boundaries_from_extraction_result"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_boundaries_from_extraction_result"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_boundaries_from_extraction_result"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_boundaries_from_extraction_result" +
                " (tried: xberg_boundaries_from_extraction_result, _xberg_boundaries_from_extraction_result). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_BOUNDARIES = LINKER.downcallHandle(
        LIB.find("xberg_detect_boundaries")
            .or(() -> LIB.find("_xberg_detect_boundaries"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_boundaries"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_boundaries"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_detect_boundaries" +
                " (tried: xberg_detect_boundaries, _xberg_detect_boundaries). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHOOSE_CALL_MODE = LINKER.downcallHandle(
        LIB.find("xberg_choose_call_mode")
            .or(() -> LIB.find("_xberg_choose_call_mode"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_choose_call_mode"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_choose_call_mode"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_choose_call_mode" +
                " (tried: xberg_choose_call_mode, _xberg_choose_call_mode). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CALCULATE_CHUNK_PLAN = LINKER.downcallHandle(
        LIB.find("xberg_calculate_chunk_plan")
            .or(() -> LIB.find("_xberg_calculate_chunk_plan"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_calculate_chunk_plan"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_calculate_chunk_plan"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_calculate_chunk_plan" +
                " (tried: xberg_calculate_chunk_plan, _xberg_calculate_chunk_plan). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CALCULATE_PLAN_FROM_OVERRIDES = LINKER.downcallHandle(
        LIB.find("xberg_calculate_plan_from_overrides")
            .or(() -> LIB.find("_xberg_calculate_plan_from_overrides"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_calculate_plan_from_overrides"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_calculate_plan_from_overrides"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_calculate_plan_from_overrides" +
                " (tried: xberg_calculate_plan_from_overrides, _xberg_calculate_plan_from_overrides). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FINGERPRINT = LINKER.downcallHandle(
        LIB.find("xberg_fingerprint")
            .or(() -> LIB.find("_xberg_fingerprint"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_fingerprint"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_fingerprint"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_fingerprint" +
                " (tried: xberg_fingerprint, _xberg_fingerprint). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_FINGERPRINT_LEN = LINKER.downcallHandle(
        LIB.find("xberg_fingerprint_len")
            .or(() -> LIB.find("_xberg_fingerprint_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_fingerprint_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_fingerprint_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_RESOLVE = LINKER.downcallHandle(
        LIB.find("xberg_resolve")
            .or(() -> LIB.find("_xberg_resolve"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_resolve"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_resolve"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_resolve" +
                " (tried: xberg_resolve, _xberg_resolve). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_STRUCTURED_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extract_structured_json")
            .or(() -> LIB.find("_xberg_extract_structured_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_structured_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_structured_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_extract_structured_json" +
                " (tried: xberg_extract_structured_json, _xberg_extract_structured_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );

    static final MethodHandle XBERG_EXTRACT_STRUCTURED_JSON_LEN = LINKER.downcallHandle(
        LIB.find("xberg_extract_structured_json_len")
            .or(() -> LIB.find("_xberg_extract_structured_json_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_structured_json_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_structured_json_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle XBERG_SPLIT_AND_EXTRACT_JSON = LINKER.downcallHandle(
        LIB.find("xberg_split_and_extract_json")
            .or(() -> LIB.find("_xberg_split_and_extract_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_split_and_extract_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_split_and_extract_json"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_split_and_extract_json" +
                " (tried: xberg_split_and_extract_json, _xberg_split_and_extract_json). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );

    static final MethodHandle XBERG_SPLIT_AND_EXTRACT_JSON_LEN = LINKER.downcallHandle(
        LIB.find("xberg_split_and_extract_json_len")
            .or(() -> LIB.find("_xberg_split_and_extract_json_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_split_and_extract_json_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_split_and_extract_json_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle XBERG_RENDER_PDF_PAGE_TO_PNG = LINKER.downcallHandle(
        LIB.find("xberg_render_pdf_page_to_png")
            .or(() -> LIB.find("_xberg_render_pdf_page_to_png"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_render_pdf_page_to_png"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_render_pdf_page_to_png"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_render_pdf_page_to_png" +
                " (tried: xberg_render_pdf_page_to_png, _xberg_render_pdf_page_to_png). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS,
            ValueLayout.ADDRESS
        )
    );


    static final MethodHandle XBERG_PDF_PAGE_COUNT = LINKER.downcallHandle(
        LIB.find("xberg_pdf_page_count")
            .or(() -> LIB.find("_xberg_pdf_page_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_pdf_page_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_pdf_page_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_pdf_page_count" +
                " (tried: xberg_pdf_page_count, _xberg_pdf_page_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CAPTION_IMAGE = LINKER.downcallHandle(
        LIB.find("xberg_caption_image")
            .or(() -> LIB.find("_xberg_caption_image"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_caption_image"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_caption_image"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_caption_image" +
                " (tried: xberg_caption_image, _xberg_caption_image). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle XBERG_CAPTION_IMAGE_LEN = LINKER.downcallHandle(
        LIB.find("xberg_caption_image_len")
            .or(() -> LIB.find("_xberg_caption_image_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_caption_image_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_caption_image_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CAPTION_IMAGE_FILE = LINKER.downcallHandle(
        LIB.find("xberg_caption_image_file")
            .or(() -> LIB.find("_xberg_caption_image_file"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_caption_image_file"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_caption_image_file"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_caption_image_file" +
                " (tried: xberg_caption_image_file, _xberg_caption_image_file). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle XBERG_CAPTION_IMAGE_FILE_LEN = LINKER.downcallHandle(
        LIB.find("xberg_caption_image_file_len")
            .or(() -> LIB.find("_xberg_caption_image_file_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_caption_image_file_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_caption_image_file_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DETECT_MIME_TYPE = LINKER.downcallHandle(
        LIB.find("xberg_detect_mime_type")
            .or(() -> LIB.find("_xberg_detect_mime_type"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_mime_type"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_mime_type"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_detect_mime_type" +
                " (tried: xberg_detect_mime_type, _xberg_detect_mime_type). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_DETECT_MIME_TYPE_LEN = LINKER.downcallHandle(
        LIB.find("xberg_detect_mime_type_len")
            .or(() -> LIB.find("_xberg_detect_mime_type_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_detect_mime_type_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_detect_mime_type_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_EMBED_TEXTS = LIB.find("xberg_embed_texts")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_EMBED_TEXTS_ASYNC = LINKER.downcallHandle(
        LIB.find("xberg_embed_texts_async")
            .or(() -> LIB.find("_xberg_embed_texts_async"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embed_texts_async"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embed_texts_async"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_embed_texts_async" +
                " (tried: xberg_embed_texts_async, _xberg_embed_texts_async). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_GET_EMBEDDING_PRESET = LINKER.downcallHandle(
        LIB.find("xberg_get_embedding_preset")
            .or(() -> LIB.find("_xberg_get_embedding_preset"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_get_embedding_preset"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_get_embedding_preset"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_get_embedding_preset" +
                " (tried: xberg_get_embedding_preset, _xberg_get_embedding_preset). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_EMBEDDING_PRESETS = LINKER.downcallHandle(
        LIB.find("xberg_list_embedding_presets")
            .or(() -> LIB.find("_xberg_list_embedding_presets"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_embedding_presets"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_embedding_presets"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_embedding_presets" +
                " (tried: xberg_list_embedding_presets, _xberg_list_embedding_presets). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANK = LINKER.downcallHandle(
        LIB.find("xberg_rerank")
            .or(() -> LIB.find("_xberg_rerank"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_rerank"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_rerank"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_rerank" +
                " (tried: xberg_rerank, _xberg_rerank). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_GET_RERANKER_PRESET = LINKER.downcallHandle(
        LIB.find("xberg_get_reranker_preset")
            .or(() -> LIB.find("_xberg_get_reranker_preset"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_get_reranker_preset"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_get_reranker_preset"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_get_reranker_preset" +
                " (tried: xberg_get_reranker_preset, _xberg_get_reranker_preset). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LIST_RERANKER_PRESETS = LINKER.downcallHandle(
        LIB.find("xberg_list_reranker_presets")
            .or(() -> LIB.find("_xberg_list_reranker_presets"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_list_reranker_presets"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_list_reranker_presets"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_list_reranker_presets" +
                " (tried: xberg_list_reranker_presets, _xberg_list_reranker_presets). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FREE_STRING = LINKER.downcallHandle(
        LIB.find("xberg_free_string")
            .or(() -> LIB.find("_xberg_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_FREE_BYTES = LINKER.downcallHandle(
        LIB.find("xberg_free_bytes")
            .or(() -> LIB.find("_xberg_free_bytes"))  // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_free_bytes"))  // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_free_bytes"))  // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_LLM_BACKEND_NEW = LINKER.downcallHandle(
        LIB.find("xberg_llm_backend_new")
            .or(() -> LIB.find("_xberg_llm_backend_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_backend_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_backend_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_llm_backend_new" +
                " (tried: xberg_llm_backend_new, _xberg_llm_backend_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_BACKEND_DETECT = LINKER.downcallHandle(
        LIB.find("xberg_llm_backend_detect")
            .or(() -> LIB.find("_xberg_llm_backend_detect"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_backend_detect"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_backend_detect"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_llm_backend_detect" +
                " (tried: xberg_llm_backend_detect, _xberg_llm_backend_detect). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_BACKEND_DETECT_WITH_CUSTOM = LINKER.downcallHandle(
        LIB.find("xberg_llm_backend_detect_with_custom")
            .or(() -> LIB.find("_xberg_llm_backend_detect_with_custom"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_backend_detect_with_custom"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_backend_detect_with_custom"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_llm_backend_detect_with_custom" +
                " (tried: xberg_llm_backend_detect_with_custom, _xberg_llm_backend_detect_with_custom). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_COUNTER_NEW = LINKER.downcallHandle(
        LIB.find("xberg_token_counter_new")
            .or(() -> LIB.find("_xberg_token_counter_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_counter_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_counter_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_token_counter_new" +
                " (tried: xberg_token_counter_new, _xberg_token_counter_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_COMPILE = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_compile")
            .or(() -> LIB.find("_xberg_meta_schema_compile"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_compile"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_compile"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_meta_schema_compile" +
                " (tried: xberg_meta_schema_compile, _xberg_meta_schema_compile). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_PARSE_PRESET = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_parse_preset")
            .or(() -> LIB.find("_xberg_meta_schema_parse_preset"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_parse_preset"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_parse_preset"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_meta_schema_parse_preset" +
                " (tried: xberg_meta_schema_parse_preset, _xberg_meta_schema_parse_preset). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle XBERG_REGISTRY_LOAD_EMBEDDED = LINKER.downcallHandle(
        LIB.find("xberg_registry_load_embedded")
            .or(() -> LIB.find("_xberg_registry_load_embedded"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_load_embedded"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_load_embedded"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_load_embedded" +
                " (tried: xberg_registry_load_embedded, _xberg_registry_load_embedded). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_GET = LINKER.downcallHandle(
        LIB.find("xberg_registry_get")
            .or(() -> LIB.find("_xberg_registry_get"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_get"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_get"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_get" +
                " (tried: xberg_registry_get, _xberg_registry_get). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_SUMMARIES = LINKER.downcallHandle(
        LIB.find("xberg_registry_summaries")
            .or(() -> LIB.find("_xberg_registry_summaries"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_summaries"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_summaries"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_summaries" +
                " (tried: xberg_registry_summaries, _xberg_registry_summaries). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_LEN = LINKER.downcallHandle(
        LIB.find("xberg_registry_len")
            .or(() -> LIB.find("_xberg_registry_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_len"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_len" +
                " (tried: xberg_registry_len, _xberg_registry_len). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_IS_EMPTY = LINKER.downcallHandle(
        LIB.find("xberg_registry_is_empty")
            .or(() -> LIB.find("_xberg_registry_is_empty"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_is_empty"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_is_empty"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_is_empty" +
                " (tried: xberg_registry_is_empty, _xberg_registry_is_empty). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_SAMPLE_BYTES = LINKER.downcallHandle(
        LIB.find("xberg_registry_sample_bytes")
            .or(() -> LIB.find("_xberg_registry_sample_bytes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_sample_bytes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_sample_bytes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_sample_bytes" +
                " (tried: xberg_registry_sample_bytes, _xberg_registry_sample_bytes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_EXTEND_FROM_DIR = LINKER.downcallHandle(
        LIB.find("xberg_registry_extend_from_dir")
            .or(() -> LIB.find("_xberg_registry_extend_from_dir"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_extend_from_dir"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_extend_from_dir"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "xberg_registry_extend_from_dir" +
                " (tried: xberg_registry_extend_from_dir, _xberg_registry_extend_from_dir). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("xberg_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle XBERG_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("xberg_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_OUTPUT_TO_JSON = LIB.find("xberg_extraction_output_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_EXTRACTION_OUTPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_output_free")
            .or(() -> LIB.find("_xberg_extraction_output_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_output_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_output_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_RESULT_TO_JSON = LIB.find("xberg_chunking_result_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CHUNKING_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunking_result_free")
            .or(() -> LIB.find("_xberg_chunking_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_DIFF_TO_JSON = LIB.find("xberg_extraction_diff_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_EXTRACTION_DIFF_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_diff_free")
            .or(() -> LIB.find("_xberg_extraction_diff_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_diff_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_diff_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_DECISION_TO_JSON = LIB.find("xberg_chunking_decision_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CHUNKING_DECISION_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunking_decision_free")
            .or(() -> LIB.find("_xberg_chunking_decision_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_decision_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_decision_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIDENCE_TO_JSON = LIB.find("xberg_extraction_confidence_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_EXTRACTION_CONFIDENCE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_confidence_free")
            .or(() -> LIB.find("_xberg_extraction_confidence_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_confidence_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_confidence_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_CALL_MODE_TO_JSON = LIB.find("xberg_structured_call_mode_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_STRUCTURED_CALL_MODE_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_call_mode_free")
            .or(() -> LIB.find("_xberg_structured_call_mode_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_call_mode_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_call_mode_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNK_PLAN_TO_JSON = LIB.find("xberg_chunk_plan_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CHUNK_PLAN_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunk_plan_free")
            .or(() -> LIB.find("_xberg_chunk_plan_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunk_plan_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunk_plan_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RESOLVED_PRESET_TO_JSON = LIB.find("xberg_resolved_preset_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_RESOLVED_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_resolved_preset_free")
            .or(() -> LIB.find("_xberg_resolved_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_resolved_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_resolved_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDING_PRESET_TO_JSON = LIB.find("xberg_embedding_preset_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_EMBEDDING_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedding_preset_free")
            .or(() -> LIB.find("_xberg_embedding_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedding_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedding_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKER_PRESET_TO_JSON = LIB.find("xberg_reranker_preset_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_RERANKER_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_reranker_preset_free")
            .or(() -> LIB.find("_xberg_reranker_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranker_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranker_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_INPUT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extract_input_from_json")
            .or(() -> LIB.find("_xberg_extract_input_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_input_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_input_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACT_INPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extract_input_free")
            .or(() -> LIB.find("_xberg_extract_input_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extract_input_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extract_input_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_config_from_json")
            .or(() -> LIB.find("_xberg_extraction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_config_free")
            .or(() -> LIB.find("_xberg_extraction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_extraction_result_from_json")
            .or(() -> LIB.find("_xberg_extraction_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EXTRACTION_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_extraction_result_free")
            .or(() -> LIB.find("_xberg_extraction_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_extraction_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_extraction_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_config_from_json")
            .or(() -> LIB.find("_xberg_page_classification_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PAGE_CLASSIFICATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_page_classification_config_free")
            .or(() -> LIB.find("_xberg_page_classification_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_page_classification_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_page_classification_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_redaction_config_from_json")
            .or(() -> LIB.find("_xberg_redaction_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REDACTION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_redaction_config_free")
            .or(() -> LIB.find("_xberg_redaction_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_redaction_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_redaction_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_translation_config_from_json")
            .or(() -> LIB.find("_xberg_translation_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TRANSLATION_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_translation_config_free")
            .or(() -> LIB.find("_xberg_translation_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_translation_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_translation_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_chunking_config_from_json")
            .or(() -> LIB.find("_xberg_chunking_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CHUNKING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_chunking_config_free")
            .or(() -> LIB.find("_xberg_chunking_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_chunking_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_chunking_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_OPTIONS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_diff_options_from_json")
            .or(() -> LIB.find("_xberg_diff_options_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_options_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_options_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DIFF_OPTIONS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_diff_options_free")
            .or(() -> LIB.find("_xberg_diff_options_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_diff_options_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_diff_options_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGION_KIND_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_region_kind_from_json")
            .or(() -> LIB.find("_xberg_region_kind_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_region_kind_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_region_kind_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGION_KIND_FREE = LINKER.downcallHandle(
        LIB.find("xberg_region_kind_free")
            .or(() -> LIB.find("_xberg_region_kind_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_region_kind_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_region_kind_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_llm_config_from_json")
            .or(() -> LIB.find("_xberg_llm_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_LLM_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_config_free")
            .or(() -> LIB.find("_xberg_llm_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKER_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_reranker_config_from_json")
            .or(() -> LIB.find("_xberg_reranker_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranker_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranker_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_RERANKER_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_reranker_config_free")
            .or(() -> LIB.find("_xberg_reranker_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_reranker_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_reranker_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_keyword_config_from_json")
            .or(() -> LIB.find("_xberg_keyword_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_KEYWORD_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_keyword_config_free")
            .or(() -> LIB.find("_xberg_keyword_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_keyword_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_keyword_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_METADATA_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_document_metadata_from_json")
            .or(() -> LIB.find("_xberg_document_metadata_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_metadata_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_metadata_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_DOCUMENT_METADATA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_document_metadata_free")
            .or(() -> LIB.find("_xberg_document_metadata_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_document_metadata_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_document_metadata_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEURISTICS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_heuristics_config_from_json")
            .or(() -> LIB.find("_xberg_heuristics_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heuristics_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heuristics_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_HEURISTICS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_heuristics_config_free")
            .or(() -> LIB.find("_xberg_heuristics_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_heuristics_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_heuristics_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONFIDENCE_SIGNALS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_confidence_signals_from_json")
            .or(() -> LIB.find("_xberg_confidence_signals_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_confidence_signals_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_confidence_signals_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONFIDENCE_SIGNALS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_confidence_signals_free")
            .or(() -> LIB.find("_xberg_confidence_signals_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_confidence_signals_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_confidence_signals_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONFIDENCE_WEIGHTS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_confidence_weights_from_json")
            .or(() -> LIB.find("_xberg_confidence_weights_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_confidence_weights_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_confidence_weights_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_CONFIDENCE_WEIGHTS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_confidence_weights_free")
            .or(() -> LIB.find("_xberg_confidence_weights_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_confidence_weights_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_confidence_weights_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_THRESHOLDS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_thresholds_from_json")
            .or(() -> LIB.find("_xberg_multidoc_thresholds_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_thresholds_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_thresholds_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_THRESHOLDS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_thresholds_free")
            .or(() -> LIB.find("_xberg_multidoc_thresholds_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_thresholds_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_thresholds_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_INPUT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_input_from_json")
            .or(() -> LIB.find("_xberg_multidoc_input_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_input_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_input_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_MULTIDOC_INPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_multidoc_input_free")
            .or(() -> LIB.find("_xberg_multidoc_input_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_multidoc_input_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_multidoc_input_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_INPUT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_structured_input_from_json")
            .or(() -> LIB.find("_xberg_structured_input_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_input_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_input_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_INPUT_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_input_free")
            .or(() -> LIB.find("_xberg_structured_input_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_input_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_input_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_THRESHOLDS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_structured_thresholds_from_json")
            .or(() -> LIB.find("_xberg_structured_thresholds_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_thresholds_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_thresholds_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_STRUCTURED_THRESHOLDS_FREE = LINKER.downcallHandle(
        LIB.find("xberg_structured_thresholds_free")
            .or(() -> LIB.find("_xberg_structured_thresholds_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_structured_thresholds_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_structured_thresholds_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_preset_from_json")
            .or(() -> LIB.find("_xberg_preset_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_FREE = LINKER.downcallHandle(
        LIB.find("xberg_preset_free")
            .or(() -> LIB.find("_xberg_preset_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_preset_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_preset_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDING_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("xberg_embedding_config_from_json")
            .or(() -> LIB.find("_xberg_embedding_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedding_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedding_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_EMBEDDING_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("xberg_embedding_config_free")
            .or(() -> LIB.find("_xberg_embedding_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_embedding_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_embedding_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_PRESET_TO_JSON = LIB.find("xberg_preset_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_LLM_BACKEND_FREE = LINKER.downcallHandle(
        LIB.find("xberg_llm_backend_free")
            .or(() -> LIB.find("_xberg_llm_backend_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_llm_backend_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_llm_backend_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_TOKEN_COUNTER_FREE = LINKER.downcallHandle(
        LIB.find("xberg_token_counter_free")
            .or(() -> LIB.find("_xberg_token_counter_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_token_counter_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_token_counter_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_META_SCHEMA_FREE = LINKER.downcallHandle(
        LIB.find("xberg_meta_schema_free")
            .or(() -> LIB.find("_xberg_meta_schema_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_meta_schema_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_meta_schema_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTRY_FREE = LINKER.downcallHandle(
        LIB.find("xberg_registry_free")
            .or(() -> LIB.find("_xberg_registry_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("xberg_registry_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_xberg_registry_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle XBERG_REGISTER_OCR_BACKEND = LIB
        .find("xberg_register_ocr_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_OCR_BACKEND = LIB
        .find("xberg_unregister_ocr_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_OCR_BACKEND = LIB
        .find("xberg_clear_ocr_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_POST_PROCESSOR = LIB
        .find("xberg_register_post_processor")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_POST_PROCESSOR = LIB
        .find("xberg_unregister_post_processor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_POST_PROCESSOR = LIB
        .find("xberg_clear_post_processor")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_VALIDATOR = LIB
        .find("xberg_register_validator")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_VALIDATOR = LIB
        .find("xberg_unregister_validator")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_VALIDATOR = LIB
        .find("xberg_clear_validator")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_EMBEDDING_BACKEND = LIB
        .find("xberg_register_embedding_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_EMBEDDING_BACKEND = LIB
        .find("xberg_unregister_embedding_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_EMBEDDING_BACKEND = LIB
        .find("xberg_clear_embedding_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_RENDERER = LIB
        .find("xberg_register_renderer")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_RENDERER = LIB
        .find("xberg_unregister_renderer")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_RENDERER = LIB
        .find("xberg_clear_renderer")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_REGISTER_RERANKER_BACKEND = LIB
        .find("xberg_register_reranker_backend")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.JAVA_LONG,
            ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_UNREGISTER_RERANKER_BACKEND = LIB
        .find("xberg_unregister_reranker_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle XBERG_CLEAR_RERANKER_BACKEND = LIB
        .find("xberg_clear_reranker_backend")
        .map(s -> LINKER.downcallHandle(s,
            FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)))
        .orElse(null);


}
