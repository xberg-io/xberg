// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Async lifecycle status for an enrichment job.
 *
 * Intended for use with any polling or event-driven pipeline that needs
 * to track whether enrichment has completed, succeeded, or failed.
 *
 * # Serialisation
 *
 * Uses an internally-tagged {@code "status"} field with {@code snake_case} variants:
 *
 * {@code }{@code json}
 * { "status": "pending" }
 * { "status": "completed", "result": { ... } }
 * { "status": "failed", "error": "text too large" }
 *
 *
 *
 *
 * use xberg.enrichment.{EnrichStatus, EnrichResult};
 *
 * let s = EnrichStatus.Pending;
 * let json = serde_json.to_value(&amp;s);
 * assert_eq!(json["status"], "pending");
 *
 * let s = EnrichStatus.Completed { result: EnrichResult.default() };
 * let json = serde_json.to_value(&amp;s);
 * assert_eq!(json["status"], "completed");
 *
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "status", visible = false)
@JsonSubTypes({
    @JsonSubTypes.Type(value = EnrichStatus.Pending.class, name = "pending"),
    @JsonSubTypes.Type(value = EnrichStatus.Completed.class, name = "completed"),
    @JsonSubTypes.Type(value = EnrichStatus.Failed.class, name = "failed")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface EnrichStatus {

    /**
     * Job submitted; processing has not yet started or is in progress.
     */
    record Pending() implements EnrichStatus {
    }

    /**
     * Processing completed successfully.
     */
    record Completed(@JsonProperty("result") EnrichResult result) implements EnrichStatus { }

    /**
     * Processing failed.
     */
    record Failed(@JsonProperty("error") String error) implements EnrichStatus { }
}
