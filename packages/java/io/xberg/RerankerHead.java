// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:597bd1ea631d4ac6d358d121dc5791450b5d9aaaabe53fda1e43bef2af72e4ac
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Selects how a local ONNX reranker's raw output tensor is turned into a score.
 *
 * - RerankerHead.CrossEncoder — classic single-logit cross-encoder head:
 *   the model emits {@code [batch, 1]} (or {@code [batch]}) logits; the caller applies
 *   sigmoid to get a {@code [0, 1]} score. This is the original, unchanged path.
 * - RerankerHead.Qwen3Generative — Qwen3 generative-reranker head: the
 *   model emits {@code [batch, seq, vocab]} logits; the score is {@code P("yes")} read
 *   from the last token's logits over the "yes"/"no" vocabulary entries,
 *   via a softmax over those two logits. Already a {@code [0, 1]} probability —
 *   no sigmoid is applied.
 *
 * Since v5.0.0.
 */
@SuppressWarnings("PMD")
public enum RerankerHead {
    /**
     * Single-logit cross-encoder head (sigmoid applied by the caller).
     */
    CrossEncoder("cross_encoder"),
    /**
     * Qwen3 generative-reranker head (softmax over yes/no token logits).
     */
    Qwen3Generative("qwen3_generative");


    /** The string value. */
    private final String value;

    RerankerHead(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static RerankerHead fromValue(final String value) {
        for (RerankerHead e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown RerankerHead value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
