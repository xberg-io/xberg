// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Outcome of the structured-extraction call-mode heuristic.
 *
 * **Distinct from {@code crate.core.config.CallMode}** which has three variants
 * and governs extraction-engine behaviour.  This enum governs whether and how
 * an already-extracted document is sent to an LLM structured-extraction
 * pipeline.
 */
@SuppressWarnings("PMD")
public enum StructuredCallMode {
    /**
     * Document is unsupported or not worth invoking the pipeline.
     */
    Skip("skip"),
    /**
     *  extracted text only; no vision model call.
     */
    TextOnly("text_only"),
    /**
     *  page rasters only; no extracted text payload.
     */
    VisionOnly("vision_only"),
    /**
     * Fuse extracted text with page rasters in a single multimodal call.
     */
    TextPlusVision("text_plus_vision"),
    /**
     * Try text-only first; escalate to vision on low confidence score.
     */
    TextOnlyWithVisionFallback("text_only_with_vision_fallback");


    /** The string value. */
    private final String value;

    StructuredCallMode(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static StructuredCallMode fromValue(final String value) {
        for (StructuredCallMode e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown StructuredCallMode value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
