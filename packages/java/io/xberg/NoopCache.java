// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import java.util.Optional;

/**
 * In-core default: a cache that stores nothing and never hits.
 *
 * This reproduces today's behavior exactly — the default extraction path does
 * no engine-level caching, so get(CacheBackend.get) always returns {@code None}
 * and put(CacheBackend.put) is inert.
 */
@SuppressWarnings("PMD")
public class NoopCache implements AutoCloseable {
    private final MemorySegment handle;

    NoopCache(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    public Optional<byte[]> get(final String key) throws XbergRsException {
        java.util.Objects.requireNonNull(key, "key must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cKey = arena.allocateFrom(key);
            // Unsupported return shape for get
            throw new XbergRsException("get: unsupported return shape", (Throwable) null);
        } catch (XbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new XbergRsException("get: failed", e);
        }
    }
    public void put(final String key, final byte[] value, final Long ttl) throws XbergRsException {
        java.util.Objects.requireNonNull(key, "key must not be null");
        java.util.Objects.requireNonNull(value, "value must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cKey = arena.allocateFrom(key);
            // Unsupported parameter type for value
            throw new XbergRsException("put: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof XbergRsException ex) { throw ex; }
            throw new XbergRsException("put: failed", e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.XBERG_NOOP_CACHE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free NoopCache: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws XbergRsException {
        try {
            int code = (int)(long) NativeLib.XBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.XBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new XbergRsException(code, msg);
        } catch (XbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new XbergRsException("failed to read last error", e);
        }
    }    // CPD-ON
}
