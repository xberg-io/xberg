// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0912795d137244d47ca46093261df585cc70f719f15f01c0a00970e77e266da2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;

/**
 * Compiled meta-schema validator over {@code preset.schema.json}.
 */
@SuppressWarnings("PMD")
public class MetaSchema implements AutoCloseable {
    private final MemorySegment handle;

    MetaSchema(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    /**
     * Validate {@code raw} against the meta-schema and deserialize into a Preset,
     * stamping the fingerprint over the canonical file bytes.
     */
    public Preset parsePreset(final String path, final byte[] raw) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(path, "path must not be null");
        java.util.Objects.requireNonNull(raw, "raw must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cPath = arena.allocateFrom(path);
            // Unsupported parameter type for raw
            throw new KreuzbergRsException("parsePreset: unsupported parameter shape", (Throwable) null);
        } catch (Throwable e) {
            if (e instanceof KreuzbergRsException ex) { throw ex; }
            throw new KreuzbergRsException("parsePreset: failed", e);
        }
    }
    /**
     * Compile the given JSON text as a Draft 2020-12 meta-schema.
     */
    public static MetaSchema compile(final String metaSchemaJson) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(metaSchemaJson, "metaSchemaJson must not be null");
        try (Arena arena = Arena.ofShared()) {
            var cMetaSchemaJson = arena.allocateFrom(metaSchemaJson);
            var handle = (MemorySegment) NativeLib.KREUZBERG_META_SCHEMA_COMPILE.invoke(cMetaSchemaJson);
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new KreuzbergRsException("compile returned null", (Throwable) null);
            }
            return new MetaSchema(handle);
        }
        catch (Throwable e) {
            if (e instanceof KreuzbergRsException) throw (KreuzbergRsException) e;
            throw new RuntimeException("compile failed: " + e.getMessage(), e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_META_SCHEMA_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free MetaSchema: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws KreuzbergRsException {
        try {
            int code = (int) NativeLib.KREUZBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.KREUZBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new KreuzbergRsException(code, msg);
        } catch (KreuzbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new KreuzbergRsException("failed to read last error", e);
        }
    }    // CPD-ON
}
