// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d715a94173c0396b2dfcbe0a068a59d1534f3a61380d193393d8da2565aeeffc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg;

import java.lang.foreign.MemorySegment;
import java.lang.foreign.Arena;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.List;
import java.util.Optional;

/**
 * Sorted map of preset id → Preset.
 */
@SuppressWarnings("PMD")
public class Registry implements AutoCloseable {
    private final MemorySegment handle;

    Registry(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }
    /**
     * Look up a preset by its identifier.
     */
    public Optional<Preset> get(final String id) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(id, "id must not be null");
        try {
            Arena arena = Arena.ofShared();
            var cId = arena.allocateFrom(id);
            // CPD-OFF — FFI JSON-roundtrip body, structurally identical for every named-result method.
            MemorySegment resultPtr = (MemorySegment) NativeLib.KREUZBERG_REGISTRY_GET.invoke(this.handle, cId);
            if (resultPtr.equals(MemorySegment.NULL)) {
                checkLastFfiError();
                return java.util.Optional.empty();
            }
            try {
                MemorySegment jsonPtr = (MemorySegment) NativeLib.KREUZBERG_PRESET_TO_JSON.invoke(resultPtr);
                if (jsonPtr.equals(MemorySegment.NULL)) {
                    checkLastFfiError();
                    throw new KreuzbergRsException("get: failed to serialize response", (Throwable) null);
                }
                String json = jsonPtr.reinterpret(Long.MAX_VALUE).getString(0);
                NativeLib.KREUZBERG_FREE_STRING.invoke(jsonPtr);
                return java.util.Optional.of(STREAM_MAPPER.readValue(json, Preset.class));
            } finally {
                NativeLib.KREUZBERG_PRESET_FREE.invoke(resultPtr);
            }
            // CPD-ON
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("get: failed", e);
        }
    }
    /**
     * Materialize a PresetSummary list for the public registry endpoint.
     */
    public List<PresetSummary> summaries() throws KreuzbergRsException {
        try {
            // Unsupported return shape for summaries
            throw new KreuzbergRsException("summaries: unsupported return shape", (Throwable) null);
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("summaries: failed", e);
        }
    }
    /**
     * Number of presets currently loaded.
     */
    public long len() throws KreuzbergRsException {
        try {
            var result = (long) NativeLib.KREUZBERG_REGISTRY_LEN.invoke(this.handle);
            return result;
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("len: failed", e);
        }
    }
    /**
     * Whether the registry contains zero presets.
     */
    public boolean isEmpty() throws KreuzbergRsException {
        try {
            var result = (int) NativeLib.KREUZBERG_REGISTRY_IS_EMPTY.invoke(this.handle);
            return result != 0;
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("isEmpty: failed", e);
        }
    }
    /**
     * Read raw sample bytes for {@code &lt;preset_id&gt;} from
     * {@code library/&lt;id&gt;/samples/&lt;name&gt;}. Returns {@code None} when the file is absent.
     */
    public Optional<byte[]> sampleBytes(final String presetId, final String name) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(presetId, "presetId must not be null");
        java.util.Objects.requireNonNull(name, "name must not be null");
        try {
            Arena arena = Arena.ofShared();
            var cPresetId = arena.allocateFrom(presetId);
            var cName = arena.allocateFrom(name);
            // Unsupported return shape for sampleBytes
            throw new KreuzbergRsException("sampleBytes: unsupported return shape", (Throwable) null);
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("sampleBytes: failed", e);
        }
    }
    /**
     * Load additional preset files from a runtime directory and insert them
     * into this registry.
     *
     * Reads every {@code *.json} file directly under {@code dir} (non-recursive),
     * validates each against the meta-schema, and inserts it. Files that fail
     * validation are rejected — the error is returned immediately and the
     * registry is left in a partially-updated state. Existing entries with the
     * same id are overwritten.
     *
     * Returns the number of presets successfully loaded from {@code dir}.
     *
     * # Use case
     *
     * This is the injection point for downstream catalogs: kreuzberg-cloud
     * calls this once at startup to add its 20+ curated presets on top of the
     * single embedded OSS preset.
     */
    public long extendFromDir(final java.nio.file.Path dir) throws KreuzbergRsException {
        java.util.Objects.requireNonNull(dir, "dir must not be null");
        try {
            Arena arena = Arena.ofShared();
            var cDir = arena.allocateFrom(dir.toString());
            var result = (long) NativeLib.KREUZBERG_REGISTRY_EXTEND_FROM_DIR.invoke(this.handle, cDir);
            return result;
        } catch (KreuzbergRsException ex) {
            throw ex;
        } catch (Throwable e) {
            throw new KreuzbergRsException("extendFromDir: failed", e);
        }
    }
    /**
     * Build the registry from preset files embedded at compile time under
     * {@code src/presets/library/}. Validates every file against the meta-schema.
     */
    public static Registry loadEmbedded() throws KreuzbergRsException {
        try {
            var handle = (MemorySegment) NativeLib.KREUZBERG_REGISTRY_LOAD_EMBEDDED.invoke();
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new KreuzbergRsException("loadEmbedded returned null", (Throwable) null);
            }
            return new Registry(handle);
        }
        catch (Throwable e) {
            if (e instanceof KreuzbergRsException) throw (KreuzbergRsException) e;
            throw new RuntimeException("loadEmbedded failed: " + e.getMessage(), e);
        }
    }
    /**
     * Return the global registry, loading it on first access.
     */
    public static Registry global() throws KreuzbergRsException {
        try {
            var handle = (MemorySegment) NativeLib.KREUZBERG_REGISTRY_GLOBAL.invoke();
            if (handle == null || handle.equals(MemorySegment.NULL)) {
                throw new KreuzbergRsException("global returned null", (Throwable) null);
            }
            return new Registry(handle);
        }
        catch (Throwable e) {
            if (e instanceof KreuzbergRsException) throw (KreuzbergRsException) e;
            throw new RuntimeException("global failed: " + e.getMessage(), e);
        }
    }
    @Override
    @SuppressWarnings("PMD.AvoidCatchingGenericException")
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.KREUZBERG_REGISTRY_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free Registry: " + e.getMessage(), e);
            }
        }
    }

    // CPD-OFF — generated FFI boilerplate, identical across all opaque types by design.
    private static void checkLastFfiError() throws KreuzbergRsException {
        try {
            int code = (int) NativeLib.KREUZBERG_LAST_ERROR_CODE.invoke();
            if (code == 0) { return; }
            MemorySegment ctxPtr = (MemorySegment) NativeLib.KREUZBERG_LAST_ERROR_CONTEXT.invoke();
            String msg = ctxPtr.equals(MemorySegment.NULL) ? "unknown" : ctxPtr.reinterpret(Long.MAX_VALUE).getString(0);
            throw new KreuzbergRsException(code, msg);
        } catch (KreuzbergRsException e) {
            throw e;
        } catch (Throwable e) {
            throw new KreuzbergRsException("failed to read last error", e);
        }
    }
    private static final ObjectMapper STREAM_MAPPER = createStreamMapper();

    private static ObjectMapper createStreamMapper() {
        return new ObjectMapper()
            .registerModule(new com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
            .setSerializationInclusion(com.fasterxml.jackson.annotation.JsonInclude.Include.NON_NULL)
            .configure(com.fasterxml.jackson.databind.MapperFeature.ACCEPT_CASE_INSENSITIVE_ENUMS, true);
    }    // CPD-ON
}
