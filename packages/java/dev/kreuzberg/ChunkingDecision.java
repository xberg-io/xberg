// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * The chunking decision made by the analyzer.
 */
@SuppressWarnings("PMD")
public enum ChunkingDecision {
    /**
     * Process without chunking (small file, text layer detected, etc.)
     */
    NoChunking("nochunking"),
    /**
     * Chunk according to plan.
     */
    Chunk("chunk"),
    /**
     * Use user-provided chunk overrides.
     */
    UseOverrides("useoverrides");


    /** The string value. */
    private final String value;

    ChunkingDecision(final String value) {
        this.value = value;
    }

    /** Returns the string value. */
    @JsonValue
    public String getValue() {
        return value;
    }

    /** Creates an instance from a string value. */
    @JsonCreator
    public static ChunkingDecision fromValue(final String value) {
        for (ChunkingDecision e : values()) {
            if (e.value.equalsIgnoreCase(value)) {
                return e;
            }
        }
        throw new IllegalArgumentException("Unknown ChunkingDecision value: " + value);
    }

    /** Returns the wire-format string value (matches JSON serialization). */
    @Override
    public String toString() {
        return value;
    }
}
