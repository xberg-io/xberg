---
priority: medium
aliases: [b]
usage: "/build"
description: "Build the project"
---
# Build

Build the project using the standard task runner.

1. Run `task build`
2. Report any build errors with context
3. Suggest fixes for any compilation failures
