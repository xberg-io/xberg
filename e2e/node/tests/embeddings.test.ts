// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:359b30ccda77f2596be4c2616d211bdd9a1656eb6c251da2069579602e2936ad
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, , embedTexts, getEmbeddingPreset, listEmbeddingPresets, type JsExtractionConfig } from 'kreuzberg';

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

describe('embeddings', () => {
  it('embed_texts_async_empty_input: embed_texts_async: empty text list', () => {
    const result = ({ texts: [] });
    expect(result.length).toBe(0);
  });

  it('embed_texts_async_happy: embed_texts_async: basic async embedding', () => {
    const result = ({ texts: ["First", "Second"] });
    expect(result.length).toBeGreaterThanOrEqual(2);
  });

  it('embed_texts_async_preset_switch: embed_texts_async: preset override', () => {
    ({ config: { embedding_model: "default" }, texts: ["Text"] });
  });

  it('embed_texts_different_preset: embed_texts: multilingual preset', () => {
    const result = embedTexts(["Hello world", "Test"] as JsExtractionConfig, { embeddingModel: "multilingual" } as JsExtractionConfig);
    expect(result.length).toBeGreaterThanOrEqual(2);
  });

  it('get_embedding_preset_known: get_embedding_preset: known preset', () => {
    getEmbeddingPreset("default");
  });

  it('get_embedding_preset_nominal: get_embedding_preset: nominal case', () => {
    getEmbeddingPreset("default");
  });

  it('get_embedding_preset_unknown: get_embedding_preset: unknown preset fails', () => {
    const result = getEmbeddingPreset("nonexistent-xyz");
    expect((result ?? "").length).toBe(0);
  });

  it('list_embedding_presets_sanity: list_embedding_presets: returns at least one', () => {
    const result = listEmbeddingPresets({  });
    expect(result.length).toBeGreaterThan(0);
  });
});
