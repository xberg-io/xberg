// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7879084e15e14eab7918bbacff0af2982c3d69e3680fb0df55f80725935a99cb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import { extractFile, extractFileSync } from "kreuzberg";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

describe("code", () => {
  it("code_shebang_detection: Test language detection from shebang line via bytes input", () => {
    const result = extractFileSync("code/script.sh", "text/x-source-code", undefined);
    expect(result.mimeType.trim()).toBe("text/x-source-code");
    expect(result.content.length).toBeGreaterThanOrEqual(10);
    expect(result.content).toContain("build");
    expect(result.content).toContain("clean");
  });
});
