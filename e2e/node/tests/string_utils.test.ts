// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:88946bee139c274ddadf0c1b3e84697ea829a632216439c4507292e2d5f49970
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, camel_to_snake, sanitize_filename, sanitize_path, snake_to_camel } from 'kreuzberg';

describe('string_utils', () => {
  it('camel_to_snake_basic: Convert camelCase to snake_case', () => {
    const result = camel_to_snake("myVariableName");
    expect(result.result.trim()).toBe("my_variable_name");
  });

  it('camel_to_snake_single: Convert single word (no camelCase) remains unchanged', () => {
    const result = camel_to_snake("hello");
    expect(result.result.trim()).toBe("hello");
  });

  it('sanitize_filename_basic: Sanitize path traversal attempts in filename', () => {
    const result = sanitize_filename("../../etc/passwd");
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it('sanitize_filename_special: Sanitize special characters in filename', () => {
    const result = sanitize_filename("file<>name|test.pdf");
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it('sanitize_path_basic: Sanitize path traversal in file path', () => {
    const result = sanitize_path("../../../secret/file.txt");
    expect(result.result.length).toBeGreaterThanOrEqual(1);
  });

  it('sanitize_path_spaces: Sanitize path with spaces', () => {
    sanitize_path("my folder/file.txt");
  });

  it('sanitize_path_traversal: Sanitize path traversal', () => {
    sanitize_path("../../../etc/passwd");
  });

  it('snake_to_camel_basic: Convert snake_case to camelCase', () => {
    const result = snake_to_camel("my_variable_name");
    expect(result.result.trim()).toBe("myVariableName");
  });

  it('snake_to_camel_single: Convert single word remains unchanged', () => {
    const result = snake_to_camel("hello");
    expect(result.result.trim()).toBe("hello");
  });
});
