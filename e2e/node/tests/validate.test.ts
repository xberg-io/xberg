// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7a734bd3bffad0210b76098ddcbae14039490f925860755d76d76937deffe7ec
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import { describe, expect, it } from 'vitest';
import { extractFile, , extractFileSync, type JsExtractionConfig } from 'kreuzberg';

function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

describe('validate', () => {
  it('validate_binarization_invalid: Invalid binarization', async () => {
    await expect(async () => {
      await ({ method: "nonexistent" });
    }).rejects.toThrow();
  });

  it('validate_binarization_valid: Valid binarization', () => {
    ({ method: "otsu" });
  });

  it('validate_chunking_overlap_exceeds: Reject chunking config where overlap exceeds max_characters', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", undefined, { chunking: { maxCharacters: 10, maxOverlap: 100 } } as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_chunking_params_invalid: Chunking overlap >= max', async () => {
    await expect(async () => {
      await ({ max_characters: 50, overlap: 60 });
    }).rejects.toThrow();
  });

  it('validate_chunking_params_valid: Valid chunking params', () => {
    ({ max_characters: 500, overlap: 50 });
  });

  it('validate_chunking_valid: Accept valid chunking config', () => {
    const result = extractFileSync("pdf/fake_memo.pdf", undefined, { chunking: { maxCharacters: 1000, maxOverlap: 200 } } as JsExtractionConfig);
    expect(result.content.length).toBeGreaterThanOrEqual(10);
  });

  it('validate_confidence_boundary: Confidence at 1.0', () => {
    ({ confidence: 1.0 });
  });

  it('validate_confidence_negative: Negative confidence', async () => {
    await expect(async () => {
      await ({ confidence: -0.5 });
    }).rejects.toThrow();
  });

  it('validate_confidence_over: Confidence > 1', async () => {
    await expect(async () => {
      await ({ confidence: 1.5 });
    }).rejects.toThrow();
  });

  it('validate_confidence_valid: Accept valid confidence threshold', () => {
    extractFileSync("pdf/fake_memo.pdf", undefined, { ocr: { confidenceThreshold: 0.8 } } as JsExtractionConfig);
  });

  it('validate_confidence_zero: Confidence at 0.0', () => {
    ({ confidence: 0.0 });
  });

  it('validate_dpi_negative: Reject negative DPI in OCR config', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", undefined, { ocr: { dpi: -1 } } as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_dpi_valid: Accept valid DPI in OCR config', () => {
    extractFileSync("pdf/fake_memo.pdf", undefined, { ocr: { dpi: 300 } } as JsExtractionConfig);
  });

  it('validate_host_empty: Empty host rejected', async () => {
    await expect(async () => {
      await ({ host: "" });
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_invalid: Invalid OCR backend', async () => {
    await expect(async () => {
      await ({ backend: "nonexistent" });
    }).rejects.toThrow();
  });

  it('validate_ocr_backend_valid: Valid OCR backend', () => {
    ({ backend: "tesseract" });
  });

  it('validate_output_format_invalid: Reject invalid output format', async () => {
    await expect(async () => {
      await extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "nonexistent_format" } as JsExtractionConfig);
    }).rejects.toThrow();
  });

  it('validate_output_format_valid: Accept valid output format', () => {
    extractFileSync("pdf/fake_memo.pdf", undefined, { outputFormat: "markdown" } as JsExtractionConfig);
  });

  it('validate_port_max: Port 65535 accepted', () => {
    ({ port: 65535 });
  });

  it('validate_port_over: Port over 65535', async () => {
    await expect(async () => {
      await ({ port: 70000 });
    }).rejects.toThrow();
  });

  it('validate_port_zero: Port 0 rejected', async () => {
    await expect(async () => {
      await ({ port: 0 });
    }).rejects.toThrow();
  });

  it('validate_reduction_level_invalid: Invalid reduction level', async () => {
    await expect(async () => {
      await ({ level: "nonexistent" });
    }).rejects.toThrow();
  });

  it('validate_reduction_level_valid: Valid reduction level', () => {
    ({ level: "moderate" });
  });
});
