// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang typescript

// Tests for code fixtures.

import { existsSync, readFileSync } from "node:fs";
import { describe, it } from "vitest";
import { assertions, buildConfig, resolveDocument, shouldSkipFixture } from "./helpers.js";
import { detectMimeTypeFromPath, extractBytesSync, extractFileSync } from "@kreuzberg/node";
import type { ExtractionResult } from "@kreuzberg/node";

const TEST_TIMEOUT_MS = 60_000;

describe("code fixtures", () => {
	it(
		"code_javascript_basic",
		() => {
			const documentPath = resolveDocument("code/app.js");
			if (!existsSync(documentPath)) {
				console.warn("Skipping code_javascript_basic: missing document at", documentPath);
				return;
			}
			const config = buildConfig(undefined);
			let result: ExtractionResult | null = null;
			try {
				result = extractFileSync(documentPath, null, config);
			} catch (error) {
				if (shouldSkipFixture(error, "code_javascript_basic", ["tree-sitter"], undefined)) {
					return;
				}
				throw error;
			}
			if (result === null) {
				return;
			}
			assertions.assertExpectedMime(result, ["text/x-source-code"]);
			assertions.assertMinContentLength(result, 10);
			assertions.assertContentContainsAll(result, ["class Application", "export function", "import"]);
		},
		TEST_TIMEOUT_MS,
	);

	it(
		"code_python_basic",
		() => {
			const documentPath = resolveDocument("code/hello.py");
			if (!existsSync(documentPath)) {
				console.warn("Skipping code_python_basic: missing document at", documentPath);
				return;
			}
			const config = buildConfig(undefined);
			let result: ExtractionResult | null = null;
			try {
				result = extractFileSync(documentPath, null, config);
			} catch (error) {
				if (shouldSkipFixture(error, "code_python_basic", ["tree-sitter"], undefined)) {
					return;
				}
				throw error;
			}
			if (result === null) {
				return;
			}
			assertions.assertExpectedMime(result, ["text/x-source-code"]);
			assertions.assertMinContentLength(result, 10);
			assertions.assertContentContainsAll(result, ["def greet", "class Greeter", "import os"]);
		},
		TEST_TIMEOUT_MS,
	);

	it(
		"code_rust_basic",
		() => {
			const documentPath = resolveDocument("code/main.rs");
			if (!existsSync(documentPath)) {
				console.warn("Skipping code_rust_basic: missing document at", documentPath);
				return;
			}
			const config = buildConfig(undefined);
			let result: ExtractionResult | null = null;
			try {
				result = extractFileSync(documentPath, null, config);
			} catch (error) {
				if (shouldSkipFixture(error, "code_rust_basic", ["tree-sitter"], undefined)) {
					return;
				}
				throw error;
			}
			if (result === null) {
				return;
			}
			assertions.assertExpectedMime(result, ["text/x-source-code"]);
			assertions.assertMinContentLength(result, 10);
			assertions.assertContentContainsAll(result, ["struct Store", "fn main", "HashMap"]);
		},
		TEST_TIMEOUT_MS,
	);

	it(
		"code_shebang_detection",
		() => {
			const documentPath = resolveDocument("code/script.sh");
			if (!existsSync(documentPath)) {
				console.warn("Skipping code_shebang_detection: missing document at", documentPath);
				return;
			}
			const config = buildConfig(undefined);
			let result: ExtractionResult | null = null;
			try {
				const fileBytes = readFileSync(documentPath);
				const mimeType = detectMimeTypeFromPath(documentPath);
				result = extractBytesSync(fileBytes, mimeType, config);
			} catch (error) {
				if (shouldSkipFixture(error, "code_shebang_detection", ["tree-sitter"], undefined)) {
					return;
				}
				throw error;
			}
			if (result === null) {
				return;
			}
			assertions.assertExpectedMime(result, ["text/x-source-code"]);
			assertions.assertMinContentLength(result, 10);
			assertions.assertContentContainsAll(result, ["build", "clean"]);
		},
		TEST_TIMEOUT_MS,
	);

	it(
		"code_tree_sitter_config",
		() => {
			const documentPath = resolveDocument("code/hello.py");
			if (!existsSync(documentPath)) {
				console.warn("Skipping code_tree_sitter_config: missing document at", documentPath);
				return;
			}
			const config = buildConfig({
				tree_sitter: { process: { structure: true, imports: true, exports: true, comments: true, docstrings: true } },
			});
			let result: ExtractionResult | null = null;
			try {
				result = extractFileSync(documentPath, null, config);
			} catch (error) {
				if (shouldSkipFixture(error, "code_tree_sitter_config", ["tree-sitter"], undefined)) {
					return;
				}
				throw error;
			}
			if (result === null) {
				return;
			}
			assertions.assertExpectedMime(result, ["text/x-source-code"]);
			assertions.assertMinContentLength(result, 10);
			assertions.assertContentContainsAll(result, ["def greet", "class Greeter"]);
		},
		TEST_TIMEOUT_MS,
	);
});
