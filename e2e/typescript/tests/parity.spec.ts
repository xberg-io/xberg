// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang typescript

import { extractFileSync } from "@kreuzberg/node";
import { describe, expect, it } from "vitest";
import { resolveDocument } from "./helpers";

describe("ExtractionResult field parity", () => {
	it("has all required fields", async () => {
		const result = await extractFileSync(resolveDocument("pdf/fake_memo.pdf"));
		const requiredFields = ["content", "metadata", "mimeType", "tables"];
		for (const field of requiredFields) {
			expect(result).toHaveProperty(field);
		}
	});

	it("has all known fields", async () => {
		const result = await extractFileSync(resolveDocument("pdf/fake_memo.pdf"));
		const allFields = [
			"annotations",
			"children",
			"chunks",
			"content",
			"detectedLanguages",
			"djotContent",
			"document",
			"elements",
			"extractedKeywords",
			"images",
			"metadata",
			"mimeType",
			"ocrElements",
			"pages",
			"processingWarnings",
			"qualityScore",
			"tables",
			"uris",
		];
		for (const field of allFields) {
			expect(result).toHaveProperty(field);
		}
	});
});

describe("ExtractionConfig field parity", () => {
	it("accepts all expected fields", () => {
		const allFields = [
			"acceleration",
			"cacheNamespace",
			"cacheTtlSecs",
			"chunking",
			"concurrency",
			"contentFilter",
			"disableOcr",
			"email",
			"enableQualityProcessing",
			"extractionTimeoutSecs",
			"forceOcr",
			"forceOcrPages",
			"htmlOptions",
			"htmlOutput",
			"images",
			"includeDocumentStructure",
			"keywords",
			"languageDetection",
			"layout",
			"maxArchiveDepth",
			"maxConcurrentExtractions",
			"ocr",
			"outputFormat",
			"pages",
			"pdfOptions",
			"postprocessor",
			"resultFormat",
			"securityLimits",
			"tokenReduction",
			"treeSitter",
			"useCache",
		];
		expect(allFields.length).toBeGreaterThan(0);
	});
});

describe("ArchiveEntry field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["mimeType", "path", "result"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("BoundingBox field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["x0", "x1", "y0", "y1"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("Keyword field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["algorithm", "positions", "score", "text"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("PdfAnnotation field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["annotationType", "boundingBox", "content", "pageNumber"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("ProcessingWarning field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["message", "source"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("Table field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["boundingBox", "cells", "markdown", "pageNumber"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});

describe("Uri field parity", () => {
	it("has expected fields in manifest", () => {
		const expectedFields = ["kind", "label", "page", "url"];
		expect(expectedFields.length).toBeGreaterThan(0);
	});
});
