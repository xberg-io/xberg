// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fb9e4a24f6dc2e49e1fcb317d5f1ddfe86ea8de1dff93b8e55e40928e74b4d4f
// To regenerate: alef e2e generate
// To verify freshness: alef verify

// E2e tests for category: format_specific
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	xberg "github.com/xberg-io/xberg/packages/go"
)

func Test_FormatDocxEquations(t *testing.T) {
	// DOCX equations extract to LaTeX math in markdown output
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_DOCX_EQUATIONS")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_docx_equations"
	}
	inputJSON := strings.ReplaceAll(`{"filename":"equations.docx","kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","uri":"$mock_url/docx/equations.docx"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	var config xberg.ExtractionConfig
	if err := json.Unmarshal([]byte(`{"output_format":"markdown"}`), &config); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, config)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Results) == 0 {
		t.Fatalf("expected non-empty %s", `result.Results`)
	}
	assert.GreaterOrEqual(t, len(result.Results[0].Content), 20, "expected length >= 20")
	if !strings.Contains(string(result.Results[0].Content), `$`) {
		t.Errorf("expected to contain %s, got %v", `$`, result.Results[0].Content)
	}
}

func Test_FormatDocxStandalone(t *testing.T) {
	// Standalone DOCX extraction using extract
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_DOCX_STANDALONE")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_docx_standalone"
	}
	inputJSON := strings.ReplaceAll(`{"filename":"fake.docx","kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","uri":"$mock_url/docx/fake.docx"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Results[0].Content), 20, "expected length >= 20")
}

func Test_FormatHwpxStandalone(t *testing.T) {
	// Standalone HWPX extraction using extract
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_HWPX_STANDALONE")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_hwpx_standalone"
	}
	inputJSON := strings.ReplaceAll(`{"filename":"simple.hwpx","kind":"uri","mime_type":"application/haansofthwpx","uri":"$mock_url/hwpx/simple.hwpx"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Results[0].Content), 20, "expected length >= 20")
	if !strings.Contains(string(result.Results[0].Content), `Hello from HWPX`) {
		t.Errorf("expected to contain %s, got %v", `Hello from HWPX`, result.Results[0].Content)
	}
}

func Test_FormatPdfText(t *testing.T) {
	// Standalone PDF text extraction using extract
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_PDF_TEXT")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_pdf_text"
	}
	inputJSON := strings.ReplaceAll(`{"filename":"fake_memo.pdf","kind":"uri","mime_type":"application/pdf","uri":"$mock_url/pdf/fake_memo.pdf"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.GreaterOrEqual(t, len(result.Results[0].Content), 50, "expected length >= 50")
	{
		found := false
		if strings.Contains(string(result.Results[0].Content), `Mallori`) {
			found = true
		}
		if strings.Contains(string(result.Results[0].Content), `May`) {
			found = true
		}
		if !found {
			t.Errorf("expected to contain at least one of the specified values")
		}
	}
}

func Test_FormatPptx(t *testing.T) {
	// PPTX presentation extraction using extract
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_PPTX")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_pptx"
	}
	inputJSON := strings.ReplaceAll(`{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.presentationml.presentation","uri":"$mock_url/pptx/simple.pptx"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	_, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_FormatXlsx(t *testing.T) {
	// XLSX spreadsheet extraction using extract
	inputMockBaseURL := os.Getenv("MOCK_SERVER_FORMAT_XLSX")
	if inputMockBaseURL == "" {
		inputMockBaseURL = os.Getenv("MOCK_SERVER_URL") + "/fixtures/format_xlsx"
	}
	inputJSON := strings.ReplaceAll(`{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","uri":"$mock_url/xlsx/stanley_cups.xlsx"}`, "$mock_url", inputMockBaseURL)
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(inputJSON), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	_, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}
