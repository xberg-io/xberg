// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b0f3e348cc193e0cfa8fdcc36fd3ebb5e08e4360d10b525744b736383abddeae
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: format_specific
package e2e_test

import (
	"encoding/json"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	xberg "github.com/xberg-io/xberg"
)

func Test_FormatDocxStandalone(t *testing.T) {
	// Standalone DOCX extraction using extract
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(`{"filename":"fake.docx","kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","uri":"docx/fake.docx"}`), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Results) > 0 {
		assert.GreaterOrEqual(t, len(result.Results[0].Content), 20, "expected length >= 20")
	}
}

func Test_FormatHwpxStandalone(t *testing.T) {
	// Standalone HWPX extraction using extract
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(`{"filename":"simple.hwpx","kind":"uri","mime_type":"application/haansofthwpx","uri":"hwpx/simple.hwpx"}`), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Results) > 0 {
		assert.GreaterOrEqual(t, len(result.Results[0].Content), 20, "expected length >= 20")
	}
	if len(result.Results) > 0 {
		if !strings.Contains(string(result.Results[0].Content), `Hello from HWPX`) {
			t.Errorf("expected to contain %s, got %v", `Hello from HWPX`, result.Results[0].Content)
		}
	}
}

func Test_FormatPdfText(t *testing.T) {
	// Standalone PDF text extraction using extract
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(`{"filename":"fake_memo.pdf","kind":"uri","mime_type":"application/pdf","uri":"pdf/fake_memo.pdf"}`), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	result, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Results) > 0 {
		assert.GreaterOrEqual(t, len(result.Results[0].Content), 50, "expected length >= 50")
	}
	if len(result.Results) > 0 {
		{
			found := false
			if strings.Contains(string(result.Results[0].Content), `Mallori`) { found = true }
			if strings.Contains(string(result.Results[0].Content), `May`) { found = true }
			if !found {
				t.Errorf("expected to contain at least one of the specified values")
			}
		}
	}
}

func Test_FormatPptx(t *testing.T) {
	// PPTX presentation extraction using extract
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(`{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.presentationml.presentation","uri":"pptx/simple.pptx"}`), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	_, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_FormatXlsx(t *testing.T) {
	// XLSX spreadsheet extraction using extract
	var input xberg.ExtractInput
	if err := json.Unmarshal([]byte(`{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","uri":"xlsx/stanley_cups.xlsx"}`), &input); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	_, err := xberg.Extract(input, xberg.ExtractionConfig{})
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}
