# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code

"""E2e tests for category: rerank."""

import pytest  # noqa: F401
from xberg import rerank_async, RerankerConfig, RerankerModelType, ExtractionConfig


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = " ".join(str(value) for value in raw_items) if isinstance(raw_items, list) else ""
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio

async def test_rerank_basic() -> None:
    """rerank: plugin model with no backend registered returns an error."""
    with pytest.raises(Exception):  # noqa: B017
        query = "What is machine learning?"
        documents = ["Machine learning is a subset of artificial intelligence focused on learning from data.", "Cats are independent animals that prefer to play alone.", "Deep learning uses neural networks with multiple layers."]
        config = RerankerConfig(model={"name": "rerank_basic", "type": "plugin"})
        await rerank_async(query, documents, config)


@pytest.mark.asyncio

async def test_rerank_empty_documents() -> None:
    """rerank: empty documents list returns empty result."""
    query = "test query"
    documents = []
    config = RerankerConfig(model={"name": "rerank_empty_documents", "type": "plugin"})

    _ = await rerank_async(query, documents, config)


@pytest.mark.asyncio

async def test_rerank_top_k() -> None:
    """rerank: plugin model with no backend registered returns an error (top_k variant)."""
    with pytest.raises(Exception):  # noqa: B017
        query = "machine learning"
        documents = ["Machine learning is a subset of artificial intelligence focused on learning from data.", "Deep learning uses neural networks with multiple layers.", "Classical statistics predates modern machine learning.", "Photosynthesis is the process by which plants convert sunlight to energy."]
        config = RerankerConfig(model={"name": "rerank_top_k", "type": "plugin"}, top_k=2)
        await rerank_async(query, documents, config)
