// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4cdcb4c98b6720c5a4a78eefaf824854cd73d837e746bfc7e9c61fb7fcdfe1bb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: rendering.
final class RenderingTests: XCTestCase {
    func testConvertHtmlBasic() throws {
        // Convert basic HTML to Markdown format
        let result = try convertHtmlToMarkdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
        XCTAssertTrue(result.content.contains("Title"), "expected to contain: \("Title")")
    }

    func testConvertHtmlComplex() throws {
        // Convert complex HTML to markdown
        let result = try convertHtmlToMarkdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testConvertHtmlEmpty() throws {
        // Convert empty HTML to markdown
        let result = try convertHtmlToMarkdown("")
    }

    func testConvertHtmlTable() throws {
        // Convert HTML table to Markdown table format
        let result = try convertHtmlToMarkdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>")
        XCTAssertTrue(result.content.contains("|"), "expected to contain: \("|")")
    }

    func testDjotToHtmlBasic() throws {
        // Convert Djot format to HTML
        let result = try djotToHtml("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testDjotToHtmlComplex() throws {
        // Complex djot to HTML
        let result = try djotToHtml("# Heading\n\nParagraph.\n\n- Item one\n- Item two")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testDjotToHtmlEmpty() throws {
        // Convert empty djot to HTML
        let result = try djotToHtml("")
    }

    func testRenderDjotBasic() throws {
        // Render HTML document to Djot format
        let result = try renderDjot("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderDjotFormatted() throws {
        // Render HTML to djot
        let result = try renderDjot("<h2>Section</h2><p>Text with <code>code</code>.</p>")
    }

    func testRenderHtmlBasic() throws {
        // Render a simple HTML document to formatted output
        let result = try renderHtml("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderHtmlStyled() throws {
        // Render HTML preserving structure
        let result = try renderHtml("<h1>Heading</h1><p><strong>bold</strong></p>")
        XCTAssertTrue(result.content.contains("Heading"), "expected to contain: \("Heading")")
    }

    func testRenderJsonBasic() throws {
        // Render HTML document to JSON format
        let result = try renderJson("<h1>Structured Data</h1><p>Content for JSON rendering.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderJsonComplex() throws {
        // Render HTML to JSON
        let result = try renderJson("<h1>Title</h1><p>Text.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderMarkdownBasic() throws {
        // Render HTML document to Markdown format
        let result = try renderMarkdown("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderMarkdownComplex() throws {
        // Render complex HTML to markdown
        let result = try renderMarkdown("<h1>Title</h1><ul><li>A</li><li>B</li></ul>")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testRenderPlainBasic() throws {
        // Render HTML document to plain text format
        let result = try renderPlain("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testRenderPlainEmpty() throws {
        // Render empty HTML
        let result = try renderPlain("")
    }

}
