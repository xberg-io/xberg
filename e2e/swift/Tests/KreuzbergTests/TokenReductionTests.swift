// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:18c5e2ed104a981bc1ca875428c6abf2a6842745630fa30f849e1180f61a83a4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: token_reduction.
final class TokenReductionTests: XCTestCase {
    func testTokenReductionAggressive() throws {
        // Tests aggressive token reduction mode significantly reduces content
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"aggressive\"}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
        XCTAssertLessThanOrEqual(result.content.count, 150)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testTokenReductionBasic() throws {
        // Tests basic token reduction on PDF document
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"moderate\"}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
        XCTAssertLessThanOrEqual(result.content.count, 200)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testTokenReductionLight() throws {
        // Tests light token reduction mode preserves most content
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"light\"}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testTokenReductionReduceAggressive() throws {
        // Reduce tokens with aggressive compression level
        let result = try reduceTokens("The quick brown fox jumps over the lazy dog.", "aggressive")
        XCTAssertGreaterThanOrEqual(result.result.count, 1)
    }

    func testTokenReductionReduceBasic() throws {
        // Reduce tokens with light compression level
        let result = try reduceTokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", "light")
        XCTAssertTrue(result.result.contains("quick"), "expected to contain: \("quick")")
    }

    func testTokenReductionWithChunking() throws {
        // Tests token reduction combined with chunking
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50},\"token_reduction\":{\"mode\":\"moderate\"}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
        XCTAssertLessThanOrEqual(result.content.count, 200)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.chunks.count, 1)
        XCTAssertTrue(result.chunks_have_content)
    }

}
