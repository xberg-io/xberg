// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1b2edd7298294dbec4070628beea8d3097694458c972dc6d3694fe6d4207eefb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: chunking.
final class ChunkingTests: XCTestCase {
    func testChunkBatchBasic() throws {
        // Batch chunking of multiple texts in a single call
        let result = try chunkTextsBatch(["Hello world. This is text one.", "Goodbye world. This is text two."], "{\"max_characters\":100,\"max_overlap\":10}")
    }

    func testChunkTextBasic() throws {
        // Basic text chunking with small chunk size and overlap
        let result = try chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":10}")
    }

    func testChunkTextLarge() throws {
        // Text chunking with large chunk size to test handling of long content
        let result = try chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":1000,\"max_overlap\":10}")
    }

    func testChunkTextNoOverlap() throws {
        // Text chunking with no overlap between chunks
        let result = try chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":0}")
    }

    func testChunkTextOverlapExceeds() throws {
        // chunk_text overlap >= max
        XCTAssertThrowsError(try chunkText("Some text.", "{\"max_characters\":50,\"overlap\":60}"))
    }

    func testChunkTextYaml() throws {
        // Chunk with YAML chunker
        let result = try chunkText("key1: value1\nkey2: value2\n---\nkey3: value3", "{\"chunker_type\":\"yaml\",\"max_characters\":50}")
    }

    func testChunkTextZeroMax() throws {
        // chunk_text max_characters zero
        XCTAssertThrowsError(try chunkText("Some text.", "{\"max_characters\":0}"))
    }

    func testChunkTextsBatchMultiple() throws {
        // Batch chunk multiple texts
        let result = try chunkTextsBatch(["First doc text.", "Second doc text."], "{\"max_characters\":100}")
    }

}
