// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:363ae9f14785e79a688a5f35c488660399b7867bc4f3741c45625e03d2137e77
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: validate.
final class ValidateTests: XCTestCase {
    func testValidateBinarizationInvalid() throws {
        // Invalid binarization
        XCTAssertThrowsError(try validateBinarizationMethod("nonexistent"))
    }

    func testValidateBinarizationValid() throws {
        // Valid binarization
        let result = try validateBinarizationMethod("otsu")
    }

    func testValidateChunkingOverlapExceeds() throws {
        // Reject chunking config where overlap exceeds max_characters
        XCTAssertThrowsError(try extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_characters\":10,\"max_overlap\":100}}"))
    }

    func testValidateChunkingParamsInvalid() throws {
        // Chunking overlap >= max
        XCTAssertThrowsError(try validateChunkingParams(50, 60))
    }

    func testValidateChunkingParamsValid() throws {
        // Valid chunking params
        let result = try validateChunkingParams(500, 50)
    }

    func testValidateChunkingValid() throws {
        // Accept valid chunking config
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_characters\":1000,\"max_overlap\":200}}")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testValidateConfidenceBoundary() throws {
        // Confidence at 1.0
        let result = try validateConfidence(1.0)
    }

    func testValidateConfidenceNegative() throws {
        // Negative confidence
        XCTAssertThrowsError(try validateConfidence(-0.5))
    }

    func testValidateConfidenceOver() throws {
        // Confidence > 1
        XCTAssertThrowsError(try validateConfidence(1.5))
    }

    func testValidateConfidenceValid() throws {
        // Accept valid confidence threshold
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"ocr\":{\"confidence_threshold\":0.8}}")
    }

    func testValidateConfidenceZero() throws {
        // Confidence at 0.0
        let result = try validateConfidence(0.0)
    }

    func testValidateDpiNegative() throws {
        // Reject negative DPI in OCR config
        XCTAssertThrowsError(try extractFileSync("pdf/fake_memo.pdf", "{\"ocr\":{\"dpi\":-1}}"))
    }

    func testValidateDpiValid() throws {
        // Accept valid DPI in OCR config
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"ocr\":{\"dpi\":300}}")
    }

    func testValidateHostEmpty() throws {
        // Empty host rejected
        XCTAssertThrowsError(try validateHost(""))
    }

    func testValidateOcrBackendInvalid() throws {
        // Invalid OCR backend
        XCTAssertThrowsError(try validateOcrBackend("nonexistent"))
    }

    func testValidateOcrBackendValid() throws {
        // Valid OCR backend
        let result = try validateOcrBackend("tesseract")
    }

    func testValidateOutputFormatInvalid() throws {
        // Reject invalid output format
        XCTAssertThrowsError(try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"nonexistent_format\"}"))
    }

    func testValidateOutputFormatValid() throws {
        // Accept valid output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
    }

    func testValidatePortMax() throws {
        // Port 65535 accepted
        let result = try validatePort(65535)
    }

    func testValidatePortOver() throws {
        // Port over 65535
        XCTAssertThrowsError(try validatePort(70000))
    }

    func testValidatePortZero() throws {
        // Port 0 rejected
        XCTAssertThrowsError(try validatePort(0))
    }

    func testValidateReductionLevelInvalid() throws {
        // Invalid reduction level
        XCTAssertThrowsError(try validateTokenReductionLevel("nonexistent"))
    }

    func testValidateReductionLevelValid() throws {
        // Valid reduction level
        let result = try validateTokenReductionLevel("moderate")
    }

}
