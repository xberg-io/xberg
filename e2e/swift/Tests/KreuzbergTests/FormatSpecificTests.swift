// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:21d0b3e3f72fe67cabb3e9beb8433e6349eb77fe591f21f167715639689d2499
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: format_specific.
final class FormatSpecificTests: XCTestCase {
    func testFormatCsv() throws {
        // CSV file extraction using extract_file_sync
        let result = try extractFileSync("csv/data_table.csv")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testFormatDocxStandalone() throws {
        // Standalone DOCX extraction using extract_bytes_sync
        let result = try extractBytesSync(nil, "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testFormatEmailEml() throws {
        // Email EML file extraction using extract_file_sync
        let result = try extractFileSync("email/html_email_multipart.eml")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testFormatPdfText() throws {
        // Standalone PDF text extraction using extract_bytes_sync
        let result = try extractBytesSync(nil, "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("Mallori") || result.content.contains("May"), "expected to contain at least one of the specified values")
    }

    func testFormatPptx() throws {
        // PPTX presentation extraction using extract_bytes_sync
        let result = try extractBytesSync(nil, "application/vnd.openxmlformats-officedocument.presentationml.presentation")
    }

    func testFormatRtf() throws {
        // RTF file extraction using extract_file_sync
        let result = try extractFileSync("rtf/extraction_test.rtf")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testFormatXlsx() throws {
        // XLSX spreadsheet extraction using extract_bytes_sync
        let result = try extractBytesSync(nil, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
    }

}
