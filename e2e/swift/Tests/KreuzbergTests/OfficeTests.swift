// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:400c36a49b12ecbfa131b4a250eaea7ac574e5711d952a1ef489dc088bb2939c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: office.
final class OfficeTests: XCTestCase {
    func testOfficeBibtexBasic() async throws {
        // BibTeX bibliography extraction.
        let result = try await extractFile("bibtex/comprehensive.bib")
        XCTAssertTrue(result.mime_type.contains("application/x-bibtex") || result.mime_type.contains("text/x-bibtex"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeCommonmarkBasic() async throws {
        // CommonMark (.commonmark) text extraction.
        let result = try await extractFile("markdown/sample.commonmark")
        XCTAssertTrue(result.mime_type.contains("text/markdown") || result.mime_type.contains("text/plain") || result.mime_type.contains("text/x-commonmark"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testOfficeDjotBasic() async throws {
        // Djot markup text extraction.
        let result = try await extractFile("markdown/tables.djot")
        XCTAssertTrue(result.mime_type.contains("text/x-djot") || result.mime_type.contains("text/djot"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeDocbookBasic() async throws {
        // DocBook XML document extraction.
        let result = try await extractFile("docbook/docbook-reader.docbook")
        XCTAssertTrue(result.mime_type.contains("application/docbook+xml") || result.mime_type.contains("text/docbook"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeDocxBasic() async throws {
        // DOCX document extraction baseline.
        let result = try await extractFile("docx/sample_document.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeDocxEquations() async throws {
        // DOCX file containing equations to validate math extraction.
        let result = try await extractFile("docx/equations.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeDocxFake() async throws {
        // Simple DOCX document to verify baseline extraction.
        let result = try await extractFile("docx/fake.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeDocxFormatting() async throws {
        // DOCX document heavy on formatting for style preservation.
        let result = try await extractFile("docx/unit_test_formatting.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeDocxHeaders() async throws {
        // DOCX document with complex headers.
        let result = try await extractFile("docx/unit_test_headers.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeDocxLists() async throws {
        // DOCX document emphasizing list formatting.
        let result = try await extractFile("docx/unit_test_lists.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeDocxTables() async throws {
        // DOCX document containing tables for table-aware extraction.
        let result = try await extractFile("docx/docx_tables.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("Simple uniform table"), "expected to contain: \("Simple uniform table")")
        XCTAssertTrue(result.content.contains("Nested Table"), "expected to contain: \("Nested Table")")
        XCTAssertTrue(result.content.contains("merged cells"), "expected to contain: \("merged cells")")
        XCTAssertTrue(result.content.contains("Header Col"), "expected to contain: \("Header Col")")
        XCTAssertGreaterThanOrEqual(result.tables.count, 1)
    }

    func testOfficeEpubBasic() async throws {
        // EPUB book extraction with text content.
        let result = try await extractFile("epub/features.epub")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/epub+zip")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testOfficeFb2Basic() async throws {
        // FictionBook (FB2) document extraction baseline.
        let result = try await extractFile("fictionbook/basic.fb2")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-fictionbook+xml")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeFictionbookBasic() async throws {
        // FictionBook (.fb2) text extraction.
        let result = try await extractFile("fictionbook/basic.fb2")
        XCTAssertTrue(result.mime_type.contains("application/x-fictionbook+xml") || result.mime_type.contains("application/x-fictionbook"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeJatsBasic() async throws {
        // JATS scientific article extraction.
        let result = try await extractFile("jats/sample_article.jats")
        XCTAssertTrue(result.mime_type.contains("application/x-jats+xml") || result.mime_type.contains("text/jats"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeJupyterBasic() async throws {
        // Jupyter notebook extraction.
        let result = try await extractFile("jupyter/rank.ipynb")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-ipynb+json")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeKeynoteBasic() async throws {
        // Keynote document extraction baseline.
        let result = try await extractFile("iwork/test.key")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-iwork-keynote-sffkey")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testOfficeLatexBasic() async throws {
        // LaTeX document text extraction.
        let result = try await extractFile("latex/basic_sections.tex")
        XCTAssertTrue(result.mime_type.contains("application/x-latex") || result.mime_type.contains("text/x-latex"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeMarkdownBasic() async throws {
        // Markdown document extraction baseline.
        let result = try await extractFile("markdown/comprehensive.md")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "text/markdown")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeNumbersBasic() async throws {
        // Numbers document extraction baseline.
        let result = try await extractFile("iwork/test.numbers")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-iwork-numbers-sffnumbers")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeOdsBasic() async throws {
        // Basic ODS spreadsheet extraction.
        let result = try await extractFile("data_formats/test_01.ods")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.oasis.opendocument.spreadsheet")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeOdtBold() async throws {
        // ODT document with bold formatting.
        let result = try await extractFile("odt/bold.odt")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.oasis.opendocument.text")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeOdtList() async throws {
        // ODT document containing unordered lists with nesting.
        let result = try await extractFile("odt/unorderedList.odt")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.oasis.opendocument.text")
        XCTAssertGreaterThanOrEqual(result.content.count, 30)
        XCTAssertTrue(result.content.contains("list item") || result.content.contains("New level") || result.content.contains("Pushed us"), "expected to contain at least one of the specified values")
    }

    func testOfficeOdtSimple() async throws {
        // Basic ODT document with paragraphs and headings.
        let result = try await extractFile("odt/simple.odt")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.oasis.opendocument.text")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("favorite things") || result.content.contains("Parrots") || result.content.contains("Analysis"), "expected to contain at least one of the specified values")
    }

    func testOfficeOdtTable() async throws {
        // ODT document with a table structure.
        let result = try await extractFile("odt/table.odt")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.oasis.opendocument.text")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertGreaterThanOrEqual(result.tables.count, 1)
    }

    func testOfficeOpmlBasic() async throws {
        // OPML outline document extraction.
        let result = try await extractFile("opml/outline.opml")
        XCTAssertTrue(result.mime_type.contains("application/xml+opml") || result.mime_type.contains("text/x-opml") || result.mime_type.contains("application/x-opml+xml"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeOrgBasic() async throws {
        // Org-mode document text extraction.
        let result = try await extractFile("org/comprehensive.org")
        XCTAssertTrue(result.mime_type.contains("text/x-org") || result.mime_type.contains("text/org"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficePagesBasic() async throws {
        // Pages document extraction baseline.
        let result = try await extractFile("iwork/test.pages")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-iwork-pages-sffpages")
        XCTAssertGreaterThanOrEqual(result.content.count, 5)
    }

    func testOfficePpsxSlideshow() async throws {
        // PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
        let result = try await extractFile("pptx/sample.ppsx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.presentationml.slideshow")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficePptmBasic() async throws {
        // PowerPoint macro-enabled presentation (.pptm) extraction.
        let result = try await extractFile("pptx/powerpoint_with_image.pptm")
        XCTAssertTrue(result.mime_type.contains("application/vnd.ms-powerpoint.presentation.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.presentationml.presentation"), "expected to contain at least one of the specified values")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testOfficePptxBasic() async throws {
        // PPTX deck should extract slides content.
        let result = try await extractFile("pptx/simple.pptx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testOfficePptxImages() async throws {
        // PPTX presentation containing images to ensure metadata extraction.
        let result = try await extractFile("pptx/powerpoint_with_image.pptx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
        XCTAssertGreaterThanOrEqual(result.content.count, 15)
    }

    func testOfficePptxPitchDeck() async throws {
        // Pitch deck PPTX used to validate large slide extraction.
        let result = try await extractFile("pptx/pitch_deck_presentation.pptx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
        XCTAssertGreaterThanOrEqual(result.content.count, 100)
    }

    func testOfficeRstBasic() async throws {
        // reStructuredText document extraction.
        let result = try await extractFile("rst/restructured_text.rst")
        XCTAssertTrue(result.mime_type.contains("text/x-rst") || result.mime_type.contains("text/prs.fallenstein.rst"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testOfficeRtfBasic() async throws {
        // RTF document text extraction.
        let result = try await extractFile("rtf/extraction_test.rtf")
        XCTAssertTrue(result.mime_type.contains("application/rtf") || result.mime_type.contains("text/rtf"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeTypstBasic() async throws {
        // Typst document text extraction.
        let result = try await extractFile("typst/headings.typ")
        XCTAssertTrue(result.mime_type.contains("application/x-typst") || result.mime_type.contains("text/x-typst"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeXlsLegacy() async throws {
        // Legacy XLS spreadsheet to ensure backward compatibility.
        let result = try await extractFile("xls/test_excel.xls")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.ms-excel")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOfficeXlsbBasic() async throws {
        // Excel binary workbook (.xlsb) extraction.
        let result = try await extractFile("xlsx/test_xlsb.xlsb")
        XCTAssertTrue(result.mime_type.contains("application/vnd.ms-excel.sheet.binary.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testOfficeXlsmBasic() async throws {
        // Excel macro-enabled workbook (.xlsm) extraction.
        let result = try await extractFile("xlsx/test_01.xlsm")
        XCTAssertTrue(result.mime_type.contains("application/vnd.ms-excel.sheet.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testOfficeXlsxBasic() async throws {
        // XLSX spreadsheet should produce metadata and table content.
        let result = try await extractFile("xlsx/stanley_cups.xlsx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        XCTAssertGreaterThanOrEqual(result.content.count, 100)
        XCTAssertTrue(result.content.contains("Team"), "expected to contain: \("Team")")
        XCTAssertTrue(result.content.contains("Location"), "expected to contain: \("Location")")
        XCTAssertTrue(result.content.contains("Stanley Cups"), "expected to contain: \("Stanley Cups")")
        XCTAssertGreaterThanOrEqual(result.tables.count, 1)
        XCTAssertGreaterThanOrEqual(result.metadata.sheet_count, 2)
        XCTAssertTrue(result.metadata.sheet_names.contains("Stanley Cups"), "expected to contain: \("Stanley Cups")")
    }

    func testOfficeXlsxMultiSheet() async throws {
        // XLSX workbook with multiple sheets.
        let result = try await extractFile("xlsx/excel_multi_sheet.xlsx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
        XCTAssertGreaterThanOrEqual(result.metadata.sheet_count, 2)
    }

    func testOfficeXlsxOfficeExample() async throws {
        // Simple XLSX spreadsheet shipped alongside office integration tests.
        let result = try await extractFile("xlsx/test_01.xlsx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

}
