// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:558f65e658c22ccf996e2c148179478739e5afed9e2f8127fa81fb8820ac650d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: archive.
final class ArchiveTests: XCTestCase {
    func testArchiveGzBasic() async throws {
        // Gzip compressed file extraction.
        let result = try await extractFile("archives/book_war_and_peace_1p.txt.gz")
        XCTAssertTrue(result.mime_type.contains("application/gzip") || result.mime_type.contains("application/x-gzip"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testArchiveSevenzBasic() async throws {
        // 7-Zip archive extraction.
        let result = try await extractFile("archives/documents.7z")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-7z-compressed")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testArchiveTarBasic() async throws {
        // TAR archive extraction.
        let result = try await extractFile("archives/documents.tar")
        XCTAssertTrue(result.mime_type.contains("application/x-tar") || result.mime_type.contains("application/tar"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testArchiveZipBasic() async throws {
        // ZIP archive extraction.
        let result = try await extractFile("archives/documents.zip")
        XCTAssertTrue(result.mime_type.contains("application/zip") || result.mime_type.contains("application/x-zip-compressed"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

}
