// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:17767ab1fb56c8130e03ae4d529180a7adafa20fa405f1c2f5135f7938f85b24
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: markup.
final class MarkupTests: XCTestCase {
    func testBoldBasic() throws {
        // Create bold markdown formatting
        let result = try bold("important")
        XCTAssertTrue(result.result.contains("important"), "expected to contain: \("important")")
    }

    func testBoldEmpty() throws {
        // Bold empty string
        let result = try bold("")
    }

    func testBoldSpecialChars() throws {
        // Bold special chars
        let result = try bold("hello **world** & <tag>")
        XCTAssertTrue(result.result.contains("hello"), "expected to contain: \("hello")")
    }

    func testCodeBasic() throws {
        // Create inline code formatting
        let result = try code("variable")
        XCTAssertTrue(result.result.contains("variable"), "expected to contain: \("variable")")
    }

    func testCodeMultiline() throws {
        // Code multiline
        let result = try code("line1\nline2")
    }

    func testItalicBasic() throws {
        // Create italic markdown formatting
        let result = try italic("emphasis")
        XCTAssertTrue(result.result.contains("emphasis"), "expected to contain: \("emphasis")")
    }

    func testItalicEmpty() throws {
        // Italic empty string
        let result = try italic("")
    }

    func testItalicSpecialChars() throws {
        // Italic special chars
        let result = try italic("emphasized *text*")
    }

    func testLinkBasic() throws {
        // Create markdown link with text and URL
        let result = try link("Click here", "https://example.com")
        XCTAssertTrue(result.result.contains("Click here"), "expected to contain: \("Click here")")
        XCTAssertTrue(result.result.contains("example.com"), "expected to contain: \("example.com")")
    }

    func testLinkEmptyText() throws {
        // Create markdown link with empty text
        let result = try link("", "https://example.com")
        XCTAssertTrue(result.result.contains("example.com"), "expected to contain: \("example.com")")
    }

    func testLinkSpecialUrl() throws {
        // Link with query params
        let result = try link("click", "https://example.com?q=1&b=2")
    }

    func testStrikethroughBasic() throws {
        // Create strikethrough formatting
        let result = try strikethrough("deleted")
        XCTAssertTrue(result.result.contains("deleted"), "expected to contain: \("deleted")")
    }

    func testStrikethroughEmpty() throws {
        // Strikethrough empty
        let result = try strikethrough("")
    }

    func testUnderlineBasic() throws {
        // Create underline formatting
        let result = try underline("underlined")
        XCTAssertTrue(result.result.contains("underlined"), "expected to contain: \("underlined")")
    }

    func testUnderlineEmpty() throws {
        // Underline empty
        let result = try underline("")
    }

}
