// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:266a809cae09883b9b652f4909f423fcaf1ccb42e05a2cd4c7c65ed281510f68
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: pdf.
final class PdfTests: XCTestCase {
    func testPdfAssemblyTechnical() async throws {
        // Assembly language technical manual with large body of text.
        let result = try await extractFile("pdf/assembly_language_for_beginners_al4_b_en.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 5000)
        XCTAssertTrue(result.content.contains("assembly") || result.content.contains("register") || result.content.contains("instruction"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfBayesianDataAnalysis() async throws {
        // Bayesian data analysis textbook PDF with large content volume.
        let result = try await extractFile("pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10000)
        XCTAssertTrue(result.content.contains("Bayesian") || result.content.contains("probability") || result.content.contains("distribution"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfCodeAndFormula() async throws {
        // PDF containing code snippets and formulas should retain substantial content.
        let result = try await extractFile("pdf/code_and_formula.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 100)
    }

    func testPdfDeepLearning() async throws {
        // Deep learning textbook PDF to ensure long-form extraction quality.
        let result = try await extractFile("pdf/fundamentals_of_deep_learning_2014.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 1000)
        XCTAssertTrue(result.content.contains("neural") || result.content.contains("network") || result.content.contains("deep learning"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfEmbeddedImages() async throws {
        // PDF with embedded images should extract text and tables when present.
        let result = try await extractFile("pdf/embedded_images_tables.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertGreaterThanOrEqual(result.tables.count, 0)
    }

    func testPdfGoogleDoc() async throws {
        // Google Docs exported PDF to verify conversion fidelity.
        let result = try await extractFile("pdf/google_doc_document.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfLargeCiml() async throws {
        // Large machine learning textbook PDF to stress extraction length.
        let result = try await extractFile("pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10000)
        XCTAssertTrue(result.content.contains("machine learning") || result.content.contains("algorithm") || result.content.contains("training"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfNonEnglishGerman() async throws {
        // German technical PDF to ensure non-ASCII content extraction.
        let result = try await extractFile("pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 100)
        XCTAssertTrue(result.content.contains("Intel") || result.content.contains("paging"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfRightToLeft() async throws {
        // Right-to-left language PDF to verify RTL extraction.
        let result = try await extractFile("pdf/right_to_left_01.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

    func testPdfSimpleText() async throws {
        // Simple text-heavy PDF should extract content without OCR or tables.
        let result = try await extractFile("pdf/fake_memo.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("To Whom it May Concern") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testPdfTablesLarge() async throws {
        // Large PDF with extensive tables to stress table extraction.
        let result = try await extractFile("pdf/large.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 500)
    }

    func testPdfTablesMedium() async throws {
        // Medium-sized PDF with multiple tables.
        let result = try await extractFile("pdf/medium.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 100)
    }

    func testPdfTechnicalStatLearning() async throws {
        // Technical statistical learning PDF requiring substantial extraction.
        let result = try await extractFile("pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10000)
        XCTAssertTrue(result.content.contains("statistical") || result.content.contains("regression") || result.content.contains("learning"), "expected to contain at least one of the specified values")
        XCTAssertEqual(result.metadata.format_type.trimmingCharacters(in: .whitespaces), "pdf")
    }

}
