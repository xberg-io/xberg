// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ae72c744d049a7304b5c58107a7fe9675eaaec195c6422d2b006cd93a42f9444
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: validation.
final class ValidationTests: XCTestCase {
    func testValidateConfidenceFnInvalid() throws {
        // Reject confidence threshold > 1.0
        XCTAssertThrowsError(try validateConfidence(2.0))
    }

    func testValidateConfidenceFnValid() throws {
        // Accept valid confidence threshold
        let result = try validateConfidence(0.95)
    }

    func testValidateFormatInvalid() throws {
        // Reject invalid output format
        XCTAssertThrowsError(try validateOutputFormat("nonexistent"))
    }

    func testValidateFormatValid() throws {
        // Accept valid output format
        let result = try validateOutputFormat("markdown")
    }

    func testValidateHostValid() throws {
        // Accept valid host address
        let result = try validateHost("127.0.0.1")
    }

    func testValidateLangInvalid() throws {
        // Reject invalid language code
        XCTAssertThrowsError(try validateLanguageCode("zzz_nonexistent_9999"))
    }

    func testValidateLangValid() throws {
        // Accept valid language code
        let result = try validateLanguageCode("eng")
    }

    func testValidateMimeInvalid() throws {
        // Reject invalid MIME type
        XCTAssertThrowsError(try validateMimeType("invalid"))
    }

    func testValidateMimeValid() throws {
        // Accept valid MIME type
        let result = try validateMimeType("application/pdf")
    }

    func testValidateOemInvalid() throws {
        // Reject invalid Tesseract OEM mode
        XCTAssertThrowsError(try validateTesseractOem(999))
    }

    func testValidateOemValid() throws {
        // Accept valid Tesseract OEM mode
        let result = try validateTesseractOem(1)
    }

    func testValidatePortInvalid() throws {
        // Reject invalid port number (zero)
        XCTAssertThrowsError(try validatePort(0))
    }

    func testValidatePortValid() throws {
        // Accept valid port number
        let result = try validatePort(8080)
    }

    func testValidatePsmInvalid() throws {
        // Reject invalid Tesseract PSM mode
        XCTAssertThrowsError(try validateTesseractPsm(999))
    }

    func testValidatePsmValid() throws {
        // Accept valid Tesseract PSM mode
        let result = try validateTesseractPsm(6)
    }

}
