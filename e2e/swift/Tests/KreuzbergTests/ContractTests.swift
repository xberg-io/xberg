// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7f44a83b5e80557f60ce2c9d2e4142507aa3ef9e2cccbdaa312b4b63bf2cff41
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: contract.
final class ContractTests: XCTestCase {
    func testApiBatchBytesAsync() async throws {
        // Tests async batch bytes extraction API (batch_extract_bytes)
        let result = try await batchExtractBytes(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiBatchBytesSync() throws {
        // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
        let result = try batchExtractBytesSync(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiBatchBytesWithConfigsAsync() async throws {
        // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
        let result = try await batchExtractBytes(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testApiBatchBytesWithConfigsSync() throws {
        // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
        let result = try batchExtractBytesSync(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testApiBatchFileAsync() async throws {
        // Tests async batch file extraction API (batch_extract_file)
        let result = try await batchExtractFile(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiBatchFileSync() throws {
        // Tests sync batch file extraction API (batch_extract_file_sync)
        let result = try batchExtractFileSync(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiBatchFileWithConfigsAsync() async throws {
        // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
        let result = try await batchExtractFile(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testApiBatchFileWithConfigsSync() throws {
        // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
        let result = try batchExtractFileSync(nil)
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testApiBatchFileWithTimeoutSync() throws {
        // Tests sync batch file extraction with per-file timeout config override
        let result = try batchExtractFileSync(nil, "{\"extraction_timeout_secs\":300}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testApiExtractBytesAsync() async throws {
        // Tests async bytes extraction API (extract_bytes)
        let result = try await extractBytes(nil, "")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiExtractBytesSync() throws {
        // Tests sync bytes extraction API (extract_bytes_sync)
        let result = try extractBytesSync(nil, "")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiExtractFileAsync() async throws {
        // Tests async file extraction API (extract_file)
        let result = try await extractFile("pdf/fake_memo.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testApiExtractFileSync() throws {
        // Tests sync file extraction API (extract_file_sync)
        let result = try extractFileSync("pdf/fake_memo.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("Mallori"), "expected to contain at least one of the specified values")
    }

    func testConfigAccelerationCpuProvider() throws {
        // Tests explicit CPU acceleration provider configuration
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"acceleration\":{\"device_id\":0,\"provider\":\"cpu\"}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("May 5, 2023") || result.content.contains("To Whom it May Concern"), "expected to contain at least one of the specified values")
    }

    func testConfigChunking() throws {
        // Tests chunking configuration with chunk assertions
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertGreaterThanOrEqual(result.chunks.count, 1)
        XCTAssertTrue(result.chunks_have_content)
    }

    func testConfigChunkingText() throws {
        // Tests text chunker type (generic whitespace/punctuation splitter)
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"chunker_type\":\"text\",\"max_chars\":500,\"max_overlap\":50}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertGreaterThanOrEqual(result.chunks.count, 1)
        XCTAssertTrue(result.chunks_have_content)
    }

    func testConfigDisableOcr() throws {
        // Tests disable_ocr configuration option - OCR is skipped for images
        let result = try extractFileSync("images/test_hello_world.png", "{\"disable_ocr\":true}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "image/png")
        XCTAssertLessThanOrEqual(result.content.count, 5)
    }

    func testConfigDocumentStructure() throws {
        // Tests include_document_structure config produces document tree
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"include_document_structure\":true}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertFalse(result.document_structure.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document_structure.nodes.count, 1)
        XCTAssertTrue(result.document_structure.node_types.contains("paragraph"), "expected to contain: \("paragraph")")
    }

    func testConfigDocumentStructureDisabled() throws {
        // Tests document field is null when include_document_structure is false
        let result = try extractFileSync("pdf/fake_memo.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
    }

    func testConfigDocumentStructureWithHeadings() throws {
        // Tests document structure with DOCX heading-driven nesting
        let result = try extractFileSync("docx/fake.docx", "{\"include_document_structure\":true}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertFalse(result.document_structure.isEmpty, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.document_structure.nodes.count, 1)
    }

    func testConfigEmailMsgFallbackCodepage() async throws {
        // Tests MSG extraction with custom fallback codepage for Cyrillic
        let result = try await extractFile("email/fake_email.msg", "{\"email\":{\"msg_fallback_codepage\":1251}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.ms-outlook")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testConfigExtractionTimeout() throws {
        // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"extraction_timeout_secs\":300}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testConfigHtmlOptions() throws {
        // Tests extraction with HTML conversion options configured
        let result = try extractFileSync("html/complex_table.html", "{\"html_options\":{\"extract_metadata\":true}}")
        XCTAssertTrue(result.mime_type.contains("text/html"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testConfigHtmlStyledCustomCss() throws {
        // HTML output with inline user CSS string is embedded verbatim in the style block
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"css\":\".kb-p { color: red; }\",\"embed_css\":true,\"theme\":\"unstyled\"},\"output_format\":\"html\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "html")
        XCTAssertTrue(result.content.contains(".kb-p { color: red; }"), "expected to contain: \(".kb-p { color: red; }")")
        XCTAssertTrue(result.content.contains("kb-doc"), "expected to contain: \("kb-doc")")
    }

    func testConfigHtmlStyledDefault() throws {
        // HTML output with default theme emits kb-* class hooks and an embedded style block
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":true,\"theme\":\"default\"},\"output_format\":\"html\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "html")
        XCTAssertTrue(result.content.contains("kb-doc"), "expected to contain: \("kb-doc")")
        XCTAssertTrue(result.content.contains("kb-content"), "expected to contain: \("kb-content")")
        XCTAssertTrue(result.content.contains("kb-p"), "expected to contain: \("kb-p")")
        XCTAssertTrue(result.content.contains("<style>"), "expected to contain: \("<style>")")
    }

    func testConfigHtmlStyledNoEmbed() throws {
        // HTML output with embed_css false emits class hooks but no style block
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":false,\"theme\":\"default\"},\"output_format\":\"html\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "html")
        XCTAssertTrue(result.content.contains("kb-doc"), "expected to contain: \("kb-doc")")
        XCTAssertTrue(result.content.contains("kb-content"), "expected to contain: \("kb-content")")
    }

    func testConfigImages() throws {
        // Tests image extraction configuration with image assertions
        let result = try extractFileSync("pdf/embedded_images_tables.pdf", "{\"images\":{\"extract_images\":true}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.images.count, 1)
    }

    func testConfigImagesWithFormats() throws {
        // Tests image extraction on PPTX containing embedded images
        let result = try extractFileSync("pptx/powerpoint_with_image.pptx", "{\"images\":{\"extract_images\":true}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.presentationml.presentation")
        XCTAssertGreaterThanOrEqual(result.images.count, 1)
    }

    func testConfigLanguageDetection() throws {
        // Tests language detection configuration
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"language_detection\":{\"enabled\":true}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.detected_languages.contains("eng"), "expected to contain: \("eng")")
        XCTAssertGreaterThanOrEqual(result.language_detection_confidence, 0.5)
    }

    func testConfigLanguageDetectionMulti() throws {
        // Tests language detection with detect_multiple and min_confidence options
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"language_detection\":{\"detect_multiple\":true,\"enabled\":true,\"min_confidence\":0.3}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.detected_languages.contains("eng"), "expected to contain: \("eng")")
    }

    func testConfigPostprocessor() throws {
        // Tests postprocessor config is accepted and extraction succeeds
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"postprocessor\":{\"enabled\":true}}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testConfigProcessingWarningsEmpty() throws {
        // Tests that a clean PDF extraction produces no processing warnings
        let result = try extractFileSync("pdf/fake_memo.pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.processing_warnings.isEmpty, "expected empty value")
    }

    func testConfigQualityDisabled() throws {
        // Tests extraction with quality processing explicitly disabled
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"enable_quality_processing\":false}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testConfigSecurityLimits() throws {
        // Tests archive extraction with custom security limits
        let result = try extractFileSync("archives/documents.zip", "{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}}")
        XCTAssertTrue(result.mime_type.contains("application/zip") || result.mime_type.contains("application/x-zip-compressed"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testConfigTablesContent() throws {
        // Tests table extraction with content_contains_any assertion on DOCX with tables
        let result = try extractFileSync("docx/docx_tables.docx")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.tables.count, 1)
    }

    func testConfigUseCacheFalse() throws {
        // Tests use_cache=false configuration option
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"use_cache\":false}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testOutputFormatBytesMarkdown() throws {
        // Tests markdown output format via bytes extraction API
        let result = try extractBytesSync(nil, "", "{\"output_format\":\"markdown\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testOutputFormatDjot() throws {
        // Tests Djot output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "djot")
    }

    func testOutputFormatHtml() throws {
        // Tests HTML output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "html")
    }

    func testOutputFormatMarkdown() throws {
        // Tests Markdown output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
    }

    func testOutputFormatPlain() throws {
        // Tests Plain output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "plain")
    }

    func testResultFormatElementBased() throws {
        // Tests ElementBased result format with element assertions
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"result_format\":\"element_based\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertEqual(result.result_format.trimmingCharacters(in: .whitespaces), "element_based")
        XCTAssertGreaterThanOrEqual(result.elements.count, 1)
    }

    func testResultFormatUnified() throws {
        // Tests Unified result format (default)
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"result_format\":\"unified\"}")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertEqual(result.result_format.trimmingCharacters(in: .whitespaces), "unified")
    }

}
