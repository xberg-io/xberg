// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a2e083ec927112a9b2759eef520337885e5d4864a60e709c9c2eccd016dd0ce5
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: structured.
final class StructuredTests: XCTestCase {
    func testStructuredCsvBasic() async throws {
        // CSV data file extraction.
        let result = try await extractFile("csv/stanley_cups.csv")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "text/csv")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testStructuredEnwBasic() async throws {
        // EndNote ENW citation format extraction.
        let result = try await extractFile("data_formats/sample.enw")
        XCTAssertTrue(result.mime_type.contains("application/x-endnote-refer") || result.mime_type.contains("application/x-endnote+xml") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
    }

    func testStructuredJsonBasic() async throws {
        // Structured JSON extraction should stream and preserve content.
        let result = try await extractFile("json/sample_document.json")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/json")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
        XCTAssertTrue(result.content.contains("Sample Document") || result.content.contains("Test Author"), "expected to contain at least one of the specified values")
    }

    func testStructuredJsonSimple() async throws {
        // Simple JSON document to verify structured extraction.
        let result = try await extractFile("json/simple.json")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/json")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
        XCTAssertTrue(result.content.contains("{") || result.content.contains("name"), "expected to contain at least one of the specified values")
    }

    func testStructuredNbibBasic() async throws {
        // PubMed NBIB citation format extraction.
        let result = try await extractFile("data_formats/sample.nbib")
        XCTAssertTrue(result.mime_type.contains("application/nbib") || result.mime_type.contains("application/x-pubmed") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testStructuredRisBasic() async throws {
        // RIS citation format extraction.
        let result = try await extractFile("data_formats/sample.ris")
        XCTAssertTrue(result.mime_type.contains("application/x-research-info-systems") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        XCTAssertFalse(result.content.isEmpty, "expected non-empty value")
    }

    func testStructuredTomlBasic() async throws {
        // TOML configuration file extraction.
        let result = try await extractFile("data_formats/cargo.toml")
        XCTAssertTrue(result.mime_type.contains("application/toml") || result.mime_type.contains("text/toml"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testStructuredTsvBasic() async throws {
        // TSV (tab-separated values) data file extraction.
        let result = try await extractFile("data_formats/employees.tsv")
        XCTAssertTrue(result.mime_type.contains("text/tab-separated-values") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testStructuredYamlBasic() async throws {
        // YAML file text extraction.
        let result = try await extractFile("yaml/simple.yaml")
        XCTAssertTrue(result.mime_type.contains("application/yaml") || result.mime_type.contains("text/yaml") || result.mime_type.contains("text/x-yaml") || result.mime_type.contains("application/x-yaml"), "expected to contain at least one of the specified values")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testStructuredYamlSimple() async throws {
        // Simple YAML document to validate structured extraction.
        let result = try await extractFile("yaml/simple.yaml")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/x-yaml")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

}
