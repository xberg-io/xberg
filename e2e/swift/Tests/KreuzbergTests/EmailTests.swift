// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:aeead02e862dc126b1aad9d09d6cab083bd930810feea70b9b07739d14af0dec
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: email.
final class EmailTests: XCTestCase {
    func testEmailEmlHtmlBody() async throws {
        // EML with HTML body content.
        let result = try await extractFile("email/html_only.eml")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "message/rfc822")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testEmailEmlMultipart() async throws {
        // EML with multipart MIME content.
        let result = try await extractFile("email/html_email_multipart.eml")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "message/rfc822")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testEmailEmlUtf16() async throws {
        // UTF-16 encoded EML file with BOM.
        let result = try await extractFile("vendored/unstructured/eml/fake-email-utf-16.eml")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "message/rfc822")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
        XCTAssertTrue(result.content.contains("Test Email") || result.content.contains("Roses are red"), "expected to contain at least one of the specified values")
    }

    func testEmailMsgBasic() async throws {
        // Outlook MSG file extraction.
        let result = try await extractFile("email/fake_email.msg")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.ms-outlook")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testEmailPstEmpty() async throws {
        // Empty Outlook PST archive with no messages.
        let result = try await extractFile("email/empty.pst")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/vnd.ms-outlook-pst")
    }

    func testEmailSampleEml() async throws {
        // Sample EML email file to verify email parsing.
        let result = try await extractFile("email/sample_email.eml")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "message/rfc822")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

}
