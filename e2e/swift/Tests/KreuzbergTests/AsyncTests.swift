// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d5a3e5190b34b5f12f0e934fbb10a3368c305d7df8a838b164e990fe78b5ca1f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: async.
final class AsyncTests: XCTestCase {
    func testAsyncExtractBytes() async throws {
        // Async extract_bytes call on PDF document
        let result = try await extractBytes(nil, "application/pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testAsyncExtractBytesEmptyMime() async throws {
        // extract_bytes empty MIME async
        do {
            _ = try await extractBytes("SGVsbG8=", "", "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAsyncExtractBytesInvalidMime() async throws {
        // extract_bytes unsupported MIME async
        do {
            _ = try await extractBytes("SGVsbG8=", "application/x-nonexistent", "{}")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testAsyncExtractFile() async throws {
        // Async extract_file call on PDF document
        let result = try await extractFile("pdf/fake_memo.pdf", "application/pdf")
        XCTAssertEqual(result.mime_type.trimmingCharacters(in: .whitespaces), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testAsyncExtractFileDocx() async throws {
        // Async extract_file call on DOCX document
        let result = try await extractFile("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content.count, 20)
    }

    func testAsyncExtractFileHtml() async throws {
        // Async extract_file call on HTML document
        let result = try await extractFile("html/simple_table.html", "text/html")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testAsyncExtractFileImage() async throws {
        // Async extract_file call on image document
        let result = try await extractFile("images/test_hello_world.png", "image/png")
    }

    func testAsyncExtractFileWithConfig() async throws {
        // Async extract_file call with output format and chunking config
        let result = try await extractFile("pdf/fake_memo.pdf", "application/pdf", "{\"chunking\":{\"max_characters\":500,\"max_overlap\":50},\"output_format\":\"markdown\"}")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

}
