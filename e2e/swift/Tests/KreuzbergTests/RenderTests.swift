// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0a114e24a97dc97a6c0c0674c4ef50b9ec6f2de6aaff49bc8f2e154825e1d316
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Kreuzberg

/// E2e tests for category: render.
final class RenderTests: XCTestCase {
    func testRenderCustomDpi() throws {
        // Render PDF page with custom DPI
        let result = try renderPage("pdf/tiny.pdf", 0)
        XCTAssertTrue(result.is_png)
        XCTAssertGreaterThanOrEqual(result.byte_length, 50)
    }

    func testRenderHighDpi() throws {
        // Render PDF page with high DPI setting (300 DPI)
        let result = try renderPage("pdf/fake_memo.pdf", 0, "{\"dpi\":300}")
    }

    func testRenderIterator() throws {
        // Iterate all PDF pages as PNG images
        let result = try renderPage("pdf/tiny.pdf", 0)
        XCTAssertTrue(result.is_png)
        XCTAssertGreaterThanOrEqual(result.page_count, 1)
    }

    func testRenderMultiPage() throws {
        // Render a page from a multi-page PDF document
        let result = try renderPage("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf", 0)
        XCTAssertGreaterThanOrEqual(result.result.count, 1000)
    }

    func testRenderOutputDjot() throws {
        // Extract PDF with Djot output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "djot")
        XCTAssertGreaterThanOrEqual(result.content.count, 10)
    }

    func testRenderOutputHtml() throws {
        // Extract PDF with HTML output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "html")
        XCTAssertTrue(result.content.contains("<") || result.content.contains(">") || result.content.contains("</"), "expected to contain at least one of the specified values")
    }

    func testRenderOutputMarkdown() throws {
        // Extract PDF with Markdown output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "markdown")
        XCTAssertTrue(result.content.contains("#") || result.content.contains("**") || result.content.contains("*"), "expected to contain at least one of the specified values")
    }

    func testRenderOutputPlain() throws {
        // Extract PDF with plain text output format
        let result = try extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}")
        XCTAssertEqual(result.output_format.trimmingCharacters(in: .whitespaces), "plain")
        XCTAssertGreaterThanOrEqual(result.content.count, 50)
    }

    func testRenderSinglePage() throws {
        // Render a single PDF page to PNG
        let result = try renderPage("pdf/tiny.pdf", 0)
        XCTAssertTrue(result.is_png)
        XCTAssertGreaterThanOrEqual(result.byte_length, 100)
    }

}
