// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4177ce3bd425ed8a934183e79f7d97a67997facc84c30de86f79a6b8490348e4
// To regenerate: alef e2e generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { extract, WasmExtractInput, WasmExtractionConfig, WasmAccelerationConfig, WasmBedrockConfig, WasmBrowserConfig, WasmCaptioningConfig, WasmChunkClassificationConfig, WasmChunkClassificationDefinition, WasmChunkingConfig, WasmConcurrencyConfig, WasmContentConfig, WasmContentFilterConfig, WasmConversionOptions, WasmCrawlConfig, WasmCsvConfig, WasmEmailConfig, WasmEmbeddingConfig, WasmFileExtractionConfig, WasmGeoJsonExtractionConfig, WasmHierarchyConfig, WasmHtmlOutputConfig, WasmImageExtractionConfig, WasmImagePreprocessingConfig, WasmKeywordConfig, WasmLanguageDetectionConfig, WasmLateInteractionConfig, WasmLayoutDetectionConfig, WasmLlmBudgetConfig, WasmLlmCacheConfig, WasmLlmConfig, WasmLlmProviderConfig, WasmLlmRateLimitConfig, WasmNerConfig, WasmNgramRange, WasmOcrConfig, WasmOcrElementConfig, WasmOcrPipelineConfig, WasmOcrPipelineStage, WasmOcrQualityThresholds, WasmPageClassificationConfig, WasmPageConfig, WasmPdfConfig, WasmPostProcessorConfig, WasmPreprocessingOptions, WasmProxyConfig, WasmRakeParams, WasmRedactionConfig, WasmRedactionPattern, WasmRedactionTerm, WasmSecurityLimits, WasmSparseEmbeddingConfig, WasmSsrfPolicy, WasmStructuredExtractionConfig, WasmSummarizationConfig, WasmSvgOptions, WasmTesseractConfig, WasmTokenReductionOptions, WasmTranscriptionConfig, WasmTranslationConfig, WasmTreeSitterConfig, WasmTreeSitterProcessConfig, WasmUrlExtractionConfig, WasmYakeParams, WasmExtractInputKind } from "@xberg-io/xberg-wasm";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";
process.env.RUST_MIN_STACK ??= "16777216";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let bytes = new Uint8Array(await response.arrayBuffer());

	// The runtime's own fetch (undici on Node, and the browser equivalent) decodes
	// `content-encoding` transparently and leaves the header in place, so by the time the
	// body reaches here it is usually already plain text. Decoding unconditionally on the
	// header alone therefore fails ("incorrect header check") against a server that really
	// did compress. Decode only when the payload still carries the encoding: gzip announces
	// itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
	// already-decoded case falls back to the bytes as received.
	if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
		const { gunzipSync } = await import("zlib");
		bytes = new Uint8Array(gunzipSync(bytes));
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		try {
			bytes = new Uint8Array(brotliDecompressSync(bytes));
		} catch {
			// Already decoded by the runtime.
		}
	}

	const text = new TextDecoder().decode(bytes);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is internally tagged, so serde merges the variant payload's own fields as
	// siblings of the tag rather than nesting them: { format_type: 'image', format: 'PNG', ... }.
	// Read `format` off the record directly. Deliberately NO nested (`record.image`) fallback --
	// a binding that regresses to the pre-flatten shape must fail this assertion loudly instead
	// of silently degrading to the variant name below.
	if (formatType === "image" && typeof record.format === "string") {
		return record.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("format_specific", () => {

	it("format_docx_equations: DOCX equations extract to LaTeX math in markdown output", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_DOCX_EQUATIONS ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_docx_equations`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.filename = "equations.docx"; _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"; _u0.uri = `${inputMockBaseUrl}/docx/equations.docx`; return _u0; })(), (() => { const _u0 = WasmExtractionConfig.default(); _u0.outputFormat = "markdown"; return _u0; })());
    expect(result.results[0].content.length).toBeGreaterThanOrEqual(20);
    expect(result.results[0].content).toContain("$");

	}, 30000);

	it("format_docx_standalone: Standalone DOCX extraction using extract", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_DOCX_STANDALONE ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_docx_standalone`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.filename = "fake.docx"; _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"; _u0.uri = `${inputMockBaseUrl}/docx/fake.docx`; return _u0; })(), undefined);
    expect(result.results[0].content.length).toBeGreaterThanOrEqual(20);

	}, 30000);

	it("format_hwpx_standalone: Standalone HWPX extraction using extract", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_HWPX_STANDALONE ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_hwpx_standalone`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.filename = "simple.hwpx"; _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/haansofthwpx"; _u0.uri = `${inputMockBaseUrl}/hwpx/simple.hwpx`; return _u0; })(), undefined);
    expect(result.results[0].content.length).toBeGreaterThanOrEqual(20);
    expect(result.results[0].content).toContain("Hello from HWPX");

	}, 30000);

	it("format_pdf_text: Standalone PDF text extraction using extract", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_PDF_TEXT ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_pdf_text`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.filename = "fake_memo.pdf"; _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/pdf"; _u0.uri = `${inputMockBaseUrl}/pdf/fake_memo.pdf`; return _u0; })(), undefined);
    expect(result.results[0].content.length).toBeGreaterThanOrEqual(50);
    expect(["Mallori", "May"].some((v) => result.results[0].content.includes(v))).toBe(true);

	}, 30000);

	it("format_pptx: PPTX presentation extraction using extract", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_PPTX ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_pptx`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/vnd.openxmlformats-officedocument.presentationml.presentation"; _u0.uri = `${inputMockBaseUrl}/pptx/simple.pptx`; return _u0; })(), undefined);
    expect(result).toBeDefined();

	}, 30000);

	it("format_xlsx: XLSX spreadsheet extraction using extract", async () => {
		const inputMockBaseUrl = process.env.MOCK_SERVER_FORMAT_XLSX ?? `${process.env.MOCK_SERVER_URL}/fixtures/format_xlsx`;
		const result = await extract((() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.mimeType = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"; _u0.uri = `${inputMockBaseUrl}/xlsx/stanley_cups.xlsx`; return _u0; })(), undefined);
    expect(result).toBeDefined();

	}, 30000);

});
