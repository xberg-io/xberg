// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:67e227919bd1cb592d2fc3c40c3ce38fc30babe5fc11fd4b7cf10390ca5263eb
// To regenerate: alef e2e generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { extract, registerEmbeddingBackend, registerOcrBackend, registerPostProcessor, registerRerankerBackend, registerTokenizerBackend, registerValidator, unregisterEmbeddingBackend, unregisterPostProcessor, unregisterRerankerBackend, unregisterTokenizerBackend, unregisterValidator, WasmAccelerationConfig, WasmBedrockConfig, WasmBrowserConfig, WasmCaptioningConfig, WasmChunkClassificationConfig, WasmChunkClassificationDefinition, WasmChunkingConfig, WasmConcurrencyConfig, WasmContentConfig, WasmContentFilterConfig, WasmConversionOptions, WasmCrawlConfig, WasmCsvConfig, WasmEmailConfig, WasmEmbeddingConfig, WasmGeoJsonExtractionConfig, WasmHierarchyConfig, WasmHtmlOutputConfig, WasmImageExtractionConfig, WasmImagePreprocessingConfig, WasmKeywordConfig, WasmLanguageDetectionConfig, WasmLateInteractionConfig, WasmLayoutDetectionConfig, WasmLlmBudgetConfig, WasmLlmCacheConfig, WasmLlmConfig, WasmLlmProviderConfig, WasmLlmRateLimitConfig, WasmNerConfig, WasmNgramRange, WasmOcrConfig, WasmOcrElementConfig, WasmOcrPipelineConfig, WasmOcrPipelineStage, WasmOcrQualityThresholds, WasmPageClassificationConfig, WasmPageConfig, WasmPdfConfig, WasmPostProcessorConfig, WasmPreprocessingOptions, WasmProxyConfig, WasmRakeParams, WasmRedactionConfig, WasmRedactionPattern, WasmRedactionTerm, WasmSecurityLimits, WasmSparseEmbeddingConfig, WasmSsrfPolicy, WasmStructuredExtractionConfig, WasmSummarizationConfig, WasmSvgOptions, WasmTesseractConfig, WasmTokenReductionOptions, WasmTranscriptionConfig, WasmTranslationConfig, WasmTreeSitterConfig, WasmTreeSitterProcessConfig, WasmUrlExtractionConfig, WasmYakeParams } from "@xberg-io/xberg-wasm";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";
process.env.RUST_MIN_STACK ??= "16777216";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let bytes = new Uint8Array(await response.arrayBuffer());

	// The runtime's own fetch (undici on Node, and the browser equivalent) decodes
	// `content-encoding` transparently and leaves the header in place, so by the time the
	// body reaches here it is usually already plain text. Decoding unconditionally on the
	// header alone therefore fails ("incorrect header check") against a server that really
	// did compress. Decode only when the payload still carries the encoding: gzip announces
	// itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
	// already-decoded case falls back to the bytes as received.
	if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
		const { gunzipSync } = await import("zlib");
		bytes = new Uint8Array(gunzipSync(bytes));
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		try {
			bytes = new Uint8Array(brotliDecompressSync(bytes));
		} catch {
			// Already decoded by the runtime.
		}
	}

	const text = new TextDecoder().decode(bytes);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is internally tagged, so serde merges the variant payload's own fields as
	// siblings of the tag rather than nesting them: { format_type: 'image', format: 'PNG', ... }.
	// Read `format` off the record directly. Deliberately NO nested (`record.image`) fallback --
	// a binding that regresses to the pre-flatten shape must fail this assertion loudly instead
	// of silently degrading to the variant name below.
	if (formatType === "image" && typeof record.format === "string") {
		return record.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("plugin_api", () => {

	it("register_embedding_backend_trait_bridge: register_embedding_backend: trait bridge", async () => {
		class _TestStub_register_embedding_backend_trait_bridge {
  name(): string { return "test-embedding-backend"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  dimensions(): number { return 768; }
  async embed(_p0?: any): Promise<Array<Array<number>>> { return []; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerEmbeddingBackend(new _TestStub_register_embedding_backend_trait_bridge())).not.toThrow();

	}, 30000);

	it("register_ocr_backend_trait_bridge: register_ocr_backend: trait bridge", async () => {
		class _TestStub_register_ocr_backend_trait_bridge {
  name(): string { return "test-backend"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  async processImage(_p0?: any, _p1?: any): Promise<WasmExtractedDocument> { return "{}" as unknown as WasmExtractedDocument; }
  async processImageFile(_p0?: any, _p1?: any): Promise<WasmExtractedDocument> { return "{}" as unknown as WasmExtractedDocument; }
  supportsLanguage(_p0?: any): boolean { return false; }
  backendType(): WasmOcrBackendType { return "\"Tesseract\"" as unknown as WasmOcrBackendType; }
  supportedLanguages(): Array<string> { return []; }
  supportsTableDetection(): boolean { return false; }
  supportsDocumentProcessing(): boolean { return false; }
  emitsStructuredMarkdown(): boolean { return false; }
  confidenceSemantics(): WasmConfidenceSemantics { return "\"Legibility\"" as unknown as WasmConfidenceSemantics; }
  pageOrientationHandling(): WasmPageOrientationHandling { return "\"SelfCorrecting\"" as unknown as WasmPageOrientationHandling; }
  async processDocument(_p0?: any, _p1?: any): Promise<WasmExtractedDocument> { return "{}" as unknown as WasmExtractedDocument; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerOcrBackend(new _TestStub_register_ocr_backend_trait_bridge())).not.toThrow();

	}, 30000);

	it("register_post_processor_trait_bridge: register_post_processor: trait bridge", async () => {
		class _TestStub_register_post_processor_trait_bridge {
  name(): string { return "test-processor"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  async process(_p0?: any, _p1?: any): Promise<void> { return undefined; }
  processingStage(): WasmProcessingStage { return "\"Early\"" as unknown as WasmProcessingStage; }
  shouldProcess(_p0?: any, _p1?: any): boolean { return false; }
  estimatedDurationMs(_p0?: any): number { return 1; }
  priority(): number { return 1; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerPostProcessor(new _TestStub_register_post_processor_trait_bridge())).not.toThrow();

	}, 30000);

	it("register_reranker_backend_trait_bridge: register_reranker_backend: trait bridge", async () => {
		class _TestStub_register_reranker_backend_trait_bridge {
  name(): string { return "test-reranker-backend"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  async rerank(_p0?: any, _p1?: any): Promise<Array<number>> { return []; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerRerankerBackend(new _TestStub_register_reranker_backend_trait_bridge())).not.toThrow();

	}, 30000);

	it("register_tokenizer_backend_trait_bridge: register_tokenizer_backend: trait bridge", async () => {
		class _TestStub_register_tokenizer_backend_trait_bridge {
  name(): string { return "test-tokenizer-backend"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  countTokens(_p0?: any): number { return 3; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerTokenizerBackend(new _TestStub_register_tokenizer_backend_trait_bridge())).not.toThrow();

	}, 30000);

	it("register_validator_trait_bridge: register_validator: trait bridge", async () => {
		class _TestStub_register_validator_trait_bridge {
  name(): string { return "test-validator"; }
  version(): string { return "0.0.0"; }
  initialize(): void { return undefined; }
  shutdown(): void { return undefined; }
  async validate(_p0?: any, _p1?: any): Promise<void> { return undefined; }
  shouldValidate(_p0?: any, _p1?: any): boolean { return false; }
  priority(): number { return 1; }
  async dispose(): Promise<void> { return undefined; }
}

		expect(() => registerValidator(new _TestStub_register_validator_trait_bridge())).not.toThrow();

	}, 30000);

	it("unregister_embedding_backend_after_register: unregister_embedding_backend", async () => {
		expect(() => unregisterEmbeddingBackend("test-embedding-backend")).not.toThrow();

	}, 30000);

	it("unregister_post_processor_after_register: unregister_post_processor", async () => {
		expect(() => unregisterPostProcessor("test-processor")).not.toThrow();

	}, 30000);

	it("unregister_reranker_backend: unregister_reranker_backend", async () => {
		expect(() => unregisterRerankerBackend("test-reranker-backend")).not.toThrow();

	}, 30000);

	it("unregister_tokenizer_backend_after_register: unregister_tokenizer_backend", async () => {
		expect(() => unregisterTokenizerBackend("test-tokenizer-backend")).not.toThrow();

	}, 30000);

	it("unregister_validator_after_register: unregister_validator", async () => {
		expect(() => unregisterValidator("test-validator")).not.toThrow();

	}, 30000);

});
