// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8453d5913baf1577021ca11dc2add30aabe004a229974d6eeecb00e9cf6f68e5
// To regenerate: alef e2e generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { extract, extractBatch, WasmExtractInput, WasmExtractionConfig, WasmAccelerationConfig, WasmBedrockConfig, WasmBrowserConfig, WasmCaptioningConfig, WasmChunkClassificationConfig, WasmChunkClassificationDefinition, WasmChunkingConfig, WasmConcurrencyConfig, WasmContentConfig, WasmContentFilterConfig, WasmConversionOptions, WasmCrawlConfig, WasmCsvConfig, WasmEmailConfig, WasmEmbeddingConfig, WasmFileExtractionConfig, WasmGeoJsonExtractionConfig, WasmHierarchyConfig, WasmHtmlOutputConfig, WasmImageExtractionConfig, WasmImagePreprocessingConfig, WasmKeywordConfig, WasmLanguageDetectionConfig, WasmLateInteractionConfig, WasmLayoutDetectionConfig, WasmLlmBudgetConfig, WasmLlmCacheConfig, WasmLlmConfig, WasmLlmProviderConfig, WasmLlmRateLimitConfig, WasmNerConfig, WasmNgramRange, WasmOcrConfig, WasmOcrElementConfig, WasmOcrPipelineConfig, WasmOcrPipelineStage, WasmOcrQualityThresholds, WasmPageClassificationConfig, WasmPageConfig, WasmPdfConfig, WasmPostProcessorConfig, WasmPreprocessingOptions, WasmProxyConfig, WasmRakeParams, WasmRedactionConfig, WasmRedactionPattern, WasmRedactionTerm, WasmSecurityLimits, WasmSparseEmbeddingConfig, WasmSsrfPolicy, WasmStructuredExtractionConfig, WasmSummarizationConfig, WasmSvgOptions, WasmTesseractConfig, WasmTokenReductionOptions, WasmTranscriptionConfig, WasmTranslationConfig, WasmTreeSitterConfig, WasmTreeSitterProcessConfig, WasmUrlExtractionConfig, WasmYakeParams, WasmExtractInputKind } from "@xberg-io/xberg-wasm";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";
process.env.RUST_MIN_STACK ??= "16777216";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let bytes = new Uint8Array(await response.arrayBuffer());

	// The runtime's own fetch (undici on Node, and the browser equivalent) decodes
	// `content-encoding` transparently and leaves the header in place, so by the time the
	// body reaches here it is usually already plain text. Decoding unconditionally on the
	// header alone therefore fails ("incorrect header check") against a server that really
	// did compress. Decode only when the payload still carries the encoding: gzip announces
	// itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
	// already-decoded case falls back to the bytes as received.
	if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
		const { gunzipSync } = await import("zlib");
		bytes = new Uint8Array(gunzipSync(bytes));
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		try {
			bytes = new Uint8Array(brotliDecompressSync(bytes));
		} catch {
			// Already decoded by the runtime.
		}
	}

	const text = new TextDecoder().decode(bytes);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is internally tagged, so serde merges the variant payload's own fields as
	// siblings of the tag rather than nesting them: { format_type: 'image', format: 'PNG', ... }.
	// Read `format` off the record directly. Deliberately NO nested (`record.image`) fallback --
	// a binding that regresses to the pre-flatten shape must fail this assertion loudly instead
	// of silently degrading to the variant name below.
	if (formatType === "image" && typeof record.format === "string") {
		return record.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("batch", () => {

	it("extract_batch_bytes_happy: extract_batch: happy path with mixed inputs", async () => {
		const result = await extractBatch([await (async () => { const _u0 = WasmExtractInput.default(); _u0.bytes = Uint8Array.from([72, 101, 108, 108, 111, 44, 32, 119, 111, 114, 108, 100, 33]); _u0.kind = WasmExtractInputKind.Bytes; _u0.mimeType = "text/plain"; return _u0; })(), await (async () => { const _u0 = WasmExtractInput.default(); _u0.bytes = Uint8Array.from([60, 104, 116, 109, 108, 62, 60, 98, 111, 100, 121, 62, 84, 101, 115, 116, 60, 47, 98, 111, 100, 121, 62, 60, 47, 104, 116, 109, 108, 62]); _u0.kind = WasmExtractInputKind.Bytes; _u0.mimeType = "text/html"; return _u0; })()], undefined);
    expect(result.results.length).toBeGreaterThanOrEqual(1);

	}, 30000);

	it("extract_batch_bytes_invalid_mime: extract_batch with invalid bytes MIME type", async () => {
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.bytes = Uint8Array.from([72, 101, 108, 108, 111]); _u0.kind = WasmExtractInputKind.Bytes; _u0.mimeType = "application/x-nonexistent"; return _u0; })()], undefined);
    expect(result).toBeDefined();

	}, 30000);

	it("extract_batch_bytes_mixed_format: extract_batch: handles unsupported MIME gracefully", async () => {
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.bytes = Uint8Array.from([80, 68, 70, 32, 112, 108, 97, 99, 101, 104, 111, 108, 100, 101, 114]); _u0.kind = WasmExtractInputKind.Bytes; _u0.mimeType = "application/x-unknown"; return _u0; })()], undefined);
    expect(result).toBeDefined();

	}, 30000);

	it("extract_batch_bytes_unsupported_mime: extract_batch with unsupported bytes MIME type", async () => {
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.bytes = Uint8Array.from([100, 97, 116, 97]); _u0.kind = WasmExtractInputKind.Bytes; _u0.mimeType = "application/x-unknown"; return _u0; })()], undefined);
    expect(result).toBeDefined();

	}, 30000);

	it("extract_batch_empty_inputs: extract_batch: empty batch", async () => {
		const result = await extractBatch([], undefined);
    expect(result.results.length).toBe(0);

	}, 30000);

	it("extract_batch_uri_all_missing: extract_batch with missing URI inputs", async () => {
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = "/nonexistent/a.pdf"; return _u0; })(), (() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = "/nonexistent/b.txt"; return _u0; })()], undefined);
    expect(Number(result.summary.results)).toBe(0);
    expect(Number(result.summary.errors)).toBe(2);

	}, 30000);

	it("extract_batch_uri_basic: extract_batch over URI inputs", async () => {
		const inputsMockBaseUrl = process.env.MOCK_SERVER_EXTRACT_BATCH_URI_BASIC ?? `${process.env.MOCK_SERVER_URL}/fixtures/extract_batch_uri_basic`;
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = `${inputsMockBaseUrl}/pdf/fake_memo.pdf`; return _u0; })(), (() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = `${inputsMockBaseUrl}/text/fake_text.txt`; return _u0; })()], undefined);
    expect(result.results.length).toBeGreaterThanOrEqual(2);

	}, 30000);

	it("extract_batch_uri_not_found: extract_batch with missing URI input", async () => {
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = "/nonexistent/a.pdf"; return _u0; })()], undefined);
    expect(Number(result.summary.results)).toBe(0);
    expect(Number(result.summary.errors)).toBe(1);

	}, 30000);

	it("extract_batch_uri_partial_failure: extract_batch with one valid URI input and one that downloads to an unparseable document", async () => {
		const inputsMockBaseUrl = process.env.MOCK_SERVER_EXTRACT_BATCH_URI_PARTIAL_FAILURE ?? `${process.env.MOCK_SERVER_URL}/fixtures/extract_batch_uri_partial_failure`;
		const result = await extractBatch([(() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = `${inputsMockBaseUrl}/text/plain.txt`; return _u0; })(), (() => { const _u0 = WasmExtractInput.default(); _u0.kind = WasmExtractInputKind.Uri; _u0.uri = `${inputsMockBaseUrl}/pdf/corrupt_truncated.pdf`; return _u0; })()], undefined);
    expect(Number(result.summary.results)).toBe(1);
    expect(Number(result.summary.errors)).toBe(1);

	}, 30000);

});
