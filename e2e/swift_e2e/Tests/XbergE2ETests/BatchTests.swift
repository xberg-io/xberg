// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: batch.
final class BatchTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testExtractBatchBytesHappy() async throws {
        // extract_batch: happy path with mixed inputs
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"bytes\":[72,101,108,108,111,44,32,119,111,114,108,100,33],\"kind\":\"bytes\",\"mime_type\":\"text/plain\"}")
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"bytes\":[60,104,116,109,108,62,60,98,111,100,121,62,84,101,115,116,60,47,98,111,100,121,62,60,47,104,116,109,108,62],\"kind\":\"bytes\",\"mime_type\":\"text/html\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
        XCTAssertGreaterThanOrEqual(result.results().count, 1)
    }

    func testExtractBatchBytesInvalidMime() async throws {
        // extract_batch with invalid bytes MIME type
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"bytes\":[72,101,108,108,111],\"kind\":\"bytes\",\"mime_type\":\"application/x-nonexistent\"}")
        let inputsArray = [_item_inputsArray_0]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchBytesMixedFormat() async throws {
        // extract_batch: handles unsupported MIME gracefully
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"bytes\":[80,68,70,32,112,108,97,99,101,104,111,108,100,101,114],\"kind\":\"bytes\",\"mime_type\":\"application/x-unknown\"}")
        let inputsArray = [_item_inputsArray_0]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchBytesUnsupportedMime() async throws {
        // extract_batch with unsupported bytes MIME type
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"bytes\":[100,97,116,97],\"kind\":\"bytes\",\"mime_type\":\"application/x-unknown\"}")
        let inputsArray = [_item_inputsArray_0]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchEmptyInputs() async throws {
        // extract_batch: empty batch
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: [], config: configObj)
        XCTAssertEqual(result.results().count, 0)
    }

    func testExtractBatchUriAllMissing() async throws {
        // extract_batch with missing URI inputs
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"/nonexistent/a.pdf\"}")
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"/nonexistent/b.txt\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchUriBasic() async throws {
        // extract_batch over URI inputs
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"pdf/fake_memo.pdf\"}")
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"text/fake_text.txt\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchUriNotFound() async throws {
        // extract_batch with missing URI input
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"/nonexistent/a.pdf\"}")
        let inputsArray = [_item_inputsArray_0]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

    func testExtractBatchUriPartialFailure() async throws {
        // extract_batch with mixed valid and missing URI inputs
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"text/plain.txt\"}")
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"/nonexistent/missing.pdf\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
    }

}
