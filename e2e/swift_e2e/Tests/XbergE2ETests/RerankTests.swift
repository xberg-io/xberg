// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: rerank.
final class RerankTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testRerankBasic() async throws {
        // rerank: plugin model with no backend registered returns an error
        do {
            let configObj = try Xberg.rerankerConfigFromJson("{\"model\":{\"name\":\"rerank_basic\",\"type\":\"plugin\"}}")
            _ = try await Xberg.rerankAsync(query: "What is machine learning?", documents: ["Machine learning is a subset of artificial intelligence focused on learning from data.", "Cats are independent animals that prefer to play alone.", "Deep learning uses neural networks with multiple layers."], config: configObj)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testRerankEmptyDocuments() async throws {
        // rerank: empty documents list returns empty result
        let configObj = try Xberg.rerankerConfigFromJson("{\"model\":{\"name\":\"rerank_empty_documents\",\"type\":\"plugin\"}}")
        let result = try await Xberg.rerankAsync(query: "test query", documents: [], config: configObj)
    }

    func testRerankTopK() async throws {
        // rerank: plugin model with no backend registered returns an error (top_k variant)
        do {
            let configObj = try Xberg.rerankerConfigFromJson("{\"model\":{\"name\":\"rerank_top_k\",\"type\":\"plugin\"},\"top_k\":2}")
            _ = try await Xberg.rerankAsync(query: "machine learning", documents: ["Machine learning is a subset of artificial intelligence focused on learning from data.", "Deep learning uses neural networks with multiple layers.", "Classical statistics predates modern machine learning.", "Photosynthesis is the process by which plants convert sunlight to energy."], config: configObj)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
