// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: format_specific.
final class FormatSpecificTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testFormatDocxStandalone() async throws {
        // Standalone DOCX extraction using extract
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_FORMAT_DOCX_STANDALONE"] ?? (AlefE2EMockServer.baseURL + "/fixtures/format_docx_standalone")
        let inputJson = "{\"filename\":\"fake.docx\",\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\",\"uri\":\"$mock_url/docx/fake.docx\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{}")
        let _vec_results_min_length_b58eca1f = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_b58eca1f[0].content().toString().count, 20)
    }

    func testFormatHwpxStandalone() async throws {
        // Standalone HWPX extraction using extract
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_FORMAT_HWPX_STANDALONE"] ?? (AlefE2EMockServer.baseURL + "/fixtures/format_hwpx_standalone")
        let inputJson = "{\"filename\":\"simple.hwpx\",\"kind\":\"uri\",\"mime_type\":\"application/haansofthwpx\",\"uri\":\"$mock_url/hwpx/simple.hwpx\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{}")
        let _vec_results_min_length_b58eca1f = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_b58eca1f[0].content().toString().count, 20)
        let _vec_results_contains_21c9f1de = result.results()
        XCTAssertTrue("Hello from HWPX".isEmpty || _vec_results_contains_21c9f1de[0].content().toString().contains("Hello from HWPX"), "expected to contain: \("Hello from HWPX")")
    }

    func testFormatPdfText() async throws {
        // Standalone PDF text extraction using extract
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_FORMAT_PDF_TEXT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/format_pdf_text")
        let inputJson = "{\"filename\":\"fake_memo.pdf\",\"kind\":\"uri\",\"mime_type\":\"application/pdf\",\"uri\":\"$mock_url/pdf/fake_memo.pdf\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{}")
        let _vec_results_min_length_a6305d90 = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_a6305d90[0].content().toString().count, 50)
        let _vec_results_contains_any_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_any_a78ad6d8[0].content().toString().contains("Mallori") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("May"), "expected to contain at least one of the specified values")
    }

    func testFormatPptx() async throws {
        // PPTX presentation extraction using extract
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_FORMAT_PPTX"] ?? (AlefE2EMockServer.baseURL + "/fixtures/format_pptx")
        let inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"uri\":\"$mock_url/pptx/simple.pptx\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{}")
    }

    func testFormatXlsx() async throws {
        // XLSX spreadsheet extraction using extract
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_FORMAT_XLSX"] ?? (AlefE2EMockServer.baseURL + "/fixtures/format_xlsx")
        let inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"uri\":\"$mock_url/xlsx/stanley_cups.xlsx\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{}")
    }

}
