// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ccb3e5f38c10b1a4c67c484851d88ff80d4d1c22a63d7f46743cd3ded86b81d0
// To regenerate: alef e2e generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: ocr_backend_management.
final class OcrBackendManagementTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        _ = "16777216".withCString { val in
            "RUST_MIN_STACK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testOcrBackendCapabilitiesList() throws {
        // List all registered OCR backends with their declared language capabilities
        XCTAssertNoThrow(try Xberg.listOcrBackendCapabilities())
    }

    func testOcrBackendSupportsLanguageUnknownBackend() throws {
        // Checking language support on an unregistered OCR backend is an error
        do {
            _ = try Xberg.ocrBackendSupportsLanguage(backend: "nonexistent-backend-xyz", language: "eng")
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testOcrBackendsClear() throws {
        // Clear all OCR backends and verify list is empty
        XCTAssertNoThrow(try Xberg.clearOcrBackends())
    }

    func testOcrBackendsList() throws {
        // List all registered OCR backends
        XCTAssertNoThrow(try Xberg.listOcrBackends())
    }

    func testOcrBackendsUnregister() throws {
        // Unregister nonexistent OCR backend gracefully
        XCTAssertNoThrow(try Xberg.unregisterOcrBackend(name: "nonexistent-backend-xyz"))
    }

}
