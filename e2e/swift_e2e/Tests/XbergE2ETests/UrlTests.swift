// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: url.
final class UrlTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testUrlBatchMixedInputs() async throws {
        // extract_batch: mixed bytes and URL inputs share one output envelope
        let inputsMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_URL_BATCH_MIXED_INPUTS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/url_batch_mixed_inputs")
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replacingOccurrences(of: "$mock_url", with: inputsMockBaseUrl))
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"bytes\":[66,97,116,99,104,32,98,121,116,101,115,32,99,111,110,116,101,110,116],\"filename\":\"inline.txt\",\"kind\":\"bytes\",\"mime_type\":\"text/plain\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{\"url\":{\"mode\":\"document\"}}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
        XCTAssertGreaterThanOrEqual(result.results().count, 2)
        let _vec_results_contains_205d6d29 = result.results()
        XCTAssertTrue("Batch URL document content".isEmpty || _vec_results_contains_205d6d29[0].content().toString().contains("Batch URL document content"), "expected to contain: \("Batch URL document content")")
        let _vec_results_contains_dd4476a2 = result.results()
        XCTAssertTrue("Batch bytes content".isEmpty || _vec_results_contains_dd4476a2[1].content().toString().contains("Batch bytes content"), "expected to contain: \("Batch bytes content")")
    }

    func testUrlCrawlLinkedPages() async throws {
        // extract: crawl mode follows linked pages
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_URL_CRAWL_LINKED_PAGES"] ?? (AlefE2EMockServer.baseURL + "/fixtures/url_crawl_linked_pages")
        let inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{\"url\":{\"crawl\":{\"max_depth\":1,\"max_pages\":4,\"respect_robots_txt\":false},\"mode\":\"crawl\"}}")
        XCTAssertGreaterThanOrEqual(result.summary().pagesCrawled(), 2)
        let _vec_results_contains_f26a637 = result.results()
        XCTAssertTrue("About crawl target".isEmpty || _vec_results_contains_f26a637[1].content().toString().contains("About crawl target"), "expected to contain: \("About crawl target")")
    }

    func testUrlHtmlPageExtract() async throws {
        // extract: website URL returns page content
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_URL_HTML_PAGE_EXTRACT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/url_html_page_extract")
        let inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{\"url\":{\"mode\":\"document\"}}")
        let _vec_results_contains_a5fc63e7 = result.results()
        XCTAssertTrue("Xberg URL Page".isEmpty || _vec_results_contains_a5fc63e7[0].content().toString().contains("Xberg URL Page"), "expected to contain: \("Xberg URL Page")")
        XCTAssertGreaterThanOrEqual(result.results().count, 1)
    }

    func testUrlRecursiveDocumentUrls() async throws {
        // extract: recursive URL extraction follows document links discovered in results
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_URL_RECURSIVE_DOCUMENT_URLS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/url_recursive_document_urls")
        let inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{\"url\":{\"crawl\":{\"document_url_depth\":1,\"follow_document_urls\":true,\"respect_robots_txt\":false},\"mode\":\"document\"}}")
        XCTAssertGreaterThanOrEqual(result.results().count, 2)
        let _vec_results_contains_ae0e8422 = result.results()
        XCTAssertTrue("Recursive document target".isEmpty || _vec_results_contains_ae0e8422[1].content().toString().contains("Recursive document target"), "expected to contain: \("Recursive document target")")
    }

    func testUrlRemoteTextDocument() async throws {
        // extract: remote text document URL
        let inputMockBaseUrl = ProcessInfo.processInfo.environment["MOCK_SERVER_URL_REMOTE_TEXT_DOCUMENT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/url_remote_text_document")
        let inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replacingOccurrences(of: "$mock_url", with: inputMockBaseUrl)
        let result = try await Xberg.extract(inputJson, "{\"url\":{\"mode\":\"document\"}}")
        let _vec_results_contains_e75bc892 = result.results()
        XCTAssertTrue("Remote document hello".isEmpty || _vec_results_contains_e75bc892[0].content().toString().contains("Remote document hello"), "expected to contain: \("Remote document hello")")
        XCTAssertEqual(result.summary().remoteUrls(), 1)
    }

}
