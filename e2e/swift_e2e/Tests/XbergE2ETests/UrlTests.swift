// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:446748155774867df28fecd60b1f51d93e20d7476cf9f2aad1cd588a1cf0950e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: url.
final class UrlTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testUrlBatchMixedInputs() async throws {
        // extract_batch: mixed bytes and URL inputs share one output envelope
        let _item_inputsArray_0 = try Xberg.extractInputFromJson("{\"kind\":\"uri\",\"uri\":\"$mock_url\"}")
        let _item_inputsArray_1 = try Xberg.extractInputFromJson("{\"bytes\":[66,97,116,99,104,32,98,121,116,101,115,32,99,111,110,116,101,110,116],\"filename\":\"inline.txt\",\"kind\":\"bytes\",\"mime_type\":\"text/plain\"}")
        let inputsArray = [_item_inputsArray_0, _item_inputsArray_1]
        let configObj = try Xberg.extractionConfigFromJson("{\"url\":{\"mode\":\"document\"}}")
        let result = try await Xberg.extractBatch(inputs: inputsArray, config: configObj)
        XCTAssertGreaterThanOrEqual(result.summary()?.results().count ?? 0, 2)
        // skipped: field 'results.0.content' not available on result type
        // skipped: field 'results.1.content' not available on result type
    }

    func testUrlCrawlLinkedPages() async throws {
        // extract: crawl mode follows linked pages
        let result = try await Xberg.extract([], "{\"url\":{\"crawl\":{\"max_depth\":1,\"max_pages\":4,\"respect_robots_txt\":false},\"mode\":\"crawl\"}}")
        XCTAssertGreaterThanOrEqual(result.summary()?.pagesCrawled().count ?? 0, 2)
        // skipped: field 'results.1.content' not available on result type
    }

    func testUrlHtmlPageExtract() async throws {
        // extract: website URL returns page content
        let result = try await Xberg.extract([], "{\"url\":{\"mode\":\"document\"}}")
        // skipped: field 'results.0.content' not available on result type
        XCTAssertGreaterThanOrEqual(result.summary()?.results().count ?? 0, 1)
    }

    func testUrlRecursiveDocumentUrls() async throws {
        // extract: recursive URL extraction follows document links discovered in results
        let result = try await Xberg.extract([], "{\"url\":{\"crawl\":{\"document_url_depth\":1,\"follow_document_urls\":true,\"respect_robots_txt\":false},\"mode\":\"document\"}}")
        XCTAssertGreaterThanOrEqual(result.summary()?.results().count ?? 0, 2)
        // skipped: field 'results.1.content' not available on result type
    }

    func testUrlRemoteTextDocument() async throws {
        // extract: remote text document URL
        let result = try await Xberg.extract([], "{\"url\":{\"mode\":\"document\"}}")
        // skipped: field 'results.0.content' not available on result type
        XCTAssertEqual(result.summary()?.remoteUrls(), 1)
    }

}
