// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2cb66c78f7aec60e31fcc9ad98498dfde4422c5c0abbbe0bf53a080f554b12cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Xberg
import RustBridge

/// E2e tests for category: smoke.
final class SmokeTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testOcrImagePng() async throws {
        // OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_684a8d17 = result.results()
        XCTAssertEqual(_vec_results_equals_684a8d17[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "image/png")
        let _vec_results_min_length_ddc57b0e = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_ddc57b0e[0].content().toString().count, 1)
        let _vec_results_contains_any_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_any_a78ad6d8[0].content().toString().contains("Hello") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("World") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("hello") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("world"), "expected to contain at least one of the specified values")
    }

    func testSmokeDocxBasic() async throws {
        // Smoke test: DOCX with formatted text
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_6dd90372 = result.results()
        XCTAssertEqual(_vec_results_equals_6dd90372[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        let _vec_results_min_length_b58eca1f = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_b58eca1f[0].content().toString().count, 20)
        let _vec_results_contains_any_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_any_a78ad6d8[0].content().toString().contains("Lorem") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("ipsum") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("document") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("text"), "expected to contain at least one of the specified values")
    }

    func testSmokeHtmlBasic() async throws {
        // Smoke test: HTML table extraction
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_e181f185 = result.results()
        XCTAssertEqual(_vec_results_equals_e181f185[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "text/html")
        let _vec_results_min_length_a5fd1ce0 = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_a5fd1ce0[0].content().toString().count, 10)
        let _vec_results_contains_any_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_any_a78ad6d8[0].content().toString().contains("Sample Data Table") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("Laptop") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("Electronics") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("Product"), "expected to contain at least one of the specified values")
    }

    func testSmokeImagePng() async throws {
        // Smoke test: PNG image (without OCR, metadata only)
        let result = try await Xberg.extract([], "{\"disable_ocr\":true}")
        let _vec_results_equals_684a8d17 = result.results()
        XCTAssertEqual(_vec_results_equals_684a8d17[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "image/png")
    }

    func testSmokeJsonBasic() async throws {
        // Smoke test: JSON file extraction
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_ad63b623 = result.results()
        XCTAssertEqual(_vec_results_equals_ad63b623[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/json")
        let _vec_results_min_length_d62a0624 = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_d62a0624[0].content().toString().count, 5)
    }

    func testSmokePdfBasic() async throws {
        // Smoke test: PDF with simple text extraction
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_eb9b1cd4 = result.results()
        XCTAssertEqual(_vec_results_equals_eb9b1cd4[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/pdf")
        let _vec_results_min_length_a6305d90 = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_a6305d90[0].content().toString().count, 50)
        let _vec_results_contains_any_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_any_a78ad6d8[0].content().toString().contains("May 5, 2023") || _vec_results_contains_any_a78ad6d8[0].content().toString().contains("To Whom it May Concern"), "expected to contain at least one of the specified values")
    }

    func testSmokeTxtBasic() async throws {
        // Smoke test: Plain text file
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_3421907d = result.results()
        XCTAssertEqual(_vec_results_equals_3421907d[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "text/plain")
        let _vec_results_min_length_d62a0624 = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_d62a0624[0].content().toString().count, 5)
    }

    func testSmokeXlsxBasic() async throws {
        // Smoke test: XLSX with basic spreadsheet data including tables
        let result = try await Xberg.extract([], "{}")
        let _vec_results_equals_5cba21b6 = result.results()
        XCTAssertEqual(_vec_results_equals_5cba21b6[0].mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        let _vec_results_min_length_2aae031d = result.results()
        XCTAssertGreaterThanOrEqual(_vec_results_min_length_2aae031d[0].content().toString().count, 100)
        let _vec_results_contains_all_a78ad6d8 = result.results()
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Team"), "expected to contain: \("Team")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Location"), "expected to contain: \("Location")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Stanley Cups"), "expected to contain: \("Stanley Cups")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Blues"), "expected to contain: \("Blues")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Flyers"), "expected to contain: \("Flyers")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("Maple Leafs"), "expected to contain: \("Maple Leafs")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("STL"), "expected to contain: \("STL")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("PHI"), "expected to contain: \("PHI")")
        XCTAssertTrue(_vec_results_contains_all_a78ad6d8[0].content().toString().contains("TOR"), "expected to contain: \("TOR")")
        let _vec_results_count_min_70305a5a = result.results()
        XCTAssertGreaterThanOrEqual(result.results()[0].tables().count, 1)
        // skipped: field 'results[0].metadata.format.excel.sheet_count' crosses a tagged-union variant boundary (not expressible in Swift)
        // skipped: field 'results[0].metadata.format.excel.sheet_names' crosses a tagged-union variant boundary (not expressible in Swift)
    }

}
