// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:decbacb38ca392b0d8e278b7044150d6b3cb3a645f79702094e10190b73e260b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzberg
import RustBridge

/// E2e tests for category: smoke.
final class SmokeTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
        let _testDocs = URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent() // <Module>Tests/
            .deletingLastPathComponent() // Tests/
            .deletingLastPathComponent() // swift_e2e/
            .deletingLastPathComponent() // e2e/
            .deletingLastPathComponent() // <repo root>
            .appendingPathComponent("test_documents")
        if FileManager.default.fileExists(atPath: _testDocs.path) {
            FileManager.default.changeCurrentDirectoryPath(_testDocs.path)
        }
    }

    func testOcrImagePng() async throws {
        // OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.
        let contentData = try Data(contentsOf: URL(fileURLWithPath: "images/test_hello_world.png"))
        let contentBytes = Array(contentData)
        let result = try await Kreuzberg.extractBytes(contentBytes, "image/png", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "image/png")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 1)
        XCTAssertTrue(result.content().toString().contains("Hello") || result.content().toString().contains("World") || result.content().toString().contains("hello") || result.content().toString().contains("world"), "expected to contain at least one of the specified values")
    }

    func testSmokeDocxBasic() async throws {
        // Smoke test: DOCX with formatted text
        let result = try await Kreuzberg.extractFile("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 20)
        XCTAssertTrue(result.content().toString().contains("Lorem") || result.content().toString().contains("ipsum") || result.content().toString().contains("document") || result.content().toString().contains("text"), "expected to contain at least one of the specified values")
    }

    func testSmokeHtmlBasic() async throws {
        // Smoke test: HTML table extraction
        let result = try await Kreuzberg.extractFile("html/simple_table.html", "text/html", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "text/html")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 10)
        XCTAssertTrue(result.content().toString().contains("Sample Data Table") || result.content().toString().contains("Laptop") || result.content().toString().contains("Electronics") || result.content().toString().contains("Product"), "expected to contain at least one of the specified values")
    }

    func testSmokeImagePng() async throws {
        // Smoke test: PNG image (without OCR, metadata only)
        let result = try await Kreuzberg.extractFile("images/sample.png", nil, "{\"disable_ocr\":true}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "image/png")
    }

    func testSmokeJsonBasic() async throws {
        // Smoke test: JSON file extraction
        let result = try await Kreuzberg.extractFile("json/simple.json", "application/json", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/json")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 5)
    }

    func testSmokePdfBasic() async throws {
        // Smoke test: PDF with simple text extraction
        let result = try await Kreuzberg.extractFile("pdf/fake_memo.pdf", "application/pdf", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/pdf")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 50)
        XCTAssertTrue(result.content().toString().contains("May 5, 2023") || result.content().toString().contains("To Whom it May Concern"), "expected to contain at least one of the specified values")
    }

    func testSmokeTxtBasic() async throws {
        // Smoke test: Plain text file
        let result = try await Kreuzberg.extractFile("text/report.txt", "text/plain", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "text/plain")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 5)
    }

    func testSmokeXlsxBasic() async throws {
        // Smoke test: XLSX with basic spreadsheet data including tables
        let result = try await Kreuzberg.extractFile("xlsx/stanley_cups.xlsx", "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "{}")
        XCTAssertEqual(result.mimeType().toString().trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
        XCTAssertGreaterThanOrEqual(result.content().toString().count, 100)
        XCTAssertTrue(result.content().toString().contains("Team"), "expected to contain: \("Team")")
        XCTAssertTrue(result.content().toString().contains("Location"), "expected to contain: \("Location")")
        XCTAssertTrue(result.content().toString().contains("Stanley Cups"), "expected to contain: \("Stanley Cups")")
        XCTAssertTrue(result.content().toString().contains("Blues"), "expected to contain: \("Blues")")
        XCTAssertTrue(result.content().toString().contains("Flyers"), "expected to contain: \("Flyers")")
        XCTAssertTrue(result.content().toString().contains("Maple Leafs"), "expected to contain: \("Maple Leafs")")
        XCTAssertTrue(result.content().toString().contains("STL"), "expected to contain: \("STL")")
        XCTAssertTrue(result.content().toString().contains("PHI"), "expected to contain: \("PHI")")
        XCTAssertTrue(result.content().toString().contains("TOR"), "expected to contain: \("TOR")")
        // skipped: field 'tables' not available on result type
        // skipped: field 'metadata.format.excel.sheet_count' not available on result type
        // skipped: field 'metadata.format.excel.sheet_names' not available on result type
    }

}
