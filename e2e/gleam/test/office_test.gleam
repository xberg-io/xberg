// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c489070d7a368590eff3e24bed354f778026a276edbab5da9bd7ceb33e53ba13
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import kreuzberg

import gleam/int
import gleam/list
import gleam/string

// BibTeX bibliography extraction.
pub fn office_bibtex_basic_test() {
  let result = kreuzberg.extract_file("bibtex/comprehensive.bib")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-bibtex") |> should.equal(True)
  result.mime_type |> string.contains("text/x-bibtex") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// CommonMark (.commonmark) text extraction.
pub fn office_commonmark_basic_test() {
  let result = kreuzberg.extract_file("markdown/sample.commonmark")
  result |> should.be_ok()
  result.mime_type |> string.contains("text/markdown") |> should.equal(True)
  result.mime_type |> string.contains("text/plain") |> should.equal(True)
  result.mime_type |> string.contains("text/x-commonmark") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Djot markup text extraction.
pub fn office_djot_basic_test() {
  let result = kreuzberg.extract_file("markdown/tables.djot")
  result |> should.be_ok()
  result.mime_type |> string.contains("text/x-djot") |> should.equal(True)
  result.mime_type |> string.contains("text/djot") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// DocBook XML document extraction.
pub fn office_docbook_basic_test() {
  let result = kreuzberg.extract_file("docbook/docbook-reader.docbook")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/docbook+xml") |> should.equal(True)
  result.mime_type |> string.contains("text/docbook") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// DOCX document extraction baseline.
pub fn office_docx_basic_test() {
  let result = kreuzberg.extract_file("docx/sample_document.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// DOCX file containing equations to validate math extraction.
pub fn office_docx_equations_test() {
  let result = kreuzberg.extract_file("docx/equations.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// Simple DOCX document to verify baseline extraction.
pub fn office_docx_fake_test() {
  let result = kreuzberg.extract_file("docx/fake.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// DOCX document heavy on formatting for style preservation.
pub fn office_docx_formatting_test() {
  let result = kreuzberg.extract_file("docx/unit_test_formatting.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// DOCX document with complex headers.
pub fn office_docx_headers_test() {
  let result = kreuzberg.extract_file("docx/unit_test_headers.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// DOCX document emphasizing list formatting.
pub fn office_docx_lists_test() {
  let result = kreuzberg.extract_file("docx/unit_test_lists.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// DOCX document containing tables for table-aware extraction.
pub fn office_docx_tables_test() {
  let result = kreuzberg.extract_file("docx/docx_tables.docx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.wordprocessingml.document")
  result.content |> string.length |> int.is_at_least(50) |> should.equal(True)
  result.content |> string.contains("Simple uniform table") |> should.equal(True)
  result.content |> string.contains("Nested Table") |> should.equal(True)
  result.content |> string.contains("merged cells") |> should.equal(True)
  result.content |> string.contains("Header Col") |> should.equal(True)
  result.tables |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// EPUB book extraction with text content.
pub fn office_epub_basic_test() {
  let result = kreuzberg.extract_file("epub/features.epub")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/epub+zip")
  result.content |> string.length |> int.is_at_least(50) |> should.equal(True)
}

// FictionBook (FB2) document extraction baseline.
pub fn office_fb2_basic_test() {
  let result = kreuzberg.extract_file("fictionbook/basic.fb2")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-fictionbook+xml")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// FictionBook (.fb2) text extraction.
pub fn office_fictionbook_basic_test() {
  let result = kreuzberg.extract_file("fictionbook/basic.fb2")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-fictionbook+xml") |> should.equal(True)
  result.mime_type |> string.contains("application/x-fictionbook") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// JATS scientific article extraction.
pub fn office_jats_basic_test() {
  let result = kreuzberg.extract_file("jats/sample_article.jats")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-jats+xml") |> should.equal(True)
  result.mime_type |> string.contains("text/jats") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Jupyter notebook extraction.
pub fn office_jupyter_basic_test() {
  let result = kreuzberg.extract_file("jupyter/rank.ipynb")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-ipynb+json")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Keynote document extraction baseline.
pub fn office_keynote_basic_test() {
  let result = kreuzberg.extract_file("iwork/test.key")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-iwork-keynote-sffkey")
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// LaTeX document text extraction.
pub fn office_latex_basic_test() {
  let result = kreuzberg.extract_file("latex/basic_sections.tex")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-latex") |> should.equal(True)
  result.mime_type |> string.contains("text/x-latex") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// Markdown document extraction baseline.
pub fn office_markdown_basic_test() {
  let result = kreuzberg.extract_file("markdown/comprehensive.md")
  result |> should.be_ok()
  result.mime_type |> should.equal("text/markdown")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Numbers document extraction baseline.
pub fn office_numbers_basic_test() {
  let result = kreuzberg.extract_file("iwork/test.numbers")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-iwork-numbers-sffnumbers")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Basic ODS spreadsheet extraction.
pub fn office_ods_basic_test() {
  let result = kreuzberg.extract_file("data_formats/test_01.ods")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.oasis.opendocument.spreadsheet")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// ODT document with bold formatting.
pub fn office_odt_bold_test() {
  let result = kreuzberg.extract_file("odt/bold.odt")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.oasis.opendocument.text")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// ODT document containing unordered lists with nesting.
pub fn office_odt_list_test() {
  let result = kreuzberg.extract_file("odt/unorderedList.odt")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.oasis.opendocument.text")
  result.content |> string.length |> int.is_at_least(30) |> should.equal(True)
  result.content |> string.contains("list item") |> should.equal(True)
  result.content |> string.contains("New level") |> should.equal(True)
  result.content |> string.contains("Pushed us") |> should.equal(True)
}

// Basic ODT document with paragraphs and headings.
pub fn office_odt_simple_test() {
  let result = kreuzberg.extract_file("odt/simple.odt")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.oasis.opendocument.text")
  result.content |> string.length |> int.is_at_least(50) |> should.equal(True)
  result.content |> string.contains("favorite things") |> should.equal(True)
  result.content |> string.contains("Parrots") |> should.equal(True)
  result.content |> string.contains("Analysis") |> should.equal(True)
}

// ODT document with a table structure.
pub fn office_odt_table_test() {
  let result = kreuzberg.extract_file("odt/table.odt")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.oasis.opendocument.text")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
  result.tables |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// OPML outline document extraction.
pub fn office_opml_basic_test() {
  let result = kreuzberg.extract_file("opml/outline.opml")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/xml+opml") |> should.equal(True)
  result.mime_type |> string.contains("text/x-opml") |> should.equal(True)
  result.mime_type |> string.contains("application/x-opml+xml") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Org-mode document text extraction.
pub fn office_org_basic_test() {
  let result = kreuzberg.extract_file("org/comprehensive.org")
  result |> should.be_ok()
  result.mime_type |> string.contains("text/x-org") |> should.equal(True)
  result.mime_type |> string.contains("text/org") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// Pages document extraction baseline.
pub fn office_pages_basic_test() {
  let result = kreuzberg.extract_file("iwork/test.pages")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-iwork-pages-sffpages")
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
pub fn office_ppsx_slideshow_test() {
  let result = kreuzberg.extract_file("pptx/sample.ppsx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.presentationml.slideshow")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// PowerPoint macro-enabled presentation (.pptm) extraction.
pub fn office_pptm_basic_test() {
  let result = kreuzberg.extract_file("pptx/powerpoint_with_image.pptm")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/vnd.ms-powerpoint.presentation.macroEnabled.12") |> should.equal(True)
  result.mime_type |> string.contains("application/vnd.openxmlformats-officedocument.presentationml.presentation") |> should.equal(True)
  result.content |> list.is_empty |> should.equal(False)
}

// PPTX deck should extract slides content.
pub fn office_pptx_basic_test() {
  let result = kreuzberg.extract_file("pptx/simple.pptx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.presentationml.presentation")
  result.content |> string.length |> int.is_at_least(50) |> should.equal(True)
}

// PPTX presentation containing images to ensure metadata extraction.
pub fn office_pptx_images_test() {
  let result = kreuzberg.extract_file("pptx/powerpoint_with_image.pptx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.presentationml.presentation")
  result.content |> string.length |> int.is_at_least(15) |> should.equal(True)
}

// Pitch deck PPTX used to validate large slide extraction.
pub fn office_pptx_pitch_deck_test() {
  let result = kreuzberg.extract_file("pptx/pitch_deck_presentation.pptx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.presentationml.presentation")
  result.content |> string.length |> int.is_at_least(100) |> should.equal(True)
}

// reStructuredText document extraction.
pub fn office_rst_basic_test() {
  let result = kreuzberg.extract_file("rst/restructured_text.rst")
  result |> should.be_ok()
  result.mime_type |> string.contains("text/x-rst") |> should.equal(True)
  result.mime_type |> string.contains("text/prs.fallenstein.rst") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// RTF document text extraction.
pub fn office_rtf_basic_test() {
  let result = kreuzberg.extract_file("rtf/extraction_test.rtf")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/rtf") |> should.equal(True)
  result.mime_type |> string.contains("text/rtf") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Typst document text extraction.
pub fn office_typst_basic_test() {
  let result = kreuzberg.extract_file("typst/headings.typ")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-typst") |> should.equal(True)
  result.mime_type |> string.contains("text/x-typst") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Legacy XLS spreadsheet to ensure backward compatibility.
pub fn office_xls_legacy_test() {
  let result = kreuzberg.extract_file("xls/test_excel.xls")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.ms-excel")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Excel binary workbook (.xlsb) extraction.
pub fn office_xlsb_basic_test() {
  let result = kreuzberg.extract_file("xlsx/test_xlsb.xlsb")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/vnd.ms-excel.sheet.binary.macroEnabled.12") |> should.equal(True)
  result.mime_type |> string.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet") |> should.equal(True)
  result.content |> list.is_empty |> should.equal(False)
}

// Excel macro-enabled workbook (.xlsm) extraction.
pub fn office_xlsm_basic_test() {
  let result = kreuzberg.extract_file("xlsx/test_01.xlsm")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/vnd.ms-excel.sheet.macroEnabled.12") |> should.equal(True)
  result.mime_type |> string.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet") |> should.equal(True)
  result.content |> list.is_empty |> should.equal(False)
}

// XLSX spreadsheet should produce metadata and table content.
pub fn office_xlsx_basic_test() {
  let result = kreuzberg.extract_file("xlsx/stanley_cups.xlsx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  result.content |> string.length |> int.is_at_least(100) |> should.equal(True)
  result.content |> string.contains("Team") |> should.equal(True)
  result.content |> string.contains("Location") |> should.equal(True)
  result.content |> string.contains("Stanley Cups") |> should.equal(True)
  result.tables |> list.length |> int.is_at_least(1) |> should.equal(True)
  result.metadata.sheet_count |> int.is_at_least(2) |> should.equal(True)
  result.metadata.sheet_names |> string.contains("Stanley Cups") |> should.equal(True)
}

// XLSX workbook with multiple sheets.
pub fn office_xlsx_multi_sheet_test() {
  let result = kreuzberg.extract_file("xlsx/excel_multi_sheet.xlsx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
  result.metadata.sheet_count |> int.is_at_least(2) |> should.equal(True)
}

// Simple XLSX spreadsheet shipped alongside office integration tests.
pub fn office_xlsx_office_example_test() {
  let result = kreuzberg.extract_file("xlsx/test_01.xlsx")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

