// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:678727d7f78d2df448a2c1e796302a196cadc47d599eada445bed5497180cbb8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import kreuzberg

// Batch chunking of multiple texts in a single call
pub fn chunk_batch_basic_test() {
  let result = kreuzberg.chunk_texts_batch(["Hello world. This is text one.", "Goodbye world. This is text two."], "{\"max_characters\":100,\"max_overlap\":10}")
  result |> should.be_ok()
}

// Basic text chunking with small chunk size and overlap
pub fn chunk_text_basic_test() {
  let result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":10}")
  result |> should.be_ok()
}

// Text chunking with large chunk size to test handling of long content
pub fn chunk_text_large_test() {
  let result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":1000,\"max_overlap\":10}")
  result |> should.be_ok()
}

// Text chunking with no overlap between chunks
pub fn chunk_text_no_overlap_test() {
  let result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":0}")
  result |> should.be_ok()
}

// chunk_text overlap >= max
pub fn chunk_text_overlap_exceeds_test() {
  kreuzberg.chunk_text("Some text.", "{\"max_characters\":50,\"overlap\":60}") |> should.be_error()
}

// Chunk with YAML chunker
pub fn chunk_text_yaml_test() {
  let result = kreuzberg.chunk_text("key1: value1\nkey2: value2\n---\nkey3: value3", "{\"chunker_type\":\"yaml\",\"max_characters\":50}")
  result |> should.be_ok()
}

// chunk_text max_characters zero
pub fn chunk_text_zero_max_test() {
  kreuzberg.chunk_text("Some text.", "{\"max_characters\":0}") |> should.be_error()
}

// Batch chunk multiple texts
pub fn chunk_texts_batch_multiple_test() {
  let result = kreuzberg.chunk_texts_batch(["First doc text.", "Second doc text."], "{\"max_characters\":100}")
  result |> should.be_ok()
}

