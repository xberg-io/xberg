// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f6044016c9b315db6e920dd1fe451b41fb3f666789a5f6a7575781ecae6cb2b6
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import kreuzberg

import gleam/list
import gleam/string

// CSV data file extraction.
pub fn structured_csv_basic_test() {
  let result = kreuzberg.extract_file("csv/stanley_cups.csv")
  result |> should.be_ok()
  result.mime_type |> should.equal("text/csv")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// EndNote ENW citation format extraction.
pub fn structured_enw_basic_test() {
  let result = kreuzberg.extract_file("data_formats/sample.enw")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-endnote-refer") |> should.equal(True)
  result.mime_type |> string.contains("application/x-endnote+xml") |> should.equal(True)
  result.mime_type |> string.contains("text/plain") |> should.equal(True)
}

// Structured JSON extraction should stream and preserve content.
pub fn structured_json_basic_test() {
  let result = kreuzberg.extract_file("json/sample_document.json")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/json")
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
  result.content |> string.contains("Sample Document") |> should.equal(True)
  result.content |> string.contains("Test Author") |> should.equal(True)
}

// Simple JSON document to verify structured extraction.
pub fn structured_json_simple_test() {
  let result = kreuzberg.extract_file("json/simple.json")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/json")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
  result.content |> string.contains("{") |> should.equal(True)
  result.content |> string.contains("name") |> should.equal(True)
}

// PubMed NBIB citation format extraction.
pub fn structured_nbib_basic_test() {
  let result = kreuzberg.extract_file("data_formats/sample.nbib")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/nbib") |> should.equal(True)
  result.mime_type |> string.contains("application/x-pubmed") |> should.equal(True)
  result.mime_type |> string.contains("text/plain") |> should.equal(True)
  result.content |> list.is_empty |> should.equal(False)
}

// RIS citation format extraction.
pub fn structured_ris_basic_test() {
  let result = kreuzberg.extract_file("data_formats/sample.ris")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/x-research-info-systems") |> should.equal(True)
  result.mime_type |> string.contains("text/plain") |> should.equal(True)
  result.content |> list.is_empty |> should.equal(False)
}

// TOML configuration file extraction.
pub fn structured_toml_basic_test() {
  let result = kreuzberg.extract_file("data_formats/cargo.toml")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/toml") |> should.equal(True)
  result.mime_type |> string.contains("text/toml") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// TSV (tab-separated values) data file extraction.
pub fn structured_tsv_basic_test() {
  let result = kreuzberg.extract_file("data_formats/employees.tsv")
  result |> should.be_ok()
  result.mime_type |> string.contains("text/tab-separated-values") |> should.equal(True)
  result.mime_type |> string.contains("text/plain") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// YAML file text extraction.
pub fn structured_yaml_basic_test() {
  let result = kreuzberg.extract_file("yaml/simple.yaml")
  result |> should.be_ok()
  result.mime_type |> string.contains("application/yaml") |> should.equal(True)
  result.mime_type |> string.contains("text/yaml") |> should.equal(True)
  result.mime_type |> string.contains("text/x-yaml") |> should.equal(True)
  result.mime_type |> string.contains("application/x-yaml") |> should.equal(True)
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Simple YAML document to validate structured extraction.
pub fn structured_yaml_simple_test() {
  let result = kreuzberg.extract_file("yaml/simple.yaml")
  result |> should.be_ok()
  result.mime_type |> should.equal("application/x-yaml")
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

