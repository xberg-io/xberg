// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a3c975f48602de75039e8f89bb68f19d2fed8f23fe169b3866344a369fc5791d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import kreuzberg

import gleam/string

// Convert basic HTML to Markdown format
pub fn convert_html_basic_test() {
  let result = kreuzberg.convert_html_to_markdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
  result |> should.be_ok()
  result.content |> string.contains("Title") |> should.equal(True)
}

// Convert complex HTML to markdown
pub fn convert_html_complex_test() {
  let result = kreuzberg.convert_html_to_markdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(20) |> should.equal(True)
}

// Convert empty HTML to markdown
pub fn convert_html_empty_test() {
  let result = kreuzberg.convert_html_to_markdown("")
  result |> should.be_ok()
}

// Convert HTML table to Markdown table format
pub fn convert_html_table_test() {
  let result = kreuzberg.convert_html_to_markdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>")
  result |> should.be_ok()
  result.content |> string.contains("|") |> should.equal(True)
}

// Convert Djot format to HTML
pub fn djot_to_html_basic_test() {
  let result = kreuzberg.djot_to_html("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Complex djot to HTML
pub fn djot_to_html_complex_test() {
  let result = kreuzberg.djot_to_html("# Heading\n\nParagraph.\n\n- Item one\n- Item two")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Convert empty djot to HTML
pub fn djot_to_html_empty_test() {
  let result = kreuzberg.djot_to_html("")
  result |> should.be_ok()
}

// Render HTML document to Djot format
pub fn render_djot_basic_test() {
  let result = kreuzberg.render_djot("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render HTML to djot
pub fn render_djot_formatted_test() {
  let result = kreuzberg.render_djot("<h2>Section</h2><p>Text with <code>code</code>.</p>")
  result |> should.be_ok()
}

// Render a simple HTML document to formatted output
pub fn render_html_basic_test() {
  let result = kreuzberg.render_html("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render HTML preserving structure
pub fn render_html_styled_test() {
  let result = kreuzberg.render_html("<h1>Heading</h1><p><strong>bold</strong></p>")
  result |> should.be_ok()
  result.content |> string.contains("Heading") |> should.equal(True)
}

// Render HTML document to JSON format
pub fn render_json_basic_test() {
  let result = kreuzberg.render_json("<h1>Structured Data</h1><p>Content for JSON rendering.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render HTML to JSON
pub fn render_json_complex_test() {
  let result = kreuzberg.render_json("<h1>Title</h1><p>Text.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render HTML document to Markdown format
pub fn render_markdown_basic_test() {
  let result = kreuzberg.render_markdown("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render complex HTML to markdown
pub fn render_markdown_complex_test() {
  let result = kreuzberg.render_markdown("<h1>Title</h1><ul><li>A</li><li>B</li></ul>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(10) |> should.equal(True)
}

// Render HTML document to plain text format
pub fn render_plain_basic_test() {
  let result = kreuzberg.render_plain("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>")
  result |> should.be_ok()
  result.content |> string.length |> int.is_at_least(5) |> should.equal(True)
}

// Render empty HTML
pub fn render_plain_empty_test() {
  let result = kreuzberg.render_plain("")
  result |> should.be_ok()
}

