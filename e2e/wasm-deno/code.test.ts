// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang wasm-deno

// Tests for code fixtures. Run with: deno test --allow-read

import type { ExtractionResult } from "./helpers.ts";
import {
	assertions,
	buildConfig,
	enableOcr,
	extractBytes,
	initWasm,
	resolveDocument,
	shouldSkipFixture,
} from "./helpers.ts";

// Initialize WASM module and enable OCR once at module load time
await initWasm();
await enableOcr();

Deno.test("code_javascript_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("code/app.js");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "text/x-source-code", config);
	} catch (error) {
		if (shouldSkipFixture(error, "code_javascript_basic", ["tree-sitter"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["text/x-source-code"]);
	assertions.assertMinContentLength(result, 10);
	assertions.assertContentContainsAll(result, ["class Application", "export function", "import"]);
});

Deno.test("code_python_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("code/hello.py");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "text/x-source-code", config);
	} catch (error) {
		if (shouldSkipFixture(error, "code_python_basic", ["tree-sitter"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["text/x-source-code"]);
	assertions.assertMinContentLength(result, 10);
	assertions.assertContentContainsAll(result, ["def greet", "class Greeter", "import os"]);
});

Deno.test("code_rust_basic", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("code/main.rs");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "text/x-source-code", config);
	} catch (error) {
		if (shouldSkipFixture(error, "code_rust_basic", ["tree-sitter"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["text/x-source-code"]);
	assertions.assertMinContentLength(result, 10);
	assertions.assertContentContainsAll(result, ["struct Store", "fn main", "HashMap"]);
});

Deno.test("code_shebang_detection", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig(undefined);
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("code/script.sh");
		// Sync bytes extraction - WASM uses extractBytes with Uint8Array
		result = await extractBytes(documentBytes, "text/x-source-code", config);
	} catch (error) {
		if (shouldSkipFixture(error, "code_shebang_detection", ["tree-sitter"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["text/x-source-code"]);
	assertions.assertMinContentLength(result, 10);
	assertions.assertContentContainsAll(result, ["build", "clean"]);
});

Deno.test("code_tree_sitter_config", { permissions: { read: true, net: true } }, async () => {
	const config = buildConfig({
		tree_sitter: { process: { structure: true, imports: true, exports: true, comments: true, docstrings: true } },
	});
	let result: ExtractionResult | null = null;
	try {
		const documentBytes = await resolveDocument("code/hello.py");
		// Sync file extraction - WASM uses extractBytes with pre-read bytes
		result = await extractBytes(documentBytes, "text/x-source-code", config);
	} catch (error) {
		if (shouldSkipFixture(error, "code_tree_sitter_config", ["tree-sitter"], undefined)) {
			return;
		}
		throw error;
	}
	if (result === null) {
		return;
	}
	assertions.assertExpectedMime(result, ["text/x-source-code"]);
	assertions.assertMinContentLength(result, 10);
	assertions.assertContentContainsAll(result, ["def greet", "class Greeter"]);
});
