// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:86c99fa991158abf2c02bf8e425fc507dac6d2cb94de8b5cd868ab7ebaec3b2b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: rendering

test "convert_html_basic" {
    // Convert basic HTML to Markdown format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.convert_html_to_markdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Title") != null);
}

test "convert_html_complex" {
    // Convert complex HTML to markdown
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.convert_html_to_markdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>");
    try testing.expect(result.content.len >= 20);
}

test "convert_html_empty" {
    // Convert empty HTML to markdown
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.convert_html_to_markdown("");
}

test "convert_html_table" {
    // Convert HTML table to Markdown table format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.convert_html_to_markdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>");
    try testing.expect(std.mem.indexOf(u8, result.content, "|") != null);
}

test "djot_to_html_basic" {
    // Convert Djot format to HTML
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.djot_to_html("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2");
    try testing.expect(result.content.len >= 10);
}

test "djot_to_html_complex" {
    // Complex djot to HTML
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.djot_to_html("# Heading\n\nParagraph.\n\n- Item one\n- Item two");
    try testing.expect(result.content.len >= 10);
}

test "djot_to_html_empty" {
    // Convert empty djot to HTML
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.djot_to_html("");
}

test "render_djot_basic" {
    // Render HTML document to Djot format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_djot("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>");
    try testing.expect(result.content.len >= 5);
}

test "render_djot_formatted" {
    // Render HTML to djot
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_djot("<h2>Section</h2><p>Text with <code>code</code>.</p>");
}

test "render_html_basic" {
    // Render a simple HTML document to formatted output
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_html("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>");
    try testing.expect(result.content.len >= 5);
}

test "render_html_styled" {
    // Render HTML preserving structure
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_html("<h1>Heading</h1><p><strong>bold</strong></p>");
    try testing.expect(std.mem.indexOf(u8, result.content, "Heading") != null);
}

test "render_json_basic" {
    // Render HTML document to JSON format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_json("<h1>Structured Data</h1><p>Content for JSON rendering.</p>");
    try testing.expect(result.content.len >= 5);
}

test "render_json_complex" {
    // Render HTML to JSON
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_json("<h1>Title</h1><p>Text.</p>");
    try testing.expect(result.content.len >= 5);
}

test "render_markdown_basic" {
    // Render HTML document to Markdown format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_markdown("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>");
    try testing.expect(result.content.len >= 5);
}

test "render_markdown_complex" {
    // Render complex HTML to markdown
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_markdown("<h1>Title</h1><ul><li>A</li><li>B</li></ul>");
    try testing.expect(result.content.len >= 10);
}

test "render_plain_basic" {
    // Render HTML document to plain text format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_plain("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>");
    try testing.expect(result.content.len >= 5);
}

test "render_plain_empty" {
    // Render empty HTML
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_plain("");
}

