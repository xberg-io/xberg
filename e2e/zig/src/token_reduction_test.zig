// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:688b08d30f0c422b9d5273b840d9861478b475b079bc6d31aa04f37c701b3a84
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: token_reduction

test "token_reduction_aggressive" {
    // Tests aggressive token reduction mode significantly reduces content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"aggressive\"}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 5);
    try testing.expect(result.content.len <= 150);
    try testing.expect(result.content.len > 0);
}

test "token_reduction_basic" {
    // Tests basic token reduction on PDF document
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"moderate\"}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 5);
    try testing.expect(result.content.len <= 200);
    try testing.expect(result.content.len > 0);
}

test "token_reduction_light" {
    // Tests light token reduction mode preserves most content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"light\"}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.content.len > 0);
}

test "token_reduction_reduce_aggressive" {
    // Reduce tokens with aggressive compression level
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.reduce_tokens("The quick brown fox jumps over the lazy dog.", "aggressive");
    try testing.expect(result.result.len >= 1);
}

test "token_reduction_reduce_basic" {
    // Reduce tokens with light compression level
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.reduce_tokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", "light");
    try testing.expect(std.mem.indexOf(u8, result.result, "quick") != null);
}

test "token_reduction_with_chunking" {
    // Tests token reduction combined with chunking
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50},\"token_reduction\":{\"mode\":\"moderate\"}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 5);
    try testing.expect(result.content.len <= 200);
    try testing.expect(result.content.len > 0);
    try testing.expect(result.chunks.len >= 1);
    try testing.expect(result.chunks_have_content);
}

