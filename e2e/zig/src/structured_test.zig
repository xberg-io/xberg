// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ff0681fa2252556e40c23b3cc4b2817a2b65429404230fff4ba409ac93a1bae1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: structured

test "structured_csv_basic" {
    // CSV data file extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("csv/stanley_cups.csv");
    try testing.expectEqual("text/csv", result.mime_type);
    try testing.expect(result.content.len >= 20);
}

test "structured_enw_basic" {
    // EndNote ENW citation format extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("data_formats/sample.enw");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-endnote-refer") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-endnote+xml") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/plain") != null);
}

test "structured_json_basic" {
    // Structured JSON extraction should stream and preserve content.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("json/sample_document.json");
    try testing.expectEqual("application/json", result.mime_type);
    try testing.expect(result.content.len >= 20);
    try testing.expect(std.mem.indexOf(u8, result.content, "Sample Document") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Test Author") != null);
}

test "structured_json_simple" {
    // Simple JSON document to verify structured extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("json/simple.json");
    try testing.expectEqual("application/json", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "{") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "name") != null);
}

test "structured_nbib_basic" {
    // PubMed NBIB citation format extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("data_formats/sample.nbib");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/nbib") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-pubmed") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/plain") != null);
    try testing.expect(result.content.len > 0);
}

test "structured_ris_basic" {
    // RIS citation format extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("data_formats/sample.ris");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-research-info-systems") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/plain") != null);
    try testing.expect(result.content.len > 0);
}

test "structured_toml_basic" {
    // TOML configuration file extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("data_formats/cargo.toml");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/toml") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/toml") != null);
    try testing.expect(result.content.len >= 10);
}

test "structured_tsv_basic" {
    // TSV (tab-separated values) data file extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("data_formats/employees.tsv");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/tab-separated-values") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/plain") != null);
    try testing.expect(result.content.len >= 10);
}

test "structured_yaml_basic" {
    // YAML file text extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("yaml/simple.yaml");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/yaml") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/yaml") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/x-yaml") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-yaml") != null);
    try testing.expect(result.content.len >= 10);
}

test "structured_yaml_simple" {
    // Simple YAML document to validate structured extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("yaml/simple.yaml");
    try testing.expectEqual("application/x-yaml", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

