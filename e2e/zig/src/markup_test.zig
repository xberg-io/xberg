// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:52dc7586faf06968a44c05b348a04e11ee5e8ffc2338ad76e1bb6a9f5073025a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: markup

test "bold_basic" {
    // Create bold markdown formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.bold("important");
    try testing.expect(std.mem.indexOf(u8, result.result, "important") != null);
}

test "bold_empty" {
    // Bold empty string
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.bold("");
}

test "bold_special_chars" {
    // Bold special chars
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.bold("hello **world** & <tag>");
    try testing.expect(std.mem.indexOf(u8, result.result, "hello") != null);
}

test "code_basic" {
    // Create inline code formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.code("variable");
    try testing.expect(std.mem.indexOf(u8, result.result, "variable") != null);
}

test "code_multiline" {
    // Code multiline
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.code("line1\nline2");
}

test "italic_basic" {
    // Create italic markdown formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.italic("emphasis");
    try testing.expect(std.mem.indexOf(u8, result.result, "emphasis") != null);
}

test "italic_empty" {
    // Italic empty string
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.italic("");
}

test "italic_special_chars" {
    // Italic special chars
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.italic("emphasized *text*");
}

test "link_basic" {
    // Create markdown link with text and URL
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.link("Click here", "https://example.com");
    try testing.expect(std.mem.indexOf(u8, result.result, "Click here") != null);
    try testing.expect(std.mem.indexOf(u8, result.result, "example.com") != null);
}

test "link_empty_text" {
    // Create markdown link with empty text
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.link("", "https://example.com");
    try testing.expect(std.mem.indexOf(u8, result.result, "example.com") != null);
}

test "link_special_url" {
    // Link with query params
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.link("click", "https://example.com?q=1&b=2");
}

test "strikethrough_basic" {
    // Create strikethrough formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.strikethrough("deleted");
    try testing.expect(std.mem.indexOf(u8, result.result, "deleted") != null);
}

test "strikethrough_empty" {
    // Strikethrough empty
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.strikethrough("");
}

test "underline_basic" {
    // Create underline formatting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.underline("underlined");
    try testing.expect(std.mem.indexOf(u8, result.result, "underlined") != null);
}

test "underline_empty" {
    // Underline empty
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.underline("");
}

