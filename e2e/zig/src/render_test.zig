// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2df163f48bd9e99ced7c6391cbbb0a491010f86137d77933280511aa7c426471
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: render

test "render_custom_dpi" {
    // Render PDF page with custom DPI
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_page("pdf/tiny.pdf", 0);
    try testing.expect(result.is_png);
    try testing.expect(result.byte_length >= 50);
}

test "render_high_dpi" {
    // Render PDF page with high DPI setting (300 DPI)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_page("pdf/fake_memo.pdf", 0, "{\"dpi\":300}");
}

test "render_iterator" {
    // Iterate all PDF pages as PNG images
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_page("pdf/tiny.pdf", 0);
    try testing.expect(result.is_png);
    try testing.expect(result.page_count >= 1);
}

test "render_multi_page" {
    // Render a page from a multi-page PDF document
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_page("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf", 0);
    try testing.expect(result.result.len >= 1000);
}

test "render_output_djot" {
    // Extract PDF with Djot output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}");
    try testing.expectEqual("djot", result.output_format);
    try testing.expect(result.content.len >= 10);
}

test "render_output_html" {
    // Extract PDF with HTML output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}");
    try testing.expectEqual("html", result.output_format);
    try testing.expect(std.mem.indexOf(u8, result.content, "<") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, ">") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "</") != null);
}

test "render_output_markdown" {
    // Extract PDF with Markdown output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}");
    try testing.expectEqual("markdown", result.output_format);
    try testing.expect(std.mem.indexOf(u8, result.content, "#") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "**") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "*") != null);
}

test "render_output_plain" {
    // Extract PDF with plain text output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}");
    try testing.expectEqual("plain", result.output_format);
    try testing.expect(result.content.len >= 50);
}

test "render_single_page" {
    // Render a single PDF page to PNG
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.render_page("pdf/tiny.pdf", 0);
    try testing.expect(result.is_png);
    try testing.expect(result.byte_length >= 100);
}

