// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e9cb71a8f39113b982ca7ff56de65363e06460ddc1471113447bca632dc95ee2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: archive

test "archive_gz_basic" {
    // Gzip compressed file extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("archives/book_war_and_peace_1p.txt.gz");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/gzip") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-gzip") != null);
    try testing.expect(result.content.len >= 10);
}

test "archive_sevenz_basic" {
    // 7-Zip archive extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("archives/documents.7z");
    try testing.expectEqual("application/x-7z-compressed", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "archive_tar_basic" {
    // TAR archive extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("archives/documents.tar");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-tar") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/tar") != null);
    try testing.expect(result.content.len >= 10);
}

test "archive_zip_basic" {
    // ZIP archive extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("archives/documents.zip");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/zip") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-zip-compressed") != null);
    try testing.expect(result.content.len >= 10);
}

