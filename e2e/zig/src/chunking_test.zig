// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4f6652e76ecfdebc881d38a10d12c44c85902997f1d69138fa7b8bd4a48ff5b0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: chunking

test "chunk_batch_basic" {
    // Batch chunking of multiple texts in a single call
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_texts_batch(&.{"Hello world. This is text one.", "Goodbye world. This is text two."}, "{\"max_characters\":100,\"max_overlap\":10}");
}

test "chunk_text_basic" {
    // Basic text chunking with small chunk size and overlap
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":10}");
}

test "chunk_text_large" {
    // Text chunking with large chunk size to test handling of long content
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":1000,\"max_overlap\":10}");
}

test "chunk_text_no_overlap" {
    // Text chunking with no overlap between chunks
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":0}");
}

test "chunk_text_overlap_exceeds" {
    // chunk_text overlap >= max
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("Some text.", "{\"max_characters\":50,\"overlap\":60}");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "chunk_text_yaml" {
    // Chunk with YAML chunker
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("key1: value1\nkey2: value2\n---\nkey3: value3", "{\"chunker_type\":\"yaml\",\"max_characters\":50}");
}

test "chunk_text_zero_max" {
    // chunk_text max_characters zero
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_text("Some text.", "{\"max_characters\":0}");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "chunk_texts_batch_multiple" {
    // Batch chunk multiple texts
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.chunk_texts_batch(&.{"First doc text.", "Second doc text."}, "{\"max_characters\":100}");
}

