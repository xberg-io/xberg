// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cdf71a96e612f689d38eb99915e90cc6498247dc81b39542baf223c137eb226d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: email

test "email_eml_html_body" {
    // EML with HTML body content.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/html_only.eml");
    try testing.expectEqual("message/rfc822", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "email_eml_multipart" {
    // EML with multipart MIME content.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/html_email_multipart.eml");
    try testing.expectEqual("message/rfc822", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "email_eml_utf16" {
    // UTF-16 encoded EML file with BOM.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("vendored/unstructured/eml/fake-email-utf-16.eml");
    try testing.expectEqual("message/rfc822", result.mime_type);
    try testing.expect(result.content.len >= 50);
    try testing.expect(std.mem.indexOf(u8, result.content, "Test Email") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Roses are red") != null);
}

test "email_msg_basic" {
    // Outlook MSG file extraction.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/fake_email.msg");
    try testing.expectEqual("application/vnd.ms-outlook", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "email_pst_empty" {
    // Empty Outlook PST archive with no messages.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/empty.pst");
    try testing.expectEqual("application/vnd.ms-outlook-pst", result.mime_type);
}

test "email_sample_eml" {
    // Sample EML email file to verify email parsing.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/sample_email.eml");
    try testing.expectEqual("message/rfc822", result.mime_type);
    try testing.expect(result.content.len >= 20);
}

