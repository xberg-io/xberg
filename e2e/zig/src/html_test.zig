// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0bea7ef93c1e7116d98654d9ca2d4081fb6dd4984087cfa2c692007ec15798b0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: html

test "html_complex_layout" {
    // Large Wikipedia HTML page to validate complex conversion.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("html/taylor_swift.html");
    try testing.expectEqual("text/html", result.mime_type);
    try testing.expect(result.content.len >= 1000);
}

test "html_simple_table" {
    // HTML table converted to markdown should retain structure.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("html/simple_table.html");
    try testing.expectEqual("text/html", result.mime_type);
    try testing.expect(result.content.len >= 100);
    try testing.expect(std.mem.indexOf(u8, result.content, "Product") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Category") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Price") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Stock") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Laptop") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Electronics") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Sample Data Table") != null);
}

