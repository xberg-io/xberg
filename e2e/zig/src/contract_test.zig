// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:86d06e516872403314ab000ed933d728ed5afa7af83d8590e54c0f4a209caeef
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const kreuzberg = @import("kreuzberg");

// E2e tests for category: contract

test "api_batch_bytes_async" {
    // Tests async batch bytes extraction API (batch_extract_bytes)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_bytes(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_batch_bytes_sync" {
    // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_bytes_sync(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_batch_bytes_with_configs_async" {
    // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_bytes(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "api_batch_bytes_with_configs_sync" {
    // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_bytes_sync(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "api_batch_file_async" {
    // Tests async batch file extraction API (batch_extract_file)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_file(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_batch_file_sync" {
    // Tests sync batch file extraction API (batch_extract_file_sync)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_file_sync(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_batch_file_with_configs_async" {
    // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_file(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "api_batch_file_with_configs_sync" {
    // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_file_sync(null);
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "api_batch_file_with_timeout_sync" {
    // Tests sync batch file extraction with per-file timeout config override
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.batch_extract_file_sync(null, "{\"extraction_timeout_secs\":300}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "api_extract_bytes_async" {
    // Tests async bytes extraction API (extract_bytes)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_bytes(null, "");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_extract_bytes_sync" {
    // Tests sync bytes extraction API (extract_bytes_sync)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_bytes_sync(null, "");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_extract_file_async" {
    // Tests async file extraction API (extract_file)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("pdf/fake_memo.pdf");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "api_extract_file_sync" {
    // Tests sync file extraction API (extract_file_sync)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "Mallori") != null);
}

test "config_acceleration_cpu_provider" {
    // Tests explicit CPU acceleration provider configuration
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"acceleration\":{\"device_id\":0,\"provider\":\"cpu\"}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 50);
    try testing.expect(std.mem.indexOf(u8, result.content, "May 5, 2023") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "To Whom it May Concern") != null);
}

test "config_chunking" {
    // Tests chunking configuration with chunk assertions
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.chunks.len >= 1);
    try testing.expect(result.chunks_have_content);
}

test "config_chunking_text" {
    // Tests text chunker type (generic whitespace/punctuation splitter)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"chunking\":{\"chunker_type\":\"text\",\"max_chars\":500,\"max_overlap\":50}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.chunks.len >= 1);
    try testing.expect(result.chunks_have_content);
}

test "config_disable_ocr" {
    // Tests disable_ocr configuration option - OCR is skipped for images
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("images/test_hello_world.png", "{\"disable_ocr\":true}");
    try testing.expectEqual("image/png", result.mime_type);
    try testing.expect(result.content.len <= 5);
}

test "config_document_structure" {
    // Tests include_document_structure config produces document tree
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"include_document_structure\":true}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.document_structure.len > 0);
    try testing.expect(result.document_structure.nodes.len >= 1);
    try testing.expect(std.mem.indexOf(u8, result.document_structure.node_types, "paragraph") != null);
}

test "config_document_structure_disabled" {
    // Tests document field is null when include_document_structure is false
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf");
    try testing.expectEqual("application/pdf", result.mime_type);
}

test "config_document_structure_with_headings" {
    // Tests document structure with DOCX heading-driven nesting
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("docx/fake.docx", "{\"include_document_structure\":true}");
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type);
    try testing.expect(result.document_structure.len > 0);
    try testing.expect(result.document_structure.nodes.len >= 1);
}

test "config_email_msg_fallback_codepage" {
    // Tests MSG extraction with custom fallback codepage for Cyrillic
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file("email/fake_email.msg", "{\"email\":{\"msg_fallback_codepage\":1251}}");
    try testing.expectEqual("application/vnd.ms-outlook", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "config_extraction_timeout" {
    // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"extraction_timeout_secs\":300}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "config_html_options" {
    // Tests extraction with HTML conversion options configured
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("html/complex_table.html", "{\"html_options\":{\"extract_metadata\":true}}");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "text/html") != null);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.content.len > 0);
}

test "config_html_styled_custom_css" {
    // HTML output with inline user CSS string is embedded verbatim in the style block
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"html_output\":{\"css\":\".kb-p { color: red; }\",\"embed_css\":true,\"theme\":\"unstyled\"},\"output_format\":\"html\"}");
    try testing.expectEqual("html", result.output_format);
    try testing.expect(std.mem.indexOf(u8, result.content, ".kb-p { color: red; }") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-doc") != null);
}

test "config_html_styled_default" {
    // HTML output with default theme emits kb-* class hooks and an embedded style block
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":true,\"theme\":\"default\"},\"output_format\":\"html\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expectEqual("html", result.output_format);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-doc") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-content") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-p") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "<style>") != null);
}

test "config_html_styled_no_embed" {
    // HTML output with embed_css false emits class hooks but no style block
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":false,\"theme\":\"default\"},\"output_format\":\"html\"}");
    try testing.expectEqual("html", result.output_format);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-doc") != null);
    try testing.expect(std.mem.indexOf(u8, result.content, "kb-content") != null);
}

test "config_images" {
    // Tests image extraction configuration with image assertions
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/embedded_images_tables.pdf", "{\"images\":{\"extract_images\":true}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.images.len >= 1);
}

test "config_images_with_formats" {
    // Tests image extraction on PPTX containing embedded images
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pptx/powerpoint_with_image.pptx", "{\"images\":{\"extract_images\":true}}");
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mime_type);
    try testing.expect(result.images.len >= 1);
}

test "config_language_detection" {
    // Tests language detection configuration
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"language_detection\":{\"enabled\":true}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.detected_languages, "eng") != null);
    try testing.expect(result.language_detection_confidence >= 0.5);
}

test "config_language_detection_multi" {
    // Tests language detection with detect_multiple and min_confidence options
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"language_detection\":{\"detect_multiple\":true,\"enabled\":true,\"min_confidence\":0.3}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(std.mem.indexOf(u8, result.detected_languages, "eng") != null);
}

test "config_postprocessor" {
    // Tests postprocessor config is accepted and extraction succeeds
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"postprocessor\":{\"enabled\":true}}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.content.len > 0);
}

test "config_processing_warnings_empty" {
    // Tests that a clean PDF extraction produces no processing warnings
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.processing_warnings.len == 0);
}

test "config_quality_disabled" {
    // Tests extraction with quality processing explicitly disabled
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"enable_quality_processing\":false}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expect(result.content.len > 0);
}

test "config_security_limits" {
    // Tests archive extraction with custom security limits
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("archives/documents.zip", "{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}}");
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/zip") != null);
    try testing.expect(std.mem.indexOf(u8, result.mime_type, "application/x-zip-compressed") != null);
    try testing.expect(result.content.len >= 10);
}

test "config_tables_content" {
    // Tests table extraction with content_contains_any assertion on DOCX with tables
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("docx/docx_tables.docx");
    try testing.expectEqual("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type);
    try testing.expect(result.tables.len >= 1);
}

test "config_use_cache_false" {
    // Tests use_cache=false configuration option
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"use_cache\":false}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
}

test "output_format_bytes_markdown" {
    // Tests markdown output format via bytes extraction API
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_bytes_sync(null, "", "{\"output_format\":\"markdown\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "output_format_djot" {
    // Tests Djot output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("djot", result.output_format);
}

test "output_format_html" {
    // Tests HTML output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("html", result.output_format);
}

test "output_format_markdown" {
    // Tests Markdown output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("markdown", result.output_format);
}

test "output_format_plain" {
    // Tests Plain output format
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("plain", result.output_format);
}

test "result_format_element_based" {
    // Tests ElementBased result format with element assertions
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"result_format\":\"element_based\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expectEqual("element_based", result.result_format);
    try testing.expect(result.elements.len >= 1);
}

test "result_format_unified" {
    // Tests Unified result format (default)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = kreuzberg.extract_file_sync("pdf/fake_memo.pdf", "{\"result_format\":\"unified\"}");
    try testing.expectEqual("application/pdf", result.mime_type);
    try testing.expect(result.content.len >= 10);
    try testing.expectEqual("unified", result.result_format);
}

