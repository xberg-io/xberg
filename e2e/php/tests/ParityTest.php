<?php

declare(strict_types=1);

// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang php

use PHPUnit\Framework\TestCase;
use PHPUnit\Framework\Attributes\Test;

class ParityTest extends TestCase
{
    #[Test]
    public function archive_entry_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\ArchiveEntry::class);
        $expected = [
            'mimeType',
            'path',
            'result',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "ArchiveEntry missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function bounding_box_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\BoundingBox::class);
        $expected = [
            'x0',
            'x1',
            'y0',
            'y1',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "BoundingBox missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function extraction_config_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\ExtractionConfig::class);
        $expected = [
            'acceleration',
            'cacheNamespace',
            'cacheTtlSecs',
            'chunking',
            'concurrency',
            'contentFilter',
            'disableOcr',
            'email',
            'enableQualityProcessing',
            'extractionTimeoutSecs',
            'forceOcr',
            'forceOcrPages',
            'htmlOptions',
            'htmlOutput',
            'images',
            'includeDocumentStructure',
            'keywords',
            'languageDetection',
            'layout',
            'maxArchiveDepth',
            'maxConcurrentExtractions',
            'ocr',
            'outputFormat',
            'pages',
            'pdfOptions',
            'postprocessor',
            'resultFormat',
            'securityLimits',
            'tokenReduction',
            'treeSitter',
            'useCache',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "ExtractionConfig missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function extraction_result_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\ExtractionResult::class);
        $expected = [
            'annotations',
            'children',
            'chunks',
            'content',
            'detectedLanguages',
            'djotContent',
            'document',
            'elements',
            'extractedKeywords',
            'images',
            'metadata',
            'mimeType',
            'ocrElements',
            'pages',
            'processingWarnings',
            'qualityScore',
            'tables',
            'uris',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "ExtractionResult missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function keyword_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\Keyword::class);
        $expected = [
            'algorithm',
            'positions',
            'score',
            'text',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "Keyword missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function pdf_annotation_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\PdfAnnotation::class);
        $expected = [
            'annotationType',
            'boundingBox',
            'content',
            'pageNumber',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "PdfAnnotation missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function processing_warning_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\ProcessingWarning::class);
        $expected = [
            'message',
            'source',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "ProcessingWarning missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function table_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\Table::class);
        $expected = [
            'boundingBox',
            'cells',
            'markdown',
            'pageNumber',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "Table missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function uri_has_all_expected_properties(): void
    {
        $ref = new \ReflectionClass(\Kreuzberg\Types\Uri::class);
        $expected = [
            'kind',
            'label',
            'page',
            'url',
        ];
        foreach ($expected as $prop) {
            // hasProperty() only covers #[php_prop] declared fields.
            // Virtual properties backed by #[php(getter)] methods are
            // accessible via $obj->prop but only show up as a getter method
            // (getXxx) in ReflectionClass, not as a declared property.
            $getter = 'get' . ucfirst($prop);
            $this->assertTrue(
                $ref->hasProperty($prop) || $ref->hasMethod($getter),
                "Uri missing property or getter: {$prop}"
            );
        }
    }

    #[Test]
    public function content_layer_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\ContentLayer::class);
        $expected = [
            'body',
            'header',
            'footer',
            'footnote',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "ContentLayer missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function element_type_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\ElementType::class);
        $expected = [
            'title',
            'narrativeText',
            'heading',
            'listItem',
            'table',
            'image',
            'pageBreak',
            'codeBlock',
            'blockQuote',
            'footer',
            'header',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "ElementType missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function keyword_algorithm_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\KeywordAlgorithm::class);
        $expected = [
            'yake',
            'rake',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "KeywordAlgorithm missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function ocr_element_level_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\OcrElementLevel::class);
        $expected = [
            'word',
            'line',
            'block',
            'page',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "OcrElementLevel missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function output_format_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\OutputFormat::class);
        $expected = [
            'plain',
            'markdown',
            'djot',
            'html',
            'structured',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "OutputFormat missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function page_unit_type_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\PageUnitType::class);
        $expected = [
            'page',
            'slide',
            'sheet',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "PageUnitType missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function pdf_annotation_type_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\PdfAnnotationType::class);
        $expected = [
            'text',
            'highlight',
            'link',
            'stamp',
            'underline',
            'strikeOut',
            'other',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "PdfAnnotationType missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function relationship_kind_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\RelationshipKind::class);
        $expected = [
            'footnoteReference',
            'citationReference',
            'internalLink',
            'caption',
            'label',
            'tocEntry',
            'crossReference',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "RelationshipKind missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function result_format_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\ResultFormat::class);
        $expected = [
            'unified',
            'elementBased',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "ResultFormat missing enum value: {$val}"
            );
        }
    }

    #[Test]
    public function uri_kind_has_all_expected_values(): void
    {
        $ref = new \ReflectionEnum(\Kreuzberg\Types\UriKind::class);
        $expected = [
            'hyperlink',
            'image',
            'anchor',
            'citation',
            'reference',
            'email',
        ];
        $cases = array_map(
            fn ($case) => $case->name,
            $ref->getCases()
        );
        foreach ($expected as $val) {
            $this->assertContains(
                $val,
                $cases,
                "UriKind missing enum value: {$val}"
            );
        }
    }

}
