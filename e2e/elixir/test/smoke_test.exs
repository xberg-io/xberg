# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2af6df6dce9923495cc1b0e22b509157c6b1b2afbd685afba5479c0ebe7de0bc
# To regenerate: alef e2e generate
# To verify freshness: alef verify
# E2e tests for category: smoke
defmodule E2e.SmokeTest do
  use ExUnit.Case, async: false

  defp alef_e2e_item_texts(item) when is_binary(item), do: [item]

  defp alef_e2e_item_texts(item) do
    [:kind, :name, :signature, :path, :alias, :text, :source]
    |> Enum.filter(&Map.has_key?(item, &1))
    |> Enum.flat_map(fn attr ->
      case Map.get(item, attr) do
        nil -> []
        atom when is_atom(atom) -> [atom |> to_string() |> String.capitalize()]
        str -> [inspect(str)]
      end
    end)
  end

  defp alef_e2e_format_to_string(value) when is_binary(value), do: value

  defp alef_e2e_format_to_string(metadata) do
    case metadata.image do
      %{format: fmt} when is_binary(fmt) ->
        fmt

      _ ->
        case metadata.pdf do
          %{} ->
            "PDF"

          _ ->
            case metadata.html do
              %{} -> "HTML"
              _ -> inspect(metadata)
            end
        end
    end
  end

  describe "ocr_image_png" do
    test "ocr_image_png" do
      input_value = %Xberg.ExtractInput{
        bytes: :binary.bin_to_list(File.read!("images/test_hello_world.png")),
        config: %{},
        filename: "test_hello_world.png",
        kind: "bytes",
        mime_type: "image/png"
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "image/png"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 1) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 1)

      assert Enum.any?(["Hello", "World", "hello", "world"], fn v -> String.contains?(to_string(Enum.at(result.results, 0).content), v) end)
    end
  end

  describe "smoke_docx_basic" do
    test "smoke_docx_basic" do
      input_mock_base_url =
        System.get_env("MOCK_SERVER_SMOKE_DOCX_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_docx_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
        uri: String.replace("$mock_url/docx/fake.docx", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "application/vnd.openxmlformats-officedocument.wordprocessingml.document"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 20) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 20)

      assert Enum.any?(["Lorem", "ipsum", "document", "text"], fn v ->
               String.contains?(to_string(Enum.at(result.results, 0).content), v)
             end)
    end
  end

  describe "smoke_html_basic" do
    test "smoke_html_basic" do
      input_mock_base_url =
        System.get_env("MOCK_SERVER_SMOKE_HTML_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_html_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "text/html",
        uri: String.replace("$mock_url/html/simple_table.html", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "text/html"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 10) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 10)

      assert Enum.any?(["Sample Data Table", "Laptop", "Electronics", "Product"], fn v ->
               String.contains?(to_string(Enum.at(result.results, 0).content), v)
             end)
    end
  end

  describe "smoke_image_png" do
    test "smoke_image_png" do
      input_mock_base_url = System.get_env("MOCK_SERVER_SMOKE_IMAGE_PNG") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_image_png"
      input_value = %Xberg.ExtractInput{kind: "uri", uri: String.replace("$mock_url/images/sample.png", "$mock_url", input_mock_base_url)}
      {:ok, result} = Xberg.extract(input: input_value, config: "{\"disable_ocr\":true}")
      assert Enum.at(result.results, 0).mime_type == "image/png"
    end
  end

  describe "smoke_json_basic" do
    test "smoke_json_basic" do
      input_mock_base_url =
        System.get_env("MOCK_SERVER_SMOKE_JSON_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_json_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "application/json",
        uri: String.replace("$mock_url/json/simple.json", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "application/json"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 5) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 5)
    end
  end

  describe "smoke_pdf_basic" do
    test "smoke_pdf_basic" do
      input_mock_base_url = System.get_env("MOCK_SERVER_SMOKE_PDF_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_pdf_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "application/pdf",
        uri: String.replace("$mock_url/pdf/fake_memo.pdf", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "application/pdf"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 50) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 50)

      assert Enum.any?(["May 5, 2023", "To Whom it May Concern"], fn v ->
               String.contains?(to_string(Enum.at(result.results, 0).content), v)
             end)
    end
  end

  describe "smoke_txt_basic" do
    test "smoke_txt_basic" do
      input_mock_base_url = System.get_env("MOCK_SERVER_SMOKE_TXT_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_txt_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "text/plain",
        uri: String.replace("$mock_url/text/report.txt", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "text/plain"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 5) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 5)
    end
  end

  describe "smoke_xlsx_basic" do
    test "smoke_xlsx_basic" do
      input_mock_base_url =
        System.get_env("MOCK_SERVER_SMOKE_XLSX_BASIC") || "#{System.get_env("MOCK_SERVER_URL")}/fixtures/smoke_xlsx_basic"

      input_value = %Xberg.ExtractInput{
        kind: "uri",
        mime_type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        uri: String.replace("$mock_url/xlsx/stanley_cups.xlsx", "$mock_url", input_mock_base_url)
      }

      {:ok, result} = Xberg.extract(input: input_value, config: "{}")
      assert Enum.at(result.results, 0).mime_type == "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"

      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 100) ||
               (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 100)

      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Team")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Location")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Stanley Cups")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Blues")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Flyers")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Maple Leafs")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "STL")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "PHI")
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "TOR")
      assert length(Enum.at(result.results, 0).tables) >= 1
      assert Enum.at(result.results, 0).metadata.format.excel.sheet_count >= 2

      assert Enum.any?(Enum.at(result.results, 0).metadata.format.excel.sheet_names, fn item ->
               Enum.any?(alef_e2e_item_texts(item), &String.contains?(&1, "Stanley Cups"))
             end)
    end
  end
end
