# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b0f3e348cc193e0cfa8fdcc36fd3ebb5e08e4360d10b525744b736383abddeae
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# E2e tests for category: format_specific
defmodule E2e.FormatSpecificTest do
  use ExUnit.Case, async: false

  describe "format_docx_standalone" do
    test "format_docx_standalone" do
      input_value = %Xberg.ExtractInput{filename: "fake.docx", kind: "uri", mime_type: "application/vnd.openxmlformats-officedocument.wordprocessingml.document", uri: "docx/fake.docx"}
      {:ok, result} = Xberg.extract(input: input_value)
      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 20) || (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 20)
    end
  end

  describe "format_hwpx_standalone" do
    test "format_hwpx_standalone" do
      input_value = %Xberg.ExtractInput{filename: "simple.hwpx", kind: "uri", mime_type: "application/haansofthwpx", uri: "hwpx/simple.hwpx"}
      {:ok, result} = Xberg.extract(input: input_value)
      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 20) || (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 20)
      assert String.contains?(to_string(Enum.at(result.results, 0).content), "Hello from HWPX")
    end
  end

  describe "format_pdf_text" do
    test "format_pdf_text" do
      input_value = %Xberg.ExtractInput{filename: "fake_memo.pdf", kind: "uri", mime_type: "application/pdf", uri: "pdf/fake_memo.pdf"}
      {:ok, result} = Xberg.extract(input: input_value)
      assert (is_binary(Enum.at(result.results, 0).content) && byte_size(Enum.at(result.results, 0).content) >= 50) || (is_list(Enum.at(result.results, 0).content) && length(Enum.at(result.results, 0).content) >= 50)
      assert Enum.any?(["Mallori", "May"], fn v -> String.contains?(to_string(Enum.at(result.results, 0).content), v) end)
    end
  end

  describe "format_pptx" do
    test "format_pptx" do
      input_value = %Xberg.ExtractInput{kind: "uri", mime_type: "application/vnd.openxmlformats-officedocument.presentationml.presentation", uri: "pptx/simple.pptx"}
      {:ok, result} = Xberg.extract(input: input_value)
    end
  end

  describe "format_xlsx" do
    test "format_xlsx" do
      input_value = %Xberg.ExtractInput{kind: "uri", mime_type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", uri: "xlsx/stanley_cups.xlsx"}
      {:ok, result} = Xberg.extract(input: input_value)
    end
  end
end
