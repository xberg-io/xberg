// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
//! E2e tests for category: format_specific

use xberg::extract;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};
use xberg::ExtractInput;

#[tokio::test]
async fn test_format_docx_standalone() {
    // Standalone DOCX extraction using extract
    let input_mock_base_url = std::env::var("MOCK_SERVER_FORMAT_DOCX_STANDALONE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FORMAT_DOCX_STANDALONE")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "format_docx_standalone"))
    });
    let input_json_text = r#"{"filename":"fake.docx","kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","uri":"$mock_url/docx/fake.docx"}"#.replace("$mock_url", &input_mock_base_url);
    let input_json: serde_json::Value = serde_json::from_str(&input_json_text).unwrap();
    let input = serde_json::from_value::<ExtractInput>(input_json).unwrap();
    let config = Default::default();
    let result = extract(input, &config).await.expect("should succeed");
    assert!(
        result.results[0].content.len() >= 20,
        "expected length >= 20, got {}",
        result.results[0].content.len()
    );
}

#[tokio::test]
async fn test_format_hwpx_standalone() {
    // Standalone HWPX extraction using extract
    let input_mock_base_url = std::env::var("MOCK_SERVER_FORMAT_HWPX_STANDALONE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FORMAT_HWPX_STANDALONE")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "format_hwpx_standalone"))
    });
    let input_json_text = r#"{"filename":"simple.hwpx","kind":"uri","mime_type":"application/haansofthwpx","uri":"$mock_url/hwpx/simple.hwpx"}"#.replace("$mock_url", &input_mock_base_url);
    let input_json: serde_json::Value = serde_json::from_str(&input_json_text).unwrap();
    let input = serde_json::from_value::<ExtractInput>(input_json).unwrap();
    let config = Default::default();
    let result = extract(input, &config).await.expect("should succeed");
    assert!(
        result.results[0].content.len() >= 20,
        "expected length >= 20, got {}",
        result.results[0].content.len()
    );
    assert!(
        format!("{:?}", result.results[0].content).contains(r#"Hello from HWPX"#),
        "expected to contain: {}",
        r#"Hello from HWPX"#
    );
}

#[tokio::test]
async fn test_format_pdf_text() {
    // Standalone PDF text extraction using extract
    let input_mock_base_url = std::env::var("MOCK_SERVER_FORMAT_PDF_TEXT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FORMAT_PDF_TEXT")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "format_pdf_text"))
    });
    let input_json_text = r#"{"filename":"fake_memo.pdf","kind":"uri","mime_type":"application/pdf","uri":"$mock_url/pdf/fake_memo.pdf"}"#.replace("$mock_url", &input_mock_base_url);
    let input_json: serde_json::Value = serde_json::from_str(&input_json_text).unwrap();
    let input = serde_json::from_value::<ExtractInput>(input_json).unwrap();
    let config = Default::default();
    let result = extract(input, &config).await.expect("should succeed");
    assert!(
        result.results[0].content.len() >= 50,
        "expected length >= 50, got {}",
        result.results[0].content.len()
    );
    assert!(
        result.results[0].content.contains(r#"Mallori"#) || result.results[0].content.contains(r#"May"#),
        "expected to contain at least one of the specified values"
    );
}

#[tokio::test]
async fn test_format_pptx() {
    // PPTX presentation extraction using extract
    let input_mock_base_url = std::env::var("MOCK_SERVER_FORMAT_PPTX").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FORMAT_PPTX")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "format_pptx"))
    });
    let input_json_text = r#"{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.presentationml.presentation","uri":"$mock_url/pptx/simple.pptx"}"#.replace("$mock_url", &input_mock_base_url);
    let input_json: serde_json::Value = serde_json::from_str(&input_json_text).unwrap();
    let input = serde_json::from_value::<ExtractInput>(input_json).unwrap();
    let config = Default::default();
    let _ = extract(input, &config).await.expect("should succeed");
}

#[tokio::test]
async fn test_format_xlsx() {
    // XLSX spreadsheet extraction using extract
    let input_mock_base_url = std::env::var("MOCK_SERVER_FORMAT_XLSX").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FORMAT_XLSX")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "format_xlsx"))
    });
    let input_json_text = r#"{"kind":"uri","mime_type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","uri":"$mock_url/xlsx/stanley_cups.xlsx"}"#.replace("$mock_url", &input_mock_base_url);
    let input_json: serde_json::Value = serde_json::from_str(&input_json_text).unwrap();
    let input = serde_json::from_value::<ExtractInput>(input_json).unwrap();
    let config = Default::default();
    let _ = extract(input, &config).await.expect("should succeed");
}
