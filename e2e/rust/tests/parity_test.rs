// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang rust

#[test]
fn test_archive_entry_field_parity() {
    // Verify ArchiveEntry has the expected fields via serde serialization
    let required_fields: &[&str] = &["mime_type", "path", "result"];
    let all_known_fields: &[&str] = &["mime_type", "path", "result"];

    let json = serde_json::to_value(&kreuzberg::ArchiveEntry::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(obj.contains_key(*field), "ArchiveEntry missing required field: {field}");
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "ArchiveEntry has unexpected field: {key}"
        );
    }
}

#[test]
fn test_bounding_box_field_parity() {
    // Verify BoundingBox has the expected fields via serde serialization
    let required_fields: &[&str] = &["x0", "x1", "y0", "y1"];
    let all_known_fields: &[&str] = &["x0", "x1", "y0", "y1"];

    let json = serde_json::to_value(&kreuzberg::BoundingBox::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(obj.contains_key(*field), "BoundingBox missing required field: {field}");
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "BoundingBox has unexpected field: {key}"
        );
    }
}

#[test]
fn test_extraction_config_field_parity() {
    // Verify ExtractionConfig has the expected fields via serde serialization
    let required_fields: &[&str] = &[
        "enable_quality_processing",
        "force_ocr",
        "include_document_structure",
        "max_archive_depth",
        "output_format",
        "result_format",
        "use_cache",
    ];
    let all_known_fields: &[&str] = &[
        "acceleration",
        "cache_namespace",
        "cache_ttl_secs",
        "chunking",
        "concurrency",
        "email",
        "enable_quality_processing",
        "extraction_timeout_secs",
        "force_ocr",
        "force_ocr_pages",
        "html_options",
        "images",
        "include_document_structure",
        "keywords",
        "language_detection",
        "layout",
        "max_archive_depth",
        "max_concurrent_extractions",
        "ocr",
        "output_format",
        "pages",
        "pdf_options",
        "postprocessor",
        "result_format",
        "security_limits",
        "token_reduction",
        "tree_sitter",
        "use_cache",
    ];

    let json = serde_json::to_value(&kreuzberg::ExtractionConfig::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(
            obj.contains_key(*field),
            "ExtractionConfig missing required field: {field}"
        );
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "ExtractionConfig has unexpected field: {key}"
        );
    }
}

#[test]
fn test_extraction_result_field_parity() {
    // Verify ExtractionResult has the expected fields via serde serialization
    let required_fields: &[&str] = &["content", "metadata", "mime_type", "processing_warnings", "tables"];
    let all_known_fields: &[&str] = &[
        "annotations",
        "children",
        "chunks",
        "content",
        "detected_languages",
        "djot_content",
        "document",
        "elements",
        "extracted_keywords",
        "images",
        "metadata",
        "mime_type",
        "ocr_elements",
        "pages",
        "processing_warnings",
        "quality_score",
        "tables",
        "uris",
    ];

    let json = serde_json::to_value(&kreuzberg::ExtractionResult::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(
            obj.contains_key(*field),
            "ExtractionResult missing required field: {field}"
        );
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "ExtractionResult has unexpected field: {key}"
        );
    }
}

#[test]
fn test_keyword_field_parity() {
    // Verify Keyword has the expected fields via serde serialization
    let required_fields: &[&str] = &["algorithm", "score", "text"];
    let all_known_fields: &[&str] = &["algorithm", "positions", "score", "text"];

    let json = serde_json::to_value(&kreuzberg::Keyword::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(obj.contains_key(*field), "Keyword missing required field: {field}");
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "Keyword has unexpected field: {key}"
        );
    }
}

#[test]
fn test_pdf_annotation_field_parity() {
    // Verify PdfAnnotation has the expected fields via serde serialization
    let required_fields: &[&str] = &["annotation_type", "page_number"];
    let all_known_fields: &[&str] = &["annotation_type", "bounding_box", "content", "page_number"];

    let json = serde_json::to_value(&kreuzberg::PdfAnnotation::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(
            obj.contains_key(*field),
            "PdfAnnotation missing required field: {field}"
        );
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "PdfAnnotation has unexpected field: {key}"
        );
    }
}

#[test]
fn test_processing_warning_field_parity() {
    // Verify ProcessingWarning has the expected fields via serde serialization
    let required_fields: &[&str] = &["message", "source"];
    let all_known_fields: &[&str] = &["message", "source"];

    let json = serde_json::to_value(&kreuzberg::ProcessingWarning::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(
            obj.contains_key(*field),
            "ProcessingWarning missing required field: {field}"
        );
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "ProcessingWarning has unexpected field: {key}"
        );
    }
}

#[test]
fn test_table_field_parity() {
    // Verify Table has the expected fields via serde serialization
    let required_fields: &[&str] = &["cells", "markdown", "page_number"];
    let all_known_fields: &[&str] = &["bounding_box", "cells", "markdown", "page_number"];

    let json = serde_json::to_value(&kreuzberg::Table::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(obj.contains_key(*field), "Table missing required field: {field}");
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "Table has unexpected field: {key}"
        );
    }
}

#[test]
fn test_uri_field_parity() {
    // Verify Uri has the expected fields via serde serialization
    let required_fields: &[&str] = &["kind", "url"];
    let all_known_fields: &[&str] = &["kind", "label", "page", "url"];

    let json = serde_json::to_value(&kreuzberg::Uri::default()).unwrap();
    let obj = json.as_object().unwrap();

    // All required fields must be present in the serialized default
    for field in required_fields {
        assert!(obj.contains_key(*field), "Uri missing required field: {field}");
    }

    // No unexpected fields should appear in the serialized output
    for key in obj.keys() {
        assert!(
            all_known_fields.contains(&key.as_str()),
            "Uri has unexpected field: {key}"
        );
    }
}
