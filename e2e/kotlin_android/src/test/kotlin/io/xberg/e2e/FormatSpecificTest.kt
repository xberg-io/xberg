// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.Xberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.ExtractionConfig
import io.xberg.ExtractInput

/** E2e tests for category: format_specific. */
class FormatSpecificTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("xberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load xberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testFormatDocxStandalone() = runBlocking {
        // Standalone DOCX extraction using extract
        val inputMockBaseUrl = System.getProperty("mockServer.format_docx_standalone", System.getenv("MOCK_SERVER_FORMAT_DOCX_STANDALONE") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/format_docx_standalone"))
        val inputJson = "{\"filename\":\"fake.docx\",\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\",\"uri\":\"\$mock_url/docx/fake.docx\"}".replace("\$mock_url", inputMockBaseUrl)
        val input = MAPPER.readValue(inputJson, ExtractInput::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.results.first().content.length >= 20, "expected length >= 20")
    }

    @Test
    fun testFormatHwpxStandalone() = runBlocking {
        // Standalone HWPX extraction using extract
        val inputMockBaseUrl = System.getProperty("mockServer.format_hwpx_standalone", System.getenv("MOCK_SERVER_FORMAT_HWPX_STANDALONE") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/format_hwpx_standalone"))
        val inputJson = "{\"filename\":\"simple.hwpx\",\"kind\":\"uri\",\"mime_type\":\"application/haansofthwpx\",\"uri\":\"\$mock_url/hwpx/simple.hwpx\"}".replace("\$mock_url", inputMockBaseUrl)
        val input = MAPPER.readValue(inputJson, ExtractInput::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.results.first().content.length >= 20, "expected length >= 20")
        assertTrue(result.results.first().content.contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX")
    }

    @Test
    fun testFormatPdfText() = runBlocking {
        // Standalone PDF text extraction using extract
        val inputMockBaseUrl = System.getProperty("mockServer.format_pdf_text", System.getenv("MOCK_SERVER_FORMAT_PDF_TEXT") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/format_pdf_text"))
        val inputJson = "{\"filename\":\"fake_memo.pdf\",\"kind\":\"uri\",\"mime_type\":\"application/pdf\",\"uri\":\"\$mock_url/pdf/fake_memo.pdf\"}".replace("\$mock_url", inputMockBaseUrl)
        val input = MAPPER.readValue(inputJson, ExtractInput::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
        assertTrue(result.results.first().content.length >= 50, "expected length >= 50")
        assertTrue(result.results.first().content.contains("Mallori") || result.results.first().content.contains("May"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testFormatPptx() = runBlocking {
        // PPTX presentation extraction using extract
        val inputMockBaseUrl = System.getProperty("mockServer.format_pptx", System.getenv("MOCK_SERVER_FORMAT_PPTX") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/format_pptx"))
        val inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"uri\":\"\$mock_url/pptx/simple.pptx\"}".replace("\$mock_url", inputMockBaseUrl)
        val input = MAPPER.readValue(inputJson, ExtractInput::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
    }

    @Test
    fun testFormatXlsx() = runBlocking {
        // XLSX spreadsheet extraction using extract
        val inputMockBaseUrl = System.getProperty("mockServer.format_xlsx", System.getenv("MOCK_SERVER_FORMAT_XLSX") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/format_xlsx"))
        val inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"uri\":\"\$mock_url/xlsx/stanley_cups.xlsx\"}".replace("\$mock_url", inputMockBaseUrl)
        val input = MAPPER.readValue(inputJson, ExtractInput::class.java)
        val result = Xberg.extract(input, ExtractionConfig())
    }

}
