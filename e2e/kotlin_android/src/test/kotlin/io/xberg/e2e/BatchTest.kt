// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8bb2cf947e486dda2e680665792a6bb805dd879866ff4dcddb7bbdaea4e8a9cf
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.Xberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.ExtractionConfig
import io.xberg.ExtractInput

/** E2e tests for category: batch. */
class BatchTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("xberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load xberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testExtractBatchBytesHappy() = runBlocking {
        // extract_batch: happy path with mixed inputs
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"bytes\":[72,101,108,108,111,44,32,119,111,114,108,100,33],\"kind\":\"bytes\",\"mime_type\":\"text/plain\"}", ExtractInput::class.java), MAPPER.readValue("{\"bytes\":[60,104,116,109,108,62,60,98,111,100,121,62,84,101,115,116,60,47,98,111,100,121,62,60,47,104,116,109,108,62],\"kind\":\"bytes\",\"mime_type\":\"text/html\"}", ExtractInput::class.java)), ExtractionConfig())
        assertTrue(result.results.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testExtractBatchBytesInvalidMime() = runBlocking {
        // extract_batch with invalid bytes MIME type
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"bytes\":[72,101,108,108,111],\"kind\":\"bytes\",\"mime_type\":\"application/x-nonexistent\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchBytesMixedFormat() = runBlocking {
        // extract_batch: handles unsupported MIME gracefully
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"bytes\":[80,68,70,32,112,108,97,99,101,104,111,108,100,101,114],\"kind\":\"bytes\",\"mime_type\":\"application/x-unknown\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchBytesUnsupportedMime() = runBlocking {
        // extract_batch with unsupported bytes MIME type
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"bytes\":[100,97,116,97],\"kind\":\"bytes\",\"mime_type\":\"application/x-unknown\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchEmptyInputs() = runBlocking {
        // extract_batch: empty batch
        val result = Xberg.extractBatch(listOf(), ExtractionConfig())
        assertEquals(0, result.results.size, "expected exactly 0 elements")
    }

    @Test
    fun testExtractBatchUriAllMissing() = runBlocking {
        // extract_batch with missing URI inputs
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"/nonexistent/a.pdf\"}", ExtractInput::class.java), MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"/nonexistent/b.txt\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchUriBasic() = runBlocking {
        // extract_batch over URI inputs
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"pdf/fake_memo.pdf\"}", ExtractInput::class.java), MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"text/fake_text.txt\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchUriNotFound() = runBlocking {
        // extract_batch with missing URI input
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"/nonexistent/a.pdf\"}", ExtractInput::class.java)), ExtractionConfig())
    }

    @Test
    fun testExtractBatchUriPartialFailure() = runBlocking {
        // extract_batch with mixed valid and missing URI inputs
        val result = Xberg.extractBatch(listOf(MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"text/plain.txt\"}", ExtractInput::class.java), MAPPER.readValue("{\"kind\":\"uri\",\"uri\":\"/nonexistent/missing.pdf\"}", ExtractInput::class.java)), ExtractionConfig())
    }

}
