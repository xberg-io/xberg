// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d976224db1dfa55fdc7f30742fd241959670389f05c3c8ddaa64c2b08e101f5d
// To regenerate: alef e2e generate
// To verify freshness: alef verify
package io.xberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlin.test.assertNotNull
import kotlinx.coroutines.runBlocking
import io.xberg.Xberg
import io.xberg.OcrBackendBridge
import io.xberg.IOcrBackend
import io.xberg.*

/** E2e tests for category: ocr_backend_management. */
class OcrBackendManagementTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            if (System.getProperty("RUST_MIN_STACK") == null) {
                System.setProperty("RUST_MIN_STACK", "16777216")
            }
            try {
                System.loadLibrary("xberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load xberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
    }
    @Test
    fun testOcrBackendCapabilitiesList(): Unit = runBlocking {
        // List all registered OCR backends with their declared language capabilities
        val result = Xberg.listOcrBackendCapabilities()
        assertNotNull(result, "expected non-null result")
    }

    @Test
    fun testOcrBackendSupportsLanguageUnknownBackend(): Unit = runBlocking {
        // Checking language support on an unregistered OCR backend is an error
        assertFailsWith<Exception> {
            Xberg.ocrBackendSupportsLanguage("nonexistent-backend-xyz", "eng")
        }
        Unit
    }

    @Test
    fun testOcrBackendsClear(): Unit = runBlocking {
        // Clear all OCR backends and verify list is empty
        val result = OcrBackendBridge.clearAll()
        assertNotNull(result, "expected non-null result")
    }

    @Test
    fun testOcrBackendsList(): Unit = runBlocking {
        // List all registered OCR backends
        val result = Xberg.listOcrBackends()
        assertNotNull(result, "expected non-null result")
    }

    @Test
    fun testOcrBackendsUnregister(): Unit = runBlocking {
        // Unregister nonexistent OCR backend gracefully
        val result = OcrBackendBridge.unregister("nonexistent-backend-xyz")
        assertNotNull(result, "expected non-null result")
    }

}
