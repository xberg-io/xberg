// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:96b2b58ba8a590655a46347fc96967b5de9d7bdb6b5abd5a9e6ef08cf7b3236d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: html. */
class HtmlTest {
    @Test
    fun testHtmlComplexLayout() {
        // Large Wikipedia HTML page to validate complex conversion.
        val result = Kreuzberg.extractFile("html/taylor_swift.html")
        assertEquals("text/html", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 1000, "expected length >= 1000")
    }

    @Test
    fun testHtmlSimpleTable() {
        // HTML table converted to markdown should retain structure.
        val result = Kreuzberg.extractFile("html/simple_table.html")
        assertEquals("text/html", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
        assertTrue(result.content.orEmpty().contains("Product"), "expected to contain: " + "Product")
        assertTrue(result.content.orEmpty().contains("Category"), "expected to contain: " + "Category")
        assertTrue(result.content.orEmpty().contains("Price"), "expected to contain: " + "Price")
        assertTrue(result.content.orEmpty().contains("Stock"), "expected to contain: " + "Stock")
        assertTrue(result.content.orEmpty().contains("Laptop"), "expected to contain: " + "Laptop")
        assertTrue(result.content.orEmpty().contains("Electronics"), "expected to contain: " + "Electronics")
        assertTrue(result.content.orEmpty().contains("Sample Data Table"), "expected to contain: " + "Sample Data Table")
    }

}
