// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a5da454635021ee8fe41e4dbc87b2ce016f73fa271e7e063826143fee32d35b3
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: archive. */
class ArchiveTest {
    @Test
    fun testArchiveGzBasic() {
        // Gzip compressed file extraction.
        val result = Kreuzberg.extractFile("archives/book_war_and_peace_1p.txt.gz")
        assertTrue(result.mime_type.contains("application/gzip") || result.mime_type.contains("application/x-gzip"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testArchiveSevenzBasic() {
        // 7-Zip archive extraction.
        val result = Kreuzberg.extractFile("archives/documents.7z")
        assertEquals("application/x-7z-compressed", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testArchiveTarBasic() {
        // TAR archive extraction.
        val result = Kreuzberg.extractFile("archives/documents.tar")
        assertTrue(result.mime_type.contains("application/x-tar") || result.mime_type.contains("application/tar"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testArchiveZipBasic() {
        // ZIP archive extraction.
        val result = Kreuzberg.extractFile("archives/documents.zip")
        assertTrue(result.mime_type.contains("application/zip") || result.mime_type.contains("application/x-zip-compressed"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

}
