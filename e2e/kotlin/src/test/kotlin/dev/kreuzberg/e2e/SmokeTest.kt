// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3391084bb3019efc3c53b4ff1e28e5d7584c6e0add7334eabbd200ec47b69295
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: smoke. */
class SmokeTest {
    @Test
    fun testSmokeCacheNamespace() {
        // Smoke test: Extraction with cache namespace and TTL configuration
        val result = Kreuzberg.extractFile("text/report.txt", "{\"cache_namespace\":\"test_tenant\",\"cache_ttl_secs\":3600,\"use_cache\":true}")
        assertEquals("text/plain", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testSmokeDocxBasic() {
        // Smoke test: DOCX with formatted text
        val result = Kreuzberg.extractFile("docx/fake.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
        assertTrue(result.content.orEmpty().contains("Lorem") || result.content.orEmpty().contains("ipsum") || result.content.orEmpty().contains("document") || result.content.orEmpty().contains("text"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testSmokeHtmlBasic() {
        // Smoke test: HTML table extraction
        val result = Kreuzberg.extractFile("html/simple_table.html")
        assertEquals("text/html", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("Sample Data Table") || result.content.orEmpty().contains("Laptop") || result.content.orEmpty().contains("Electronics") || result.content.orEmpty().contains("Product"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testSmokeJsonBasic() {
        // Smoke test: JSON file extraction
        val result = Kreuzberg.extractFile("json/simple.json")
        assertEquals("application/json", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testSmokePdfBasic() {
        // Smoke test: PDF with simple text extraction
        val result = Kreuzberg.extractFile("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("To Whom it May Concern"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testSmokeTxtBasic() {
        // Smoke test: Plain text file
        val result = Kreuzberg.extractFile("text/report.txt")
        assertEquals("text/plain", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testSmokeXlsxBasic() {
        // Smoke test: XLSX with basic spreadsheet data including tables
        val result = Kreuzberg.extractFile("xlsx/stanley_cups.xlsx")
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
        assertTrue(result.content.orEmpty().contains("Team"), "expected to contain: " + "Team")
        assertTrue(result.content.orEmpty().contains("Location"), "expected to contain: " + "Location")
        assertTrue(result.content.orEmpty().contains("Stanley Cups"), "expected to contain: " + "Stanley Cups")
        assertTrue(result.content.orEmpty().contains("Blues"), "expected to contain: " + "Blues")
        assertTrue(result.content.orEmpty().contains("Flyers"), "expected to contain: " + "Flyers")
        assertTrue(result.content.orEmpty().contains("Maple Leafs"), "expected to contain: " + "Maple Leafs")
        assertTrue(result.content.orEmpty().contains("STL"), "expected to contain: " + "STL")
        assertTrue(result.content.orEmpty().contains("PHI"), "expected to contain: " + "PHI")
        assertTrue(result.content.orEmpty().contains("TOR"), "expected to contain: " + "TOR")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
        assertTrue(result.metadata.sheet_count >= 2, "expected >= {kotlin_val}")
        assertTrue(result.metadata.sheet_names.contains("Stanley Cups"), "expected to contain: " + "Stanley Cups")
    }

}
