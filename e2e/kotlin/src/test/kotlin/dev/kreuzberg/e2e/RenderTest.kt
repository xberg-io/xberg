// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:060e26be767386b050ba381f68c49c15e6bd104925f3d2f575e048cb66dc2f65
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: render. */
class RenderTest {
    @Test
    fun testRenderCustomDpi() {
        // Render PDF page with custom DPI
        val result = Kreuzberg.renderPage("pdf/tiny.pdf", 0)
        assertTrue(result.is_png, "expected true")
        assertTrue(result.byte_length >= 50, "expected >= {kotlin_val}")
    }

    @Test
    fun testRenderHighDpi() {
        // Render PDF page with high DPI setting (300 DPI)
        val result = Kreuzberg.renderPage("pdf/fake_memo.pdf", 0, "{\"dpi\":300}")
    }

    @Test
    fun testRenderIterator() {
        // Iterate all PDF pages as PNG images
        val result = Kreuzberg.renderPage("pdf/tiny.pdf", 0)
        assertTrue(result.is_png, "expected true")
        assertTrue(result.page_count >= 1, "expected >= {kotlin_val}")
    }

    @Test
    fun testRenderMultiPage() {
        // Render a page from a multi-page PDF document
        val result = Kreuzberg.renderPage("pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf", 0)
        assertTrue(result.result.length >= 1000, "expected length >= 1000")
    }

    @Test
    fun testRenderOutputDjot() {
        // Extract PDF with Djot output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}")
        assertEquals("djot", result.output_format.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testRenderOutputHtml() {
        // Extract PDF with HTML output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}")
        assertEquals("html", result.output_format.trim())
        assertTrue(result.content.orEmpty().contains("<") || result.content.orEmpty().contains(">") || result.content.orEmpty().contains("</"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testRenderOutputMarkdown() {
        // Extract PDF with Markdown output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
        assertEquals("markdown", result.output_format.trim())
        assertTrue(result.content.orEmpty().contains("#") || result.content.orEmpty().contains("**") || result.content.orEmpty().contains("*"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testRenderOutputPlain() {
        // Extract PDF with plain text output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}")
        assertEquals("plain", result.output_format.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
    }

    @Test
    fun testRenderSinglePage() {
        // Render a single PDF page to PNG
        val result = Kreuzberg.renderPage("pdf/tiny.pdf", 0)
        assertTrue(result.is_png, "expected true")
        assertTrue(result.byte_length >= 100, "expected >= {kotlin_val}")
    }

}
