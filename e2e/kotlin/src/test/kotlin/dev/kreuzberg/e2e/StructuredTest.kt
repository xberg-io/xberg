// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fad05f7a4ce64204d9df7d891025b60ae3ca632ec968d8c0bf179d465765c1ae
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: structured. */
class StructuredTest {
    @Test
    fun testStructuredCsvBasic() {
        // CSV data file extraction.
        val result = Kreuzberg.extractFile("csv/stanley_cups.csv")
        assertEquals("text/csv", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testStructuredEnwBasic() {
        // EndNote ENW citation format extraction.
        val result = Kreuzberg.extractFile("data_formats/sample.enw")
        assertTrue(result.mime_type.contains("application/x-endnote-refer") || result.mime_type.contains("application/x-endnote+xml") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testStructuredJsonBasic() {
        // Structured JSON extraction should stream and preserve content.
        val result = Kreuzberg.extractFile("json/sample_document.json")
        assertEquals("application/json", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
        assertTrue(result.content.orEmpty().contains("Sample Document") || result.content.orEmpty().contains("Test Author"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testStructuredJsonSimple() {
        // Simple JSON document to verify structured extraction.
        val result = Kreuzberg.extractFile("json/simple.json")
        assertEquals("application/json", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("{") || result.content.orEmpty().contains("name"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testStructuredNbibBasic() {
        // PubMed NBIB citation format extraction.
        val result = Kreuzberg.extractFile("data_formats/sample.nbib")
        assertTrue(result.mime_type.contains("application/nbib") || result.mime_type.contains("application/x-pubmed") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testStructuredRisBasic() {
        // RIS citation format extraction.
        val result = Kreuzberg.extractFile("data_formats/sample.ris")
        assertTrue(result.mime_type.contains("application/x-research-info-systems") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testStructuredTomlBasic() {
        // TOML configuration file extraction.
        val result = Kreuzberg.extractFile("data_formats/cargo.toml")
        assertTrue(result.mime_type.contains("application/toml") || result.mime_type.contains("text/toml"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testStructuredTsvBasic() {
        // TSV (tab-separated values) data file extraction.
        val result = Kreuzberg.extractFile("data_formats/employees.tsv")
        assertTrue(result.mime_type.contains("text/tab-separated-values") || result.mime_type.contains("text/plain"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testStructuredYamlBasic() {
        // YAML file text extraction.
        val result = Kreuzberg.extractFile("yaml/simple.yaml")
        assertTrue(result.mime_type.contains("application/yaml") || result.mime_type.contains("text/yaml") || result.mime_type.contains("text/x-yaml") || result.mime_type.contains("application/x-yaml"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testStructuredYamlSimple() {
        // Simple YAML document to validate structured extraction.
        val result = Kreuzberg.extractFile("yaml/simple.yaml")
        assertEquals("application/x-yaml", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

}
