// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a1a24cc79ee709e531e86a5399b2b14811c18f2c9874193232a3162743863aad
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: string_utils. */
class StringUtilsTest {
    @Test
    fun testCamelToSnakeBasic() {
        // Convert camelCase to snake_case
        val result = Kreuzberg.camelToSnake("myVariableName")
        assertEquals("my_variable_name", result.result.trim())
    }

    @Test
    fun testCamelToSnakeSingle() {
        // Convert single word (no camelCase) remains unchanged
        val result = Kreuzberg.camelToSnake("hello")
        assertEquals("hello", result.result.trim())
    }

    @Test
    fun testSanitizeFilenameBasic() {
        // Sanitize path traversal attempts in filename
        val result = Kreuzberg.sanitizeFilename("../../etc/passwd")
        assertTrue(result.result.length >= 1, "expected length >= 1")
    }

    @Test
    fun testSanitizeFilenameSpecial() {
        // Sanitize special characters in filename
        val result = Kreuzberg.sanitizeFilename("file<>name|test.pdf")
        assertTrue(result.result.length >= 1, "expected length >= 1")
    }

    @Test
    fun testSanitizePathBasic() {
        // Sanitize path traversal in file path
        val result = Kreuzberg.sanitizePath("../../../secret/file.txt")
        assertTrue(result.result.length >= 1, "expected length >= 1")
    }

    @Test
    fun testSanitizePathSpaces() {
        // Sanitize path with spaces
        val result = Kreuzberg.sanitizePath("my folder/file.txt")
    }

    @Test
    fun testSanitizePathTraversal() {
        // Sanitize path traversal
        val result = Kreuzberg.sanitizePath("../../../etc/passwd")
    }

    @Test
    fun testSnakeToCamelBasic() {
        // Convert snake_case to camelCase
        val result = Kreuzberg.snakeToCamel("my_variable_name")
        assertEquals("myVariableName", result.result.trim())
    }

    @Test
    fun testSnakeToCamelSingle() {
        // Convert single word remains unchanged
        val result = Kreuzberg.snakeToCamel("hello")
        assertEquals("hello", result.result.trim())
    }

}
