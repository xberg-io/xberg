// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6444ac8f27606f643af69249139fc76140f847934b73b6c2a2262f7c33720e2f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: contract. */
class ContractTest {
    @Test
    fun testApiBatchBytesAsync() {
        // Tests async batch bytes extraction API (batch_extract_bytes)
        val result = Kreuzberg.batchExtractBytes(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiBatchBytesSync() {
        // Tests sync batch bytes extraction API (batch_extract_bytes_sync)
        val result = Kreuzberg.batchExtractBytesSync(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiBatchBytesWithConfigsAsync() {
        // Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)
        val result = Kreuzberg.batchExtractBytes(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testApiBatchBytesWithConfigsSync() {
        // Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)
        val result = Kreuzberg.batchExtractBytesSync(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testApiBatchFileAsync() {
        // Tests async batch file extraction API (batch_extract_file)
        val result = Kreuzberg.batchExtractFile(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiBatchFileSync() {
        // Tests sync batch file extraction API (batch_extract_file_sync)
        val result = Kreuzberg.batchExtractFileSync(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiBatchFileWithConfigsAsync() {
        // Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)
        val result = Kreuzberg.batchExtractFile(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testApiBatchFileWithConfigsSync() {
        // Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)
        val result = Kreuzberg.batchExtractFileSync(null)
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testApiBatchFileWithTimeoutSync() {
        // Tests sync batch file extraction with per-file timeout config override
        val result = Kreuzberg.batchExtractFileSync(null, "{\"extraction_timeout_secs\":300}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testApiExtractBytesAsync() {
        // Tests async bytes extraction API (extract_bytes)
        val result = Kreuzberg.extractBytes(null, "")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiExtractBytesSync() {
        // Tests sync bytes extraction API (extract_bytes_sync)
        val result = Kreuzberg.extractBytesSync(null, "")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiExtractFileAsync() {
        // Tests async file extraction API (extract_file)
        val result = Kreuzberg.extractFile("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testApiExtractFileSync() {
        // Tests sync file extraction API (extract_file_sync)
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testConfigAccelerationCpuProvider() {
        // Tests explicit CPU acceleration provider configuration
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"acceleration\":{\"device_id\":0,\"provider\":\"cpu\"}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("To Whom it May Concern"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testConfigChunking() {
        // Tests chunking configuration with chunk assertions
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.chunks.size >= 1, "expected at least 1 elements")
        assertTrue(result.chunks_have_content, "expected true")
    }

    @Test
    fun testConfigChunkingText() {
        // Tests text chunker type (generic whitespace/punctuation splitter)
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"chunker_type\":\"text\",\"max_chars\":500,\"max_overlap\":50}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.chunks.size >= 1, "expected at least 1 elements")
        assertTrue(result.chunks_have_content, "expected true")
    }

    @Test
    fun testConfigDisableOcr() {
        // Tests disable_ocr configuration option - OCR is skipped for images
        val result = Kreuzberg.extractFileSync("images/test_hello_world.png", "{\"disable_ocr\":true}")
        assertEquals("image/png", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length <= 5, "expected length <= 5")
    }

    @Test
    fun testConfigDocumentStructure() {
        // Tests include_document_structure config produces document tree
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"include_document_structure\":true}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertFalse(result.document_structure.isEmpty(), "expected non-empty value")
        assertTrue(result.document_structure.nodes.size >= 1, "expected at least 1 elements")
        assertTrue(result.document_structure.node_types.contains("paragraph"), "expected to contain: " + "paragraph")
    }

    @Test
    fun testConfigDocumentStructureDisabled() {
        // Tests document field is null when include_document_structure is false
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
    }

    @Test
    fun testConfigDocumentStructureWithHeadings() {
        // Tests document structure with DOCX heading-driven nesting
        val result = Kreuzberg.extractFileSync("docx/fake.docx", "{\"include_document_structure\":true}")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertFalse(result.document_structure.isEmpty(), "expected non-empty value")
        assertTrue(result.document_structure.nodes.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testConfigEmailMsgFallbackCodepage() {
        // Tests MSG extraction with custom fallback codepage for Cyrillic
        val result = Kreuzberg.extractFile("email/fake_email.msg", "{\"email\":{\"msg_fallback_codepage\":1251}}")
        assertEquals("application/vnd.ms-outlook", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testConfigExtractionTimeout() {
        // Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"extraction_timeout_secs\":300}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testConfigHtmlOptions() {
        // Tests extraction with HTML conversion options configured
        val result = Kreuzberg.extractFileSync("html/complex_table.html", "{\"html_options\":{\"extract_metadata\":true}}")
        assertTrue(result.mime_type.contains("text/html"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testConfigHtmlStyledCustomCss() {
        // HTML output with inline user CSS string is embedded verbatim in the style block
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"css\":\".kb-p { color: red; }\",\"embed_css\":true,\"theme\":\"unstyled\"},\"output_format\":\"html\"}")
        assertEquals("html", result.output_format.trim())
        assertTrue(result.content.orEmpty().contains(".kb-p { color: red; }"), "expected to contain: " + ".kb-p { color: red; }")
        assertTrue(result.content.orEmpty().contains("kb-doc"), "expected to contain: " + "kb-doc")
    }

    @Test
    fun testConfigHtmlStyledDefault() {
        // HTML output with default theme emits kb-* class hooks and an embedded style block
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":true,\"theme\":\"default\"},\"output_format\":\"html\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertEquals("html", result.output_format.trim())
        assertTrue(result.content.orEmpty().contains("kb-doc"), "expected to contain: " + "kb-doc")
        assertTrue(result.content.orEmpty().contains("kb-content"), "expected to contain: " + "kb-content")
        assertTrue(result.content.orEmpty().contains("kb-p"), "expected to contain: " + "kb-p")
        assertTrue(result.content.orEmpty().contains("<style>"), "expected to contain: " + "<style>")
    }

    @Test
    fun testConfigHtmlStyledNoEmbed() {
        // HTML output with embed_css false emits class hooks but no style block
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"html_output\":{\"embed_css\":false,\"theme\":\"default\"},\"output_format\":\"html\"}")
        assertEquals("html", result.output_format.trim())
        assertTrue(result.content.orEmpty().contains("kb-doc"), "expected to contain: " + "kb-doc")
        assertTrue(result.content.orEmpty().contains("kb-content"), "expected to contain: " + "kb-content")
    }

    @Test
    fun testConfigImages() {
        // Tests image extraction configuration with image assertions
        val result = Kreuzberg.extractFileSync("pdf/embedded_images_tables.pdf", "{\"images\":{\"extract_images\":true}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.images.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testConfigImagesWithFormats() {
        // Tests image extraction on PPTX containing embedded images
        val result = Kreuzberg.extractFileSync("pptx/powerpoint_with_image.pptx", "{\"images\":{\"extract_images\":true}}")
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mime_type.trim())
        assertTrue(result.images.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testConfigLanguageDetection() {
        // Tests language detection configuration
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"language_detection\":{\"enabled\":true}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.detected_languages.contains("eng"), "expected to contain: " + "eng")
        assertTrue(result.language_detection_confidence >= 0.5d, "expected >= {kotlin_val}")
    }

    @Test
    fun testConfigLanguageDetectionMulti() {
        // Tests language detection with detect_multiple and min_confidence options
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"language_detection\":{\"detect_multiple\":true,\"enabled\":true,\"min_confidence\":0.3}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.detected_languages.contains("eng"), "expected to contain: " + "eng")
    }

    @Test
    fun testConfigPostprocessor() {
        // Tests postprocessor config is accepted and extraction succeeds
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"postprocessor\":{\"enabled\":true}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testConfigProcessingWarningsEmpty() {
        // Tests that a clean PDF extraction produces no processing warnings
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.processing_warnings.isEmpty(), "expected empty value")
    }

    @Test
    fun testConfigQualityDisabled() {
        // Tests extraction with quality processing explicitly disabled
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"enable_quality_processing\":false}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testConfigSecurityLimits() {
        // Tests archive extraction with custom security limits
        val result = Kreuzberg.extractFileSync("archives/documents.zip", "{\"security_limits\":{\"max_archive_size\":104857600,\"max_compression_ratio\":50,\"max_files_in_archive\":100}}")
        assertTrue(result.mime_type.contains("application/zip") || result.mime_type.contains("application/x-zip-compressed"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testConfigTablesContent() {
        // Tests table extraction with content_contains_any assertion on DOCX with tables
        val result = Kreuzberg.extractFileSync("docx/docx_tables.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testConfigUseCacheFalse() {
        // Tests use_cache=false configuration option
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"use_cache\":false}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOutputFormatBytesMarkdown() {
        // Tests markdown output format via bytes extraction API
        val result = Kreuzberg.extractBytesSync(null, "", "{\"output_format\":\"markdown\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testOutputFormatDjot() {
        // Tests Djot output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"djot\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("djot", result.output_format.trim())
    }

    @Test
    fun testOutputFormatHtml() {
        // Tests HTML output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"html\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("html", result.output_format.trim())
    }

    @Test
    fun testOutputFormatMarkdown() {
        // Tests Markdown output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"markdown\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("markdown", result.output_format.trim())
    }

    @Test
    fun testOutputFormatPlain() {
        // Tests Plain output format
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"output_format\":\"plain\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("plain", result.output_format.trim())
    }

    @Test
    fun testResultFormatElementBased() {
        // Tests ElementBased result format with element assertions
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"result_format\":\"element_based\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertEquals("element_based", result.result_format.trim())
        assertTrue(result.elements.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testResultFormatUnified() {
        // Tests Unified result format (default)
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"result_format\":\"unified\"}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertEquals("unified", result.result_format.trim())
    }

}
