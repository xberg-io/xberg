// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c929feed55f381e949eab8aee5a3b95720f718e3469ec23c682e0b7203ca8674
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: error. */
class ErrorTest {
    @Test
    fun testErrorEmptyBytes() {
        // Graceful handling of empty bytes (should not error)
        val result = Kreuzberg.extractBytesSync("text/empty.txt".toByteArray(), "text/plain")
    }

    @Test
    fun testErrorEmptyMime() {
        // Error when extracting with empty MIME type
        assertFailsWith<Exception> { Kreuzberg.extractBytesSync("text/plain.txt".toByteArray(), "") }
    }

    @Test
    fun testErrorExtractBytesConflictingOcr() {
        // extract_bytes force+disable OCR
        assertFailsWith<Exception> { Kreuzberg.extractBytesSync("SGVsbG8=".toByteArray(), "text/plain", "{\"disable_ocr\":true,\"force_ocr\":true}") }
    }

    @Test
    fun testErrorFileNotFound() {
        // Error when file does not exist (sync)
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("nonexistent/file.pdf") }
    }

    @Test
    fun testErrorFileNotFoundAsync() {
        // Error when file does not exist (async)
        assertFailsWith<Exception> { Kreuzberg.extractFile("nonexistent/file.pdf") }
    }

    @Test
    fun testErrorInvalidChunkOverlap() {
        // Error when chunk overlap exceeds max_characters
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_characters\":10,\"overlap_characters\":100}}") }
    }

    @Test
    fun testErrorInvalidConfidence() {
        // Error when OCR confidence threshold is out of range
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"ocr\":{\"confidence_threshold\":2.0}}") }
    }

    @Test
    fun testErrorInvalidDpi() {
        // Error when OCR DPI is invalid (negative)
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"ocr\":{\"dpi\":-1}}") }
    }

    @Test
    fun testErrorInvalidMimeFormat() {
        // Error when extracting with invalid MIME type format
        assertFailsWith<Exception> { Kreuzberg.extractBytesSync("text/plain.txt".toByteArray(), "not-a-mime") }
    }

    @Test
    fun testErrorInvalidOcrLanguage() {
        // Error when OCR language pack is not available
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("images/balance_sheet_1.png", "{\"ocr\":{\"force_ocr\":true,\"language\":\"zzz_nonexistent\"}}") }
    }

    @Test
    fun testErrorPdfCorrupt() {
        // Error when PDF is truncated/corrupt
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/corrupt_truncated.pdf") }
    }

    @Test
    fun testErrorPdfNeedsPassword() {
        // Error when password-protected PDF is accessed without password
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/password_protected.pdf") }
    }

    @Test
    fun testErrorPdfPageOutOfRange() {
        // Graceful handling of out-of-range page extraction
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"pages\":\"999-1000\"}")
    }

    @Test
    fun testErrorPdfWrongPassword() {
        // Error when PDF password is incorrect
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/password_protected.pdf", "{\"pdf\":{\"password\":\"wrongpassword\"}}") }
    }

    @Test
    fun testErrorRenderCorruptPdf() {
        // Error when rendering page from corrupt PDF
        assertFailsWith<Exception> { Kreuzberg.renderPage("pdf/corrupt_truncated.pdf", 0) }
    }

    @Test
    fun testErrorRenderInvalidPage() {
        // Error when rendering page number exceeds document page count
        assertFailsWith<Exception> { Kreuzberg.renderPage("pdf/fake_memo.pdf", 999) }
    }

    @Test
    fun testErrorSecurityArchiveFiles() {
        // Error when archive file count exceeds security limit
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("archives/documents.zip", "{\"security_limits\":{\"max_files_in_archive\":0}}") }
    }

    @Test
    fun testErrorSecurityArchiveSize() {
        // Error when archive size exceeds security limit
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("archives/documents.zip", "{\"security_limits\":{\"max_archive_size\":1}}") }
    }

    @Test
    fun testErrorSecurityContentTooLarge() {
        // Error when content exceeds security limit for max size
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"security_limits\":{\"max_content_size\":10}}") }
    }

    @Test
    fun testErrorSecurityNestingDepth() {
        // Error when XML nesting depth exceeds security limit
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("html/complex_table.html", "{\"security_limits\":{\"max_nesting_depth\":1}}") }
    }

    @Test
    fun testErrorTimeoutExtraction() {
        // Error when extraction times out
        assertFailsWith<Exception> { Kreuzberg.extractFileSync("pdf/large.pdf", "{\"extraction_timeout_secs\":1}") }
    }

    @Test
    fun testErrorUnsupportedMime() {
        // Error when extracting with unsupported MIME type
        assertFailsWith<Exception> { Kreuzberg.extractBytesSync("text/plain.txt".toByteArray(), "application/x-nonexistent") }
    }

}
