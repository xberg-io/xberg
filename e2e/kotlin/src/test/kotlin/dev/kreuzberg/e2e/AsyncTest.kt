// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4dfe91f65f23d3ac8ff94b9e7ba20a4e975107d3567ab6c4f80fa3557266a7a0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: async. */
class AsyncTest {
    @Test
    fun testAsyncExtractBytes() {
        // Async extract_bytes call on PDF document
        val result = Kreuzberg.extractBytes(null, "application/pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
    }

    @Test
    fun testAsyncExtractBytesEmptyMime() {
        // extract_bytes empty MIME async
        assertFailsWith<Exception> { Kreuzberg.extractBytes("SGVsbG8=".toByteArray(), "", "{}") }
    }

    @Test
    fun testAsyncExtractBytesInvalidMime() {
        // extract_bytes unsupported MIME async
        assertFailsWith<Exception> { Kreuzberg.extractBytes("SGVsbG8=".toByteArray(), "application/x-nonexistent", "{}") }
    }

    @Test
    fun testAsyncExtractFile() {
        // Async extract_file call on PDF document
        val result = Kreuzberg.extractFile("pdf/fake_memo.pdf", "application/pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
    }

    @Test
    fun testAsyncExtractFileDocx() {
        // Async extract_file call on DOCX document
        val result = Kreuzberg.extractFile("docx/fake.docx", "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testAsyncExtractFileHtml() {
        // Async extract_file call on HTML document
        val result = Kreuzberg.extractFile("html/simple_table.html", "text/html")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testAsyncExtractFileImage() {
        // Async extract_file call on image document
        val result = Kreuzberg.extractFile("images/test_hello_world.png", "image/png")
    }

    @Test
    fun testAsyncExtractFileWithConfig() {
        // Async extract_file call with output format and chunking config
        val result = Kreuzberg.extractFile("pdf/fake_memo.pdf", "application/pdf", "{\"chunking\":{\"max_characters\":500,\"max_overlap\":50},\"output_format\":\"markdown\"}")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

}
