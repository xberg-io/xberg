// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0b5aa0d1773f1c077d313ae6c4772c521d01e2dc2d2a33ad176b4a13a1448e4d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: office. */
class OfficeTest {
    @Test
    fun testOfficeBibtexBasic() {
        // BibTeX bibliography extraction.
        val result = Kreuzberg.extractFile("bibtex/comprehensive.bib")
        assertTrue(result.mime_type.contains("application/x-bibtex") || result.mime_type.contains("text/x-bibtex"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeCommonmarkBasic() {
        // CommonMark (.commonmark) text extraction.
        val result = Kreuzberg.extractFile("markdown/sample.commonmark")
        assertTrue(result.mime_type.contains("text/markdown") || result.mime_type.contains("text/plain") || result.mime_type.contains("text/x-commonmark"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testOfficeDjotBasic() {
        // Djot markup text extraction.
        val result = Kreuzberg.extractFile("markdown/tables.djot")
        assertTrue(result.mime_type.contains("text/x-djot") || result.mime_type.contains("text/djot"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeDocbookBasic() {
        // DocBook XML document extraction.
        val result = Kreuzberg.extractFile("docbook/docbook-reader.docbook")
        assertTrue(result.mime_type.contains("application/docbook+xml") || result.mime_type.contains("text/docbook"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeDocxBasic() {
        // DOCX document extraction baseline.
        val result = Kreuzberg.extractFile("docx/sample_document.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeDocxEquations() {
        // DOCX file containing equations to validate math extraction.
        val result = Kreuzberg.extractFile("docx/equations.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeDocxFake() {
        // Simple DOCX document to verify baseline extraction.
        val result = Kreuzberg.extractFile("docx/fake.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeDocxFormatting() {
        // DOCX document heavy on formatting for style preservation.
        val result = Kreuzberg.extractFile("docx/unit_test_formatting.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeDocxHeaders() {
        // DOCX document with complex headers.
        val result = Kreuzberg.extractFile("docx/unit_test_headers.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeDocxLists() {
        // DOCX document emphasizing list formatting.
        val result = Kreuzberg.extractFile("docx/unit_test_lists.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeDocxTables() {
        // DOCX document containing tables for table-aware extraction.
        val result = Kreuzberg.extractFile("docx/docx_tables.docx")
        assertEquals("application/vnd.openxmlformats-officedocument.wordprocessingml.document", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("Simple uniform table"), "expected to contain: " + "Simple uniform table")
        assertTrue(result.content.orEmpty().contains("Nested Table"), "expected to contain: " + "Nested Table")
        assertTrue(result.content.orEmpty().contains("merged cells"), "expected to contain: " + "merged cells")
        assertTrue(result.content.orEmpty().contains("Header Col"), "expected to contain: " + "Header Col")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testOfficeEpubBasic() {
        // EPUB book extraction with text content.
        val result = Kreuzberg.extractFile("epub/features.epub")
        assertEquals("application/epub+zip", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
    }

    @Test
    fun testOfficeFb2Basic() {
        // FictionBook (FB2) document extraction baseline.
        val result = Kreuzberg.extractFile("fictionbook/basic.fb2")
        assertEquals("application/x-fictionbook+xml", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeFictionbookBasic() {
        // FictionBook (.fb2) text extraction.
        val result = Kreuzberg.extractFile("fictionbook/basic.fb2")
        assertTrue(result.mime_type.contains("application/x-fictionbook+xml") || result.mime_type.contains("application/x-fictionbook"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeJatsBasic() {
        // JATS scientific article extraction.
        val result = Kreuzberg.extractFile("jats/sample_article.jats")
        assertTrue(result.mime_type.contains("application/x-jats+xml") || result.mime_type.contains("text/jats"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeJupyterBasic() {
        // Jupyter notebook extraction.
        val result = Kreuzberg.extractFile("jupyter/rank.ipynb")
        assertEquals("application/x-ipynb+json", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeKeynoteBasic() {
        // Keynote document extraction baseline.
        val result = Kreuzberg.extractFile("iwork/test.key")
        assertEquals("application/x-iwork-keynote-sffkey", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testOfficeLatexBasic() {
        // LaTeX document text extraction.
        val result = Kreuzberg.extractFile("latex/basic_sections.tex")
        assertTrue(result.mime_type.contains("application/x-latex") || result.mime_type.contains("text/x-latex"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeMarkdownBasic() {
        // Markdown document extraction baseline.
        val result = Kreuzberg.extractFile("markdown/comprehensive.md")
        assertEquals("text/markdown", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeNumbersBasic() {
        // Numbers document extraction baseline.
        val result = Kreuzberg.extractFile("iwork/test.numbers")
        assertEquals("application/x-iwork-numbers-sffnumbers", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeOdsBasic() {
        // Basic ODS spreadsheet extraction.
        val result = Kreuzberg.extractFile("data_formats/test_01.ods")
        assertEquals("application/vnd.oasis.opendocument.spreadsheet", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeOdtBold() {
        // ODT document with bold formatting.
        val result = Kreuzberg.extractFile("odt/bold.odt")
        assertEquals("application/vnd.oasis.opendocument.text", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeOdtList() {
        // ODT document containing unordered lists with nesting.
        val result = Kreuzberg.extractFile("odt/unorderedList.odt")
        assertEquals("application/vnd.oasis.opendocument.text", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 30, "expected length >= 30")
        assertTrue(result.content.orEmpty().contains("list item") || result.content.orEmpty().contains("New level") || result.content.orEmpty().contains("Pushed us"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testOfficeOdtSimple() {
        // Basic ODT document with paragraphs and headings.
        val result = Kreuzberg.extractFile("odt/simple.odt")
        assertEquals("application/vnd.oasis.opendocument.text", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("favorite things") || result.content.orEmpty().contains("Parrots") || result.content.orEmpty().contains("Analysis"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testOfficeOdtTable() {
        // ODT document with a table structure.
        val result = Kreuzberg.extractFile("odt/table.odt")
        assertEquals("application/vnd.oasis.opendocument.text", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
    }

    @Test
    fun testOfficeOpmlBasic() {
        // OPML outline document extraction.
        val result = Kreuzberg.extractFile("opml/outline.opml")
        assertTrue(result.mime_type.contains("application/xml+opml") || result.mime_type.contains("text/x-opml") || result.mime_type.contains("application/x-opml+xml"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeOrgBasic() {
        // Org-mode document text extraction.
        val result = Kreuzberg.extractFile("org/comprehensive.org")
        assertTrue(result.mime_type.contains("text/x-org") || result.mime_type.contains("text/org"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficePagesBasic() {
        // Pages document extraction baseline.
        val result = Kreuzberg.extractFile("iwork/test.pages")
        assertEquals("application/x-iwork-pages-sffpages", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testOfficePpsxSlideshow() {
        // PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.
        val result = Kreuzberg.extractFile("pptx/sample.ppsx")
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.slideshow", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficePptmBasic() {
        // PowerPoint macro-enabled presentation (.pptm) extraction.
        val result = Kreuzberg.extractFile("pptx/powerpoint_with_image.pptm")
        assertTrue(result.mime_type.contains("application/vnd.ms-powerpoint.presentation.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.presentationml.presentation"), "expected to contain at least one of the specified values")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testOfficePptxBasic() {
        // PPTX deck should extract slides content.
        val result = Kreuzberg.extractFile("pptx/simple.pptx")
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
    }

    @Test
    fun testOfficePptxImages() {
        // PPTX presentation containing images to ensure metadata extraction.
        val result = Kreuzberg.extractFile("pptx/powerpoint_with_image.pptx")
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 15, "expected length >= 15")
    }

    @Test
    fun testOfficePptxPitchDeck() {
        // Pitch deck PPTX used to validate large slide extraction.
        val result = Kreuzberg.extractFile("pptx/pitch_deck_presentation.pptx")
        assertEquals("application/vnd.openxmlformats-officedocument.presentationml.presentation", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
    }

    @Test
    fun testOfficeRstBasic() {
        // reStructuredText document extraction.
        val result = Kreuzberg.extractFile("rst/restructured_text.rst")
        assertTrue(result.mime_type.contains("text/x-rst") || result.mime_type.contains("text/prs.fallenstein.rst"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testOfficeRtfBasic() {
        // RTF document text extraction.
        val result = Kreuzberg.extractFile("rtf/extraction_test.rtf")
        assertTrue(result.mime_type.contains("application/rtf") || result.mime_type.contains("text/rtf"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeTypstBasic() {
        // Typst document text extraction.
        val result = Kreuzberg.extractFile("typst/headings.typ")
        assertTrue(result.mime_type.contains("application/x-typst") || result.mime_type.contains("text/x-typst"), "expected to contain at least one of the specified values")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeXlsLegacy() {
        // Legacy XLS spreadsheet to ensure backward compatibility.
        val result = Kreuzberg.extractFile("xls/test_excel.xls")
        assertEquals("application/vnd.ms-excel", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testOfficeXlsbBasic() {
        // Excel binary workbook (.xlsb) extraction.
        val result = Kreuzberg.extractFile("xlsx/test_xlsb.xlsb")
        assertTrue(result.mime_type.contains("application/vnd.ms-excel.sheet.binary.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testOfficeXlsmBasic() {
        // Excel macro-enabled workbook (.xlsm) extraction.
        val result = Kreuzberg.extractFile("xlsx/test_01.xlsm")
        assertTrue(result.mime_type.contains("application/vnd.ms-excel.sheet.macroEnabled.12") || result.mime_type.contains("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"), "expected to contain at least one of the specified values")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testOfficeXlsxBasic() {
        // XLSX spreadsheet should produce metadata and table content.
        val result = Kreuzberg.extractFile("xlsx/stanley_cups.xlsx")
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
        assertTrue(result.content.orEmpty().contains("Team"), "expected to contain: " + "Team")
        assertTrue(result.content.orEmpty().contains("Location"), "expected to contain: " + "Location")
        assertTrue(result.content.orEmpty().contains("Stanley Cups"), "expected to contain: " + "Stanley Cups")
        assertTrue(result.tables.size >= 1, "expected at least 1 elements")
        assertTrue(result.metadata.sheet_count >= 2, "expected >= {kotlin_val}")
        assertTrue(result.metadata.sheet_names.contains("Stanley Cups"), "expected to contain: " + "Stanley Cups")
    }

    @Test
    fun testOfficeXlsxMultiSheet() {
        // XLSX workbook with multiple sheets.
        val result = Kreuzberg.extractFile("xlsx/excel_multi_sheet.xlsx")
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
        assertTrue(result.metadata.sheet_count >= 2, "expected >= {kotlin_val}")
    }

    @Test
    fun testOfficeXlsxOfficeExample() {
        // Simple XLSX spreadsheet shipped alongside office integration tests.
        val result = Kreuzberg.extractFile("xlsx/test_01.xlsx")
        assertEquals("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

}
