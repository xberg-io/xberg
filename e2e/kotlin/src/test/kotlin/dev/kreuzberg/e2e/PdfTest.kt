// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6f002a1a03dc340dbc162a55a85cc2b891d7e104fb2385126b0858a82289eb38
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: pdf. */
class PdfTest {
    @Test
    fun testPdfAssemblyTechnical() {
        // Assembly language technical manual with large body of text.
        val result = Kreuzberg.extractFile("pdf/assembly_language_for_beginners_al4_b_en.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5000, "expected length >= 5000")
        assertTrue(result.content.orEmpty().contains("assembly") || result.content.orEmpty().contains("register") || result.content.orEmpty().contains("instruction"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfBayesianDataAnalysis() {
        // Bayesian data analysis textbook PDF with large content volume.
        val result = Kreuzberg.extractFile("pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10000, "expected length >= 10000")
        assertTrue(result.content.orEmpty().contains("Bayesian") || result.content.orEmpty().contains("probability") || result.content.orEmpty().contains("distribution"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfCodeAndFormula() {
        // PDF containing code snippets and formulas should retain substantial content.
        val result = Kreuzberg.extractFile("pdf/code_and_formula.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
    }

    @Test
    fun testPdfDeepLearning() {
        // Deep learning textbook PDF to ensure long-form extraction quality.
        val result = Kreuzberg.extractFile("pdf/fundamentals_of_deep_learning_2014.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 1000, "expected length >= 1000")
        assertTrue(result.content.orEmpty().contains("neural") || result.content.orEmpty().contains("network") || result.content.orEmpty().contains("deep learning"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfEmbeddedImages() {
        // PDF with embedded images should extract text and tables when present.
        val result = Kreuzberg.extractFile("pdf/embedded_images_tables.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.tables.size >= 0, "expected at least 0 elements")
    }

    @Test
    fun testPdfGoogleDoc() {
        // Google Docs exported PDF to verify conversion fidelity.
        val result = Kreuzberg.extractFile("pdf/google_doc_document.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfLargeCiml() {
        // Large machine learning textbook PDF to stress extraction length.
        val result = Kreuzberg.extractFile("pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10000, "expected length >= 10000")
        assertTrue(result.content.orEmpty().contains("machine learning") || result.content.orEmpty().contains("algorithm") || result.content.orEmpty().contains("training"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfNonEnglishGerman() {
        // German technical PDF to ensure non-ASCII content extraction.
        val result = Kreuzberg.extractFile("pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
        assertTrue(result.content.orEmpty().contains("Intel") || result.content.orEmpty().contains("paging"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfRightToLeft() {
        // Right-to-left language PDF to verify RTL extraction.
        val result = Kreuzberg.extractFile("pdf/right_to_left_01.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

    @Test
    fun testPdfSimpleText() {
        // Simple text-heavy PDF should extract content without OCR or tables.
        val result = Kreuzberg.extractFile("pdf/fake_memo.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("May 5, 2023") || result.content.orEmpty().contains("To Whom it May Concern") || result.content.orEmpty().contains("Mallori"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testPdfTablesLarge() {
        // Large PDF with extensive tables to stress table extraction.
        val result = Kreuzberg.extractFile("pdf/large.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 500, "expected length >= 500")
    }

    @Test
    fun testPdfTablesMedium() {
        // Medium-sized PDF with multiple tables.
        val result = Kreuzberg.extractFile("pdf/medium.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 100, "expected length >= 100")
    }

    @Test
    fun testPdfTechnicalStatLearning() {
        // Technical statistical learning PDF requiring substantial extraction.
        val result = Kreuzberg.extractFile("pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10000, "expected length >= 10000")
        assertTrue(result.content.orEmpty().contains("statistical") || result.content.orEmpty().contains("regression") || result.content.orEmpty().contains("learning"), "expected to contain at least one of the specified values")
        assertEquals("pdf", result.metadata.format_type.trim())
    }

}
