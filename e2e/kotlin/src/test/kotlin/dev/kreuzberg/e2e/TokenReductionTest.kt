// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c2ee35843e995bbbfed92369dcb6825590230fcb827a79ce251b3369e68830a7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: token_reduction. */
class TokenReductionTest {
    @Test
    fun testTokenReductionAggressive() {
        // Tests aggressive token reduction mode significantly reduces content
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"aggressive\"}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
        assertTrue(result.content.orEmpty().length <= 150, "expected length <= 150")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testTokenReductionBasic() {
        // Tests basic token reduction on PDF document
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"moderate\"}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
        assertTrue(result.content.orEmpty().length <= 200, "expected length <= 200")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testTokenReductionLight() {
        // Tests light token reduction mode preserves most content
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"token_reduction\":{\"mode\":\"light\"}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
    }

    @Test
    fun testTokenReductionReduceAggressive() {
        // Reduce tokens with aggressive compression level
        val result = Kreuzberg.reduceTokens("The quick brown fox jumps over the lazy dog.", "aggressive")
        assertTrue(result.result.length >= 1, "expected length >= 1")
    }

    @Test
    fun testTokenReductionReduceBasic() {
        // Reduce tokens with light compression level
        val result = Kreuzberg.reduceTokens("The quick brown fox jumps over the lazy dog. This is a test sentence.", "light")
        assertTrue(result.result.contains("quick"), "expected to contain: " + "quick")
    }

    @Test
    fun testTokenReductionWithChunking() {
        // Tests token reduction combined with chunking
        val result = Kreuzberg.extractFileSync("pdf/fake_memo.pdf", "{\"chunking\":{\"max_chars\":500,\"max_overlap\":50},\"token_reduction\":{\"mode\":\"moderate\"}}")
        assertEquals("application/pdf", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
        assertTrue(result.content.orEmpty().length <= 200, "expected length <= 200")
        assertFalse(result.content.orEmpty().isEmpty(), "expected non-empty value")
        assertTrue(result.chunks.size >= 1, "expected at least 1 elements")
        assertTrue(result.chunks_have_content, "expected true")
    }

}
