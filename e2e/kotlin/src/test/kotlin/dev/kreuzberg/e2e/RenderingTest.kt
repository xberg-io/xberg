// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b1f2887191de5d416e8785f4e4ddf8c1034434e976805c2503335f6764438ba8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: rendering. */
class RenderingTest {
    @Test
    fun testConvertHtmlBasic() {
        // Convert basic HTML to Markdown format
        val result = Kreuzberg.convertHtmlToMarkdown("<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
        assertTrue(result.content.orEmpty().contains("Title"), "expected to contain: " + "Title")
    }

    @Test
    fun testConvertHtmlComplex() {
        // Convert complex HTML to markdown
        val result = Kreuzberg.convertHtmlToMarkdown("<h1>Title</h1><p>Text with <strong>bold</strong> and <a href=\"https://example.com\">link</a>.</p>")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testConvertHtmlEmpty() {
        // Convert empty HTML to markdown
        val result = Kreuzberg.convertHtmlToMarkdown("")
    }

    @Test
    fun testConvertHtmlTable() {
        // Convert HTML table to Markdown table format
        val result = Kreuzberg.convertHtmlToMarkdown("<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>")
        assertTrue(result.content.orEmpty().contains("|"), "expected to contain: " + "|")
    }

    @Test
    fun testDjotToHtmlBasic() {
        // Convert Djot format to HTML
        val result = Kreuzberg.djotToHtml("# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testDjotToHtmlComplex() {
        // Complex djot to HTML
        val result = Kreuzberg.djotToHtml("# Heading\n\nParagraph.\n\n- Item one\n- Item two")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testDjotToHtmlEmpty() {
        // Convert empty djot to HTML
        val result = Kreuzberg.djotToHtml("")
    }

    @Test
    fun testRenderDjotBasic() {
        // Render HTML document to Djot format
        val result = Kreuzberg.renderDjot("<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderDjotFormatted() {
        // Render HTML to djot
        val result = Kreuzberg.renderDjot("<h2>Section</h2><p>Text with <code>code</code>.</p>")
    }

    @Test
    fun testRenderHtmlBasic() {
        // Render a simple HTML document to formatted output
        val result = Kreuzberg.renderHtml("<h1>Test Document</h1><p>This is a paragraph with some test content.</p>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderHtmlStyled() {
        // Render HTML preserving structure
        val result = Kreuzberg.renderHtml("<h1>Heading</h1><p><strong>bold</strong></p>")
        assertTrue(result.content.orEmpty().contains("Heading"), "expected to contain: " + "Heading")
    }

    @Test
    fun testRenderJsonBasic() {
        // Render HTML document to JSON format
        val result = Kreuzberg.renderJson("<h1>Structured Data</h1><p>Content for JSON rendering.</p>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderJsonComplex() {
        // Render HTML to JSON
        val result = Kreuzberg.renderJson("<h1>Title</h1><p>Text.</p>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderMarkdownBasic() {
        // Render HTML document to Markdown format
        val result = Kreuzberg.renderMarkdown("<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderMarkdownComplex() {
        // Render complex HTML to markdown
        val result = Kreuzberg.renderMarkdown("<h1>Title</h1><ul><li>A</li><li>B</li></ul>")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testRenderPlainBasic() {
        // Render HTML document to plain text format
        val result = Kreuzberg.renderPlain("<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testRenderPlainEmpty() {
        // Render empty HTML
        val result = Kreuzberg.renderPlain("")
    }

}
