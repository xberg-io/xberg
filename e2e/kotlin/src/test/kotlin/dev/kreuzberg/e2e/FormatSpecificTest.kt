// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d80df6efee5a715046c0578ef5ffc1a717aaac73733cf63c2329aeb811956e96
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: format_specific. */
class FormatSpecificTest {
    @Test
    fun testFormatCsv() {
        // CSV file extraction using extract_file_sync
        val result = Kreuzberg.extractFileSync("csv/data_table.csv")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testFormatDocxStandalone() {
        // Standalone DOCX extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(null, "application/vnd.openxmlformats-officedocument.wordprocessingml.document")
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

    @Test
    fun testFormatEmailEml() {
        // Email EML file extraction using extract_file_sync
        val result = Kreuzberg.extractFileSync("email/html_email_multipart.eml")
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testFormatPdfText() {
        // Standalone PDF text extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(null, "application/pdf")
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("Mallori") || result.content.orEmpty().contains("May"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testFormatPptx() {
        // PPTX presentation extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(null, "application/vnd.openxmlformats-officedocument.presentationml.presentation")
    }

    @Test
    fun testFormatRtf() {
        // RTF file extraction using extract_file_sync
        val result = Kreuzberg.extractFileSync("rtf/extraction_test.rtf")
        assertTrue(result.content.orEmpty().length >= 5, "expected length >= 5")
    }

    @Test
    fun testFormatXlsx() {
        // XLSX spreadsheet extraction using extract_bytes_sync
        val result = Kreuzberg.extractBytesSync(null, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
    }

}
