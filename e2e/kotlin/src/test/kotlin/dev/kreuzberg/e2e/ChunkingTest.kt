// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:dd5c8f7c539d010d14de5559cf495d6a572d73c54ce3fdeb6a097e066a140eb0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: chunking. */
class ChunkingTest {
    @Test
    fun testChunkBatchBasic() {
        // Batch chunking of multiple texts in a single call
        val result = Kreuzberg.chunkTextsBatch(listOf("Hello world. This is text one.", "Goodbye world. This is text two."), "{\"max_characters\":100,\"max_overlap\":10}")
    }

    @Test
    fun testChunkTextBasic() {
        // Basic text chunking with small chunk size and overlap
        val result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":10}")
    }

    @Test
    fun testChunkTextLarge() {
        // Text chunking with large chunk size to test handling of long content
        val result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":1000,\"max_overlap\":10}")
    }

    @Test
    fun testChunkTextNoOverlap() {
        // Text chunking with no overlap between chunks
        val result = Kreuzberg.chunkText("The quick brown fox jumps over the lazy dog. This is a long paragraph that should be split into multiple chunks when the chunk size is small enough. Each chunk should contain coherent text.", "{\"max_characters\":50,\"max_overlap\":0}")
    }

    @Test
    fun testChunkTextOverlapExceeds() {
        // chunk_text overlap >= max
        assertFailsWith<Exception> { Kreuzberg.chunkText("Some text.", "{\"max_characters\":50,\"overlap\":60}") }
    }

    @Test
    fun testChunkTextYaml() {
        // Chunk with YAML chunker
        val result = Kreuzberg.chunkText("key1: value1\nkey2: value2\n---\nkey3: value3", "{\"chunker_type\":\"yaml\",\"max_characters\":50}")
    }

    @Test
    fun testChunkTextZeroMax() {
        // chunk_text max_characters zero
        assertFailsWith<Exception> { Kreuzberg.chunkText("Some text.", "{\"max_characters\":0}") }
    }

    @Test
    fun testChunkTextsBatchMultiple() {
        // Batch chunk multiple texts
        val result = Kreuzberg.chunkTextsBatch(listOf("First doc text.", "Second doc text."), "{\"max_characters\":100}")
    }

}
