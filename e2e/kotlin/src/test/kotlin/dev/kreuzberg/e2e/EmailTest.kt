// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c8512635977b06ee129e9ff9dd9227a43a3d79028eac3226b75a21e0750f9cb0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith

/** E2e tests for category: email. */
class EmailTest {
    @Test
    fun testEmailEmlHtmlBody() {
        // EML with HTML body content.
        val result = Kreuzberg.extractFile("email/html_only.eml")
        assertEquals("message/rfc822", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testEmailEmlMultipart() {
        // EML with multipart MIME content.
        val result = Kreuzberg.extractFile("email/html_email_multipart.eml")
        assertEquals("message/rfc822", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testEmailEmlUtf16() {
        // UTF-16 encoded EML file with BOM.
        val result = Kreuzberg.extractFile("vendored/unstructured/eml/fake-email-utf-16.eml")
        assertEquals("message/rfc822", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 50, "expected length >= 50")
        assertTrue(result.content.orEmpty().contains("Test Email") || result.content.orEmpty().contains("Roses are red"), "expected to contain at least one of the specified values")
    }

    @Test
    fun testEmailMsgBasic() {
        // Outlook MSG file extraction.
        val result = Kreuzberg.extractFile("email/fake_email.msg")
        assertEquals("application/vnd.ms-outlook", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 10, "expected length >= 10")
    }

    @Test
    fun testEmailPstEmpty() {
        // Empty Outlook PST archive with no messages.
        val result = Kreuzberg.extractFile("email/empty.pst")
        assertEquals("application/vnd.ms-outlook-pst", result.mime_type.trim())
    }

    @Test
    fun testEmailSampleEml() {
        // Sample EML email file to verify email parsing.
        val result = Kreuzberg.extractFile("email/sample_email.eml")
        assertEquals("message/rfc822", result.mime_type.trim())
        assertTrue(result.content.orEmpty().length >= 20, "expected length >= 20")
    }

}
