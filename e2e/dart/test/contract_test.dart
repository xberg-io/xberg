// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c1ac051700a942d7058f9cbfaf7e73aeb8940d0f6d084f5e95715223e559e1e8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: contract
void main() {
  test('Tests async batch bytes extraction API (batch_extract_bytes)', () async {
    final result = await batchExtractBytes(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests sync batch bytes extraction API (batch_extract_bytes_sync)', () {
    final result = batchExtractBytesSync(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests async batch bytes extraction with per-file configs (batch_extract_bytes with file_configs parameter)', () async {
    final result = await batchExtractBytes(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests sync batch bytes extraction with per-file configs (batch_extract_bytes_sync with file_configs parameter)', () {
    final result = batchExtractBytesSync(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests async batch file extraction API (batch_extract_file)', () async {
    final result = await batchExtractFile(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests sync batch file extraction API (batch_extract_file_sync)', () {
    final result = batchExtractFileSync(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests async batch file extraction with per-file configs (batch_extract_files with file_configs parameter)', () async {
    final result = await batchExtractFile(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests sync batch file extraction with per-file configs (batch_extract_files_sync with file_configs parameter)', () {
    final result = batchExtractFileSync(null);
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests sync batch file extraction with per-file timeout config override', () {
    final result = batchExtractFileSync(null, '{"extraction_timeout_secs":300}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests async bytes extraction API (extract_bytes)', () async {
    final result = await extractBytes(null, '');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests sync bytes extraction API (extract_bytes_sync)', () {
    final result = extractBytesSync(null, '');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests async file extraction API (extract_file)', () async {
    final result = await extractFile('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests sync file extraction API (extract_file_sync)', () {
    final result = extractFileSync('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('May 5, 2023') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests explicit CPU acceleration provider configuration', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"acceleration":{"device_id":0,"provider":"cpu"}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.content.contains('May 5, 2023') || result.content.contains('To Whom it May Concern'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Tests chunking configuration with chunk assertions', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"chunking":{"max_chars":500,"max_overlap":50}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.chunks.length, greaterThanOrEqualTo(1));
    expect(result.chunks_have_content, isTrue);
  });

  test('Tests text chunker type (generic whitespace/punctuation splitter)', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"chunking":{"chunker_type":"text","max_chars":500,"max_overlap":50}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.chunks.length, greaterThanOrEqualTo(1));
    expect(result.chunks_have_content, isTrue);
  });

  test('Tests disable_ocr configuration option - OCR is skipped for images', () {
    final result = extractFileSync('images/test_hello_world.png', '{"disable_ocr":true}');
    expect(result.mime_type.trim(), equals('image/png'));
    expect(result.content.length, lessThanOrEqualTo(5));
  });

  test('Tests include_document_structure config produces document tree', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"include_document_structure":true}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.document_structure, isNotEmpty);
    expect(result.document_structure.nodes.length, greaterThanOrEqualTo(1));
    expect(result.document_structure.node_types, contains('paragraph'));
  });

  test('Tests document field is null when include_document_structure is false', () {
    final result = extractFileSync('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
  });

  test('Tests document structure with DOCX heading-driven nesting', () {
    final result = extractFileSync('docx/fake.docx', '{"include_document_structure":true}');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.document_structure, isNotEmpty);
    expect(result.document_structure.nodes.length, greaterThanOrEqualTo(1));
  });

  test('Tests MSG extraction with custom fallback codepage for Cyrillic', () async {
    final result = await extractFile('email/fake_email.msg', '{"email":{"msg_fallback_codepage":1251}}');
    expect(result.mime_type.trim(), equals('application/vnd.ms-outlook'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests that extraction_timeout_secs config field is accepted and does not affect fast extractions', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"extraction_timeout_secs":300}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests extraction with HTML conversion options configured', () {
    final result = extractFileSync('html/complex_table.html', '{"html_options":{"extract_metadata":true}}');
    expect(result.mime_type.contains('text/html'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content, isNotEmpty);
  });

  test('HTML output with inline user CSS string is embedded verbatim in the style block', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"html_output":{"css":".kb-p { color: red; }","embed_css":true,"theme":"unstyled"},"output_format":"html"}');
    expect(result.output_format.trim(), equals('html'));
    expect(result.content, contains('.kb-p { color: red; }'));
    expect(result.content, contains('kb-doc'));
  });

  test('HTML output with default theme emits kb-* class hooks and an embedded style block', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"html_output":{"embed_css":true,"theme":"default"},"output_format":"html"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.output_format.trim(), equals('html'));
    expect(result.content, contains('kb-doc'));
    expect(result.content, contains('kb-content'));
    expect(result.content, contains('kb-p'));
    expect(result.content, contains('<style>'));
  });

  test('HTML output with embed_css false emits class hooks but no style block', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"html_output":{"embed_css":false,"theme":"default"},"output_format":"html"}');
    expect(result.output_format.trim(), equals('html'));
    expect(result.content, contains('kb-doc'));
    expect(result.content, contains('kb-content'));
  });

  test('Tests image extraction configuration with image assertions', () {
    final result = extractFileSync('pdf/embedded_images_tables.pdf', '{"images":{"extract_images":true}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.images.length, greaterThanOrEqualTo(1));
  });

  test('Tests image extraction on PPTX containing embedded images', () {
    final result = extractFileSync('pptx/powerpoint_with_image.pptx', '{"images":{"extract_images":true}}');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.presentationml.presentation'));
    expect(result.images.length, greaterThanOrEqualTo(1));
  });

  test('Tests language detection configuration', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"language_detection":{"enabled":true}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.detected_languages, contains('eng'));
    expect(result.language_detection_confidence, greaterThanOrEqualTo(0.5));
  });

  test('Tests language detection with detect_multiple and min_confidence options', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"language_detection":{"detect_multiple":true,"enabled":true,"min_confidence":0.3}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.detected_languages, contains('eng'));
  });

  test('Tests postprocessor config is accepted and extraction succeeds', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"postprocessor":{"enabled":true}}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content, isNotEmpty);
  });

  test('Tests that a clean PDF extraction produces no processing warnings', () {
    final result = extractFileSync('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.processing_warnings, isEmpty);
  });

  test('Tests extraction with quality processing explicitly disabled', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"enable_quality_processing":false}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content, isNotEmpty);
  });

  test('Tests archive extraction with custom security limits', () {
    final result = extractFileSync('archives/documents.zip', '{"security_limits":{"max_archive_size":104857600,"max_compression_ratio":50,"max_files_in_archive":100}}');
    expect(result.mime_type.contains('application/zip') || result.mime_type.contains('application/x-zip-compressed'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests table extraction with content_contains_any assertion on DOCX with tables', () {
    final result = extractFileSync('docx/docx_tables.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.tables.length, greaterThanOrEqualTo(1));
  });

  test('Tests use_cache=false configuration option', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"use_cache":false}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Tests markdown output format via bytes extraction API', () {
    final result = extractBytesSync(null, '', '{"output_format":"markdown"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests Djot output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"djot"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('djot'));
  });

  test('Tests HTML output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"html"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('html'));
  });

  test('Tests Markdown output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"markdown"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('markdown'));
  });

  test('Tests Plain output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"plain"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.output_format.trim(), equals('plain'));
  });

  test('Tests ElementBased result format with element assertions', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"result_format":"element_based"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.result_format.trim(), equals('element_based'));
    expect(result.elements.length, greaterThanOrEqualTo(1));
  });

  test('Tests Unified result format (default)', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"result_format":"unified"}');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.result_format.trim(), equals('unified'));
  });

}
