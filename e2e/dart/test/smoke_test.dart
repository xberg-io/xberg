// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8277cd90828be10e7055614a11f1a6a482f9fad81a80a6bdc6013ebfd656c66e
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: smoke
void main() {
  test('Smoke test: Extraction with cache namespace and TTL configuration', () async {
    final result = await extractFile('text/report.txt', '{"cache_namespace":"test_tenant","cache_ttl_secs":3600,"use_cache":true}');
    expect(result.mime_type.trim(), equals('text/plain'));
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Smoke test: DOCX with formatted text', () async {
    final result = await extractFile('docx/fake.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
    expect(result.content.contains('Lorem') || result.content.contains('ipsum') || result.content.contains('document') || result.content.contains('text'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Smoke test: HTML table extraction', () async {
    final result = await extractFile('html/simple_table.html');
    expect(result.mime_type.trim(), equals('text/html'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('Sample Data Table') || result.content.contains('Laptop') || result.content.contains('Electronics') || result.content.contains('Product'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Smoke test: JSON file extraction', () async {
    final result = await extractFile('json/simple.json');
    expect(result.mime_type.trim(), equals('application/json'));
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Smoke test: PDF with simple text extraction', () async {
    final result = await extractFile('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.content.contains('May 5, 2023') || result.content.contains('To Whom it May Concern'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Smoke test: Plain text file', () async {
    final result = await extractFile('text/report.txt');
    expect(result.mime_type.trim(), equals('text/plain'));
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Smoke test: XLSX with basic spreadsheet data including tables', () async {
    final result = await extractFile('xlsx/stanley_cups.xlsx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'));
    expect(result.content.length, greaterThanOrEqualTo(100));
    expect(result.content, contains('Team'));
    expect(result.content, contains('Location'));
    expect(result.content, contains('Stanley Cups'));
    expect(result.content, contains('Blues'));
    expect(result.content, contains('Flyers'));
    expect(result.content, contains('Maple Leafs'));
    expect(result.content, contains('STL'));
    expect(result.content, contains('PHI'));
    expect(result.content, contains('TOR'));
    expect(result.tables.length, greaterThanOrEqualTo(1));
    expect(result.metadata.sheet_count, greaterThanOrEqualTo(2));
    expect(result.metadata.sheet_names, contains('Stanley Cups'));
  });

}
