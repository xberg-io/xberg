// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1fc64340f51104b40cf0abba8bec000bbb37c19ff69c31cb71c14a8a8f76c3da
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// ignore_for_file: unused_local_variable, unused_element

import 'package:test/test.dart';
import 'package:xberg/xberg.dart';
import 'package:xberg/src/xberg_bridge_generated/frb_generated.dart' show RustLib;
import 'dart:ffi';
import 'package:ffi/ffi.dart';

// E2e tests for category: smoke

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

void _setEnv(String key, String value) {
  final libc = DynamicLibrary.process();
  final setenv = libc.lookupFunction<
      Int32 Function(Pointer<Utf8>, Pointer<Utf8>, Int32),
      int Function(Pointer<Utf8>, Pointer<Utf8>, int)>('setenv');
  final keyPtr = key.toNativeUtf8();
  final valuePtr = value.toNativeUtf8();
  try {
    final result = setenv(keyPtr, valuePtr, 1);
    if (result != 0) {
      throw StateError('setenv failed for ${key}=${value} with return code $result');
    }
  } finally {
    calloc.free(keyPtr);
    calloc.free(valuePtr);
  }
}

void main() {
  var _rustLibInitialized = false;

  setUpAll(() async {
    _setEnv('CRAWLBERG_ALLOW_PRIVATE_NETWORK', 'true');
    await RustLib.init();
    _rustLibInitialized = true;
  });

  tearDownAll(() async {
    if (_rustLibInitialized) {
      RustLib.dispose();
    }
  });

  test('OCR: PNG image extraction with OCR enabled. In WASM this exercises the Uint8Array bridge parameter and Promise await in the generated OcrBackend bridge.', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('image/png'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(1));
    expect(result.results[0].content.contains('Hello') || result.results[0].content.contains('World') || result.results[0].content.contains('hello') || result.results[0].content.contains('world'), isTrue);
  });

  test('Smoke test: DOCX with formatted text', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(20));
    expect(result.results[0].content.contains('Lorem') || result.results[0].content.contains('ipsum') || result.results[0].content.contains('document') || result.results[0].content.contains('text'), isTrue);
  });

  test('Smoke test: HTML table extraction', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('text/html'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(10));
    expect(result.results[0].content.contains('Sample Data Table') || result.results[0].content.contains('Laptop') || result.results[0].content.contains('Electronics') || result.results[0].content.contains('Product'), isTrue);
  });

  test('Smoke test: PNG image (without OCR, metadata only)', () async {
    final _config = await createExtractionConfigFromJson(json: '{"disable_ocr":true}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('image/png'.toString().trim()));
  });

  test('Smoke test: JSON file extraction', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('application/json'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(5));
  });

  test('Smoke test: PDF with simple text extraction', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('application/pdf'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(50));
    expect(result.results[0].content.contains('May 5, 2023') || result.results[0].content.contains('To Whom it May Concern'), isTrue);
  });

  test('Smoke test: Plain text file', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('text/plain'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(5));
  });

  test('Smoke test: XLSX with basic spreadsheet data including tables', () async {
    final _config = await createExtractionConfigFromJson(json: '{}');
    final result = await XbergBridge.extract(config: _config);
    expect(result.results[0].mimeType.toString().trim(), equals('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'.toString().trim()));
    expect(result.results[0].content.length, greaterThanOrEqualTo(100));
    expect(result.results[0].content, contains('Team'));
    expect(result.results[0].content, contains('Location'));
    expect(result.results[0].content, contains('Stanley Cups'));
    expect(result.results[0].content, contains('Blues'));
    expect(result.results[0].content, contains('Flyers'));
    expect(result.results[0].content, contains('Maple Leafs'));
    expect(result.results[0].content, contains('STL'));
    expect(result.results[0].content, contains('PHI'));
    expect(result.results[0].content, contains('TOR'));
    expect(result.results[0].tables.length, greaterThanOrEqualTo(1));
    // skipped: field 'results[0].metadata.format.excel.sheet_count' crosses a tagged-union variant boundary (not expressible in Dart)
    // skipped: field 'results[0].metadata.format.excel.sheet_names' crosses a tagged-union variant boundary (not expressible in Dart)
  });

}
