// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e2924ff464eab070f897924abcddc5ead1ef8c54993fbe4f2cf1518686306672
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: render
void main() {
  test('Render PDF page with custom DPI', () {
    final result = renderPage('pdf/tiny.pdf', 0);
    expect(result.is_png, isTrue);
    expect(result.byte_length, greaterThanOrEqualTo(50));
  });

  test('Render PDF page with high DPI setting (300 DPI)', () {
    final result = renderPage('pdf/fake_memo.pdf', 0, '{"dpi":300}');
  });

  test('Iterate all PDF pages as PNG images', () {
    final result = renderPage('pdf/tiny.pdf', 0);
    expect(result.is_png, isTrue);
    expect(result.page_count, greaterThanOrEqualTo(1));
  });

  test('Render a page from a multi-page PDF document', () {
    final result = renderPage('pdf/a_brief_introduction_to_neural_networks_neuronalenetze_en_zeta2_2col_dkrieselcom.pdf', 0);
    expect(result.result.length, greaterThanOrEqualTo(1000));
  });

  test('Extract PDF with Djot output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"djot"}');
    expect(result.output_format.trim(), equals('djot'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Extract PDF with HTML output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"html"}');
    expect(result.output_format.trim(), equals('html'));
    expect(result.content.contains('<') || result.content.contains('>') || result.content.contains('</'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Extract PDF with Markdown output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"markdown"}');
    expect(result.output_format.trim(), equals('markdown'));
    expect(result.content.contains('#') || result.content.contains('**') || result.content.contains('*'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Extract PDF with plain text output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"plain"}');
    expect(result.output_format.trim(), equals('plain'));
    expect(result.content.length, greaterThanOrEqualTo(50));
  });

  test('Render a single PDF page to PNG', () {
    final result = renderPage('pdf/tiny.pdf', 0);
    expect(result.is_png, isTrue);
    expect(result.byte_length, greaterThanOrEqualTo(100));
  });

}
