// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4487d70cca459f04bdcde5213a333fb9510fd15f038057fb5bd106693b093b5c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: pdf
void main() {
  test('Assembly language technical manual with large body of text.', () async {
    final result = await extractFile('pdf/assembly_language_for_beginners_al4_b_en.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(5000));
    expect(result.content.contains('assembly') || result.content.contains('register') || result.content.contains('instruction'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('Bayesian data analysis textbook PDF with large content volume.', () async {
    final result = await extractFile('pdf/bayesian_data_analysis_third_edition_13th_feb_2020.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10000));
    expect(result.content.contains('Bayesian') || result.content.contains('probability') || result.content.contains('distribution'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('PDF containing code snippets and formulas should retain substantial content.', () async {
    final result = await extractFile('pdf/code_and_formula.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(100));
  });

  test('Deep learning textbook PDF to ensure long-form extraction quality.', () async {
    final result = await extractFile('pdf/fundamentals_of_deep_learning_2014.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(1000));
    expect(result.content.contains('neural') || result.content.contains('network') || result.content.contains('deep learning'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('PDF with embedded images should extract text and tables when present.', () async {
    final result = await extractFile('pdf/embedded_images_tables.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.tables.length, greaterThanOrEqualTo(0));
  });

  test('Google Docs exported PDF to verify conversion fidelity.', () async {
    final result = await extractFile('pdf/google_doc_document.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('Large machine learning textbook PDF to stress extraction length.', () async {
    final result = await extractFile('pdf/a_course_in_machine_learning_ciml_v0_9_all.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10000));
    expect(result.content.contains('machine learning') || result.content.contains('algorithm') || result.content.contains('training'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('German technical PDF to ensure non-ASCII content extraction.', () async {
    final result = await extractFile('pdf/5_level_paging_and_5_level_ept_intel_revision_1_1_may_2017.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(100));
    expect(result.content.contains('Intel') || result.content.contains('paging'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('Right-to-left language PDF to verify RTL extraction.', () async {
    final result = await extractFile('pdf/right_to_left_01.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

  test('Simple text-heavy PDF should extract content without OCR or tables.', () async {
    final result = await extractFile('pdf/fake_memo.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.content.contains('May 5, 2023') || result.content.contains('To Whom it May Concern') || result.content.contains('Mallori'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Large PDF with extensive tables to stress table extraction.', () async {
    final result = await extractFile('pdf/large.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(500));
  });

  test('Medium-sized PDF with multiple tables.', () async {
    final result = await extractFile('pdf/medium.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(100));
  });

  test('Technical statistical learning PDF requiring substantial extraction.', () async {
    final result = await extractFile('pdf/an_introduction_to_statistical_learning_with_applications_in_r_islr_sixth_printing.pdf');
    expect(result.mime_type.trim(), equals('application/pdf'));
    expect(result.content.length, greaterThanOrEqualTo(10000));
    expect(result.content.contains('statistical') || result.content.contains('regression') || result.content.contains('learning'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.metadata.format_type.trim(), equals('pdf'));
  });

}
