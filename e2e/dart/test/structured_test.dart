// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d70206e9203155928d96bdf74c8083a81a39d2eca36647c575e2b9a2b75a9282
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: structured
void main() {
  test('CSV data file extraction.', () async {
    final result = await extractFile('csv/stanley_cups.csv');
    expect(result.mime_type.trim(), equals('text/csv'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('EndNote ENW citation format extraction.', () async {
    final result = await extractFile('data_formats/sample.enw');
    expect(result.mime_type.contains('application/x-endnote-refer') || result.mime_type.contains('application/x-endnote+xml') || result.mime_type.contains('text/plain'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Structured JSON extraction should stream and preserve content.', () async {
    final result = await extractFile('json/sample_document.json');
    expect(result.mime_type.trim(), equals('application/json'));
    expect(result.content.length, greaterThanOrEqualTo(20));
    expect(result.content.contains('Sample Document') || result.content.contains('Test Author'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Simple JSON document to verify structured extraction.', () async {
    final result = await extractFile('json/simple.json');
    expect(result.mime_type.trim(), equals('application/json'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.content.contains('{') || result.content.contains('name'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('PubMed NBIB citation format extraction.', () async {
    final result = await extractFile('data_formats/sample.nbib');
    expect(result.mime_type.contains('application/nbib') || result.mime_type.contains('application/x-pubmed') || result.mime_type.contains('text/plain'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content, isNotEmpty);
  });

  test('RIS citation format extraction.', () async {
    final result = await extractFile('data_formats/sample.ris');
    expect(result.mime_type.contains('application/x-research-info-systems') || result.mime_type.contains('text/plain'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content, isNotEmpty);
  });

  test('TOML configuration file extraction.', () async {
    final result = await extractFile('data_formats/cargo.toml');
    expect(result.mime_type.contains('application/toml') || result.mime_type.contains('text/toml'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('TSV (tab-separated values) data file extraction.', () async {
    final result = await extractFile('data_formats/employees.tsv');
    expect(result.mime_type.contains('text/tab-separated-values') || result.mime_type.contains('text/plain'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('YAML file text extraction.', () async {
    final result = await extractFile('yaml/simple.yaml');
    expect(result.mime_type.contains('application/yaml') || result.mime_type.contains('text/yaml') || result.mime_type.contains('text/x-yaml') || result.mime_type.contains('application/x-yaml'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Simple YAML document to validate structured extraction.', () async {
    final result = await extractFile('yaml/simple.yaml');
    expect(result.mime_type.trim(), equals('application/x-yaml'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

}
