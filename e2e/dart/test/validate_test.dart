// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:234c6abd3d84911790dcdec3da4e3a631f4e410a6d0f3c02b32d522287b1ade6
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: validate
void main() {
  test('Invalid binarization', () {
    expect(() => validateBinarizationMethod('nonexistent'), throwsA(isA<Exception>()));
  });

  test('Valid binarization', () {
    final result = validateBinarizationMethod('otsu');
  });

  test('Reject chunking config where overlap exceeds max_characters', () {
    expect(() => extractFileSync('pdf/fake_memo.pdf', '{"chunking":{"max_characters":10,"max_overlap":100}}'), throwsA(isA<Exception>()));
  });

  test('Chunking overlap >= max', () {
    expect(() => validateChunkingParams(50, 60), throwsA(isA<Exception>()));
  });

  test('Valid chunking params', () {
    final result = validateChunkingParams(500, 50);
  });

  test('Accept valid chunking config', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"chunking":{"max_characters":1000,"max_overlap":200}}');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Confidence at 1.0', () {
    final result = validateConfidence(1.0);
  });

  test('Negative confidence', () {
    expect(() => validateConfidence(-0.5), throwsA(isA<Exception>()));
  });

  test('Confidence > 1', () {
    expect(() => validateConfidence(1.5), throwsA(isA<Exception>()));
  });

  test('Accept valid confidence threshold', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"ocr":{"confidence_threshold":0.8}}');
  });

  test('Confidence at 0.0', () {
    final result = validateConfidence(0.0);
  });

  test('Reject negative DPI in OCR config', () {
    expect(() => extractFileSync('pdf/fake_memo.pdf', '{"ocr":{"dpi":-1}}'), throwsA(isA<Exception>()));
  });

  test('Accept valid DPI in OCR config', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"ocr":{"dpi":300}}');
  });

  test('Empty host rejected', () {
    expect(() => validateHost(''), throwsA(isA<Exception>()));
  });

  test('Invalid OCR backend', () {
    expect(() => validateOcrBackend('nonexistent'), throwsA(isA<Exception>()));
  });

  test('Valid OCR backend', () {
    final result = validateOcrBackend('tesseract');
  });

  test('Reject invalid output format', () {
    expect(() => extractFileSync('pdf/fake_memo.pdf', '{"output_format":"nonexistent_format"}'), throwsA(isA<Exception>()));
  });

  test('Accept valid output format', () {
    final result = extractFileSync('pdf/fake_memo.pdf', '{"output_format":"markdown"}');
  });

  test('Port 65535 accepted', () {
    final result = validatePort(65535);
  });

  test('Port over 65535', () {
    expect(() => validatePort(70000), throwsA(isA<Exception>()));
  });

  test('Port 0 rejected', () {
    expect(() => validatePort(0), throwsA(isA<Exception>()));
  });

  test('Invalid reduction level', () {
    expect(() => validateTokenReductionLevel('nonexistent'), throwsA(isA<Exception>()));
  });

  test('Valid reduction level', () {
    final result = validateTokenReductionLevel('moderate');
  });

}
