// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:51139efd101fef75ee5c648cbcd35383c6bd817b340ef616a1d70400b6ebfeb1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: rendering
void main() {
  test('Convert basic HTML to Markdown format', () {
    final result = convertHtmlToMarkdown('<h1>Title</h1><p>Paragraph text.</p><ul><li>Item 1</li><li>Item 2</li></ul>');
    expect(result.content, contains('Title'));
  });

  test('Convert complex HTML to markdown', () {
    final result = convertHtmlToMarkdown('<h1>Title</h1><p>Text with <strong>bold</strong> and <a href="https://example.com">link</a>.</p>');
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('Convert empty HTML to markdown', () {
    final result = convertHtmlToMarkdown('');
  });

  test('Convert HTML table to Markdown table format', () {
    final result = convertHtmlToMarkdown('<table><tr><th>Column A</th><th>Column B</th></tr><tr><td>1</td><td>2</td></tr><tr><td>3</td><td>4</td></tr></table>');
    expect(result.content, contains('|'));
  });

  test('Convert Djot format to HTML', () {
    final result = djotToHtml('# Title\n\nParagraph text with **bold** and *italic*.\n\n- Item 1\n- Item 2');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Complex djot to HTML', () {
    final result = djotToHtml('# Heading\n\nParagraph.\n\n- Item one\n- Item two');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Convert empty djot to HTML', () {
    final result = djotToHtml('');
  });

  test('Render HTML document to Djot format', () {
    final result = renderDjot('<h1>Test Title</h1><p>This is a test document in Djot format.</p><ul><li>Item 1</li><li>Item 2</li></ul>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render HTML to djot', () {
    final result = renderDjot('<h2>Section</h2><p>Text with <code>code</code>.</p>');
  });

  test('Render a simple HTML document to formatted output', () {
    final result = renderHtml('<h1>Test Document</h1><p>This is a paragraph with some test content.</p>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render HTML preserving structure', () {
    final result = renderHtml('<h1>Heading</h1><p><strong>bold</strong></p>');
    expect(result.content, contains('Heading'));
  });

  test('Render HTML document to JSON format', () {
    final result = renderJson('<h1>Structured Data</h1><p>Content for JSON rendering.</p>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render HTML to JSON', () {
    final result = renderJson('<h1>Title</h1><p>Text.</p>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render HTML document to Markdown format', () {
    final result = renderMarkdown('<h1>Heading</h1><p>Paragraph text with <strong>bold</strong> and <em>italic</em>.</p>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render complex HTML to markdown', () {
    final result = renderMarkdown('<h1>Title</h1><ul><li>A</li><li>B</li></ul>');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Render HTML document to plain text format', () {
    final result = renderPlain('<h1>Title</h1><p>Plain text rendering removes HTML tags.</p>');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Render empty HTML', () {
    final result = renderPlain('');
  });

}
