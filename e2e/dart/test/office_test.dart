// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:564de6c9885e80e03209e259eaed94ba0c8652fb98bd7d22e8bde7e214d70070
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:kreuzberg/kreuzberg.dart';

// E2e tests for category: office
void main() {
  test('BibTeX bibliography extraction.', () async {
    final result = await extractFile('bibtex/comprehensive.bib');
    expect(result.mime_type.contains('application/x-bibtex') || result.mime_type.contains('text/x-bibtex'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('CommonMark (.commonmark) text extraction.', () async {
    final result = await extractFile('markdown/sample.commonmark');
    expect(result.mime_type.contains('text/markdown') || result.mime_type.contains('text/plain') || result.mime_type.contains('text/x-commonmark'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('Djot markup text extraction.', () async {
    final result = await extractFile('markdown/tables.djot');
    expect(result.mime_type.contains('text/x-djot') || result.mime_type.contains('text/djot'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('DocBook XML document extraction.', () async {
    final result = await extractFile('docbook/docbook-reader.docbook');
    expect(result.mime_type.contains('application/docbook+xml') || result.mime_type.contains('text/docbook'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('DOCX document extraction baseline.', () async {
    final result = await extractFile('docx/sample_document.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('DOCX file containing equations to validate math extraction.', () async {
    final result = await extractFile('docx/equations.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('Simple DOCX document to verify baseline extraction.', () async {
    final result = await extractFile('docx/fake.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('DOCX document heavy on formatting for style preservation.', () async {
    final result = await extractFile('docx/unit_test_formatting.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('DOCX document with complex headers.', () async {
    final result = await extractFile('docx/unit_test_headers.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('DOCX document emphasizing list formatting.', () async {
    final result = await extractFile('docx/unit_test_lists.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('DOCX document containing tables for table-aware extraction.', () async {
    final result = await extractFile('docx/docx_tables.docx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.wordprocessingml.document'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.content, contains('Simple uniform table'));
    expect(result.content, contains('Nested Table'));
    expect(result.content, contains('merged cells'));
    expect(result.content, contains('Header Col'));
    expect(result.tables.length, greaterThanOrEqualTo(1));
  });

  test('EPUB book extraction with text content.', () async {
    final result = await extractFile('epub/features.epub');
    expect(result.mime_type.trim(), equals('application/epub+zip'));
    expect(result.content.length, greaterThanOrEqualTo(50));
  });

  test('FictionBook (FB2) document extraction baseline.', () async {
    final result = await extractFile('fictionbook/basic.fb2');
    expect(result.mime_type.trim(), equals('application/x-fictionbook+xml'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('FictionBook (.fb2) text extraction.', () async {
    final result = await extractFile('fictionbook/basic.fb2');
    expect(result.mime_type.contains('application/x-fictionbook+xml') || result.mime_type.contains('application/x-fictionbook'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('JATS scientific article extraction.', () async {
    final result = await extractFile('jats/sample_article.jats');
    expect(result.mime_type.contains('application/x-jats+xml') || result.mime_type.contains('text/jats'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Jupyter notebook extraction.', () async {
    final result = await extractFile('jupyter/rank.ipynb');
    expect(result.mime_type.trim(), equals('application/x-ipynb+json'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Keynote document extraction baseline.', () async {
    final result = await extractFile('iwork/test.key');
    expect(result.mime_type.trim(), equals('application/x-iwork-keynote-sffkey'));
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('LaTeX document text extraction.', () async {
    final result = await extractFile('latex/basic_sections.tex');
    expect(result.mime_type.contains('application/x-latex') || result.mime_type.contains('text/x-latex'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('Markdown document extraction baseline.', () async {
    final result = await extractFile('markdown/comprehensive.md');
    expect(result.mime_type.trim(), equals('text/markdown'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Numbers document extraction baseline.', () async {
    final result = await extractFile('iwork/test.numbers');
    expect(result.mime_type.trim(), equals('application/x-iwork-numbers-sffnumbers'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Basic ODS spreadsheet extraction.', () async {
    final result = await extractFile('data_formats/test_01.ods');
    expect(result.mime_type.trim(), equals('application/vnd.oasis.opendocument.spreadsheet'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('ODT document with bold formatting.', () async {
    final result = await extractFile('odt/bold.odt');
    expect(result.mime_type.trim(), equals('application/vnd.oasis.opendocument.text'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('ODT document containing unordered lists with nesting.', () async {
    final result = await extractFile('odt/unorderedList.odt');
    expect(result.mime_type.trim(), equals('application/vnd.oasis.opendocument.text'));
    expect(result.content.length, greaterThanOrEqualTo(30));
    expect(result.content.contains('list item') || result.content.contains('New level') || result.content.contains('Pushed us'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('Basic ODT document with paragraphs and headings.', () async {
    final result = await extractFile('odt/simple.odt');
    expect(result.mime_type.trim(), equals('application/vnd.oasis.opendocument.text'));
    expect(result.content.length, greaterThanOrEqualTo(50));
    expect(result.content.contains('favorite things') || result.content.contains('Parrots') || result.content.contains('Analysis'), isTrue, reason: 'expected to contain at least one of the specified values');
  });

  test('ODT document with a table structure.', () async {
    final result = await extractFile('odt/table.odt');
    expect(result.mime_type.trim(), equals('application/vnd.oasis.opendocument.text'));
    expect(result.content.length, greaterThanOrEqualTo(10));
    expect(result.tables.length, greaterThanOrEqualTo(1));
  });

  test('OPML outline document extraction.', () async {
    final result = await extractFile('opml/outline.opml');
    expect(result.mime_type.contains('application/xml+opml') || result.mime_type.contains('text/x-opml') || result.mime_type.contains('application/x-opml+xml'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Org-mode document text extraction.', () async {
    final result = await extractFile('org/comprehensive.org');
    expect(result.mime_type.contains('text/x-org') || result.mime_type.contains('text/org'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('Pages document extraction baseline.', () async {
    final result = await extractFile('iwork/test.pages');
    expect(result.mime_type.trim(), equals('application/x-iwork-pages-sffpages'));
    expect(result.content.length, greaterThanOrEqualTo(5));
  });

  test('PPSX (PowerPoint Show) files should extract slides content identical to PPTX. GitHub Issue #321 Bug 2.', () async {
    final result = await extractFile('pptx/sample.ppsx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.presentationml.slideshow'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('PowerPoint macro-enabled presentation (.pptm) extraction.', () async {
    final result = await extractFile('pptx/powerpoint_with_image.pptm');
    expect(result.mime_type.contains('application/vnd.ms-powerpoint.presentation.macroEnabled.12') || result.mime_type.contains('application/vnd.openxmlformats-officedocument.presentationml.presentation'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content, isNotEmpty);
  });

  test('PPTX deck should extract slides content.', () async {
    final result = await extractFile('pptx/simple.pptx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.presentationml.presentation'));
    expect(result.content.length, greaterThanOrEqualTo(50));
  });

  test('PPTX presentation containing images to ensure metadata extraction.', () async {
    final result = await extractFile('pptx/powerpoint_with_image.pptx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.presentationml.presentation'));
    expect(result.content.length, greaterThanOrEqualTo(15));
  });

  test('Pitch deck PPTX used to validate large slide extraction.', () async {
    final result = await extractFile('pptx/pitch_deck_presentation.pptx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.presentationml.presentation'));
    expect(result.content.length, greaterThanOrEqualTo(100));
  });

  test('reStructuredText document extraction.', () async {
    final result = await extractFile('rst/restructured_text.rst');
    expect(result.mime_type.contains('text/x-rst') || result.mime_type.contains('text/prs.fallenstein.rst'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(20));
  });

  test('RTF document text extraction.', () async {
    final result = await extractFile('rtf/extraction_test.rtf');
    expect(result.mime_type.contains('application/rtf') || result.mime_type.contains('text/rtf'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Typst document text extraction.', () async {
    final result = await extractFile('typst/headings.typ');
    expect(result.mime_type.contains('application/x-typst') || result.mime_type.contains('text/x-typst'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Legacy XLS spreadsheet to ensure backward compatibility.', () async {
    final result = await extractFile('xls/test_excel.xls');
    expect(result.mime_type.trim(), equals('application/vnd.ms-excel'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

  test('Excel binary workbook (.xlsb) extraction.', () async {
    final result = await extractFile('xlsx/test_xlsb.xlsb');
    expect(result.mime_type.contains('application/vnd.ms-excel.sheet.binary.macroEnabled.12') || result.mime_type.contains('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content, isNotEmpty);
  });

  test('Excel macro-enabled workbook (.xlsm) extraction.', () async {
    final result = await extractFile('xlsx/test_01.xlsm');
    expect(result.mime_type.contains('application/vnd.ms-excel.sheet.macroEnabled.12') || result.mime_type.contains('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'), isTrue, reason: 'expected to contain at least one of the specified values');
    expect(result.content, isNotEmpty);
  });

  test('XLSX spreadsheet should produce metadata and table content.', () async {
    final result = await extractFile('xlsx/stanley_cups.xlsx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'));
    expect(result.content.length, greaterThanOrEqualTo(100));
    expect(result.content, contains('Team'));
    expect(result.content, contains('Location'));
    expect(result.content, contains('Stanley Cups'));
    expect(result.tables.length, greaterThanOrEqualTo(1));
    expect(result.metadata.sheet_count, greaterThanOrEqualTo(2));
    expect(result.metadata.sheet_names, contains('Stanley Cups'));
  });

  test('XLSX workbook with multiple sheets.', () async {
    final result = await extractFile('xlsx/excel_multi_sheet.xlsx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'));
    expect(result.content.length, greaterThanOrEqualTo(20));
    expect(result.metadata.sheet_count, greaterThanOrEqualTo(2));
  });

  test('Simple XLSX spreadsheet shipped alongside office integration tests.', () async {
    final result = await extractFile('xlsx/test_01.xlsx');
    expect(result.mime_type.trim(), equals('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'));
    expect(result.content.length, greaterThanOrEqualTo(10));
  });

}
