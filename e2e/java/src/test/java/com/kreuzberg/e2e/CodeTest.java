package com.kreuzberg.e2e;

// CHECKSTYLE.OFF: UnusedImports - generated code
// CHECKSTYLE.OFF: LineLength - generated code
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import dev.kreuzberg.ExtractionResult;
import dev.kreuzberg.Kreuzberg;
import dev.kreuzberg.config.ExtractionConfig;
import java.nio.file.Files;
import java.nio.file.Path;
import java.util.Arrays;
import org.junit.jupiter.api.Test;

// CHECKSTYLE.ON: UnusedImports
// CHECKSTYLE.ON: LineLength

// Code generated by kreuzberg-e2e-generator. DO NOT EDIT.
// To regenerate: cargo run -p kreuzberg-e2e-generator -- generate --lang java

/** Tests for code fixtures. */
public class CodeTest {
  private static final ObjectMapper MAPPER = new ObjectMapper();

  @Test
  public void codeJavascriptBasic() throws Exception {
    JsonNode config = null;
    E2EHelpers.skipIfFeatureUnavailable("tree-sitter");
    E2EHelpers.runFixture(
        "code_javascript_basic",
        "code/app.js",
        config,
        Arrays.asList("tree-sitter"),
        null,
        true,
        result -> {
          E2EHelpers.Assertions.assertExpectedMime(result, Arrays.asList("text/x-source-code"));
          E2EHelpers.Assertions.assertMinContentLength(result, 10);
          E2EHelpers.Assertions.assertContentContainsAll(
              result, Arrays.asList("class Application", "export function", "import"));
        });
  }

  @Test
  public void codePythonBasic() throws Exception {
    JsonNode config = null;
    E2EHelpers.skipIfFeatureUnavailable("tree-sitter");
    E2EHelpers.runFixture(
        "code_python_basic",
        "code/hello.py",
        config,
        Arrays.asList("tree-sitter"),
        null,
        true,
        result -> {
          E2EHelpers.Assertions.assertExpectedMime(result, Arrays.asList("text/x-source-code"));
          E2EHelpers.Assertions.assertMinContentLength(result, 10);
          E2EHelpers.Assertions.assertContentContainsAll(
              result, Arrays.asList("def greet", "class Greeter", "import os"));
        });
  }

  @Test
  public void codeRustBasic() throws Exception {
    JsonNode config = null;
    E2EHelpers.skipIfFeatureUnavailable("tree-sitter");
    E2EHelpers.runFixture(
        "code_rust_basic",
        "code/main.rs",
        config,
        Arrays.asList("tree-sitter"),
        null,
        true,
        result -> {
          E2EHelpers.Assertions.assertExpectedMime(result, Arrays.asList("text/x-source-code"));
          E2EHelpers.Assertions.assertMinContentLength(result, 10);
          E2EHelpers.Assertions.assertContentContainsAll(
              result, Arrays.asList("struct Store", "fn main", "HashMap"));
        });
  }

  @Test
  public void codeShebangDetection() throws Exception {
    JsonNode config = null;
    E2EHelpers.skipIfFeatureUnavailable("tree-sitter");
    Path documentPath = E2EHelpers.resolveDocument("code/script.sh");

    if (!Files.exists(documentPath)) {
      String msg =
          String.format("Skipping code_shebang_detection: missing document at %s", documentPath);
      System.err.println(msg);
      org.junit.jupiter.api.Assumptions.assumeTrue(false, msg);
      return;
    }

    byte[] documentBytes = Files.readAllBytes(documentPath);
    String mimeType = Kreuzberg.detectMimeType(documentBytes);
    ExtractionConfig extractionConfig = E2EHelpers.buildConfig(config);
    ExtractionResult result;
    try {
      result = Kreuzberg.extractBytes(documentBytes, mimeType, extractionConfig);
    } catch (Exception e) {
      String skipReason =
          E2EHelpers.skipReasonFor(e, "code_shebang_detection", Arrays.asList("tree-sitter"), null);
      if (skipReason != null) {
        org.junit.jupiter.api.Assumptions.assumeTrue(false, skipReason);
        return;
      }
      throw e;
    }

    E2EHelpers.Assertions.assertExpectedMime(result, Arrays.asList("text/x-source-code"));
    E2EHelpers.Assertions.assertMinContentLength(result, 10);
    E2EHelpers.Assertions.assertContentContainsAll(result, Arrays.asList("build", "clean"));
  }

  @Test
  public void codeTreeSitterConfig() throws Exception {
    JsonNode config =
        MAPPER.readTree(
            "{\"tree_sitter\":{\"process\":{\"structure\":true,\"imports\":true,\"exports\":true,\"comments\":true,\"docstrings\":true}}}");
    E2EHelpers.skipIfFeatureUnavailable("tree-sitter");
    E2EHelpers.runFixture(
        "code_tree_sitter_config",
        "code/hello.py",
        config,
        Arrays.asList("tree-sitter"),
        null,
        true,
        result -> {
          E2EHelpers.Assertions.assertExpectedMime(result, Arrays.asList("text/x-source-code"));
          E2EHelpers.Assertions.assertMinContentLength(result, 10);
          E2EHelpers.Assertions.assertContentContainsAll(
              result, Arrays.asList("def greet", "class Greeter"));
        });
  }
}
