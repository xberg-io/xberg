// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:937abf091abbfd6f8eb63eac2ecb04286f5c2226ac373626a3a0688f23226846
// To regenerate: alef e2e generate
// To verify freshness: alef verify

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;
import org.junit.jupiter.api.BeforeAll;

/** E2e tests for category: format_specific. */
public class FormatSpecificTest {

    @BeforeAll
    static void initEnv() {
        if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
            System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true");
        }
        if (System.getProperty("RUST_MIN_STACK") == null) {
            System.setProperty("RUST_MIN_STACK", "16777216");
        }
    }


    @Test
    void testFormatDocxEquations() throws Exception {
        // DOCX equations extract to LaTeX math in markdown output
        String inputMockBaseUrl = System.getProperty("mockServer.format_docx_equations", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_DOCX_EQUATIONS", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_docx_equations"));
        String inputJson = "{\"filename\":\"equations.docx\",\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\",\"uri\":\"$mock_url/docx/equations.docx\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"output_format\":\"markdown\"}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertTrue(result.results().get(0).content().length() >= 20, "expected length >= 20");
        assertTrue(result.results().get(0).content().contains("$"), "expected to contain: " + "$");


    }


    @Test
    void testFormatDocxStandalone() throws Exception {
        // Standalone DOCX extraction using extract
        String inputMockBaseUrl = System.getProperty("mockServer.format_docx_standalone", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_DOCX_STANDALONE", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_docx_standalone"));
        String inputJson = "{\"filename\":\"fake.docx\",\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\",\"uri\":\"$mock_url/docx/fake.docx\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertTrue(result.results().get(0).content().length() >= 20, "expected length >= 20");


    }


    @Test
    void testFormatHwpxStandalone() throws Exception {
        // Standalone HWPX extraction using extract
        String inputMockBaseUrl = System.getProperty("mockServer.format_hwpx_standalone", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_HWPX_STANDALONE", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_hwpx_standalone"));
        String inputJson = "{\"filename\":\"simple.hwpx\",\"kind\":\"uri\",\"mime_type\":\"application/haansofthwpx\",\"uri\":\"$mock_url/hwpx/simple.hwpx\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertTrue(result.results().get(0).content().length() >= 20, "expected length >= 20");
        assertTrue(result.results().get(0).content().contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX");


    }


    @Test
    void testFormatPdfText() throws Exception {
        // Standalone PDF text extraction using extract
        String inputMockBaseUrl = System.getProperty("mockServer.format_pdf_text", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_PDF_TEXT", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_pdf_text"));
        String inputJson = "{\"filename\":\"fake_memo.pdf\",\"kind\":\"uri\",\"mime_type\":\"application/pdf\",\"uri\":\"$mock_url/pdf/fake_memo.pdf\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertTrue(result.results().get(0).content().length() >= 50, "expected length >= 50");
        assertTrue(result.results().get(0).content().contains("Mallori") || result.results().get(0).content().contains("May"), "expected to contain at least one of the specified values");


    }


    @Test
    void testFormatPptx() throws Exception {
        // PPTX presentation extraction using extract
        String inputMockBaseUrl = System.getProperty("mockServer.format_pptx", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_PPTX", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_pptx"));
        String inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"uri\":\"$mock_url/pptx/simple.pptx\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertNotNull(result, "expected non-null response");


    }


    @Test
    void testFormatXlsx() throws Exception {
        // XLSX spreadsheet extraction using extract
        String inputMockBaseUrl = System.getProperty("mockServer.format_xlsx", System.getenv().getOrDefault("MOCK_SERVER_FORMAT_XLSX", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/format_xlsx"));
        String inputJson = "{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"uri\":\"$mock_url/xlsx/stanley_cups.xlsx\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertNotNull(result, "expected non-null response");


    }

}
