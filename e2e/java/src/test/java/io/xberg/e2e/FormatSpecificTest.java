// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b0f3e348cc193e0cfa8fdcc36fd3ebb5e08e4360d10b525744b736383abddeae
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;
import org.junit.jupiter.api.BeforeAll;
/** E2e tests for category: format_specific. */
public class FormatSpecificTest {
    @BeforeAll
    static void initEnv() {        if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
            System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true");
        }    }

    @Test
    void testFormatDocxStandalone() throws Exception {
        // Standalone DOCX extraction using extract
        var input = JsonUtil.fromJson("{\"filename\":\"fake.docx\",\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.wordprocessingml.document\",\"uri\":\"docx/fake.docx\"}", ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.results().get(0).content().length() >= 20, "expected length >= 20");

    }


    @Test
    void testFormatHwpxStandalone() throws Exception {
        // Standalone HWPX extraction using extract
        var input = JsonUtil.fromJson("{\"filename\":\"simple.hwpx\",\"kind\":\"uri\",\"mime_type\":\"application/haansofthwpx\",\"uri\":\"hwpx/simple.hwpx\"}", ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.results().get(0).content().length() >= 20, "expected length >= 20");assertTrue(result.results().get(0).content().contains("Hello from HWPX"), "expected to contain: " + "Hello from HWPX");

    }


    @Test
    void testFormatPdfText() throws Exception {
        // Standalone PDF text extraction using extract
        var input = JsonUtil.fromJson("{\"filename\":\"fake_memo.pdf\",\"kind\":\"uri\",\"mime_type\":\"application/pdf\",\"uri\":\"pdf/fake_memo.pdf\"}", ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
assertTrue(result.results().get(0).content().length() >= 50, "expected length >= 50");assertTrue(result.results().get(0).content().contains("Mallori") || result.results().get(0).content().contains("May"), "expected to contain at least one of the specified values");

    }


    @Test
    void testFormatPptx() throws Exception {
        // PPTX presentation extraction using extract
        var input = JsonUtil.fromJson("{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.presentationml.presentation\",\"uri\":\"pptx/simple.pptx\"}", ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());


    }


    @Test
    void testFormatXlsx() throws Exception {
        // XLSX spreadsheet extraction using extract
        var input = JsonUtil.fromJson("{\"kind\":\"uri\",\"mime_type\":\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\"uri\":\"xlsx/stanley_cups.xlsx\"}", ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());


    }

}
