// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9b6c82fdc18c3106bdec2c926b44f0262b7dcc51189a52c181c04410d16a2fd9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;
import org.junit.jupiter.api.BeforeAll;
/** E2e tests for category: url. */
public class UrlTest {
    @BeforeAll
    static void initEnv() {        if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
        System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true");
    }    }

    @Test
    void testUrlBatchMixedInputs() throws Exception {
        // extract_batch: mixed bytes and URL inputs share one output envelope
        var config = JsonUtil.fromJson("{\"url\":{\"mode\":\"document\"}}", ExtractionConfig.class);

        String inputsMockBaseUrl = System.getProperty("mockServer.url_batch_mixed_inputs", System.getenv().getOrDefault("MOCK_SERVER_URL_BATCH_MIXED_INPUTS", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/url_batch_mixed_inputs"));
        String inputsJson0 = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replace("$mock_url", inputsMockBaseUrl);
        String inputsJson1 = "{\"kind\":\"bytes\",\"bytes\":[66,97,116,99,104,32,98,121,116,101,115,32,99,111,110,116,101,110,116],\"mime_type\":\"text/plain\",\"filename\":\"inline.txt\"}".replace("$mock_url", inputsMockBaseUrl);
        var result = Xberg.extractBatch(java.util.Arrays.asList(JsonUtil.fromJson(inputsJson0, ExtractInput.class), JsonUtil.fromJson(inputsJson1, ExtractInput.class)), config);
        assertTrue(result.results().size() >= 2, "expected at least 2 elements");assertTrue(result.results().get(0).content().contains("Batch URL document content"), "expected to contain: " + "Batch URL document content");assertTrue(result.results().get(1).content().contains("Batch bytes content"), "expected to contain: " + "Batch bytes content");

    }


    @Test
    void testUrlCrawlLinkedPages() throws Exception {
        // extract: crawl mode follows linked pages
        String inputMockBaseUrl = System.getProperty("mockServer.url_crawl_linked_pages", System.getenv().getOrDefault("MOCK_SERVER_URL_CRAWL_LINKED_PAGES", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/url_crawl_linked_pages"));
        String inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"url\":{\"mode\":\"crawl\",\"crawl\":{\"max_depth\":1,\"max_pages\":4,\"respect_robots_txt\":false}}}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertTrue(result.summary().pagesCrawled() >= 2, "expected >= 2");assertTrue(result.results().get(1).content().contains("About crawl target"), "expected to contain: " + "About crawl target");

    }


    @Test
    void testUrlHtmlPageExtract() throws Exception {
        // extract: website URL returns page content
        String inputMockBaseUrl = System.getProperty("mockServer.url_html_page_extract", System.getenv().getOrDefault("MOCK_SERVER_URL_HTML_PAGE_EXTRACT", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/url_html_page_extract"));
        String inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"url\":{\"mode\":\"document\"}}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertTrue(result.results().get(0).content().contains("Xberg URL Page"), "expected to contain: " + "Xberg URL Page");assertTrue(result.results().size() >= 1, "expected at least 1 elements");

    }


    @Test
    void testUrlRecursiveDocumentUrls() throws Exception {
        // extract: recursive URL extraction follows document links discovered in results
        String inputMockBaseUrl = System.getProperty("mockServer.url_recursive_document_urls", System.getenv().getOrDefault("MOCK_SERVER_URL_RECURSIVE_DOCUMENT_URLS", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/url_recursive_document_urls"));
        String inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"url\":{\"mode\":\"document\",\"crawl\":{\"follow_document_urls\":true,\"document_url_depth\":1,\"respect_robots_txt\":false}}}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertTrue(result.results().size() >= 2, "expected at least 2 elements");assertTrue(result.results().get(1).content().contains("Recursive document target"), "expected to contain: " + "Recursive document target");

    }


    @Test
    void testUrlRemoteTextDocument() throws Exception {
        // extract: remote text document URL
        String inputMockBaseUrl = System.getProperty("mockServer.url_remote_text_document", System.getenv().getOrDefault("MOCK_SERVER_URL_REMOTE_TEXT_DOCUMENT", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/url_remote_text_document"));
        String inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"url\":{\"mode\":\"document\"}}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertTrue(result.results().get(0).content().contains("Remote document hello"), "expected to contain: " + "Remote document hello");assertEquals(1, result.summary().remoteUrls());

    }

}
