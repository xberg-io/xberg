// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9b6c82fdc18c3106bdec2c926b44f0262b7dcc51189a52c181c04410d16a2fd9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;
import org.junit.jupiter.api.BeforeAll;
/** E2e tests for category: summarization. */
public class SummarizationTest {
    @BeforeAll
    static void initEnv() {        if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
        System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true");
    }    }

    @Test
    void testSummarizationExtractiveSmoke() throws Exception {
        // TextRank extractive summary over a multi-paragraph plain text document. Pure-Rust, deterministic, no external services required.
        String inputMockBaseUrl = System.getProperty("mockServer.summarization_extractive_smoke", System.getenv().getOrDefault("MOCK_SERVER_SUMMARIZATION_EXTRACTIVE_SMOKE", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/summarization_extractive_smoke"));
        String inputJson = "{\"kind\":\"uri\",\"uri\":\"$mock_url/text/book_war_and_peace_1p.txt\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);
        var config = JsonUtil.fromJson("{\"summarization\":{\"strategy\":\"extractive\",\"max_tokens\":80}}", ExtractionConfig.class);

        var result = Xberg.extract(input, config);
        assertEquals("text/plain", result.results().get(0).mimeType().trim());assertFalse(result.results().get(0).summary().text().isEmpty(), "expected non-empty value");assertEquals("extractive", result.results().get(0).summary().strategy().getValue().trim());

    }

}
