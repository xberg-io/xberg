// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6b85539a5f005cf6ce39fca5d19714afbcd0b0cb0de71b5126459325834b61d4
// To regenerate: alef e2e generate
// To verify freshness: alef verify

package io.xberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import io.xberg.Xberg;
import io.xberg.ExtractInput;
import io.xberg.ExtractionConfig;
import java.util.Optional;
import io.xberg.JsonUtil;
import org.junit.jupiter.api.BeforeAll;

/** E2e tests for category: code. */
public class CodeTest {

    @BeforeAll
    static void initEnv() {
        if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
            System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true");
        }
        if (System.getProperty("RUST_MIN_STACK") == null) {
            System.setProperty("RUST_MIN_STACK", "16777216");
        }
    }


    @Test
    void testCodeShebangDetection() throws Exception {
        // Test language detection from shebang line via bytes input
        String inputMockBaseUrl = System.getProperty("mockServer.code_shebang_detection", System.getenv().getOrDefault("MOCK_SERVER_CODE_SHEBANG_DETECTION", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/code_shebang_detection"));
        String inputJson = "{\"kind\":\"uri\",\"mime_type\":\"text/x-source-code\",\"uri\":\"$mock_url/code/script.sh\"}".replace("$mock_url", inputMockBaseUrl);
        var input = JsonUtil.fromJson(inputJson, ExtractInput.class);

        var result = Xberg.extract(input, ExtractionConfig.builder().build());
        assertEquals("text/x-source-code", result.results().get(0).mimeType());
        assertTrue(result.results().get(0).content().length() >= 10, "expected length >= 10");
        assertTrue(result.results().get(0).content().contains("build"), "expected to contain: " + "build");
        assertTrue(result.results().get(0).content().contains("clean"), "expected to contain: " + "clean");


    }

}
