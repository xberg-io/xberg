// n8n loads nodes via the `n8n` block in package.json (see the `nodes` array),
// not through this entry point. This file exists only to satisfy the
// package `main` field and npm tooling; it intentionally exports nothing. ~keep
module.exports = {};
