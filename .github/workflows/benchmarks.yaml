name: Benchmarks

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Optional branch or commit (defaults to the dispatched revision)"
        required: false
        default: ""
        type: string
      timeout:
        description: "Timeout per document in seconds"
        required: false
        default: "900"
        type: string

env:
  ITERATIONS: "3"
  TIKA_VERSION: "3.3.1"
  ORT_VERSION: "1.24.2"
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  CARGO_PROFILE_DEV_DEBUG: 0
  RUST_BACKTRACE: short
  RUST_MIN_STACK: 16777216
  RUSTFLAGS: "-C strip=symbols"
  MEASURE_QUALITY: "true"
  OCR_ENABLED: "true"
  RUN_OCR_BENCHMARKS: "true"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  GROUND_TRUTH_DIR: "test_documents/ground_truth"

concurrency:
  group: benchmarks-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  setup:
    name: Build harness + native libs
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
      id-token: write
    outputs:
      artifact-name: benchmarks-target
      source-sha: ${{ steps.source-revision.outputs.sha }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch || github.sha }}

          submodules: recursive
          lfs: true
      - name: Pin benchmark source revision
        id: source-revision
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Ensure benchmark harness exists
        run: scripts/benchmarks/ensure-benchmark-harness-exists.sh

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-setup
          use-sccache: "false"
          disable-cache: "false"
          report-stats: "true"

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Layout Models
        uses: ./.github/actions/setup-layout-models

      - name: Install Task
        uses: xberg-io/actions/install-task@v1

      - name: Install BLAKE3 verifier
        run: |
          sudo apt-get update
          sudo apt-get install -y b3sum

      - name: Validate benchmark scripts
        run: task benchmark:validate:scripts

      - name: Cache benchmark harness
        uses: ./.github/actions/cache-benchmark-harness
        with:
          build-profile: release

      - name: Build xberg-cli (release, all features)
        run: cargo build --locked --release -p xberg-cli --features all

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_BENCHMARK_SA }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Install zstd (corpus cache decompression)
        run: sudo apt-get install -y zstd

      - name: Restore reference corpus cache
        env:
          GCP_BENCHMARK_BUCKET: ${{ secrets.GCP_BENCHMARK_BUCKET }}
        run: scripts/benchmarks/restore-corpus-cache.sh

      - name: Validate ground truth
        run: cargo run --locked --release -p benchmark-harness -- validate-gt --fixtures tools/benchmark-harness/fixtures/pdf/ --strict

      - name: Validate benchmark cohorts
        run: cargo test --locked --release -p benchmark-harness 'cohort::tests::'

      - name: Package reference corpus cache
        run: tar -C test_documents -cf corpus-cache.tar .corpus-cache/pdf .corpus-cache/ground_truth

      - name: Upload reference corpus cache
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-corpus-cache
          path: corpus-cache.tar
          retention-days: 7

      - name: Log disk space before artifact upload
        run: scripts/ci/validate/show-disk-space.sh "Disk space before artifact upload"

      - name: Upload build artifacts (harness binary + xberg-cli)
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-target
          path: |
            target/release/benchmark-harness
            target/release/xberg
          retention-days: 7
          if-no-files-found: warn

      - name: Upload benchmark harness binary (for third-party jobs)
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-harness-binary
          path: |
            target/release/benchmark-harness
          retention-days: 7

      - name: Log disk space after artifact upload
        run: scripts/ci/validate/show-disk-space.sh "Disk space after artifact upload"

  bench-rust:
    name: bench-rust (${{ matrix.pipeline }}, ${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: setup
    runs-on: ${{ matrix.pipeline == 'layout' && 'runner-large-arm64' || 'runner-medium-arm64' }}
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        pipeline: [baseline, layout]
        output_format: [markdown, plaintext]
        mode: [single-file, batch]
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Install system dependencies
        uses: ./.github/actions/install-system-deps

      - name: Setup Rust toolchain
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-rust-${{ matrix.pipeline }}
          use-sccache: "true"
          report-stats: "true"

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.setup.outputs.artifact-name }}
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Layout Models
        if: matrix.pipeline == 'layout'
        uses: ./.github/actions/setup-layout-models

      - name: Run benchmark
        env:
          FRAMEWORK: xberg-${{ matrix.output_format }}-${{ matrix.pipeline }}
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          BATCH_SIZE: ${{ matrix.mode == 'batch' && (matrix.cohort == 'native-pdf-fast-b8' && 8 || 4) || '' }}
          OCR_ENABLED: ${{ matrix.cohort == 'ocr-pdf-fast-b4' }}
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-rust-${{ matrix.pipeline }}-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-docling:
    name: docling (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file, batch]
        output_format: [markdown, plaintext]
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-docling
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-docling"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Run benchmark
        env:
          FRAMEWORK: docling
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          BATCH_SIZE: ${{ matrix.mode == 'batch' && (matrix.cohort == 'native-pdf-fast-b8' && 8 || 4) || '' }}
          OCR_ENABLED: ${{ matrix.cohort == 'ocr-pdf-fast-b4' }}
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-docling-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-markitdown:
    name: markitdown (${{ matrix.output_format }}, ${{ matrix.mode }}, native-pdf-fast-b8)
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-markitdown
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-markitdown --prerelease=allow"

      - name: Run benchmark
        env:
          FRAMEWORK: markitdown
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          OCR_ENABLED: "false"
          COHORT: tools/benchmark-harness/cohorts/native-pdf-fast-b8.json
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-markitdown-${{ matrix.output_format }}-${{ matrix.mode }}-native-pdf-fast-b8-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-unstructured:
    name: unstructured (${{ matrix.output_format }}, ${{ matrix.mode }}, native-pdf-fast-b8)
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [plaintext]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libmagic-dev poppler-utils libreoffice pandoc

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-unstructured
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-unstructured"

      - name: Run benchmark
        env:
          FRAMEWORK: unstructured
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/native-pdf-fast-b8.json
          OCR_ENABLED: "false"
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-unstructured-${{ matrix.output_format }}-${{ matrix.mode }}-native-pdf-fast-b8-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-tika:
    name: tika (${{ matrix.output_format }}, ${{ matrix.mode }}, native-pdf-fast-b8)
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        exclude:
          - output_format: markdown
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Java
        uses: actions/setup-java@v5
        id: setup-java
        with:
          distribution: "temurin"
          java-version: "25"

      - name: Download Apache Tika
        run: |
          mkdir -p tools/benchmark-harness/libs
          curl -fsSL --retry 5 --retry-delay 5 -o "tools/benchmark-harness/libs/tika-app-${{ env.TIKA_VERSION }}.jar" \
            "https://repo1.maven.org/maven2/org/apache/tika/tika-app/${{ env.TIKA_VERSION }}/tika-app-${{ env.TIKA_VERSION }}.jar"

      - name: Run benchmark
        env:
          FRAMEWORK: tika
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/native-pdf-fast-b8.json
          OCR_ENABLED: "false"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-tika-${{ matrix.output_format }}-${{ matrix.mode }}-native-pdf-fast-b8-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-pymupdf4llm:
    name: pymupdf4llm (${{ matrix.output_format }}, ${{ matrix.mode }}, native-pdf-fast-b8)
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libjpeg-dev libpng-dev libtiff-dev

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-pymupdf4llm
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-pymupdf4llm"

      - name: Run benchmark
        env:
          FRAMEWORK: pymupdf4llm
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          TESSDATA_PREFIX: /usr/share/tesseract-ocr/5/tessdata
          COHORT: tools/benchmark-harness/cohorts/native-pdf-fast-b8.json
          OCR_ENABLED: "false"
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-pymupdf4llm-${{ matrix.output_format }}-${{ matrix.mode }}-native-pdf-fast-b8-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  prefetch-mineru-models:
    name: Prefetch MinerU model weights
    needs: setup
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 120
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}
          submodules: recursive
          lfs: true

      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-mineru
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-mineru"

      - name: Install system dependencies (libgl1 + libglib2.0-0 for mineru)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Configure Hugging Face cache
        run: |
          mkdir -p "$HOME/.cache/huggingface/hub"
          {
            echo "HF_HOME=$HOME/.cache/huggingface"
            echo "HF_HUB_CACHE=$HOME/.cache/huggingface/hub"
            echo "MINERU_MODEL_SOURCE=huggingface"
          } >> "$GITHUB_ENV"

      - name: Restore MinerU model cache
        id: mineru-model-cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/huggingface/hub
          key: mineru-models-3.4.4-${{ runner.os }}-${{ runner.arch }}

      - name: Warm MinerU model weights (native + OCR pipelines)
        if: steps.mineru-model-cache.outputs.cache-hit != 'true'
        env:
          UV_NO_SYNC: "1"
        run: |
          set -euo pipefail
          txt_fixture="test_documents/pdf/fake_memo.pdf"
          ocr_fixture="test_documents/pdf/non_searchable.pdf"
          echo "Warming MinerU pipeline weights (txt path) on ${txt_fixture}..."
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --no-ocr --format=markdown sync "$txt_fixture" > /dev/null
          echo "Warming MinerU OCR weights (ocr path) on ${ocr_fixture}..."
          uv run --no-sync python tools/benchmark-harness/scripts/mineru_extract.py \
            --ocr --format=markdown sync "$ocr_fixture" > /dev/null
          echo "MinerU model cache populated at $HF_HUB_CACHE"
          du -sh "$HF_HUB_CACHE" || true

      # actions/cache is best-effort cross-run only: a ~1.1 GB blob gets evicted
      # from the shared 10 GB repo budget by concurrent CI before bench-mineru
      # runs. Deliver the weights to the bench jobs via a run-scoped artifact,
      # which is immune to cache eviction. tar preserves the Hugging Face hub
      # blob/snapshot symlink layout that a plain artifact upload would flatten.
      - name: Package MinerU model cache
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/mineru-model-cache"
          tar -C "$HOME/.cache/huggingface" -cf "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar" hub
          ls -lh "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar"

      - name: Upload MinerU model cache
        uses: actions/upload-artifact@v7
        with:
          name: mineru-model-cache-${{ github.run_id }}
          path: ${{ runner.temp }}/mineru-model-cache/mineru-models.tar
          retention-days: 1
          if-no-files-found: error

  bench-mineru:
    name: mineru (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust, prefetch-mineru-models]
    if: ${{ needs.setup.result == 'success' && needs.prefetch-mineru-models.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file]
        output_format: [markdown, plaintext]
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4]
        exclude:
          - output_format: plaintext
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Python
        uses: xberg-io/actions/setup-python-env@v1
        with:
          python-version: "3.11"
          cache-prefix: benchmark-mineru
          install-command: "uv sync --no-install-project --no-install-workspace --group dev --group bench-mineru"

      - name: Install system dependencies (libgl1 + libglib2.0-0 for mineru)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1 libglib2.0-0 tesseract-ocr tesseract-ocr-eng

      - name: Configure Hugging Face cache
        run: |
          mkdir -p "$HOME/.cache/huggingface/hub"
          {
            echo "HF_HOME=$HOME/.cache/huggingface"
            echo "HF_HUB_CACHE=$HOME/.cache/huggingface/hub"
            echo "MINERU_MODEL_SOURCE=huggingface"
            # The weights are delivered complete via the run-scoped artifact
            # below, so resolve purely from the local cache. Without this,
            # huggingface_hub still makes revision/ETag calls to huggingface.co
            # at extraction time; this runner cannot reach HF, so those calls
            # hang out the full per-doc timeout (900s) even though every weight
            # is already on disk. Offline mode also turns a genuinely missing
            # file into an immediate error instead of a timeout.
            echo "HF_HUB_OFFLINE=1"
            echo "TRANSFORMERS_OFFLINE=1"
          } >> "$GITHUB_ENV"

      # Model weights arrive as a run-scoped artifact from prefetch-mineru-models
      # (actions/cache is unreliable here: the ~1.1 GB blob is evicted from the
      # shared repo cache budget by concurrent CI before this job runs).
      - name: Download MinerU model cache
        uses: actions/download-artifact@v8
        with:
          name: mineru-model-cache-${{ github.run_id }}
          path: ${{ runner.temp }}/mineru-model-cache

      - name: Unpack MinerU model cache
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.cache/huggingface"
          tar -C "$HOME/.cache/huggingface" -xf "$RUNNER_TEMP/mineru-model-cache/mineru-models.tar"
          du -sh "$HOME/.cache/huggingface/hub" || true

      - name: Run benchmark
        env:
          FRAMEWORK: mineru
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          OCR_ENABLED: ${{ matrix.cohort == 'ocr-pdf-fast-b4' }}
          UV_NO_SYNC: "1"
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-mineru-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  bench-liteparse:
    name: liteparse (${{ matrix.output_format }}, ${{ matrix.mode }}, ${{ matrix.cohort }})
    needs: [setup, bench-rust]
    if: ${{ needs.setup.result == 'success' && !cancelled() }}
    runs-on: runner-medium-arm64
    timeout-minutes: 360
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        mode: [single-file, batch]
        output_format: [markdown, plaintext]
        cohort: [native-pdf-fast-b8, ocr-pdf-fast-b4]
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha }}

          submodules: recursive
          lfs: true
      - name: Free disk space
        uses: xberg-io/actions/free-disk-space-linux@v1

      - name: Download benchmark harness binary
        uses: actions/download-artifact@v8
        with:
          name: benchmark-harness-binary
          path: target/release

      - name: Restore benchmark binary permissions
        run: scripts/benchmarks/restore-binary-permissions.sh

      - name: Pull test_documents LFS objects
        run: git -C test_documents lfs pull

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Install system dependencies (libheif + native libs for harness binary)
        uses: ./.github/actions/install-system-deps

      - name: Setup ONNX Runtime
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Setup Rust toolchain
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: benchmarks-liteparse
          use-sccache: "true"

      - name: Install system tesseract (liteparse default feature)
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-eng libtesseract-dev libleptonica-dev

      - name: Install liteparse CLI (lit)
        run: |
          cargo install liteparse --version 2.9.0 --locked
          lit --version

      - name: Run benchmark
        env:
          FRAMEWORK: liteparse
          MODE: ${{ matrix.mode }}
          OUTPUT_FORMAT: ${{ matrix.output_format }}
          ITERATIONS: ${{ env.ITERATIONS }}
          TIMEOUT: ${{ github.event.inputs.timeout }}
          COHORT: tools/benchmark-harness/cohorts/${{ matrix.cohort }}.json
          BATCH_SIZE: ${{ matrix.mode == 'batch' && (matrix.cohort == 'native-pdf-fast-b8' && 8 || 4) || '' }}
          OCR_ENABLED: ${{ matrix.cohort == 'ocr-pdf-fast-b4' }}
        run: scripts/benchmarks/run-benchmark.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: benchmarks-liteparse-${{ matrix.output_format }}-${{ matrix.mode }}-${{ matrix.cohort }}-${{ github.run_id }}
          path: benchmark-results/
          retention-days: 30

  aggregate-and-publish:
    name: Aggregate & Release Results
    needs:
      [
        setup,
        bench-rust,
        bench-docling,
        bench-markitdown,
        bench-unstructured,
        bench-tika,
        bench-pymupdf4llm,
        bench-mineru,
        bench-liteparse,
      ]
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: write
    if: ${{ always() && !cancelled() && (github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch') }}
    steps:
      - name: Create GitHub App token (if credentials available)
        id: app-token
        continue-on-error: true
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          owner: xberg-dev

      - uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.source-sha || github.sha }}
          submodules: recursive
          lfs: true

      - name: Download reference corpus cache
        uses: actions/download-artifact@v8
        with:
          name: benchmark-corpus-cache
          path: ${{ runner.temp }}/benchmark-corpus-cache

      - name: Unpack reference corpus cache
        run: tar -C test_documents -xf "${RUNNER_TEMP}/benchmark-corpus-cache/corpus-cache.tar"

      - name: Setup Rust
        if: needs.setup.result == 'success'
        uses: xberg-io/actions/setup-rust@v1
        with:
          cache-key-prefix: aggregate

      - name: Install system dependencies (libheif + native libs for harness binary)
        if: needs.setup.result == 'success'
        uses: ./.github/actions/install-system-deps

      - name: Install BLAKE3 verifier
        if: needs.setup.result == 'success'
        run: |
          sudo apt-get update
          sudo apt-get install -y b3sum

      - name: Setup ONNX Runtime
        if: needs.setup.result == 'success'
        uses: ./.github/actions/setup-onnx-runtime
        with:
          ort-version: ${{ env.ORT_VERSION }}

      - name: Build xberg-cli (release, all features) for disk measurement
        if: needs.setup.result == 'success'
        run: cargo build --locked --release -p xberg-cli --features all

      - name: Download native benchmark artifacts
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: "benchmarks-*-native-pdf-fast-b8-${{ github.run_id }}"
          path: benchmark-artifacts/native/
          merge-multiple: false

      - name: Download OCR benchmark artifacts
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          pattern: "benchmarks-*-ocr-pdf-fast-b4-${{ github.run_id }}"
          path: benchmark-artifacts/ocr/
          merge-multiple: false

      - name: Validate native benchmark artifacts
        id: validate-native
        if: always()
        run: >-
          python3 scripts/ci/benchmarks/validate-benchmark-artifacts.py
          --cohort native
          --artifacts-dir benchmark-artifacts/native
          --cohort-manifest tools/benchmark-harness/cohorts/native-pdf-fast-b8.json
          --fixtures-root tools/benchmark-harness/fixtures
          --source-sha '${{ needs.setup.outputs.source-sha }}'
          --run-id '${{ github.run_id }}'
          --iterations '${{ env.ITERATIONS }}'

      - name: Validate OCR benchmark artifacts
        id: validate-ocr
        if: always()
        run: >-
          python3 scripts/ci/benchmarks/validate-benchmark-artifacts.py
          --cohort ocr
          --artifacts-dir benchmark-artifacts/ocr
          --cohort-manifest tools/benchmark-harness/cohorts/ocr-pdf-fast-b4.json
          --fixtures-root tools/benchmark-harness/fixtures
          --source-sha '${{ needs.setup.outputs.source-sha }}'
          --run-id '${{ github.run_id }}'
          --iterations '${{ env.ITERATIONS }}'

      - name: Consolidate native and OCR results
        if: ${{ always() && steps.validate-native.outcome == 'success' && steps.validate-ocr.outcome == 'success' }}
        run: |
          set -euo pipefail
          NATIVE_INPUTS=$(find benchmark-artifacts/native -mindepth 1 -maxdepth 1 -type d | sort | paste -sd, -)
          OCR_INPUTS=$(find benchmark-artifacts/ocr -mindepth 1 -maxdepth 1 -type d | sort | paste -sd, -)
          cargo run --locked --release --package benchmark-harness --bin benchmark-harness -- consolidate \
            --inputs "$NATIVE_INPUTS" \
            --output consolidated-output/native/
          cargo run --locked --release --package benchmark-harness --bin benchmark-harness -- consolidate \
            --inputs "$OCR_INPUTS" \
            --output consolidated-output/ocr/

      - name: Validate native and OCR aggregates
        id: validate-aggregates
        if: ${{ always() && steps.validate-native.outcome == 'success' && steps.validate-ocr.outcome == 'success' }}
        run: |
          set -euo pipefail
          python3 scripts/ci/benchmarks/validate-benchmark-artifacts.py \
            --cohort native \
            --aggregated-file consolidated-output/native/aggregated.json
          python3 scripts/ci/benchmarks/validate-benchmark-artifacts.py \
            --cohort ocr \
            --aggregated-file consolidated-output/ocr/aggregated.json

      - name: Prepare cohort release assets
        id: prepare-release-assets
        if: ${{ always() && steps.validate-aggregates.outcome == 'success' }}
        run: |
          set -euo pipefail
          mkdir -p release-assets
          cp consolidated-output/native/aggregated.json release-assets/benchmarks-native-aggregated.json
          cp consolidated-output/ocr/aggregated.json release-assets/benchmarks-ocr-aggregated.json
          SOURCE_SHA="${{ needs.setup.outputs.source-sha }}"
          UPDATED_AT="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          jq -n \
            --arg cohort "native-pdf-fast-b8-v1" \
            --arg cohort_hash "c10d1f78d3f9d61070c0d91e7bfe90a904b69c9d3261536eb1dcff2081c73f6b" \
            --arg commit "$SOURCE_SHA" \
            --arg run_id "${{ github.run_id }}" \
            --arg run_url "$RUN_URL" \
            --arg updated_at "$UPDATED_AT" \
            '{cohort: $cohort, cohort_manifest_blake3: $cohort_hash, expected_matrix_keys: 21, commit: $commit, run_id: $run_id, run_url: $run_url, updated_at: $updated_at}' \
            > release-assets/benchmarks-native-metadata.json
          jq -n \
            --arg cohort "ocr-pdf-fast-b4-v1" \
            --arg cohort_hash "c740a9480fd0ad4311a4905206345c58d7ef7a987682cc96644e2f0ffe616a13" \
            --arg commit "$SOURCE_SHA" \
            --arg run_id "${{ github.run_id }}" \
            --arg run_url "$RUN_URL" \
            --arg updated_at "$UPDATED_AT" \
            '{cohort: $cohort, cohort_manifest_blake3: $cohort_hash, expected_matrix_keys: 17, commit: $commit, run_id: $run_id, run_url: $run_url, updated_at: $updated_at}' \
            > release-assets/benchmarks-ocr-metadata.json
          jq -n \
            --arg commit "$SOURCE_SHA" \
            --arg run_id "${{ github.run_id }}" \
            --arg run_url "$RUN_URL" \
            --arg updated_at "$UPDATED_AT" \
            '{
              schema_version: 1,
              commit: $commit,
              run_id: $run_id,
              run_url: $run_url,
              updated_at: $updated_at,
              cohorts: {
                native: {aggregated: "benchmarks-native-aggregated.json", metadata: "benchmarks-native-metadata.json"},
                ocr: {aggregated: "benchmarks-ocr-aggregated.json", metadata: "benchmarks-ocr-metadata.json"}
              }
            }' > release-assets/benchmarks-index.json

      - name: Create GitHub Release with benchmark results
        if: >-
          ${{
            always() &&
            steps.validate-native.outcome == 'success' &&
            steps.validate-ocr.outcome == 'success' &&
            steps.validate-aggregates.outcome == 'success' &&
            steps.prepare-release-assets.outcome == 'success' &&
            needs.setup.result == 'success' &&
            needs.bench-rust.result == 'success' &&
            needs.bench-docling.result == 'success' &&
            needs.bench-markitdown.result == 'success' &&
            needs.bench-unstructured.result == 'success' &&
            needs.bench-tika.result == 'success' &&
            needs.bench-pymupdf4llm.result == 'success' &&
            needs.bench-mineru.result == 'success' &&
            needs.bench-liteparse.result == 'success'
          }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          echo "=== Creating GitHub Release with benchmark results ==="

          SOURCE_SHA="${{ needs.setup.outputs.source-sha }}"
          SHORT_SHA="${SOURCE_SHA}"
          SHORT_SHA="${SHORT_SHA:0:7}"
          TAG="benchmark-run-${{ github.run_id }}"
          DATE=$(date -u +"%Y-%m-%d")

          gh release create "$TAG" \
            --prerelease \
            --target "${SOURCE_SHA}" \
            --title "Benchmark Results ${DATE} (${SHORT_SHA})" \
            --notes "Comparative benchmark results from workflow run [${{ github.run_id }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}).

          **Commit:** ${SOURCE_SHA}
          **Date:** ${DATE}" \
            release-assets/benchmarks-native-aggregated.json \
            release-assets/benchmarks-native-metadata.json \
            release-assets/benchmarks-ocr-aggregated.json \
            release-assets/benchmarks-ocr-metadata.json \
            release-assets/benchmarks-index.json
