name: Validate Issues

on:
  issues:
    types: [opened, edited]

permissions:
  issues: write
  repository-projects: write

jobs:
  validate:
    uses: jamon8888/actions/.github/workflows/reusable-validate-issues.yml@v1
    secrets: inherit
