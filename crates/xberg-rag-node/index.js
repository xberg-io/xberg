// @ts-nocheck
/* generated by napi-rs for xberg-rag-node */

'use strict'

const { existsSync } = require('node:fs')
const { join } = require('node:path')

let nativeBinding = null
const loadErrors = []

const localFile = (name) => join(__dirname, name)

if (process.platform === 'win32' && process.arch === 'x64') {
  const f = localFile('xberg_rag_node.win32-x64-msvc.node')
  if (existsSync(f)) {
    try { nativeBinding = require(f) } catch (e) { loadErrors.push(e) }
  }
} else if (process.platform === 'linux' && process.arch === 'x64') {
  const f = localFile('xberg_rag_node.linux-x64-gnu.node')
  if (existsSync(f)) {
    try { nativeBinding = require(f) } catch (e) { loadErrors.push(e) }
  }
} else if (process.platform === 'darwin') {
  const f = localFile('xberg_rag_node.darwin-arm64.node')
  if (existsSync(f)) {
    try { nativeBinding = require(f) } catch (e) { loadErrors.push(e) }
  }
}

if (!nativeBinding) {
  throw new Error(
    `Failed to load xberg-rag-node native binding.\n` +
    `Platform: ${process.platform}-${process.arch}\n` +
    loadErrors.map(e => e.message).join('\n')
  )
}

module.exports = nativeBinding
