# Generated by alef. Do not edit by hand.
# alef:hash:ac22b703a8053dd8fa02ddc2b11dc259b02126bbe7b3490f7e51b76581ca792c

[package]
name = "xberg-jni"
version.workspace = true
edition = "2024"
license.workspace = true
description = "High-performance document intelligence library"
readme = false
keywords = ["document", "extraction", "ocr", "pdf", "text"]
categories = []

# `base64`, `futures-util`, `serde_json`, and `tokio` are emitted unconditionally below
# so the manifest is stable across regens (they are used when the umbrella
# crate declares binary top-level params, async fns, streaming adapters, or JSON-marshalled types),
# but for an umbrella crate that has none of those they are genuinely unused.
# List them here so `cargo machete` doesn't flag the no-async-no-streaming
# case as a real finding.
[package.metadata.cargo-machete]
ignored = ["async-trait", "base64", "futures-util", "serde_json", "tokio", "tracing"]

[lib]
name = "xberg_jni"
crate-type = ["cdylib"]

[dependencies]
async-trait.workspace = true
base64.workspace = true
futures-util = "0.3"
jni = "0.22"
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true

[target.'cfg(all(target_os = "macos", target_arch = "x86_64"))'.dependencies]
xberg = { version = "1.1.6", path = "../xberg", default-features = false, features = ["macos-intel-target", "simd-utf8"] }

[target.'cfg(not(any(target_os = "android", target_os = "ios", target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))))'.dependencies]
xberg = { version = "1.1.6", path = "../xberg", features = ["android-target", "no-ort-target", "pdf", "pdf-native", "html", "notebook", "xml", "email", "language-detection", "chunking", "chunking-tokenizers", "quality", "keywords", "keywords-yake", "keywords-rake", "markdown-footnotes", "enrichment", "heuristics", "office", "iwork", "hwp", "mdx", "archives", "stopwords", "embedding-presets", "static-embeddings", "reranker-presets", "sparse-embedding-presets", "late-interaction-presets", "layout-types", "auto-rotate-types", "paddle-ocr-types", "ner", "presets", "redaction", "summarization", "qr-codes", "svg", "image-encode", "transcription-types", "excel", "tree-sitter", "ocr", "ocr-pipeline", "sceptre-ocr-tract", "tract", "auto-rotate-tract", "layout-tract", "paddle-ocr-tract", "api", "api-types", "tower-service", "mcp", "liter-llm", "structured", "hwpx", "diff", "classification", "captioning", "ner-llm", "url-config-types", "url-ingestion", "tokio-runtime"] }

[target.'cfg(target_os = "android")'.dependencies]
xberg = { version = "1.1.6", path = "../xberg", default-features = false, features = ["android-target", "simd-utf8"] }

[target.'cfg(target_os = "ios")'.dependencies]
xberg = { version = "1.1.6", path = "../xberg", default-features = false, features = ["android-target", "simd-utf8"] }

[target.'cfg(target_os = "windows")'.dependencies]
xberg = { version = "1.1.6", path = "../xberg", default-features = false, features = ["windows-target", "simd-utf8"] }

# This crate deliberately does not use `[lints]` / `workspace = true`: its C-ABI /
# PyO3 / napi / ext-php-rs / NIF boundary requires `unsafe` code, and the workspace's
# `[workspace.lints.rust]` sets `unsafe_code = "deny"` -- an all-or-nothing table that
# would turn every such boundary into a compile error. The `[lints.clippy]` block below
# instead carries the subset of the workspace's deny-by-default lint policy this crate
# can actually satisfy. ~keep
[lints.clippy]
dbg_macro = "deny"
print_stderr = "deny"
print_stdout = "deny"
