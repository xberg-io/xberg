# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b525d88dfd4ccb81c5e4b4dcfb57b334cbe610ab3f6739017ab750b49ab3b23a
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code

[package]
name = "xberg-wasm"
version = "1.0.0-rc.1"
edition = "2024"
license = "MIT"
description = "High-performance document intelligence library"
repository = "https://github.com/xberg-io/xberg"
keywords = ["document", "extraction", "pdf", "ocr", "text"]

[package.metadata.cargo-machete]
ignored = [
    "futures",
    "futures-util",
    "js-sys",
    "wasm-bindgen-futures",
    "serde",
    "serde_json",
]

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[lib]
crate-type = ["cdylib"]

[features]
# Enabled by default so `wasm-pack build` (which builds default features) ships
# them: in-wasm rehydration (AES-GCM decrypt_map/rehydrate — gated in engine.rs)
# and YAKE/RAKE keyword extraction, both needed by the MCP server. All pure-Rust,
# wasm-safe (aes-gcm, scrypt, rake, stopwords — no tokio/native deps).
default = ["redaction-rehydrate", "keywords-yake", "keywords-rake"]
api = ["xberg/api"]
api-types = ["xberg/api-types"]
auto-rotate-types = ["xberg/auto-rotate-types"]
chunking-tokenizers = ["xberg/chunking-tokenizers"]
diff = ["xberg/diff"]
heic = ["xberg/heic"]
heuristics = ["xberg/heuristics"]
html = ["xberg/html"]
keywords-rake = ["xberg/keywords-rake"]
keywords-yake = ["xberg/keywords-yake"]
layout-types = ["xberg/layout-types"]
liter-llm = ["xberg/liter-llm"]
markdown-footnotes = ["xberg/markdown-footnotes"]
ocr = ["xberg/ocr"]
ocr-wasm = ["xberg/ocr-wasm"]
office = ["xberg/office"]
paddle-ocr-types = ["xberg/paddle-ocr-types"]
pdf = ["xberg/pdf"]
presets = ["xberg/presets"]
quality = ["xberg/quality"]
redaction = ["xberg/redaction"]
redaction-rehydrate = ["xberg/redaction-rehydrate"]
reranker = ["xberg/reranker"]
reranker-presets = ["xberg/reranker-presets"]
svg = ["xberg/svg"]
transcription-types = ["xberg/transcription-types"]
tree-sitter = ["xberg/tree-sitter"]
xml = ["xberg/xml"]

[dependencies]
async-trait = "0.1"
crawlberg = { workspace = true }
futures = "0.3"
futures-util = "0.3"
js-sys = "0.3"
serde = { version = "1", features = ["derive"] }
serde-wasm-bindgen = "0.6"
serde_json = "1"
sha2 = "0.10"
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
# `wasm-target` expanded minus `ocr-wasm`: the MCP/Node host injects OCR via the
# `ocr` bridge, so in-binary Tesseract (which pulls Leptonica + WASI fd/path/mkstemp/
# system imports that wasm-bindgen's nodejs target can't satisfy) is unnecessary here.
xberg = { path = "../xberg", default-features = false, features = ["no-ort-target", "excel-wasm", "tree-sitter-wasm", "ner-candle-wasm", "url-ingestion"] }
xberg-rag = { default-features = false, features = ["vector-store", "pipeline"], path = "../xberg-rag" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.4", features = ["wasm_js"] }
getrandom_02 = { package = "getrandom", version = "0.2", features = ["js"] }
getrandom_03 = { package = "getrandom", version = "0.3", features = ["wasm_js"] }
