// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:2c32aea7e7fa3cb250eebcf1961502da00184c458765d2f38a3c2ebc14b208a0
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables, missing_docs)]
#![allow(unsafe_code)]
#![allow(non_snake_case)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::map_identity,
    clippy::just_underscores_and_digits,
    clippy::unnecessary_cast,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::redundant_field_names,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::arc_with_non_send_sync,
    clippy::collapsible_if,
    clippy::clone_on_copy,
    clippy::should_implement_trait,
    clippy::useless_conversion
)]
#![cfg_attr(windows, feature(abi_vectorcall))]
#![cfg_attr(windows, allow(unused_features))]

use ext_php_rs::prelude::*;
use ext_php_rs::rc::PhpRc;
use std::collections::HashMap;
use std::sync::Arc;
use std::sync::Mutex;
use xberg::Validator;
use xberg::engine::seams::PresetResolver;

#[derive(Debug, Clone, Default)]
pub struct PhpBytes(pub Vec<u8>);

impl<'a> ext_php_rs::convert::FromZval<'a> for PhpBytes {
    const TYPE: ext_php_rs::flags::DataType = ext_php_rs::flags::DataType::String;
    fn from_zval(zval: &'a ext_php_rs::types::Zval) -> Option<Self> {
        zval.zend_str().map(|zs| PhpBytes(zs.as_bytes().to_vec()))
    }
}

impl From<PhpBytes> for Vec<u8> {
    fn from(b: PhpBytes) -> Self {
        b.0
    }
}

impl From<Vec<u8>> for PhpBytes {
    fn from(v: Vec<u8>) -> Self {
        PhpBytes(v)
    }
}

static WORKER_RUNTIME: std::sync::LazyLock<tokio::runtime::Runtime> = std::sync::LazyLock::new(|| {
    // 16 MiB: tokio's ~2 MB default worker stack can overflow on a deep extraction
    // future (a nested archive member, a multi-stage OCR pipeline), and a stack overflow
    // aborts the process with SIGBUS instead of raising a catchable panic.
    const WORKER_RUNTIME_STACK_SIZE_BYTES: usize = 16 * 1024 * 1024;
    tokio::runtime::Builder::new_multi_thread()
        .enable_all()
        .thread_stack_size(WORKER_RUNTIME_STACK_SIZE_BYTES)
        .build()
        .expect("Failed to create Tokio runtime")
});

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\AccelerationConfig")]
#[serde(default)]
pub struct AccelerationConfig {
    /// Execution provider to use for ONNX inference.
    #[php(prop, name = "provider")]
    #[serde(default = "crate::serde_defaults::acceleration_config_provider")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub provider: String,
    /// GPU device ID (for CUDA/TensorRT). Ignored for CPU/CoreML/Auto.
    #[php(prop, name = "deviceId")]
    #[serde(alias = "deviceId")]
    #[serde(default)]
    pub device_id: u32,
}

impl Default for AccelerationConfig {
    fn default() -> Self {
        <xberg::AccelerationConfig as Default>::default().into()
    }
}

#[php_impl]
impl AccelerationConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(provider: String, deviceId: u32) -> Self {
        Self {
            provider,
            device_id: deviceId,
        }
    }

    pub fn get_provider(&self) -> String {
        self.provider.clone()
    }

    pub fn get_device_id(&self) -> u32 {
        self.device_id.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ArchiveEntry")]
#[serde(default)]
pub struct ArchiveEntry {
    /// Archive-relative file path (e.g. "folder/document.pdf").
    #[php(prop, name = "path")]
    pub path: String,
    /// Detected MIME type of the file.
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
    /// Full extraction result for this file.
    pub result: ExtractedDocument,
}

#[php_impl]
impl ArchiveEntry {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(path: String, mimeType: String, result: &ExtractedDocument) -> Self {
        Self {
            path,
            mime_type: mimeType,
            result: result.clone(),
        }
    }

    pub fn get_path(&self) -> String {
        self.path.clone()
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }

    pub fn get_result(&self) -> ExtractedDocument {
        self.result.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ArchiveMetadata")]
#[serde(default)]
pub struct ArchiveMetadata {
    /// Archive format ("ZIP", "TAR", "7Z", etc.)
    #[php(prop, name = "format")]
    pub format: String,
    /// Total number of files in the archive
    #[php(prop, name = "fileCount")]
    #[serde(alias = "fileCount")]
    pub file_count: u32,
    /// List of file paths within the archive
    #[php(prop, name = "fileList")]
    #[serde(alias = "fileList")]
    pub file_list: Vec<String>,
    /// Total uncompressed size in bytes
    #[php(prop, name = "totalSize")]
    #[serde(alias = "totalSize")]
    pub total_size: i64,
    /// Compressed size in bytes (if available)
    #[php(prop, name = "compressedSize")]
    #[serde(alias = "compressedSize")]
    pub compressed_size: Option<i64>,
}

impl Default for ArchiveMetadata {
    fn default() -> Self {
        <xberg::ArchiveMetadata as Default>::default().into()
    }
}

#[php_impl]
impl ArchiveMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        format: String,
        fileCount: u32,
        fileList: Vec<String>,
        totalSize: i64,
        compressedSize: Option<i64>,
    ) -> Self {
        Self {
            format,
            file_count: fileCount,
            file_list: fileList,
            total_size: totalSize,
            compressed_size: compressedSize,
        }
    }

    pub fn get_format(&self) -> String {
        self.format.clone()
    }

    pub fn get_file_count(&self) -> u32 {
        self.file_count.clone()
    }

    pub fn get_file_list(&self) -> Vec<String> {
        self.file_list.clone()
    }

    pub fn get_total_size(&self) -> i64 {
        self.total_size.clone()
    }

    pub fn get_compressed_size(&self) -> Option<i64> {
        self.compressed_size.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\Attributes")]
#[serde(default)]
pub struct Attributes {
    /// Element ID (#identifier)
    #[php(prop, name = "id")]
    pub id: Option<String>,
    /// CSS classes (.class1 .class2)
    #[php(prop, name = "classes")]
    #[serde(default)]
    pub classes: Vec<String>,
    /// Key-value pairs (key="value")
    #[serde(alias = "keyValues")]
    #[serde(default)]
    pub key_values: Vec<KeyValueAttribute>,
}

impl Default for Attributes {
    fn default() -> Self {
        <xberg::Attributes as Default>::default().into()
    }
}

#[php_impl]
impl Attributes {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        classes: Vec<String>,
        keyValues: &ext_php_rs::types::ZendHashTable,
        id: Option<String>,
    ) -> PhpResult<Self> {
        let mut keyValues_core_result: Vec<KeyValueAttribute> = Vec::new();
        for (_, item) in keyValues.iter() {
            if let Some(parsed) = <&KeyValueAttribute as ext_php_rs::convert::FromZval>::from_zval(item) {
                keyValues_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to KeyValueAttribute".to_string(),
                ));
            }
        }
        let keyValues_core: Vec<KeyValueAttribute> = keyValues_core_result;
        Ok(Self {
            id,
            classes,
            key_values: keyValues_core,
        })
    }

    pub fn get_id(&self) -> Option<String> {
        self.id.clone()
    }

    pub fn get_classes(&self) -> Vec<String> {
        self.classes.clone()
    }

    pub fn get_key_values(&self) -> Vec<KeyValueAttribute> {
        self.key_values.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\AudioMetadata")]
#[serde(default)]
pub struct AudioMetadata {
    /// Duration in milliseconds derived from the decoded audio stream.
    #[php(prop, name = "durationMs")]
    #[serde(alias = "durationMs")]
    pub duration_ms: Option<i64>,
    /// Audio codec (e.g. "mp3", "aac", "opus", "flac").
    #[php(prop, name = "codec")]
    pub codec: Option<String>,
    /// Container format (e.g. "mpeg", "mp4", "ogg", "wav").
    #[php(prop, name = "container")]
    pub container: Option<String>,
    /// Sample rate in Hz after decode (always 16000 when resampled for Whisper).
    #[php(prop, name = "sampleRateHz")]
    #[serde(alias = "sampleRateHz")]
    pub sample_rate_hz: Option<u32>,
    /// Number of audio channels (1 = mono, 2 = stereo).
    #[php(prop, name = "channels")]
    pub channels: Option<u16>,
    /// Audio bitrate in kbps from the source file tags/properties.
    #[php(prop, name = "bitrate")]
    pub bitrate: Option<u32>,
}

impl Default for AudioMetadata {
    fn default() -> Self {
        <xberg::AudioMetadata as Default>::default().into()
    }
}

#[php_impl]
impl AudioMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_duration_ms(&self) -> Option<i64> {
        self.duration_ms.clone()
    }

    pub fn get_codec(&self) -> Option<String> {
        self.codec.clone()
    }

    pub fn get_container(&self) -> Option<String> {
        self.container.clone()
    }

    pub fn get_sample_rate_hz(&self) -> Option<u32> {
        self.sample_rate_hz.clone()
    }

    pub fn get_channels(&self) -> Option<u16> {
        self.channels.clone()
    }

    pub fn get_bitrate(&self) -> Option<u32> {
        self.bitrate.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\BBox")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct BBox {
    /// Left edge (x-coordinate of the top-left corner).
    #[php(prop, name = "x1")]
    pub x1: f32,
    /// Top edge (y-coordinate of the top-left corner).
    #[php(prop, name = "y1")]
    pub y1: f32,
    /// Right edge (x-coordinate of the bottom-right corner).
    #[php(prop, name = "x2")]
    pub x2: f32,
    /// Bottom edge (y-coordinate of the bottom-right corner).
    #[php(prop, name = "y2")]
    pub y2: f32,
}

#[php_impl]
impl BBox {
    #[php(constructor)]
    pub fn new(x1: f32, y1: f32, x2: f32, y2: f32) -> Self {
        Self { x1, y1, x2, y2 }
    }

    pub fn get_x1(&self) -> f32 {
        self.x1.clone()
    }

    pub fn get_y1(&self) -> f32 {
        self.y1.clone()
    }

    pub fn get_x2(&self) -> f32 {
        self.x2.clone()
    }

    pub fn get_y2(&self) -> f32 {
        self.y2.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\BedrockConfig")]
#[serde(default)]
pub struct BedrockConfig {
    /// AWS region (e.g. `"us-east-1"`).
    #[php(prop, name = "region")]
    #[serde(default)]
    pub region: Option<String>,
    /// Cross-region inference profile prefix (e.g. `"us"`).
    #[php(prop, name = "crossRegionPrefix")]
    #[serde(alias = "crossRegionPrefix")]
    #[serde(default)]
    pub cross_region_prefix: Option<String>,
    /// Explicit AWS access key ID. Secret — never logged.
    #[php(prop, name = "accessKeyId")]
    #[serde(alias = "accessKeyId")]
    #[serde(default)]
    pub access_key_id: Option<String>,
    /// Explicit AWS secret access key. Secret — never logged.
    #[php(prop, name = "secretAccessKey")]
    #[serde(alias = "secretAccessKey")]
    #[serde(default)]
    pub secret_access_key: Option<String>,
    /// Explicit AWS session token for temporary credentials. Secret — never logged.
    #[php(prop, name = "sessionToken")]
    #[serde(alias = "sessionToken")]
    #[serde(default)]
    pub session_token: Option<String>,
}

impl Default for BedrockConfig {
    fn default() -> Self {
        <xberg::BedrockConfig as Default>::default().into()
    }
}

#[php_impl]
impl BedrockConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_region(&self) -> Option<String> {
        self.region.clone()
    }

    pub fn get_cross_region_prefix(&self) -> Option<String> {
        self.cross_region_prefix.clone()
    }

    pub fn get_access_key_id(&self) -> Option<String> {
        self.access_key_id.clone()
    }

    pub fn get_secret_access_key(&self) -> Option<String> {
        self.secret_access_key.clone()
    }

    pub fn get_session_token(&self) -> Option<String> {
        self.session_token.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\BibtexMetadata")]
#[serde(default)]
pub struct BibtexMetadata {
    /// Number of entries in the bibliography.
    #[php(prop, name = "entryCount")]
    #[serde(alias = "entryCount")]
    pub entry_count: i64,
    /// BibTeX citation keys (e.g. `"knuth1984"`) for all entries.
    #[php(prop, name = "citationKeys")]
    #[serde(alias = "citationKeys")]
    #[serde(default)]
    pub citation_keys: Vec<String>,
    /// Author names collected across all bibliography entries.
    #[php(prop, name = "authors")]
    #[serde(default)]
    pub authors: Vec<String>,
    /// Earliest and latest publication years found in the bibliography.
    #[serde(alias = "yearRange")]
    pub year_range: Option<YearRange>,
    /// Count of entries grouped by BibTeX entry type (e.g. `"article"` → 5).
    #[php(prop, name = "entryTypes")]
    #[serde(alias = "entryTypes")]
    pub entry_types: Option<HashMap<String, i64>>,
}

impl Default for BibtexMetadata {
    fn default() -> Self {
        <xberg::BibtexMetadata as Default>::default().into()
    }
}

#[php_impl]
impl BibtexMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        entryCount: i64,
        citationKeys: Vec<String>,
        authors: Vec<String>,
        yearRange: Option<&YearRange>,
        entryTypes: Option<HashMap<String, i64>>,
    ) -> Self {
        Self {
            entry_count: entryCount,
            citation_keys: citationKeys,
            authors,
            year_range: yearRange.cloned(),
            entry_types: entryTypes,
        }
    }

    pub fn get_entry_count(&self) -> i64 {
        self.entry_count.clone()
    }

    pub fn get_citation_keys(&self) -> Vec<String> {
        self.citation_keys.clone()
    }

    pub fn get_authors(&self) -> Vec<String> {
        self.authors.clone()
    }

    pub fn get_year_range(&self) -> Option<YearRange> {
        self.year_range.clone().map(Into::into)
    }

    pub fn set_year_range(&mut self, value: Option<&YearRange>) {
        self.year_range = value.map(|value| value.clone().into());
    }

    pub fn get_entry_types(&self) -> Option<HashMap<String, i64>> {
        self.entry_types.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\BoundingBox")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct BoundingBox {
    /// Left x-coordinate
    #[php(prop, name = "x0")]
    pub x0: f64,
    /// Bottom y-coordinate
    #[php(prop, name = "y0")]
    pub y0: f64,
    /// Right x-coordinate
    #[php(prop, name = "x1")]
    pub x1: f64,
    /// Top y-coordinate
    #[php(prop, name = "y1")]
    pub y1: f64,
}

impl Default for BoundingBox {
    fn default() -> Self {
        <xberg::BoundingBox as Default>::default().into()
    }
}

#[php_impl]
impl BoundingBox {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(x0: f64, y0: f64, x1: f64, y1: f64) -> Self {
        Self { x0, y0, x1, y1 }
    }

    pub fn get_x0(&self) -> f64 {
        self.x0.clone()
    }

    pub fn get_y0(&self) -> f64 {
        self.y0.clone()
    }

    pub fn get_x1(&self) -> f64 {
        self.x1.clone()
    }

    pub fn get_y1(&self) -> f64 {
        self.y1.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\BrowserConfig")]
#[serde(default)]
pub struct BrowserConfig {
    /// When to use the headless browser fallback.
    #[php(prop, name = "mode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub mode: String,
    /// Browser backend used to render JavaScript-heavy pages.
    #[php(prop, name = "backend")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub backend: String,
    /// CDP WebSocket endpoint for connecting to an external browser instance.
    #[php(prop, name = "endpoint")]
    pub endpoint: Option<String>,
    /// Timeout for browser page load and rendering (in milliseconds when serialized).
    #[php(prop, name = "timeout")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub timeout: Option<i64>,
    /// Wait strategy after browser navigation.
    #[php(prop, name = "wait")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub wait: String,
    /// CSS selector to wait for when `wait` is `Selector`.
    #[php(prop, name = "waitSelector")]
    #[serde(alias = "waitSelector")]
    pub wait_selector: Option<String>,
    /// Extra time to wait after the wait condition is met.
    #[php(prop, name = "extraWait")]
    #[serde(alias = "extraWait")]
    #[serde(default)]
    pub extra_wait: Option<i64>,
    /// Proxy for browser fetches. Overrides `CrawlConfig.proxy` when set.
    /// Native backend supports http/https only (no SOCKS5).
    #[serde(default)]
    pub proxy: Option<ProxyConfig>,
    /// URL patterns to block before the network request fires. Supports `*`
    /// wildcards. Useful for skipping ads/analytics/large images. Honored by
    /// `BrowserBackend.Native`; chromiumoxide ignores this field today.
    #[php(prop, name = "blockUrlPatterns")]
    #[serde(alias = "blockUrlPatterns")]
    #[serde(default)]
    pub block_url_patterns: Vec<String>,
    /// JavaScript snippet evaluated after navigation completes.
    ///
    /// Scraping captures the native backend result in `ScrapeResult.browser.eval_result`.
    /// Interactions run this script before page actions on both browser backends but do
    /// not include the script result in `InteractionResult`.
    #[php(prop, name = "evalScript")]
    #[serde(alias = "evalScript")]
    #[serde(default)]
    pub eval_script: Option<String>,
    /// User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent`
    /// (or crawlberg's default) if unset. Native only.
    #[php(prop, name = "robotsUserAgent")]
    #[serde(alias = "robotsUserAgent")]
    #[serde(default)]
    pub robots_user_agent: Option<String>,
    /// Capture the full network event stream into the result. Default false
    /// (only the document event is captured). Native only.
    #[php(prop, name = "captureNetworkEvents")]
    #[serde(default = "crate::serde_defaults::browser_config_capture_network_events")]
    #[serde(alias = "captureNetworkEvents")]
    pub capture_network_events: bool,
    /// Enable session affinity: reuse chromiumoxide Pages for same-domain
    /// requests so cookies + fingerprint + solved challenges persist.
    /// Default: true. When false, each request gets a fresh Page.
    #[php(prop, name = "sessionAffinity")]
    #[serde(default = "crate::serde_defaults::browser_config_session_affinity")]
    #[serde(alias = "sessionAffinity")]
    pub session_affinity: bool,
}

impl Default for BrowserConfig {
    fn default() -> Self {
        <crawlberg::BrowserConfig as Default>::default().into()
    }
}

#[php_impl]
impl BrowserConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        mode: String,
        backend: String,
        wait: String,
        blockUrlPatterns: Vec<String>,
        captureNetworkEvents: bool,
        sessionAffinity: bool,
        endpoint: Option<String>,
        timeout: Option<i64>,
        waitSelector: Option<String>,
        extraWait: Option<i64>,
        proxy: Option<&ProxyConfig>,
        evalScript: Option<String>,
        robotsUserAgent: Option<String>,
    ) -> Self {
        Self {
            mode,
            backend,
            endpoint,
            timeout,
            wait,
            wait_selector: waitSelector,
            extra_wait: extraWait,
            proxy: proxy.cloned(),
            block_url_patterns: blockUrlPatterns,
            eval_script: evalScript,
            robots_user_agent: robotsUserAgent,
            capture_network_events: captureNetworkEvents,
            session_affinity: sessionAffinity,
        }
    }

    pub fn get_mode(&self) -> String {
        self.mode.clone()
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }

    pub fn get_endpoint(&self) -> Option<String> {
        self.endpoint.clone()
    }

    pub fn get_timeout(&self) -> Option<i64> {
        self.timeout.clone()
    }

    pub fn get_wait(&self) -> String {
        self.wait.clone()
    }

    pub fn get_wait_selector(&self) -> Option<String> {
        self.wait_selector.clone()
    }

    pub fn get_extra_wait(&self) -> Option<i64> {
        self.extra_wait.clone()
    }

    pub fn get_proxy(&self) -> Option<ProxyConfig> {
        self.proxy.clone().map(Into::into)
    }

    pub fn set_proxy(&mut self, value: Option<&ProxyConfig>) {
        self.proxy = value.map(|value| value.clone().into());
    }

    pub fn get_block_url_patterns(&self) -> Vec<String> {
        self.block_url_patterns.clone()
    }

    pub fn get_eval_script(&self) -> Option<String> {
        self.eval_script.clone()
    }

    pub fn get_robots_user_agent(&self) -> Option<String> {
        self.robots_user_agent.clone()
    }

    pub fn get_capture_network_events(&self) -> bool {
        self.capture_network_events.clone()
    }

    pub fn get_session_affinity(&self) -> bool {
        self.session_affinity.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CacheStats")]
#[serde(default)]
pub struct CacheStats {
    /// Total number of files currently in the cache directory.
    #[php(prop, name = "totalFiles")]
    #[serde(alias = "totalFiles")]
    pub total_files: i64,
    /// Combined size of all cache files in megabytes.
    #[php(prop, name = "totalSizeMb")]
    #[serde(alias = "totalSizeMb")]
    pub total_size_mb: f64,
    /// Free disk space available on the cache volume, in megabytes.
    #[php(prop, name = "availableSpaceMb")]
    #[serde(alias = "availableSpaceMb")]
    pub available_space_mb: f64,
    /// Age of the oldest cache file in days (0.0 if the cache is empty).
    #[php(prop, name = "oldestFileAgeDays")]
    #[serde(alias = "oldestFileAgeDays")]
    pub oldest_file_age_days: f64,
    /// Age of the most recently written cache file in days (0.0 if the cache is empty).
    #[php(prop, name = "newestFileAgeDays")]
    #[serde(alias = "newestFileAgeDays")]
    pub newest_file_age_days: f64,
}

#[php_impl]
impl CacheStats {
    #[php(constructor)]
    pub fn new(
        totalFiles: i64,
        totalSizeMb: f64,
        availableSpaceMb: f64,
        oldestFileAgeDays: f64,
        newestFileAgeDays: f64,
    ) -> Self {
        Self {
            total_files: totalFiles,
            total_size_mb: totalSizeMb,
            available_space_mb: availableSpaceMb,
            oldest_file_age_days: oldestFileAgeDays,
            newest_file_age_days: newestFileAgeDays,
        }
    }

    pub fn get_total_files(&self) -> i64 {
        self.total_files.clone()
    }

    pub fn get_total_size_mb(&self) -> f64 {
        self.total_size_mb.clone()
    }

    pub fn get_available_space_mb(&self) -> f64 {
        self.available_space_mb.clone()
    }

    pub fn get_oldest_file_age_days(&self) -> f64 {
        self.oldest_file_age_days.clone()
    }

    pub fn get_newest_file_age_days(&self) -> f64 {
        self.newest_file_age_days.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CaptioningConfig")]
#[serde(default)]
pub struct CaptioningConfig {
    /// LLM configuration used for the VLM call.
    pub llm: LlmConfig,
    /// Optional custom caption prompt. `None` uses the default `RegionKind.Caption`
    /// prompt that ships with `crate.llm.region_extractor`.
    #[php(prop, name = "prompt")]
    pub prompt: Option<String>,
    /// Skip images whose `width * height` is below this threshold (in pixels).
    /// Default `1_000` filters out icons and decorations.
    #[php(prop, name = "minImageArea")]
    #[serde(alias = "minImageArea")]
    pub min_image_area: u32,
}

#[php_impl]
impl CaptioningConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(llm: &LlmConfig, minImageArea: u32, prompt: Option<String>) -> Self {
        Self {
            llm: llm.clone(),
            prompt,
            min_image_area: minImageArea,
        }
    }

    pub fn get_llm(&self) -> LlmConfig {
        self.llm.clone()
    }

    pub fn get_prompt(&self) -> Option<String> {
        self.prompt.clone()
    }

    pub fn get_min_image_area(&self) -> u32 {
        self.min_image_area.clone()
    }

    /// Default `Self::min_image_area`: 1000 px.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultMinImageArea")]
    pub fn default_min_image_area() -> u32 {
        xberg::CaptioningConfig::default_min_image_area()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CellChange")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct CellChange {
    /// Zero-based row index.
    #[php(prop, name = "row")]
    pub row: i64,
    /// Zero-based column index.
    #[php(prop, name = "col")]
    pub col: i64,
    /// Value before the change.
    #[php(prop, name = "from")]
    pub from: String,
    /// Value after the change.
    #[php(prop, name = "to")]
    pub to: String,
}

#[php_impl]
impl CellChange {
    #[php(constructor)]
    pub fn new(row: i64, col: i64, from: String, to: String) -> Self {
        Self { row, col, from, to }
    }

    pub fn get_row(&self) -> i64 {
        self.row.clone()
    }

    pub fn get_col(&self) -> i64 {
        self.col.clone()
    }

    pub fn get_from(&self) -> String {
        self.from.clone()
    }

    pub fn get_to(&self) -> String {
        self.to.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Chunk")]
#[serde(default)]
pub struct Chunk {
    /// The text content of this chunk.
    #[php(prop, name = "content")]
    pub content: String,
    /// Semantic structural classification of this chunk.
    ///
    /// Assigned by the heuristic classifier based on content patterns and
    /// heading context. Defaults to `ChunkType.Unknown` when no rule matches.
    #[php(prop, name = "chunkType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "chunkType")]
    #[serde(default)]
    pub chunk_type: String,
    /// Optional embedding vector for this chunk.
    ///
    /// Only populated when `EmbeddingConfig` is provided in chunking configuration.
    /// The dimensionality depends on the chosen embedding model.
    #[php(prop, name = "embedding")]
    pub embedding: Option<Vec<f32>>,
    /// Optional sparse (SPLADE) learned embedding for this chunk.
    ///
    /// Only populated when sparse-embedding generation is configured for chunking.
    /// `None` otherwise, including on builds without the `sparse-embeddings` feature.
    ///
    /// Uses the crate-root `SparseEmbedding` alias rather than
    /// `crate.sparse_embeddings.SparseEmbedding` directly: the `sparse_embeddings`
    /// module itself only compiles under `sparse-embeddings`/`sparse-embedding-presets`,
    /// while the crate-root alias is always defined (a field-compatible stub on builds
    /// without either feature), so this field — and `Chunk` itself — compiles on every
    /// feature combination, including the crate's default features.
    #[serde(alias = "sparseEmbedding")]
    #[serde(default)]
    pub sparse_embedding: Option<SparseEmbedding>,
    /// Optional ColBERT-style multi-vector (late-interaction) embedding for this chunk.
    ///
    /// Only populated when late-interaction embedding generation is configured for
    /// chunking. `None` otherwise, including on builds without the `late-interaction`
    /// feature.
    ///
    /// Uses the crate-root `MultiVectorEmbedding` alias for the same reason
    /// `sparse_embedding` uses `SparseEmbedding` — see that field's docs.
    #[serde(alias = "lateInteraction")]
    #[serde(default)]
    pub late_interaction: Option<MultiVectorEmbedding>,
    /// Metadata about this chunk's position and properties.
    pub metadata: ChunkMetadata,
}

#[php_impl]
impl Chunk {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        chunkType: String,
        metadata: &ChunkMetadata,
        embedding: Option<Vec<f32>>,
        sparseEmbedding: Option<&SparseEmbedding>,
        lateInteraction: Option<&MultiVectorEmbedding>,
    ) -> Self {
        Self {
            content,
            chunk_type: chunkType,
            embedding,
            sparse_embedding: sparseEmbedding.cloned(),
            late_interaction: lateInteraction.cloned(),
            metadata: metadata.clone(),
        }
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_chunk_type(&self) -> String {
        self.chunk_type.clone()
    }

    pub fn get_embedding(&self) -> Option<Vec<f32>> {
        self.embedding.clone()
    }

    pub fn get_sparse_embedding(&self) -> Option<SparseEmbedding> {
        self.sparse_embedding.clone().map(Into::into)
    }

    pub fn set_sparse_embedding(&mut self, value: Option<&SparseEmbedding>) {
        self.sparse_embedding = value.map(|value| value.clone().into());
    }

    pub fn get_late_interaction(&self) -> Option<MultiVectorEmbedding> {
        self.late_interaction.clone().map(Into::into)
    }

    pub fn set_late_interaction(&mut self, value: Option<&MultiVectorEmbedding>) {
        self.late_interaction = value.map(|value| value.clone().into());
    }

    pub fn get_metadata(&self) -> ChunkMetadata {
        self.metadata.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ChunkClassificationConfig")]
#[serde(default)]
pub struct ChunkClassificationConfig {
    /// Minijinja prompt template. Receives `{{ definitions }}` (rendered label +
    /// description list) and `{{ chunks }}` (a numbered list of chunk texts in
    /// the current batch) variables. `None` lets the backend pick a sensible
    /// default.
    #[php(prop, name = "promptTemplate")]
    #[serde(alias = "promptTemplate")]
    pub prompt_template: Option<String>,
    /// The set of label definitions the classifier may emit. Must contain at
    /// least one entry.
    pub definitions: Vec<ChunkClassificationDefinition>,
    /// LLM configuration used for classification.
    pub llm: LlmConfig,
    /// Number of chunks batched into a single LLM request.
    ///
    /// Larger batches amortize the fixed prompt cost (definitions block) across
    /// more chunks, at the risk of exceeding the model's context window for
    /// very large taxonomies or chunk texts. Defaults to `DEFAULT_BATCH_SIZE`.
    #[php(prop, name = "batchSize")]
    #[serde(alias = "batchSize")]
    pub batch_size: i64,
    /// Maximum number of in-flight batch requests.
    ///
    /// Bounds concurrency against the configured LLM provider. Defaults to
    /// `DEFAULT_MAX_CONCURRENCY`.
    #[php(prop, name = "maxConcurrency")]
    #[serde(alias = "maxConcurrency")]
    pub max_concurrency: i64,
}

#[php_impl]
impl ChunkClassificationConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        definitions: &ext_php_rs::types::ZendHashTable,
        llm: &LlmConfig,
        batchSize: i64,
        maxConcurrency: i64,
        promptTemplate: Option<String>,
    ) -> PhpResult<Self> {
        let mut definitions_core_result: Vec<ChunkClassificationDefinition> = Vec::new();
        for (_, item) in definitions.iter() {
            if let Some(parsed) = <&ChunkClassificationDefinition as ext_php_rs::convert::FromZval>::from_zval(item) {
                definitions_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ChunkClassificationDefinition".to_string(),
                ));
            }
        }
        let definitions_core: Vec<ChunkClassificationDefinition> = definitions_core_result;
        Ok(Self {
            prompt_template: promptTemplate,
            definitions: definitions_core,
            llm: llm.clone(),
            batch_size: batchSize,
            max_concurrency: maxConcurrency,
        })
    }

    pub fn get_prompt_template(&self) -> Option<String> {
        self.prompt_template.clone()
    }

    pub fn get_definitions(&self) -> Vec<ChunkClassificationDefinition> {
        self.definitions.clone()
    }

    pub fn get_llm(&self) -> LlmConfig {
        self.llm.clone()
    }

    pub fn get_batch_size(&self) -> i64 {
        self.batch_size.clone()
    }

    pub fn get_max_concurrency(&self) -> i64 {
        self.max_concurrency.clone()
    }

    /// Default `Self::batch_size`: `DEFAULT_BATCH_SIZE`.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultBatchSize")]
    pub fn default_batch_size() -> i64 {
        xberg::ChunkClassificationConfig::default_batch_size() as i64
    }

    /// Default `Self::max_concurrency`: `DEFAULT_MAX_CONCURRENCY`.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultMaxConcurrency")]
    pub fn default_max_concurrency() -> i64 {
        xberg::ChunkClassificationConfig::default_max_concurrency() as i64
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ChunkClassificationDefinition")]
#[serde(default)]
pub struct ChunkClassificationDefinition {
    /// Label name returned in `ChunkMetadata.classifications`.
    #[php(prop, name = "label")]
    pub label: String,
    /// Semantic description of when this label applies. Injected verbatim into
    /// the classification prompt next to the label name.
    #[php(prop, name = "description")]
    pub description: String,
}

#[php_impl]
impl ChunkClassificationDefinition {
    #[php(constructor)]
    pub fn new(label: String, description: String) -> Self {
        Self { label, description }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_description(&self) -> String {
        self.description.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ChunkClassificationEnrichmentConfig")]
#[serde(default)]
pub struct ChunkClassificationEnrichmentConfig {
    /// Label-definition set and LLM/batching settings for the chunk-classification stage.
    pub config: ChunkClassificationConfig,
}

#[php_impl]
impl ChunkClassificationEnrichmentConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(config: &ChunkClassificationConfig) -> Self {
        Self { config: config.clone() }
    }

    pub fn get_config(&self) -> ChunkClassificationConfig {
        self.config.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ChunkInfo")]
#[serde(default)]
pub struct ChunkInfo {
    /// Zero-based chunk index.
    #[php(prop, name = "index")]
    pub index: u32,
    /// Page range for this chunk.
    pub pages: PageRange,
    /// Estimated processing time for this chunk in milliseconds.
    #[php(prop, name = "estimatedTimeMs")]
    #[serde(alias = "estimatedTimeMs")]
    pub estimated_time_ms: i64,
}

#[php_impl]
impl ChunkInfo {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(index: u32, pages: &PageRange, estimatedTimeMs: i64) -> Self {
        Self {
            index,
            pages: pages.clone(),
            estimated_time_ms: estimatedTimeMs,
        }
    }

    pub fn get_index(&self) -> u32 {
        self.index.clone()
    }

    pub fn get_pages(&self) -> PageRange {
        self.pages.clone()
    }

    pub fn get_estimated_time_ms(&self) -> i64 {
        self.estimated_time_ms.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ChunkMetadata")]
#[serde(default)]
pub struct ChunkMetadata {
    /// Byte offset where this chunk starts in the original text (UTF-8 valid boundary).
    #[php(prop, name = "byteStart")]
    #[serde(alias = "byteStart")]
    pub byte_start: i64,
    /// Byte offset where this chunk ends in the original text (UTF-8 valid boundary).
    #[php(prop, name = "byteEnd")]
    #[serde(alias = "byteEnd")]
    pub byte_end: i64,
    /// Number of tokens in this chunk (if available).
    ///
    /// This is calculated by the embedding model's tokenizer if embeddings are enabled.
    #[php(prop, name = "tokenCount")]
    #[serde(alias = "tokenCount")]
    pub token_count: Option<i64>,
    /// Zero-based index of this chunk in the document.
    #[php(prop, name = "chunkIndex")]
    #[serde(alias = "chunkIndex")]
    pub chunk_index: i64,
    /// Total number of chunks in the document.
    #[php(prop, name = "totalChunks")]
    #[serde(alias = "totalChunks")]
    pub total_chunks: i64,
    /// First page number this chunk spans (1-indexed).
    ///
    /// Only populated when page tracking is enabled in extraction configuration.
    #[php(prop, name = "firstPage")]
    #[serde(alias = "firstPage")]
    pub first_page: Option<u32>,
    /// Last page number this chunk spans (1-indexed, equal to first_page for single-page chunks).
    ///
    /// Only populated when page tracking is enabled in extraction configuration.
    #[php(prop, name = "lastPage")]
    #[serde(alias = "lastPage")]
    pub last_page: Option<u32>,
    /// Heading context when using Markdown chunker.
    ///
    /// Contains the heading hierarchy this chunk falls under.
    /// Only populated when `ChunkerType.Markdown` is used.
    #[serde(alias = "headingContext")]
    #[serde(default)]
    pub heading_context: Option<HeadingContext>,
    /// Flattened heading trail from document root to this chunk's section.
    ///
    /// Each element is a heading's text, outermost first. Derived from
    /// `heading_context` when present; empty otherwise.
    /// Provides a binding-friendly, RAG-shaped breadcrumb without requiring
    /// callers to walk the nested `HeadingContext` structure.
    #[php(prop, name = "headingPath")]
    #[serde(alias = "headingPath")]
    #[serde(default)]
    pub heading_path: Vec<String>,
    /// Indices into `ExtractedDocument.images` for images on pages covered by this chunk.
    ///
    /// Contains zero-based indices into the top-level `images` collection for every
    /// image whose `page_number` falls within `[first_page, last_page]`.
    /// Empty when image extraction is disabled or the chunk spans no pages with images.
    #[php(prop, name = "imageIndices")]
    #[serde(alias = "imageIndices")]
    #[serde(default)]
    pub image_indices: Vec<u32>,
    /// Ids of the `DocumentNode`s
    /// this chunk was derived from.
    ///
    /// Joins a chunk back to the structured document tree via
    /// `DocumentNode.id`.
    /// Populated from exact node provenance when available, with a textual
    /// containment fallback for rendered chunks that do not retain byte offsets.
    #[php(prop, name = "nodeIds")]
    #[serde(alias = "nodeIds")]
    #[serde(default)]
    pub node_ids: Vec<String>,
    /// Per-page bounding-box spans this chunk covers, for viewer highlighting (#1295).
    ///
    /// One entry per page the chunk overlaps, in page order — the first and last entries'
    /// `page` fields equal `first_page`/`last_page`.
    /// Populated whenever page-boundary provenance is available (the same condition under
    /// which `first_page`/`last_page` are populated); each entry's `bbox` is additionally
    /// populated when the document's structured node tree (`ExtractedDocument.document`) is
    /// available, as the union of that page's body-layer node bounding boxes found within this
    /// chunk. Empty when page-boundary provenance is unavailable (mirrors `first_page`/
    /// `last_page` being `None`).
    #[serde(alias = "pageSpans")]
    #[serde(default)]
    pub page_spans: Vec<PageSpan>,
    /// Multi-label classification result for this chunk.
    ///
    /// Populated by the chunk-classification post-processor when
    /// `ExtractionConfig.chunk_classification`
    /// is set. A chunk may match zero, one, or many of the configured label
    /// definitions. Empty when chunk classification was not configured.
    #[serde(default)]
    pub classifications: Vec<ClassificationLabel>,
}

#[php_impl]
impl ChunkMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        byteStart: i64,
        byteEnd: i64,
        chunkIndex: i64,
        totalChunks: i64,
        headingPath: Vec<String>,
        imageIndices: Vec<u32>,
        nodeIds: Vec<String>,
        pageSpans: &ext_php_rs::types::ZendHashTable,
        classifications: &ext_php_rs::types::ZendHashTable,
        tokenCount: Option<i64>,
        firstPage: Option<u32>,
        lastPage: Option<u32>,
        headingContext: Option<&HeadingContext>,
    ) -> PhpResult<Self> {
        let mut pageSpans_core_result: Vec<PageSpan> = Vec::new();
        for (_, item) in pageSpans.iter() {
            if let Some(parsed) = <&PageSpan as ext_php_rs::convert::FromZval>::from_zval(item) {
                pageSpans_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to PageSpan".to_string(),
                ));
            }
        }
        let pageSpans_core: Vec<PageSpan> = pageSpans_core_result;
        let mut classifications_core_result: Vec<ClassificationLabel> = Vec::new();
        for (_, item) in classifications.iter() {
            if let Some(parsed) = <&ClassificationLabel as ext_php_rs::convert::FromZval>::from_zval(item) {
                classifications_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ClassificationLabel".to_string(),
                ));
            }
        }
        let classifications_core: Vec<ClassificationLabel> = classifications_core_result;
        Ok(Self {
            byte_start: byteStart,
            byte_end: byteEnd,
            token_count: tokenCount,
            chunk_index: chunkIndex,
            total_chunks: totalChunks,
            first_page: firstPage,
            last_page: lastPage,
            heading_context: headingContext.cloned(),
            heading_path: headingPath,
            image_indices: imageIndices,
            node_ids: nodeIds,
            page_spans: pageSpans_core,
            classifications: classifications_core,
        })
    }

    pub fn get_byte_start(&self) -> i64 {
        self.byte_start.clone()
    }

    pub fn get_byte_end(&self) -> i64 {
        self.byte_end.clone()
    }

    pub fn get_token_count(&self) -> Option<i64> {
        self.token_count.clone()
    }

    pub fn get_chunk_index(&self) -> i64 {
        self.chunk_index.clone()
    }

    pub fn get_total_chunks(&self) -> i64 {
        self.total_chunks.clone()
    }

    pub fn get_first_page(&self) -> Option<u32> {
        self.first_page.clone()
    }

    pub fn get_last_page(&self) -> Option<u32> {
        self.last_page.clone()
    }

    pub fn get_heading_context(&self) -> Option<HeadingContext> {
        self.heading_context.clone().map(Into::into)
    }

    pub fn set_heading_context(&mut self, value: Option<&HeadingContext>) {
        self.heading_context = value.map(|value| value.clone().into());
    }

    pub fn get_heading_path(&self) -> Vec<String> {
        self.heading_path.clone()
    }

    pub fn get_image_indices(&self) -> Vec<u32> {
        self.image_indices.clone()
    }

    pub fn get_node_ids(&self) -> Vec<String> {
        self.node_ids.clone()
    }

    pub fn get_page_spans(&self) -> Vec<PageSpan> {
        self.page_spans.clone()
    }

    pub fn get_classifications(&self) -> Vec<ClassificationLabel> {
        self.classifications.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ChunkingConfig")]
#[serde(default)]
pub struct ChunkingConfig {
    /// Maximum size per chunk (in units determined by `sizing`).
    ///
    /// When `sizing` is `Characters` (default), this is the max character count.
    /// When using token-based sizing, this is the max token count.
    ///
    /// Default: 1000
    #[php(prop, name = "maxCharacters")]
    #[serde(default = "crate::serde_defaults::chunking_config_max_characters")]
    #[serde(alias = "maxCharacters")]
    pub max_characters: i64,
    /// Overlap between chunks (in units determined by `sizing`).
    ///
    /// Default: 200
    #[php(prop, name = "overlap")]
    #[serde(default = "crate::serde_defaults::chunking_config_overlap")]
    pub overlap: i64,
    /// Whether to trim whitespace from chunk boundaries.
    ///
    /// Default: true
    #[php(prop, name = "trim")]
    #[serde(default = "crate::serde_defaults::chunking_config_trim")]
    pub trim: bool,
    /// Type of chunker to use (Text or Markdown).
    ///
    /// Default: Text
    #[php(prop, name = "chunkerType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "chunkerType")]
    pub chunker_type: String,
    /// Optional embedding configuration for chunk embeddings.
    pub embedding: Option<EmbeddingConfig>,
    /// Optional sparse (SPLADE) embedding configuration for chunk embeddings.
    ///
    /// When set, sparse vectors are generated for each chunk's content and attached
    /// via `sparse_embedding`. Requires the `sparse-embeddings`
    /// feature; without it, a warning is emitted and no sparse vectors are attached.
    ///
    /// Config-file only: like `RerankerConfig` and the local-ONNX branch of
    /// `embedding`, this has no CLI flag and no environment variable. Only the secret/identity
    /// fields of LLM-routed configs (model, API key, base URL) get that reach.
    #[serde(alias = "sparseEmbedding")]
    pub sparse_embedding: Option<SparseEmbeddingConfig>,
    /// Optional late-interaction (ColBERT) embedding configuration for chunk embeddings.
    ///
    /// When set, multi-vector embeddings are generated for each chunk's content and
    /// attached via `late_interaction`. Requires the
    /// `late-interaction` feature; without it, a warning is emitted and no
    /// late-interaction vectors are attached.
    ///
    /// Config-file only, for the same reason as `sparse_embedding` above.
    #[serde(alias = "lateInteraction")]
    pub late_interaction: Option<LateInteractionConfig>,
    /// Use a preset configuration (overrides individual settings if provided).
    #[php(prop, name = "preset")]
    pub preset: Option<String>,
    /// How to measure chunk size.
    ///
    /// Default: `Characters` (Unicode character count).
    /// Enable `chunking-tiktoken` or `chunking-tokenizers` features for token-based sizing.
    #[serde(default)]
    pub sizing: ChunkSizing,
    /// Optional cosine similarity threshold for semantic topic boundary detection.
    ///
    /// Only used when `chunker_type` is `Semantic` and an `EmbeddingConfig` is
    /// provided. You almost never need to set this. When omitted, defaults to
    /// `0.75` which works well for most documents. Lower values detect more
    /// topic boundaries (more, smaller chunks); higher values detect fewer.
    /// Range: `0.0..=1.0`.
    #[php(prop, name = "topicThreshold")]
    #[serde(alias = "topicThreshold")]
    #[serde(default)]
    pub topic_threshold: Option<f32>,
    /// How to handle markdown tables that exceed the chunk size limit.
    ///
    /// Only applies when `chunker_type` is `Markdown`.
    ///
    /// * `Split` (default) — tables are split at row boundaries; continuation
    ///   chunks do not repeat the header.
    /// * `RepeatHeader` — the table header row and separator are prepended to
    ///   every continuation chunk so each chunk is self-contained.
    ///
    /// Default: `Split`
    #[php(prop, name = "tableChunking")]
    #[serde(default = "crate::serde_defaults::chunking_config_table_chunking")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "tableChunking")]
    pub table_chunking: String,
}

impl Default for ChunkingConfig {
    fn default() -> Self {
        <xberg::ChunkingConfig as Default>::default().into()
    }
}

#[php_impl]
impl ChunkingConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        maxCharacters: i64,
        overlap: i64,
        trim: bool,
        chunkerType: String,
        sizing: &ChunkSizing,
        tableChunking: String,
        embedding: Option<&EmbeddingConfig>,
        sparseEmbedding: Option<&SparseEmbeddingConfig>,
        lateInteraction: Option<&LateInteractionConfig>,
        preset: Option<String>,
        topicThreshold: Option<f32>,
    ) -> Self {
        Self {
            max_characters: maxCharacters,
            overlap,
            trim,
            chunker_type: chunkerType,
            embedding: embedding.cloned(),
            sparse_embedding: sparseEmbedding.cloned(),
            late_interaction: lateInteraction.cloned(),
            preset,
            sizing: sizing.clone(),
            topic_threshold: topicThreshold,
            table_chunking: tableChunking,
        }
    }

    pub fn get_max_characters(&self) -> i64 {
        self.max_characters.clone()
    }

    pub fn get_overlap(&self) -> i64 {
        self.overlap.clone()
    }

    pub fn get_trim(&self) -> bool {
        self.trim.clone()
    }

    pub fn get_chunker_type(&self) -> String {
        self.chunker_type.clone()
    }

    pub fn get_embedding(&self) -> Option<EmbeddingConfig> {
        self.embedding.clone().map(Into::into)
    }

    pub fn set_embedding(&mut self, value: Option<&EmbeddingConfig>) {
        self.embedding = value.map(|value| value.clone().into());
    }

    pub fn get_sparse_embedding(&self) -> Option<SparseEmbeddingConfig> {
        self.sparse_embedding.clone().map(Into::into)
    }

    pub fn set_sparse_embedding(&mut self, value: Option<&SparseEmbeddingConfig>) {
        self.sparse_embedding = value.map(|value| value.clone().into());
    }

    pub fn get_late_interaction(&self) -> Option<LateInteractionConfig> {
        self.late_interaction.clone().map(Into::into)
    }

    pub fn set_late_interaction(&mut self, value: Option<&LateInteractionConfig>) {
        self.late_interaction = value.map(|value| value.clone().into());
    }

    pub fn get_preset(&self) -> Option<String> {
        self.preset.clone()
    }

    pub fn get_sizing(&self) -> ChunkSizing {
        self.sizing.clone()
    }

    pub fn get_topic_threshold(&self) -> Option<f32> {
        self.topic_threshold.clone()
    }

    pub fn get_table_chunking(&self) -> String {
        self.table_chunking.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ChunkingConfig {
        xberg::ChunkingConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Citation")]
#[serde(default)]
pub struct Citation {
    /// The label of the citation (e.g., "src1" in `[^src1]: ...`).
    #[php(prop, name = "label")]
    pub label: String,
    /// The source reference (path, URL, or identifier).
    #[php(prop, name = "source")]
    pub source: String,
    /// Optional locator within the source (e.g., "page 3" or "section 2.1").
    #[php(prop, name = "locator")]
    pub locator: Option<String>,
    /// Optional excerpt — quoted text from the source.
    #[php(prop, name = "excerpt")]
    pub excerpt: Option<String>,
}

#[php_impl]
impl Citation {
    #[php(constructor)]
    pub fn new(label: String, source: String, locator: Option<String>, excerpt: Option<String>) -> Self {
        Self {
            label,
            source,
            locator,
            excerpt,
        }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_source(&self) -> String {
        self.source.clone()
    }

    pub fn get_locator(&self) -> Option<String> {
        self.locator.clone()
    }

    pub fn get_excerpt(&self) -> Option<String> {
        self.excerpt.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CitationMetadata")]
#[serde(default)]
pub struct CitationMetadata {
    /// Total number of citation records in the file.
    #[php(prop, name = "citationCount")]
    #[serde(alias = "citationCount")]
    pub citation_count: i64,
    /// Detected citation file format (e.g. `"ris"`, `"pubmed"`, `"endnote"`).
    #[php(prop, name = "format")]
    pub format: Option<String>,
    /// Author names collected across all citation records.
    #[php(prop, name = "authors")]
    #[serde(default)]
    pub authors: Vec<String>,
    /// Earliest and latest publication years found in the file.
    #[serde(alias = "yearRange")]
    pub year_range: Option<YearRange>,
    /// DOI identifiers found in the citation records.
    #[php(prop, name = "dois")]
    #[serde(default)]
    pub dois: Vec<String>,
    /// Keywords collected from all citation records.
    #[php(prop, name = "keywords")]
    #[serde(default)]
    pub keywords: Vec<String>,
}

impl Default for CitationMetadata {
    fn default() -> Self {
        <xberg::CitationMetadata as Default>::default().into()
    }
}

#[php_impl]
impl CitationMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        citationCount: i64,
        authors: Vec<String>,
        dois: Vec<String>,
        keywords: Vec<String>,
        format: Option<String>,
        yearRange: Option<&YearRange>,
    ) -> Self {
        Self {
            citation_count: citationCount,
            format,
            authors,
            year_range: yearRange.cloned(),
            dois,
            keywords,
        }
    }

    pub fn get_citation_count(&self) -> i64 {
        self.citation_count.clone()
    }

    pub fn get_format(&self) -> Option<String> {
        self.format.clone()
    }

    pub fn get_authors(&self) -> Vec<String> {
        self.authors.clone()
    }

    pub fn get_year_range(&self) -> Option<YearRange> {
        self.year_range.clone().map(Into::into)
    }

    pub fn set_year_range(&mut self, value: Option<&YearRange>) {
        self.year_range = value.map(|value| value.clone().into());
    }

    pub fn get_dois(&self) -> Vec<String> {
        self.dois.clone()
    }

    pub fn get_keywords(&self) -> Vec<String> {
        self.keywords.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ClassificationLabel")]
#[serde(default)]
pub struct ClassificationLabel {
    /// Label name as configured in `PageClassificationConfig.labels`.
    #[php(prop, name = "label")]
    pub label: String,
    /// Backend-reported confidence in `[0.0, 1.0]`. `None` when the backend (e.g. an LLM
    /// prompt without explicit confidence schema) did not report one.
    #[php(prop, name = "confidence")]
    pub confidence: Option<f32>,
}

#[php_impl]
impl ClassificationLabel {
    #[php(constructor)]
    pub fn new(label: String, confidence: Option<f32>) -> Self {
        Self { label, confidence }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_confidence(&self) -> Option<f32> {
        self.confidence.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CodeChunkInfo")]
#[serde(default)]
pub struct CodeChunkInfo {
    /// The raw source text of this chunk.
    #[php(prop, name = "text")]
    pub text: String,
    /// Hierarchical path of enclosing structural items (e.g. `["MyClass", "my_method"]`).
    #[php(prop, name = "contextPath")]
    #[serde(alias = "contextPath")]
    pub context_path: Vec<String>,
    /// Tree-sitter node kinds that appear at the top level of this chunk (e.g.
    /// `"function_definition"`, `"class_definition"`).
    #[php(prop, name = "nodeTypes")]
    #[serde(alias = "nodeTypes")]
    pub node_types: Vec<String>,
    /// Inclusive start byte offset of this chunk in the original source.
    #[php(prop, name = "byteStart")]
    #[serde(alias = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of this chunk in the original source.
    #[php(prop, name = "byteEnd")]
    #[serde(alias = "byteEnd")]
    pub byte_end: i64,
}

#[php_impl]
impl CodeChunkInfo {
    #[php(constructor)]
    pub fn new(text: String, contextPath: Vec<String>, nodeTypes: Vec<String>, byteStart: i64, byteEnd: i64) -> Self {
        Self {
            text,
            context_path: contextPath,
            node_types: nodeTypes,
            byte_start: byteStart,
            byte_end: byteEnd,
        }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_context_path(&self) -> Vec<String> {
        self.context_path.clone()
    }

    pub fn get_node_types(&self) -> Vec<String> {
        self.node_types.clone()
    }

    pub fn get_byte_start(&self) -> i64 {
        self.byte_start.clone()
    }

    pub fn get_byte_end(&self) -> i64 {
        self.byte_end.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CodeDataAttribute")]
#[serde(default)]
pub struct CodeDataAttribute {
    /// Attribute name (e.g. `"class"`, `"href"`).
    #[php(prop, name = "name")]
    pub name: String,
    /// Attribute value as a raw string (quotes stripped).
    #[php(prop, name = "value")]
    pub value: String,
    /// Inclusive start byte offset of the `name="value"` attribute token.
    #[php(prop, name = "byteStart")]
    #[serde(alias = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of the `name="value"` attribute token.
    #[php(prop, name = "byteEnd")]
    #[serde(alias = "byteEnd")]
    pub byte_end: i64,
}

#[php_impl]
impl CodeDataAttribute {
    #[php(constructor)]
    pub fn new(name: String, value: String, byteStart: i64, byteEnd: i64) -> Self {
        Self {
            name,
            value,
            byte_start: byteStart,
            byte_end: byteEnd,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_value(&self) -> String {
        self.value.clone()
    }

    pub fn get_byte_start(&self) -> i64 {
        self.byte_start.clone()
    }

    pub fn get_byte_end(&self) -> i64 {
        self.byte_end.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\CodeDataNode")]
#[serde(default)]
pub struct CodeDataNode {
    /// Whether this node is a key/value pair, XML element, or sequence item.
    #[php(prop, name = "kind")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub kind: String,
    /// Key, attribute name, tag name, or positional index (`"0"`, `"1"`, …).
    /// `None` at the document root.
    #[php(prop, name = "key")]
    #[serde(default)]
    pub key: Option<String>,
    /// Leaf scalar value, if any. `None` for containers (objects, arrays, XML
    /// elements with child elements).
    #[php(prop, name = "value")]
    #[serde(default)]
    pub value: Option<String>,
    /// Attributes on element-shape nodes (XML `STag` attributes). Empty for all
    /// other kinds.
    #[serde(default)]
    pub attributes: Vec<CodeDataAttribute>,
    /// Children for nested containers and XML element bodies.
    #[serde(default)]
    pub children: Vec<CodeDataNode>,
    /// Inclusive start byte offset of this node in the original source.
    #[php(prop, name = "byteStart")]
    #[serde(alias = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of this node in the original source.
    #[php(prop, name = "byteEnd")]
    #[serde(alias = "byteEnd")]
    pub byte_end: i64,
}

#[php_impl]
impl CodeDataNode {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        kind: String,
        attributes: &ext_php_rs::types::ZendHashTable,
        children: &ext_php_rs::types::ZendHashTable,
        byteStart: i64,
        byteEnd: i64,
        key: Option<String>,
        value: Option<String>,
    ) -> PhpResult<Self> {
        let mut attributes_core_result: Vec<CodeDataAttribute> = Vec::new();
        for (_, item) in attributes.iter() {
            if let Some(parsed) = <&CodeDataAttribute as ext_php_rs::convert::FromZval>::from_zval(item) {
                attributes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to CodeDataAttribute".to_string(),
                ));
            }
        }
        let attributes_core: Vec<CodeDataAttribute> = attributes_core_result;
        let mut children_core_result: Vec<CodeDataNode> = Vec::new();
        for (_, item) in children.iter() {
            if let Some(parsed) = <&CodeDataNode as ext_php_rs::convert::FromZval>::from_zval(item) {
                children_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to CodeDataNode".to_string(),
                ));
            }
        }
        let children_core: Vec<CodeDataNode> = children_core_result;
        Ok(Self {
            kind,
            key,
            value,
            attributes: attributes_core,
            children: children_core,
            byte_start: byteStart,
            byte_end: byteEnd,
        })
    }

    pub fn get_kind(&self) -> String {
        self.kind.clone()
    }

    pub fn get_key(&self) -> Option<String> {
        self.key.clone()
    }

    pub fn get_value(&self) -> Option<String> {
        self.value.clone()
    }

    pub fn get_attributes(&self) -> Vec<CodeDataAttribute> {
        self.attributes.clone()
    }

    pub fn get_children(&self) -> Vec<CodeDataNode> {
        self.children.clone()
    }

    pub fn get_byte_start(&self) -> i64 {
        self.byte_start.clone()
    }

    pub fn get_byte_end(&self) -> i64 {
        self.byte_end.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CodeMetadata")]
#[serde(default)]
pub struct CodeMetadata {
    /// Structural code chunks (function/class/module boundaries).
    pub chunks: Vec<CodeChunkInfo>,
    /// Hierarchical key/value data tree extracted from data-format source
    /// (JSON, YAML, TOML, XML, CSV, etc.), when data extraction was enabled.
    #[serde(default)]
    pub data: Option<CodeDataNode>,
}

impl Default for CodeMetadata {
    fn default() -> Self {
        <xberg::CodeMetadata as Default>::default().into()
    }
}

#[php_impl]
impl CodeMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(chunks: &ext_php_rs::types::ZendHashTable, data: Option<&CodeDataNode>) -> PhpResult<Self> {
        let mut chunks_core_result: Vec<CodeChunkInfo> = Vec::new();
        for (_, item) in chunks.iter() {
            if let Some(parsed) = <&CodeChunkInfo as ext_php_rs::convert::FromZval>::from_zval(item) {
                chunks_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to CodeChunkInfo".to_string(),
                ));
            }
        }
        let chunks_core: Vec<CodeChunkInfo> = chunks_core_result;
        Ok(Self {
            chunks: chunks_core,
            data: data.cloned(),
        })
    }

    pub fn get_chunks(&self) -> Vec<CodeChunkInfo> {
        self.chunks.clone()
    }

    pub fn get_data(&self) -> Option<CodeDataNode> {
        self.data.clone().map(Into::into)
    }

    pub fn set_data(&mut self, value: Option<&CodeDataNode>) {
        self.data = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ConcurrencyConfig")]
#[serde(default)]
pub struct ConcurrencyConfig {
    /// Maximum number of threads for all internal thread pools.
    ///
    /// Caps Rayon global pool size, ONNX Runtime intra-op threads, and the
    /// combined document/inner-task budget for batch extraction. When `None`,
    /// the effective budget is `min(detected_cpu_cores, 8)` unless a Linux
    /// cgroup CPU quota is present, in which case the quota is used as the
    /// ceiling instead. On hosts with more than 8 cores and no cgroup quota,
    /// set `max_threads` explicitly to use the additional cores — the
    /// default will not scale past 8 on its own.
    #[php(prop, name = "maxThreads")]
    #[serde(alias = "maxThreads")]
    pub max_threads: Option<i64>,
}

impl Default for ConcurrencyConfig {
    fn default() -> Self {
        <xberg::ConcurrencyConfig as Default>::default().into()
    }
}

#[php_impl]
impl ConcurrencyConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_max_threads(&self) -> Option<i64> {
        self.max_threads.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ContentConfig")]
#[serde(default)]
pub struct ContentConfig {
    /// Output format: `"markdown"` (default), `"plain"`, `"djot"`.
    #[php(prop, name = "outputFormat")]
    #[serde(default = "crate::serde_defaults::content_config_output_format")]
    #[serde(alias = "outputFormat")]
    pub output_format: String,
    /// Preprocessing aggressiveness: `"minimal"`, `"standard"` (default), `"aggressive"`.
    ///
    /// - Minimal: only scripts/styles removed.
    /// - Standard: also removes nav, nav-hinted headers/footers/asides, forms.
    /// - Aggressive: removes all footers/asides unconditionally.
    #[php(prop, name = "preprocessingPreset")]
    #[serde(default = "crate::serde_defaults::content_config_preprocessing_preset")]
    #[serde(alias = "preprocessingPreset")]
    pub preprocessing_preset: String,
    /// Remove navigation elements (nav, breadcrumbs, menus). Default: `true`.
    #[php(prop, name = "removeNavigation")]
    #[serde(default = "crate::serde_defaults::content_config_remove_navigation")]
    #[serde(alias = "removeNavigation")]
    pub remove_navigation: bool,
    /// Remove form elements. Default: `true`.
    #[php(prop, name = "removeForms")]
    #[serde(default = "crate::serde_defaults::content_config_remove_forms")]
    #[serde(alias = "removeForms")]
    pub remove_forms: bool,
    /// HTML tag names to strip (render children only, remove the tag wrapper).
    /// Default: `[]`.
    #[php(prop, name = "stripTags")]
    #[serde(alias = "stripTags")]
    #[serde(default)]
    pub strip_tags: Vec<String>,
    /// HTML tag names to preserve as raw HTML in output.
    #[php(prop, name = "preserveTags")]
    #[serde(alias = "preserveTags")]
    #[serde(default)]
    pub preserve_tags: Vec<String>,
    /// CSS selectors for elements to exclude entirely (element + all content).
    ///
    /// Unlike `strip_tags` (which removes the wrapper but keeps children),
    /// excluded elements and all descendants are dropped. Supports CSS selectors:
    /// `.class`, `#id`, `[attribute]`, compound selectors.
    ///
    /// Default: `["noscript"]`. `<noscript>` fallback content (no-JS notices,
    /// tracking pixels, GTM iframes) is meant for browsers with JavaScript
    /// disabled, not for a markdown reader, and `strip_tags` cannot drop it —
    /// on `preprocessing_preset: "standard"` (crawlberg's only path) it only
    /// removes the wrapper and still renders the children.
    ///
    /// Example: `[".cookie-banner", "#ad-container", "[role='complementary']"]`
    #[php(prop, name = "excludeSelectors")]
    #[serde(alias = "excludeSelectors")]
    pub exclude_selectors: Vec<String>,
    /// Skip image elements in output. Default: `false`.
    #[php(prop, name = "skipImages")]
    #[serde(default = "crate::serde_defaults::content_config_skip_images")]
    #[serde(alias = "skipImages")]
    pub skip_images: bool,
    /// Max DOM traversal depth. Prevents stack overflow on deeply nested HTML.
    #[php(prop, name = "maxDepth")]
    #[serde(alias = "maxDepth")]
    pub max_depth: Option<i64>,
    /// Enable line wrapping. Default: `false`.
    #[php(prop, name = "wrap")]
    #[serde(default = "crate::serde_defaults::content_config_wrap")]
    pub wrap: bool,
    /// Wrap width when `wrap` is enabled. Default: `80`.
    #[php(prop, name = "wrapWidth")]
    #[serde(default = "crate::serde_defaults::content_config_wrap_width")]
    #[serde(alias = "wrapWidth")]
    pub wrap_width: i64,
    /// Include document structure tree in output. Default: `true`.
    #[php(prop, name = "includeDocumentStructure")]
    #[serde(default = "crate::serde_defaults::content_config_include_document_structure")]
    #[serde(alias = "includeDocumentStructure")]
    pub include_document_structure: bool,
}

impl Default for ContentConfig {
    fn default() -> Self {
        <crawlberg::ContentConfig as Default>::default().into()
    }
}

#[php_impl]
impl ContentConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        outputFormat: String,
        preprocessingPreset: String,
        removeNavigation: bool,
        removeForms: bool,
        stripTags: Vec<String>,
        preserveTags: Vec<String>,
        excludeSelectors: Vec<String>,
        skipImages: bool,
        wrap: bool,
        wrapWidth: i64,
        includeDocumentStructure: bool,
        maxDepth: Option<i64>,
    ) -> Self {
        Self {
            output_format: outputFormat,
            preprocessing_preset: preprocessingPreset,
            remove_navigation: removeNavigation,
            remove_forms: removeForms,
            strip_tags: stripTags,
            preserve_tags: preserveTags,
            exclude_selectors: excludeSelectors,
            skip_images: skipImages,
            max_depth: maxDepth,
            wrap,
            wrap_width: wrapWidth,
            include_document_structure: includeDocumentStructure,
        }
    }

    pub fn get_output_format(&self) -> String {
        self.output_format.clone()
    }

    pub fn get_preprocessing_preset(&self) -> String {
        self.preprocessing_preset.clone()
    }

    pub fn get_remove_navigation(&self) -> bool {
        self.remove_navigation.clone()
    }

    pub fn get_remove_forms(&self) -> bool {
        self.remove_forms.clone()
    }

    pub fn get_strip_tags(&self) -> Vec<String> {
        self.strip_tags.clone()
    }

    pub fn get_preserve_tags(&self) -> Vec<String> {
        self.preserve_tags.clone()
    }

    pub fn get_exclude_selectors(&self) -> Vec<String> {
        self.exclude_selectors.clone()
    }

    pub fn get_skip_images(&self) -> bool {
        self.skip_images.clone()
    }

    pub fn get_max_depth(&self) -> Option<i64> {
        self.max_depth.clone()
    }

    pub fn get_wrap(&self) -> bool {
        self.wrap.clone()
    }

    pub fn get_wrap_width(&self) -> i64 {
        self.wrap_width.clone()
    }

    pub fn get_include_document_structure(&self) -> bool {
        self.include_document_structure.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ContentFilterConfig")]
#[serde(default)]
pub struct ContentFilterConfig {
    /// Include running headers in extraction output.
    ///
    /// - PDF: Disables top-margin furniture stripping and prevents the layout
    ///   model from treating `PageHeader`-classified regions as furniture.
    /// - DOCX: Includes document headers in text output.
    /// - RTF/ODT: Headers already included; this is a no-op when true.
    /// - HTML/EPUB: Keeps `<header>` element content.
    ///
    /// Default: `false` (headers are stripped or excluded).
    #[php(prop, name = "includeHeaders")]
    #[serde(default = "crate::serde_defaults::content_filter_config_include_headers")]
    #[serde(alias = "includeHeaders")]
    pub include_headers: bool,
    /// Include running footers in extraction output.
    ///
    /// - PDF: Disables bottom-margin furniture stripping and prevents the layout
    ///   model from treating `PageFooter`-classified regions as furniture.
    /// - DOCX: Includes document footers in text output.
    /// - RTF/ODT: Footers already included; this is a no-op when true.
    /// - HTML/EPUB: Keeps `<footer>` element content.
    ///
    /// Default: `false` (footers are stripped or excluded).
    #[php(prop, name = "includeFooters")]
    #[serde(default = "crate::serde_defaults::content_filter_config_include_footers")]
    #[serde(alias = "includeFooters")]
    pub include_footers: bool,
    /// Include footnote bodies in extraction output.
    ///
    /// - PDF: Prevents the layout model from treating `Footnote`-classified
    ///   regions as furniture, so footnote bodies survive alongside the main
    ///   text instead of being silently dropped.
    /// - Other formats: No effect currently.
    ///
    /// Default: `false` (footnotes are stripped), matching the existing
    /// `include_headers` / `include_footers` defaults.
    #[php(prop, name = "includeFootnotes")]
    #[serde(default = "crate::serde_defaults::content_filter_config_include_footnotes")]
    #[serde(alias = "includeFootnotes")]
    pub include_footnotes: bool,
    /// Enable the heuristic cross-page repeating text detector.
    ///
    /// When `true` (default), text that repeats verbatim across a supermajority
    /// of pages is classified as furniture and stripped.  Disable this if brand
    /// names or repeated headings are being incorrectly removed by the heuristic.
    ///
    /// Note: when a layout-detection model is active, the model may independently
    /// classify page-header / page-footer / footnote regions as furniture on a
    /// per-page basis. To preserve those regions, set `include_headers = true`,
    /// `include_footers = true`, `include_footnotes = true`, or any combination,
    /// in addition to disabling this flag.
    ///
    /// Primarily affects PDF extraction.
    ///
    /// Default: `true`.
    #[php(prop, name = "stripRepeatingText")]
    #[serde(default = "crate::serde_defaults::content_filter_config_strip_repeating_text")]
    #[serde(alias = "stripRepeatingText")]
    pub strip_repeating_text: bool,
    /// Include watermark text in extraction output.
    ///
    /// - PDF: Keeps watermark artifacts and arXiv identifiers.
    /// - Other formats: No effect currently.
    ///
    /// Default: `false` (watermarks are stripped).
    #[php(prop, name = "includeWatermarks")]
    #[serde(default = "crate::serde_defaults::content_filter_config_include_watermarks")]
    #[serde(alias = "includeWatermarks")]
    pub include_watermarks: bool,
}

impl Default for ContentFilterConfig {
    fn default() -> Self {
        <xberg::ContentFilterConfig as Default>::default().into()
    }
}

#[php_impl]
impl ContentFilterConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        includeHeaders: bool,
        includeFooters: bool,
        includeFootnotes: bool,
        stripRepeatingText: bool,
        includeWatermarks: bool,
    ) -> Self {
        Self {
            include_headers: includeHeaders,
            include_footers: includeFooters,
            include_footnotes: includeFootnotes,
            strip_repeating_text: stripRepeatingText,
            include_watermarks: includeWatermarks,
        }
    }

    pub fn get_include_headers(&self) -> bool {
        self.include_headers.clone()
    }

    pub fn get_include_footers(&self) -> bool {
        self.include_footers.clone()
    }

    pub fn get_include_footnotes(&self) -> bool {
        self.include_footnotes.clone()
    }

    pub fn get_strip_repeating_text(&self) -> bool {
        self.strip_repeating_text.clone()
    }

    pub fn get_include_watermarks(&self) -> bool {
        self.include_watermarks.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ContentFilterConfig {
        xberg::ContentFilterConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ContributorRole")]
#[serde(default)]
pub struct ContributorRole {
    /// Contributor display name.
    #[php(prop, name = "name")]
    pub name: String,
    /// Contributor role (e.g. `"author"`, `"editor"`).
    #[php(prop, name = "role")]
    pub role: Option<String>,
}

#[php_impl]
impl ContributorRole {
    #[php(constructor)]
    pub fn new(name: String, role: Option<String>) -> Self {
        Self { name, role }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_role(&self) -> Option<String> {
        self.role.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ConversionOptions")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ConversionOptions {
    /// Heading style to use in Markdown output (ATX `#` or Setext underline).
    #[php(prop, name = "headingStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "headingStyle")]
    pub heading_style: String,
    /// How to indent nested list items (spaces or tab).
    #[php(prop, name = "listIndentType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "listIndentType")]
    pub list_indent_type: String,
    /// Number of spaces (or tabs) to use for each level of list indentation.
    #[php(prop, name = "listIndentWidth")]
    #[serde(default = "crate::serde_defaults::conversion_options_list_indent_width")]
    #[serde(alias = "listIndentWidth")]
    pub list_indent_width: i64,
    /// Bullet character(s) to use for unordered list items (e.g. `"-"`, `"*"`).
    #[php(prop, name = "bullets")]
    #[serde(default = "crate::serde_defaults::conversion_options_bullets")]
    pub bullets: String,
    /// Character used for bold/italic emphasis markers (`*` or `_`).
    #[php(prop, name = "strongEmSymbol")]
    #[serde(alias = "strongEmSymbol")]
    pub strong_em_symbol: String,
    /// Escape `*` characters in plain text to avoid unintended bold/italic.
    #[php(prop, name = "escapeAsterisks")]
    #[serde(default = "crate::serde_defaults::conversion_options_escape_asterisks")]
    #[serde(alias = "escapeAsterisks")]
    pub escape_asterisks: bool,
    /// Escape `_` characters in plain text to avoid unintended bold/italic.
    #[php(prop, name = "escapeUnderscores")]
    #[serde(default = "crate::serde_defaults::conversion_options_escape_underscores")]
    #[serde(alias = "escapeUnderscores")]
    pub escape_underscores: bool,
    /// Escape miscellaneous Markdown metacharacters (`[]()#` etc.) in plain text.
    #[php(prop, name = "escapeMisc")]
    #[serde(default = "crate::serde_defaults::conversion_options_escape_misc")]
    #[serde(alias = "escapeMisc")]
    pub escape_misc: bool,
    /// Escape ASCII characters that have special meaning in certain Markdown dialects.
    #[php(prop, name = "escapeAscii")]
    #[serde(default = "crate::serde_defaults::conversion_options_escape_ascii")]
    #[serde(alias = "escapeAscii")]
    pub escape_ascii: bool,
    /// Default language annotation for fenced code blocks that have no language hint.
    #[php(prop, name = "codeLanguage")]
    #[serde(default = "crate::serde_defaults::conversion_options_code_language")]
    #[serde(alias = "codeLanguage")]
    pub code_language: String,
    /// Automatically convert bare URLs into Markdown autolinks.
    #[php(prop, name = "autolinks")]
    #[serde(default = "crate::serde_defaults::conversion_options_autolinks")]
    pub autolinks: bool,
    /// Emit a default title when no `<title>` tag is present.
    #[php(prop, name = "defaultTitle")]
    #[serde(default = "crate::serde_defaults::conversion_options_default_title")]
    #[serde(alias = "defaultTitle")]
    pub default_title: bool,
    /// Render `<br>` elements inside table cells as literal line breaks.
    #[php(prop, name = "brInTables")]
    #[serde(default = "crate::serde_defaults::conversion_options_br_in_tables")]
    #[serde(alias = "brInTables")]
    pub br_in_tables: bool,
    /// Emit tables without column padding (compact GFM format).
    ///
    /// When `true`, column widths are not computed and cells are emitted with
    /// no trailing spaces. Separator rows use exactly `---` per column.
    /// Produces token-efficient output suitable for RAG / LLM contexts.
    ///
    /// Default `false` (aligned padding preserved).
    #[php(prop, name = "compactTables")]
    #[serde(default = "crate::serde_defaults::conversion_options_compact_tables")]
    #[serde(alias = "compactTables")]
    pub compact_tables: bool,
    /// Style used for `<mark>` / highlighted text (e.g. `==text==`).
    #[php(prop, name = "highlightStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "highlightStyle")]
    pub highlight_style: String,
    /// Populate `result.metadata` with `<head>` / `<meta>` extraction
    /// (title, description, Open Graph, Twitter Card, JSON-LD, …).
    ///
    /// Default `true`. Disabling skips the metadata pass only — table
    /// extraction into `result.tables` runs unconditionally.
    #[php(prop, name = "extractMetadata")]
    #[serde(default = "crate::serde_defaults::conversion_options_extract_metadata")]
    #[serde(alias = "extractMetadata")]
    pub extract_metadata: bool,
    /// Controls how whitespace sequences are normalised in the converted output.
    ///
    /// - `WhitespaceMode.Normalized` (default) — collapses consecutive whitespace characters
    ///   (spaces, tabs, newlines) to a single space, matching browser rendering behaviour.
    /// - `WhitespaceMode.Strict` — preserves all whitespace exactly as it appears in the
    ///   source HTML, including runs of spaces and embedded newlines.
    ///
    /// Choose `Strict` only when the source HTML uses deliberate whitespace (e.g. pre-formatted
    /// content outside `<pre>` tags). For most documents `Normalized` produces cleaner output.
    #[php(prop, name = "whitespaceMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "whitespaceMode")]
    pub whitespace_mode: String,
    /// Strip all newlines from the output, producing a single-line result.
    #[php(prop, name = "stripNewlines")]
    #[serde(default = "crate::serde_defaults::conversion_options_strip_newlines")]
    #[serde(alias = "stripNewlines")]
    pub strip_newlines: bool,
    /// Wrap long lines at `wrap_width` characters.
    #[php(prop, name = "wrap")]
    #[serde(default = "crate::serde_defaults::conversion_options_wrap")]
    pub wrap: bool,
    /// Maximum output line width in characters when `wrap` is `true` (default `80`).
    ///
    /// Lines are broken at word boundaries so that no line exceeds this length. A value of `0`
    /// is treated as "no limit" — equivalent to leaving `wrap` disabled. Has no
    /// effect when `wrap` is `false`.
    #[php(prop, name = "wrapWidth")]
    #[serde(default = "crate::serde_defaults::conversion_options_wrap_width")]
    #[serde(alias = "wrapWidth")]
    pub wrap_width: i64,
    /// Treat the entire document as inline content (no block-level wrappers).
    #[php(prop, name = "convertAsInline")]
    #[serde(default = "crate::serde_defaults::conversion_options_convert_as_inline")]
    #[serde(alias = "convertAsInline")]
    pub convert_as_inline: bool,
    /// Markdown notation for subscript text (e.g. `"~"`).
    #[php(prop, name = "subSymbol")]
    #[serde(default = "crate::serde_defaults::conversion_options_sub_symbol")]
    #[serde(alias = "subSymbol")]
    pub sub_symbol: String,
    /// Markdown notation for superscript text (e.g. `"^"`).
    #[php(prop, name = "supSymbol")]
    #[serde(default = "crate::serde_defaults::conversion_options_sup_symbol")]
    #[serde(alias = "supSymbol")]
    pub sup_symbol: String,
    /// How to encode hard line breaks (`<br>`) in Markdown.
    #[php(prop, name = "newlineStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "newlineStyle")]
    pub newline_style: String,
    /// Style used for fenced code blocks (backticks or tilde).
    #[php(prop, name = "codeBlockStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "codeBlockStyle")]
    pub code_block_style: String,
    /// HTML tag names whose `<img>` children are kept inline instead of block.
    #[php(prop, name = "keepInlineImagesIn")]
    #[serde(alias = "keepInlineImagesIn")]
    pub keep_inline_images_in: Vec<String>,
    /// Options for the HTML pre-processing pass applied before conversion begins.
    ///
    /// Pre-processing runs before the HTML is handed to the converter and can perform operations
    /// such as unwrapping redundant wrapper elements, removing tracking pixels, and normalising
    /// vendor-specific markup. See `PreprocessingOptions` for the full set of knobs.
    ///
    /// Defaults to `PreprocessingOptions.default()`, which enables the standard cleaning
    /// passes. Set individual fields on `PreprocessingOptions` (or construct via
    /// `ConversionOptions.builder`) to opt in or out of specific passes.
    pub preprocessing: PreprocessingOptions,
    /// Expected character encoding of the input HTML (default `"utf-8"`).
    #[php(prop, name = "encoding")]
    #[serde(default = "crate::serde_defaults::conversion_options_encoding")]
    pub encoding: String,
    /// Emit debug information during conversion.
    #[php(prop, name = "debug")]
    #[serde(default = "crate::serde_defaults::conversion_options_debug")]
    pub debug: bool,
    /// HTML tag names whose content is stripped from the output entirely.
    #[php(prop, name = "stripTags")]
    #[serde(alias = "stripTags")]
    pub strip_tags: Vec<String>,
    /// HTML tag names that are preserved verbatim in the output.
    #[php(prop, name = "preserveTags")]
    #[serde(alias = "preserveTags")]
    pub preserve_tags: Vec<String>,
    /// Skip conversion of `<img>` elements (omit images from output).
    #[php(prop, name = "skipImages")]
    #[serde(default = "crate::serde_defaults::conversion_options_skip_images")]
    #[serde(alias = "skipImages")]
    pub skip_images: bool,
    /// URL encoding strategy for link and image destinations.
    ///
    /// Controls how special characters in URL destinations are escaped:
    /// - `UrlEscapeStyle.Angle` (default) — wraps the destination in angle brackets when it
    ///   contains spaces or newlines. parsers misinterpret `>` inside such a destination.
    /// - `UrlEscapeStyle.Percent` — percent-encodes every character that is not an RFC 3986
    ///   unreserved character or `/`, producing a destination that all Markdown parsers handle
    ///   correctly even when the URL contains `<`, `>`, spaces, or parentheses.
    #[php(prop, name = "urlEscapeStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "urlEscapeStyle")]
    pub url_escape_style: String,
    /// Link rendering style (inline or reference).
    #[php(prop, name = "linkStyle")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "linkStyle")]
    pub link_style: String,
    /// Target output format (Markdown, plain text, etc.).
    #[php(prop, name = "outputFormat")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "outputFormat")]
    pub output_format: String,
    /// Include structured document tree in result.
    #[php(prop, name = "includeDocumentStructure")]
    #[serde(default = "crate::serde_defaults::conversion_options_include_document_structure")]
    #[serde(alias = "includeDocumentStructure")]
    pub include_document_structure: bool,
    /// Extract inline images from data URIs and SVGs.
    #[php(prop, name = "extractImages")]
    #[serde(default = "crate::serde_defaults::conversion_options_extract_images")]
    #[serde(alias = "extractImages")]
    pub extract_images: bool,
    /// Maximum decoded image size in bytes (default 5MB).
    #[php(prop, name = "maxImageSize")]
    #[serde(default = "crate::serde_defaults::conversion_options_max_image_size")]
    #[serde(alias = "maxImageSize")]
    pub max_image_size: i64,
    /// Capture SVG elements as images.
    #[php(prop, name = "captureSvg")]
    #[serde(default = "crate::serde_defaults::conversion_options_capture_svg")]
    #[serde(alias = "captureSvg")]
    pub capture_svg: bool,
    /// Infer image dimensions from data.
    #[php(prop, name = "inferDimensions")]
    #[serde(default = "crate::serde_defaults::conversion_options_infer_dimensions")]
    #[serde(alias = "inferDimensions")]
    pub infer_dimensions: bool,
    /// Maximum DOM traversal depth.
    ///
    /// `None` uses the library's internal native-stack safety limit. Explicit
    /// values above that safety limit are clamped to prevent process-aborting
    /// stack overflows on pathologically deep DOM trees.
    #[php(prop, name = "maxDepth")]
    #[serde(alias = "maxDepth")]
    pub max_depth: Option<i64>,
    /// CSS selectors for elements to exclude entirely (element + all content).
    ///
    /// Unlike `strip_tags` (which removes the tag wrapper but keeps children),
    /// excluded elements and all their descendants are dropped from the output.
    /// Supports any CSS selector that `tl` supports: tag names, `.class`,
    /// `#id`, `[attribute]`, etc.
    ///
    /// Invalid selectors are silently skipped at conversion time.
    ///
    /// Example: `vec![".cookie-banner".into(), "#ad-container".into(), "[role='complementary']".into()]`
    #[php(prop, name = "excludeSelectors")]
    #[serde(alias = "excludeSelectors")]
    #[serde(default)]
    pub exclude_selectors: Vec<String>,
    /// Which conversion tier to use.
    ///
    /// - `TierStrategy.Auto` (default) — automatically choose the best path.
    /// - `TierStrategy.Tier2` — always use the Tier-2 DOM-walk path.
    #[php(prop, name = "tierStrategy")]
    #[serde(default = "crate::serde_defaults::conversion_options_tier_strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "tierStrategy")]
    pub tier_strategy: String,
}

impl Default for ConversionOptions {
    fn default() -> Self {
        <html_to_markdown_rs::ConversionOptions as Default>::default().into()
    }
}

#[php_impl]
impl ConversionOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        headingStyle: String,
        listIndentType: String,
        listIndentWidth: i64,
        bullets: String,
        strongEmSymbol: String,
        escapeAsterisks: bool,
        escapeUnderscores: bool,
        escapeMisc: bool,
        escapeAscii: bool,
        codeLanguage: String,
        autolinks: bool,
        defaultTitle: bool,
        brInTables: bool,
        compactTables: bool,
        highlightStyle: String,
        extractMetadata: bool,
        whitespaceMode: String,
        stripNewlines: bool,
        wrap: bool,
        wrapWidth: i64,
        convertAsInline: bool,
        subSymbol: String,
        supSymbol: String,
        newlineStyle: String,
        codeBlockStyle: String,
        keepInlineImagesIn: Vec<String>,
        preprocessing: &PreprocessingOptions,
        encoding: String,
        debug: bool,
        stripTags: Vec<String>,
        preserveTags: Vec<String>,
        skipImages: bool,
        urlEscapeStyle: String,
        linkStyle: String,
        outputFormat: String,
        includeDocumentStructure: bool,
        extractImages: bool,
        maxImageSize: i64,
        captureSvg: bool,
        inferDimensions: bool,
        excludeSelectors: Vec<String>,
        tierStrategy: String,
        maxDepth: Option<i64>,
    ) -> Self {
        Self {
            heading_style: headingStyle,
            list_indent_type: listIndentType,
            list_indent_width: listIndentWidth,
            bullets,
            strong_em_symbol: strongEmSymbol,
            escape_asterisks: escapeAsterisks,
            escape_underscores: escapeUnderscores,
            escape_misc: escapeMisc,
            escape_ascii: escapeAscii,
            code_language: codeLanguage,
            autolinks,
            default_title: defaultTitle,
            br_in_tables: brInTables,
            compact_tables: compactTables,
            highlight_style: highlightStyle,
            extract_metadata: extractMetadata,
            whitespace_mode: whitespaceMode,
            strip_newlines: stripNewlines,
            wrap,
            wrap_width: wrapWidth,
            convert_as_inline: convertAsInline,
            sub_symbol: subSymbol,
            sup_symbol: supSymbol,
            newline_style: newlineStyle,
            code_block_style: codeBlockStyle,
            keep_inline_images_in: keepInlineImagesIn,
            preprocessing: preprocessing.clone(),
            encoding,
            debug,
            strip_tags: stripTags,
            preserve_tags: preserveTags,
            skip_images: skipImages,
            url_escape_style: urlEscapeStyle,
            link_style: linkStyle,
            output_format: outputFormat,
            include_document_structure: includeDocumentStructure,
            extract_images: extractImages,
            max_image_size: maxImageSize,
            capture_svg: captureSvg,
            infer_dimensions: inferDimensions,
            max_depth: maxDepth,
            exclude_selectors: excludeSelectors,
            tier_strategy: tierStrategy,
        }
    }

    pub fn get_heading_style(&self) -> String {
        self.heading_style.clone()
    }

    pub fn get_list_indent_type(&self) -> String {
        self.list_indent_type.clone()
    }

    pub fn get_list_indent_width(&self) -> i64 {
        self.list_indent_width.clone()
    }

    pub fn get_bullets(&self) -> String {
        self.bullets.clone()
    }

    pub fn get_strong_em_symbol(&self) -> String {
        self.strong_em_symbol.clone()
    }

    pub fn get_escape_asterisks(&self) -> bool {
        self.escape_asterisks.clone()
    }

    pub fn get_escape_underscores(&self) -> bool {
        self.escape_underscores.clone()
    }

    pub fn get_escape_misc(&self) -> bool {
        self.escape_misc.clone()
    }

    pub fn get_escape_ascii(&self) -> bool {
        self.escape_ascii.clone()
    }

    pub fn get_code_language(&self) -> String {
        self.code_language.clone()
    }

    pub fn get_autolinks(&self) -> bool {
        self.autolinks.clone()
    }

    pub fn get_default_title(&self) -> bool {
        self.default_title.clone()
    }

    pub fn get_br_in_tables(&self) -> bool {
        self.br_in_tables.clone()
    }

    pub fn get_compact_tables(&self) -> bool {
        self.compact_tables.clone()
    }

    pub fn get_highlight_style(&self) -> String {
        self.highlight_style.clone()
    }

    pub fn get_extract_metadata(&self) -> bool {
        self.extract_metadata.clone()
    }

    pub fn get_whitespace_mode(&self) -> String {
        self.whitespace_mode.clone()
    }

    pub fn get_strip_newlines(&self) -> bool {
        self.strip_newlines.clone()
    }

    pub fn get_wrap(&self) -> bool {
        self.wrap.clone()
    }

    pub fn get_wrap_width(&self) -> i64 {
        self.wrap_width.clone()
    }

    pub fn get_convert_as_inline(&self) -> bool {
        self.convert_as_inline.clone()
    }

    pub fn get_sub_symbol(&self) -> String {
        self.sub_symbol.clone()
    }

    pub fn get_sup_symbol(&self) -> String {
        self.sup_symbol.clone()
    }

    pub fn get_newline_style(&self) -> String {
        self.newline_style.clone()
    }

    pub fn get_code_block_style(&self) -> String {
        self.code_block_style.clone()
    }

    pub fn get_keep_inline_images_in(&self) -> Vec<String> {
        self.keep_inline_images_in.clone()
    }

    pub fn get_preprocessing(&self) -> PreprocessingOptions {
        self.preprocessing.clone()
    }

    pub fn get_encoding(&self) -> String {
        self.encoding.clone()
    }

    pub fn get_debug(&self) -> bool {
        self.debug.clone()
    }

    pub fn get_strip_tags(&self) -> Vec<String> {
        self.strip_tags.clone()
    }

    pub fn get_preserve_tags(&self) -> Vec<String> {
        self.preserve_tags.clone()
    }

    pub fn get_skip_images(&self) -> bool {
        self.skip_images.clone()
    }

    pub fn get_url_escape_style(&self) -> String {
        self.url_escape_style.clone()
    }

    pub fn get_link_style(&self) -> String {
        self.link_style.clone()
    }

    pub fn get_output_format(&self) -> String {
        self.output_format.clone()
    }

    pub fn get_include_document_structure(&self) -> bool {
        self.include_document_structure.clone()
    }

    pub fn get_extract_images(&self) -> bool {
        self.extract_images.clone()
    }

    pub fn get_max_image_size(&self) -> i64 {
        self.max_image_size.clone()
    }

    pub fn get_capture_svg(&self) -> bool {
        self.capture_svg.clone()
    }

    pub fn get_infer_dimensions(&self) -> bool {
        self.infer_dimensions.clone()
    }

    pub fn get_max_depth(&self) -> Option<i64> {
        self.max_depth.clone()
    }

    pub fn get_exclude_selectors(&self) -> Vec<String> {
        self.exclude_selectors.clone()
    }

    pub fn get_tier_strategy(&self) -> String {
        self.tier_strategy.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CoreProperties")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct CoreProperties {
    /// Document title
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Document subject/topic
    #[php(prop, name = "subject")]
    pub subject: Option<String>,
    /// Document creator/author
    #[php(prop, name = "creator")]
    pub creator: Option<String>,
    /// Keywords or tags
    #[php(prop, name = "keywords")]
    pub keywords: Option<String>,
    /// Document description/abstract
    #[php(prop, name = "description")]
    pub description: Option<String>,
    /// User who last modified the document
    #[php(prop, name = "lastModifiedBy")]
    #[serde(alias = "lastModifiedBy")]
    pub last_modified_by: Option<String>,
    /// Revision number
    #[php(prop, name = "revision")]
    pub revision: Option<String>,
    /// Creation timestamp (ISO 8601)
    #[php(prop, name = "created")]
    pub created: Option<String>,
    /// Last modification timestamp (ISO 8601)
    #[php(prop, name = "modified")]
    pub modified: Option<String>,
    /// Document category
    #[php(prop, name = "category")]
    pub category: Option<String>,
    /// Content status (Draft, Final, etc.)
    #[php(prop, name = "contentStatus")]
    #[serde(alias = "contentStatus")]
    pub content_status: Option<String>,
    /// Document language
    #[php(prop, name = "language")]
    pub language: Option<String>,
    /// Unique identifier
    #[php(prop, name = "identifier")]
    pub identifier: Option<String>,
    /// Document version
    #[php(prop, name = "version")]
    pub version: Option<String>,
    /// Last print timestamp (ISO 8601)
    #[php(prop, name = "lastPrinted")]
    #[serde(alias = "lastPrinted")]
    pub last_printed: Option<String>,
}

impl Default for CoreProperties {
    fn default() -> Self {
        <xberg::CoreProperties as Default>::default().into()
    }
}

#[php_impl]
impl CoreProperties {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_subject(&self) -> Option<String> {
        self.subject.clone()
    }

    pub fn get_creator(&self) -> Option<String> {
        self.creator.clone()
    }

    pub fn get_keywords(&self) -> Option<String> {
        self.keywords.clone()
    }

    pub fn get_description(&self) -> Option<String> {
        self.description.clone()
    }

    pub fn get_last_modified_by(&self) -> Option<String> {
        self.last_modified_by.clone()
    }

    pub fn get_revision(&self) -> Option<String> {
        self.revision.clone()
    }

    pub fn get_created(&self) -> Option<String> {
        self.created.clone()
    }

    pub fn get_modified(&self) -> Option<String> {
        self.modified.clone()
    }

    pub fn get_category(&self) -> Option<String> {
        self.category.clone()
    }

    pub fn get_content_status(&self) -> Option<String> {
        self.content_status.clone()
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_identifier(&self) -> Option<String> {
        self.identifier.clone()
    }

    pub fn get_version(&self) -> Option<String> {
        self.version.clone()
    }

    pub fn get_last_printed(&self) -> Option<String> {
        self.last_printed.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CrawlConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct CrawlConfig {
    /// Maximum crawl depth (number of link hops from the start URL).
    #[php(prop, name = "maxDepth")]
    #[serde(alias = "maxDepth")]
    pub max_depth: Option<i64>,
    /// Maximum number of pages to crawl.
    #[php(prop, name = "maxPages")]
    #[serde(alias = "maxPages")]
    pub max_pages: Option<i64>,
    /// Maximum links enqueued from a single page. Defaults to 10000.
    ///
    /// Bounds the work one hostile or pathological page can create; links past the
    /// cap are dropped and a warning is logged.
    #[php(prop, name = "maxLinksPerPage")]
    #[serde(alias = "maxLinksPerPage")]
    pub max_links_per_page: Option<i64>,
    /// Maximum number of concurrent requests.
    #[php(prop, name = "maxConcurrent")]
    #[serde(alias = "maxConcurrent")]
    pub max_concurrent: Option<i64>,
    /// Traversal order. Defaults to breadth-first.
    ///
    /// A frontier or strategy set explicitly on `CrawlEngineBuilder` takes precedence over
    /// this field.
    #[php(prop, name = "crawlStrategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "crawlStrategy")]
    pub crawl_strategy: String,
    /// Content filter applied to each page. `None` keeps every page.
    ///
    /// A content filter set explicitly on `CrawlEngineBuilder` takes precedence.
    #[php(prop, name = "contentFilter")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "contentFilter")]
    pub content_filter: Option<String>,
    /// Query the BM25 content filter scores pages against. Required by `ContentFilterKind.Bm25`.
    #[php(prop, name = "bm25Query")]
    #[serde(alias = "bm25Query")]
    pub bm25_query: Option<String>,
    /// Minimum BM25 score a page must reach to be kept. Defaults to `0.0`.
    #[php(prop, name = "bm25Threshold")]
    #[serde(alias = "bm25Threshold")]
    pub bm25_threshold: Option<f64>,
    /// Whether to respect robots.txt directives.
    #[php(prop, name = "respectRobotsTxt")]
    #[serde(default = "crate::serde_defaults::crawl_config_respect_robots_txt")]
    #[serde(alias = "respectRobotsTxt")]
    pub respect_robots_txt: bool,
    /// When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF blocks)
    /// are surfaced as `ScrapeResult` records with the matching `status_code` rather
    /// than raised as `CrawlError`. Default `false` preserves the historical
    /// throw-on-error contract for direct fetches. Independently of this flag,
    /// 404s reached at the end of a redirect chain are *always* surfaced softly —
    /// the user opted into redirect-following, so receiving a 404 there is part of
    /// the normal flow rather than an unexpected error.
    #[php(prop, name = "softHttpErrors")]
    #[serde(default = "crate::serde_defaults::crawl_config_soft_http_errors")]
    #[serde(alias = "softHttpErrors")]
    pub soft_http_errors: bool,
    /// Custom user-agent string.
    #[php(prop, name = "userAgent")]
    #[serde(alias = "userAgent")]
    pub user_agent: Option<String>,
    /// Whether to restrict crawling to the same domain.
    #[php(prop, name = "stayOnDomain")]
    #[serde(default = "crate::serde_defaults::crawl_config_stay_on_domain")]
    #[serde(alias = "stayOnDomain")]
    pub stay_on_domain: bool,
    /// Whether to allow subdomains when `stay_on_domain` is true.
    #[php(prop, name = "allowSubdomains")]
    #[serde(default = "crate::serde_defaults::crawl_config_allow_subdomains")]
    #[serde(alias = "allowSubdomains")]
    pub allow_subdomains: bool,
    /// Regex patterns for paths to include during crawling.
    #[php(prop, name = "includePaths")]
    #[serde(alias = "includePaths")]
    #[serde(default)]
    pub include_paths: Vec<String>,
    /// Regex patterns for paths to exclude during crawling.
    #[php(prop, name = "excludePaths")]
    #[serde(alias = "excludePaths")]
    #[serde(default)]
    pub exclude_paths: Vec<String>,
    /// Custom HTTP headers to send with each request.
    #[php(prop, name = "customHeaders")]
    #[serde(alias = "customHeaders")]
    #[serde(default)]
    pub custom_headers: HashMap<String, String>,
    /// Timeout for individual HTTP requests (in milliseconds when serialized).
    #[php(prop, name = "requestTimeout")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "requestTimeout")]
    pub request_timeout: Option<i64>,
    /// Per-domain rate limit in milliseconds. When set, enforces a minimum delay
    /// between requests to the same domain. Defaults to 200ms when `None`.
    #[php(prop, name = "rateLimitMs")]
    #[serde(alias = "rateLimitMs")]
    pub rate_limit_ms: Option<i64>,
    /// Maximum number of redirects to follow.
    #[php(prop, name = "maxRedirects")]
    #[serde(default = "crate::serde_defaults::crawl_config_max_redirects")]
    #[serde(alias = "maxRedirects")]
    pub max_redirects: i64,
    /// Number of retry attempts for failed requests.
    #[php(prop, name = "retryCount")]
    #[serde(default = "crate::serde_defaults::crawl_config_retry_count")]
    #[serde(alias = "retryCount")]
    pub retry_count: i64,
    /// HTTP status codes that should trigger a retry.
    #[php(prop, name = "retryCodes")]
    #[serde(alias = "retryCodes")]
    #[serde(default)]
    pub retry_codes: Vec<u16>,
    /// Whether to enable cookie handling.
    #[php(prop, name = "cookiesEnabled")]
    #[serde(default = "crate::serde_defaults::crawl_config_cookies_enabled")]
    #[serde(alias = "cookiesEnabled")]
    pub cookies_enabled: bool,
    /// Authentication configuration.
    pub auth: Option<AuthConfig>,
    /// Maximum response body size in bytes.
    ///
    /// `None` does not mean unbounded: an unset cap falls back to a 100 MiB safety
    /// ceiling, because HTTP responses are decompressed while being read and a few
    /// hundred compressed bytes can otherwise expand to gigabytes in memory. To read
    /// bodies larger than that, set this explicitly.
    #[php(prop, name = "maxBodySize")]
    #[serde(alias = "maxBodySize")]
    pub max_body_size: Option<i64>,
    /// CSS selectors for tags to remove from HTML before processing.
    #[php(prop, name = "removeTags")]
    #[serde(alias = "removeTags")]
    #[serde(default)]
    pub remove_tags: Vec<String>,
    /// Content extraction and conversion configuration.
    #[serde(default)]
    pub content: ContentConfig,
    /// Maximum number of URLs to return from a map operation.
    #[php(prop, name = "mapLimit")]
    #[serde(alias = "mapLimit")]
    pub map_limit: Option<i64>,
    /// Search filter for map results (case-insensitive substring match on URLs).
    #[php(prop, name = "mapSearch")]
    #[serde(alias = "mapSearch")]
    pub map_search: Option<String>,
    /// Whether to download assets (CSS, JS, images, etc.) from the page.
    #[php(prop, name = "downloadAssets")]
    #[serde(default = "crate::serde_defaults::crawl_config_download_assets")]
    #[serde(alias = "downloadAssets")]
    pub download_assets: bool,
    /// Filter for asset categories to download.
    #[php(prop, name = "assetTypes")]
    #[serde(alias = "assetTypes")]
    #[serde(default)]
    pub asset_types: Vec<String>,
    /// Maximum size in bytes for individual asset downloads.
    #[php(prop, name = "maxAssetSize")]
    #[serde(alias = "maxAssetSize")]
    pub max_asset_size: Option<i64>,
    /// Browser configuration.
    #[serde(default)]
    pub browser: BrowserConfig,
    /// Proxy configuration for HTTP requests.
    pub proxy: Option<ProxyConfig>,
    /// List of user-agent strings for rotation. If non-empty, overrides `user_agent`.
    #[php(prop, name = "userAgents")]
    #[serde(alias = "userAgents")]
    #[serde(default)]
    pub user_agents: Vec<String>,
    /// Whether to capture a screenshot when using the browser.
    ///
    /// Only supported by `scrape()` with `BrowserBackend.Chromiumoxide` and
    /// `BrowserMode.Always` or `Stealth`. A screenshot is 100–500 KB of PNG per page,
    /// so `crawl()` does not carry screenshots in `CrawlPageResult`/`CrawlResult` at
    /// all — a multi-thousand-page crawl holding one per page in memory is not a safe
    /// default. Setting this with any other configuration (a different backend,
    /// `BrowserMode.Auto`/`Never`, or during `crawl()`) has no effect and logs a
    /// warning rather than silently doing nothing.
    #[php(prop, name = "captureScreenshot")]
    #[serde(default = "crate::serde_defaults::crawl_config_capture_screenshot")]
    #[serde(alias = "captureScreenshot")]
    pub capture_screenshot: bool,
    /// Re-enqueue discovered `LinkType.Document` URLs into the crawl frontier so
    /// the crawl follows links *from* document pages (PDFs, etc.) as it would
    /// from HTML pages. Default: `false` (documents terminate at materialisation).
    #[php(prop, name = "followDocumentUrls")]
    #[serde(default = "crate::serde_defaults::crawl_config_follow_document_urls")]
    #[serde(alias = "followDocumentUrls")]
    pub follow_document_urls: bool,
    /// Maximum document-depth (from the seed URL through document links only)
    /// when `follow_document_urls` is true. `None` means inherit `max_depth`.
    /// Independent of `max_depth`: a document URL is enqueued only if BOTH the
    /// outer `max_depth` and (if set) `document_url_depth` permit it.
    #[php(prop, name = "documentUrlDepth")]
    #[serde(alias = "documentUrlDepth")]
    #[serde(default)]
    pub document_url_depth: Option<u32>,
    /// Whether to download non-HTML documents (PDF, DOCX, images, code, etc.) instead of skipping them.
    /// Defaults to `true` — unlike `download_assets` and `capture_screenshot`, which default to `false`.
    #[php(prop, name = "downloadDocuments")]
    #[serde(default = "crate::serde_defaults::crawl_config_download_documents")]
    #[serde(alias = "downloadDocuments")]
    pub download_documents: bool,
    /// Maximum size in bytes for document downloads. Defaults to 50 MB.
    #[php(prop, name = "documentMaxSize")]
    #[serde(alias = "documentMaxSize")]
    pub document_max_size: Option<i64>,
    /// Allowlist of MIME types to download. If empty, uses built-in defaults.
    #[php(prop, name = "documentMimeTypes")]
    #[serde(alias = "documentMimeTypes")]
    #[serde(default)]
    pub document_mime_types: Vec<String>,
    /// Directory to stream downloaded document bytes into instead of holding them in
    /// memory on `DownloadedDocument.content`. When set, `content` is left empty and
    /// `DownloadedDocument.content_path` is populated with `<dir>/<content_hash>.<ext>`.
    /// `None` (default) preserves today's in-memory-only behavior. Has no effect on
    /// wasm32, which has no filesystem — use `document_content_encoding` there instead.
    #[php(prop, name = "documentOutputDir")]
    #[serde(alias = "documentOutputDir")]
    #[serde(default)]
    pub document_output_dir: Option<String>,
    /// Opt-in encoding that duplicates `DownloadedDocument.content` into a serializable
    /// field for language bindings that need the bytes in-memory (`content` itself is
    /// `alef(skip)`ed). `None` (default) means no encoding is produced. Independent of
    /// `document_output_dir` — set both to get a file on disk and an in-memory copy.
    #[php(prop, name = "documentContentEncoding")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "documentContentEncoding")]
    #[serde(default)]
    pub document_content_encoding: Option<String>,
    /// Path to write WARC output. If `None`, WARC output is disabled.
    #[php(prop, name = "warcOutput")]
    #[serde(alias = "warcOutput")]
    pub warc_output: Option<String>,
    /// Named browser profile for persistent sessions (cookies, localStorage).
    ///
    /// Chromiumoxide backend only. The native backend runs an in-process JavaScript
    /// engine with no Chrome process and therefore no profile directory, so this is
    /// ignored there and logs a warning. It is also ignored — with a warning — when a
    /// shared browser pool is in use (the pool launches before any per-crawl config
    /// exists) or when connecting to an external CDP endpoint whose process crawlberg
    /// does not own.
    #[php(prop, name = "browserProfile")]
    #[serde(alias = "browserProfile")]
    pub browser_profile: Option<String>,
    /// Whether to save changes back to the browser profile on exit.
    #[php(prop, name = "saveBrowserProfile")]
    #[serde(default = "crate::serde_defaults::crawl_config_save_browser_profile")]
    #[serde(alias = "saveBrowserProfile")]
    pub save_browser_profile: bool,
    /// SSRF policy for outbound network requests. Default: deny private networks,
    /// allow http/https only, max 5 redirects.
    ///
    /// All policy fields are exposed to language bindings.
    ///
    /// **wasm32 (including Node.js): `deny_private` does not stop hostname-based
    /// requests.** There is no DNS resolution on this target, so only a literal IP host is
    /// checked against the policy — a domain name is always permitted, regardless of
    /// `deny_private`. Under Node, where `fetch` enforces no CORS, this means a service
    /// embedding the wasm binding can be driven to internal hosts by domain name even with
    /// `deny_private = true`. Enforce egress restrictions at the network layer for that
    /// deployment target; do not rely on this field. See `crawlberg.net.validate_url`.
    #[serde(default = "crate::serde_defaults::crawl_config_ssrf")]
    pub ssrf: SsrfPolicy,
    /// Pins `SsrfPolicy.deny_private` to a caller-chosen value, bypassing the
    /// `CRAWLBERG_ALLOW_PRIVATE_NETWORK` operator override entirely for this config.
    ///
    /// `ssrf.deny_private` is a plain, always-serialized `bool`: several alef-generated
    /// bindings construct `SsrfPolicy.default()` (hardcoding `deny_private: true`)
    /// whenever their caller never touches SSRF settings at all, so `true` on that field
    /// alone cannot distinguish "the caller wants private networks denied" from "the
    /// binding's own structural default landed on `true`". The environment variable
    /// exists precisely to resolve that ambiguity in the common case by treating any
    /// `true` as inconclusive and deferring to the operator.
    ///
    /// Set this field when that default-deferral is wrong for your call — e.g. a test
    /// that must prove `deny_private: true` still denies even while the operator has set
    /// `CRAWLBERG_ALLOW_PRIVATE_NETWORK` suite-wide for every other call. `None` (default)
    /// preserves today's behavior: the environment variable may still flip
    /// `ssrf.deny_private` to `false`. `Some(value)` pins `ssrf.deny_private` to `value`
    /// and the environment variable is not consulted for this config.
    #[php(prop, name = "ssrfDenyPrivateExplicit")]
    #[serde(alias = "ssrfDenyPrivateExplicit")]
    #[serde(default)]
    pub ssrf_deny_private_explicit: Option<bool>,
}

impl Default for CrawlConfig {
    fn default() -> Self {
        <crawlberg::CrawlConfig as Default>::default().into()
    }
}

#[php_impl]
impl CrawlConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        crawlStrategy: String,
        respectRobotsTxt: bool,
        softHttpErrors: bool,
        stayOnDomain: bool,
        allowSubdomains: bool,
        includePaths: Vec<String>,
        excludePaths: Vec<String>,
        customHeaders: HashMap<String, String>,
        maxRedirects: i64,
        retryCount: i64,
        retryCodes: Vec<u16>,
        cookiesEnabled: bool,
        removeTags: Vec<String>,
        content: &ContentConfig,
        downloadAssets: bool,
        assetTypes: Vec<String>,
        browser: &BrowserConfig,
        userAgents: Vec<String>,
        captureScreenshot: bool,
        followDocumentUrls: bool,
        downloadDocuments: bool,
        documentMimeTypes: Vec<String>,
        saveBrowserProfile: bool,
        ssrf: &SsrfPolicy,
        maxDepth: Option<i64>,
        maxPages: Option<i64>,
        maxLinksPerPage: Option<i64>,
        maxConcurrent: Option<i64>,
        contentFilter: Option<String>,
        bm25Query: Option<String>,
        bm25Threshold: Option<f64>,
        userAgent: Option<String>,
        requestTimeout: Option<i64>,
        rateLimitMs: Option<i64>,
        auth: Option<&AuthConfig>,
        maxBodySize: Option<i64>,
        mapLimit: Option<i64>,
        mapSearch: Option<String>,
        maxAssetSize: Option<i64>,
        proxy: Option<&ProxyConfig>,
        documentUrlDepth: Option<u32>,
        documentMaxSize: Option<i64>,
        documentOutputDir: Option<String>,
        documentContentEncoding: Option<String>,
        warcOutput: Option<String>,
        browserProfile: Option<String>,
        ssrfDenyPrivateExplicit: Option<bool>,
    ) -> Self {
        Self {
            max_depth: maxDepth,
            max_pages: maxPages,
            max_links_per_page: maxLinksPerPage,
            max_concurrent: maxConcurrent,
            crawl_strategy: crawlStrategy,
            content_filter: contentFilter,
            bm25_query: bm25Query,
            bm25_threshold: bm25Threshold,
            respect_robots_txt: respectRobotsTxt,
            soft_http_errors: softHttpErrors,
            user_agent: userAgent,
            stay_on_domain: stayOnDomain,
            allow_subdomains: allowSubdomains,
            include_paths: includePaths,
            exclude_paths: excludePaths,
            custom_headers: customHeaders,
            request_timeout: requestTimeout,
            rate_limit_ms: rateLimitMs,
            max_redirects: maxRedirects,
            retry_count: retryCount,
            retry_codes: retryCodes,
            cookies_enabled: cookiesEnabled,
            auth: auth.cloned(),
            max_body_size: maxBodySize,
            remove_tags: removeTags,
            content: content.clone(),
            map_limit: mapLimit,
            map_search: mapSearch,
            download_assets: downloadAssets,
            asset_types: assetTypes,
            max_asset_size: maxAssetSize,
            browser: browser.clone(),
            proxy: proxy.cloned(),
            user_agents: userAgents,
            capture_screenshot: captureScreenshot,
            follow_document_urls: followDocumentUrls,
            document_url_depth: documentUrlDepth,
            download_documents: downloadDocuments,
            document_max_size: documentMaxSize,
            document_mime_types: documentMimeTypes,
            document_output_dir: documentOutputDir,
            document_content_encoding: documentContentEncoding,
            warc_output: warcOutput,
            browser_profile: browserProfile,
            save_browser_profile: saveBrowserProfile,
            ssrf: ssrf.clone(),
            ssrf_deny_private_explicit: ssrfDenyPrivateExplicit,
        }
    }

    pub fn get_max_depth(&self) -> Option<i64> {
        self.max_depth.clone()
    }

    pub fn get_max_pages(&self) -> Option<i64> {
        self.max_pages.clone()
    }

    pub fn get_max_links_per_page(&self) -> Option<i64> {
        self.max_links_per_page.clone()
    }

    pub fn get_max_concurrent(&self) -> Option<i64> {
        self.max_concurrent.clone()
    }

    pub fn get_crawl_strategy(&self) -> String {
        self.crawl_strategy.clone()
    }

    pub fn get_content_filter(&self) -> Option<String> {
        self.content_filter.clone().map(Into::into)
    }

    pub fn get_bm25_query(&self) -> Option<String> {
        self.bm25_query.clone()
    }

    pub fn get_bm25_threshold(&self) -> Option<f64> {
        self.bm25_threshold.clone()
    }

    pub fn get_respect_robots_txt(&self) -> bool {
        self.respect_robots_txt.clone()
    }

    pub fn get_soft_http_errors(&self) -> bool {
        self.soft_http_errors.clone()
    }

    pub fn get_user_agent(&self) -> Option<String> {
        self.user_agent.clone()
    }

    pub fn get_stay_on_domain(&self) -> bool {
        self.stay_on_domain.clone()
    }

    pub fn get_allow_subdomains(&self) -> bool {
        self.allow_subdomains.clone()
    }

    pub fn get_include_paths(&self) -> Vec<String> {
        self.include_paths.clone()
    }

    pub fn get_exclude_paths(&self) -> Vec<String> {
        self.exclude_paths.clone()
    }

    pub fn get_custom_headers(&self) -> HashMap<String, String> {
        self.custom_headers.clone()
    }

    pub fn get_request_timeout(&self) -> Option<i64> {
        self.request_timeout.clone()
    }

    pub fn get_rate_limit_ms(&self) -> Option<i64> {
        self.rate_limit_ms.clone()
    }

    pub fn get_max_redirects(&self) -> i64 {
        self.max_redirects.clone()
    }

    pub fn get_retry_count(&self) -> i64 {
        self.retry_count.clone()
    }

    pub fn get_retry_codes(&self) -> Vec<u16> {
        self.retry_codes.clone()
    }

    pub fn get_cookies_enabled(&self) -> bool {
        self.cookies_enabled.clone()
    }

    pub fn get_auth(&self) -> Option<AuthConfig> {
        self.auth.clone().map(Into::into)
    }

    pub fn set_auth(&mut self, value: Option<&AuthConfig>) {
        self.auth = value.map(|value| value.clone().into());
    }

    pub fn get_max_body_size(&self) -> Option<i64> {
        self.max_body_size.clone()
    }

    pub fn get_remove_tags(&self) -> Vec<String> {
        self.remove_tags.clone()
    }

    pub fn get_content(&self) -> ContentConfig {
        self.content.clone()
    }

    pub fn get_map_limit(&self) -> Option<i64> {
        self.map_limit.clone()
    }

    pub fn get_map_search(&self) -> Option<String> {
        self.map_search.clone()
    }

    pub fn get_download_assets(&self) -> bool {
        self.download_assets.clone()
    }

    pub fn get_asset_types(&self) -> Vec<String> {
        self.asset_types.clone()
    }

    pub fn get_max_asset_size(&self) -> Option<i64> {
        self.max_asset_size.clone()
    }

    pub fn get_browser(&self) -> BrowserConfig {
        self.browser.clone()
    }

    pub fn get_proxy(&self) -> Option<ProxyConfig> {
        self.proxy.clone().map(Into::into)
    }

    pub fn set_proxy(&mut self, value: Option<&ProxyConfig>) {
        self.proxy = value.map(|value| value.clone().into());
    }

    pub fn get_user_agents(&self) -> Vec<String> {
        self.user_agents.clone()
    }

    pub fn get_capture_screenshot(&self) -> bool {
        self.capture_screenshot.clone()
    }

    pub fn get_follow_document_urls(&self) -> bool {
        self.follow_document_urls.clone()
    }

    pub fn get_document_url_depth(&self) -> Option<u32> {
        self.document_url_depth.clone()
    }

    pub fn get_download_documents(&self) -> bool {
        self.download_documents.clone()
    }

    pub fn get_document_max_size(&self) -> Option<i64> {
        self.document_max_size.clone()
    }

    pub fn get_document_mime_types(&self) -> Vec<String> {
        self.document_mime_types.clone()
    }

    pub fn get_document_output_dir(&self) -> Option<String> {
        self.document_output_dir.clone()
    }

    pub fn get_document_content_encoding(&self) -> Option<String> {
        self.document_content_encoding.clone().map(Into::into)
    }

    pub fn get_warc_output(&self) -> Option<String> {
        self.warc_output.clone()
    }

    pub fn get_browser_profile(&self) -> Option<String> {
        self.browser_profile.clone()
    }

    pub fn get_save_browser_profile(&self) -> bool {
        self.save_browser_profile.clone()
    }

    pub fn get_ssrf(&self) -> SsrfPolicy {
        self.ssrf.clone()
    }

    pub fn get_ssrf_deny_private_explicit(&self) -> Option<bool> {
        self.ssrf_deny_private_explicit.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CsvConfig")]
#[serde(default)]
pub struct CsvConfig {
    /// Field delimiter, as a single-character string (e.g. `","`, `";"`,
    /// `"\t"`, `"|"`). When `None` (default), the delimiter is auto-detected
    /// from a sample of the file.
    ///
    /// Must be exactly one ASCII byte when set — `ExtractionConfig.validate`
    /// rejects an empty string or a multi-byte value with a helpful error.
    /// The TSV MIME type (`text/tab-separated-values`) always forces `\t`
    /// regardless of this setting.
    #[php(prop, name = "delimiter")]
    pub delimiter: Option<String>,
    /// Line prefixes that mark a comment line to skip entirely during row
    /// parsing (e.g. `["#"]`). A line is treated as a comment when its
    /// trimmed start matches any of these prefixes exactly.
    ///
    /// Default: empty, meaning no line is treated as a comment (matches the
    /// pre-existing extractor behavior).
    #[php(prop, name = "commentPrefixes")]
    #[serde(alias = "commentPrefixes")]
    pub comment_prefixes: Vec<String>,
}

impl Default for CsvConfig {
    fn default() -> Self {
        <xberg::CsvConfig as Default>::default().into()
    }
}

#[php_impl]
impl CsvConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(commentPrefixes: Vec<String>, delimiter: Option<String>) -> Self {
        Self {
            delimiter,
            comment_prefixes: commentPrefixes,
        }
    }

    pub fn get_delimiter(&self) -> Option<String> {
        self.delimiter.clone()
    }

    pub fn get_comment_prefixes(&self) -> Vec<String> {
        self.comment_prefixes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\CsvMetadata")]
#[serde(default)]
pub struct CsvMetadata {
    /// Total number of data rows (excluding the header row if present).
    #[php(prop, name = "rowCount")]
    #[serde(alias = "rowCount")]
    pub row_count: u32,
    /// Number of columns detected.
    #[php(prop, name = "columnCount")]
    #[serde(alias = "columnCount")]
    pub column_count: u32,
    /// Field delimiter character (e.g. `","` or `"\t"`).
    #[php(prop, name = "delimiter")]
    pub delimiter: Option<String>,
    /// Whether the first row was treated as a header.
    #[php(prop, name = "hasHeader")]
    #[serde(alias = "hasHeader")]
    pub has_header: bool,
    /// Inferred data type for each column (e.g. `"string"`, `"integer"`, `"float"`).
    #[php(prop, name = "columnTypes")]
    #[serde(alias = "columnTypes")]
    pub column_types: Option<Vec<String>>,
}

impl Default for CsvMetadata {
    fn default() -> Self {
        <xberg::CsvMetadata as Default>::default().into()
    }
}

#[php_impl]
impl CsvMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        rowCount: u32,
        columnCount: u32,
        hasHeader: bool,
        delimiter: Option<String>,
        columnTypes: Option<Vec<String>>,
    ) -> Self {
        Self {
            row_count: rowCount,
            column_count: columnCount,
            delimiter,
            has_header: hasHeader,
            column_types: columnTypes,
        }
    }

    pub fn get_row_count(&self) -> u32 {
        self.row_count.clone()
    }

    pub fn get_column_count(&self) -> u32 {
        self.column_count.clone()
    }

    pub fn get_delimiter(&self) -> Option<String> {
        self.delimiter.clone()
    }

    pub fn get_has_header(&self) -> bool {
        self.has_header.clone()
    }

    pub fn get_column_types(&self) -> Option<Vec<String>> {
        self.column_types.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DbfFieldInfo")]
#[serde(default)]
pub struct DbfFieldInfo {
    /// Field (column) name.
    #[php(prop, name = "name")]
    pub name: String,
    /// dBASE field type character (e.g. `"C"` for character, `"N"` for numeric).
    #[php(prop, name = "fieldType")]
    #[serde(alias = "fieldType")]
    pub field_type: String,
}

#[php_impl]
impl DbfFieldInfo {
    #[php(constructor)]
    pub fn new(name: String, fieldType: String) -> Self {
        Self {
            name,
            field_type: fieldType,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_field_type(&self) -> String {
        self.field_type.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DbfMetadata")]
#[serde(default)]
pub struct DbfMetadata {
    /// Total number of data records in the DBF file.
    #[php(prop, name = "recordCount")]
    #[serde(alias = "recordCount")]
    pub record_count: i64,
    /// Number of field (column) definitions.
    #[php(prop, name = "fieldCount")]
    #[serde(alias = "fieldCount")]
    pub field_count: i64,
    /// Descriptor for each field in the table schema.
    #[serde(default)]
    pub fields: Vec<DbfFieldInfo>,
}

impl Default for DbfMetadata {
    fn default() -> Self {
        <xberg::DbfMetadata as Default>::default().into()
    }
}

#[php_impl]
impl DbfMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(recordCount: i64, fieldCount: i64, fields: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut fields_core_result: Vec<DbfFieldInfo> = Vec::new();
        for (_, item) in fields.iter() {
            if let Some(parsed) = <&DbfFieldInfo as ext_php_rs::convert::FromZval>::from_zval(item) {
                fields_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DbfFieldInfo".to_string(),
                ));
            }
        }
        let fields_core: Vec<DbfFieldInfo> = fields_core_result;
        Ok(Self {
            record_count: recordCount,
            field_count: fieldCount,
            fields: fields_core,
        })
    }

    pub fn get_record_count(&self) -> i64 {
        self.record_count.clone()
    }

    pub fn get_field_count(&self) -> i64 {
        self.field_count.clone()
    }

    pub fn get_fields(&self) -> Vec<DbfFieldInfo> {
        self.fields.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DeepseekOcrBackendOptions")]
#[serde(default)]
pub struct DeepseekOcrBackendOptions {
    /// Local DeepSeek-OCR model directory. The backend requires this option.
    #[php(prop, name = "modelPath")]
    #[serde(alias = "modelPath")]
    pub model_path: Option<String>,
    /// Optional per-call device override.
    #[php(prop, name = "device")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub device: Option<String>,
    /// DeepSeek-OCR model generation, either 1 or 2. Defaults to 2.
    #[php(prop, name = "version")]
    pub version: Option<u32>,
}

impl Default for DeepseekOcrBackendOptions {
    fn default() -> Self {
        <xberg::candle_ocr::DeepseekOcrBackendOptions as Default>::default().into()
    }
}

#[php_impl]
impl DeepseekOcrBackendOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_model_path(&self) -> Option<String> {
        self.model_path.clone()
    }

    pub fn get_device(&self) -> Option<String> {
        self.device.clone().map(Into::into)
    }

    pub fn get_version(&self) -> Option<u32> {
        self.version.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DetectResponse")]
#[serde(default)]
pub struct DetectResponse {
    /// Detected MIME type
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
    /// Original filename (if provided)
    #[php(prop, name = "filename")]
    pub filename: Option<String>,
}

#[php_impl]
impl DetectResponse {
    #[php(constructor)]
    pub fn new(mimeType: String, filename: Option<String>) -> Self {
        Self {
            mime_type: mimeType,
            filename,
        }
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }

    pub fn get_filename(&self) -> Option<String> {
        self.filename.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DetectionResult")]
#[serde(default)]
pub struct DetectionResult {
    /// Page width in pixels (as seen by the model).
    #[php(prop, name = "pageWidth")]
    #[serde(alias = "pageWidth")]
    pub page_width: u32,
    /// Page height in pixels (as seen by the model).
    #[php(prop, name = "pageHeight")]
    #[serde(alias = "pageHeight")]
    pub page_height: u32,
    /// All layout detections on this page after postprocessing.
    pub detections: Vec<LayoutDetection>,
}

#[php_impl]
impl DetectionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(pageWidth: u32, pageHeight: u32, detections: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut detections_core_result: Vec<LayoutDetection> = Vec::new();
        for (_, item) in detections.iter() {
            if let Some(parsed) = <&LayoutDetection as ext_php_rs::convert::FromZval>::from_zval(item) {
                detections_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to LayoutDetection".to_string(),
                ));
            }
        }
        let detections_core: Vec<LayoutDetection> = detections_core_result;
        Ok(Self {
            page_width: pageWidth,
            page_height: pageHeight,
            detections: detections_core,
        })
    }

    pub fn get_page_width(&self) -> u32 {
        self.page_width.clone()
    }

    pub fn get_page_height(&self) -> u32 {
        self.page_height.clone()
    }

    pub fn get_detections(&self) -> Vec<LayoutDetection> {
        self.detections.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DiffHunk")]
#[serde(default)]
pub struct DiffHunk {
    /// Starting line number in the old content (0-indexed).
    #[php(prop, name = "fromLine")]
    #[serde(alias = "fromLine")]
    pub from_line: i64,
    /// Number of lines from the old content in this hunk.
    #[php(prop, name = "fromCount")]
    #[serde(alias = "fromCount")]
    pub from_count: i64,
    /// Starting line number in the new content (0-indexed).
    #[php(prop, name = "toLine")]
    #[serde(alias = "toLine")]
    pub to_line: i64,
    /// Number of lines from the new content in this hunk.
    #[php(prop, name = "toCount")]
    #[serde(alias = "toCount")]
    pub to_count: i64,
    /// Lines that make up this hunk.
    pub lines: Vec<DiffLine>,
}

#[php_impl]
impl DiffHunk {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        fromLine: i64,
        fromCount: i64,
        toLine: i64,
        toCount: i64,
        lines: &ext_php_rs::types::ZendHashTable,
    ) -> PhpResult<Self> {
        let mut lines_core_result: Vec<DiffLine> = Vec::new();
        for (_, item) in lines.iter() {
            if let Some(parsed) = <&DiffLine as ext_php_rs::convert::FromZval>::from_zval(item) {
                lines_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DiffLine".to_string(),
                ));
            }
        }
        let lines_core: Vec<DiffLine> = lines_core_result;
        Ok(Self {
            from_line: fromLine,
            from_count: fromCount,
            to_line: toLine,
            to_count: toCount,
            lines: lines_core,
        })
    }

    pub fn get_from_line(&self) -> i64 {
        self.from_line.clone()
    }

    pub fn get_from_count(&self) -> i64 {
        self.from_count.clone()
    }

    pub fn get_to_line(&self) -> i64 {
        self.to_line.clone()
    }

    pub fn get_to_count(&self) -> i64 {
        self.to_count.clone()
    }

    pub fn get_lines(&self) -> Vec<DiffLine> {
        self.lines.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DiffOptions")]
#[serde(default)]
pub struct DiffOptions {
    /// Include metadata changes in the diff. Default: `true`.
    #[php(prop, name = "includeMetadata")]
    #[serde(default = "crate::serde_defaults::diff_options_include_metadata")]
    #[serde(alias = "includeMetadata")]
    pub include_metadata: bool,
    /// Include embedded-children changes in the diff. Default: `true`.
    #[php(prop, name = "includeEmbedded")]
    #[serde(default = "crate::serde_defaults::diff_options_include_embedded")]
    #[serde(alias = "includeEmbedded")]
    pub include_embedded: bool,
    /// Truncate content to this many characters before diffing.
    ///
    /// Useful for very large documents where only the first N characters matter.
    /// `None` means no truncation.
    #[php(prop, name = "maxContentChars")]
    #[serde(alias = "maxContentChars")]
    pub max_content_chars: Option<i64>,
}

impl Default for DiffOptions {
    fn default() -> Self {
        <xberg::DiffOptions as Default>::default().into()
    }
}

#[php_impl]
impl DiffOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(includeMetadata: bool, includeEmbedded: bool, maxContentChars: Option<i64>) -> Self {
        Self {
            include_metadata: includeMetadata,
            include_embedded: includeEmbedded,
            max_content_chars: maxContentChars,
        }
    }

    pub fn get_include_metadata(&self) -> bool {
        self.include_metadata.clone()
    }

    pub fn get_include_embedded(&self) -> bool {
        self.include_embedded.clone()
    }

    pub fn get_max_content_chars(&self) -> Option<i64> {
        self.max_content_chars.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> DiffOptions {
        xberg::DiffOptions::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DjotAttributeGroup")]
#[serde(default)]
pub struct DjotAttributeGroup {
    /// Element identifier used by the Djot attribute map.
    #[php(prop, name = "identifier")]
    pub identifier: String,
    /// Attributes associated with the element.
    pub attributes: Attributes,
}

impl Default for DjotAttributeGroup {
    fn default() -> Self {
        <xberg::DjotAttributeGroup as Default>::default().into()
    }
}

#[php_impl]
impl DjotAttributeGroup {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(identifier: String, attributes: &Attributes) -> Self {
        Self {
            identifier,
            attributes: attributes.clone(),
        }
    }

    pub fn get_identifier(&self) -> String {
        self.identifier.clone()
    }

    pub fn get_attributes(&self) -> Attributes {
        self.attributes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DjotContent")]
#[serde(default)]
pub struct DjotContent {
    /// Plain text representation for backwards compatibility
    #[php(prop, name = "plainText")]
    #[serde(alias = "plainText")]
    pub plain_text: String,
    /// Structured block-level content
    pub blocks: Vec<FormattedBlock>,
    /// Metadata from YAML frontmatter
    pub metadata: Metadata,
    /// Extracted tables as structured data
    pub tables: Vec<Table>,
    /// Extracted images with metadata
    pub images: Vec<DjotImage>,
    /// Extracted links with URLs
    pub links: Vec<DjotLink>,
    /// Footnote definitions
    pub footnotes: Vec<Footnote>,
    /// Attributes mapped by element identifier (if present)
    #[serde(default)]
    pub attributes: Vec<DjotAttributeGroup>,
}

#[php_impl]
impl DjotContent {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        plainText: String,
        blocks: &ext_php_rs::types::ZendHashTable,
        metadata: &Metadata,
        tables: &ext_php_rs::types::ZendHashTable,
        images: &ext_php_rs::types::ZendHashTable,
        links: &ext_php_rs::types::ZendHashTable,
        footnotes: &ext_php_rs::types::ZendHashTable,
        attributes: &ext_php_rs::types::ZendHashTable,
    ) -> PhpResult<Self> {
        let mut blocks_core_result: Vec<FormattedBlock> = Vec::new();
        for (_, item) in blocks.iter() {
            if let Some(parsed) = <&FormattedBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                blocks_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to FormattedBlock".to_string(),
                ));
            }
        }
        let blocks_core: Vec<FormattedBlock> = blocks_core_result;
        let mut tables_core_result: Vec<Table> = Vec::new();
        for (_, item) in tables.iter() {
            if let Some(parsed) = <&Table as ext_php_rs::convert::FromZval>::from_zval(item) {
                tables_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Table".to_string(),
                ));
            }
        }
        let tables_core: Vec<Table> = tables_core_result;
        let mut images_core_result: Vec<DjotImage> = Vec::new();
        for (_, item) in images.iter() {
            if let Some(parsed) = <&DjotImage as ext_php_rs::convert::FromZval>::from_zval(item) {
                images_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DjotImage".to_string(),
                ));
            }
        }
        let images_core: Vec<DjotImage> = images_core_result;
        let mut links_core_result: Vec<DjotLink> = Vec::new();
        for (_, item) in links.iter() {
            if let Some(parsed) = <&DjotLink as ext_php_rs::convert::FromZval>::from_zval(item) {
                links_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DjotLink".to_string(),
                ));
            }
        }
        let links_core: Vec<DjotLink> = links_core_result;
        let mut footnotes_core_result: Vec<Footnote> = Vec::new();
        for (_, item) in footnotes.iter() {
            if let Some(parsed) = <&Footnote as ext_php_rs::convert::FromZval>::from_zval(item) {
                footnotes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Footnote".to_string(),
                ));
            }
        }
        let footnotes_core: Vec<Footnote> = footnotes_core_result;
        let mut attributes_core_result: Vec<DjotAttributeGroup> = Vec::new();
        for (_, item) in attributes.iter() {
            if let Some(parsed) = <&DjotAttributeGroup as ext_php_rs::convert::FromZval>::from_zval(item) {
                attributes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DjotAttributeGroup".to_string(),
                ));
            }
        }
        let attributes_core: Vec<DjotAttributeGroup> = attributes_core_result;
        Ok(Self {
            plain_text: plainText,
            blocks: blocks_core,
            metadata: metadata.clone(),
            tables: tables_core,
            images: images_core,
            links: links_core,
            footnotes: footnotes_core,
            attributes: attributes_core,
        })
    }

    pub fn get_plain_text(&self) -> String {
        self.plain_text.clone()
    }

    pub fn get_blocks(&self) -> Vec<FormattedBlock> {
        self.blocks.clone()
    }

    pub fn get_metadata(&self) -> Metadata {
        self.metadata.clone()
    }

    pub fn get_tables(&self) -> Vec<Table> {
        self.tables.clone()
    }

    pub fn get_images(&self) -> Vec<DjotImage> {
        self.images.clone()
    }

    pub fn get_links(&self) -> Vec<DjotLink> {
        self.links.clone()
    }

    pub fn get_footnotes(&self) -> Vec<Footnote> {
        self.footnotes.clone()
    }

    pub fn get_attributes(&self) -> Vec<DjotAttributeGroup> {
        self.attributes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DjotImage")]
#[serde(default)]
pub struct DjotImage {
    /// Image source URL or path
    #[php(prop, name = "src")]
    pub src: String,
    /// Alternative text
    #[php(prop, name = "alt")]
    pub alt: String,
    /// Optional title
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Element attributes
    pub attributes: Option<Attributes>,
}

#[php_impl]
impl DjotImage {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(src: String, alt: String, title: Option<String>, attributes: Option<&Attributes>) -> Self {
        Self {
            src,
            alt,
            title,
            attributes: attributes.cloned(),
        }
    }

    pub fn get_src(&self) -> String {
        self.src.clone()
    }

    pub fn get_alt(&self) -> String {
        self.alt.clone()
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_attributes(&self) -> Option<Attributes> {
        self.attributes.clone().map(Into::into)
    }

    pub fn set_attributes(&mut self, value: Option<&Attributes>) {
        self.attributes = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DjotLink")]
#[serde(default)]
pub struct DjotLink {
    /// Link URL
    #[php(prop, name = "url")]
    pub url: String,
    /// Link text content
    #[php(prop, name = "text")]
    pub text: String,
    /// Optional title
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Element attributes
    pub attributes: Option<Attributes>,
}

#[php_impl]
impl DjotLink {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(url: String, text: String, title: Option<String>, attributes: Option<&Attributes>) -> Self {
        Self {
            url,
            text,
            title,
            attributes: attributes.cloned(),
        }
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_attributes(&self) -> Option<Attributes> {
        self.attributes.clone().map(Into::into)
    }

    pub fn set_attributes(&mut self, value: Option<&Attributes>) {
        self.attributes = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DoctorCheck")]
#[serde(default)]
pub struct DoctorCheck {
    /// Check identifier, e.g. `ocr.tesseract` or `layout.rtdetr`.
    #[php(prop, name = "name")]
    pub name: String,
    /// Pass / warn / fail / skip verdict.
    #[php(prop, name = "status")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub status: String,
    /// One-line reason or detail (e.g. missing language, resolved path, error).
    #[php(prop, name = "message")]
    pub message: String,
}

#[php_impl]
impl DoctorCheck {
    #[php(constructor)]
    pub fn new(name: String, status: String, message: String) -> Self {
        Self { name, status, message }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_status(&self) -> String {
        self.status.clone()
    }

    pub fn get_message(&self) -> String {
        self.message.clone()
    }

    /// A `ProbeStatus::Pass` verdict: the checked backend or setting will
    /// work as configured on this host.
    #[php(name = "pass")]
    pub fn pass(name: String, message: String) -> DoctorCheck {
        xberg::DoctorCheck::pass(name, message).into()
    }

    /// A `ProbeStatus::Warn` verdict: the check ran and found something
    /// actionable, but nothing is broken. Never fails the report.
    #[php(name = "warn")]
    pub fn warn(name: String, message: String) -> DoctorCheck {
        xberg::DoctorCheck::warn(name, message).into()
    }

    /// A `ProbeStatus::Fail` verdict: the configured setup will not work (or
    /// will silently degrade) on this host.
    #[php(name = "fail")]
    pub fn fail(name: String, message: String) -> DoctorCheck {
        xberg::DoctorCheck::fail(name, message).into()
    }

    /// A `ProbeStatus::Skip` verdict: the check cannot run locally; first
    /// real use decides, possibly after a download.
    #[php(name = "skip")]
    pub fn skip(name: String, message: String) -> DoctorCheck {
        xberg::DoctorCheck::skip(name, message).into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DoctorReport")]
#[serde(default)]
pub struct DoctorReport {
    /// Individual check verdicts, in execution order.
    pub checks: Vec<DoctorCheck>,
}

impl Default for DoctorReport {
    fn default() -> Self {
        <xberg::DoctorReport as Default>::default().into()
    }
}

#[php_impl]
impl DoctorReport {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(checks: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut checks_core_result: Vec<DoctorCheck> = Vec::new();
        for (_, item) in checks.iter() {
            if let Some(parsed) = <&DoctorCheck as ext_php_rs::convert::FromZval>::from_zval(item) {
                checks_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DoctorCheck".to_string(),
                ));
            }
        }
        let checks_core: Vec<DoctorCheck> = checks_core_result;
        Ok(Self { checks: checks_core })
    }

    pub fn get_checks(&self) -> Vec<DoctorCheck> {
        self.checks.clone()
    }

    pub fn is_ok(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::DoctorReport {
            checks: self.checks.clone().into_iter().map(Into::into).collect(),
            ..Default::default()
        };
        core_self.is_ok()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentBoundary")]
#[serde(default)]
pub struct DocumentBoundary {
    /// 1-indexed start page (inclusive).
    #[php(prop, name = "startPage")]
    #[serde(alias = "startPage")]
    pub start_page: u32,
    /// 1-indexed end page (inclusive).
    #[php(prop, name = "endPage")]
    #[serde(alias = "endPage")]
    pub end_page: u32,
    /// Confidence in this boundary, `[0.0, 1.0]`.
    #[php(prop, name = "confidence")]
    pub confidence: f32,
    /// Reason for the boundary detection.
    #[php(prop, name = "reason")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub reason: String,
}

#[php_impl]
impl DocumentBoundary {
    #[php(constructor)]
    pub fn new(startPage: u32, endPage: u32, confidence: f32, reason: String) -> Self {
        Self {
            start_page: startPage,
            end_page: endPage,
            confidence,
            reason,
        }
    }

    pub fn get_start_page(&self) -> u32 {
        self.start_page.clone()
    }

    pub fn get_end_page(&self) -> u32 {
        self.end_page.clone()
    }

    pub fn get_confidence(&self) -> f32 {
        self.confidence.clone()
    }

    pub fn get_reason(&self) -> String {
        self.reason.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DocumentCounts")]
#[serde(default)]
pub struct DocumentCounts {
    /// Total pages in the source document (`0` when not page-addressable).
    #[php(prop, name = "pages")]
    pub pages: i64,
    /// Tables detected in the document.
    #[php(prop, name = "tables")]
    pub tables: i64,
    /// Images detected in the document.
    #[php(prop, name = "images")]
    pub images: i64,
}

impl Default for DocumentCounts {
    fn default() -> Self {
        <xberg::DocumentCounts as Default>::default().into()
    }
}

#[php_impl]
impl DocumentCounts {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(pages: i64, tables: i64, images: i64) -> Self {
        Self { pages, tables, images }
    }

    pub fn get_pages(&self) -> i64 {
        self.pages.clone()
    }

    pub fn get_tables(&self) -> i64 {
        self.tables.clone()
    }

    pub fn get_images(&self) -> i64 {
        self.images.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentMetadata")]
#[serde(default)]
pub struct DocumentMetadata {
    /// MIME type of the document.
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
    /// File size in bytes.
    #[php(prop, name = "sizeBytes")]
    #[serde(alias = "sizeBytes")]
    pub size_bytes: i64,
    /// Page count (if known, e.g., from previous analysis).
    #[php(prop, name = "pageCount")]
    #[serde(alias = "pageCount")]
    pub page_count: Option<u32>,
    /// Whether OCR is forced regardless of text layer.
    #[php(prop, name = "forceOcr")]
    #[serde(alias = "forceOcr")]
    pub force_ocr: bool,
    /// User-provided chunk configuration overrides.
    #[serde(alias = "userChunkConfig")]
    pub user_chunk_config: Option<UserChunkConfig>,
    /// Whether chunking is enabled for this job.
    #[php(prop, name = "chunkingEnabled")]
    #[serde(alias = "chunkingEnabled")]
    pub chunking_enabled: bool,
}

#[php_impl]
impl DocumentMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        mimeType: String,
        sizeBytes: i64,
        forceOcr: bool,
        chunkingEnabled: bool,
        pageCount: Option<u32>,
        userChunkConfig: Option<&UserChunkConfig>,
    ) -> Self {
        Self {
            mime_type: mimeType,
            size_bytes: sizeBytes,
            page_count: pageCount,
            force_ocr: forceOcr,
            user_chunk_config: userChunkConfig.cloned(),
            chunking_enabled: chunkingEnabled,
        }
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }

    pub fn get_size_bytes(&self) -> i64 {
        self.size_bytes.clone()
    }

    pub fn get_page_count(&self) -> Option<u32> {
        self.page_count.clone()
    }

    pub fn get_force_ocr(&self) -> bool {
        self.force_ocr.clone()
    }

    pub fn get_user_chunk_config(&self) -> Option<UserChunkConfig> {
        self.user_chunk_config.clone().map(Into::into)
    }

    pub fn set_user_chunk_config(&mut self, value: Option<&UserChunkConfig>) {
        self.user_chunk_config = value.map(|value| value.clone().into());
    }

    pub fn get_chunking_enabled(&self) -> bool {
        self.chunking_enabled.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentNode")]
#[serde(default)]
pub struct DocumentNode {
    /// Deterministic identifier (hash of node type + text + page + position).
    ///
    /// Stable and unique within a single extraction response: every internal
    /// construction path threads the node's position (its index in
    /// `DocumentStructure.nodes`) into the hash, so identical
    /// `(node_type, text, page)` tuples at different positions never collide.
    /// Always serialised — `ChunkMetadata.node_ids` references it to join
    /// chunks back to the nodes they were derived from.
    /// `#[serde(default)]` covers the missing-field case on inbound JSON
    /// (e.g. documents serialised before this field existed).
    #[php(prop, name = "id")]
    #[serde(default)]
    pub id: String,
    /// Node content — tagged enum, type-specific data only.
    pub content: NodeContent,
    /// Parent node index (`None` = root-level node).
    #[php(prop, name = "parent")]
    pub parent: Option<u32>,
    /// Child node indices in reading order.
    #[php(prop, name = "children")]
    #[serde(default)]
    pub children: Vec<u32>,
    /// Content layer classification.
    ///
    /// Always serialised — Kotlin-Android (and any other typed binding) treats
    /// the field as non-nullable, so omitting it from the JSON wire would
    /// break consumer deserialisation.  `#[serde(default)]` covers the
    /// missing-field case on inbound JSON.
    #[php(prop, name = "contentLayer")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "contentLayer")]
    #[serde(default)]
    pub content_layer: String,
    /// Page number where this node starts (1-indexed).
    #[php(prop, name = "page")]
    pub page: Option<u32>,
    /// Page number where this node ends (for multi-page tables/sections).
    #[php(prop, name = "pageEnd")]
    #[serde(alias = "pageEnd")]
    pub page_end: Option<u32>,
    /// Bounding box in document coordinates.
    pub bbox: Option<BoundingBox>,
    /// Inline annotations (formatting, links) on this node's text content.
    ///
    /// Only meaningful for text-carrying nodes; empty for containers.
    #[serde(default)]
    pub annotations: Vec<TextAnnotation>,
    /// Format-specific key-value attributes.
    ///
    /// Extensible bag for miscellaneous data without a dedicated typed field: CSS classes,
    /// LaTeX environment names, Excel cell formulas, slide layout names, etc.
    #[php(prop, name = "attributes")]
    pub attributes: Option<HashMap<String, String>>,
}

#[php_impl]
impl DocumentNode {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        id: String,
        content: &NodeContent,
        children: Vec<u32>,
        contentLayer: String,
        annotations: &ext_php_rs::types::ZendHashTable,
        parent: Option<u32>,
        page: Option<u32>,
        pageEnd: Option<u32>,
        bbox: Option<&BoundingBox>,
        attributes: Option<HashMap<String, String>>,
    ) -> PhpResult<Self> {
        let mut annotations_core_result: Vec<TextAnnotation> = Vec::new();
        for (_, item) in annotations.iter() {
            if let Some(parsed) = <&TextAnnotation as ext_php_rs::convert::FromZval>::from_zval(item) {
                annotations_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to TextAnnotation".to_string(),
                ));
            }
        }
        let annotations_core: Vec<TextAnnotation> = annotations_core_result;
        Ok(Self {
            id,
            content: content.clone(),
            parent,
            children,
            content_layer: contentLayer,
            page,
            page_end: pageEnd,
            bbox: bbox.cloned(),
            annotations: annotations_core,
            attributes,
        })
    }

    pub fn get_id(&self) -> String {
        self.id.clone()
    }

    pub fn get_content(&self) -> NodeContent {
        self.content.clone()
    }

    pub fn get_parent(&self) -> Option<u32> {
        self.parent.clone()
    }

    pub fn get_children(&self) -> Vec<u32> {
        self.children.clone()
    }

    pub fn get_content_layer(&self) -> String {
        self.content_layer.clone()
    }

    pub fn get_page(&self) -> Option<u32> {
        self.page.clone()
    }

    pub fn get_page_end(&self) -> Option<u32> {
        self.page_end.clone()
    }

    pub fn get_bbox(&self) -> Option<BoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&BoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }

    pub fn get_annotations(&self) -> Vec<TextAnnotation> {
        self.annotations.clone()
    }

    pub fn get_attributes(&self) -> Option<HashMap<String, String>> {
        self.attributes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentRelationship")]
#[serde(default)]
pub struct DocumentRelationship {
    /// Source node index (the referencing node).
    #[php(prop, name = "source")]
    pub source: u32,
    /// Target node index (the referenced node).
    #[php(prop, name = "target")]
    pub target: u32,
    /// Semantic kind of the relationship.
    #[php(prop, name = "kind")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub kind: String,
}

#[php_impl]
impl DocumentRelationship {
    #[php(constructor)]
    pub fn new(source: u32, target: u32, kind: String) -> Self {
        Self { source, target, kind }
    }

    pub fn get_source(&self) -> u32 {
        self.source.clone()
    }

    pub fn get_target(&self) -> u32 {
        self.target.clone()
    }

    pub fn get_kind(&self) -> String {
        self.kind.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentRevision")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct DocumentRevision {
    /// Format-specific revision identifier.
    ///
    /// For DOCX this is the `w:id` attribute value on the change element
    /// (e.g. `"42"`). When the attribute is absent a synthetic fallback is
    /// generated (`"docx-ins-0"`, `"docx-del-3"`, …).
    #[php(prop, name = "revisionId")]
    #[serde(alias = "revisionId")]
    pub revision_id: String,
    /// Display name of the author who made this change, when available.
    #[php(prop, name = "author")]
    pub author: Option<String>,
    /// ISO-8601 timestamp of the change, when available.
    ///
    /// Stored as a plain string so this type remains FFI-friendly and
    /// unconditionally available without the `chrono` optional dep.
    /// DOCX populates this from the `w:date` attribute (e.g.
    /// `"2024-03-15T10:30:00Z"`).
    #[php(prop, name = "timestamp")]
    pub timestamp: Option<String>,
    /// Semantic kind of this revision.
    #[php(prop, name = "kind")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub kind: String,
    /// Best-effort document location for this revision.
    ///
    /// Resolution is format-dependent and may be `None` when the location
    /// cannot be determined (e.g. changes inside table cells before
    /// table-cell anchor support is added).
    pub anchor: Option<RevisionAnchor>,
    /// The content changes that make up this revision.
    pub delta: RevisionDelta,
}

#[php_impl]
impl DocumentRevision {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        revisionId: String,
        kind: String,
        delta: &RevisionDelta,
        author: Option<String>,
        timestamp: Option<String>,
        anchor: Option<&RevisionAnchor>,
    ) -> Self {
        Self {
            revision_id: revisionId,
            author,
            timestamp,
            kind,
            anchor: anchor.cloned(),
            delta: delta.clone(),
        }
    }

    pub fn get_revision_id(&self) -> String {
        self.revision_id.clone()
    }

    pub fn get_author(&self) -> Option<String> {
        self.author.clone()
    }

    pub fn get_timestamp(&self) -> Option<String> {
        self.timestamp.clone()
    }

    pub fn get_kind(&self) -> String {
        self.kind.clone()
    }

    pub fn get_anchor(&self) -> Option<RevisionAnchor> {
        self.anchor.clone().map(Into::into)
    }

    pub fn set_anchor(&mut self, value: Option<&RevisionAnchor>) {
        self.anchor = value.map(|value| value.clone().into());
    }

    pub fn get_delta(&self) -> RevisionDelta {
        self.delta.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DocumentStructure")]
#[serde(default)]
pub struct DocumentStructure {
    /// All nodes in document/reading order.
    pub nodes: Vec<DocumentNode>,
    /// Origin format identifier (e.g. "docx", "pptx", "html", "pdf").
    ///
    /// Allows renderers to apply format-aware heuristics when converting
    /// the document tree to output formats.
    #[php(prop, name = "sourceFormat")]
    #[serde(alias = "sourceFormat")]
    pub source_format: Option<String>,
    /// Resolved relationships between nodes (footnote refs, citations, anchor links, etc.).
    ///
    /// Populated during derivation from the internal document representation.
    /// Empty when no relationships are detected.
    #[serde(default)]
    pub relationships: Vec<DocumentRelationship>,
    /// Sorted, deduplicated list of node type names present in this document.
    ///
    /// Each value is the snake_case `node_type` tag of the corresponding
    /// `NodeContent` variant (e.g. `"paragraph"`, `"heading"`, `"table"`, …).
    ///
    /// Computed from `nodes` via `DocumentStructure.finalize_node_types`.
    /// Empty until that method is called (internal construction paths call it
    /// at the end of derivation).
    #[php(prop, name = "nodeTypes")]
    #[serde(alias = "nodeTypes")]
    #[serde(default)]
    pub node_types: Vec<String>,
}

impl Default for DocumentStructure {
    fn default() -> Self {
        <xberg::DocumentStructure as Default>::default().into()
    }
}

#[php_impl]
impl DocumentStructure {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        nodes: &ext_php_rs::types::ZendHashTable,
        relationships: &ext_php_rs::types::ZendHashTable,
        nodeTypes: Vec<String>,
        sourceFormat: Option<String>,
    ) -> PhpResult<Self> {
        let mut nodes_core_result: Vec<DocumentNode> = Vec::new();
        for (_, item) in nodes.iter() {
            if let Some(parsed) = <&DocumentNode as ext_php_rs::convert::FromZval>::from_zval(item) {
                nodes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DocumentNode".to_string(),
                ));
            }
        }
        let nodes_core: Vec<DocumentNode> = nodes_core_result;
        let mut relationships_core_result: Vec<DocumentRelationship> = Vec::new();
        for (_, item) in relationships.iter() {
            if let Some(parsed) = <&DocumentRelationship as ext_php_rs::convert::FromZval>::from_zval(item) {
                relationships_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DocumentRelationship".to_string(),
                ));
            }
        }
        let relationships_core: Vec<DocumentRelationship> = relationships_core_result;
        Ok(Self {
            nodes: nodes_core,
            source_format: sourceFormat,
            relationships: relationships_core,
            node_types: nodeTypes,
        })
    }

    pub fn get_nodes(&self) -> Vec<DocumentNode> {
        self.nodes.clone()
    }

    pub fn get_source_format(&self) -> Option<String> {
        self.source_format.clone()
    }

    pub fn get_relationships(&self) -> Vec<DocumentRelationship> {
        self.relationships.clone()
    }

    pub fn get_node_types(&self) -> Vec<String> {
        self.node_types.clone()
    }

    pub fn is_empty(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::DocumentStructure {
            nodes: self.nodes.clone().into_iter().map(Into::into).collect(),
            source_format: self.source_format.clone(),
            relationships: self.relationships.clone().into_iter().map(Into::into).collect(),
            node_types: self.node_types.clone(),
            ..Default::default()
        };
        core_self.is_empty()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> DocumentStructure {
        xberg::DocumentStructure::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\DocumentSummary")]
#[serde(default)]
pub struct DocumentSummary {
    /// Summary text (plain prose).
    #[php(prop, name = "text")]
    pub text: String,
    /// Strategy that produced this summary.
    #[php(prop, name = "strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub strategy: String,
    /// Approximate token count of the summary, when known.
    #[php(prop, name = "tokenCount")]
    #[serde(alias = "tokenCount")]
    pub token_count: Option<u32>,
}

#[php_impl]
impl DocumentSummary {
    #[php(constructor)]
    pub fn new(text: String, strategy: String, tokenCount: Option<u32>) -> Self {
        Self {
            text,
            strategy,
            token_count: tokenCount,
        }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_strategy(&self) -> String {
        self.strategy.clone()
    }

    pub fn get_token_count(&self) -> Option<u32> {
        self.token_count.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DocxAppProperties")]
#[serde(default)]
pub struct DocxAppProperties {
    /// Application name (e.g., "Microsoft Office Word")
    #[php(prop, name = "application")]
    pub application: Option<String>,
    /// Application version
    #[php(prop, name = "appVersion")]
    #[serde(alias = "appVersion")]
    pub app_version: Option<String>,
    /// Template filename
    #[php(prop, name = "template")]
    pub template: Option<String>,
    /// Total editing time in minutes
    #[php(prop, name = "totalTime")]
    #[serde(alias = "totalTime")]
    pub total_time: Option<i32>,
    /// Number of pages
    #[php(prop, name = "pages")]
    pub pages: Option<i32>,
    /// Number of words
    #[php(prop, name = "words")]
    pub words: Option<i32>,
    /// Number of characters (excluding spaces)
    #[php(prop, name = "characters")]
    pub characters: Option<i32>,
    /// Number of characters (including spaces)
    #[php(prop, name = "charactersWithSpaces")]
    #[serde(alias = "charactersWithSpaces")]
    pub characters_with_spaces: Option<i32>,
    /// Number of lines
    #[php(prop, name = "lines")]
    pub lines: Option<i32>,
    /// Number of paragraphs
    #[php(prop, name = "paragraphs")]
    pub paragraphs: Option<i32>,
    /// Company name
    #[php(prop, name = "company")]
    pub company: Option<String>,
    /// Document security level
    #[php(prop, name = "docSecurity")]
    #[serde(alias = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[php(prop, name = "scaleCrop")]
    #[serde(alias = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[php(prop, name = "linksUpToDate")]
    #[serde(alias = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[php(prop, name = "sharedDoc")]
    #[serde(alias = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[php(prop, name = "hyperlinksChanged")]
    #[serde(alias = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
}

impl Default for DocxAppProperties {
    fn default() -> Self {
        <xberg::DocxAppProperties as Default>::default().into()
    }
}

#[php_impl]
impl DocxAppProperties {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_application(&self) -> Option<String> {
        self.application.clone()
    }

    pub fn get_app_version(&self) -> Option<String> {
        self.app_version.clone()
    }

    pub fn get_template(&self) -> Option<String> {
        self.template.clone()
    }

    pub fn get_total_time(&self) -> Option<i32> {
        self.total_time.clone()
    }

    pub fn get_pages(&self) -> Option<i32> {
        self.pages.clone()
    }

    pub fn get_words(&self) -> Option<i32> {
        self.words.clone()
    }

    pub fn get_characters(&self) -> Option<i32> {
        self.characters.clone()
    }

    pub fn get_characters_with_spaces(&self) -> Option<i32> {
        self.characters_with_spaces.clone()
    }

    pub fn get_lines(&self) -> Option<i32> {
        self.lines.clone()
    }

    pub fn get_paragraphs(&self) -> Option<i32> {
        self.paragraphs.clone()
    }

    pub fn get_company(&self) -> Option<String> {
        self.company.clone()
    }

    pub fn get_doc_security(&self) -> Option<i32> {
        self.doc_security.clone()
    }

    pub fn get_scale_crop(&self) -> Option<bool> {
        self.scale_crop.clone()
    }

    pub fn get_links_up_to_date(&self) -> Option<bool> {
        self.links_up_to_date.clone()
    }

    pub fn get_shared_doc(&self) -> Option<bool> {
        self.shared_doc.clone()
    }

    pub fn get_hyperlinks_changed(&self) -> Option<bool> {
        self.hyperlinks_changed.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\DocxMetadata")]
#[serde(default)]
pub struct DocxMetadata {
    /// Core properties from docProps/core.xml (Dublin Core metadata)
    ///
    /// Contains title, creator, subject, keywords, dates, etc.
    /// Shared format across DOCX/PPTX/XLSX documents.
    #[serde(alias = "coreProperties")]
    pub core_properties: Option<CoreProperties>,
    /// Application properties from docProps/app.xml (Word-specific statistics)
    ///
    /// Contains word count, page count, paragraph count, editing time, etc.
    /// DOCX-specific variant of Office application properties.
    #[serde(alias = "appProperties")]
    pub app_properties: Option<DocxAppProperties>,
    /// Custom properties from docProps/custom.xml (user-defined properties)
    ///
    /// Contains key-value pairs defined by users or applications.
    /// Values can be strings, numbers, booleans, or dates.
    #[doc = "PHP: initialize this field through fromJson() and read it via get_custom_properties(); its value type cannot be
exposed as a directly settable ext-php-rs property."]
    #[serde(alias = "customProperties")]
    pub custom_properties: Option<HashMap<String, serde_json::Value>>,
}

impl Default for DocxMetadata {
    fn default() -> Self {
        <xberg::DocxMetadata as Default>::default().into()
    }
}

#[php_impl]
impl DocxMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_core_properties(&self) -> Option<CoreProperties> {
        self.core_properties.clone().map(Into::into)
    }

    pub fn set_core_properties(&mut self, value: Option<&CoreProperties>) {
        self.core_properties = value.map(|value| value.clone().into());
    }

    pub fn get_app_properties(&self) -> Option<DocxAppProperties> {
        self.app_properties.clone().map(Into::into)
    }

    pub fn set_app_properties(&mut self, value: Option<&DocxAppProperties>) {
        self.app_properties = value.map(|value| value.clone().into());
    }

    pub fn get_custom_properties(&self) -> Option<String> {
        self.custom_properties
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Element")]
#[serde(default)]
pub struct Element {
    /// Deterministic element identifier. Empty only when deserializing legacy payloads
    /// that predate this field's wire representation.
    #[php(prop, name = "elementId")]
    #[serde(alias = "elementId")]
    #[serde(default)]
    pub element_id: String,
    /// Semantic type of this element
    #[php(prop, name = "elementType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "elementType")]
    pub element_type: String,
    /// Text content of the element
    #[php(prop, name = "text")]
    pub text: String,
    /// Metadata about the element
    pub metadata: ElementMetadata,
}

#[php_impl]
impl Element {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(elementId: String, elementType: String, text: String, metadata: &ElementMetadata) -> Self {
        Self {
            element_id: elementId,
            element_type: elementType,
            text,
            metadata: metadata.clone(),
        }
    }

    pub fn get_element_id(&self) -> String {
        self.element_id.clone()
    }

    pub fn get_element_type(&self) -> String {
        self.element_type.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_metadata(&self) -> ElementMetadata {
        self.metadata.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ElementMetadata")]
#[serde(default)]
pub struct ElementMetadata {
    /// Page number (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: Option<u32>,
    /// Source filename or document name
    #[php(prop, name = "filename")]
    pub filename: Option<String>,
    /// Bounding box coordinates if available
    pub coordinates: Option<BoundingBox>,
    /// Position index in the element sequence
    #[php(prop, name = "elementIndex")]
    #[serde(alias = "elementIndex")]
    pub element_index: Option<i64>,
    /// Additional custom metadata
    #[php(prop, name = "additional")]
    pub additional: HashMap<String, String>,
}

#[php_impl]
impl ElementMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        additional: HashMap<String, String>,
        pageNumber: Option<u32>,
        filename: Option<String>,
        coordinates: Option<&BoundingBox>,
        elementIndex: Option<i64>,
    ) -> Self {
        Self {
            page_number: pageNumber,
            filename,
            coordinates: coordinates.cloned(),
            element_index: elementIndex,
            additional,
        }
    }

    pub fn get_page_number(&self) -> Option<u32> {
        self.page_number.clone()
    }

    pub fn get_filename(&self) -> Option<String> {
        self.filename.clone()
    }

    pub fn get_coordinates(&self) -> Option<BoundingBox> {
        self.coordinates.clone().map(Into::into)
    }

    pub fn set_coordinates(&mut self, value: Option<&BoundingBox>) {
        self.coordinates = value.map(|value| value.clone().into());
    }

    pub fn get_element_index(&self) -> Option<i64> {
        self.element_index.clone()
    }

    pub fn get_additional(&self) -> HashMap<String, String> {
        self.additional.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\EmailAttachment")]
#[serde(default)]
pub struct EmailAttachment {
    /// Attachment name (from Content-Disposition header)
    #[php(prop, name = "name")]
    pub name: Option<String>,
    /// Filename of the attachment
    #[php(prop, name = "filename")]
    pub filename: Option<String>,
    /// MIME type of the attachment
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: Option<String>,
    /// Size in bytes
    #[php(prop, name = "size")]
    pub size: Option<i64>,
    /// Whether this attachment is an image
    #[php(prop, name = "isImage")]
    #[serde(alias = "isImage")]
    pub is_image: bool,
    /// Attachment data (if extracted).
    /// Uses `bytes.Bytes` for cheap cloning of large buffers.
    pub data: Option<Vec<u8>>,
}

#[php_impl]
impl EmailAttachment {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        isImage: bool,
        name: Option<String>,
        filename: Option<String>,
        mimeType: Option<String>,
        size: Option<i64>,
        data: Option<PhpBytes>,
    ) -> Self {
        Self {
            name,
            filename,
            mime_type: mimeType,
            size,
            is_image: isImage,
            data: data.map(|b| b.0),
        }
    }

    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    pub fn get_filename(&self) -> Option<String> {
        self.filename.clone()
    }

    pub fn get_mime_type(&self) -> Option<String> {
        self.mime_type.clone()
    }

    pub fn get_size(&self) -> Option<i64> {
        self.size.clone()
    }

    pub fn get_is_image(&self) -> bool {
        self.is_image.clone()
    }

    pub fn get_data(&self) -> Option<Vec<u8>> {
        self.data.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\EmailConfig")]
#[serde(default)]
pub struct EmailConfig {
    /// Windows codepage number to use when an MSG file contains no codepage property.
    /// Defaults to `None`, which falls back to windows-1252.
    ///
    /// If an unrecognized or invalid codepage number is supplied (including 0),
    /// the behavior silently falls back to windows-1252 — the same as when the
    /// MSG file itself contains an unrecognized codepage. No error or warning is
    /// emitted. Users should verify output when supplying unusual values.
    ///
    /// Common values:
    /// - 1250: Central European (Polish, Czech, Hungarian, etc.)
    /// - 1251: Cyrillic (Russian, Ukrainian, Bulgarian, etc.)
    /// - 1252: Western European (default)
    /// - 1253: Greek
    /// - 1254: Turkish
    /// - 1255: Hebrew
    /// - 1256: Arabic
    /// - 932:  Japanese (Shift-JIS)
    /// - 936:  Simplified Chinese (GBK)
    #[php(prop, name = "msgFallbackCodepage")]
    #[serde(alias = "msgFallbackCodepage")]
    pub msg_fallback_codepage: Option<u32>,
}

impl Default for EmailConfig {
    fn default() -> Self {
        <xberg::EmailConfig as Default>::default().into()
    }
}

#[php_impl]
impl EmailConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_msg_fallback_codepage(&self) -> Option<u32> {
        self.msg_fallback_codepage.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\EmailExtractionResult")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct EmailExtractionResult {
    /// Email subject line
    #[php(prop, name = "subject")]
    pub subject: Option<String>,
    /// Sender email address
    #[php(prop, name = "fromEmail")]
    #[serde(alias = "fromEmail")]
    pub from_email: Option<String>,
    /// Primary recipient email addresses
    #[php(prop, name = "toEmails")]
    #[serde(alias = "toEmails")]
    pub to_emails: Vec<String>,
    /// CC recipient email addresses
    #[php(prop, name = "ccEmails")]
    #[serde(alias = "ccEmails")]
    pub cc_emails: Vec<String>,
    /// BCC recipient email addresses
    #[php(prop, name = "bccEmails")]
    #[serde(alias = "bccEmails")]
    pub bcc_emails: Vec<String>,
    /// Email date/timestamp
    #[php(prop, name = "date")]
    pub date: Option<String>,
    /// Message-ID header value
    #[php(prop, name = "messageId")]
    #[serde(alias = "messageId")]
    pub message_id: Option<String>,
    /// Plain text version of the email body
    #[php(prop, name = "plainText")]
    #[serde(alias = "plainText")]
    pub plain_text: Option<String>,
    /// HTML version of the email body
    #[php(prop, name = "htmlContent")]
    #[serde(alias = "htmlContent")]
    pub html_content: Option<String>,
    /// Cleaned/processed text content. Aliased as `cleaned_text` for back-compat.
    #[php(prop, name = "content")]
    pub content: String,
    /// List of email attachments
    pub attachments: Vec<EmailAttachment>,
    /// Additional email headers and metadata
    #[php(prop, name = "metadata")]
    pub metadata: HashMap<String, String>,
}

#[php_impl]
impl EmailExtractionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        toEmails: Vec<String>,
        ccEmails: Vec<String>,
        bccEmails: Vec<String>,
        content: String,
        attachments: &ext_php_rs::types::ZendHashTable,
        metadata: HashMap<String, String>,
        subject: Option<String>,
        fromEmail: Option<String>,
        date: Option<String>,
        messageId: Option<String>,
        plainText: Option<String>,
        htmlContent: Option<String>,
    ) -> PhpResult<Self> {
        let mut attachments_core_result: Vec<EmailAttachment> = Vec::new();
        for (_, item) in attachments.iter() {
            if let Some(parsed) = <&EmailAttachment as ext_php_rs::convert::FromZval>::from_zval(item) {
                attachments_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to EmailAttachment".to_string(),
                ));
            }
        }
        let attachments_core: Vec<EmailAttachment> = attachments_core_result;
        Ok(Self {
            subject,
            from_email: fromEmail,
            to_emails: toEmails,
            cc_emails: ccEmails,
            bcc_emails: bccEmails,
            date,
            message_id: messageId,
            plain_text: plainText,
            html_content: htmlContent,
            content,
            attachments: attachments_core,
            metadata,
        })
    }

    pub fn get_subject(&self) -> Option<String> {
        self.subject.clone()
    }

    pub fn get_from_email(&self) -> Option<String> {
        self.from_email.clone()
    }

    pub fn get_to_emails(&self) -> Vec<String> {
        self.to_emails.clone()
    }

    pub fn get_cc_emails(&self) -> Vec<String> {
        self.cc_emails.clone()
    }

    pub fn get_bcc_emails(&self) -> Vec<String> {
        self.bcc_emails.clone()
    }

    pub fn get_date(&self) -> Option<String> {
        self.date.clone()
    }

    pub fn get_message_id(&self) -> Option<String> {
        self.message_id.clone()
    }

    pub fn get_plain_text(&self) -> Option<String> {
        self.plain_text.clone()
    }

    pub fn get_html_content(&self) -> Option<String> {
        self.html_content.clone()
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_attachments(&self) -> Vec<EmailAttachment> {
        self.attachments.clone()
    }

    pub fn get_metadata(&self) -> HashMap<String, String> {
        self.metadata.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\EmailMetadata")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct EmailMetadata {
    /// Sender's email address
    #[php(prop, name = "fromEmail")]
    #[serde(alias = "fromEmail")]
    pub from_email: Option<String>,
    /// Sender's display name
    #[php(prop, name = "fromName")]
    #[serde(alias = "fromName")]
    pub from_name: Option<String>,
    /// Primary recipients
    #[php(prop, name = "toEmails")]
    #[serde(alias = "toEmails")]
    pub to_emails: Vec<String>,
    /// CC recipients
    #[php(prop, name = "ccEmails")]
    #[serde(alias = "ccEmails")]
    pub cc_emails: Vec<String>,
    /// BCC recipients
    #[php(prop, name = "bccEmails")]
    #[serde(alias = "bccEmails")]
    pub bcc_emails: Vec<String>,
    /// Message-ID header value
    #[php(prop, name = "messageId")]
    #[serde(alias = "messageId")]
    pub message_id: Option<String>,
    /// List of attachment filenames
    #[php(prop, name = "attachments")]
    pub attachments: Vec<String>,
}

impl Default for EmailMetadata {
    fn default() -> Self {
        <xberg::EmailMetadata as Default>::default().into()
    }
}

#[php_impl]
impl EmailMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        toEmails: Vec<String>,
        ccEmails: Vec<String>,
        bccEmails: Vec<String>,
        attachments: Vec<String>,
        fromEmail: Option<String>,
        fromName: Option<String>,
        messageId: Option<String>,
    ) -> Self {
        Self {
            from_email: fromEmail,
            from_name: fromName,
            to_emails: toEmails,
            cc_emails: ccEmails,
            bcc_emails: bccEmails,
            message_id: messageId,
            attachments,
        }
    }

    pub fn get_from_email(&self) -> Option<String> {
        self.from_email.clone()
    }

    pub fn get_from_name(&self) -> Option<String> {
        self.from_name.clone()
    }

    pub fn get_to_emails(&self) -> Vec<String> {
        self.to_emails.clone()
    }

    pub fn get_cc_emails(&self) -> Vec<String> {
        self.cc_emails.clone()
    }

    pub fn get_bcc_emails(&self) -> Vec<String> {
        self.bcc_emails.clone()
    }

    pub fn get_message_id(&self) -> Option<String> {
        self.message_id.clone()
    }

    pub fn get_attachments(&self) -> Vec<String> {
        self.attachments.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\EmbeddedChanges")]
#[serde(default)]
pub struct EmbeddedChanges {
    /// Children present in `b` but not in `a` (matched by `path`).
    pub added: Vec<ArchiveEntry>,
    /// Children present in `a` but not in `b` (matched by `path`).
    pub removed: Vec<ArchiveEntry>,
    /// Children present in both but with differing content (matched by `path`).
    ///
    /// Each entry holds the diff of the nested `ExtractedDocument`.
    pub changed: Vec<EmbeddedDiff>,
}

impl Default for EmbeddedChanges {
    fn default() -> Self {
        <xberg::EmbeddedChanges as Default>::default().into()
    }
}

#[php_impl]
impl EmbeddedChanges {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        added: &ext_php_rs::types::ZendHashTable,
        removed: &ext_php_rs::types::ZendHashTable,
        changed: &ext_php_rs::types::ZendHashTable,
    ) -> PhpResult<Self> {
        let mut added_core_result: Vec<ArchiveEntry> = Vec::new();
        for (_, item) in added.iter() {
            if let Some(parsed) = <&ArchiveEntry as ext_php_rs::convert::FromZval>::from_zval(item) {
                added_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ArchiveEntry".to_string(),
                ));
            }
        }
        let added_core: Vec<ArchiveEntry> = added_core_result;
        let mut removed_core_result: Vec<ArchiveEntry> = Vec::new();
        for (_, item) in removed.iter() {
            if let Some(parsed) = <&ArchiveEntry as ext_php_rs::convert::FromZval>::from_zval(item) {
                removed_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ArchiveEntry".to_string(),
                ));
            }
        }
        let removed_core: Vec<ArchiveEntry> = removed_core_result;
        let mut changed_core_result: Vec<EmbeddedDiff> = Vec::new();
        for (_, item) in changed.iter() {
            if let Some(parsed) = <&EmbeddedDiff as ext_php_rs::convert::FromZval>::from_zval(item) {
                changed_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to EmbeddedDiff".to_string(),
                ));
            }
        }
        let changed_core: Vec<EmbeddedDiff> = changed_core_result;
        Ok(Self {
            added: added_core,
            removed: removed_core,
            changed: changed_core,
        })
    }

    pub fn get_added(&self) -> Vec<ArchiveEntry> {
        self.added.clone()
    }

    pub fn get_removed(&self) -> Vec<ArchiveEntry> {
        self.removed.clone()
    }

    pub fn get_changed(&self) -> Vec<EmbeddedDiff> {
        self.changed.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\EmbeddedDiff")]
#[serde(default)]
pub struct EmbeddedDiff {
    /// Archive-relative path identifying this entry.
    #[php(prop, name = "path")]
    pub path: String,
    /// The recursive diff of the entry's extraction result.
    pub diff: ExtractionDiff,
}

#[php_impl]
impl EmbeddedDiff {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(path: String, diff: &ExtractionDiff) -> Self {
        Self {
            path,
            diff: diff.clone(),
        }
    }

    pub fn get_path(&self) -> String {
        self.path.clone()
    }

    pub fn get_diff(&self) -> ExtractionDiff {
        self.diff.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\EmbeddedFile")]
#[serde(default)]
pub struct EmbeddedFile {
    /// The filename as stored in the PDF name tree.
    #[php(prop, name = "name")]
    pub name: String,
    /// Raw file bytes from the embedded stream (already decompressed by lopdf).
    pub data: Vec<u8>,
    /// Compressed byte count of the original stream (before decompression).
    ///
    /// Used by callers to compute the decompression ratio and detect zip-bomb-style
    /// attacks that embed a tiny compressed stream expanding to gigabytes of data.
    #[php(prop, name = "compressedSize")]
    #[serde(alias = "compressedSize")]
    pub compressed_size: i64,
    /// MIME type if specified in the filespec, otherwise `None`.
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: Option<String>,
}

#[php_impl]
impl EmbeddedFile {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(name: String, data: PhpBytes, compressedSize: i64, mimeType: Option<String>) -> Self {
        Self {
            name,
            data: data.0,
            compressed_size: compressedSize,
            mime_type: mimeType,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_data(&self) -> Vec<u8> {
        self.data.clone()
    }

    pub fn get_compressed_size(&self) -> i64 {
        self.compressed_size.clone()
    }

    pub fn get_mime_type(&self) -> Option<String> {
        self.mime_type.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\EmbeddingConfig")]
#[serde(default)]
pub struct EmbeddingConfig {
    /// The embedding model to use (defaults to "gte-modernbert-base" preset if not specified)
    pub model: EmbeddingModelType,
    /// Whether to normalize embedding vectors (recommended for cosine similarity)
    #[php(prop, name = "normalize")]
    #[serde(default = "crate::serde_defaults::embedding_config_normalize")]
    pub normalize: bool,
    /// Batch size for embedding generation
    #[php(prop, name = "batchSize")]
    #[serde(default = "crate::serde_defaults::embedding_config_batch_size")]
    #[serde(alias = "batchSize")]
    pub batch_size: i64,
    /// Show model download progress.
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). Covers both
    /// local backends (ONNX and static/model2vec). A warm Hugging Face cache transfers nothing and
    /// so reports nothing. Ignored by `EmbeddingModelType.Llm` and
    /// `EmbeddingModelType.Plugin`, which download no model.
    #[php(prop, name = "showDownloadProgress")]
    #[serde(default = "crate::serde_defaults::embedding_config_show_download_progress")]
    #[serde(alias = "showDownloadProgress")]
    pub show_download_progress: bool,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows `HF_HUB_CACHE`, `HUGGINGFACE_HUB_CACHE`,
    /// `HF_HOME`, XDG, and platform defaults. Prefer those environment variables
    /// when configuring the cache process-wide.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the embedding ONNX model.
    ///
    /// When set, controls which execution provider (CPU, CUDA, CoreML, TensorRT)
    /// is used for inference. Defaults to `None` (auto-select per platform).
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single `embed()` call when
    /// using `EmbeddingModelType.Plugin`.
    ///
    /// Applies only to the in-process plugin path — protects against hung
    /// host-language backends (e.g. a Python callback deadlocked on the GIL,
    /// a model stuck on CUDA OOM retries, etc.). On timeout, the dispatcher
    /// returns `Plugin` instead of blocking forever.
    ///
    /// `None` disables the timeout. The default (60 seconds) is conservative
    /// for common in-process inference; increase for large batches on slow
    /// hardware.
    #[php(prop, name = "maxEmbedDurationSecs")]
    #[serde(alias = "maxEmbedDurationSecs")]
    #[serde(default)]
    pub max_embed_duration_secs: Option<i64>,
    /// Maximum number of tokens fed to the tokenizer before truncation when
    /// embedding a chunk with a local ONNX model (Preset/Custom).
    ///
    /// A chunk longer than this many tokens has its tail dropped before
    /// inference, so only the prefix contributes to the stored vector. `None`
    /// falls back to 512 (the historical default). The effective value is
    /// always capped at the model's own `model_max_length`, so raising it past
    /// what the model supports has no effect — set it to match a long-context
    /// model (e.g. 8192 for Jina/Nomic) so long chunks embed in full.
    ///
    /// Ignored by the `Llm` and `Plugin` model types, which own their own
    /// tokenization.
    #[php(prop, name = "maxSequenceLength")]
    #[serde(alias = "maxSequenceLength")]
    #[serde(default)]
    pub max_sequence_length: Option<i64>,
}

impl Default for EmbeddingConfig {
    fn default() -> Self {
        <xberg::EmbeddingConfig as Default>::default().into()
    }
}

#[php_impl]
impl EmbeddingConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: &EmbeddingModelType,
        normalize: bool,
        batchSize: i64,
        showDownloadProgress: bool,
        cacheDir: Option<String>,
        acceleration: Option<&AccelerationConfig>,
        maxEmbedDurationSecs: Option<i64>,
        maxSequenceLength: Option<i64>,
    ) -> Self {
        Self {
            model: model.clone(),
            normalize,
            batch_size: batchSize,
            show_download_progress: showDownloadProgress,
            cache_dir: cacheDir,
            acceleration: acceleration.cloned(),
            max_embed_duration_secs: maxEmbedDurationSecs,
            max_sequence_length: maxSequenceLength,
        }
    }

    pub fn get_model(&self) -> EmbeddingModelType {
        self.model.clone()
    }

    pub fn get_normalize(&self) -> bool {
        self.normalize.clone()
    }

    pub fn get_batch_size(&self) -> i64 {
        self.batch_size.clone()
    }

    pub fn get_show_download_progress(&self) -> bool {
        self.show_download_progress.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_max_embed_duration_secs(&self) -> Option<i64> {
        self.max_embed_duration_secs.clone()
    }

    pub fn get_max_sequence_length(&self) -> Option<i64> {
        self.max_sequence_length.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> EmbeddingConfig {
        xberg::EmbeddingConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Entity")]
#[serde(default)]
pub struct Entity {
    /// Canonical category the entity belongs to (PERSON, ORG, LOCATION, etc.).
    #[php(prop, name = "category")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub category: String,
    /// Raw mention text exactly as it appeared in the source.
    #[php(prop, name = "text")]
    pub text: String,
    /// Byte-offset span in `ExtractedDocument.content` where the mention starts.
    #[php(prop, name = "start")]
    pub start: u32,
    /// Byte-offset span in `ExtractedDocument.content` where the mention ends (exclusive).
    #[php(prop, name = "end")]
    pub end: u32,
    /// Backend-reported confidence in `[0.0, 1.0]`. `None` when the backend does not
    /// expose confidence scores.
    #[php(prop, name = "confidence")]
    pub confidence: Option<f32>,
}

#[php_impl]
impl Entity {
    #[php(constructor)]
    pub fn new(category: String, text: String, start: u32, end: u32, confidence: Option<f32>) -> Self {
        Self {
            category,
            text,
            start,
            end,
            confidence,
        }
    }

    pub fn get_category(&self) -> String {
        self.category.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_start(&self) -> u32 {
        self.start.clone()
    }

    pub fn get_end(&self) -> u32 {
        self.end.clone()
    }

    pub fn get_confidence(&self) -> Option<f32> {
        self.confidence.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\EpubMetadata")]
#[serde(default)]
pub struct EpubMetadata {
    /// Dublin Core `coverage` field (geographic or temporal scope).
    #[php(prop, name = "coverage")]
    pub coverage: Option<String>,
    /// Dublin Core `format` field (media type of the resource).
    #[php(prop, name = "dcFormat")]
    #[serde(alias = "dcFormat")]
    pub dc_format: Option<String>,
    /// Dublin Core `relation` field (related resource identifier).
    #[php(prop, name = "relation")]
    pub relation: Option<String>,
    /// Dublin Core `source` field (origin resource identifier).
    #[php(prop, name = "source")]
    pub source: Option<String>,
    /// Dublin Core `type` field (nature or genre of the resource).
    #[php(prop, name = "dcType")]
    #[serde(alias = "dcType")]
    pub dc_type: Option<String>,
    /// Path or identifier of the cover image within the EPUB container.
    #[php(prop, name = "coverImage")]
    #[serde(alias = "coverImage")]
    pub cover_image: Option<String>,
}

impl Default for EpubMetadata {
    fn default() -> Self {
        <xberg::EpubMetadata as Default>::default().into()
    }
}

#[php_impl]
impl EpubMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_coverage(&self) -> Option<String> {
        self.coverage.clone()
    }

    pub fn get_dc_format(&self) -> Option<String> {
        self.dc_format.clone()
    }

    pub fn get_relation(&self) -> Option<String> {
        self.relation.clone()
    }

    pub fn get_source(&self) -> Option<String> {
        self.source.clone()
    }

    pub fn get_dc_type(&self) -> Option<String> {
        self.dc_type.clone()
    }

    pub fn get_cover_image(&self) -> Option<String> {
        self.cover_image.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ErrorMetadata")]
#[serde(default)]
pub struct ErrorMetadata {
    /// Machine-readable error type identifier (e.g. "UnsupportedFormat").
    #[php(prop, name = "errorType")]
    #[serde(alias = "errorType")]
    pub error_type: String,
    /// Human-readable error description.
    #[php(prop, name = "message")]
    pub message: String,
}

#[php_impl]
impl ErrorMetadata {
    #[php(constructor)]
    pub fn new(errorType: String, message: String) -> Self {
        Self {
            error_type: errorType,
            message,
        }
    }

    pub fn get_error_type(&self) -> String {
        self.error_type.clone()
    }

    pub fn get_message(&self) -> String {
        self.message.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExcelMetadata")]
#[serde(default)]
pub struct ExcelMetadata {
    /// Number of sheets in the workbook.
    #[php(prop, name = "sheetCount")]
    #[serde(alias = "sheetCount")]
    pub sheet_count: Option<u32>,
    /// Names of all sheets in the workbook.
    #[php(prop, name = "sheetNames")]
    #[serde(alias = "sheetNames")]
    pub sheet_names: Option<Vec<String>>,
}

impl Default for ExcelMetadata {
    fn default() -> Self {
        <xberg::ExcelMetadata as Default>::default().into()
    }
}

#[php_impl]
impl ExcelMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_sheet_count(&self) -> Option<u32> {
        self.sheet_count.clone()
    }

    pub fn get_sheet_names(&self) -> Option<Vec<String>> {
        self.sheet_names.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ExcelSheet")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ExcelSheet {
    /// Sheet name as it appears in Excel
    #[php(prop, name = "name")]
    pub name: String,
    /// Sheet content converted to Markdown tables
    #[php(prop, name = "markdown")]
    pub markdown: String,
    /// Number of rows
    #[php(prop, name = "rowCount")]
    #[serde(alias = "rowCount")]
    pub row_count: i64,
    /// Number of columns
    #[php(prop, name = "colCount")]
    #[serde(alias = "colCount")]
    pub col_count: i64,
    /// Total number of non-empty cells
    #[php(prop, name = "cellCount")]
    #[serde(alias = "cellCount")]
    pub cell_count: i64,
    /// Pre-extracted table cells (2D vector of cell values)
    /// Populated during markdown generation to avoid re-parsing markdown.
    /// undefined for empty sheets.
    #[serde(alias = "tableCells")]
    pub table_cells: Option<Vec<Vec<String>>>,
}

#[php_impl]
impl ExcelSheet {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        name: String,
        markdown: String,
        rowCount: i64,
        colCount: i64,
        cellCount: i64,
        tableCells: Option<Vec<Vec<String>>>,
    ) -> Self {
        Self {
            name,
            markdown,
            row_count: rowCount,
            col_count: colCount,
            cell_count: cellCount,
            table_cells: tableCells,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_markdown(&self) -> String {
        self.markdown.clone()
    }

    pub fn get_row_count(&self) -> i64 {
        self.row_count.clone()
    }

    pub fn get_col_count(&self) -> i64 {
        self.col_count.clone()
    }

    pub fn get_cell_count(&self) -> i64 {
        self.cell_count.clone()
    }

    pub fn get_table_cells(&self) -> Option<Vec<Vec<String>>> {
        self.table_cells.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ExcelWorkbook")]
#[serde(default)]
pub struct ExcelWorkbook {
    /// All sheets in the workbook
    pub sheets: Vec<ExcelSheet>,
    /// Workbook-level metadata (author, creation date, etc.)
    #[php(prop, name = "metadata")]
    pub metadata: HashMap<String, String>,
    /// Collaborative-edit revision headers from `xl/revisions/revisionHeaders.xml`.
    ///
    /// Populated for legacy shared-workbook `.xlsx` files that contain the
    /// `xl/revisions/` directory. Each `<header>` element maps to one
    /// `DocumentRevision { kind: FormatChange }` carrying the header's `guid`
    /// (→ `revision_id`), `userName` (→ `author`), and `dateTime` (→ `timestamp`).
    /// `anchor` and `delta` are `None`/empty for v1 (per-cell log parsing is a
    /// follow-up). `None` when `xl/revisions/revisionHeaders.xml` is absent.
    #[serde(default)]
    pub revisions: Option<Vec<DocumentRevision>>,
}

#[php_impl]
impl ExcelWorkbook {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        sheets: &ext_php_rs::types::ZendHashTable,
        metadata: HashMap<String, String>,
        revisions: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let mut sheets_core_result: Vec<ExcelSheet> = Vec::new();
        for (_, item) in sheets.iter() {
            if let Some(parsed) = <&ExcelSheet as ext_php_rs::convert::FromZval>::from_zval(item) {
                sheets_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ExcelSheet".to_string(),
                ));
            }
        }
        let sheets_core: Vec<ExcelSheet> = sheets_core_result;
        let revisions_core: Option<Vec<DocumentRevision>> = if let Some(ht) = revisions {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&DocumentRevision as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to DocumentRevision".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            sheets: sheets_core,
            metadata,
            revisions: revisions_core,
        })
    }

    pub fn get_sheets(&self) -> Vec<ExcelSheet> {
        self.sheets.clone()
    }

    pub fn get_metadata(&self) -> HashMap<String, String> {
        self.metadata.clone()
    }

    pub fn get_revisions(&self) -> Option<Vec<DocumentRevision>> {
        self.revisions.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractInput")]
#[serde(default)]
pub struct ExtractInput {
    /// Source kind. `bytes` requires `bytes`; `uri` requires `uri`.
    #[php(prop, name = "kind")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub kind: String,
    /// Raw bytes for `kind = "bytes"`.
    pub bytes: Option<Vec<u8>>,
    /// Local path, `file://` URI, or HTTP(S) URL for `kind = "uri"`.
    #[php(prop, name = "uri")]
    pub uri: Option<String>,
    /// MIME type hint.
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: Option<String>,
    /// Filename hint used for MIME detection and metadata.
    #[php(prop, name = "filename")]
    pub filename: Option<String>,
    /// Per-input extraction overrides.
    pub config: Option<FileExtractionConfig>,
}

impl Default for ExtractInput {
    fn default() -> Self {
        <xberg::ExtractInput as Default>::default().into()
    }
}

#[php_impl]
impl ExtractInput {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        kind: String,
        bytes: Option<PhpBytes>,
        uri: Option<String>,
        mimeType: Option<String>,
        filename: Option<String>,
        config: Option<&FileExtractionConfig>,
    ) -> Self {
        Self {
            kind,
            bytes: bytes.map(|b| b.0),
            uri,
            mime_type: mimeType,
            filename,
            config: config.cloned(),
        }
    }

    pub fn get_kind(&self) -> String {
        self.kind.clone()
    }

    pub fn get_bytes(&self) -> Option<Vec<u8>> {
        self.bytes.clone()
    }

    pub fn get_uri(&self) -> Option<String> {
        self.uri.clone()
    }

    pub fn get_mime_type(&self) -> Option<String> {
        self.mime_type.clone()
    }

    pub fn get_filename(&self) -> Option<String> {
        self.filename.clone()
    }

    pub fn get_config(&self) -> Option<FileExtractionConfig> {
        self.config.clone().map(Into::into)
    }

    pub fn set_config(&mut self, value: Option<&FileExtractionConfig>) {
        self.config = value.map(|value| value.clone().into());
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ExtractInput {
        xberg::ExtractInput::default().into()
    }

    /// Build a bytes input with a MIME type and optional filename hint.
    #[php(name = "fromBytes")]
    pub fn from_bytes(bytes: PhpBytes, mimeType: String, filename: Option<String>) -> ExtractInput {
        xberg::ExtractInput::from_bytes(bytes.0, mimeType, filename).into()
    }

    /// Build a URI input from a local path, `file://` URI, or HTTP(S) URL.
    #[php(name = "fromUri")]
    pub fn from_uri(uri: String) -> ExtractInput {
        xberg::ExtractInput::from_uri(uri).into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractedDocument")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ExtractedDocument {
    /// Plain-text representation of the extracted document content.
    #[php(prop, name = "content")]
    pub content: String,
    /// MIME type of the source document (e.g. `"application/pdf"`).
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
    /// Document-level metadata (author, title, dates, format-specific fields).
    pub metadata: Metadata,
    /// Extraction strategy used to produce the returned text.
    ///
    /// Populated when the extractor can reliably distinguish native text extraction,
    /// OCR-only extraction, or mixed native/OCR output.
    #[php(prop, name = "extractionMethod")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "extractionMethod")]
    #[serde(default)]
    pub extraction_method: Option<String>,
    /// Tables extracted from the document, each with structured cell data.
    pub tables: Vec<Table>,
    /// Cheap structural counts (pages, tables, images).
    ///
    /// Always populated by the extraction pipeline, even when the `pages` /
    /// `images` collections are `None`. See `DocumentCounts`.
    #[serde(default)]
    pub counts: DocumentCounts,
    /// ISO 639-1 language codes detected in the document content.
    #[php(prop, name = "detectedLanguages")]
    #[serde(alias = "detectedLanguages")]
    pub detected_languages: Option<Vec<String>>,
    /// Structured per-language detection results: confidence, document share, script,
    /// and reliability, alongside the ISO-code-only `detected_languages` (#261).
    ///
    /// One entry per language in `detected_languages`, in the same order. `None` under
    /// the same conditions as `detected_languages`: detection disabled, empty input
    /// text, or no language met the configured `min_confidence`.
    #[serde(alias = "detectedLanguageConfidences")]
    pub detected_language_confidences: Option<Vec<LanguageConfidence>>,
    /// Text chunks when chunking is enabled.
    ///
    /// When chunking configuration is provided, the content is split into
    /// overlapping chunks for efficient processing. Each chunk contains the text,
    /// optional embeddings (if enabled), and metadata about its position.
    pub chunks: Option<Vec<Chunk>>,
    /// Extracted images from the document.
    ///
    /// When image extraction is enabled via `ImageExtractionConfig`, this field
    /// contains all images found in the document with their raw data and metadata.
    /// Each image may optionally contain a nested `ocr_result` if OCR was performed.
    pub images: Option<Vec<ExtractedImage>>,
    /// Per-page content when page extraction is enabled.
    ///
    /// When page extraction is configured, the document is split into per-page content
    /// with tables and images mapped to their respective pages.
    pub pages: Option<Vec<PageContent>>,
    /// Semantic elements when element-based result format is enabled.
    ///
    /// When result_format is set to ElementBased, this field contains semantic
    /// elements with type classification, unique identifiers, and metadata for
    /// Unstructured-compatible element-based processing.
    #[serde(default)]
    pub elements: Option<Vec<Element>>,
    /// Rich Djot content structure (when extracting Djot documents).
    ///
    /// When extracting Djot documents with structured extraction enabled,
    /// this field contains the full semantic structure including:
    /// - Block-level elements with nesting
    /// - Inline formatting with attributes
    /// - Links, images, footnotes
    /// - Math expressions
    /// - Complete attribute information
    ///
    /// The `content` field still contains plain text for backward compatibility.
    ///
    /// Always `None` for non-Djot documents.
    #[serde(alias = "djotContent")]
    #[serde(default)]
    pub djot_content: Option<DjotContent>,
    /// OCR elements with full spatial and confidence metadata.
    ///
    /// When OCR is performed with element extraction enabled, this field contains
    /// the structured representation of detected text including:
    /// - Bounding geometry (rectangles or quadrilaterals)
    /// - Confidence scores (detection and recognition)
    /// - Rotation information
    /// - Hierarchical relationships (Tesseract only)
    ///
    /// This field preserves all metadata that would otherwise be lost when
    /// converting to plain text or markdown output formats.
    ///
    /// Only populated when `OcrElementConfig.include_elements` is true.
    #[serde(alias = "ocrElements")]
    #[serde(default)]
    pub ocr_elements: Option<Vec<OcrElement>>,
    /// Structured document tree (when document structure extraction is enabled).
    ///
    /// When `include_document_structure` is true in `ExtractionConfig`, this field
    /// contains the full hierarchical representation of the document including:
    /// - Heading-driven section nesting
    /// - Table grids with cell-level metadata
    /// - Content layer classification (body, header, footer, footnote)
    /// - Inline text annotations (formatting, links)
    /// - Bounding boxes and page numbers
    ///
    /// Independent of `result_format` — can be combined with Unified or ElementBased.
    #[serde(default)]
    pub document: Option<DocumentStructure>,
    /// Extracted keywords when keyword extraction is enabled.
    ///
    /// When keyword extraction (RAKE or YAKE) is configured, this field contains
    /// the extracted keywords with scores, algorithm info, and position data.
    /// Previously stored in `metadata.additional["keywords"]`.
    #[serde(alias = "extractedKeywords")]
    #[serde(default)]
    pub extracted_keywords: Option<Vec<Keyword>>,
    /// Text cleanliness/readability score from quality analysis.
    ///
    /// A value between 0.0 and 1.0 describing the quality of the text that was
    /// retained. This is not a completeness or recall score: clean text can score
    /// highly even when an extractor omitted or rejected other content. Inspect
    /// `processing_warnings` separately for known degraded or partial extraction.
    /// Previously stored in `metadata.additional["quality_score"]`.
    #[php(prop, name = "qualityScore")]
    #[serde(alias = "qualityScore")]
    #[serde(default)]
    pub quality_score: Option<f64>,
    /// Non-fatal warnings collected during processing pipeline stages.
    ///
    /// Captures errors from optional pipeline features (embedding, chunking,
    /// language detection, output formatting) that don't prevent extraction
    /// but may indicate degraded or incomplete results. These warnings are
    /// independent of `quality_score`, which assesses only retained text.
    /// Previously stored as individual keys in `metadata.additional`.
    #[serde(alias = "processingWarnings")]
    #[serde(default)]
    pub processing_warnings: Vec<ProcessingWarning>,
    /// PDF annotations extracted from the document.
    ///
    /// When annotation extraction is enabled via `PdfConfig.extract_annotations`,
    /// this field contains text notes, highlights, links, stamps, and other
    /// annotations found in PDF documents.
    #[serde(default)]
    pub annotations: Option<Vec<PdfAnnotation>>,
    /// Nested extraction results from archive contents.
    ///
    /// When extracting archives, each processable file inside produces its own
    /// full extraction result. Set to `None` for non-archive formats.
    /// Use `max_archive_depth` in config to control recursion depth.
    #[serde(default)]
    pub children: Option<Vec<ArchiveEntry>>,
    /// URIs/links discovered during document extraction.
    ///
    /// Contains hyperlinks, image references, citations, email addresses, and
    /// other URI-like references found in the document. Always extracted when
    /// present in the source document.
    #[serde(default)]
    pub uris: Option<Vec<ExtractedUri>>,
    /// Tracked changes embedded in the source document.
    ///
    /// Populated by per-format extractors that understand change-tracking
    /// metadata (DOCX `w:ins`/`w:del`/`w:rPrChange`, ODT `text:change-*`,
    /// …). Every extractor defaults to `None` until its format-specific
    /// implementation is added. Extractors that do populate this field follow
    /// the "accepted-changes" convention: inserted text is present in
    /// `content`, deleted text is absent — the revision list is the separate
    /// audit trail.
    #[serde(default)]
    pub revisions: Option<Vec<DocumentRevision>>,
    /// Structured extraction output from LLM-based JSON schema extraction.
    ///
    /// When `structured_extraction` is configured in `ExtractionConfig`, the
    /// extracted document content is sent to a VLM with the provided JSON schema.
    /// The response is parsed and stored here as a JSON value matching the schema.
    #[serde(alias = "structuredOutput")]
    #[serde(default)]
    pub structured_output: Option<serde_json::Value>,
    /// Code intelligence results from tree-sitter analysis.
    ///
    /// Populated when extracting source code files with the `tree-sitter` feature.
    /// Contains metrics, structural analysis, imports/exports, comments,
    /// docstrings, symbols, diagnostics, and optionally chunked code segments.
    ///
    /// Stored as an opaque JSON value so that all language bindings (Go, Java,
    /// C#, …) can deserialize it as a raw JSON object rather than a typed struct.
    /// The underlying type is `tree_sitter_language_pack.ProcessResult`.
    #[serde(alias = "codeIntelligence")]
    #[serde(default)]
    pub code_intelligence: Option<serde_json::Value>,
    /// LLM token usage and cost data for all LLM calls made during this extraction.
    ///
    /// Contains one entry per LLM call. Multiple entries are produced when
    /// VLM OCR, structured extraction, or LLM embeddings run during
    /// the same extraction.
    ///
    /// `None` when no LLM was used.
    #[serde(alias = "llmUsage")]
    #[serde(default)]
    pub llm_usage: Option<Vec<LlmUsage>>,
    /// Named entities detected in `content` by the NER post-processor.
    ///
    /// `None` when no NER backend is configured. Populated by the `xberg-gliner`
    /// ONNX backend or the LLM-driven backend (see `crates/xberg/src/text/ner/`).
    #[serde(default)]
    pub entities: Option<Vec<Entity>>,
    /// Summary of `content` produced by the summarisation post-processor.
    ///
    /// `None` when summarisation is not configured. Populated by the TextRank
    /// extractive backend (deterministic, no external service) or by the
    /// liter-llm-driven abstractive backend.
    #[serde(default)]
    pub summary: Option<DocumentSummary>,
    /// Confidence score computed by the heuristics pipeline.
    ///
    /// Populated when the `heuristics` feature is enabled and confidence
    /// scoring has been performed.  Combines text-coverage, OCR aggregate
    /// confidence, and schema-compliance into a single `[0, 1]` value.
    ///
    /// `None` when confidence scoring is not configured or the feature is
    /// absent.
    #[serde(alias = "extractionConfidence")]
    #[serde(default)]
    pub extraction_confidence: Option<ExtractionConfidence>,
    /// Translation of `content` produced by the translation post-processor.
    ///
    /// `None` when translation is not configured.
    #[serde(default)]
    pub translation: Option<Translation>,
    /// Per-page classifications produced by the page-classification post-processor.
    ///
    /// `None` when classification is not configured.
    #[serde(alias = "pageClassifications")]
    #[serde(default)]
    pub page_classifications: Option<Vec<PageClassification>>,
    /// Audit report of redactions applied by the redaction post-processor.
    ///
    /// The redaction processor rewrites `content`, `formatted_content`, every
    /// chunk's text, and the textual fields of `entities` / `summary` / `translation` /
    /// `page_classifications` in place. This report describes what was found and how it
    /// was replaced. `None` when redaction is not configured.
    #[serde(alias = "redactionReport")]
    #[serde(default)]
    pub redaction_report: Option<RedactionReport>,
    /// Mathematical formulas recognized in the document.
    ///
    /// Populated from every source that produces formulas: layout-guided OCR
    /// (with geometry), VLM OCR (text only), and markup extraction (DOCX,
    /// PPTX, ODT, EPUB, HTML, JATS, LaTeX, Markdown, and related formats,
    /// without geometry). Empty when the document contains no formulas.
    #[serde(default)]
    pub formulas: Vec<Formula>,
    /// Form fields extracted from a PDF's AcroForm or XFA structure.
    ///
    /// Populated by the PDF extractor when `PdfConfig.extract_form_fields` is
    /// enabled (default) and the document is a fillable form. Empty otherwise.
    #[serde(alias = "formFields")]
    #[serde(default)]
    pub form_fields: Vec<PdfFormField>,
}

impl Default for ExtractedDocument {
    fn default() -> Self {
        <xberg::ExtractedDocument as Default>::default().into()
    }
}

#[php_impl]
impl ExtractedDocument {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        mimeType: String,
        metadata: &Metadata,
        tables: &ext_php_rs::types::ZendHashTable,
        counts: &DocumentCounts,
        processingWarnings: &ext_php_rs::types::ZendHashTable,
        formulas: &ext_php_rs::types::ZendHashTable,
        formFields: &ext_php_rs::types::ZendHashTable,
        extractionMethod: Option<String>,
        detectedLanguages: Option<Vec<String>>,
        detectedLanguageConfidences: Option<&ext_php_rs::types::ZendHashTable>,
        chunks: Option<&ext_php_rs::types::ZendHashTable>,
        images: Option<&ext_php_rs::types::ZendHashTable>,
        pages: Option<&ext_php_rs::types::ZendHashTable>,
        elements: Option<&ext_php_rs::types::ZendHashTable>,
        djotContent: Option<&DjotContent>,
        ocrElements: Option<&ext_php_rs::types::ZendHashTable>,
        document: Option<&DocumentStructure>,
        qualityScore: Option<f64>,
        annotations: Option<&ext_php_rs::types::ZendHashTable>,
        children: Option<&ext_php_rs::types::ZendHashTable>,
        uris: Option<&ext_php_rs::types::ZendHashTable>,
        revisions: Option<&ext_php_rs::types::ZendHashTable>,
        structuredOutput: Option<String>,
        llmUsage: Option<&ext_php_rs::types::ZendHashTable>,
        entities: Option<&ext_php_rs::types::ZendHashTable>,
        summary: Option<&DocumentSummary>,
        translation: Option<&Translation>,
        pageClassifications: Option<&ext_php_rs::types::ZendHashTable>,
        redactionReport: Option<&RedactionReport>,
    ) -> PhpResult<Self> {
        let mut tables_core_result: Vec<Table> = Vec::new();
        for (_, item) in tables.iter() {
            if let Some(parsed) = <&Table as ext_php_rs::convert::FromZval>::from_zval(item) {
                tables_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Table".to_string(),
                ));
            }
        }
        let tables_core: Vec<Table> = tables_core_result;
        let detectedLanguageConfidences_core: Option<Vec<LanguageConfidence>> =
            if let Some(ht) = detectedLanguageConfidences {
                let mut result = Vec::new();
                for (_, item) in ht.iter() {
                    if let Some(parsed) = <&LanguageConfidence as ext_php_rs::convert::FromZval>::from_zval(item) {
                        result.push(parsed.clone());
                    } else {
                        return Err(ext_php_rs::exception::PhpException::default(
                            "Failed to convert array element to LanguageConfidence".to_string(),
                        ));
                    }
                }
                Some(result)
            } else {
                None
            };
        let chunks_core: Option<Vec<Chunk>> = if let Some(ht) = chunks {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&Chunk as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to Chunk".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let images_core: Option<Vec<ExtractedImage>> = if let Some(ht) = images {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&ExtractedImage as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to ExtractedImage".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let pages_core: Option<Vec<PageContent>> = if let Some(ht) = pages {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageContent as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageContent".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let elements_core: Option<Vec<Element>> = if let Some(ht) = elements {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&Element as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to Element".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let ocrElements_core: Option<Vec<OcrElement>> = if let Some(ht) = ocrElements {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&OcrElement as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to OcrElement".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let mut processingWarnings_core_result: Vec<ProcessingWarning> = Vec::new();
        for (_, item) in processingWarnings.iter() {
            if let Some(parsed) = <&ProcessingWarning as ext_php_rs::convert::FromZval>::from_zval(item) {
                processingWarnings_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ProcessingWarning".to_string(),
                ));
            }
        }
        let processingWarnings_core: Vec<ProcessingWarning> = processingWarnings_core_result;
        let annotations_core: Option<Vec<PdfAnnotation>> = if let Some(ht) = annotations {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PdfAnnotation as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PdfAnnotation".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let children_core: Option<Vec<ArchiveEntry>> = if let Some(ht) = children {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&ArchiveEntry as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to ArchiveEntry".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let uris_core: Option<Vec<ExtractedUri>> = if let Some(ht) = uris {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&ExtractedUri as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to ExtractedUri".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let revisions_core: Option<Vec<DocumentRevision>> = if let Some(ht) = revisions {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&DocumentRevision as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to DocumentRevision".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let llmUsage_core: Option<Vec<LlmUsage>> = if let Some(ht) = llmUsage {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&LlmUsage as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to LlmUsage".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let entities_core: Option<Vec<Entity>> = if let Some(ht) = entities {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&Entity as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to Entity".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let pageClassifications_core: Option<Vec<PageClassification>> = if let Some(ht) = pageClassifications {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageClassification as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageClassification".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let mut formulas_core_result: Vec<Formula> = Vec::new();
        for (_, item) in formulas.iter() {
            if let Some(parsed) = <&Formula as ext_php_rs::convert::FromZval>::from_zval(item) {
                formulas_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Formula".to_string(),
                ));
            }
        }
        let formulas_core: Vec<Formula> = formulas_core_result;
        let mut formFields_core_result: Vec<PdfFormField> = Vec::new();
        for (_, item) in formFields.iter() {
            if let Some(parsed) = <&PdfFormField as ext_php_rs::convert::FromZval>::from_zval(item) {
                formFields_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to PdfFormField".to_string(),
                ));
            }
        }
        let formFields_core: Vec<PdfFormField> = formFields_core_result;
        Ok(Self {
            content,
            mime_type: mimeType,
            metadata: metadata.clone(),
            extraction_method: extractionMethod,
            tables: tables_core,
            counts: counts.clone(),
            detected_languages: detectedLanguages,
            detected_language_confidences: detectedLanguageConfidences_core,
            chunks: chunks_core,
            images: images_core,
            pages: pages_core,
            elements: elements_core,
            djot_content: djotContent.cloned(),
            ocr_elements: ocrElements_core,
            document: document.cloned(),
            extracted_keywords: Default::default(),
            quality_score: qualityScore,
            processing_warnings: processingWarnings_core,
            annotations: annotations_core,
            children: children_core,
            uris: uris_core,
            revisions: revisions_core,
            structured_output: structuredOutput
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            code_intelligence: Default::default(),
            llm_usage: llmUsage_core,
            entities: entities_core,
            summary: summary.cloned(),
            extraction_confidence: Default::default(),
            translation: translation.cloned(),
            page_classifications: pageClassifications_core,
            redaction_report: redactionReport.cloned(),
            formulas: formulas_core,
            form_fields: formFields_core,
        })
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }

    pub fn get_metadata(&self) -> Metadata {
        self.metadata.clone()
    }

    pub fn get_extraction_method(&self) -> Option<String> {
        self.extraction_method.clone().map(Into::into)
    }

    pub fn get_tables(&self) -> Vec<Table> {
        self.tables.clone()
    }

    pub fn get_counts(&self) -> DocumentCounts {
        self.counts.clone()
    }

    pub fn get_detected_languages(&self) -> Option<Vec<String>> {
        self.detected_languages.clone()
    }

    pub fn get_detected_language_confidences(&self) -> Option<Vec<LanguageConfidence>> {
        self.detected_language_confidences.clone()
    }

    pub fn get_chunks(&self) -> Option<Vec<Chunk>> {
        self.chunks.clone()
    }

    pub fn get_images(&self) -> Option<Vec<ExtractedImage>> {
        self.images.clone()
    }

    pub fn get_pages(&self) -> Option<Vec<PageContent>> {
        self.pages.clone()
    }

    pub fn get_elements(&self) -> Option<Vec<Element>> {
        self.elements.clone()
    }

    pub fn get_djot_content(&self) -> Option<DjotContent> {
        self.djot_content.clone().map(Into::into)
    }

    pub fn set_djot_content(&mut self, value: Option<&DjotContent>) {
        self.djot_content = value.map(|value| value.clone().into());
    }

    pub fn get_ocr_elements(&self) -> Option<Vec<OcrElement>> {
        self.ocr_elements.clone()
    }

    pub fn get_document(&self) -> Option<DocumentStructure> {
        self.document.clone().map(Into::into)
    }

    pub fn set_document(&mut self, value: Option<&DocumentStructure>) {
        self.document = value.map(|value| value.clone().into());
    }

    pub fn get_extracted_keywords(&self) -> Option<Vec<Keyword>> {
        self.extracted_keywords.clone()
    }

    pub fn get_quality_score(&self) -> Option<f64> {
        self.quality_score.clone()
    }

    pub fn get_processing_warnings(&self) -> Vec<ProcessingWarning> {
        self.processing_warnings.clone()
    }

    pub fn get_annotations(&self) -> Option<Vec<PdfAnnotation>> {
        self.annotations.clone()
    }

    pub fn get_children(&self) -> Option<Vec<ArchiveEntry>> {
        self.children.clone()
    }

    pub fn get_uris(&self) -> Option<Vec<ExtractedUri>> {
        self.uris.clone()
    }

    pub fn get_revisions(&self) -> Option<Vec<DocumentRevision>> {
        self.revisions.clone()
    }

    pub fn get_structured_output(&self) -> Option<String> {
        self.structured_output
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_code_intelligence(&self) -> Option<String> {
        self.code_intelligence
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_llm_usage(&self) -> Option<Vec<LlmUsage>> {
        self.llm_usage.clone()
    }

    pub fn get_entities(&self) -> Option<Vec<Entity>> {
        self.entities.clone()
    }

    pub fn get_summary(&self) -> Option<DocumentSummary> {
        self.summary.clone().map(Into::into)
    }

    pub fn set_summary(&mut self, value: Option<&DocumentSummary>) {
        self.summary = value.map(|value| value.clone().into());
    }

    pub fn get_extraction_confidence(&self) -> Option<ExtractionConfidence> {
        self.extraction_confidence.clone().map(Into::into)
    }

    pub fn set_extraction_confidence(&mut self, value: Option<&ExtractionConfidence>) {
        self.extraction_confidence = value.map(|value| value.clone().into());
    }

    pub fn get_translation(&self) -> Option<Translation> {
        self.translation.clone().map(Into::into)
    }

    pub fn set_translation(&mut self, value: Option<&Translation>) {
        self.translation = value.map(|value| value.clone().into());
    }

    pub fn get_page_classifications(&self) -> Option<Vec<PageClassification>> {
        self.page_classifications.clone()
    }

    pub fn get_redaction_report(&self) -> Option<RedactionReport> {
        self.redaction_report.clone().map(Into::into)
    }

    pub fn set_redaction_report(&mut self, value: Option<&RedactionReport>) {
        self.redaction_report = value.map(|value| value.clone().into());
    }

    pub fn get_formulas(&self) -> Vec<Formula> {
        self.formulas.clone()
    }

    pub fn get_form_fields(&self) -> Vec<PdfFormField> {
        self.form_fields.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractedImage")]
#[serde(default)]
pub struct ExtractedImage {
    /// Raw image data (PNG, JPEG, WebP, etc. bytes).
    /// Uses `bytes.Bytes` for cheap cloning of large buffers.
    pub data: Vec<u8>,
    /// Image format (e.g., "jpeg", "png", "webp")
    /// Uses Cow<, str> to avoid allocation for static literals.
    #[php(prop, name = "format")]
    pub format: String,
    /// Zero-indexed position of this image in the document/page
    #[php(prop, name = "imageIndex")]
    #[serde(alias = "imageIndex")]
    pub image_index: u32,
    /// Page/slide number where image was found (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: Option<u32>,
    /// Image width in pixels
    #[php(prop, name = "width")]
    pub width: Option<u32>,
    /// Image height in pixels
    #[php(prop, name = "height")]
    pub height: Option<u32>,
    /// Colorspace information (e.g., "RGB", "CMYK", "Gray")
    #[php(prop, name = "colorspace")]
    pub colorspace: Option<String>,
    /// Bits per color component (e.g., 8, 16)
    #[php(prop, name = "bitsPerComponent")]
    #[serde(alias = "bitsPerComponent")]
    pub bits_per_component: Option<u32>,
    /// Whether this image is a mask image
    #[php(prop, name = "isMask")]
    #[serde(alias = "isMask")]
    #[serde(default)]
    pub is_mask: bool,
    /// Optional description of the image
    #[php(prop, name = "description")]
    pub description: Option<String>,
    /// Nested OCR extraction result (if image was OCRed)
    ///
    /// When OCR is performed on this image, the result is embedded here
    /// rather than in a separate collection, making the relationship explicit.
    #[serde(alias = "ocrResult")]
    pub ocr_result: Option<ExtractedDocument>,
    /// Bounding box of the image on the page (PDF coordinates: x0=left, y0=bottom, x1=right, y1=top).
    /// Only populated for PDF-extracted images when position data is available from the PDF extractor.
    #[serde(alias = "boundingBox")]
    #[serde(default)]
    pub bounding_box: Option<BoundingBox>,
    /// Original source path of the image within the document archive (e.g., "media/image1.png" in DOCX).
    /// Used for rendering image references when the binary data is not extracted.
    #[php(prop, name = "sourcePath")]
    #[serde(alias = "sourcePath")]
    #[serde(default)]
    pub source_path: Option<String>,
    /// Heuristic classification of what this image likely depicts.
    /// `None` if classification was disabled or inconclusive.
    #[php(prop, name = "imageKind")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "imageKind")]
    #[serde(default)]
    pub image_kind: Option<String>,
    /// Confidence score for `image_kind`, in the range 0.0 to 1.0.
    #[php(prop, name = "kindConfidence")]
    #[serde(alias = "kindConfidence")]
    #[serde(default)]
    pub kind_confidence: Option<f32>,
    /// Identifier shared across images that form a single logical figure
    /// (e.g. all raster tiles of one technical drawing). `None` for singletons.
    #[php(prop, name = "clusterId")]
    #[serde(alias = "clusterId")]
    #[serde(default)]
    pub cluster_id: Option<u32>,
    /// VLM-generated caption describing the image, when captioning is configured.
    ///
    /// Populated by the captioning post-processor
    /// (`crates/xberg/src/plugins/processor/builtin/captioning.rs`), which routes
    /// each image through `crate.llm.region_extractor.extract_region_with_vlm` in
    /// caption mode. `None` when captioning is disabled or the VLM declined to caption.
    #[php(prop, name = "caption")]
    #[serde(default)]
    pub caption: Option<String>,
    /// QR codes decoded from this image, when QR detection is enabled.
    ///
    /// Populated by the QR post-processor (`crates/xberg/src/extractors/qr.rs`) via
    /// the pure-Rust `rqrr` decoder. `None` when QR detection is disabled; an empty
    /// `Some(vec![])` when detection ran but found nothing.
    #[serde(alias = "qrCodes")]
    #[serde(default)]
    pub qr_codes: Option<Vec<QrCode>>,
    /// Base64-encoded copy of `data`; populated when `ImageExtractionConfig.include_data_base64`
    /// is `true`. Omitted from JSON by default; use instead of `data` in JSON-only clients.
    #[php(prop, name = "dataBase64")]
    #[serde(alias = "dataBase64")]
    #[serde(default)]
    pub data_base64: Option<String>,
}

impl Default for ExtractedImage {
    fn default() -> Self {
        <xberg::ExtractedImage as Default>::default().into()
    }
}

#[php_impl]
impl ExtractedImage {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        data: PhpBytes,
        format: String,
        imageIndex: u32,
        isMask: bool,
        pageNumber: Option<u32>,
        width: Option<u32>,
        height: Option<u32>,
        colorspace: Option<String>,
        bitsPerComponent: Option<u32>,
        description: Option<String>,
        ocrResult: Option<&ExtractedDocument>,
        boundingBox: Option<&BoundingBox>,
        sourcePath: Option<String>,
        imageKind: Option<String>,
        kindConfidence: Option<f32>,
        clusterId: Option<u32>,
        caption: Option<String>,
        qrCodes: Option<&ext_php_rs::types::ZendHashTable>,
        dataBase64: Option<String>,
    ) -> PhpResult<Self> {
        let qrCodes_core: Option<Vec<QrCode>> = if let Some(ht) = qrCodes {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&QrCode as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to QrCode".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            data: data.0,
            format,
            image_index: imageIndex,
            page_number: pageNumber,
            width,
            height,
            colorspace,
            bits_per_component: bitsPerComponent,
            is_mask: isMask,
            description,
            ocr_result: ocrResult.cloned(),
            bounding_box: boundingBox.cloned(),
            source_path: sourcePath,
            image_kind: imageKind,
            kind_confidence: kindConfidence,
            cluster_id: clusterId,
            caption,
            qr_codes: qrCodes_core,
            data_base64: dataBase64,
        })
    }

    pub fn get_data(&self) -> Vec<u8> {
        self.data.clone()
    }

    pub fn get_format(&self) -> String {
        self.format.clone()
    }

    pub fn get_image_index(&self) -> u32 {
        self.image_index.clone()
    }

    pub fn get_page_number(&self) -> Option<u32> {
        self.page_number.clone()
    }

    pub fn get_width(&self) -> Option<u32> {
        self.width.clone()
    }

    pub fn get_height(&self) -> Option<u32> {
        self.height.clone()
    }

    pub fn get_colorspace(&self) -> Option<String> {
        self.colorspace.clone()
    }

    pub fn get_bits_per_component(&self) -> Option<u32> {
        self.bits_per_component.clone()
    }

    pub fn get_is_mask(&self) -> bool {
        self.is_mask.clone()
    }

    pub fn get_description(&self) -> Option<String> {
        self.description.clone()
    }

    pub fn get_ocr_result(&self) -> Option<ExtractedDocument> {
        self.ocr_result.clone().map(Into::into)
    }

    pub fn set_ocr_result(&mut self, value: Option<&ExtractedDocument>) {
        self.ocr_result = value.map(|value| value.clone().into());
    }

    pub fn get_bounding_box(&self) -> Option<BoundingBox> {
        self.bounding_box.clone().map(Into::into)
    }

    pub fn set_bounding_box(&mut self, value: Option<&BoundingBox>) {
        self.bounding_box = value.map(|value| value.clone().into());
    }

    pub fn get_source_path(&self) -> Option<String> {
        self.source_path.clone()
    }

    pub fn get_image_kind(&self) -> Option<String> {
        self.image_kind.clone().map(Into::into)
    }

    pub fn get_kind_confidence(&self) -> Option<f32> {
        self.kind_confidence.clone()
    }

    pub fn get_cluster_id(&self) -> Option<u32> {
        self.cluster_id.clone()
    }

    pub fn get_caption(&self) -> Option<String> {
        self.caption.clone()
    }

    pub fn get_qr_codes(&self) -> Option<Vec<QrCode>> {
        self.qr_codes.clone()
    }

    pub fn get_data_base64(&self) -> Option<String> {
        self.data_base64.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ExtractedUri")]
#[serde(default)]
pub struct ExtractedUri {
    /// The URL or path string.
    #[php(prop, name = "url")]
    pub url: String,
    /// Optional display text / label for the link.
    #[php(prop, name = "label")]
    pub label: Option<String>,
    /// Optional page number where the URI was found (1-indexed).
    #[php(prop, name = "page")]
    pub page: Option<u32>,
    /// Semantic classification of the URI.
    #[php(prop, name = "kind")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub kind: String,
}

#[php_impl]
impl ExtractedUri {
    #[php(constructor)]
    pub fn new(url: String, kind: String, label: Option<String>, page: Option<u32>) -> Self {
        Self { url, label, page, kind }
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }

    pub fn get_label(&self) -> Option<String> {
        self.label.clone()
    }

    pub fn get_page(&self) -> Option<u32> {
        self.page.clone()
    }

    pub fn get_kind(&self) -> String {
        self.kind.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ExtractionConfidence")]
#[serde(default)]
pub struct ExtractionConfidence {
    /// Fraction of pages with a usable text layer.
    #[php(prop, name = "textCoverage")]
    #[serde(alias = "textCoverage")]
    pub text_coverage: f32,
    /// Mean OCR per-element recognition confidence when OCR ran; `None` when it did not.
    #[php(prop, name = "ocrAggregate")]
    #[serde(alias = "ocrAggregate")]
    pub ocr_aggregate: Option<f32>,
    /// Whether the merged output validates against the preset schema.
    #[php(prop, name = "schemaCompliance")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "schemaCompliance")]
    pub schema_compliance: String,
    /// Weighted blend in `[0, 1]`.  The value compared against the fallback threshold.
    #[php(prop, name = "combined")]
    pub combined: f32,
}

#[php_impl]
impl ExtractionConfidence {
    #[php(constructor)]
    pub fn new(textCoverage: f32, schemaCompliance: String, combined: f32, ocrAggregate: Option<f32>) -> Self {
        Self {
            text_coverage: textCoverage,
            ocr_aggregate: ocrAggregate,
            schema_compliance: schemaCompliance,
            combined,
        }
    }

    pub fn get_text_coverage(&self) -> f32 {
        self.text_coverage.clone()
    }

    pub fn get_ocr_aggregate(&self) -> Option<f32> {
        self.ocr_aggregate.clone()
    }

    pub fn get_schema_compliance(&self) -> String {
        self.schema_compliance.clone()
    }

    pub fn get_combined(&self) -> f32 {
        self.combined.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractionConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ExtractionConfig {
    /// Controls whether MIME inference prefers content, a supported extension, or content alone.
    #[php(prop, name = "mimeDetectionPolicy")]
    #[serde(default = "crate::serde_defaults::extraction_config_mime_detection_policy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "mimeDetectionPolicy")]
    pub mime_detection_policy: String,
    /// Enable caching of extraction results
    #[php(prop, name = "useCache")]
    #[serde(default = "crate::serde_defaults::extraction_config_use_cache")]
    #[serde(alias = "useCache")]
    pub use_cache: bool,
    /// Enable quality post-processing
    #[php(prop, name = "enableQualityProcessing")]
    #[serde(default = "crate::serde_defaults::extraction_config_enable_quality_processing")]
    #[serde(alias = "enableQualityProcessing")]
    pub enable_quality_processing: bool,
    /// OCR configuration.
    ///
    /// `None` does not run OCR for documents that already have usable text. Under
    /// `OcrStrategy.Auto`, a PDF with no text layer at all (a scan) is still routed
    /// to OCR with default settings so it is not returned empty (#1338). Set
    /// `Self.disable_ocr` to hard-disable OCR regardless of the detected content.
    #[serde(default)]
    pub ocr: Option<OcrConfig>,
    /// Force OCR even for searchable PDFs
    #[php(prop, name = "forceOcr")]
    #[serde(default = "crate::serde_defaults::extraction_config_force_ocr")]
    #[serde(alias = "forceOcr")]
    pub force_ocr: bool,
    /// Which pages get OCR'd when neither `force_ocr` nor `force_ocr_pages` applies.
    ///
    /// Defaults to `OcrStrategy.Auto`, which OCRs only pages whose native text
    /// fails a quality check. Only applies to PDF documents. Cannot be
    /// `OcrStrategy.ScannedPages` while `disable_ocr` is `true`.
    #[serde(alias = "ocrStrategy")]
    #[serde(default)]
    pub ocr_strategy: OcrStrategy,
    /// Force OCR on specific pages only (1-indexed page numbers, must be >= 1).
    ///
    /// When set, only the listed pages are OCR'd regardless of text layer quality.
    /// Unlisted pages use native text extraction. Ignored when `force_ocr` is `true`.
    /// Only applies to PDF documents. Duplicates are automatically deduplicated.
    /// An `ocr` config is recommended for backend/language selection; defaults are used if absent.
    #[php(prop, name = "forceOcrPages")]
    #[serde(alias = "forceOcrPages")]
    #[serde(default)]
    pub force_ocr_pages: Option<Vec<u32>>,
    /// Disable OCR entirely, even for images.
    ///
    /// When `true`, OCR is skipped for all document types. Images return metadata
    /// only (dimensions, format, EXIF) without text extraction. PDFs use only
    /// native text extraction without OCR fallback.
    ///
    /// Cannot be `true` simultaneously with `force_ocr`.
    #[php(prop, name = "disableOcr")]
    #[serde(default = "crate::serde_defaults::extraction_config_disable_ocr")]
    #[serde(alias = "disableOcr")]
    pub disable_ocr: bool,
    /// Text chunking configuration (undefined = chunking disabled)
    #[serde(default)]
    pub chunking: Option<ChunkingConfig>,
    /// Content filtering configuration (undefined = use extractor defaults).
    ///
    /// Controls whether document "furniture" (headers, footers, watermarks,
    /// repeating text) is included in or stripped from extraction results.
    /// See `ContentFilterConfig` for per-field documentation.
    #[serde(alias = "contentFilter")]
    #[serde(default)]
    pub content_filter: Option<ContentFilterConfig>,
    /// Image extraction configuration (undefined = no image extraction)
    #[serde(default)]
    pub images: Option<ImageExtractionConfig>,
    /// PDF-specific options (undefined = use defaults)
    #[serde(alias = "pdfOptions")]
    #[serde(default)]
    pub pdf_options: Option<PdfConfig>,
    /// Token reduction configuration (undefined = no token reduction)
    #[serde(alias = "tokenReduction")]
    #[serde(default)]
    pub token_reduction: Option<TokenReductionOptions>,
    /// Language detection configuration (undefined = no language detection)
    #[serde(alias = "languageDetection")]
    #[serde(default)]
    pub language_detection: Option<LanguageDetectionConfig>,
    /// Page extraction configuration (undefined = no page tracking)
    #[serde(default)]
    pub pages: Option<PageConfig>,
    /// Keyword extraction configuration (undefined = no keyword extraction)
    #[serde(default)]
    pub keywords: Option<KeywordConfig>,
    /// Post-processor configuration (undefined = use defaults)
    #[serde(default)]
    pub postprocessor: Option<PostProcessorConfig>,
    /// HTML to Markdown conversion options (undefined = use defaults)
    ///
    /// Configure how HTML documents are converted to Markdown, including heading styles,
    /// list formatting, code block styles, and preprocessing options.
    #[serde(alias = "htmlOptions")]
    #[serde(default)]
    pub html_options: Option<ConversionOptions>,
    /// Styled HTML output configuration.
    ///
    /// When set alongside `output_format = OutputFormat.Html`, the extraction
    /// pipeline uses `StyledHtmlRenderer`
    /// which emits stable `kb-*` CSS class hooks on every structural element
    /// and optionally embeds theme CSS or user-supplied CSS in a `<style>` block.
    ///
    /// When `None`, the existing plain comrak-based HTML renderer is used.
    #[serde(alias = "htmlOutput")]
    #[serde(default)]
    pub html_output: Option<HtmlOutputConfig>,
    /// Default per-file timeout in seconds for batch extraction.
    ///
    /// When set, each file in a batch will be canceled after this duration
    /// unless overridden by `FileExtractionConfig.timeout_secs`.
    ///
    /// Defaults to `Some(600)` (10 minutes) to prevent pathological files
    /// (e.g. deeply nested archives, documents with millions of cells) from
    /// running indefinitely and exhausting caller resources, while still
    /// giving slow paths (VLM-based OCR, large scanned documents) enough
    /// headroom to finish. Set to `None` to disable the timeout for trusted
    /// input or long-running workloads.
    #[php(prop, name = "extractionTimeoutSecs")]
    #[serde(alias = "extractionTimeoutSecs")]
    pub extraction_timeout_secs: Option<i64>,
    /// Maximum concurrent document extractions in batch operations.
    ///
    /// This is a ceiling within the configured total thread budget, not an
    /// independent pool size. When unset, the scheduler derives document and
    /// per-document concurrency from `ConcurrencyConfig.max_threads`.
    #[php(prop, name = "maxConcurrentExtractions")]
    #[serde(alias = "maxConcurrentExtractions")]
    #[serde(default)]
    pub max_concurrent_extractions: Option<i64>,
    /// Result structure format
    ///
    /// Controls whether results are returned in unified format (default) with all
    /// content in the `content` field, or element-based format with semantic
    /// elements (for Unstructured-compatible output).
    #[php(prop, name = "resultFormat")]
    #[serde(default = "crate::serde_defaults::extraction_config_result_format")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "resultFormat")]
    pub result_format: String,
    /// Security limits for archive extraction.
    ///
    /// Controls maximum archive size, compression ratio, file count, and other
    /// security thresholds to prevent decompression bomb attacks. Also caps
    /// nesting depth, iteration count, entity / token length, total content size,
    /// decoded image allocation, and table cell count for every extraction path
    /// that ingests user-controlled bytes.
    /// When `None`, default limits are used.
    #[serde(alias = "securityLimits")]
    #[serde(default)]
    pub security_limits: Option<SecurityLimits>,
    /// Maximum uncompressed size in bytes for a single embedded file before
    /// recursive extraction is attempted (default: 50 MiB).
    ///
    /// Applies to embedded objects inside OOXML containers (DOCX, PPTX) and
    /// to email attachments processed via recursive extraction. Files that
    /// exceed this limit are skipped with a `ProcessingWarning` rather than
    /// passed to the extraction pipeline, preventing a single oversized
    /// embedded object from consuming unbounded memory or time.
    ///
    /// Set to `None` to disable the per-embedded-file cap (falls back to
    /// `security_limits.max_archive_size` as the only guard).
    #[php(prop, name = "maxEmbeddedFileBytes")]
    #[serde(alias = "maxEmbeddedFileBytes")]
    pub max_embedded_file_bytes: Option<i64>,
    /// Content text format (default: Plain).
    ///
    /// Controls the format of the extracted content:
    /// - `Plain`: Raw extracted text (default)
    /// - `Markdown`: Markdown formatted output
    /// - `Djot`: Djot markup format (requires djot feature)
    /// - `Html`: HTML formatted output
    ///
    /// When set to a structured format, extraction results will include
    /// formatted output. The `formatted_content` field may be populated
    /// when format conversion is applied.
    #[php(prop, name = "outputFormat")]
    #[serde(default = "crate::serde_defaults::extraction_config_output_format")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "outputFormat")]
    pub output_format: String,
    /// Escape Markdown special characters in rendered prose (default: `true`).
    ///
    /// When `output_format` is `Markdown` or `Djot`, the renderer backslash-escapes
    /// CommonMark-significant leading characters (e.g. `-`, `#`) so that literal
    /// text such as `#06-18` or `- clause` round-trips safely through a CommonMark
    /// parser instead of being reinterpreted as a heading or list marker.
    ///
    /// Table cell text is never escaped, so escaped prose can look inconsistent
    /// with table cells containing the same characters. Set this to `false` to
    /// disable prose escaping and make `content`, `pages[].content`, and
    /// `chunks[].content` read identically to table cell text — useful for LLM
    /// prompts or search indexing where CommonMark round-tripping does not matter.
    ///
    /// Defaults to `true` to preserve existing behavior.
    #[php(prop, name = "escapeMarkdown")]
    #[serde(default = "crate::serde_defaults::extraction_config_escape_markdown")]
    #[serde(alias = "escapeMarkdown")]
    pub escape_markdown: bool,
    /// Emit an opt-in anchor marker before each table's rendered Markdown
    /// block (default: `false`).
    ///
    /// When `output_format` is `Markdown` (or `Djot`) and this is `true`, the
    /// renderer inserts a `[TABLE:{table_id}]` marker immediately before each
    /// table's Markdown in `content`, `pages[].content`, and
    /// `chunks[].content`, where `table_id` matches the corresponding
    /// entry's `table_id`. This lets a consumer
    /// reconcile a rendered Markdown table block with its structured
    /// `tables[]` entry.
    ///
    /// Defaults to `false` so existing output is byte-identical unless
    /// explicitly enabled.
    #[php(prop, name = "tableAnchors")]
    #[serde(default = "crate::serde_defaults::extraction_config_table_anchors")]
    #[serde(alias = "tableAnchors")]
    pub table_anchors: bool,
    /// Controls how Jupyter notebook (`.ipynb`) code cells are rendered.
    ///
    /// - `Both` (default): code source plus the notebook's saved outputs
    /// - `Source`: only the code source (fenced code blocks)
    /// - `Outputs`: only the saved outputs
    ///
    /// Cells are never executed; `Outputs`/`Both` surface only outputs already
    /// stored in the notebook.
    #[php(prop, name = "jupyterCellRendering")]
    #[serde(default = "crate::serde_defaults::extraction_config_jupyter_cell_rendering")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "jupyterCellRendering")]
    pub jupyter_cell_rendering: String,
    /// Apply Jupyter Book/MyST cell visibility tags while rendering notebooks.
    ///
    /// When enabled, `remove-cell`/`hide-cell`, `remove-input`/`hide-input`,
    /// and `remove-output`/`hide-output` suppress the corresponding saved
    /// source or output. Cells are never executed, and their metadata remains
    /// available even when their rendered content is suppressed.
    ///
    /// Defaults to `true`. Set this to `false` to preserve all saved notebook
    /// content regardless of cell tags.
    #[php(prop, name = "applyNotebookCellTags")]
    #[serde(default = "crate::serde_defaults::extraction_config_apply_notebook_cell_tags")]
    #[serde(alias = "applyNotebookCellTags")]
    pub apply_notebook_cell_tags: bool,
    /// Layout detection configuration (undefined = layout detection disabled).
    ///
    /// When set, PDF pages and images are analyzed for document structure
    /// (headings, code, formulas, tables, figures, etc.) using RT-DETR models
    /// via ONNX Runtime. For PDFs, layout hints override paragraph classification
    /// in the markdown pipeline. For images, per-region OCR is performed with
    /// markdown formatting based on detected layout classes.
    /// Requires the `layout-detection` feature to run inference; the field is
    /// present whenever the `layout-types` feature is active (which includes
    /// `layout-detection` as well as the no-ORT target groups).
    #[serde(default)]
    pub layout: Option<LayoutDetectionConfig>,
    /// Transcription (speech-to-text) configuration for audio/video files.
    ///
    /// When set and `enabled`, files with audio/video MIME types (mp3, mp4,
    /// m4a, wav, webm, etc.) are routed to the Whisper-based transcription
    /// pipeline. The actual heavy dependencies are only active under the
    /// `transcription` feature; the field is visible under `transcription-types`
    /// (including on WASM and Android targets that use the no-ORT preset).
    ///
    /// Default: `None` (transcription disabled). This is an additive,
    /// non-breaking change.
    #[serde(default)]
    pub transcription: Option<TranscriptionConfig>,
    /// Run layout detection on the non-OCR PDF markdown path.
    ///
    /// When `true` and `layout` is `Some(_)`, layout regions inform reading
    /// order, region grouping, and table detection while native font/tag
    /// semantics remain authoritative for headings, lists, code, and formulas.
    /// OCR layout classification is unchanged. This improves structural output
    /// at the cost of inference latency (~150-300ms/page CPU, ~20-50ms/page
    /// GPU). Default: `false`. Requires the `layout-detection` feature.
    #[php(prop, name = "useLayoutForMarkdown")]
    #[serde(default = "crate::serde_defaults::extraction_config_use_layout_for_markdown")]
    #[serde(alias = "useLayoutForMarkdown")]
    pub use_layout_for_markdown: bool,
    /// Enable structured document tree output.
    ///
    /// When true, populates the `document` field on `ExtractedDocument` with a
    /// hierarchical `DocumentStructure` containing heading-driven section nesting,
    /// table grids, content layer classification, and inline annotations.
    ///
    /// Independent of `result_format` — can be combined with Unified or ElementBased.
    #[php(prop, name = "includeDocumentStructure")]
    #[serde(default = "crate::serde_defaults::extraction_config_include_document_structure")]
    #[serde(alias = "includeDocumentStructure")]
    pub include_document_structure: bool,
    /// Hardware acceleration configuration for ONNX Runtime models.
    ///
    /// Controls execution provider selection for layout detection and embedding
    /// models. When `None`, uses platform defaults (CoreML on macOS, CUDA on
    /// Linux, CPU on Windows).
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Cache namespace for tenant isolation.
    ///
    /// When set, cache entries are stored under `{cache_dir}/{namespace}/`.
    /// Must be alphanumeric, hyphens, or underscores only (max 64 chars).
    /// Different namespaces have isolated cache spaces on the same filesystem.
    #[php(prop, name = "cacheNamespace")]
    #[serde(alias = "cacheNamespace")]
    #[serde(default)]
    pub cache_namespace: Option<String>,
    /// Per-request cache TTL in seconds.
    ///
    /// Overrides the global `max_age_days` for this specific extraction.
    /// When `0`, caching is completely skipped (no read or write).
    /// When `None`, the global TTL applies.
    #[php(prop, name = "cacheTtlSecs")]
    #[serde(alias = "cacheTtlSecs")]
    #[serde(default)]
    pub cache_ttl_secs: Option<i64>,
    /// Email extraction configuration (undefined = use defaults).
    ///
    /// Currently supports configuring the fallback codepage for MSG files
    /// that do not specify one. See `EmailConfig` for details.
    #[serde(default)]
    pub email: Option<EmailConfig>,
    /// CSV/TSV extraction configuration (undefined = use defaults).
    ///
    /// Lets callers set an explicit delimiter and declare comment-line
    /// prefixes to skip, instead of relying solely on delimiter
    /// auto-detection. See `CsvConfig` for details.
    #[serde(default)]
    pub csv: Option<CsvConfig>,
    /// GeoJSON extraction configuration (undefined = bounded summary).
    ///
    /// By default, GeoJSON coordinates are replaced by aggregate counts and bounds
    /// so large geometry arrays do not become unbounded rendered output. Set
    /// `include_full_coordinates`
    /// explicitly to retain the legacy full-coordinate output.
    #[serde(default)]
    pub geojson: Option<GeoJsonExtractionConfig>,
    /// Concurrency limits for constrained environments (undefined = use defaults).
    ///
    /// Controls Rayon thread pool size, ONNX Runtime intra-op threads, and the
    /// combined document/inner-task budget for batch extraction. See
    /// `ConcurrencyConfig` for details.
    #[serde(default)]
    pub concurrency: Option<ConcurrencyConfig>,
    /// URL ingestion and crawl configuration.
    #[serde(default)]
    pub url: UrlExtractionConfig,
    /// Maximum recursion depth for archive extraction (default: 3).
    /// Set to 0 to disable recursive extraction (legacy behavior).
    #[php(prop, name = "maxArchiveDepth")]
    #[serde(default = "crate::serde_defaults::extraction_config_max_archive_depth")]
    #[serde(alias = "maxArchiveDepth")]
    pub max_archive_depth: i64,
    /// Tree-sitter language pack configuration (undefined = tree-sitter disabled).
    ///
    /// When set, enables code file extraction using tree-sitter parsers.
    /// Controls grammar download behavior and code analysis options.
    #[serde(alias = "treeSitter")]
    #[serde(default)]
    pub tree_sitter: Option<TreeSitterConfig>,
    /// Structured extraction via LLM (undefined = disabled).
    ///
    /// When set, the extracted document content is sent to an LLM with the
    /// provided JSON schema. The structured response is stored in
    /// `ExtractedDocument.structured_output`.
    #[serde(alias = "structuredExtraction")]
    #[serde(default)]
    pub structured_extraction: Option<StructuredExtractionConfig>,
    /// Named-entity recognition configuration. When set, the NER post-processor runs at
    /// the Middle stage and populates `ExtractedDocument.entities`.
    #[serde(default)]
    pub ner: Option<NerConfig>,
    /// Redaction / anonymisation configuration. When set, the redaction post-processor
    /// runs at the Late stage and rewrites every textual field in `ExtractedDocument`,
    /// emitting an audit trail in `ExtractedDocument.redaction_report`.
    #[serde(default)]
    pub redaction: Option<RedactionConfig>,
    /// Summarisation configuration. When set, the summarisation post-processor runs at
    /// the Middle stage and populates `ExtractedDocument.summary`.
    #[serde(default)]
    pub summarization: Option<SummarizationConfig>,
    /// Translation configuration. When set, the translation post-processor runs at the
    /// Middle stage and populates `ExtractedDocument.translation`.
    #[serde(default)]
    pub translation: Option<TranslationConfig>,
    /// Per-page classification configuration. When set, the classification post-processor
    /// runs at the Middle stage and populates `ExtractedDocument.page_classifications`.
    #[serde(alias = "pageClassification")]
    #[serde(default)]
    pub page_classification: Option<PageClassificationConfig>,
    /// Per-chunk multi-label classification configuration. When set, the
    /// chunk-classification post-processor runs at the Middle stage (after
    /// chunking) and populates `ChunkMetadata.classifications` on every chunk.
    #[serde(alias = "chunkClassification")]
    #[serde(default)]
    pub chunk_classification: Option<ChunkClassificationConfig>,
    /// VLM captioning configuration for extracted images. When set, the captioning
    /// post-processor runs at the Middle stage and writes a caption into each
    /// `ExtractedImage.caption`.
    #[serde(default)]
    pub captioning: Option<CaptioningConfig>,
    /// Enable QR-code detection in extracted images. When `true`, the QR post-processor
    /// runs at the Middle stage and populates `ExtractedImage.qr_codes`.
    #[php(prop, name = "qrCodes")]
    #[serde(alias = "qrCodes")]
    #[serde(default)]
    pub qr_codes: Option<bool>,
}

impl Default for ExtractionConfig {
    fn default() -> Self {
        <xberg::ExtractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl ExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        mimeDetectionPolicy: String,
        useCache: bool,
        enableQualityProcessing: bool,
        forceOcr: bool,
        ocrStrategy: &OcrStrategy,
        disableOcr: bool,
        resultFormat: String,
        outputFormat: String,
        escapeMarkdown: bool,
        tableAnchors: bool,
        jupyterCellRendering: String,
        applyNotebookCellTags: bool,
        useLayoutForMarkdown: bool,
        includeDocumentStructure: bool,
        url: &UrlExtractionConfig,
        maxArchiveDepth: i64,
        ocr: Option<&OcrConfig>,
        forceOcrPages: Option<Vec<u32>>,
        chunking: Option<&ChunkingConfig>,
        contentFilter: Option<&ContentFilterConfig>,
        images: Option<&ImageExtractionConfig>,
        tokenReduction: Option<&TokenReductionOptions>,
        languageDetection: Option<&LanguageDetectionConfig>,
        pages: Option<&PageConfig>,
        postprocessor: Option<&PostProcessorConfig>,
        extractionTimeoutSecs: Option<i64>,
        maxConcurrentExtractions: Option<i64>,
        securityLimits: Option<&SecurityLimits>,
        maxEmbeddedFileBytes: Option<i64>,
        acceleration: Option<&AccelerationConfig>,
        cacheNamespace: Option<String>,
        cacheTtlSecs: Option<i64>,
        email: Option<&EmailConfig>,
        csv: Option<&CsvConfig>,
        geojson: Option<&GeoJsonExtractionConfig>,
        concurrency: Option<&ConcurrencyConfig>,
        structuredExtraction: Option<&StructuredExtractionConfig>,
        ner: Option<&NerConfig>,
        redaction: Option<&RedactionConfig>,
        summarization: Option<&SummarizationConfig>,
        translation: Option<&TranslationConfig>,
        pageClassification: Option<&PageClassificationConfig>,
        chunkClassification: Option<&ChunkClassificationConfig>,
        captioning: Option<&CaptioningConfig>,
        qrCodes: Option<bool>,
    ) -> Self {
        Self {
            mime_detection_policy: mimeDetectionPolicy,
            use_cache: useCache,
            enable_quality_processing: enableQualityProcessing,
            ocr: ocr.cloned(),
            force_ocr: forceOcr,
            ocr_strategy: ocrStrategy.clone(),
            force_ocr_pages: forceOcrPages,
            disable_ocr: disableOcr,
            chunking: chunking.cloned(),
            content_filter: contentFilter.cloned(),
            images: images.cloned(),
            pdf_options: Default::default(),
            token_reduction: tokenReduction.cloned(),
            language_detection: languageDetection.cloned(),
            pages: pages.cloned(),
            keywords: Default::default(),
            postprocessor: postprocessor.cloned(),
            html_options: Default::default(),
            html_output: Default::default(),
            extraction_timeout_secs: extractionTimeoutSecs,
            max_concurrent_extractions: maxConcurrentExtractions,
            result_format: resultFormat,
            security_limits: securityLimits.cloned(),
            max_embedded_file_bytes: maxEmbeddedFileBytes,
            output_format: outputFormat,
            escape_markdown: escapeMarkdown,
            table_anchors: tableAnchors,
            jupyter_cell_rendering: jupyterCellRendering,
            apply_notebook_cell_tags: applyNotebookCellTags,
            layout: Default::default(),
            transcription: Default::default(),
            use_layout_for_markdown: useLayoutForMarkdown,
            include_document_structure: includeDocumentStructure,
            acceleration: acceleration.cloned(),
            cache_namespace: cacheNamespace,
            cache_ttl_secs: cacheTtlSecs,
            email: email.cloned(),
            csv: csv.cloned(),
            geojson: geojson.cloned(),
            concurrency: concurrency.cloned(),
            url: url.clone(),
            max_archive_depth: maxArchiveDepth,
            tree_sitter: Default::default(),
            structured_extraction: structuredExtraction.cloned(),
            ner: ner.cloned(),
            redaction: redaction.cloned(),
            summarization: summarization.cloned(),
            translation: translation.cloned(),
            page_classification: pageClassification.cloned(),
            chunk_classification: chunkClassification.cloned(),
            captioning: captioning.cloned(),
            qr_codes: qrCodes,
        }
    }

    pub fn get_mime_detection_policy(&self) -> String {
        self.mime_detection_policy.clone()
    }

    pub fn get_use_cache(&self) -> bool {
        self.use_cache.clone()
    }

    pub fn get_enable_quality_processing(&self) -> bool {
        self.enable_quality_processing.clone()
    }

    pub fn get_ocr(&self) -> Option<OcrConfig> {
        self.ocr.clone().map(Into::into)
    }

    pub fn set_ocr(&mut self, value: Option<&OcrConfig>) {
        self.ocr = value.map(|value| value.clone().into());
    }

    pub fn get_force_ocr(&self) -> bool {
        self.force_ocr.clone()
    }

    pub fn get_ocr_strategy(&self) -> OcrStrategy {
        self.ocr_strategy.clone()
    }

    pub fn get_force_ocr_pages(&self) -> Option<Vec<u32>> {
        self.force_ocr_pages.clone()
    }

    pub fn get_disable_ocr(&self) -> bool {
        self.disable_ocr.clone()
    }

    pub fn get_chunking(&self) -> Option<ChunkingConfig> {
        self.chunking.clone().map(Into::into)
    }

    pub fn set_chunking(&mut self, value: Option<&ChunkingConfig>) {
        self.chunking = value.map(|value| value.clone().into());
    }

    pub fn get_content_filter(&self) -> Option<ContentFilterConfig> {
        self.content_filter.clone().map(Into::into)
    }

    pub fn set_content_filter(&mut self, value: Option<&ContentFilterConfig>) {
        self.content_filter = value.map(|value| value.clone().into());
    }

    pub fn get_images(&self) -> Option<ImageExtractionConfig> {
        self.images.clone().map(Into::into)
    }

    pub fn set_images(&mut self, value: Option<&ImageExtractionConfig>) {
        self.images = value.map(|value| value.clone().into());
    }

    pub fn get_pdf_options(&self) -> Option<PdfConfig> {
        self.pdf_options.clone().map(Into::into)
    }

    pub fn set_pdf_options(&mut self, value: Option<&PdfConfig>) {
        self.pdf_options = value.map(|value| value.clone().into());
    }

    pub fn get_token_reduction(&self) -> Option<TokenReductionOptions> {
        self.token_reduction.clone().map(Into::into)
    }

    pub fn set_token_reduction(&mut self, value: Option<&TokenReductionOptions>) {
        self.token_reduction = value.map(|value| value.clone().into());
    }

    pub fn get_language_detection(&self) -> Option<LanguageDetectionConfig> {
        self.language_detection.clone().map(Into::into)
    }

    pub fn set_language_detection(&mut self, value: Option<&LanguageDetectionConfig>) {
        self.language_detection = value.map(|value| value.clone().into());
    }

    pub fn get_pages(&self) -> Option<PageConfig> {
        self.pages.clone().map(Into::into)
    }

    pub fn set_pages(&mut self, value: Option<&PageConfig>) {
        self.pages = value.map(|value| value.clone().into());
    }

    pub fn get_keywords(&self) -> Option<KeywordConfig> {
        self.keywords.clone().map(Into::into)
    }

    pub fn set_keywords(&mut self, value: Option<&KeywordConfig>) {
        self.keywords = value.map(|value| value.clone().into());
    }

    pub fn get_postprocessor(&self) -> Option<PostProcessorConfig> {
        self.postprocessor.clone().map(Into::into)
    }

    pub fn set_postprocessor(&mut self, value: Option<&PostProcessorConfig>) {
        self.postprocessor = value.map(|value| value.clone().into());
    }

    pub fn get_html_options(&self) -> Option<ConversionOptions> {
        self.html_options.clone().map(Into::into)
    }

    pub fn set_html_options(&mut self, value: Option<&ConversionOptions>) {
        self.html_options = value.map(|value| value.clone().into());
    }

    pub fn get_html_output(&self) -> Option<HtmlOutputConfig> {
        self.html_output.clone().map(Into::into)
    }

    pub fn set_html_output(&mut self, value: Option<&HtmlOutputConfig>) {
        self.html_output = value.map(|value| value.clone().into());
    }

    pub fn get_extraction_timeout_secs(&self) -> Option<i64> {
        self.extraction_timeout_secs.clone()
    }

    pub fn get_max_concurrent_extractions(&self) -> Option<i64> {
        self.max_concurrent_extractions.clone()
    }

    pub fn get_result_format(&self) -> String {
        self.result_format.clone()
    }

    pub fn get_security_limits(&self) -> Option<SecurityLimits> {
        self.security_limits.clone().map(Into::into)
    }

    pub fn set_security_limits(&mut self, value: Option<&SecurityLimits>) {
        self.security_limits = value.map(|value| value.clone().into());
    }

    pub fn get_max_embedded_file_bytes(&self) -> Option<i64> {
        self.max_embedded_file_bytes.clone()
    }

    pub fn get_output_format(&self) -> String {
        self.output_format.clone()
    }

    pub fn get_escape_markdown(&self) -> bool {
        self.escape_markdown.clone()
    }

    pub fn get_table_anchors(&self) -> bool {
        self.table_anchors.clone()
    }

    pub fn get_jupyter_cell_rendering(&self) -> String {
        self.jupyter_cell_rendering.clone()
    }

    pub fn get_apply_notebook_cell_tags(&self) -> bool {
        self.apply_notebook_cell_tags.clone()
    }

    pub fn get_layout(&self) -> Option<LayoutDetectionConfig> {
        self.layout.clone().map(Into::into)
    }

    pub fn set_layout(&mut self, value: Option<&LayoutDetectionConfig>) {
        self.layout = value.map(|value| value.clone().into());
    }

    pub fn get_transcription(&self) -> Option<TranscriptionConfig> {
        self.transcription.clone().map(Into::into)
    }

    pub fn set_transcription(&mut self, value: Option<&TranscriptionConfig>) {
        self.transcription = value.map(|value| value.clone().into());
    }

    pub fn get_use_layout_for_markdown(&self) -> bool {
        self.use_layout_for_markdown.clone()
    }

    pub fn get_include_document_structure(&self) -> bool {
        self.include_document_structure.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_cache_namespace(&self) -> Option<String> {
        self.cache_namespace.clone()
    }

    pub fn get_cache_ttl_secs(&self) -> Option<i64> {
        self.cache_ttl_secs.clone()
    }

    pub fn get_email(&self) -> Option<EmailConfig> {
        self.email.clone().map(Into::into)
    }

    pub fn set_email(&mut self, value: Option<&EmailConfig>) {
        self.email = value.map(|value| value.clone().into());
    }

    pub fn get_csv(&self) -> Option<CsvConfig> {
        self.csv.clone().map(Into::into)
    }

    pub fn set_csv(&mut self, value: Option<&CsvConfig>) {
        self.csv = value.map(|value| value.clone().into());
    }

    pub fn get_geojson(&self) -> Option<GeoJsonExtractionConfig> {
        self.geojson.clone().map(Into::into)
    }

    pub fn set_geojson(&mut self, value: Option<&GeoJsonExtractionConfig>) {
        self.geojson = value.map(|value| value.clone().into());
    }

    pub fn get_concurrency(&self) -> Option<ConcurrencyConfig> {
        self.concurrency.clone().map(Into::into)
    }

    pub fn set_concurrency(&mut self, value: Option<&ConcurrencyConfig>) {
        self.concurrency = value.map(|value| value.clone().into());
    }

    pub fn get_url(&self) -> UrlExtractionConfig {
        self.url.clone()
    }

    pub fn get_max_archive_depth(&self) -> i64 {
        self.max_archive_depth.clone()
    }

    pub fn get_tree_sitter(&self) -> Option<TreeSitterConfig> {
        self.tree_sitter.clone().map(Into::into)
    }

    pub fn set_tree_sitter(&mut self, value: Option<&TreeSitterConfig>) {
        self.tree_sitter = value.map(|value| value.clone().into());
    }

    pub fn get_structured_extraction(&self) -> Option<StructuredExtractionConfig> {
        self.structured_extraction.clone().map(Into::into)
    }

    pub fn set_structured_extraction(&mut self, value: Option<&StructuredExtractionConfig>) {
        self.structured_extraction = value.map(|value| value.clone().into());
    }

    pub fn get_ner(&self) -> Option<NerConfig> {
        self.ner.clone().map(Into::into)
    }

    pub fn set_ner(&mut self, value: Option<&NerConfig>) {
        self.ner = value.map(|value| value.clone().into());
    }

    pub fn get_redaction(&self) -> Option<RedactionConfig> {
        self.redaction.clone().map(Into::into)
    }

    pub fn set_redaction(&mut self, value: Option<&RedactionConfig>) {
        self.redaction = value.map(|value| value.clone().into());
    }

    pub fn get_summarization(&self) -> Option<SummarizationConfig> {
        self.summarization.clone().map(Into::into)
    }

    pub fn set_summarization(&mut self, value: Option<&SummarizationConfig>) {
        self.summarization = value.map(|value| value.clone().into());
    }

    pub fn get_translation(&self) -> Option<TranslationConfig> {
        self.translation.clone().map(Into::into)
    }

    pub fn set_translation(&mut self, value: Option<&TranslationConfig>) {
        self.translation = value.map(|value| value.clone().into());
    }

    pub fn get_page_classification(&self) -> Option<PageClassificationConfig> {
        self.page_classification.clone().map(Into::into)
    }

    pub fn set_page_classification(&mut self, value: Option<&PageClassificationConfig>) {
        self.page_classification = value.map(|value| value.clone().into());
    }

    pub fn get_chunk_classification(&self) -> Option<ChunkClassificationConfig> {
        self.chunk_classification.clone().map(Into::into)
    }

    pub fn set_chunk_classification(&mut self, value: Option<&ChunkClassificationConfig>) {
        self.chunk_classification = value.map(|value| value.clone().into());
    }

    pub fn get_captioning(&self) -> Option<CaptioningConfig> {
        self.captioning.clone().map(Into::into)
    }

    pub fn set_captioning(&mut self, value: Option<&CaptioningConfig>) {
        self.captioning = value.map(|value| value.clone().into());
    }

    pub fn get_qr_codes(&self) -> Option<bool> {
        self.qr_codes.clone()
    }

    pub fn validate(&self) -> PhpResult<()> {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ExtractionConfig {
            mime_detection_policy: match self.mime_detection_policy.as_str() {
                "prefer_content" => xberg::MimeDetectionPolicy::PreferContent,
                "trust_extension" => xberg::MimeDetectionPolicy::TrustExtension,
                "content_only" => xberg::MimeDetectionPolicy::ContentOnly,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'mime_detection_policy' of MimeDetectionPolicy: expected one of [prefer_content, trust_extension, content_only]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            use_cache: self.use_cache,
            enable_quality_processing: self.enable_quality_processing,
            ocr: self.ocr.clone().map(Into::into),
            force_ocr: self.force_ocr,
            ocr_strategy: self.ocr_strategy.clone().into(),
            force_ocr_pages: self.force_ocr_pages.clone(),
            disable_ocr: self.disable_ocr,
            chunking: self.chunking.clone().map(Into::into),
            content_filter: self.content_filter.clone().map(Into::into),
            images: self.images.clone().map(Into::into),
            token_reduction: self.token_reduction.clone().map(Into::into),
            language_detection: self.language_detection.clone().map(Into::into),
            pages: self.pages.clone().map(Into::into),
            postprocessor: self.postprocessor.clone().map(Into::into),
            extraction_timeout_secs: self.extraction_timeout_secs.map(|v| v as u64),
            max_concurrent_extractions: self.max_concurrent_extractions.map(|v| v as usize),
            result_format: match self.result_format.as_str() {
                "unified" => xberg::ResultFormat::Unified,
                "element_based" => xberg::ResultFormat::ElementBased,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'result_format' of ResultFormat: expected one of [unified, element_based]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            security_limits: self.security_limits.clone().map(Into::into),
            max_embedded_file_bytes: self.max_embedded_file_bytes.map(|v| v as u64),
            output_format: match self.output_format.as_str() {
                "plain" => xberg::OutputFormat::Plain,
                "markdown" => xberg::OutputFormat::Markdown,
                "djot" => xberg::OutputFormat::Djot,
                "html" => xberg::OutputFormat::Html,
                "json" => xberg::OutputFormat::Json,
                "doctags" => xberg::OutputFormat::DocTags,
                "custom" => xberg::OutputFormat::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            escape_markdown: self.escape_markdown,
            table_anchors: self.table_anchors,
            jupyter_cell_rendering: match self.jupyter_cell_rendering.as_str() {
                "source" => xberg::JupyterCellRendering::Source,
                "outputs" => xberg::JupyterCellRendering::Outputs,
                "both" => xberg::JupyterCellRendering::Both,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'jupyter_cell_rendering' of JupyterCellRendering: expected one of [source, outputs, both]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            apply_notebook_cell_tags: self.apply_notebook_cell_tags,
            use_layout_for_markdown: self.use_layout_for_markdown,
            include_document_structure: self.include_document_structure,
            acceleration: self.acceleration.clone().map(Into::into),
            cache_namespace: self.cache_namespace.clone(),
            cache_ttl_secs: self.cache_ttl_secs.map(|v| v as u64),
            email: self.email.clone().map(Into::into),
            csv: self.csv.clone().map(Into::into),
            geojson: self.geojson.clone().map(Into::into),
            concurrency: self.concurrency.clone().map(Into::into),
            url: self.url.clone().into(),
            max_archive_depth: self.max_archive_depth as usize,
            structured_extraction: self.structured_extraction.clone().map(Into::into),
            ner: self.ner.clone().map(Into::into),
            redaction: self.redaction.clone().map(Into::into),
            summarization: self.summarization.clone().map(Into::into),
            translation: self.translation.clone().map(Into::into),
            page_classification: self.page_classification.clone().map(Into::into),
            chunk_classification: self.chunk_classification.clone().map(Into::into),
            captioning: self.captioning.clone().map(Into::into),
            qr_codes: self.qr_codes,
            ..Default::default()
        };
        let result = core_self
            .validate()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    pub fn needs_image_data(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ExtractionConfig {
            mime_detection_policy: match self.mime_detection_policy.as_str() {
                "prefer_content" => xberg::MimeDetectionPolicy::PreferContent,
                "trust_extension" => xberg::MimeDetectionPolicy::TrustExtension,
                "content_only" => xberg::MimeDetectionPolicy::ContentOnly,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'mime_detection_policy' of MimeDetectionPolicy: expected one of [prefer_content, trust_extension, content_only]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            use_cache: self.use_cache,
            enable_quality_processing: self.enable_quality_processing,
            ocr: self.ocr.clone().map(Into::into),
            force_ocr: self.force_ocr,
            ocr_strategy: self.ocr_strategy.clone().into(),
            force_ocr_pages: self.force_ocr_pages.clone(),
            disable_ocr: self.disable_ocr,
            chunking: self.chunking.clone().map(Into::into),
            content_filter: self.content_filter.clone().map(Into::into),
            images: self.images.clone().map(Into::into),
            token_reduction: self.token_reduction.clone().map(Into::into),
            language_detection: self.language_detection.clone().map(Into::into),
            pages: self.pages.clone().map(Into::into),
            postprocessor: self.postprocessor.clone().map(Into::into),
            extraction_timeout_secs: self.extraction_timeout_secs.map(|v| v as u64),
            max_concurrent_extractions: self.max_concurrent_extractions.map(|v| v as usize),
            result_format: match self.result_format.as_str() {
                "unified" => xberg::ResultFormat::Unified,
                "element_based" => xberg::ResultFormat::ElementBased,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'result_format' of ResultFormat: expected one of [unified, element_based]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            security_limits: self.security_limits.clone().map(Into::into),
            max_embedded_file_bytes: self.max_embedded_file_bytes.map(|v| v as u64),
            output_format: match self.output_format.as_str() {
                "plain" => xberg::OutputFormat::Plain,
                "markdown" => xberg::OutputFormat::Markdown,
                "djot" => xberg::OutputFormat::Djot,
                "html" => xberg::OutputFormat::Html,
                "json" => xberg::OutputFormat::Json,
                "doctags" => xberg::OutputFormat::DocTags,
                "custom" => xberg::OutputFormat::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            escape_markdown: self.escape_markdown,
            table_anchors: self.table_anchors,
            jupyter_cell_rendering: match self.jupyter_cell_rendering.as_str() {
                "source" => xberg::JupyterCellRendering::Source,
                "outputs" => xberg::JupyterCellRendering::Outputs,
                "both" => xberg::JupyterCellRendering::Both,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'jupyter_cell_rendering' of JupyterCellRendering: expected one of [source, outputs, both]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            apply_notebook_cell_tags: self.apply_notebook_cell_tags,
            use_layout_for_markdown: self.use_layout_for_markdown,
            include_document_structure: self.include_document_structure,
            acceleration: self.acceleration.clone().map(Into::into),
            cache_namespace: self.cache_namespace.clone(),
            cache_ttl_secs: self.cache_ttl_secs.map(|v| v as u64),
            email: self.email.clone().map(Into::into),
            csv: self.csv.clone().map(Into::into),
            geojson: self.geojson.clone().map(Into::into),
            concurrency: self.concurrency.clone().map(Into::into),
            url: self.url.clone().into(),
            max_archive_depth: self.max_archive_depth as usize,
            structured_extraction: self.structured_extraction.clone().map(Into::into),
            ner: self.ner.clone().map(Into::into),
            redaction: self.redaction.clone().map(Into::into),
            summarization: self.summarization.clone().map(Into::into),
            translation: self.translation.clone().map(Into::into),
            page_classification: self.page_classification.clone().map(Into::into),
            chunk_classification: self.chunk_classification.clone().map(Into::into),
            captioning: self.captioning.clone().map(Into::into),
            qr_codes: self.qr_codes,
            ..Default::default()
        };
        core_self.needs_image_data()
    }

    pub fn needs_image_processing(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ExtractionConfig {
            mime_detection_policy: match self.mime_detection_policy.as_str() {
                "prefer_content" => xberg::MimeDetectionPolicy::PreferContent,
                "trust_extension" => xberg::MimeDetectionPolicy::TrustExtension,
                "content_only" => xberg::MimeDetectionPolicy::ContentOnly,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'mime_detection_policy' of MimeDetectionPolicy: expected one of [prefer_content, trust_extension, content_only]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            use_cache: self.use_cache,
            enable_quality_processing: self.enable_quality_processing,
            ocr: self.ocr.clone().map(Into::into),
            force_ocr: self.force_ocr,
            ocr_strategy: self.ocr_strategy.clone().into(),
            force_ocr_pages: self.force_ocr_pages.clone(),
            disable_ocr: self.disable_ocr,
            chunking: self.chunking.clone().map(Into::into),
            content_filter: self.content_filter.clone().map(Into::into),
            images: self.images.clone().map(Into::into),
            token_reduction: self.token_reduction.clone().map(Into::into),
            language_detection: self.language_detection.clone().map(Into::into),
            pages: self.pages.clone().map(Into::into),
            postprocessor: self.postprocessor.clone().map(Into::into),
            extraction_timeout_secs: self.extraction_timeout_secs.map(|v| v as u64),
            max_concurrent_extractions: self.max_concurrent_extractions.map(|v| v as usize),
            result_format: match self.result_format.as_str() {
                "unified" => xberg::ResultFormat::Unified,
                "element_based" => xberg::ResultFormat::ElementBased,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'result_format' of ResultFormat: expected one of [unified, element_based]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            security_limits: self.security_limits.clone().map(Into::into),
            max_embedded_file_bytes: self.max_embedded_file_bytes.map(|v| v as u64),
            output_format: match self.output_format.as_str() {
                "plain" => xberg::OutputFormat::Plain,
                "markdown" => xberg::OutputFormat::Markdown,
                "djot" => xberg::OutputFormat::Djot,
                "html" => xberg::OutputFormat::Html,
                "json" => xberg::OutputFormat::Json,
                "doctags" => xberg::OutputFormat::DocTags,
                "custom" => xberg::OutputFormat::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            escape_markdown: self.escape_markdown,
            table_anchors: self.table_anchors,
            jupyter_cell_rendering: match self.jupyter_cell_rendering.as_str() {
                "source" => xberg::JupyterCellRendering::Source,
                "outputs" => xberg::JupyterCellRendering::Outputs,
                "both" => xberg::JupyterCellRendering::Both,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'jupyter_cell_rendering' of JupyterCellRendering: expected one of [source, outputs, both]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            apply_notebook_cell_tags: self.apply_notebook_cell_tags,
            use_layout_for_markdown: self.use_layout_for_markdown,
            include_document_structure: self.include_document_structure,
            acceleration: self.acceleration.clone().map(Into::into),
            cache_namespace: self.cache_namespace.clone(),
            cache_ttl_secs: self.cache_ttl_secs.map(|v| v as u64),
            email: self.email.clone().map(Into::into),
            csv: self.csv.clone().map(Into::into),
            geojson: self.geojson.clone().map(Into::into),
            concurrency: self.concurrency.clone().map(Into::into),
            url: self.url.clone().into(),
            max_archive_depth: self.max_archive_depth as usize,
            structured_extraction: self.structured_extraction.clone().map(Into::into),
            ner: self.ner.clone().map(Into::into),
            redaction: self.redaction.clone().map(Into::into),
            summarization: self.summarization.clone().map(Into::into),
            translation: self.translation.clone().map(Into::into),
            page_classification: self.page_classification.clone().map(Into::into),
            chunk_classification: self.chunk_classification.clone().map(Into::into),
            captioning: self.captioning.clone().map(Into::into),
            qr_codes: self.qr_codes,
            ..Default::default()
        };
        core_self.needs_image_processing()
    }

    /// Default `Self::max_archive_depth`: 3 levels of nested archives.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultArchiveDepth")]
    pub fn default_archive_depth() -> i64 {
        xberg::ExtractionConfig::default_archive_depth() as i64
    }

    /// Default per-embedded-file cap: 50 MiB.
    ///
    /// A single embedded object larger than this can consume significant memory
    /// when the recursive extractor materialises it. 50 MiB is generous for
    /// real-world embedded documents while still bounding worst-case allocation.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultMaxEmbeddedFileBytes")]
    pub fn default_max_embedded_file_bytes() -> Option<i64> {
        xberg::ExtractionConfig::default_max_embedded_file_bytes().map(|v| v as i64)
    }

    /// Default extraction timeout: 600 seconds (10 minutes).
    ///
    /// Pathological files (deeply nested archives, sheets with millions of cells,
    /// adversarial PDFs) can otherwise run indefinitely and exhaust caller
    /// resources. 600 s bounds the worst-case cost of a single untrusted input
    /// while giving legitimate but slow paths — VLM-based OCR, large scanned
    /// documents — enough headroom to finish instead of being cut off at the
    /// previous 60 s default.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultExtractionTimeout")]
    pub fn default_extraction_timeout() -> Option<i64> {
        xberg::ExtractionConfig::default_extraction_timeout().map(|v| v as i64)
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ExtractionConfig {
        xberg::ExtractionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractionDiff")]
#[serde(default)]
pub struct ExtractionDiff {
    /// Unified-diff hunks for the `content` field.
    ///
    /// Empty when the content is identical.
    #[serde(alias = "contentDiff")]
    pub content_diff: Vec<DiffHunk>,
    /// Tables present in `b` but not in `a` (by index position, excess right-side tables).
    #[serde(alias = "tablesAdded")]
    pub tables_added: Vec<Table>,
    /// Tables present in `a` but not in `b` (by index position, excess left-side tables).
    #[serde(alias = "tablesRemoved")]
    pub tables_removed: Vec<Table>,
    /// Cell-level changes for table pairs that share the same index and dimensions.
    #[serde(alias = "tablesChanged")]
    pub tables_changed: Vec<TableDiff>,
    /// Metadata difference, encoded as a JSON object with three top-level keys:
    /// `added` (keys present in `b` but not `a`), `removed` (keys present in `a`
    /// but not `b`), and `changed` (keys whose values differ — each entry is
    /// `{ "from": <value-in-a>, "to": <value-in-b> }`).
    ///
    /// This is NOT RFC 6902 JSON Patch — we deliberately chose a flatter shape
    /// to avoid pulling in a json-patch crate. If you need RFC 6902 semantics
    /// (with JSON Pointer paths) feed `a.metadata` and `b.metadata` to your
    /// preferred json-patch impl directly.
    #[serde(alias = "metadataChanged")]
    pub metadata_changed: serde_json::Value,
    /// Changes to embedded archive children.
    #[serde(alias = "embeddedChanges")]
    pub embedded_changes: EmbeddedChanges,
}

impl Default for ExtractionDiff {
    fn default() -> Self {
        <xberg::ExtractionDiff as Default>::default().into()
    }
}

#[php_impl]
impl ExtractionDiff {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        contentDiff: &ext_php_rs::types::ZendHashTable,
        tablesAdded: &ext_php_rs::types::ZendHashTable,
        tablesRemoved: &ext_php_rs::types::ZendHashTable,
        tablesChanged: &ext_php_rs::types::ZendHashTable,
        metadataChanged: String,
        embeddedChanges: &EmbeddedChanges,
    ) -> PhpResult<Self> {
        let mut contentDiff_core_result: Vec<DiffHunk> = Vec::new();
        for (_, item) in contentDiff.iter() {
            if let Some(parsed) = <&DiffHunk as ext_php_rs::convert::FromZval>::from_zval(item) {
                contentDiff_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DiffHunk".to_string(),
                ));
            }
        }
        let contentDiff_core: Vec<DiffHunk> = contentDiff_core_result;
        let mut tablesAdded_core_result: Vec<Table> = Vec::new();
        for (_, item) in tablesAdded.iter() {
            if let Some(parsed) = <&Table as ext_php_rs::convert::FromZval>::from_zval(item) {
                tablesAdded_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Table".to_string(),
                ));
            }
        }
        let tablesAdded_core: Vec<Table> = tablesAdded_core_result;
        let mut tablesRemoved_core_result: Vec<Table> = Vec::new();
        for (_, item) in tablesRemoved.iter() {
            if let Some(parsed) = <&Table as ext_php_rs::convert::FromZval>::from_zval(item) {
                tablesRemoved_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Table".to_string(),
                ));
            }
        }
        let tablesRemoved_core: Vec<Table> = tablesRemoved_core_result;
        let mut tablesChanged_core_result: Vec<TableDiff> = Vec::new();
        for (_, item) in tablesChanged.iter() {
            if let Some(parsed) = <&TableDiff as ext_php_rs::convert::FromZval>::from_zval(item) {
                tablesChanged_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to TableDiff".to_string(),
                ));
            }
        }
        let tablesChanged_core: Vec<TableDiff> = tablesChanged_core_result;
        Ok(Self {
            content_diff: contentDiff_core,
            tables_added: tablesAdded_core,
            tables_removed: tablesRemoved_core,
            tables_changed: tablesChanged_core,
            metadata_changed: serde_json::from_str(&metadataChanged)
                .map_err(|e| PhpException::default(e.to_string()))?,
            embedded_changes: embeddedChanges.clone(),
        })
    }

    pub fn get_content_diff(&self) -> Vec<DiffHunk> {
        self.content_diff.clone()
    }

    pub fn get_tables_added(&self) -> Vec<Table> {
        self.tables_added.clone()
    }

    pub fn get_tables_removed(&self) -> Vec<Table> {
        self.tables_removed.clone()
    }

    pub fn get_tables_changed(&self) -> Vec<TableDiff> {
        self.tables_changed.clone()
    }

    pub fn get_metadata_changed(&self) -> Option<String> {
        serde_json::to_string(&self.metadata_changed).ok()
    }

    pub fn get_embedded_changes(&self) -> EmbeddedChanges {
        self.embedded_changes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ExtractionErrorItem")]
#[serde(default)]
pub struct ExtractionErrorItem {
    /// Input index in the original request.
    #[php(prop, name = "index")]
    pub index: i64,
    /// Stable numeric error code.
    #[php(prop, name = "code")]
    pub code: u32,
    /// Stable snake_case error kind.
    #[php(prop, name = "errorType")]
    #[serde(alias = "errorType")]
    pub error_type: String,
    /// Best-effort source identifier.
    #[php(prop, name = "source")]
    pub source: String,
    /// Error message.
    #[php(prop, name = "message")]
    pub message: String,
}

#[php_impl]
impl ExtractionErrorItem {
    #[php(constructor)]
    pub fn new(index: i64, code: u32, errorType: String, source: String, message: String) -> Self {
        Self {
            index,
            code,
            error_type: errorType,
            source,
            message,
        }
    }

    pub fn get_index(&self) -> i64 {
        self.index.clone()
    }

    pub fn get_code(&self) -> u32 {
        self.code.clone()
    }

    pub fn get_error_type(&self) -> String {
        self.error_type.clone()
    }

    pub fn get_source(&self) -> String {
        self.source.clone()
    }

    pub fn get_message(&self) -> String {
        self.message.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractionResult")]
#[serde(default)]
pub struct ExtractionResult {
    /// Extracted documents in discovery order.
    pub results: Vec<ExtractedDocument>,
    /// Non-fatal per-input errors.
    #[serde(default)]
    pub errors: Vec<ExtractionErrorItem>,
    /// Aggregate counts for the operation.
    pub summary: ExtractionSummary,
    /// Final URLs reached after redirects during URL ingestion.
    #[php(prop, name = "crawlFinalUrls")]
    #[serde(alias = "crawlFinalUrls")]
    #[serde(default)]
    pub crawl_final_urls: Vec<String>,
    /// Total redirects followed while fetching or crawling URLs.
    #[php(prop, name = "crawlRedirectCount")]
    #[serde(alias = "crawlRedirectCount")]
    #[serde(default)]
    pub crawl_redirect_count: i64,
    /// Unique normalized URLs discovered by crawls.
    #[php(prop, name = "crawlUniqueNormalizedUrls")]
    #[serde(alias = "crawlUniqueNormalizedUrls")]
    #[serde(default)]
    pub crawl_unique_normalized_urls: Vec<String>,
}

impl Default for ExtractionResult {
    fn default() -> Self {
        <xberg::ExtractionResult as Default>::default().into()
    }
}

#[php_impl]
impl ExtractionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        results: &ext_php_rs::types::ZendHashTable,
        errors: &ext_php_rs::types::ZendHashTable,
        summary: &ExtractionSummary,
        crawlFinalUrls: Vec<String>,
        crawlRedirectCount: i64,
        crawlUniqueNormalizedUrls: Vec<String>,
    ) -> PhpResult<Self> {
        let mut results_core_result: Vec<ExtractedDocument> = Vec::new();
        for (_, item) in results.iter() {
            if let Some(parsed) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(item) {
                results_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ExtractedDocument".to_string(),
                ));
            }
        }
        let results_core: Vec<ExtractedDocument> = results_core_result;
        let mut errors_core_result: Vec<ExtractionErrorItem> = Vec::new();
        for (_, item) in errors.iter() {
            if let Some(parsed) = <&ExtractionErrorItem as ext_php_rs::convert::FromZval>::from_zval(item) {
                errors_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ExtractionErrorItem".to_string(),
                ));
            }
        }
        let errors_core: Vec<ExtractionErrorItem> = errors_core_result;
        Ok(Self {
            results: results_core,
            errors: errors_core,
            summary: summary.clone(),
            crawl_final_urls: crawlFinalUrls,
            crawl_redirect_count: crawlRedirectCount,
            crawl_unique_normalized_urls: crawlUniqueNormalizedUrls,
        })
    }

    pub fn get_results(&self) -> Vec<ExtractedDocument> {
        self.results.clone()
    }

    pub fn get_errors(&self) -> Vec<ExtractionErrorItem> {
        self.errors.clone()
    }

    pub fn get_summary(&self) -> ExtractionSummary {
        self.summary.clone()
    }

    pub fn get_crawl_final_urls(&self) -> Vec<String> {
        self.crawl_final_urls.clone()
    }

    pub fn get_crawl_redirect_count(&self) -> i64 {
        self.crawl_redirect_count.clone()
    }

    pub fn get_crawl_unique_normalized_urls(&self) -> Vec<String> {
        self.crawl_unique_normalized_urls.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ExtractionSummary")]
#[serde(default)]
pub struct ExtractionSummary {
    /// Number of inputs submitted by the caller.
    #[php(prop, name = "inputs")]
    pub inputs: i64,
    /// Number of extraction results produced.
    #[php(prop, name = "results")]
    pub results: i64,
    /// Number of per-input errors.
    #[php(prop, name = "errors")]
    pub errors: i64,
    /// Number of URI inputs that resolved to remote HTTP(S) URLs.
    #[php(prop, name = "remoteUrls")]
    #[serde(alias = "remoteUrls")]
    pub remote_urls: i64,
    /// Number of HTML pages crawled or scraped.
    #[php(prop, name = "pagesCrawled")]
    #[serde(alias = "pagesCrawled")]
    pub pages_crawled: i64,
    /// Number of downloaded non-HTML documents extracted from URLs.
    #[php(prop, name = "documentsDownloaded")]
    #[serde(alias = "documentsDownloaded")]
    pub documents_downloaded: i64,
}

impl Default for ExtractionSummary {
    fn default() -> Self {
        <xberg::ExtractionSummary as Default>::default().into()
    }
}

#[php_impl]
impl ExtractionSummary {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        inputs: i64,
        results: i64,
        errors: i64,
        remoteUrls: i64,
        pagesCrawled: i64,
        documentsDownloaded: i64,
    ) -> Self {
        Self {
            inputs,
            results,
            errors,
            remote_urls: remoteUrls,
            pages_crawled: pagesCrawled,
            documents_downloaded: documentsDownloaded,
        }
    }

    pub fn get_inputs(&self) -> i64 {
        self.inputs.clone()
    }

    pub fn get_results(&self) -> i64 {
        self.results.clone()
    }

    pub fn get_errors(&self) -> i64 {
        self.errors.clone()
    }

    pub fn get_remote_urls(&self) -> i64 {
        self.remote_urls.clone()
    }

    pub fn get_pages_crawled(&self) -> i64 {
        self.pages_crawled.clone()
    }

    pub fn get_documents_downloaded(&self) -> i64 {
        self.documents_downloaded.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\FictionBookMetadata")]
#[serde(default)]
pub struct FictionBookMetadata {
    /// Genre tags as declared in the FB2 `<genre>` elements.
    #[php(prop, name = "genres")]
    #[serde(default)]
    pub genres: Vec<String>,
    /// Book series (sequence) names, if any.
    #[php(prop, name = "sequences")]
    #[serde(default)]
    pub sequences: Vec<String>,
    /// Short annotation / summary from the FB2 `<annotation>` element.
    #[php(prop, name = "annotation")]
    pub annotation: Option<String>,
}

impl Default for FictionBookMetadata {
    fn default() -> Self {
        <xberg::FictionBookMetadata as Default>::default().into()
    }
}

#[php_impl]
impl FictionBookMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(genres: Vec<String>, sequences: Vec<String>, annotation: Option<String>) -> Self {
        Self {
            genres,
            sequences,
            annotation,
        }
    }

    pub fn get_genres(&self) -> Vec<String> {
        self.genres.clone()
    }

    pub fn get_sequences(&self) -> Vec<String> {
        self.sequences.clone()
    }

    pub fn get_annotation(&self) -> Option<String> {
        self.annotation.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\FileExtractionConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct FileExtractionConfig {
    /// Override MIME inference policy for this file.
    #[php(prop, name = "mimeDetectionPolicy")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "mimeDetectionPolicy")]
    pub mime_detection_policy: Option<String>,
    /// Override quality post-processing for this file.
    #[php(prop, name = "enableQualityProcessing")]
    #[serde(alias = "enableQualityProcessing")]
    pub enable_quality_processing: Option<bool>,
    /// Override OCR configuration for this file (undefined in the Option = use batch default).
    pub ocr: Option<OcrConfig>,
    /// Override force OCR for this file.
    #[php(prop, name = "forceOcr")]
    #[serde(alias = "forceOcr")]
    pub force_ocr: Option<bool>,
    /// Override the OCR page-selection strategy for this file.
    #[serde(alias = "ocrStrategy")]
    pub ocr_strategy: Option<OcrStrategy>,
    /// Override force OCR pages for this file (1-indexed page numbers).
    #[php(prop, name = "forceOcrPages")]
    #[serde(alias = "forceOcrPages")]
    pub force_ocr_pages: Option<Vec<u32>>,
    /// Override disable OCR for this file.
    #[php(prop, name = "disableOcr")]
    #[serde(alias = "disableOcr")]
    pub disable_ocr: Option<bool>,
    /// Override chunking configuration for this file.
    pub chunking: Option<ChunkingConfig>,
    /// Override content filtering configuration for this file.
    #[serde(alias = "contentFilter")]
    pub content_filter: Option<ContentFilterConfig>,
    /// Override image extraction configuration for this file.
    pub images: Option<ImageExtractionConfig>,
    /// Override PDF options for this file.
    #[serde(alias = "pdfOptions")]
    pub pdf_options: Option<PdfConfig>,
    /// Override token reduction for this file.
    #[serde(alias = "tokenReduction")]
    pub token_reduction: Option<TokenReductionOptions>,
    /// Override language detection for this file.
    #[serde(alias = "languageDetection")]
    pub language_detection: Option<LanguageDetectionConfig>,
    /// Override page extraction for this file.
    pub pages: Option<PageConfig>,
    /// Override keyword extraction for this file.
    pub keywords: Option<KeywordConfig>,
    /// Override post-processor for this file.
    pub postprocessor: Option<PostProcessorConfig>,
    /// Override HTML conversion options for this file.
    #[serde(alias = "htmlOptions")]
    pub html_options: Option<ConversionOptions>,
    /// Override styled HTML output configuration for this file.
    #[serde(alias = "htmlOutput")]
    pub html_output: Option<HtmlOutputConfig>,
    /// Override result format for this file.
    #[php(prop, name = "resultFormat")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "resultFormat")]
    pub result_format: Option<String>,
    /// Override output content format for this file.
    #[php(prop, name = "outputFormat")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "outputFormat")]
    pub output_format: Option<String>,
    /// Override document structure output for this file.
    #[php(prop, name = "includeDocumentStructure")]
    #[serde(alias = "includeDocumentStructure")]
    pub include_document_structure: Option<bool>,
    /// Override layout detection for this file.
    pub layout: Option<LayoutDetectionConfig>,
    /// Transcription configuration (see ExtractionConfig for docs).
    pub transcription: Option<TranscriptionConfig>,
    /// Override per-file extraction timeout in seconds.
    ///
    /// When set, the extraction for this file will be canceled after the
    /// specified duration. A timed-out file produces an error result without
    /// affecting other files in the batch.
    #[php(prop, name = "timeoutSecs")]
    #[serde(alias = "timeoutSecs")]
    pub timeout_secs: Option<i64>,
    /// Override tree-sitter configuration for this file.
    #[serde(alias = "treeSitter")]
    pub tree_sitter: Option<TreeSitterConfig>,
    /// Override structured extraction configuration for this file.
    ///
    /// When set, enables LLM-based structured extraction with a JSON schema
    /// for this specific file. The extracted content is sent to a VLM/LLM
    /// and the response is parsed according to the provided schema.
    #[serde(alias = "structuredExtraction")]
    pub structured_extraction: Option<StructuredExtractionConfig>,
    /// Override URL ingestion and crawl configuration for this file.
    pub url: Option<UrlExtractionConfig>,
    /// Override named-entity recognition configuration for this file.
    pub ner: Option<NerConfig>,
    /// Override redaction configuration for this file.
    pub redaction: Option<RedactionConfig>,
    /// Override summarization configuration for this file.
    pub summarization: Option<SummarizationConfig>,
    /// Override translation configuration for this file.
    pub translation: Option<TranslationConfig>,
    /// Override per-page classification configuration for this file.
    #[serde(alias = "pageClassification")]
    pub page_classification: Option<PageClassificationConfig>,
    /// Override per-chunk classification configuration for this file.
    #[serde(alias = "chunkClassification")]
    pub chunk_classification: Option<ChunkClassificationConfig>,
    /// Override VLM captioning configuration for this file.
    pub captioning: Option<CaptioningConfig>,
    /// Override QR-code detection for this file.
    #[php(prop, name = "qrCodes")]
    #[serde(alias = "qrCodes")]
    pub qr_codes: Option<bool>,
}

impl Default for FileExtractionConfig {
    fn default() -> Self {
        <xberg::FileExtractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl FileExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_mime_detection_policy(&self) -> Option<String> {
        self.mime_detection_policy.clone().map(Into::into)
    }

    pub fn get_enable_quality_processing(&self) -> Option<bool> {
        self.enable_quality_processing.clone()
    }

    pub fn get_ocr(&self) -> Option<OcrConfig> {
        self.ocr.clone().map(Into::into)
    }

    pub fn set_ocr(&mut self, value: Option<&OcrConfig>) {
        self.ocr = value.map(|value| value.clone().into());
    }

    pub fn get_force_ocr(&self) -> Option<bool> {
        self.force_ocr.clone()
    }

    pub fn get_ocr_strategy(&self) -> Option<OcrStrategy> {
        self.ocr_strategy.clone().map(Into::into)
    }

    pub fn set_ocr_strategy(&mut self, value: Option<&OcrStrategy>) {
        self.ocr_strategy = value.map(|value| value.clone().into());
    }

    pub fn get_force_ocr_pages(&self) -> Option<Vec<u32>> {
        self.force_ocr_pages.clone()
    }

    pub fn get_disable_ocr(&self) -> Option<bool> {
        self.disable_ocr.clone()
    }

    pub fn get_chunking(&self) -> Option<ChunkingConfig> {
        self.chunking.clone().map(Into::into)
    }

    pub fn set_chunking(&mut self, value: Option<&ChunkingConfig>) {
        self.chunking = value.map(|value| value.clone().into());
    }

    pub fn get_content_filter(&self) -> Option<ContentFilterConfig> {
        self.content_filter.clone().map(Into::into)
    }

    pub fn set_content_filter(&mut self, value: Option<&ContentFilterConfig>) {
        self.content_filter = value.map(|value| value.clone().into());
    }

    pub fn get_images(&self) -> Option<ImageExtractionConfig> {
        self.images.clone().map(Into::into)
    }

    pub fn set_images(&mut self, value: Option<&ImageExtractionConfig>) {
        self.images = value.map(|value| value.clone().into());
    }

    pub fn get_pdf_options(&self) -> Option<PdfConfig> {
        self.pdf_options.clone().map(Into::into)
    }

    pub fn set_pdf_options(&mut self, value: Option<&PdfConfig>) {
        self.pdf_options = value.map(|value| value.clone().into());
    }

    pub fn get_token_reduction(&self) -> Option<TokenReductionOptions> {
        self.token_reduction.clone().map(Into::into)
    }

    pub fn set_token_reduction(&mut self, value: Option<&TokenReductionOptions>) {
        self.token_reduction = value.map(|value| value.clone().into());
    }

    pub fn get_language_detection(&self) -> Option<LanguageDetectionConfig> {
        self.language_detection.clone().map(Into::into)
    }

    pub fn set_language_detection(&mut self, value: Option<&LanguageDetectionConfig>) {
        self.language_detection = value.map(|value| value.clone().into());
    }

    pub fn get_pages(&self) -> Option<PageConfig> {
        self.pages.clone().map(Into::into)
    }

    pub fn set_pages(&mut self, value: Option<&PageConfig>) {
        self.pages = value.map(|value| value.clone().into());
    }

    pub fn get_keywords(&self) -> Option<KeywordConfig> {
        self.keywords.clone().map(Into::into)
    }

    pub fn set_keywords(&mut self, value: Option<&KeywordConfig>) {
        self.keywords = value.map(|value| value.clone().into());
    }

    pub fn get_postprocessor(&self) -> Option<PostProcessorConfig> {
        self.postprocessor.clone().map(Into::into)
    }

    pub fn set_postprocessor(&mut self, value: Option<&PostProcessorConfig>) {
        self.postprocessor = value.map(|value| value.clone().into());
    }

    pub fn get_html_options(&self) -> Option<ConversionOptions> {
        self.html_options.clone().map(Into::into)
    }

    pub fn set_html_options(&mut self, value: Option<&ConversionOptions>) {
        self.html_options = value.map(|value| value.clone().into());
    }

    pub fn get_html_output(&self) -> Option<HtmlOutputConfig> {
        self.html_output.clone().map(Into::into)
    }

    pub fn set_html_output(&mut self, value: Option<&HtmlOutputConfig>) {
        self.html_output = value.map(|value| value.clone().into());
    }

    pub fn get_result_format(&self) -> Option<String> {
        self.result_format.clone().map(Into::into)
    }

    pub fn get_output_format(&self) -> Option<String> {
        self.output_format.clone().map(Into::into)
    }

    pub fn get_include_document_structure(&self) -> Option<bool> {
        self.include_document_structure.clone()
    }

    pub fn get_layout(&self) -> Option<LayoutDetectionConfig> {
        self.layout.clone().map(Into::into)
    }

    pub fn set_layout(&mut self, value: Option<&LayoutDetectionConfig>) {
        self.layout = value.map(|value| value.clone().into());
    }

    pub fn get_transcription(&self) -> Option<TranscriptionConfig> {
        self.transcription.clone().map(Into::into)
    }

    pub fn set_transcription(&mut self, value: Option<&TranscriptionConfig>) {
        self.transcription = value.map(|value| value.clone().into());
    }

    pub fn get_timeout_secs(&self) -> Option<i64> {
        self.timeout_secs.clone()
    }

    pub fn get_tree_sitter(&self) -> Option<TreeSitterConfig> {
        self.tree_sitter.clone().map(Into::into)
    }

    pub fn set_tree_sitter(&mut self, value: Option<&TreeSitterConfig>) {
        self.tree_sitter = value.map(|value| value.clone().into());
    }

    pub fn get_structured_extraction(&self) -> Option<StructuredExtractionConfig> {
        self.structured_extraction.clone().map(Into::into)
    }

    pub fn set_structured_extraction(&mut self, value: Option<&StructuredExtractionConfig>) {
        self.structured_extraction = value.map(|value| value.clone().into());
    }

    pub fn get_url(&self) -> Option<UrlExtractionConfig> {
        self.url.clone().map(Into::into)
    }

    pub fn set_url(&mut self, value: Option<&UrlExtractionConfig>) {
        self.url = value.map(|value| value.clone().into());
    }

    pub fn get_ner(&self) -> Option<NerConfig> {
        self.ner.clone().map(Into::into)
    }

    pub fn set_ner(&mut self, value: Option<&NerConfig>) {
        self.ner = value.map(|value| value.clone().into());
    }

    pub fn get_redaction(&self) -> Option<RedactionConfig> {
        self.redaction.clone().map(Into::into)
    }

    pub fn set_redaction(&mut self, value: Option<&RedactionConfig>) {
        self.redaction = value.map(|value| value.clone().into());
    }

    pub fn get_summarization(&self) -> Option<SummarizationConfig> {
        self.summarization.clone().map(Into::into)
    }

    pub fn set_summarization(&mut self, value: Option<&SummarizationConfig>) {
        self.summarization = value.map(|value| value.clone().into());
    }

    pub fn get_translation(&self) -> Option<TranslationConfig> {
        self.translation.clone().map(Into::into)
    }

    pub fn set_translation(&mut self, value: Option<&TranslationConfig>) {
        self.translation = value.map(|value| value.clone().into());
    }

    pub fn get_page_classification(&self) -> Option<PageClassificationConfig> {
        self.page_classification.clone().map(Into::into)
    }

    pub fn set_page_classification(&mut self, value: Option<&PageClassificationConfig>) {
        self.page_classification = value.map(|value| value.clone().into());
    }

    pub fn get_chunk_classification(&self) -> Option<ChunkClassificationConfig> {
        self.chunk_classification.clone().map(Into::into)
    }

    pub fn set_chunk_classification(&mut self, value: Option<&ChunkClassificationConfig>) {
        self.chunk_classification = value.map(|value| value.clone().into());
    }

    pub fn get_captioning(&self) -> Option<CaptioningConfig> {
        self.captioning.clone().map(Into::into)
    }

    pub fn set_captioning(&mut self, value: Option<&CaptioningConfig>) {
        self.captioning = value.map(|value| value.clone().into());
    }

    pub fn get_qr_codes(&self) -> Option<bool> {
        self.qr_codes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Footnote")]
#[serde(default)]
pub struct Footnote {
    /// Footnote label
    #[php(prop, name = "label")]
    pub label: String,
    /// Footnote content blocks
    pub content: Vec<FormattedBlock>,
}

#[php_impl]
impl Footnote {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(label: String, content: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut content_core_result: Vec<FormattedBlock> = Vec::new();
        for (_, item) in content.iter() {
            if let Some(parsed) = <&FormattedBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                content_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to FormattedBlock".to_string(),
                ));
            }
        }
        let content_core: Vec<FormattedBlock> = content_core_result;
        Ok(Self {
            label,
            content: content_core,
        })
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_content(&self) -> Vec<FormattedBlock> {
        self.content.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\FootnoteAnchor")]
#[serde(default)]
pub struct FootnoteAnchor {
    /// The label of the footnote reference (e.g., "1" in `[^1]`).
    #[php(prop, name = "label")]
    pub label: String,
    /// Byte offset of the anchor in the markdown text.
    #[php(prop, name = "offset")]
    pub offset: i64,
}

#[php_impl]
impl FootnoteAnchor {
    #[php(constructor)]
    pub fn new(label: String, offset: i64) -> Self {
        Self { label, offset }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_offset(&self) -> i64 {
        self.offset.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\FootnoteConfig")]
#[serde(default)]
pub struct FootnoteConfig {
    /// Whether to parse the structured citation block (default: true).
    ///
    /// When enabled, the parser will look for and extract citations from
    /// the block after `---` + `<!-- citations ... -->`.
    #[php(prop, name = "parseCitations")]
    #[serde(default = "crate::serde_defaults::footnote_config_parse_citations")]
    #[serde(alias = "parseCitations")]
    pub parse_citations: bool,
}

impl Default for FootnoteConfig {
    fn default() -> Self {
        <xberg::FootnoteConfig as Default>::default().into()
    }
}

#[php_impl]
impl FootnoteConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(parseCitations: bool) -> Self {
        Self {
            parse_citations: parseCitations,
        }
    }

    pub fn get_parse_citations(&self) -> bool {
        self.parse_citations.clone()
    }

    pub fn with_parse_citations(&self, enabled: bool) -> FootnoteConfig {
        #[allow(clippy::needless_update)]
        let core_self = xberg::FootnoteConfig {
            parse_citations: self.parse_citations,
            ..Default::default()
        };
        core_self.with_parse_citations(enabled).into()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> FootnoteConfig {
        xberg::FootnoteConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\FootnoteDefinition")]
#[serde(default)]
pub struct FootnoteDefinition {
    /// The label of the footnote (e.g., "1" in `[^1]: ...`).
    #[php(prop, name = "label")]
    pub label: String,
    /// The full content of the footnote definition.
    #[php(prop, name = "content")]
    pub content: String,
    /// Byte offset of the definition line in the markdown text.
    #[php(prop, name = "offset")]
    pub offset: i64,
}

#[php_impl]
impl FootnoteDefinition {
    #[php(constructor)]
    pub fn new(label: String, content: String, offset: i64) -> Self {
        Self { label, content, offset }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_offset(&self) -> i64 {
        self.offset.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\FormattedBlock")]
#[serde(default)]
pub struct FormattedBlock {
    /// Type of block element
    #[php(prop, name = "blockType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "blockType")]
    pub block_type: String,
    /// Heading level (1-6) for headings, or nesting level for lists
    #[php(prop, name = "level")]
    pub level: Option<i64>,
    /// Inline content within the block
    #[serde(alias = "inlineContent")]
    pub inline_content: Vec<InlineElement>,
    /// Element attributes (classes, IDs, key-value pairs)
    pub attributes: Option<Attributes>,
    /// Language identifier for code blocks
    #[php(prop, name = "language")]
    pub language: Option<String>,
    /// Raw code content for code blocks
    #[php(prop, name = "code")]
    pub code: Option<String>,
    /// Nested blocks for containers (blockquotes, list items, divs)
    #[serde(default)]
    pub children: Vec<FormattedBlock>,
}

#[php_impl]
impl FormattedBlock {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        blockType: String,
        inlineContent: &ext_php_rs::types::ZendHashTable,
        children: &ext_php_rs::types::ZendHashTable,
        level: Option<i64>,
        attributes: Option<&Attributes>,
        language: Option<String>,
        code: Option<String>,
    ) -> PhpResult<Self> {
        let mut inlineContent_core_result: Vec<InlineElement> = Vec::new();
        for (_, item) in inlineContent.iter() {
            if let Some(parsed) = <&InlineElement as ext_php_rs::convert::FromZval>::from_zval(item) {
                inlineContent_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to InlineElement".to_string(),
                ));
            }
        }
        let inlineContent_core: Vec<InlineElement> = inlineContent_core_result;
        let mut children_core_result: Vec<FormattedBlock> = Vec::new();
        for (_, item) in children.iter() {
            if let Some(parsed) = <&FormattedBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                children_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to FormattedBlock".to_string(),
                ));
            }
        }
        let children_core: Vec<FormattedBlock> = children_core_result;
        Ok(Self {
            block_type: blockType,
            level,
            inline_content: inlineContent_core,
            attributes: attributes.cloned(),
            language,
            code,
            children: children_core,
        })
    }

    pub fn get_block_type(&self) -> String {
        self.block_type.clone()
    }

    pub fn get_level(&self) -> Option<i64> {
        self.level.clone()
    }

    pub fn get_inline_content(&self) -> Vec<InlineElement> {
        self.inline_content.clone()
    }

    pub fn get_attributes(&self) -> Option<Attributes> {
        self.attributes.clone().map(Into::into)
    }

    pub fn set_attributes(&mut self, value: Option<&Attributes>) {
        self.attributes = value.map(|value| value.clone().into());
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_code(&self) -> Option<String> {
        self.code.clone()
    }

    pub fn get_children(&self) -> Vec<FormattedBlock> {
        self.children.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Formula")]
#[serde(default)]
pub struct Formula {
    /// LaTeX source of the formula, without surrounding `$$` delimiters.
    ///
    /// Markup converters and formula OCR produce real LaTeX. The native PDF
    /// layout path stores the plain text of a detected formula region, which
    /// keeps the original Unicode math characters instead of LaTeX commands.
    /// To render the formula in Markdown or other formats, wrap it in `$$..$$`.
    #[php(prop, name = "latex")]
    pub latex: String,
    /// Bounding box of the formula region on its page. `None` for markup sources.
    ///
    /// PDF OCR sources report PDF point coordinates with the origin at the
    /// bottom-left of the page, comparable to native PDF geometry. Image
    /// sources, and PDF pages whose geometry is unavailable, report pixels of
    /// the image the OCR backend saw. The C FFI reports an absent bbox as a
    /// null pointer.
    #[serde(default)]
    pub bbox: Option<BoundingBox>,
    /// 1-indexed page number the formula appears on. `None` when the source
    /// format has no page concept. The C FFI reports an absent page as `0`.
    #[php(prop, name = "page")]
    #[serde(default)]
    pub page: Option<u32>,
}

#[php_impl]
impl Formula {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(latex: String, bbox: Option<&BoundingBox>, page: Option<u32>) -> Self {
        Self {
            latex,
            bbox: bbox.cloned(),
            page,
        }
    }

    pub fn get_latex(&self) -> String {
        self.latex.clone()
    }

    pub fn get_bbox(&self) -> Option<BoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&BoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }

    pub fn get_page(&self) -> Option<u32> {
        self.page.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\GeoJsonExtractionConfig")]
#[serde(default)]
pub struct GeoJsonExtractionConfig {
    /// Include every coordinate in rendered content and `flattened_fields` metadata.
    ///
    /// Defaults to `false`. When false, extraction reports feature, property, geometry,
    /// position-count, bounds, truncation, and discarded-category metadata and emits a
    /// `ProcessingWarning` for every GeoJSON input.
    #[php(prop, name = "includeFullCoordinates")]
    #[serde(alias = "includeFullCoordinates")]
    pub include_full_coordinates: bool,
}

impl Default for GeoJsonExtractionConfig {
    fn default() -> Self {
        <xberg::GeoJsonExtractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl GeoJsonExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(includeFullCoordinates: bool) -> Self {
        Self {
            include_full_coordinates: includeFullCoordinates,
        }
    }

    pub fn get_include_full_coordinates(&self) -> bool {
        self.include_full_coordinates.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\GlmOcrBackendOptions")]
#[serde(default)]
pub struct GlmOcrBackendOptions {
    /// Optional recognition task; the backend constructor's task is used when absent.
    #[php(prop, name = "task")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub task: Option<String>,
    /// Optional per-call device override.
    #[php(prop, name = "device")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub device: Option<String>,
    /// Optional whole-page or layout-paired dispatch mode.
    #[php(prop, name = "layoutMode")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "layoutMode")]
    pub layout_mode: Option<String>,
    /// Whether chart regions use chart understanding instead of captioning.
    #[php(prop, name = "enableChartUnderstanding")]
    #[serde(alias = "enableChartUnderstanding")]
    pub enable_chart_understanding: Option<bool>,
    /// Optional Hugging Face cache root.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
}

impl Default for GlmOcrBackendOptions {
    fn default() -> Self {
        <xberg::candle_ocr::GlmOcrBackendOptions as Default>::default().into()
    }
}

#[php_impl]
impl GlmOcrBackendOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_task(&self) -> Option<String> {
        self.task.clone().map(Into::into)
    }

    pub fn get_device(&self) -> Option<String> {
        self.device.clone().map(Into::into)
    }

    pub fn get_layout_mode(&self) -> Option<String> {
        self.layout_mode.clone().map(Into::into)
    }

    pub fn get_enable_chart_understanding(&self) -> Option<bool> {
        self.enable_chart_understanding.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\GridCell")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct GridCell {
    /// Cell text content.
    #[php(prop, name = "content")]
    pub content: String,
    /// Zero-indexed row position.
    #[php(prop, name = "row")]
    pub row: u32,
    /// Zero-indexed column position.
    #[php(prop, name = "col")]
    pub col: u32,
    /// Number of rows this cell spans.
    #[php(prop, name = "rowSpan")]
    #[serde(alias = "rowSpan")]
    pub row_span: u32,
    /// Number of columns this cell spans.
    #[php(prop, name = "colSpan")]
    #[serde(alias = "colSpan")]
    pub col_span: u32,
    /// Whether this is a header cell.
    #[php(prop, name = "isHeader")]
    #[serde(alias = "isHeader")]
    #[serde(default)]
    pub is_header: bool,
    /// Bounding box for this cell (if available).
    pub bbox: Option<BoundingBox>,
    /// Outline level (1-6) of the heading style this cell's text carries, when it has one.
    ///
    /// A DOCX banner row -- row 0, one cell spanning the grid, styled `Heading1`..`Heading6` --
    /// is what Word's navigation pane and a `TOC` field treat as the document's outline, but as
    /// a table cell it reached consumers as anonymous text (GH#1587). `content` is deliberately
    /// left as the bare cell text: prefixing it with `#` would put a markdown heading inside a
    /// table cell, which is invalid where it lands and changes text every existing consumer
    /// already reads. This field is the signal instead, so a caller can decide for itself
    /// whether a `heading 2` in a banner row is a section title or a column label.
    #[php(prop, name = "headingLevel")]
    #[serde(alias = "headingLevel")]
    #[serde(default)]
    pub heading_level: Option<u8>,
    /// Human-readable name of the paragraph style applied to this cell's text (`heading 2`).
    ///
    /// Carries the style even when it resolves to no outline level, so a caller can key on a
    /// named style this crate does not map to a heading. See `GridCell.heading_level`.
    #[php(prop, name = "styleName")]
    #[serde(alias = "styleName")]
    #[serde(default)]
    pub style_name: Option<String>,
}

#[php_impl]
impl GridCell {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        row: u32,
        col: u32,
        rowSpan: u32,
        colSpan: u32,
        isHeader: bool,
        bbox: Option<&BoundingBox>,
        headingLevel: Option<u8>,
        styleName: Option<String>,
    ) -> Self {
        Self {
            content,
            row,
            col,
            row_span: rowSpan,
            col_span: colSpan,
            is_header: isHeader,
            bbox: bbox.cloned(),
            heading_level: headingLevel,
            style_name: styleName,
        }
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_row(&self) -> u32 {
        self.row.clone()
    }

    pub fn get_col(&self) -> u32 {
        self.col.clone()
    }

    pub fn get_row_span(&self) -> u32 {
        self.row_span.clone()
    }

    pub fn get_col_span(&self) -> u32 {
        self.col_span.clone()
    }

    pub fn get_is_header(&self) -> bool {
        self.is_header.clone()
    }

    pub fn get_bbox(&self) -> Option<BoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&BoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }

    pub fn get_heading_level(&self) -> Option<u8> {
        self.heading_level.clone()
    }

    pub fn get_style_name(&self) -> Option<String> {
        self.style_name.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\HeaderMetadata")]
#[serde(default)]
pub struct HeaderMetadata {
    /// Header level: 1 (h1) through 6 (h6)
    #[php(prop, name = "level")]
    pub level: u8,
    /// Normalized text content of the header
    #[php(prop, name = "text")]
    pub text: String,
    /// HTML id attribute if present
    #[php(prop, name = "id")]
    pub id: Option<String>,
    /// Document tree depth at the header element
    #[php(prop, name = "depth")]
    pub depth: u32,
    /// Byte offset in original HTML document
    #[php(prop, name = "htmlOffset")]
    #[serde(alias = "htmlOffset")]
    pub html_offset: u32,
}

#[php_impl]
impl HeaderMetadata {
    #[php(constructor)]
    pub fn new(level: u8, text: String, depth: u32, htmlOffset: u32, id: Option<String>) -> Self {
        Self {
            level,
            text,
            id,
            depth,
            html_offset: htmlOffset,
        }
    }

    pub fn get_level(&self) -> u8 {
        self.level.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_id(&self) -> Option<String> {
        self.id.clone()
    }

    pub fn get_depth(&self) -> u32 {
        self.depth.clone()
    }

    pub fn get_html_offset(&self) -> u32 {
        self.html_offset.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\HeadingContext")]
#[serde(default)]
pub struct HeadingContext {
    /// The heading hierarchy from document root to this chunk's section.
    /// Index 0 is the outermost (h1), last element is the most specific.
    pub headings: Vec<HeadingLevel>,
}

#[php_impl]
impl HeadingContext {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(headings: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut headings_core_result: Vec<HeadingLevel> = Vec::new();
        for (_, item) in headings.iter() {
            if let Some(parsed) = <&HeadingLevel as ext_php_rs::convert::FromZval>::from_zval(item) {
                headings_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to HeadingLevel".to_string(),
                ));
            }
        }
        let headings_core: Vec<HeadingLevel> = headings_core_result;
        Ok(Self {
            headings: headings_core,
        })
    }

    pub fn get_headings(&self) -> Vec<HeadingLevel> {
        self.headings.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\HeadingLevel")]
#[serde(default)]
pub struct HeadingLevel {
    /// Heading depth (1 = h1, 2 = h2, etc.)
    #[php(prop, name = "level")]
    pub level: u8,
    /// The text content of the heading.
    #[php(prop, name = "text")]
    pub text: String,
}

#[php_impl]
impl HeadingLevel {
    #[php(constructor)]
    pub fn new(level: u8, text: String) -> Self {
        Self { level, text }
    }

    pub fn get_level(&self) -> u8 {
        self.level.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\HeuristicsConfig")]
#[serde(default)]
pub struct HeuristicsConfig {
    /// Enable PDF text-layer detection heuristics.
    ///
    /// When `true`, PDFs with a substantial text layer will skip chunking.
    /// Default: `true`.
    #[php(prop, name = "enablePdfTextHeuristics")]
    #[serde(default = "crate::serde_defaults::heuristics_config_enable_pdf_text_heuristics")]
    #[serde(alias = "enablePdfTextHeuristics")]
    pub enable_pdf_text_heuristics: bool,
    /// Minimum fraction of pages that must have text to skip chunking.
    ///
    /// Range `0.0..=1.0`. Default: `0.7` (70 % of pages).
    #[php(prop, name = "textLayerThreshold")]
    #[serde(default = "crate::serde_defaults::heuristics_config_text_layer_threshold")]
    #[serde(alias = "textLayerThreshold")]
    pub text_layer_threshold: f32,
    /// File size threshold in bytes for considering chunking.
    ///
    /// Files smaller than this are processed without chunking.
    /// Default: 10 MiB (10 × 1 024 × 1 024).
    #[php(prop, name = "fileSizeThresholdBytes")]
    #[serde(default = "crate::serde_defaults::heuristics_config_file_size_threshold_bytes")]
    #[serde(alias = "fileSizeThresholdBytes")]
    pub file_size_threshold_bytes: i64,
    /// Page count threshold for considering chunking.
    ///
    /// Documents with fewer pages are processed without chunking.
    /// Default: 50.
    #[php(prop, name = "pageCountThreshold")]
    #[serde(default = "crate::serde_defaults::heuristics_config_page_count_threshold")]
    #[serde(alias = "pageCountThreshold")]
    pub page_count_threshold: u32,
    /// Target number of pages per chunk for optimal parallel processing.
    ///
    /// Default: 10.
    #[php(prop, name = "targetPagesPerChunk")]
    #[serde(default = "crate::serde_defaults::heuristics_config_target_pages_per_chunk")]
    #[serde(alias = "targetPagesPerChunk")]
    pub target_pages_per_chunk: u32,
    /// Hard cap on pages per chunk.
    ///
    /// No chunk will exceed this limit. Must be ≥ `target_pages_per_chunk`.
    /// Default: 25.
    #[php(prop, name = "maxPagesPerChunk")]
    #[serde(default = "crate::serde_defaults::heuristics_config_max_pages_per_chunk")]
    #[serde(alias = "maxPagesPerChunk")]
    pub max_pages_per_chunk: u32,
    /// File size threshold for disk-based processing.
    ///
    /// Files larger than this are buffered to disk to prevent OOM.
    /// Default: 50 MiB (50 × 1 024 × 1 024).
    #[php(prop, name = "diskProcessingThresholdBytes")]
    #[serde(default = "crate::serde_defaults::heuristics_config_disk_processing_threshold_bytes")]
    #[serde(alias = "diskProcessingThresholdBytes")]
    pub disk_processing_threshold_bytes: i64,
    /// Minimum characters per page to consider a page as having text.
    ///
    /// Default: 50.
    #[php(prop, name = "minCharsPerPage")]
    #[serde(default = "crate::serde_defaults::heuristics_config_min_chars_per_page")]
    #[serde(alias = "minCharsPerPage")]
    pub min_chars_per_page: u32,
    /// Maximum sheet count allowed in an XLSX workbook.
    ///
    /// Workbooks beyond this are rejected pre-extraction to avoid OOM /
    /// abusive billing inflation. Default: 200.
    #[php(prop, name = "maxXlsxSheetCount")]
    #[serde(default = "crate::serde_defaults::heuristics_config_max_xlsx_sheet_count")]
    #[serde(alias = "maxXlsxSheetCount")]
    pub max_xlsx_sheet_count: u32,
    /// Maximum cell count (sheets × rows × columns approximation) in an XLSX workbook.
    ///
    /// Default: 5 000 000 (≈ 200 sheets × 25 k cells).
    #[php(prop, name = "maxXlsxWorkbookCells")]
    #[serde(default = "crate::serde_defaults::heuristics_config_max_xlsx_workbook_cells")]
    #[serde(alias = "maxXlsxWorkbookCells")]
    pub max_xlsx_workbook_cells: i64,
    /// Maximum number of OLE-embedded objects extractable from a single PPTX or DOCX.
    ///
    /// Protects against zip-bomb-style nested-document abuse. Default: 50.
    #[php(prop, name = "maxPptxEmbeddedCount")]
    #[serde(default = "crate::serde_defaults::heuristics_config_max_pptx_embedded_count")]
    #[serde(alias = "maxPptxEmbeddedCount")]
    pub max_pptx_embedded_count: u32,
}

impl Default for HeuristicsConfig {
    fn default() -> Self {
        <xberg::HeuristicsConfig as Default>::default().into()
    }
}

#[php_impl]
impl HeuristicsConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        enablePdfTextHeuristics: bool,
        textLayerThreshold: f32,
        fileSizeThresholdBytes: i64,
        pageCountThreshold: u32,
        targetPagesPerChunk: u32,
        maxPagesPerChunk: u32,
        diskProcessingThresholdBytes: i64,
        minCharsPerPage: u32,
        maxXlsxSheetCount: u32,
        maxXlsxWorkbookCells: i64,
        maxPptxEmbeddedCount: u32,
    ) -> Self {
        Self {
            enable_pdf_text_heuristics: enablePdfTextHeuristics,
            text_layer_threshold: textLayerThreshold,
            file_size_threshold_bytes: fileSizeThresholdBytes,
            page_count_threshold: pageCountThreshold,
            target_pages_per_chunk: targetPagesPerChunk,
            max_pages_per_chunk: maxPagesPerChunk,
            disk_processing_threshold_bytes: diskProcessingThresholdBytes,
            min_chars_per_page: minCharsPerPage,
            max_xlsx_sheet_count: maxXlsxSheetCount,
            max_xlsx_workbook_cells: maxXlsxWorkbookCells,
            max_pptx_embedded_count: maxPptxEmbeddedCount,
        }
    }

    pub fn get_enable_pdf_text_heuristics(&self) -> bool {
        self.enable_pdf_text_heuristics.clone()
    }

    pub fn get_text_layer_threshold(&self) -> f32 {
        self.text_layer_threshold.clone()
    }

    pub fn get_file_size_threshold_bytes(&self) -> i64 {
        self.file_size_threshold_bytes.clone()
    }

    pub fn get_page_count_threshold(&self) -> u32 {
        self.page_count_threshold.clone()
    }

    pub fn get_target_pages_per_chunk(&self) -> u32 {
        self.target_pages_per_chunk.clone()
    }

    pub fn get_max_pages_per_chunk(&self) -> u32 {
        self.max_pages_per_chunk.clone()
    }

    pub fn get_disk_processing_threshold_bytes(&self) -> i64 {
        self.disk_processing_threshold_bytes.clone()
    }

    pub fn get_min_chars_per_page(&self) -> u32 {
        self.min_chars_per_page.clone()
    }

    pub fn get_max_xlsx_sheet_count(&self) -> u32 {
        self.max_xlsx_sheet_count.clone()
    }

    pub fn get_max_xlsx_workbook_cells(&self) -> i64 {
        self.max_xlsx_workbook_cells.clone()
    }

    pub fn get_max_pptx_embedded_count(&self) -> u32 {
        self.max_pptx_embedded_count.clone()
    }

    pub fn validate(&self) -> PhpResult<()> {
        #[allow(clippy::needless_update)]
        let core_self = xberg::HeuristicsConfig {
            enable_pdf_text_heuristics: self.enable_pdf_text_heuristics,
            text_layer_threshold: self.text_layer_threshold,
            file_size_threshold_bytes: self.file_size_threshold_bytes as u64,
            page_count_threshold: self.page_count_threshold,
            target_pages_per_chunk: self.target_pages_per_chunk,
            max_pages_per_chunk: self.max_pages_per_chunk,
            disk_processing_threshold_bytes: self.disk_processing_threshold_bytes as u64,
            min_chars_per_page: self.min_chars_per_page,
            max_xlsx_sheet_count: self.max_xlsx_sheet_count,
            max_xlsx_workbook_cells: self.max_xlsx_workbook_cells as u64,
            max_pptx_embedded_count: self.max_pptx_embedded_count,
            ..Default::default()
        };
        let result = core_self
            .validate()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> HeuristicsConfig {
        xberg::HeuristicsConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\HierarchicalBlock")]
#[serde(default)]
pub struct HierarchicalBlock {
    /// The text content of this block
    #[php(prop, name = "text")]
    pub text: String,
    /// The font size of the text in this block
    #[php(prop, name = "fontSize")]
    #[serde(alias = "fontSize")]
    pub font_size: f32,
    /// The hierarchy level of this block (H1-H6 or Body)
    ///
    /// Levels correspond to HTML heading tags:
    /// - "h1": Top-level heading
    /// - "h2": Secondary heading
    /// - "h3": Tertiary heading
    /// - "h4": Quaternary heading
    /// - "h5": Quinary heading
    /// - "h6": Senary heading
    /// - "body": Body text (no heading level)
    #[php(prop, name = "level")]
    pub level: String,
    /// Bounding box information for the block
    ///
    /// Contains left, top, right, and bottom coordinates in PDF units.
    pub bbox: Option<HierarchicalBoundingBox>,
}

#[php_impl]
impl HierarchicalBlock {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(text: String, fontSize: f32, level: String, bbox: Option<&HierarchicalBoundingBox>) -> Self {
        Self {
            text,
            font_size: fontSize,
            level,
            bbox: bbox.cloned(),
        }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_font_size(&self) -> f32 {
        self.font_size.clone()
    }

    pub fn get_level(&self) -> String {
        self.level.clone()
    }

    pub fn get_bbox(&self) -> Option<HierarchicalBoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&HierarchicalBoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\HierarchicalBoundingBox")]
#[serde(default)]
pub struct HierarchicalBoundingBox {
    /// Left coordinate.
    #[php(prop, name = "left")]
    pub left: f32,
    /// Top coordinate.
    #[php(prop, name = "top")]
    pub top: f32,
    /// Right coordinate.
    #[php(prop, name = "right")]
    pub right: f32,
    /// Bottom coordinate.
    #[php(prop, name = "bottom")]
    pub bottom: f32,
}

impl Default for HierarchicalBoundingBox {
    fn default() -> Self {
        <xberg::HierarchicalBoundingBox as Default>::default().into()
    }
}

#[php_impl]
impl HierarchicalBoundingBox {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(left: f32, top: f32, right: f32, bottom: f32) -> Self {
        Self {
            left,
            top,
            right,
            bottom,
        }
    }

    pub fn get_left(&self) -> f32 {
        self.left.clone()
    }

    pub fn get_top(&self) -> f32 {
        self.top.clone()
    }

    pub fn get_right(&self) -> f32 {
        self.right.clone()
    }

    pub fn get_bottom(&self) -> f32 {
        self.bottom.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\HierarchyConfig")]
#[serde(default)]
pub struct HierarchyConfig {
    /// Enable hierarchy extraction
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::hierarchy_config_enabled")]
    pub enabled: bool,
    /// Number of font size clusters to use for hierarchy levels (1-7)
    ///
    /// Default: 3, which provides two heading levels plus body text.
    /// Larger values create more fine-grained hierarchy levels.
    #[php(prop, name = "kClusters")]
    #[serde(default = "crate::serde_defaults::hierarchy_config_k_clusters")]
    #[serde(alias = "kClusters")]
    pub k_clusters: i64,
    /// Include bounding box information in hierarchy blocks
    #[php(prop, name = "includeBbox")]
    #[serde(default = "crate::serde_defaults::hierarchy_config_include_bbox")]
    #[serde(alias = "includeBbox")]
    pub include_bbox: bool,
}

impl Default for HierarchyConfig {
    fn default() -> Self {
        <xberg::HierarchyConfig as Default>::default().into()
    }
}

#[php_impl]
impl HierarchyConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(enabled: bool, kClusters: i64, includeBbox: bool) -> Self {
        Self {
            enabled,
            k_clusters: kClusters,
            include_bbox: includeBbox,
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_k_clusters(&self) -> i64 {
        self.k_clusters.clone()
    }

    pub fn get_include_bbox(&self) -> bool {
        self.include_bbox.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> HierarchyConfig {
        xberg::HierarchyConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\HtmlMetadata")]
#[serde(default)]
pub struct HtmlMetadata {
    /// Document title from `<title>` tag
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Document description from `<meta name="description">` tag
    #[php(prop, name = "description")]
    pub description: Option<String>,
    /// Document keywords from `<meta name="keywords">` tag, split on commas
    #[php(prop, name = "keywords")]
    #[serde(default)]
    pub keywords: Vec<String>,
    /// Document author from `<meta name="author">` tag
    #[php(prop, name = "author")]
    pub author: Option<String>,
    /// Canonical URL from `<link rel="canonical">` tag
    #[php(prop, name = "canonicalUrl")]
    #[serde(alias = "canonicalUrl")]
    pub canonical_url: Option<String>,
    /// Base URL from `<base href="">` tag for resolving relative URLs
    #[php(prop, name = "baseHref")]
    #[serde(alias = "baseHref")]
    pub base_href: Option<String>,
    /// Document language from `lang` attribute
    #[php(prop, name = "language")]
    pub language: Option<String>,
    /// Document text direction from `dir` attribute
    #[php(prop, name = "textDirection")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "textDirection")]
    pub text_direction: Option<String>,
    /// Open Graph metadata (og:* properties) for social media
    /// Keys like "title", "description", "image", "url", etc.
    #[php(prop, name = "openGraph")]
    #[serde(alias = "openGraph")]
    #[serde(default)]
    pub open_graph: HashMap<String, String>,
    /// Twitter Card metadata (twitter:* properties)
    /// Keys like "card", "site", "creator", "title", "description", "image", etc.
    #[php(prop, name = "twitterCard")]
    #[serde(alias = "twitterCard")]
    #[serde(default)]
    pub twitter_card: HashMap<String, String>,
    /// Additional meta tags not covered by specific fields
    /// Keys are meta name/property attributes, values are content
    #[php(prop, name = "metaTags")]
    #[serde(alias = "metaTags")]
    #[serde(default)]
    pub meta_tags: HashMap<String, String>,
    /// Extracted header elements with hierarchy
    #[serde(default)]
    pub headers: Vec<HeaderMetadata>,
    /// Extracted hyperlinks with type classification
    #[serde(default)]
    pub links: Vec<LinkMetadata>,
    /// Extracted images with source and dimensions
    #[serde(default)]
    pub images: Vec<ImageMetadataType>,
    /// Extracted structured data blocks
    #[serde(alias = "structuredData")]
    #[serde(default)]
    pub structured_data: Vec<StructuredData>,
}

impl Default for HtmlMetadata {
    fn default() -> Self {
        <xberg::HtmlMetadata as Default>::default().into()
    }
}

#[php_impl]
impl HtmlMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        keywords: Vec<String>,
        openGraph: HashMap<String, String>,
        twitterCard: HashMap<String, String>,
        metaTags: HashMap<String, String>,
        headers: &ext_php_rs::types::ZendHashTable,
        links: &ext_php_rs::types::ZendHashTable,
        images: &ext_php_rs::types::ZendHashTable,
        structuredData: &ext_php_rs::types::ZendHashTable,
        title: Option<String>,
        description: Option<String>,
        author: Option<String>,
        canonicalUrl: Option<String>,
        baseHref: Option<String>,
        language: Option<String>,
        textDirection: Option<String>,
    ) -> PhpResult<Self> {
        let mut headers_core_result: Vec<HeaderMetadata> = Vec::new();
        for (_, item) in headers.iter() {
            if let Some(parsed) = <&HeaderMetadata as ext_php_rs::convert::FromZval>::from_zval(item) {
                headers_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to HeaderMetadata".to_string(),
                ));
            }
        }
        let headers_core: Vec<HeaderMetadata> = headers_core_result;
        let mut links_core_result: Vec<LinkMetadata> = Vec::new();
        for (_, item) in links.iter() {
            if let Some(parsed) = <&LinkMetadata as ext_php_rs::convert::FromZval>::from_zval(item) {
                links_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to LinkMetadata".to_string(),
                ));
            }
        }
        let links_core: Vec<LinkMetadata> = links_core_result;
        let mut images_core_result: Vec<ImageMetadataType> = Vec::new();
        for (_, item) in images.iter() {
            if let Some(parsed) = <&ImageMetadataType as ext_php_rs::convert::FromZval>::from_zval(item) {
                images_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ImageMetadataType".to_string(),
                ));
            }
        }
        let images_core: Vec<ImageMetadataType> = images_core_result;
        let mut structuredData_core_result: Vec<StructuredData> = Vec::new();
        for (_, item) in structuredData.iter() {
            if let Some(parsed) = <&StructuredData as ext_php_rs::convert::FromZval>::from_zval(item) {
                structuredData_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to StructuredData".to_string(),
                ));
            }
        }
        let structuredData_core: Vec<StructuredData> = structuredData_core_result;
        Ok(Self {
            title,
            description,
            keywords,
            author,
            canonical_url: canonicalUrl,
            base_href: baseHref,
            language,
            text_direction: textDirection,
            open_graph: openGraph,
            twitter_card: twitterCard,
            meta_tags: metaTags,
            headers: headers_core,
            links: links_core,
            images: images_core,
            structured_data: structuredData_core,
        })
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_description(&self) -> Option<String> {
        self.description.clone()
    }

    pub fn get_keywords(&self) -> Vec<String> {
        self.keywords.clone()
    }

    pub fn get_author(&self) -> Option<String> {
        self.author.clone()
    }

    pub fn get_canonical_url(&self) -> Option<String> {
        self.canonical_url.clone()
    }

    pub fn get_base_href(&self) -> Option<String> {
        self.base_href.clone()
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_text_direction(&self) -> Option<String> {
        self.text_direction.clone().map(Into::into)
    }

    pub fn get_open_graph(&self) -> HashMap<String, String> {
        self.open_graph.clone()
    }

    pub fn get_twitter_card(&self) -> HashMap<String, String> {
        self.twitter_card.clone()
    }

    pub fn get_meta_tags(&self) -> HashMap<String, String> {
        self.meta_tags.clone()
    }

    pub fn get_headers(&self) -> Vec<HeaderMetadata> {
        self.headers.clone()
    }

    pub fn get_links(&self) -> Vec<LinkMetadata> {
        self.links.clone()
    }

    pub fn get_images(&self) -> Vec<ImageMetadataType> {
        self.images.clone()
    }

    pub fn get_structured_data(&self) -> Vec<StructuredData> {
        self.structured_data.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\HtmlOutputConfig")]
#[serde(default)]
pub struct HtmlOutputConfig {
    /// Inline CSS string injected into the output after the theme stylesheet.
    /// Concatenated after `css_file` content when both are set.
    #[php(prop, name = "css")]
    #[serde(default)]
    pub css: Option<String>,
    /// Path to a CSS file loaded once at renderer construction time.
    /// Concatenated before `css` when both are set.
    #[php(prop, name = "cssFile")]
    #[serde(alias = "cssFile")]
    #[serde(default)]
    pub css_file: Option<String>,
    /// Built-in colour/typography theme. Default: `HtmlTheme.Unstyled`.
    #[php(prop, name = "theme")]
    #[serde(default = "crate::serde_defaults::html_output_config_theme")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub theme: String,
    /// CSS class prefix applied to every emitted class name.
    ///
    /// Default: `"kb-"`. Change this if your host application already uses
    /// classes that start with `kb-`.
    #[php(prop, name = "classPrefix")]
    #[serde(default = "crate::serde_defaults::html_output_config_class_prefix")]
    #[serde(alias = "classPrefix")]
    pub class_prefix: String,
    /// When `true` (default), write the resolved CSS into a `<style>` block
    /// immediately after the opening `<div class="{prefix}doc">`.
    ///
    /// Set to `false` to emit only the structural markup and wire up your
    /// own stylesheet targeting the `kb-*` class names.
    #[php(prop, name = "embedCss")]
    #[serde(default = "crate::serde_defaults::html_output_config_embed_css")]
    #[serde(alias = "embedCss")]
    pub embed_css: bool,
}

impl Default for HtmlOutputConfig {
    fn default() -> Self {
        <xberg::HtmlOutputConfig as Default>::default().into()
    }
}

#[php_impl]
impl HtmlOutputConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        theme: String,
        classPrefix: String,
        embedCss: bool,
        css: Option<String>,
        cssFile: Option<String>,
    ) -> Self {
        Self {
            css,
            css_file: cssFile,
            theme,
            class_prefix: classPrefix,
            embed_css: embedCss,
        }
    }

    pub fn get_css(&self) -> Option<String> {
        self.css.clone()
    }

    pub fn get_css_file(&self) -> Option<String> {
        self.css_file.clone()
    }

    pub fn get_theme(&self) -> String {
        self.theme.clone()
    }

    pub fn get_class_prefix(&self) -> String {
        self.class_prefix.clone()
    }

    pub fn get_embed_css(&self) -> bool {
        self.embed_css.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> HtmlOutputConfig {
        xberg::HtmlOutputConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ImageDimensions")]
#[serde(default)]
pub struct ImageDimensions {
    /// Width in pixels.
    #[php(prop, name = "width")]
    pub width: u32,
    /// Height in pixels.
    #[php(prop, name = "height")]
    pub height: u32,
}

impl Default for ImageDimensions {
    fn default() -> Self {
        <xberg::ImageDimensions as Default>::default().into()
    }
}

#[php_impl]
impl ImageDimensions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(width: u32, height: u32) -> Self {
        Self { width, height }
    }

    pub fn get_width(&self) -> u32 {
        self.width.clone()
    }

    pub fn get_height(&self) -> u32 {
        self.height.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ImageDpi")]
#[serde(default)]
pub struct ImageDpi {
    /// Horizontal resolution.
    #[php(prop, name = "horizontal")]
    pub horizontal: f64,
    /// Vertical resolution.
    #[php(prop, name = "vertical")]
    pub vertical: f64,
}

impl Default for ImageDpi {
    fn default() -> Self {
        <xberg::ImageDpi as Default>::default().into()
    }
}

#[php_impl]
impl ImageDpi {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(horizontal: f64, vertical: f64) -> Self {
        Self { horizontal, vertical }
    }

    pub fn get_horizontal(&self) -> f64 {
        self.horizontal.clone()
    }

    pub fn get_vertical(&self) -> f64 {
        self.vertical.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ImageExtractionConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ImageExtractionConfig {
    /// Extract images from documents
    #[php(prop, name = "extractImages")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_extract_images")]
    #[serde(alias = "extractImages")]
    pub extract_images: bool,
    /// Target DPI for image normalization
    #[php(prop, name = "targetDpi")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_target_dpi")]
    #[serde(alias = "targetDpi")]
    pub target_dpi: i32,
    /// Maximum dimension for images (width or height)
    #[php(prop, name = "maxImageDimension")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_max_image_dimension")]
    #[serde(alias = "maxImageDimension")]
    pub max_image_dimension: i32,
    /// Whether to inject image reference placeholders into markdown output.
    /// When `true` (default), image references like `![Image 1](embedded:p1_i0)`
    /// are appended to the markdown. Set to `false` to extract images as data
    /// without polluting the markdown output.
    #[php(prop, name = "injectPlaceholders")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_inject_placeholders")]
    #[serde(alias = "injectPlaceholders")]
    pub inject_placeholders: bool,
    /// Automatically adjust DPI based on image content
    #[php(prop, name = "autoAdjustDpi")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_auto_adjust_dpi")]
    #[serde(alias = "autoAdjustDpi")]
    pub auto_adjust_dpi: bool,
    /// Minimum DPI threshold
    #[php(prop, name = "minDpi")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_min_dpi")]
    #[serde(alias = "minDpi")]
    pub min_dpi: i32,
    /// Maximum DPI threshold
    #[php(prop, name = "maxDpi")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_max_dpi")]
    #[serde(alias = "maxDpi")]
    pub max_dpi: i32,
    /// Maximum number of image objects to extract per PDF page.
    ///
    /// Some PDFs (e.g. technical diagrams stored as thousands of raster fragments)
    /// can trigger extremely long or indefinite extraction times when every image
    /// object on a dense page is decoded individually via the PDF extractor. Setting this
    /// limit causes xberg to stop collecting individual images once the count
    /// per page reaches the cap and emit a warning instead.
    ///
    /// `None` (default) means no limit — all images are extracted.
    #[php(prop, name = "maxImagesPerPage")]
    #[serde(alias = "maxImagesPerPage")]
    #[serde(default)]
    pub max_images_per_page: Option<u32>,
    /// When `true`, extracted images are classified by kind and grouped
    /// into clusters where they appear to belong to one figure.
    /// Defaults to `false` — opt in explicitly to avoid unexpected ML overhead.
    #[php(prop, name = "classify")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_classify")]
    pub classify: bool,
    /// When `true`, full-page renders produced during OCR preprocessing are captured
    /// and returned as `ImageKind.PageRaster` entries in `ExtractedDocument.images`.
    ///
    /// **PDF + OCR only.** No rasters are captured for non-PDF inputs or when the
    /// document-level OCR bypass is active (whole-document backend). When OCR is
    /// enabled and this flag is set but the active backend skips per-page rendering,
    /// a `ProcessingWarning` is emitted in `ExtractedDocument.processing_warnings`.
    ///
    /// Defaults to `false`. Enable when downstream consumers need page thumbnails
    /// (e.g. citation previews, visual grounding).
    #[php(prop, name = "includePageRasters")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_include_page_rasters")]
    #[serde(alias = "includePageRasters")]
    pub include_page_rasters: bool,
    /// Run OCR on extracted images and include the recognized text in the document content.
    ///
    /// When `true` (default) and `ExtractionConfig.ocr` is configured, extracted images
    /// are processed with the configured OCR backend. Set to `false` to extract images
    /// without OCR processing, even when OCR is enabled.
    #[php(prop, name = "runOcrOnImages")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_run_ocr_on_images")]
    #[serde(alias = "runOcrOnImages")]
    pub run_ocr_on_images: bool,
    /// When `true`, image OCR results are rendered as plain text without the
    /// `![...](...)` markdown placeholder. Only takes effect when `run_ocr_on_images`
    /// is also `true`.
    #[php(prop, name = "ocrTextOnly")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_ocr_text_only")]
    #[serde(alias = "ocrTextOnly")]
    pub ocr_text_only: bool,
    /// When `true` and `ocr_text_only` is `false`, append the OCR text after
    /// the image placeholder in the rendered output.
    #[php(prop, name = "appendOcrText")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_append_ocr_text")]
    #[serde(alias = "appendOcrText")]
    pub append_ocr_text: bool,
    /// Target format for re-encoding extracted images.
    ///
    /// When set to anything other than `Native`, each extracted image is
    /// re-encoded to the requested format before being returned. This lets
    /// callers receive uniform output without duplicating encode logic
    /// downstream.
    ///
    /// Defaults to `Native` — no re-encode pass is performed and
    /// `ExtractedImage.format` reflects the source extractor's output.
    #[serde(alias = "outputFormat")]
    #[serde(default)]
    pub output_format: ImageOutputFormat,
    /// SVG-specific knobs for the image-encode pipeline.
    ///
    /// Controls sanitization and rasterization DPI when the source or output
    /// format is SVG.  Only available when the `svg` feature is active.
    #[serde(default)]
    pub svg: SvgOptions,
    /// When `true`, populate `ExtractedImage.data_base64` with a Base64-encoded
    /// copy of the raw image bytes.
    ///
    /// Useful for JSON-only clients that cannot efficiently parse the default
    /// integer-array serialization of `data`. Defaults to `false`; enabling it
    /// doubles the in-memory image representation for the duration of the response.
    #[php(prop, name = "includeDataBase64")]
    #[serde(default = "crate::serde_defaults::image_extraction_config_include_data_base64")]
    #[serde(alias = "includeDataBase64")]
    pub include_data_base64: bool,
}

impl Default for ImageExtractionConfig {
    fn default() -> Self {
        <xberg::ImageExtractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl ImageExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        extractImages: bool,
        targetDpi: i32,
        maxImageDimension: i32,
        injectPlaceholders: bool,
        autoAdjustDpi: bool,
        minDpi: i32,
        maxDpi: i32,
        classify: bool,
        includePageRasters: bool,
        runOcrOnImages: bool,
        ocrTextOnly: bool,
        appendOcrText: bool,
        outputFormat: &ImageOutputFormat,
        includeDataBase64: bool,
        maxImagesPerPage: Option<u32>,
    ) -> Self {
        Self {
            extract_images: extractImages,
            target_dpi: targetDpi,
            max_image_dimension: maxImageDimension,
            inject_placeholders: injectPlaceholders,
            auto_adjust_dpi: autoAdjustDpi,
            min_dpi: minDpi,
            max_dpi: maxDpi,
            max_images_per_page: maxImagesPerPage,
            classify,
            include_page_rasters: includePageRasters,
            run_ocr_on_images: runOcrOnImages,
            ocr_text_only: ocrTextOnly,
            append_ocr_text: appendOcrText,
            output_format: outputFormat.clone(),
            svg: Default::default(),
            include_data_base64: includeDataBase64,
        }
    }

    pub fn get_extract_images(&self) -> bool {
        self.extract_images.clone()
    }

    pub fn get_target_dpi(&self) -> i32 {
        self.target_dpi.clone()
    }

    pub fn get_max_image_dimension(&self) -> i32 {
        self.max_image_dimension.clone()
    }

    pub fn get_inject_placeholders(&self) -> bool {
        self.inject_placeholders.clone()
    }

    pub fn get_auto_adjust_dpi(&self) -> bool {
        self.auto_adjust_dpi.clone()
    }

    pub fn get_min_dpi(&self) -> i32 {
        self.min_dpi.clone()
    }

    pub fn get_max_dpi(&self) -> i32 {
        self.max_dpi.clone()
    }

    pub fn get_max_images_per_page(&self) -> Option<u32> {
        self.max_images_per_page.clone()
    }

    pub fn get_classify(&self) -> bool {
        self.classify.clone()
    }

    pub fn get_include_page_rasters(&self) -> bool {
        self.include_page_rasters.clone()
    }

    pub fn get_run_ocr_on_images(&self) -> bool {
        self.run_ocr_on_images.clone()
    }

    pub fn get_ocr_text_only(&self) -> bool {
        self.ocr_text_only.clone()
    }

    pub fn get_append_ocr_text(&self) -> bool {
        self.append_ocr_text.clone()
    }

    pub fn get_output_format(&self) -> ImageOutputFormat {
        self.output_format.clone()
    }

    pub fn get_svg(&self) -> SvgOptions {
        self.svg.clone()
    }

    pub fn get_include_data_base64(&self) -> bool {
        self.include_data_base64.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ImageExtractionConfig {
        xberg::ImageExtractionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ImageMetadata")]
#[serde(default)]
pub struct ImageMetadata {
    /// Image width in pixels
    #[php(prop, name = "width")]
    pub width: u32,
    /// Image height in pixels
    #[php(prop, name = "height")]
    pub height: u32,
    /// Image format (e.g., "PNG", "JPEG", "TIFF")
    #[php(prop, name = "format")]
    pub format: String,
    /// EXIF metadata tags
    #[php(prop, name = "exif")]
    pub exif: HashMap<String, String>,
}

impl Default for ImageMetadata {
    fn default() -> Self {
        <xberg::ImageMetadata as Default>::default().into()
    }
}

#[php_impl]
impl ImageMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(width: u32, height: u32, format: String, exif: HashMap<String, String>) -> Self {
        Self {
            width,
            height,
            format,
            exif,
        }
    }

    pub fn get_width(&self) -> u32 {
        self.width.clone()
    }

    pub fn get_height(&self) -> u32 {
        self.height.clone()
    }

    pub fn get_format(&self) -> String {
        self.format.clone()
    }

    pub fn get_exif(&self) -> HashMap<String, String> {
        self.exif.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ImageMetadataType")]
#[serde(default)]
pub struct ImageMetadataType {
    /// Image source (URL, data URI, or SVG content)
    #[php(prop, name = "src")]
    pub src: String,
    /// Alternative text from alt attribute
    #[php(prop, name = "alt")]
    pub alt: Option<String>,
    /// Title attribute
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Image dimensions if available.
    pub dimensions: Option<ImageDimensions>,
    /// Image type classification
    #[php(prop, name = "imageType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "imageType")]
    pub image_type: String,
    /// Additional attributes as key-value pairs.
    pub attributes: Vec<KeyValueAttribute>,
}

#[php_impl]
impl ImageMetadataType {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        src: String,
        imageType: String,
        attributes: &ext_php_rs::types::ZendHashTable,
        alt: Option<String>,
        title: Option<String>,
        dimensions: Option<&ImageDimensions>,
    ) -> PhpResult<Self> {
        let mut attributes_core_result: Vec<KeyValueAttribute> = Vec::new();
        for (_, item) in attributes.iter() {
            if let Some(parsed) = <&KeyValueAttribute as ext_php_rs::convert::FromZval>::from_zval(item) {
                attributes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to KeyValueAttribute".to_string(),
                ));
            }
        }
        let attributes_core: Vec<KeyValueAttribute> = attributes_core_result;
        Ok(Self {
            src,
            alt,
            title,
            dimensions: dimensions.cloned(),
            image_type: imageType,
            attributes: attributes_core,
        })
    }

    pub fn get_src(&self) -> String {
        self.src.clone()
    }

    pub fn get_alt(&self) -> Option<String> {
        self.alt.clone()
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_dimensions(&self) -> Option<ImageDimensions> {
        self.dimensions.clone().map(Into::into)
    }

    pub fn set_dimensions(&mut self, value: Option<&ImageDimensions>) {
        self.dimensions = value.map(|value| value.clone().into());
    }

    pub fn get_image_type(&self) -> String {
        self.image_type.clone()
    }

    pub fn get_attributes(&self) -> Vec<KeyValueAttribute> {
        self.attributes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ImagePreprocessingConfig")]
#[serde(default)]
pub struct ImagePreprocessingConfig {
    /// Target DPI for the image (300 is standard, 600 for small text).
    #[php(prop, name = "targetDpi")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_target_dpi")]
    #[serde(alias = "targetDpi")]
    pub target_dpi: i32,
    /// Auto-detect and correct image rotation.
    #[php(prop, name = "autoRotate")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_auto_rotate")]
    #[serde(alias = "autoRotate")]
    pub auto_rotate: bool,
    /// Correct skew (tilted images). Must be `false` with `none` or `off` binarization.
    #[php(prop, name = "deskew")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_deskew")]
    pub deskew: bool,
    /// Remove noise from the image.
    #[php(prop, name = "denoise")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_denoise")]
    pub denoise: bool,
    /// Enhance contrast for better text visibility. With `none` or `off` binarization, this
    /// applies background normalization and sharpening while preserving grayscale pixels.
    #[php(prop, name = "contrastEnhance")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_contrast_enhance")]
    #[serde(alias = "contrastEnhance")]
    pub contrast_enhance: bool,
    /// Binarization method: "none" (alias "off"), "otsu", "sauvola", or "adaptive".
    /// `deskew` must be `false` when this is `none` or `off`.
    #[php(prop, name = "binarizationMethod")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_binarization_method")]
    #[serde(alias = "binarizationMethod")]
    pub binarization_method: String,
    /// Invert colors (white text on black → black on white).
    #[php(prop, name = "invertColors")]
    #[serde(default = "crate::serde_defaults::image_preprocessing_config_invert_colors")]
    #[serde(alias = "invertColors")]
    pub invert_colors: bool,
}

impl Default for ImagePreprocessingConfig {
    fn default() -> Self {
        <xberg::ImagePreprocessingConfig as Default>::default().into()
    }
}

#[php_impl]
impl ImagePreprocessingConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        targetDpi: i32,
        autoRotate: bool,
        deskew: bool,
        denoise: bool,
        contrastEnhance: bool,
        binarizationMethod: String,
        invertColors: bool,
    ) -> Self {
        Self {
            target_dpi: targetDpi,
            auto_rotate: autoRotate,
            deskew,
            denoise,
            contrast_enhance: contrastEnhance,
            binarization_method: binarizationMethod,
            invert_colors: invertColors,
        }
    }

    pub fn get_target_dpi(&self) -> i32 {
        self.target_dpi.clone()
    }

    pub fn get_auto_rotate(&self) -> bool {
        self.auto_rotate.clone()
    }

    pub fn get_deskew(&self) -> bool {
        self.deskew.clone()
    }

    pub fn get_denoise(&self) -> bool {
        self.denoise.clone()
    }

    pub fn get_contrast_enhance(&self) -> bool {
        self.contrast_enhance.clone()
    }

    pub fn get_binarization_method(&self) -> String {
        self.binarization_method.clone()
    }

    pub fn get_invert_colors(&self) -> bool {
        self.invert_colors.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ImagePreprocessingConfig {
        xberg::ImagePreprocessingConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ImagePreprocessingMetadata")]
#[serde(default)]
pub struct ImagePreprocessingMetadata {
    /// Original image dimensions in pixels.
    #[serde(alias = "originalDimensions")]
    pub original_dimensions: PixelDimensions,
    /// Original image resolution.
    #[serde(alias = "originalDpi")]
    pub original_dpi: ImageDpi,
    /// Target DPI from configuration
    #[php(prop, name = "targetDpi")]
    #[serde(alias = "targetDpi")]
    pub target_dpi: i32,
    /// Scaling factor applied to the image
    #[php(prop, name = "scaleFactor")]
    #[serde(alias = "scaleFactor")]
    pub scale_factor: f64,
    /// Whether DPI was auto-adjusted based on content
    #[php(prop, name = "autoAdjusted")]
    #[serde(alias = "autoAdjusted")]
    pub auto_adjusted: bool,
    /// Final DPI after processing
    #[php(prop, name = "finalDpi")]
    #[serde(alias = "finalDpi")]
    pub final_dpi: i32,
    /// New dimensions after resizing (if resized).
    #[serde(alias = "newDimensions")]
    pub new_dimensions: Option<PixelDimensions>,
    /// Resampling algorithm used ("LANCZOS3", "CATMULLROM", etc.)
    #[php(prop, name = "resampleMethod")]
    #[serde(alias = "resampleMethod")]
    pub resample_method: String,
    /// Whether dimensions were clamped to max_image_dimension
    #[php(prop, name = "dimensionClamped")]
    #[serde(alias = "dimensionClamped")]
    pub dimension_clamped: bool,
    /// Calculated optimal DPI (if auto_adjust_dpi enabled)
    #[php(prop, name = "calculatedDpi")]
    #[serde(alias = "calculatedDpi")]
    pub calculated_dpi: Option<i32>,
    /// Whether resize was skipped (dimensions already optimal)
    #[php(prop, name = "skippedResize")]
    #[serde(alias = "skippedResize")]
    pub skipped_resize: bool,
    /// Error message if resize failed
    #[php(prop, name = "resizeError")]
    #[serde(alias = "resizeError")]
    pub resize_error: Option<String>,
}

#[php_impl]
impl ImagePreprocessingMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        originalDimensions: &PixelDimensions,
        originalDpi: &ImageDpi,
        targetDpi: i32,
        scaleFactor: f64,
        autoAdjusted: bool,
        finalDpi: i32,
        resampleMethod: String,
        dimensionClamped: bool,
        skippedResize: bool,
        newDimensions: Option<&PixelDimensions>,
        calculatedDpi: Option<i32>,
        resizeError: Option<String>,
    ) -> Self {
        Self {
            original_dimensions: originalDimensions.clone(),
            original_dpi: originalDpi.clone(),
            target_dpi: targetDpi,
            scale_factor: scaleFactor,
            auto_adjusted: autoAdjusted,
            final_dpi: finalDpi,
            new_dimensions: newDimensions.cloned(),
            resample_method: resampleMethod,
            dimension_clamped: dimensionClamped,
            calculated_dpi: calculatedDpi,
            skipped_resize: skippedResize,
            resize_error: resizeError,
        }
    }

    pub fn get_original_dimensions(&self) -> PixelDimensions {
        self.original_dimensions.clone()
    }

    pub fn get_original_dpi(&self) -> ImageDpi {
        self.original_dpi.clone()
    }

    pub fn get_target_dpi(&self) -> i32 {
        self.target_dpi.clone()
    }

    pub fn get_scale_factor(&self) -> f64 {
        self.scale_factor.clone()
    }

    pub fn get_auto_adjusted(&self) -> bool {
        self.auto_adjusted.clone()
    }

    pub fn get_final_dpi(&self) -> i32 {
        self.final_dpi.clone()
    }

    pub fn get_new_dimensions(&self) -> Option<PixelDimensions> {
        self.new_dimensions.clone().map(Into::into)
    }

    pub fn set_new_dimensions(&mut self, value: Option<&PixelDimensions>) {
        self.new_dimensions = value.map(|value| value.clone().into());
    }

    pub fn get_resample_method(&self) -> String {
        self.resample_method.clone()
    }

    pub fn get_dimension_clamped(&self) -> bool {
        self.dimension_clamped.clone()
    }

    pub fn get_calculated_dpi(&self) -> Option<i32> {
        self.calculated_dpi.clone()
    }

    pub fn get_skipped_resize(&self) -> bool {
        self.skipped_resize.clone()
    }

    pub fn get_resize_error(&self) -> Option<String> {
        self.resize_error.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\InlineElement")]
#[serde(default)]
pub struct InlineElement {
    /// Type of inline element
    #[php(prop, name = "elementType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "elementType")]
    pub element_type: String,
    /// Text content
    #[php(prop, name = "content")]
    pub content: String,
    /// Element attributes
    pub attributes: Option<Attributes>,
    /// Additional metadata (e.g., href for links, src/alt for images)
    #[php(prop, name = "metadata")]
    pub metadata: Option<HashMap<String, String>>,
}

#[php_impl]
impl InlineElement {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        elementType: String,
        content: String,
        attributes: Option<&Attributes>,
        metadata: Option<HashMap<String, String>>,
    ) -> Self {
        Self {
            element_type: elementType,
            content,
            attributes: attributes.cloned(),
            metadata,
        }
    }

    pub fn get_element_type(&self) -> String {
        self.element_type.clone()
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_attributes(&self) -> Option<Attributes> {
        self.attributes.clone().map(Into::into)
    }

    pub fn set_attributes(&mut self, value: Option<&Attributes>) {
        self.attributes = value.map(|value| value.clone().into());
    }

    pub fn get_metadata(&self) -> Option<HashMap<String, String>> {
        self.metadata.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\JatsMetadata")]
#[serde(default)]
pub struct JatsMetadata {
    /// Copyright statement from the article's `<permissions>` element.
    #[php(prop, name = "copyright")]
    pub copyright: Option<String>,
    /// Open-access license URI from the article's `<license>` element.
    #[php(prop, name = "license")]
    pub license: Option<String>,
    /// Publication history dates keyed by event type (e.g. `"received"`, `"accepted"`).
    #[php(prop, name = "historyDates")]
    #[serde(alias = "historyDates")]
    #[serde(default)]
    pub history_dates: HashMap<String, String>,
    /// Authors and contributors with their stated roles.
    #[serde(alias = "contributorRoles")]
    #[serde(default)]
    pub contributor_roles: Vec<ContributorRole>,
}

impl Default for JatsMetadata {
    fn default() -> Self {
        <xberg::JatsMetadata as Default>::default().into()
    }
}

#[php_impl]
impl JatsMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        historyDates: HashMap<String, String>,
        contributorRoles: &ext_php_rs::types::ZendHashTable,
        copyright: Option<String>,
        license: Option<String>,
    ) -> PhpResult<Self> {
        let mut contributorRoles_core_result: Vec<ContributorRole> = Vec::new();
        for (_, item) in contributorRoles.iter() {
            if let Some(parsed) = <&ContributorRole as ext_php_rs::convert::FromZval>::from_zval(item) {
                contributorRoles_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ContributorRole".to_string(),
                ));
            }
        }
        let contributorRoles_core: Vec<ContributorRole> = contributorRoles_core_result;
        Ok(Self {
            copyright,
            license,
            history_dates: historyDates,
            contributor_roles: contributorRoles_core,
        })
    }

    pub fn get_copyright(&self) -> Option<String> {
        self.copyright.clone()
    }

    pub fn get_license(&self) -> Option<String> {
        self.license.clone()
    }

    pub fn get_history_dates(&self) -> HashMap<String, String> {
        self.history_dates.clone()
    }

    pub fn get_contributor_roles(&self) -> Vec<ContributorRole> {
        self.contributor_roles.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\KeyValueAttribute")]
#[serde(default)]
pub struct KeyValueAttribute {
    /// Attribute name.
    #[php(prop, name = "key")]
    pub key: String,
    /// Attribute value.
    #[php(prop, name = "value")]
    pub value: String,
}

impl Default for KeyValueAttribute {
    fn default() -> Self {
        <xberg::KeyValueAttribute as Default>::default().into()
    }
}

#[php_impl]
impl KeyValueAttribute {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(key: String, value: String) -> Self {
        Self { key, value }
    }

    pub fn get_key(&self) -> String {
        self.key.clone()
    }

    pub fn get_value(&self) -> String {
        self.value.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Keyword")]
#[serde(default)]
pub struct Keyword {
    /// The keyword text.
    #[php(prop, name = "text")]
    pub text: String,
    /// Relevance score (higher is better, algorithm-specific range).
    #[php(prop, name = "score")]
    pub score: f32,
    /// Algorithm that extracted this keyword.
    #[php(prop, name = "algorithm")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub algorithm: String,
    /// Optional positions where keyword appears in text (character offsets).
    #[php(prop, name = "positions")]
    pub positions: Option<Vec<i64>>,
}

#[php_impl]
impl Keyword {
    #[php(constructor)]
    pub fn new(text: String, score: f32, algorithm: String, positions: Option<Vec<i64>>) -> Self {
        Self {
            text,
            score,
            algorithm,
            positions,
        }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_score(&self) -> f32 {
        self.score.clone()
    }

    pub fn get_algorithm(&self) -> String {
        self.algorithm.clone()
    }

    pub fn get_positions(&self) -> Option<Vec<i64>> {
        self.positions.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\KeywordConfig")]
#[serde(default)]
pub struct KeywordConfig {
    /// Algorithm to use for extraction.
    #[php(prop, name = "algorithm")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(default)]
    pub algorithm: String,
    /// Maximum number of keywords to extract (default: 10).
    #[php(prop, name = "maxKeywords")]
    #[serde(default = "crate::serde_defaults::keyword_config_max_keywords")]
    #[serde(alias = "maxKeywords")]
    pub max_keywords: i64,
    /// Minimum score threshold (0.0-1.0, default: 0.0).
    ///
    /// Keywords with scores below this threshold are filtered out.
    /// Note: Score ranges differ between algorithms.
    #[php(prop, name = "minScore")]
    #[serde(default = "crate::serde_defaults::keyword_config_min_score")]
    #[serde(alias = "minScore")]
    pub min_score: f32,
    /// N-gram range for keyword extraction (min, max).
    ///
    /// (1, 1) = unigrams only
    /// (1, 2) = unigrams and bigrams
    /// (1, 3) = unigrams, bigrams, and trigrams (default)
    #[serde(alias = "ngramRange")]
    #[serde(default)]
    pub ngram_range: NgramRange,
    /// Language code for stopword filtering (e.g., "en", "de", "fr").
    ///
    /// If undefined, no stopword filtering is applied.
    #[php(prop, name = "language")]
    pub language: Option<String>,
    /// YAKE-specific tuning parameters.
    #[serde(alias = "yakeParams")]
    pub yake_params: Option<YakeParams>,
    /// RAKE-specific tuning parameters.
    #[serde(alias = "rakeParams")]
    pub rake_params: Option<RakeParams>,
}

impl Default for KeywordConfig {
    fn default() -> Self {
        <xberg::KeywordConfig as Default>::default().into()
    }
}

#[php_impl]
impl KeywordConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        algorithm: String,
        maxKeywords: i64,
        minScore: f32,
        ngramRange: &NgramRange,
        language: Option<String>,
    ) -> Self {
        Self {
            algorithm,
            max_keywords: maxKeywords,
            min_score: minScore,
            ngram_range: ngramRange.clone(),
            language,
            yake_params: Default::default(),
            rake_params: Default::default(),
        }
    }

    pub fn get_algorithm(&self) -> String {
        self.algorithm.clone()
    }

    pub fn get_max_keywords(&self) -> i64 {
        self.max_keywords.clone()
    }

    pub fn get_min_score(&self) -> f32 {
        self.min_score.clone()
    }

    pub fn get_ngram_range(&self) -> NgramRange {
        self.ngram_range.clone()
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_yake_params(&self) -> Option<YakeParams> {
        self.yake_params.clone().map(Into::into)
    }

    pub fn set_yake_params(&mut self, value: Option<&YakeParams>) {
        self.yake_params = value.map(|value| value.clone().into());
    }

    pub fn get_rake_params(&self) -> Option<RakeParams> {
        self.rake_params.clone().map(Into::into)
    }

    pub fn set_rake_params(&mut self, value: Option<&RakeParams>) {
        self.rake_params = value.map(|value| value.clone().into());
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> KeywordConfig {
        xberg::KeywordConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\LanguageConfidence")]
#[serde(default)]
pub struct LanguageConfidence {
    /// ISO 639-3 language code, matching the corresponding entry in `detected_languages`.
    #[php(prop, name = "language")]
    pub language: String,
    /// Confidence for this language, in `[0.0, 1.0]`.
    ///
    /// In single-language mode this is whatlang's `Info.confidence()` for the whole
    /// document. In multi-language mode this is the average whatlang confidence across
    /// the document's 200-character chunks that were classified as this language.
    #[php(prop, name = "confidence")]
    pub confidence: f64,
    /// Share of the document's analyzed content classified as this language, in `[0.0, 1.0]`.
    ///
    /// In single-language mode this is always `1.0`. In multi-language mode this is the
    /// fraction of 200-character chunks classified as this language (chunks that did not
    /// meet `min_confidence` for any language are excluded from the count but still count
    /// toward the denominator).
    #[php(prop, name = "proportion")]
    pub proportion: f64,
    /// Writing system whatlang detected for this language (e.g. `"Latin"`, `"Cyrillic"`).
    #[php(prop, name = "script")]
    pub script: String,
    /// Whether this detection is considered reliable.
    ///
    /// In single-language mode this is whatlang's own `Info.is_reliable()` (confidence
    /// above whatlang's internal 0.9 threshold). In multi-language mode this is the
    /// chunk-averaged `confidence` above that same 0.9 threshold, since whatlang's
    /// `is_reliable()` only applies to a single detection.
    #[php(prop, name = "reliable")]
    pub reliable: bool,
}

#[php_impl]
impl LanguageConfidence {
    #[php(constructor)]
    pub fn new(language: String, confidence: f64, proportion: f64, script: String, reliable: bool) -> Self {
        Self {
            language,
            confidence,
            proportion,
            script,
            reliable,
        }
    }

    pub fn get_language(&self) -> String {
        self.language.clone()
    }

    pub fn get_confidence(&self) -> f64 {
        self.confidence.clone()
    }

    pub fn get_proportion(&self) -> f64 {
        self.proportion.clone()
    }

    pub fn get_script(&self) -> String {
        self.script.clone()
    }

    pub fn get_reliable(&self) -> bool {
        self.reliable.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LanguageDetectionConfig")]
#[serde(default)]
pub struct LanguageDetectionConfig {
    /// Enable language detection
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::language_detection_config_enabled")]
    pub enabled: bool,
    /// Minimum confidence threshold (0.0-1.0)
    #[php(prop, name = "minConfidence")]
    #[serde(default = "crate::serde_defaults::language_detection_config_min_confidence")]
    #[serde(alias = "minConfidence")]
    pub min_confidence: f64,
    /// Detect multiple languages in the document
    #[php(prop, name = "detectMultiple")]
    #[serde(default = "crate::serde_defaults::language_detection_config_detect_multiple")]
    #[serde(alias = "detectMultiple")]
    pub detect_multiple: bool,
}

impl Default for LanguageDetectionConfig {
    fn default() -> Self {
        <xberg::LanguageDetectionConfig as Default>::default().into()
    }
}

#[php_impl]
impl LanguageDetectionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(enabled: bool, minConfidence: f64, detectMultiple: bool) -> Self {
        Self {
            enabled,
            min_confidence: minConfidence,
            detect_multiple: detectMultiple,
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_min_confidence(&self) -> f64 {
        self.min_confidence.clone()
    }

    pub fn get_detect_multiple(&self) -> bool {
        self.detect_multiple.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> LanguageDetectionConfig {
        xberg::LanguageDetectionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LateInteractionConfig")]
#[serde(default)]
pub struct LateInteractionConfig {
    /// The late-interaction model to use (defaults to the "gte-moderncolbert" preset).
    pub model: LateInteractionModelType,
    /// Batch size for local ONNX inference.
    ///
    /// ColBERT emits a `[seq, dim]` multi-vector embedding per document, so
    /// memory scales with batch size — keep this modest.
    #[php(prop, name = "batchSize")]
    #[serde(default = "crate::serde_defaults::late_interaction_config_batch_size")]
    #[serde(alias = "batchSize")]
    pub batch_size: i64,
    /// Maximum token sequence length for the tokenizer (documents).
    #[php(prop, name = "maxLength")]
    #[serde(default = "crate::serde_defaults::late_interaction_config_max_length")]
    #[serde(alias = "maxLength")]
    pub max_length: i64,
    /// Fixed padded length for query augmentation.
    ///
    /// ColBERT queries are padded (with the mask token, kept attention-live)
    /// to exactly this many tokens rather than truncated/left as-is — this is
    /// the "query augmentation" trick from the ColBERT paper.
    #[php(prop, name = "queryMaxLength")]
    #[serde(default = "crate::serde_defaults::late_interaction_config_query_max_length")]
    #[serde(alias = "queryMaxLength")]
    pub query_max_length: i64,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `LateInteractionModelType.Plugin`, which downloads no model.
    #[php(prop, name = "showDownloadProgress")]
    #[serde(default = "crate::serde_defaults::late_interaction_config_show_download_progress")]
    #[serde(alias = "showDownloadProgress")]
    pub show_download_progress: bool,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the late-interaction ONNX model.
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `LateInteractionModelType.Plugin`. `None` disables the timeout.
    #[php(prop, name = "maxEmbedDurationSecs")]
    #[serde(alias = "maxEmbedDurationSecs")]
    pub max_embed_duration_secs: Option<i64>,
}

impl Default for LateInteractionConfig {
    fn default() -> Self {
        <xberg::LateInteractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl LateInteractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: &LateInteractionModelType,
        batchSize: i64,
        maxLength: i64,
        queryMaxLength: i64,
        showDownloadProgress: bool,
        cacheDir: Option<String>,
        acceleration: Option<&AccelerationConfig>,
        maxEmbedDurationSecs: Option<i64>,
    ) -> Self {
        Self {
            model: model.clone(),
            batch_size: batchSize,
            max_length: maxLength,
            query_max_length: queryMaxLength,
            show_download_progress: showDownloadProgress,
            cache_dir: cacheDir,
            acceleration: acceleration.cloned(),
            max_embed_duration_secs: maxEmbedDurationSecs,
        }
    }

    pub fn get_model(&self) -> LateInteractionModelType {
        self.model.clone()
    }

    pub fn get_batch_size(&self) -> i64 {
        self.batch_size.clone()
    }

    pub fn get_max_length(&self) -> i64 {
        self.max_length.clone()
    }

    pub fn get_query_max_length(&self) -> i64 {
        self.query_max_length.clone()
    }

    pub fn get_show_download_progress(&self) -> bool {
        self.show_download_progress.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_max_embed_duration_secs(&self) -> Option<i64> {
        self.max_embed_duration_secs.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> LateInteractionConfig {
        xberg::LateInteractionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\LateInteractionMatch")]
#[serde(default)]
pub struct LateInteractionMatch {
    /// Position of this document in the original input slice.
    #[php(prop, name = "index")]
    pub index: i64,
    /// MaxSim relevance score. Higher means more relevant to the query.
    #[php(prop, name = "score")]
    pub score: f32,
}

#[php_impl]
impl LateInteractionMatch {
    #[php(constructor)]
    pub fn new(index: i64, score: f32) -> Self {
        Self { index, score }
    }

    pub fn get_index(&self) -> i64 {
        self.index.clone()
    }

    pub fn get_score(&self) -> f32 {
        self.score.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\LateInteractionPreset")]
#[serde(default)]
pub struct LateInteractionPreset {
    /// Stable preset name referenced from config.
    #[php(prop, name = "name")]
    pub name: String,
    /// HuggingFace repository hosting the ONNX model.
    #[php(prop, name = "modelRepo")]
    #[serde(alias = "modelRepo")]
    pub model_repo: String,
    /// Path to the ONNX file within the repo.
    #[php(prop, name = "modelFile")]
    #[serde(alias = "modelFile")]
    pub model_file: String,
    /// Sibling files that must be downloaded alongside `model_file`.
    #[php(prop, name = "additionalFiles")]
    #[serde(alias = "additionalFiles")]
    pub additional_files: Vec<String>,
    /// Maximum document token sequence length.
    #[php(prop, name = "maxLength")]
    #[serde(alias = "maxLength")]
    pub max_length: i64,
    /// Fixed padded query length (ColBERT query augmentation).
    #[php(prop, name = "queryMaxLength")]
    #[serde(alias = "queryMaxLength")]
    pub query_max_length: i64,
    /// Per-token embedding dimensionality.
    #[php(prop, name = "dim")]
    pub dim: i64,
    /// Human-readable description.
    #[php(prop, name = "description")]
    pub description: String,
}

#[php_impl]
impl LateInteractionPreset {
    #[php(constructor)]
    pub fn new(
        name: String,
        modelRepo: String,
        modelFile: String,
        additionalFiles: Vec<String>,
        maxLength: i64,
        queryMaxLength: i64,
        dim: i64,
        description: String,
    ) -> Self {
        Self {
            name,
            model_repo: modelRepo,
            model_file: modelFile,
            additional_files: additionalFiles,
            max_length: maxLength,
            query_max_length: queryMaxLength,
            dim,
            description,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_model_repo(&self) -> String {
        self.model_repo.clone()
    }

    pub fn get_model_file(&self) -> String {
        self.model_file.clone()
    }

    pub fn get_additional_files(&self) -> Vec<String> {
        self.additional_files.clone()
    }

    pub fn get_max_length(&self) -> i64 {
        self.max_length.clone()
    }

    pub fn get_query_max_length(&self) -> i64 {
        self.query_max_length.clone()
    }

    pub fn get_dim(&self) -> i64 {
        self.dim.clone()
    }

    pub fn get_description(&self) -> String {
        self.description.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\LayoutDetection")]
#[serde(default)]
pub struct LayoutDetection {
    /// Detected layout class (e.g. `Table`, `Text`, `Title`).
    #[php(prop, name = "className")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "className")]
    pub class_name: String,
    /// Detection confidence score in `[0.0, 1.0]`.
    #[php(prop, name = "confidence")]
    pub confidence: f32,
    /// Bounding box in image pixel coordinates.
    pub bbox: BBox,
}

#[php_impl]
impl LayoutDetection {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(className: String, confidence: f32, bbox: &BBox) -> Self {
        Self {
            class_name: className,
            confidence,
            bbox: bbox.clone(),
        }
    }

    pub fn get_class_name(&self) -> String {
        self.class_name.clone()
    }

    pub fn get_confidence(&self) -> f32 {
        self.confidence.clone()
    }

    pub fn get_bbox(&self) -> BBox {
        self.bbox.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LayoutDetectionConfig")]
#[serde(default)]
pub struct LayoutDetectionConfig {
    /// Which pages the layout model runs on.
    ///
    /// Defaults to `LayoutStrategy.Always`, the historical behavior:
    /// every page is rendered and inferred. `LayoutStrategy.Auto`
    /// pre-screens pages with cheap signals and skips the model where it
    /// cannot help.
    #[php(prop, name = "strategy")]
    #[serde(default = "crate::serde_defaults::layout_detection_config_strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub strategy: String,
    /// Confidence threshold override (undefined = use model default).
    #[php(prop, name = "confidenceThreshold")]
    #[serde(alias = "confidenceThreshold")]
    #[serde(default)]
    pub confidence_threshold: Option<f32>,
    /// Whether to apply postprocessing heuristics (default: true).
    #[php(prop, name = "applyHeuristics")]
    #[serde(default = "crate::serde_defaults::layout_detection_config_apply_heuristics")]
    #[serde(alias = "applyHeuristics")]
    pub apply_heuristics: bool,
    /// Table structure recognition model.
    ///
    /// Controls which model is used for table cell detection within layout-detected
    /// table regions. Defaults to `TableModel.Tatr`.
    #[php(prop, name = "tableModel")]
    #[serde(default = "crate::serde_defaults::layout_detection_config_table_model")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "tableModel")]
    pub table_model: String,
    /// Formula recognition model for layout-detected formula regions.
    ///
    /// `None` (the default) keeps the plain OCR text of the region. Setting a
    /// model converts each formula region crop to LaTeX. Requires the
    /// `formula-recognition` feature; without it the setting is ignored.
    #[php(prop, name = "formulaModel")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "formulaModel")]
    #[serde(default)]
    pub formula_model: Option<String>,
    /// How to resolve overlapping native vs layout tables.
    ///
    /// When a native table and a layout (TATR/SLANeXT) table overlap on the
    /// same region, this controls which one is kept. Defaults to
    /// `TableOverlapPreference.Content` (historical behavior: keep the table with
    /// more content). Set to `TableOverlapPreference.Native` to favor source
    /// reading order (higher text F1) over the model's cell reflow.
    #[php(prop, name = "tableOverlapPreference")]
    #[serde(default = "crate::serde_defaults::layout_detection_config_table_overlap_preference")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "tableOverlapPreference")]
    pub table_overlap_preference: String,
    /// Hardware acceleration for ONNX models (layout detection + table structure).
    ///
    /// When set, controls which execution provider (CPU, CUDA, CoreML, TensorRT)
    /// is used for inference. Defaults to `None` (auto-select per platform).
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Route regions classified as charts to the chart-understanding OCR task.
    ///
    /// When `true`, layout regions detected as charts are sent to the VLM
    /// chart task (data-series/axis recovery) instead of being treated as
    /// generic image regions. Defaults to `false` — chart understanding is
    /// opt-in and has no effect on standard text/table extraction scores.
    #[php(prop, name = "enableChartUnderstanding")]
    #[serde(default = "crate::serde_defaults::layout_detection_config_enable_chart_understanding")]
    #[serde(alias = "enableChartUnderstanding")]
    pub enable_chart_understanding: bool,
}

impl Default for LayoutDetectionConfig {
    fn default() -> Self {
        <xberg::LayoutDetectionConfig as Default>::default().into()
    }
}

#[php_impl]
impl LayoutDetectionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        strategy: String,
        applyHeuristics: bool,
        tableModel: String,
        tableOverlapPreference: String,
        enableChartUnderstanding: bool,
        confidenceThreshold: Option<f32>,
        formulaModel: Option<String>,
        acceleration: Option<&AccelerationConfig>,
    ) -> Self {
        Self {
            strategy,
            confidence_threshold: confidenceThreshold,
            apply_heuristics: applyHeuristics,
            table_model: tableModel,
            formula_model: formulaModel,
            table_overlap_preference: tableOverlapPreference,
            acceleration: acceleration.cloned(),
            enable_chart_understanding: enableChartUnderstanding,
        }
    }

    pub fn get_strategy(&self) -> String {
        self.strategy.clone()
    }

    pub fn get_confidence_threshold(&self) -> Option<f32> {
        self.confidence_threshold.clone()
    }

    pub fn get_apply_heuristics(&self) -> bool {
        self.apply_heuristics.clone()
    }

    pub fn get_table_model(&self) -> String {
        self.table_model.clone()
    }

    pub fn get_formula_model(&self) -> Option<String> {
        self.formula_model.clone().map(Into::into)
    }

    pub fn get_table_overlap_preference(&self) -> String {
        self.table_overlap_preference.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_enable_chart_understanding(&self) -> bool {
        self.enable_chart_understanding.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> LayoutDetectionConfig {
        xberg::LayoutDetectionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LayoutRegion")]
#[serde(default)]
pub struct LayoutRegion {
    /// Layout class name (e.g. "picture", "table", "text", "section_header").
    #[php(prop, name = "className")]
    #[serde(alias = "className")]
    pub class_name: String,
    /// Confidence score from the layout detection model (0.0 to 1.0).
    #[php(prop, name = "confidence")]
    pub confidence: f64,
    /// Bounding box in document coordinate space.
    #[serde(alias = "boundingBox")]
    pub bounding_box: BoundingBox,
    /// Fraction of the page area covered by this region (0.0 to 1.0).
    #[php(prop, name = "areaFraction")]
    #[serde(alias = "areaFraction")]
    pub area_fraction: f64,
}

impl Default for LayoutRegion {
    fn default() -> Self {
        <xberg::LayoutRegion as Default>::default().into()
    }
}

#[php_impl]
impl LayoutRegion {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(className: String, confidence: f64, boundingBox: &BoundingBox, areaFraction: f64) -> Self {
        Self {
            class_name: className,
            confidence,
            bounding_box: boundingBox.clone(),
            area_fraction: areaFraction,
        }
    }

    pub fn get_class_name(&self) -> String {
        self.class_name.clone()
    }

    pub fn get_confidence(&self) -> f64 {
        self.confidence.clone()
    }

    pub fn get_bounding_box(&self) -> BoundingBox {
        self.bounding_box.clone()
    }

    pub fn get_area_fraction(&self) -> f64 {
        self.area_fraction.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\LinkMetadata")]
#[serde(default)]
pub struct LinkMetadata {
    /// The href URL value
    #[php(prop, name = "href")]
    pub href: String,
    /// Link text content (normalized)
    #[php(prop, name = "text")]
    pub text: String,
    /// Optional title attribute
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Link type classification
    #[php(prop, name = "linkType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "linkType")]
    pub link_type: String,
    /// Rel attribute values
    #[php(prop, name = "rel")]
    pub rel: Vec<String>,
    /// Additional attributes as key-value pairs.
    pub attributes: Vec<KeyValueAttribute>,
}

#[php_impl]
impl LinkMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        href: String,
        text: String,
        linkType: String,
        rel: Vec<String>,
        attributes: &ext_php_rs::types::ZendHashTable,
        title: Option<String>,
    ) -> PhpResult<Self> {
        let mut attributes_core_result: Vec<KeyValueAttribute> = Vec::new();
        for (_, item) in attributes.iter() {
            if let Some(parsed) = <&KeyValueAttribute as ext_php_rs::convert::FromZval>::from_zval(item) {
                attributes_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to KeyValueAttribute".to_string(),
                ));
            }
        }
        let attributes_core: Vec<KeyValueAttribute> = attributes_core_result;
        Ok(Self {
            href,
            text,
            title,
            link_type: linkType,
            rel,
            attributes: attributes_core,
        })
    }

    pub fn get_href(&self) -> String {
        self.href.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_link_type(&self) -> String {
        self.link_type.clone()
    }

    pub fn get_rel(&self) -> Vec<String> {
        self.rel.clone()
    }

    pub fn get_attributes(&self) -> Vec<KeyValueAttribute> {
        self.attributes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmBudgetConfig")]
#[serde(default)]
pub struct LlmBudgetConfig {
    /// Global spend limit in USD.
    #[php(prop, name = "globalLimit")]
    #[serde(alias = "globalLimit")]
    #[serde(default)]
    pub global_limit: Option<f64>,
    /// Per-model spend limits in USD, keyed by model name.
    #[php(prop, name = "modelLimits")]
    #[serde(alias = "modelLimits")]
    #[serde(default)]
    pub model_limits: Option<HashMap<String, f64>>,
    /// Enforcement mode: `"hard"` (reject over-budget requests) or `"soft"` (log only).
    #[php(prop, name = "enforcement")]
    #[serde(default)]
    pub enforcement: Option<String>,
}

impl Default for LlmBudgetConfig {
    fn default() -> Self {
        <xberg::LlmBudgetConfig as Default>::default().into()
    }
}

#[php_impl]
impl LlmBudgetConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_global_limit(&self) -> Option<f64> {
        self.global_limit.clone()
    }

    pub fn get_model_limits(&self) -> Option<HashMap<String, f64>> {
        self.model_limits.clone()
    }

    pub fn get_enforcement(&self) -> Option<String> {
        self.enforcement.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmCacheConfig")]
#[serde(default)]
pub struct LlmCacheConfig {
    /// Maximum number of cached entries.
    #[php(prop, name = "maxEntries")]
    #[serde(alias = "maxEntries")]
    #[serde(default)]
    pub max_entries: Option<i64>,
    /// Cache entry time-to-live, in seconds.
    #[php(prop, name = "ttlSeconds")]
    #[serde(alias = "ttlSeconds")]
    #[serde(default)]
    pub ttl_seconds: Option<i64>,
    /// Cache backend name (e.g. `"memory"`, or an `opendal` scheme).
    #[php(prop, name = "backend")]
    #[serde(default)]
    pub backend: Option<String>,
    /// Backend-specific configuration key/value pairs.
    #[php(prop, name = "backendConfig")]
    #[serde(alias = "backendConfig")]
    #[serde(default)]
    pub backend_config: Option<HashMap<String, String>>,
}

impl Default for LlmCacheConfig {
    fn default() -> Self {
        <xberg::LlmCacheConfig as Default>::default().into()
    }
}

#[php_impl]
impl LlmCacheConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_max_entries(&self) -> Option<i64> {
        self.max_entries.clone()
    }

    pub fn get_ttl_seconds(&self) -> Option<i64> {
        self.ttl_seconds.clone()
    }

    pub fn get_backend(&self) -> Option<String> {
        self.backend.clone()
    }

    pub fn get_backend_config(&self) -> Option<HashMap<String, String>> {
        self.backend_config.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmConfig")]
#[serde(default)]
pub struct LlmConfig {
    /// Provider/model string using liter-llm routing format.
    ///
    /// Examples: `"openai/gpt-4o"`, `"anthropic/claude-sonnet-4-20250514"`,
    /// `"groq/llama-3.1-70b-versatile"`.
    #[php(prop, name = "model")]
    pub model: String,
    /// API key for the provider. When `None`, liter-llm falls back to
    /// the provider's standard environment variable (e.g., `OPENAI_API_KEY`).
    #[php(prop, name = "apiKey")]
    #[serde(alias = "apiKey")]
    #[serde(default)]
    pub api_key: Option<String>,
    /// Custom base URL override for the provider endpoint.
    #[php(prop, name = "baseUrl")]
    #[serde(alias = "baseUrl")]
    #[serde(default)]
    pub base_url: Option<String>,
    /// Request timeout in seconds. When `None`, liter-llm's built-in 60s default
    /// applies, except the VLM OCR path which uses a 300s default (a single page
    /// image transcription routinely exceeds 60s). Set explicitly to override.
    #[php(prop, name = "timeoutSecs")]
    #[serde(alias = "timeoutSecs")]
    #[serde(default)]
    pub timeout_secs: Option<i64>,
    /// Maximum retry attempts (default: 3).
    #[php(prop, name = "maxRetries")]
    #[serde(alias = "maxRetries")]
    #[serde(default)]
    pub max_retries: Option<u32>,
    /// Sampling temperature for generation tasks.
    #[php(prop, name = "temperature")]
    #[serde(default)]
    pub temperature: Option<f64>,
    /// Maximum tokens to generate.
    #[php(prop, name = "maxTokens")]
    #[serde(alias = "maxTokens")]
    #[serde(default)]
    pub max_tokens: Option<i64>,
    /// Nucleus sampling parameter for generation tasks, applied to individual
    /// requests built from this config. Restricts sampling to the smallest set of
    /// tokens whose cumulative probability mass is at least this value; lower is
    /// more focused. Validated to `[0.0, 1.0]` by `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.top_p`. A request-time
    /// parameter like `temperature`/`max_tokens` above, not a client-level
    /// setting.
    #[php(prop, name = "topP")]
    #[serde(alias = "topP")]
    #[serde(default)]
    pub top_p: Option<f64>,
    /// Stop sequence(s) that halt token generation, applied to individual requests
    /// built from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.stop`
    /// (`types.common.StopSequence`), which liter-llm represents as either a
    /// single string or a list of strings via an untagged enum. Always expressed
    /// here as a list — even one stop sequence is `["..."]` — so the field has a
    /// single, FFI-friendly shape across every language binding instead of a
    /// single-or-list union type. Converted to liter-llm's
    /// `StopSequence.Multiple` at each request-building call site; see
    /// `llm.client.to_stop_sequence`.
    #[php(prop, name = "stop")]
    #[serde(default)]
    pub stop: Option<Vec<String>>,
    /// Random seed for reproducible outputs, applied to individual requests built
    /// from this config. Provider support varies — some silently ignore it.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.seed`.
    #[php(prop, name = "seed")]
    #[serde(default)]
    pub seed: Option<i64>,
    /// Presence penalty for generation tasks, applied to individual requests
    /// built from this config. Positive values discourage the model from
    /// repeating topics already present in the conversation. Validated to
    /// `[-2.0, 2.0]` by `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.presence_penalty`.
    #[php(prop, name = "presencePenalty")]
    #[serde(alias = "presencePenalty")]
    #[serde(default)]
    pub presence_penalty: Option<f64>,
    /// Frequency penalty for generation tasks, applied to individual requests
    /// built from this config. Positive values discourage the model from
    /// repeating the same tokens verbatim. Validated to `[-2.0, 2.0]` by
    /// `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.frequency_penalty`.
    #[php(prop, name = "frequencyPenalty")]
    #[serde(alias = "frequencyPenalty")]
    #[serde(default)]
    pub frequency_penalty: Option<f64>,
    /// Reasoning effort level for extended-thinking models, applied to individual
    /// requests built from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.reasoning_effort`
    /// (`types.chat.ReasoningEffort`). A request-time parameter like `temperature`/
    /// `max_tokens` above, not a client-level setting — `into_client_builder` does not
    /// map it. Accepted as a plain string — one of `"low"`, `"medium"`, `"high"`,
    /// `"minimal"`, `"max"` (case-insensitive; liter-llm's own
    /// `#[serde(rename_all = "lowercase")]` spelling) — rather than importing
    /// liter-llm's enum, because this module compiles even when the `liter-llm`
    /// feature is disabled. See `llm.client.parse_reasoning_effort` for the
    /// conversion into `liter_llm.ReasoningEffort`.
    #[php(prop, name = "reasoningEffort")]
    #[serde(alias = "reasoningEffort")]
    #[serde(default)]
    pub reasoning_effort: Option<String>,
    /// Provider-specific extra parameters merged into the request body (guardrails,
    /// safety settings, grounding config, etc.), applied to individual requests built
    /// from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.extra_body`. A request-time
    /// parameter like `temperature`/`max_tokens` above, not a client-level setting.
    #[serde(alias = "extraBody")]
    #[serde(default)]
    pub extra_body: Option<serde_json::Value>,
    /// Whether liter-llm should load provider credentials from environment variables.
    ///
    /// Mirrors liter-llm's `ClientConfigBuilder.load_env`. When `None`, liter-llm's
    /// own default behavior applies.
    #[php(prop, name = "loadEnv")]
    #[serde(alias = "loadEnv")]
    #[serde(default)]
    pub load_env: Option<bool>,
    /// Extra HTTP headers sent with every request to the provider.
    ///
    /// Mirrors liter-llm's `ClientConfigBuilder.header`, for gateways or providers
    /// that require custom auth/routing headers.
    #[php(prop, name = "headers")]
    #[serde(default)]
    pub headers: Option<HashMap<String, String>>,
    /// Custom provider configurations, in addition to liter-llm's built-in providers.
    ///
    /// Mirrors liter-llm's `LlmConfig.providers`, for OpenAI-compatible gateways
    /// and self-hosted model servers that are not in the built-in provider catalog.
    #[serde(default)]
    pub providers: Option<Vec<LlmProviderConfig>>,
    /// Response cache configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.cache`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    ///
    /// Boxed for the same reason as `bedrock` (a4579589ac): `LlmConfig` is the payload
    /// of `EmbeddingModelType.Llm` and `RerankerModelType.Llm`, whose other variants
    /// are tens of bytes. Inlining this and the two sub-configs below pushed that
    /// variant to 480 bytes and tripped `clippy.large_enum_variant` on the
    /// `--features full` leg.
    #[serde(default)]
    pub cache: Option<LlmCacheConfig>,
    /// Budget enforcement configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.budget`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    #[serde(default)]
    pub budget: Option<LlmBudgetConfig>,
    /// Per-model rate limiting configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.rate_limit`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    #[serde(alias = "rateLimit")]
    #[serde(default)]
    pub rate_limit: Option<LlmRateLimitConfig>,
    /// Enable per-request cost tracking.
    ///
    /// Mirrors liter-llm's `LlmConfig.cost_tracking`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[php(prop, name = "costTracking")]
    #[serde(alias = "costTracking")]
    #[serde(default)]
    pub cost_tracking: Option<bool>,
    /// Enable OpenTelemetry-compatible tracing spans.
    ///
    /// Mirrors liter-llm's `LlmConfig.tracing`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    #[php(prop, name = "tracing")]
    #[serde(default)]
    pub tracing: Option<bool>,
    /// Cooldown duration after transient errors, in seconds.
    ///
    /// Mirrors liter-llm's `LlmConfig.cooldown_secs`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[php(prop, name = "cooldownSecs")]
    #[serde(alias = "cooldownSecs")]
    #[serde(default)]
    pub cooldown_secs: Option<i64>,
    /// Background health check interval, in seconds.
    ///
    /// Mirrors liter-llm's `LlmConfig.health_check_secs`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[php(prop, name = "healthCheckSecs")]
    #[serde(alias = "healthCheckSecs")]
    #[serde(default)]
    pub health_check_secs: Option<i64>,
    /// AWS Bedrock settings (region, cross-region routing, explicit credentials).
    ///
    /// Only consulted for `bedrock/`-prefixed models. When `None` — or when an
    /// individual field inside it is `None` — liter-llm falls back to the standard
    /// AWS environment variables and the default credential chain.
    #[serde(default)]
    pub bedrock: Option<BedrockConfig>,
    /// Managed OAuth2/STS credential provider for auth modes liter-llm cannot express via a
    /// static `api_key` — Azure AD, Vertex AI OAuth2, Vertex AI Application Default
    /// Credentials, and AWS STS Web Identity (EKS IRSA) for Bedrock.
    ///
    /// Mirrors liter-llm's `client.ClientConfigBuilder.credential_provider`, which takes an
    /// `Arc<dyn liter_llm.auth.CredentialProvider>` trait object — that cannot appear in a
    /// serde DTO. Every `CredentialProviderConfig` variant is plain data instead, so it
    /// round-trips through TOML/JSON/YAML and every language binding like the rest of
    /// `LlmConfig`.
    ///
    /// Managed credential providers are unavailable on `wasm32`, where liter-llm uses
    /// browser HTTP rather than its native authentication modules. `LlmConfig.validate`
    /// rejects a configured provider on that target instead of silently ignoring it.
    ///
    /// GitHub Copilot's device-flow provider has no variant here: it takes no configuration at
    /// all (`liter_llm.auth.github_copilot.GithubCopilotCredentialProvider.new` accepts only
    /// an HTTP client) and drives an interactive terminal prompt, so it cannot be expressed as
    /// data. A Rust embedder who needs it — or any other fully custom `CredentialProvider` — can
    /// call `xberg.llm.client.create_client_with_credential_provider` directly with a
    /// `liter-llm` dependency of their own.
    #[serde(alias = "credentialProvider")]
    #[serde(default)]
    pub credential_provider: Option<CredentialProviderConfig>,
    /// Maximum number of simultaneously in-flight requests to the LLM provider this
    /// config resolves to.
    ///
    /// This is a real, global bound on *provider* concurrency, not a per-extraction
    /// allowance: `xberg.llm.client.create_client` shares one process-wide client
    /// instance per distinct resolved config, so every concurrent extraction that
    /// resolves to the same config shares the one in-flight-request limit this value
    /// configures, instead of each minting its own (GH#1465). `None` means unlimited.
    ///
    /// PDF and image OCR batch sizing are **not** derived from this field, even when the
    /// configured OCR backend or `vlm_fallback` policy can reach a VLM — those call sites
    /// mix CPU-bound raster/OCR work with, at most, occasional remote requests, so they
    /// size their batches from the general thread budget
    /// (`max_threads`) unconditionally (GH#1465). Captioning is
    /// the one feature that *additionally* uses this value to bound its own
    /// per-extraction async request fan-out (issuing only VLM requests, with no CPU
    /// batching to protect), on top of the global provider-side limit described above;
    /// that per-extraction bound clamps a value below 1 up to 1. The global provider-side
    /// limit does not clamp: `Some(0)` reaches liter-llm as-is, and liter-llm rejects it
    /// when building the client (zero permitted in-flight requests is never useful), so it
    /// surfaces as a `create_client` error rather than a silent clamp to 1.
    ///
    /// This field is intentionally last to preserve positional constructor
    /// compatibility in generated language bindings.
    #[php(prop, name = "maxConcurrency")]
    #[serde(alias = "maxConcurrency")]
    #[serde(default)]
    pub max_concurrency: Option<i64>,
}

impl Default for LlmConfig {
    fn default() -> Self {
        <xberg::LlmConfig as Default>::default().into()
    }
}

#[php_impl]
impl LlmConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: String,
        apiKey: Option<String>,
        baseUrl: Option<String>,
        timeoutSecs: Option<i64>,
        maxRetries: Option<u32>,
        temperature: Option<f64>,
        maxTokens: Option<i64>,
        topP: Option<f64>,
        stop: Option<Vec<String>>,
        seed: Option<i64>,
        presencePenalty: Option<f64>,
        frequencyPenalty: Option<f64>,
        reasoningEffort: Option<String>,
        extraBody: Option<String>,
        loadEnv: Option<bool>,
        headers: Option<HashMap<String, String>>,
        providers: Option<&ext_php_rs::types::ZendHashTable>,
        cache: Option<&LlmCacheConfig>,
        budget: Option<&LlmBudgetConfig>,
        rateLimit: Option<&LlmRateLimitConfig>,
        costTracking: Option<bool>,
        tracing: Option<bool>,
        cooldownSecs: Option<i64>,
        healthCheckSecs: Option<i64>,
        bedrock: Option<&BedrockConfig>,
        credentialProvider: Option<&CredentialProviderConfig>,
        maxConcurrency: Option<i64>,
    ) -> PhpResult<Self> {
        let providers_core: Option<Vec<LlmProviderConfig>> = if let Some(ht) = providers {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&LlmProviderConfig as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to LlmProviderConfig".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            model,
            api_key: apiKey,
            base_url: baseUrl,
            timeout_secs: timeoutSecs,
            max_retries: maxRetries,
            temperature,
            max_tokens: maxTokens,
            top_p: topP,
            stop,
            seed,
            presence_penalty: presencePenalty,
            frequency_penalty: frequencyPenalty,
            reasoning_effort: reasoningEffort,
            extra_body: extraBody
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            load_env: loadEnv,
            headers,
            providers: providers_core,
            cache: cache.cloned(),
            budget: budget.cloned(),
            rate_limit: rateLimit.cloned(),
            cost_tracking: costTracking,
            tracing,
            cooldown_secs: cooldownSecs,
            health_check_secs: healthCheckSecs,
            bedrock: bedrock.cloned(),
            credential_provider: credentialProvider.cloned(),
            max_concurrency: maxConcurrency,
        })
    }

    pub fn get_model(&self) -> String {
        self.model.clone()
    }

    pub fn get_api_key(&self) -> Option<String> {
        self.api_key.clone()
    }

    pub fn get_base_url(&self) -> Option<String> {
        self.base_url.clone()
    }

    pub fn get_timeout_secs(&self) -> Option<i64> {
        self.timeout_secs.clone()
    }

    pub fn get_max_retries(&self) -> Option<u32> {
        self.max_retries.clone()
    }

    pub fn get_temperature(&self) -> Option<f64> {
        self.temperature.clone()
    }

    pub fn get_max_tokens(&self) -> Option<i64> {
        self.max_tokens.clone()
    }

    pub fn get_top_p(&self) -> Option<f64> {
        self.top_p.clone()
    }

    pub fn get_stop(&self) -> Option<Vec<String>> {
        self.stop.clone()
    }

    pub fn get_seed(&self) -> Option<i64> {
        self.seed.clone()
    }

    pub fn get_presence_penalty(&self) -> Option<f64> {
        self.presence_penalty.clone()
    }

    pub fn get_frequency_penalty(&self) -> Option<f64> {
        self.frequency_penalty.clone()
    }

    pub fn get_reasoning_effort(&self) -> Option<String> {
        self.reasoning_effort.clone()
    }

    pub fn get_extra_body(&self) -> Option<String> {
        self.extra_body.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_load_env(&self) -> Option<bool> {
        self.load_env.clone()
    }

    pub fn get_headers(&self) -> Option<HashMap<String, String>> {
        self.headers.clone()
    }

    pub fn get_providers(&self) -> Option<Vec<LlmProviderConfig>> {
        self.providers.clone()
    }

    pub fn get_cache(&self) -> Option<LlmCacheConfig> {
        self.cache.clone().map(Into::into)
    }

    pub fn set_cache(&mut self, value: Option<&LlmCacheConfig>) {
        self.cache = value.map(|value| value.clone().into());
    }

    pub fn get_budget(&self) -> Option<LlmBudgetConfig> {
        self.budget.clone().map(Into::into)
    }

    pub fn set_budget(&mut self, value: Option<&LlmBudgetConfig>) {
        self.budget = value.map(|value| value.clone().into());
    }

    pub fn get_rate_limit(&self) -> Option<LlmRateLimitConfig> {
        self.rate_limit.clone().map(Into::into)
    }

    pub fn set_rate_limit(&mut self, value: Option<&LlmRateLimitConfig>) {
        self.rate_limit = value.map(|value| value.clone().into());
    }

    pub fn get_cost_tracking(&self) -> Option<bool> {
        self.cost_tracking.clone()
    }

    pub fn get_tracing(&self) -> Option<bool> {
        self.tracing.clone()
    }

    pub fn get_cooldown_secs(&self) -> Option<i64> {
        self.cooldown_secs.clone()
    }

    pub fn get_health_check_secs(&self) -> Option<i64> {
        self.health_check_secs.clone()
    }

    pub fn get_bedrock(&self) -> Option<BedrockConfig> {
        self.bedrock.clone().map(Into::into)
    }

    pub fn set_bedrock(&mut self, value: Option<&BedrockConfig>) {
        self.bedrock = value.map(|value| value.clone().into());
    }

    pub fn get_credential_provider(&self) -> Option<CredentialProviderConfig> {
        self.credential_provider.clone().map(Into::into)
    }

    pub fn set_credential_provider(&mut self, value: Option<&CredentialProviderConfig>) {
        self.credential_provider = value.map(|value| value.clone().into());
    }

    pub fn get_max_concurrency(&self) -> Option<i64> {
        self.max_concurrency.clone()
    }

    pub fn validate(&self) -> PhpResult<()> {
        #[allow(clippy::needless_update)]
        let core_self = xberg::LlmConfig {
            model: self.model.clone(),
            api_key: self.api_key.clone(),
            base_url: self.base_url.clone(),
            timeout_secs: self.timeout_secs.map(|v| v as u64),
            max_retries: self.max_retries,
            temperature: self.temperature,
            max_tokens: self.max_tokens.map(|v| v as u64),
            top_p: self.top_p,
            stop: self.stop.clone(),
            seed: self.seed,
            presence_penalty: self.presence_penalty,
            frequency_penalty: self.frequency_penalty,
            reasoning_effort: self.reasoning_effort.clone(),
            extra_body: Default::default(),
            load_env: self.load_env,
            headers: self.headers.clone().map(|m| m.into_iter().collect()),
            providers: self.providers.clone().map(|v| v.into_iter().map(Into::into).collect()),
            cache: self.cache.clone().map(|v| Box::new(v.into())),
            budget: self.budget.clone().map(|v| Box::new(v.into())),
            rate_limit: self.rate_limit.clone().map(|v| Box::new(v.into())),
            cost_tracking: self.cost_tracking,
            tracing: self.tracing,
            cooldown_secs: self.cooldown_secs.map(|v| v as u64),
            health_check_secs: self.health_check_secs.map(|v| v as u64),
            bedrock: self.bedrock.clone().map(|v| Box::new(v.into())),
            credential_provider: self.credential_provider.clone().map(|v| Box::new(v.into())),
            max_concurrency: self.max_concurrency.map(|v| v as usize),
            ..Default::default()
        };
        let result = core_self
            .validate()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmProviderConfig")]
#[serde(default)]
pub struct LlmProviderConfig {
    /// Provider name, used to key model prefix matching.
    #[php(prop, name = "name")]
    pub name: String,
    /// Base URL for the provider's OpenAI-compatible API.
    #[php(prop, name = "baseUrl")]
    #[serde(alias = "baseUrl")]
    pub base_url: String,
    /// Header name used to carry the API key (defaults to `Authorization` when unset).
    #[php(prop, name = "authHeader")]
    #[serde(alias = "authHeader")]
    #[serde(default)]
    pub auth_header: Option<String>,
    /// Model name prefixes routed to this provider (e.g. `["my-provider/"]`).
    #[php(prop, name = "modelPrefixes")]
    #[serde(alias = "modelPrefixes")]
    #[serde(default)]
    pub model_prefixes: Vec<String>,
}

impl Default for LlmProviderConfig {
    fn default() -> Self {
        <xberg::LlmProviderConfig as Default>::default().into()
    }
}

#[php_impl]
impl LlmProviderConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(name: String, baseUrl: String, modelPrefixes: Vec<String>, authHeader: Option<String>) -> Self {
        Self {
            name,
            base_url: baseUrl,
            auth_header: authHeader,
            model_prefixes: modelPrefixes,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_base_url(&self) -> String {
        self.base_url.clone()
    }

    pub fn get_auth_header(&self) -> Option<String> {
        self.auth_header.clone()
    }

    pub fn get_model_prefixes(&self) -> Vec<String> {
        self.model_prefixes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmRateLimitConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct LlmRateLimitConfig {
    /// Requests per minute limit.
    #[php(prop, name = "rpm")]
    #[serde(default)]
    pub rpm: Option<u32>,
    /// Tokens per minute limit.
    #[php(prop, name = "tpm")]
    #[serde(default)]
    pub tpm: Option<i64>,
    /// Rate limit window, in seconds.
    #[php(prop, name = "windowSeconds")]
    #[serde(alias = "windowSeconds")]
    #[serde(default)]
    pub window_seconds: Option<i64>,
}

impl Default for LlmRateLimitConfig {
    fn default() -> Self {
        <xberg::LlmRateLimitConfig as Default>::default().into()
    }
}

#[php_impl]
impl LlmRateLimitConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_rpm(&self) -> Option<u32> {
        self.rpm.clone()
    }

    pub fn get_tpm(&self) -> Option<i64> {
        self.tpm.clone()
    }

    pub fn get_window_seconds(&self) -> Option<i64> {
        self.window_seconds.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\LlmUsage")]
#[serde(default)]
pub struct LlmUsage {
    /// The LLM model identifier (e.g. "openai/gpt-4o", "anthropic/claude-sonnet-4-20250514").
    #[php(prop, name = "model")]
    pub model: String,
    /// The pipeline stage that triggered this LLM call
    /// (e.g. "vlm_ocr", "structured_extraction", "embeddings").
    #[php(prop, name = "source")]
    pub source: String,
    /// Number of input/prompt tokens consumed.
    #[php(prop, name = "inputTokens")]
    #[serde(alias = "inputTokens")]
    pub input_tokens: Option<i64>,
    /// Number of output/completion tokens generated.
    #[php(prop, name = "outputTokens")]
    #[serde(alias = "outputTokens")]
    pub output_tokens: Option<i64>,
    /// Total tokens (input + output).
    #[php(prop, name = "totalTokens")]
    #[serde(alias = "totalTokens")]
    pub total_tokens: Option<i64>,
    /// Estimated cost in USD based on the provider's published pricing.
    #[php(prop, name = "estimatedCost")]
    #[serde(alias = "estimatedCost")]
    pub estimated_cost: Option<f64>,
    /// Why the model stopped generating (e.g. "stop", "length", "content_filter").
    #[php(prop, name = "finishReason")]
    #[serde(alias = "finishReason")]
    pub finish_reason: Option<String>,
}

impl Default for LlmUsage {
    fn default() -> Self {
        <xberg::LlmUsage as Default>::default().into()
    }
}

#[php_impl]
impl LlmUsage {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: String,
        source: String,
        inputTokens: Option<i64>,
        outputTokens: Option<i64>,
        totalTokens: Option<i64>,
        estimatedCost: Option<f64>,
        finishReason: Option<String>,
    ) -> Self {
        Self {
            model,
            source,
            input_tokens: inputTokens,
            output_tokens: outputTokens,
            total_tokens: totalTokens,
            estimated_cost: estimatedCost,
            finish_reason: finishReason,
        }
    }

    pub fn get_model(&self) -> String {
        self.model.clone()
    }

    pub fn get_source(&self) -> String {
        self.source.clone()
    }

    pub fn get_input_tokens(&self) -> Option<i64> {
        self.input_tokens.clone()
    }

    pub fn get_output_tokens(&self) -> Option<i64> {
        self.output_tokens.clone()
    }

    pub fn get_total_tokens(&self) -> Option<i64> {
        self.total_tokens.clone()
    }

    pub fn get_estimated_cost(&self) -> Option<f64> {
        self.estimated_cost.clone()
    }

    pub fn get_finish_reason(&self) -> Option<String> {
        self.finish_reason.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\MapResult")]
#[serde(default)]
pub struct MapResult {
    /// The list of discovered URLs.
    pub urls: Vec<SitemapUrl>,
}

impl Default for MapResult {
    fn default() -> Self {
        <crawlberg::MapResult as Default>::default().into()
    }
}

#[php_impl]
impl MapResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(urls: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut urls_core_result: Vec<SitemapUrl> = Vec::new();
        for (_, item) in urls.iter() {
            if let Some(parsed) = <&SitemapUrl as ext_php_rs::convert::FromZval>::from_zval(item) {
                urls_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to SitemapUrl".to_string(),
                ));
            }
        }
        let urls_core: Vec<SitemapUrl> = urls_core_result;
        Ok(Self { urls: urls_core })
    }

    pub fn get_urls(&self) -> Vec<SitemapUrl> {
        self.urls.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\MarkdownCodeBlock")]
#[serde(default)]
pub struct MarkdownCodeBlock {
    /// Declared language identifier, or an empty string when absent.
    #[php(prop, name = "language")]
    pub language: String,
    /// Code block content.
    #[php(prop, name = "code")]
    pub code: String,
}

impl Default for MarkdownCodeBlock {
    fn default() -> Self {
        <xberg::MarkdownCodeBlock as Default>::default().into()
    }
}

#[php_impl]
impl MarkdownCodeBlock {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(language: String, code: String) -> Self {
        Self { language, code }
    }

    pub fn get_language(&self) -> String {
        self.language.clone()
    }

    pub fn get_code(&self) -> String {
        self.code.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\MarkdownLink")]
#[serde(default)]
pub struct MarkdownLink {
    /// Visible link text.
    #[php(prop, name = "text")]
    pub text: String,
    /// Link destination.
    #[php(prop, name = "url")]
    pub url: String,
}

impl Default for MarkdownLink {
    fn default() -> Self {
        <xberg::MarkdownLink as Default>::default().into()
    }
}

#[php_impl]
impl MarkdownLink {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(text: String, url: String) -> Self {
        Self { text, url }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }
}

#[derive(Clone)]
#[php_class]
#[php(name = "Xberg\\MetaSchema")]
pub struct MetaSchema {
    inner: Arc<xberg::MetaSchema>,
}

#[php_impl]
impl MetaSchema {
    /// Validate `raw` against the meta-schema and deserialize into a `Preset`,
    /// stamping the fingerprint over the canonical file bytes.
    pub fn parse_preset(&self, path: String, raw: PhpBytes) -> PhpResult<Preset> {
        let result = self
            .inner
            .parse_preset(&path, &raw.0[..])
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result.into())
    }

    /// Compile the given JSON text as a Draft 2020-12 meta-schema.
    #[php(name = "compile")]
    pub fn compile(metaSchemaJson: String) -> PhpResult<MetaSchema> {
        xberg::MetaSchema::compile(&metaSchemaJson)
            .map(|val| Self { inner: Arc::new(val) })
            .map_err(|e| PhpException::default(e.to_string()))
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\Metadata")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct Metadata {
    /// Document title
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Document subject or description
    #[php(prop, name = "subject")]
    pub subject: Option<String>,
    /// Primary author(s) - always Vec for consistency
    #[php(prop, name = "authors")]
    pub authors: Option<Vec<String>>,
    /// Keywords/tags - always Vec for consistency
    #[php(prop, name = "keywords")]
    pub keywords: Option<Vec<String>>,
    /// Primary language (ISO 639 code)
    #[php(prop, name = "language")]
    pub language: Option<String>,
    /// Creation timestamp (ISO 8601 format)
    #[php(prop, name = "createdAt")]
    #[serde(alias = "createdAt")]
    pub created_at: Option<String>,
    /// Last modification timestamp (ISO 8601 format)
    #[php(prop, name = "modifiedAt")]
    #[serde(alias = "modifiedAt")]
    pub modified_at: Option<String>,
    /// User who created the document
    #[php(prop, name = "createdBy")]
    #[serde(alias = "createdBy")]
    pub created_by: Option<String>,
    /// User who last modified the document
    #[php(prop, name = "modifiedBy")]
    #[serde(alias = "modifiedBy")]
    pub modified_by: Option<String>,
    /// Page/slide/sheet structure with boundaries
    pub pages: Option<PageStructure>,
    /// Format-specific metadata (discriminated union)
    ///
    /// Contains detailed metadata specific to the document format.
    /// Serialized as a nested `"format"` object with a `format_type` discriminator field.
    pub format: Option<FormatMetadata>,
    /// Image preprocessing metadata (when OCR preprocessing was applied)
    #[serde(alias = "imagePreprocessing")]
    pub image_preprocessing: Option<ImagePreprocessingMetadata>,
    /// JSON schema (for structured data extraction)
    #[serde(alias = "jsonSchema")]
    pub json_schema: Option<serde_json::Value>,
    /// Error metadata (for batch operations)
    pub error: Option<ErrorMetadata>,
    /// Extraction duration in milliseconds (for benchmarking).
    ///
    /// This field is populated by batch extraction to provide per-file timing
    /// information. It's `None` for single-file extraction (which uses external timing).
    #[php(prop, name = "extractionDurationMs")]
    #[serde(alias = "extractionDurationMs")]
    pub extraction_duration_ms: Option<i64>,
    /// Document category (from frontmatter or classification).
    #[php(prop, name = "category")]
    pub category: Option<String>,
    /// Document tags (from frontmatter).
    #[php(prop, name = "tags")]
    pub tags: Option<Vec<String>>,
    /// Document version string (from frontmatter).
    #[php(prop, name = "documentVersion")]
    #[serde(alias = "documentVersion")]
    pub document_version: Option<String>,
    /// Abstract or summary text (from frontmatter).
    #[php(prop, name = "abstractText")]
    #[serde(alias = "abstractText")]
    pub abstract_text: Option<String>,
    /// Output format identifier (e.g., "markdown", "html", "text").
    ///
    /// Set by the output format pipeline stage when format conversion is applied.
    /// Previously stored in `metadata.additional["output_format"]`.
    #[php(prop, name = "outputFormat")]
    #[serde(alias = "outputFormat")]
    pub output_format: Option<String>,
    /// Whether OCR was used during extraction.
    ///
    /// Set to `true` whenever the extraction pipeline ran an OCR backend
    /// (Tesseract, PaddleOCR, VLM, etc.) and used that output as the primary
    /// or fallback text. `false` means native text extraction was used exclusively.
    #[php(prop, name = "ocrUsed")]
    #[serde(alias = "ocrUsed")]
    #[serde(default)]
    pub ocr_used: bool,
    /// Additional custom fields from postprocessors.
    ///
    /// Serialized as a nested `"additional"` object (not flattened at root level).
    /// Uses `Cow<'static, str>` keys so static string keys avoid allocation.
    #[doc = "PHP: initialize this field through fromJson() and read it via get_additional(); its value type cannot be
exposed as a directly settable ext-php-rs property."]
    #[serde(default)]
    pub additional: HashMap<String, serde_json::Value>,
}

impl Default for Metadata {
    fn default() -> Self {
        <xberg::Metadata as Default>::default().into()
    }
}

#[php_impl]
impl Metadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        ocrUsed: bool,
        title: Option<String>,
        subject: Option<String>,
        authors: Option<Vec<String>>,
        keywords: Option<Vec<String>>,
        language: Option<String>,
        createdAt: Option<String>,
        modifiedAt: Option<String>,
        createdBy: Option<String>,
        modifiedBy: Option<String>,
        pages: Option<&PageStructure>,
        format: Option<&FormatMetadata>,
        imagePreprocessing: Option<&ImagePreprocessingMetadata>,
        jsonSchema: Option<String>,
        error: Option<&ErrorMetadata>,
        extractionDurationMs: Option<i64>,
        category: Option<String>,
        tags: Option<Vec<String>>,
        documentVersion: Option<String>,
        abstractText: Option<String>,
        outputFormat: Option<String>,
    ) -> PhpResult<Self> {
        Ok(Self {
            title,
            subject,
            authors,
            keywords,
            language,
            created_at: createdAt,
            modified_at: modifiedAt,
            created_by: createdBy,
            modified_by: modifiedBy,
            pages: pages.cloned(),
            format: format.cloned(),
            image_preprocessing: imagePreprocessing.cloned(),
            json_schema: jsonSchema
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            error: error.cloned(),
            extraction_duration_ms: extractionDurationMs,
            category,
            tags,
            document_version: documentVersion,
            abstract_text: abstractText,
            output_format: outputFormat,
            ocr_used: ocrUsed,
            additional: Default::default(),
        })
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_subject(&self) -> Option<String> {
        self.subject.clone()
    }

    pub fn get_authors(&self) -> Option<Vec<String>> {
        self.authors.clone()
    }

    pub fn get_keywords(&self) -> Option<Vec<String>> {
        self.keywords.clone()
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_created_at(&self) -> Option<String> {
        self.created_at.clone()
    }

    pub fn get_modified_at(&self) -> Option<String> {
        self.modified_at.clone()
    }

    pub fn get_created_by(&self) -> Option<String> {
        self.created_by.clone()
    }

    pub fn get_modified_by(&self) -> Option<String> {
        self.modified_by.clone()
    }

    pub fn get_pages(&self) -> Option<PageStructure> {
        self.pages.clone().map(Into::into)
    }

    pub fn set_pages(&mut self, value: Option<&PageStructure>) {
        self.pages = value.map(|value| value.clone().into());
    }

    pub fn get_format(&self) -> Option<FormatMetadata> {
        self.format.clone().map(Into::into)
    }

    pub fn set_format(&mut self, value: Option<&FormatMetadata>) {
        self.format = value.map(|value| value.clone().into());
    }

    pub fn get_image_preprocessing(&self) -> Option<ImagePreprocessingMetadata> {
        self.image_preprocessing.clone().map(Into::into)
    }

    pub fn set_image_preprocessing(&mut self, value: Option<&ImagePreprocessingMetadata>) {
        self.image_preprocessing = value.map(|value| value.clone().into());
    }

    pub fn get_json_schema(&self) -> Option<String> {
        self.json_schema.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_error(&self) -> Option<ErrorMetadata> {
        self.error.clone().map(Into::into)
    }

    pub fn set_error(&mut self, value: Option<&ErrorMetadata>) {
        self.error = value.map(|value| value.clone().into());
    }

    pub fn get_extraction_duration_ms(&self) -> Option<i64> {
        self.extraction_duration_ms.clone()
    }

    pub fn get_category(&self) -> Option<String> {
        self.category.clone()
    }

    pub fn get_tags(&self) -> Option<Vec<String>> {
        self.tags.clone()
    }

    pub fn get_document_version(&self) -> Option<String> {
        self.document_version.clone()
    }

    pub fn get_abstract_text(&self) -> Option<String> {
        self.abstract_text.clone()
    }

    pub fn get_output_format(&self) -> Option<String> {
        self.output_format.clone()
    }

    pub fn get_ocr_used(&self) -> bool {
        self.ocr_used.clone()
    }

    pub fn get_additional(&self) -> Option<String> {
        serde_json::to_string(&self.additional).ok()
    }

    pub fn is_empty(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::Metadata {
            title: self.title.clone(),
            subject: self.subject.clone(),
            authors: self.authors.clone(),
            keywords: self.keywords.clone(),
            language: self.language.clone(),
            created_at: self.created_at.clone(),
            modified_at: self.modified_at.clone(),
            created_by: self.created_by.clone(),
            modified_by: self.modified_by.clone(),
            pages: self.pages.clone().map(Into::into),
            format: self.format.clone().map(Into::into),
            image_preprocessing: self.image_preprocessing.clone().map(Into::into),
            json_schema: Default::default(),
            error: self.error.clone().map(Into::into),
            extraction_duration_ms: self.extraction_duration_ms.map(|v| v as u64),
            category: self.category.clone(),
            tags: self.tags.clone(),
            document_version: self.document_version.clone(),
            abstract_text: self.abstract_text.clone(),
            output_format: self.output_format.clone(),
            ocr_used: self.ocr_used,
            additional: Default::default(),
            ..Default::default()
        };
        core_self.is_empty()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ModelPaths")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ModelPaths {
    /// Exact path to the detection ONNX model in the Hugging Face snapshot.
    #[php(prop, name = "detModel")]
    #[serde(alias = "detModel")]
    pub det_model: String,
    /// Exact path to the classification ONNX model in the Hugging Face snapshot.
    #[php(prop, name = "clsModel")]
    #[serde(alias = "clsModel")]
    pub cls_model: String,
    /// Exact path to the recognition ONNX model in the Hugging Face snapshot.
    #[php(prop, name = "recModel")]
    #[serde(alias = "recModel")]
    pub rec_model: String,
    /// Path to the character dictionary file.
    #[php(prop, name = "dictFile")]
    #[serde(alias = "dictFile")]
    pub dict_file: String,
}

#[php_impl]
impl ModelPaths {
    #[php(constructor)]
    pub fn new(detModel: String, clsModel: String, recModel: String, dictFile: String) -> Self {
        Self {
            det_model: detModel,
            cls_model: clsModel,
            rec_model: recModel,
            dict_file: dictFile,
        }
    }

    pub fn get_det_model(&self) -> String {
        self.det_model.clone()
    }

    pub fn get_cls_model(&self) -> String {
        self.cls_model.clone()
    }

    pub fn get_rec_model(&self) -> String {
        self.rec_model.clone()
    }

    pub fn get_dict_file(&self) -> String {
        self.dict_file.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\MultiVectorEmbedding")]
#[serde(default)]
pub struct MultiVectorEmbedding {
    /// Number of attention-live token rows (padding rows are dropped, not
    /// zeroed — see `engine.normalize_tokens`).
    #[php(prop, name = "numTokens")]
    #[serde(alias = "numTokens")]
    pub num_tokens: u32,
    /// Dimensionality of each per-token vector.
    #[php(prop, name = "dim")]
    pub dim: u32,
    /// Flat row-major buffer, length `num_tokens * dim`.
    #[php(prop, name = "data")]
    pub data: Vec<f32>,
}

#[php_impl]
impl MultiVectorEmbedding {
    #[php(constructor)]
    pub fn new(numTokens: u32, dim: u32, data: Vec<f32>) -> Self {
        Self {
            num_tokens: numTokens,
            dim,
            data,
        }
    }

    pub fn get_num_tokens(&self) -> u32 {
        self.num_tokens.clone()
    }

    pub fn get_dim(&self) -> u32 {
        self.dim.clone()
    }

    pub fn get_data(&self) -> Vec<f32> {
        self.data.clone()
    }

    pub fn is_well_formed(&self) -> bool {
        let core_self = xberg::MultiVectorEmbedding {
            num_tokens: self.num_tokens,
            dim: self.dim,
            data: self.data.clone(),
        };
        core_self.is_well_formed()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\MultidocInput")]
#[serde(default)]
pub struct MultidocInput {
    /// Total number of pages in the PDF.
    #[php(prop, name = "pageCount")]
    #[serde(alias = "pageCount")]
    pub page_count: u32,
    /// Per-page signals extracted from the PDF.
    pub pages: Vec<PageSignals>,
}

#[php_impl]
impl MultidocInput {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(pageCount: u32, pages: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut pages_core_result: Vec<PageSignals> = Vec::new();
        for (_, item) in pages.iter() {
            if let Some(parsed) = <&PageSignals as ext_php_rs::convert::FromZval>::from_zval(item) {
                pages_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to PageSignals".to_string(),
                ));
            }
        }
        let pages_core: Vec<PageSignals> = pages_core_result;
        Ok(Self {
            page_count: pageCount,
            pages: pages_core,
        })
    }

    pub fn get_page_count(&self) -> u32 {
        self.page_count.clone()
    }

    pub fn get_pages(&self) -> Vec<PageSignals> {
        self.pages.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\MultidocThresholds")]
#[serde(default)]
pub struct MultidocThresholds {
    /// Text density difference threshold for `DensityShift` detection.
    /// Default: 0.3.
    #[php(prop, name = "densityShiftThreshold")]
    #[serde(default = "crate::serde_defaults::multidoc_thresholds_density_shift_threshold")]
    #[serde(alias = "densityShiftThreshold")]
    pub density_shift_threshold: f32,
    /// Minimum bigram-overlap ratio below which a density shift is promoted to
    /// a `DensityShift` boundary.  Default: 0.1 (10 % overlap).
    #[php(prop, name = "bigramOverlapMin")]
    #[serde(default = "crate::serde_defaults::multidoc_thresholds_bigram_overlap_min")]
    #[serde(alias = "bigramOverlapMin")]
    pub bigram_overlap_min: f32,
}

impl Default for MultidocThresholds {
    fn default() -> Self {
        <xberg::MultidocThresholds as Default>::default().into()
    }
}

#[php_impl]
impl MultidocThresholds {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(densityShiftThreshold: f32, bigramOverlapMin: f32) -> Self {
        Self {
            density_shift_threshold: densityShiftThreshold,
            bigram_overlap_min: bigramOverlapMin,
        }
    }

    pub fn get_density_shift_threshold(&self) -> f32 {
        self.density_shift_threshold.clone()
    }

    pub fn get_bigram_overlap_min(&self) -> f32 {
        self.bigram_overlap_min.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> MultidocThresholds {
        xberg::MultidocThresholds::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\NerConfig")]
#[serde(default)]
pub struct NerConfig {
    /// Backend that runs the entity detection.
    #[php(prop, name = "backend")]
    #[serde(default = "crate::serde_defaults::ner_config_backend")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub backend: String,
    /// Entity categories to detect. Defaults to a sensible PERSON/ORG/LOCATION/EMAIL set
    /// when empty.
    #[php(prop, name = "categories")]
    #[serde(default)]
    pub categories: Vec<String>,
    /// Override the default model — only used by `NerBackendKind.Onnx`.
    /// `None` lets the backend pick its pinned default xberg GLiNER model alias.
    #[php(prop, name = "model")]
    pub model: Option<String>,
    /// Optional LLM configuration — only used by `NerBackendKind.Llm`. Token usage
    /// for LLM backends is recorded in `ExtractedDocument.llm_usage`.
    pub llm: Option<LlmConfig>,
    /// Arbitrary user-supplied entity labels for zero-shot detection.
    ///
    /// `xberg-gliner` natively supports zero-shot inference over caller-supplied
    /// labels. The LLM backend also honours these
    /// labels by including them in the structured-output schema. Custom labels
    /// surface as `EntityCategory.Custom` in the resulting `Entity` stream.
    ///
    /// Use this when you need domain-specific entity types (e.g. `"Treatment"`,
    /// `"Product"`, `"Vessel"`) without forking GLiNER's taxonomy.
    #[php(prop, name = "customLabels")]
    #[serde(alias = "customLabels")]
    #[serde(default)]
    pub custom_labels: Vec<String>,
}

impl Default for NerConfig {
    fn default() -> Self {
        <xberg::NerConfig as Default>::default().into()
    }
}

#[php_impl]
impl NerConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        backend: String,
        categories: Vec<String>,
        customLabels: Vec<String>,
        model: Option<String>,
        llm: Option<&LlmConfig>,
    ) -> Self {
        Self {
            backend,
            categories,
            model,
            llm: llm.cloned(),
            custom_labels: customLabels,
        }
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }

    pub fn get_categories(&self) -> Vec<String> {
        self.categories.clone()
    }

    pub fn get_model(&self) -> Option<String> {
        self.model.clone()
    }

    pub fn get_llm(&self) -> Option<LlmConfig> {
        self.llm.clone().map(Into::into)
    }

    pub fn set_llm(&mut self, value: Option<&LlmConfig>) {
        self.llm = value.map(|value| value.clone().into());
    }

    pub fn get_custom_labels(&self) -> Vec<String> {
        self.custom_labels.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\NgramRange")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct NgramRange {
    /// Minimum number of words in a candidate.
    #[php(prop, name = "min")]
    #[serde(default = "crate::serde_defaults::ngram_range_min")]
    pub min: i64,
    /// Maximum number of words in a candidate.
    #[php(prop, name = "max")]
    #[serde(default = "crate::serde_defaults::ngram_range_max")]
    pub max: i64,
}

impl Default for NgramRange {
    fn default() -> Self {
        <xberg::NgramRange as Default>::default().into()
    }
}

#[php_impl]
impl NgramRange {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(min: i64, max: i64) -> Self {
        Self { min, max }
    }

    pub fn get_min(&self) -> i64 {
        self.min.clone()
    }

    pub fn get_max(&self) -> i64 {
        self.max.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> NgramRange {
        xberg::NgramRange::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrConfidence")]
#[serde(default)]
pub struct OcrConfidence {
    /// Detection confidence: how confident the OCR engine is that text exists here.
    ///
    /// PaddleOCR provides this as `box_score`, Tesseract doesn't have a direct equivalent.
    /// Range: 0.0 to 1.0 (or undefined if not available).
    #[php(prop, name = "detection")]
    #[serde(default)]
    pub detection: Option<f64>,
    /// Recognition confidence: how confident about the text content.
    ///
    /// Range: 0.0 to 1.0.
    #[php(prop, name = "recognition")]
    pub recognition: f64,
}

impl Default for OcrConfidence {
    fn default() -> Self {
        <xberg::OcrConfidence as Default>::default().into()
    }
}

#[php_impl]
impl OcrConfidence {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(recognition: f64, detection: Option<f64>) -> Self {
        Self { detection, recognition }
    }

    pub fn get_detection(&self) -> Option<f64> {
        self.detection.clone()
    }

    pub fn get_recognition(&self) -> f64 {
        self.recognition.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrConfig")]
#[serde(default)]
pub struct OcrConfig {
    /// Whether OCR is enabled.
    ///
    /// Setting `enabled: false` is a shorthand for `disable_ocr: true` on the parent
    /// `ExtractionConfig`. Images return
    /// metadata only; PDFs use native text extraction without OCR fallback.
    ///
    /// Defaults to `true`. When `false`, all other OCR settings are ignored.
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::ocr_config_enabled")]
    pub enabled: bool,
    /// OCR backend: tesseract, paddleocr, paddle-ocr, sceptre, or vlm.
    /// Sceptre uses ONNX Runtime on desktop/server and tract on supported mobile builds.
    /// Browser WebAssembly uses the separate byte-fed Sceptre worker API.
    #[php(prop, name = "backend")]
    #[serde(default = "crate::serde_defaults::ocr_config_backend")]
    pub backend: String,
    /// Language code(s) for OCR recognition. Defaults to `["eng"]`. For Tesseract,
    /// languages are joined with "+".
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs (Python, Node, PHP, WASM, etc.): `["eng", "deu"]`. When
    /// deserializing from a config file, JSON body, or the REST/MCP API, a
    /// single string is also accepted, either as one code ("eng") or
    /// "+"-joined ("eng+deu").
    #[php(prop, name = "language")]
    pub language: Vec<String>,
    /// Tesseract-specific configuration (optional)
    #[serde(alias = "tesseractConfig")]
    #[serde(default)]
    pub tesseract_config: Option<TesseractConfig>,
    /// Output format for OCR results (optional, for format conversion)
    #[php(prop, name = "outputFormat")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "outputFormat")]
    #[serde(default)]
    pub output_format: Option<String>,
    /// PaddleOCR-specific configuration (optional, JSON passthrough).
    ///
    /// Deserialized into a `PaddleOcrConfig`, so any of its fields can be
    /// overridden here — most notably `model_version` (`"pp-ocrv6"` default / `"pp-ocrv5"`) and
    /// `model_tier`. In TOML:
    ///
    /// ```toml
    /// [ocr.paddle_ocr_config]
    /// model_version = "pp-ocrv5"
    /// model_tier = "server"
    /// ```
    ///
    /// The `XBERG_OCR_MODEL_VERSION` / `XBERG_OCR_MODEL_TIER` environment variables set the same two
    /// keys for env-configured servers (issue #1279).
    #[serde(alias = "paddleOcrConfig")]
    #[serde(default)]
    pub paddle_ocr_config: Option<serde_json::Value>,
    /// Arbitrary per-call options passed through to the backend unchanged.
    ///
    /// Custom OCR backends and built-in backends that support runtime tuning
    /// can read this value and deserialize the keys they care about. Keys
    /// unknown to the backend are silently ignored.
    ///
    /// This is the recommended extension point for per-call parameters that
    /// are not covered by the typed fields above (e.g. mode switching,
    /// preprocessing flags, inference batch size).
    ///
    /// **Scope:** when `pipeline` is `None`, this value is propagated to the
    /// primary stage of the auto-constructed pipeline. When `pipeline` is
    /// explicitly set, this field has **no effect** — the caller must set
    /// `OcrPipelineStage.backend_options` directly on the relevant stage(s)
    /// instead.
    ///
    /// Example:
    /// ```json
    /// { "mode": "fast", "enable_layout": true, "timeout_ms": 5000 }
    /// ```
    #[serde(alias = "backendOptions")]
    #[serde(default)]
    pub backend_options: Option<serde_json::Value>,
    /// OCR element extraction configuration
    #[serde(alias = "elementConfig")]
    #[serde(default)]
    pub element_config: Option<OcrElementConfig>,
    /// Quality thresholds for the native-text-to-OCR fallback decision.
    /// When undefined, uses compiled defaults (matching previous hardcoded behavior).
    #[serde(alias = "qualityThresholds")]
    #[serde(default)]
    pub quality_thresholds: Option<OcrQualityThresholds>,
    /// Multi-backend OCR pipeline configuration. When set, enables weighted
    /// fallback across multiple OCR backends based on output quality.
    /// When undefined, uses the single `backend` field (same as today).
    #[serde(default)]
    pub pipeline: Option<OcrPipelineConfig>,
    /// Enable automatic page rotation based on orientation detection.
    ///
    /// When enabled, page orientation (0/90/180/270 degrees) is detected with an
    /// ONNX PP-LCNet document-orientation classifier — NOT Tesseract's own
    /// `DetectOrientationScript()`/OSD, which this crate does not call. If the
    /// page is rotated with high confidence, the image is corrected before
    /// recognition. Applies to every OCR backend; the tesseract-only
    /// `TesseractConfig.preprocessing` field of the same name is OR'd in on
    /// top of this one and affects tesseract alone.
    ///
    /// Independent of a page's PDF `/Rotate` entry, which is handled separately
    /// on every OCR route regardless of this setting.
    #[php(prop, name = "autoRotate")]
    #[serde(default = "crate::serde_defaults::ocr_config_auto_rotate")]
    #[serde(alias = "autoRotate")]
    pub auto_rotate: bool,
    /// Ergonomic VLM fallback policy.
    ///
    /// When set to anything other than `VlmFallbackPolicy.Disabled` and
    /// `OcrConfig.pipeline` is `None`, a multi-stage pipeline is synthesised
    /// automatically:
    ///
    /// - `VlmFallbackPolicy.OnLowQuality` → `[classical_stage, vlm_stage]` with the
    ///   `quality_threshold` mapped onto `OcrQualityThresholds.pipeline_min_quality`.
    /// - `VlmFallbackPolicy.Always` → `[vlm_stage]` only.
    ///
    /// Requires `OcrConfig.vlm_config` to be `Some` when not `Disabled`.
    /// When `OcrConfig.pipeline` is explicitly set, this field is ignored.
    #[serde(alias = "vlmFallback")]
    #[serde(default)]
    pub vlm_fallback: VlmFallbackPolicy,
    /// VLM (Vision Language Model) OCR configuration.
    ///
    /// Required when `backend` is `"vlm"` or when `vlm_fallback` is not
    /// `VlmFallbackPolicy.Disabled`. Uses liter-llm to send page images to a
    /// vision model for text extraction.
    #[serde(alias = "vlmConfig")]
    #[serde(default)]
    pub vlm_config: Option<LlmConfig>,
    /// Custom Jinja2 prompt template for VLM OCR.
    ///
    /// When `None`, uses the default template. Available variables:
    /// - `{{ language }}` — The document language code (e.g., "eng", "deu").
    #[php(prop, name = "vlmPrompt")]
    #[serde(alias = "vlmPrompt")]
    #[serde(default)]
    pub vlm_prompt: Option<String>,
    /// Hardware acceleration for ONNX Runtime models (e.g. PaddleOCR, layout detection).
    ///
    /// Not user-configurable via config files — injected at runtime from
    /// `ExtractionConfig.acceleration` before each `process_image` call.
    pub acceleration: Option<AccelerationConfig>,
    /// Security limits applied when decoding raw image bytes for OCR (GH#1554).
    ///
    /// Not user-configurable via config files — injected at runtime from
    /// `ExtractionConfig.security_limits` before each `process_image` call, the same
    /// pattern `Self.acceleration` uses. `ExtractionConfig.security_limits` is the
    /// single source of truth: this field only ever holds a copy the caller placed here
    /// immediately before dispatch, so the two cannot drift. A backend consulting a
    /// `backend_options` override for this call may still let that override win, but in
    /// the absence of one this field is what backends should fall back to instead of
    /// `SecurityLimits.default()`. `None` means "use `SecurityLimits.default()`", never
    /// "disable the check".
    #[serde(alias = "securityLimits")]
    pub security_limits: Option<SecurityLimits>,
    /// Caller-supplied Tesseract `traineddata` bytes per language code.
    ///
    /// Primary use case is the WASM build, which has no filesystem and cannot
    /// download tessdata at runtime. Native builds typically rely on
    /// `TessdataManager` and ignore this field. When present, the WASM
    /// Tesseract backend prefers these bytes over its compile-time-bundled
    /// English data.
    ///
    /// Skipped by serde to keep config files small — supply via the typed API
    /// at runtime.
    #[doc = "PHP: initialize this field through fromJson() and read it via get_tessdata_bytes(); its value type cannot be
exposed as a directly settable ext-php-rs property."]
    #[serde(alias = "tessdataBytes")]
    pub tessdata_bytes: Option<HashMap<String, Vec<u8>>>,
    /// Runtime override for tessdata directory path.
    ///
    /// When set, uses this path as the highest-priority tessdata location,
    /// bypassing environment variables and cache directories. Useful for
    /// embedding pre-installed tessdata in applications. When `None`, uses
    /// the standard resolution chain: TESSDATA_PREFIX env, cache dir, system paths.
    #[php(prop, name = "tessdataPath")]
    #[serde(alias = "tessdataPath")]
    #[serde(default)]
    pub tessdata_path: Option<String>,
}

impl Default for OcrConfig {
    fn default() -> Self {
        <xberg::OcrConfig as Default>::default().into()
    }
}

#[php_impl]
impl OcrConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        enabled: bool,
        backend: String,
        language: Vec<String>,
        autoRotate: bool,
        vlmFallback: &VlmFallbackPolicy,
        tesseractConfig: Option<&TesseractConfig>,
        outputFormat: Option<String>,
        paddleOcrConfig: Option<String>,
        backendOptions: Option<String>,
        elementConfig: Option<&OcrElementConfig>,
        qualityThresholds: Option<&OcrQualityThresholds>,
        pipeline: Option<&OcrPipelineConfig>,
        vlmConfig: Option<&LlmConfig>,
        vlmPrompt: Option<String>,
        acceleration: Option<&AccelerationConfig>,
        securityLimits: Option<&SecurityLimits>,
        tessdataPath: Option<String>,
    ) -> PhpResult<Self> {
        Ok(Self {
            enabled,
            backend,
            language,
            tesseract_config: tesseractConfig.cloned(),
            output_format: outputFormat,
            paddle_ocr_config: paddleOcrConfig
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            backend_options: backendOptions
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            element_config: elementConfig.cloned(),
            quality_thresholds: qualityThresholds.cloned(),
            pipeline: pipeline.cloned(),
            auto_rotate: autoRotate,
            vlm_fallback: vlmFallback.clone(),
            vlm_config: vlmConfig.cloned(),
            vlm_prompt: vlmPrompt,
            acceleration: acceleration.cloned(),
            security_limits: securityLimits.cloned(),
            tessdata_bytes: Default::default(),
            tessdata_path: tessdataPath,
        })
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }

    pub fn get_language(&self) -> Vec<String> {
        self.language.clone()
    }

    pub fn get_tesseract_config(&self) -> Option<TesseractConfig> {
        self.tesseract_config.clone().map(Into::into)
    }

    pub fn set_tesseract_config(&mut self, value: Option<&TesseractConfig>) {
        self.tesseract_config = value.map(|value| value.clone().into());
    }

    pub fn get_output_format(&self) -> Option<String> {
        self.output_format.clone().map(Into::into)
    }

    pub fn get_paddle_ocr_config(&self) -> Option<String> {
        self.paddle_ocr_config
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_backend_options(&self) -> Option<String> {
        self.backend_options
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_element_config(&self) -> Option<OcrElementConfig> {
        self.element_config.clone().map(Into::into)
    }

    pub fn set_element_config(&mut self, value: Option<&OcrElementConfig>) {
        self.element_config = value.map(|value| value.clone().into());
    }

    pub fn get_quality_thresholds(&self) -> Option<OcrQualityThresholds> {
        self.quality_thresholds.clone().map(Into::into)
    }

    pub fn set_quality_thresholds(&mut self, value: Option<&OcrQualityThresholds>) {
        self.quality_thresholds = value.map(|value| value.clone().into());
    }

    pub fn get_pipeline(&self) -> Option<OcrPipelineConfig> {
        self.pipeline.clone().map(Into::into)
    }

    pub fn set_pipeline(&mut self, value: Option<&OcrPipelineConfig>) {
        self.pipeline = value.map(|value| value.clone().into());
    }

    pub fn get_auto_rotate(&self) -> bool {
        self.auto_rotate.clone()
    }

    pub fn get_vlm_fallback(&self) -> VlmFallbackPolicy {
        self.vlm_fallback.clone()
    }

    pub fn get_vlm_config(&self) -> Option<LlmConfig> {
        self.vlm_config.clone().map(Into::into)
    }

    pub fn set_vlm_config(&mut self, value: Option<&LlmConfig>) {
        self.vlm_config = value.map(|value| value.clone().into());
    }

    pub fn get_vlm_prompt(&self) -> Option<String> {
        self.vlm_prompt.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_security_limits(&self) -> Option<SecurityLimits> {
        self.security_limits.clone().map(Into::into)
    }

    pub fn set_security_limits(&mut self, value: Option<&SecurityLimits>) {
        self.security_limits = value.map(|value| value.clone().into());
    }

    pub fn get_tessdata_bytes(&self) -> Option<HashMap<String, Vec<u8>>> {
        self.tessdata_bytes.clone()
    }

    pub fn get_tessdata_path(&self) -> Option<String> {
        self.tessdata_path.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> OcrConfig {
        xberg::OcrConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrElement")]
#[serde(default)]
pub struct OcrElement {
    /// The recognized text content.
    #[php(prop, name = "text")]
    pub text: String,
    /// Bounding geometry (rectangle or quadrilateral).
    pub geometry: OcrBoundingGeometry,
    /// Confidence scores for detection and recognition.
    pub confidence: OcrConfidence,
    /// Hierarchical level (word, line, block, page).
    #[php(prop, name = "level")]
    #[serde(default = "crate::serde_defaults::ocr_element_level")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub level: String,
    /// Rotation information (if detected).
    pub rotation: Option<OcrRotation>,
    /// Page number (1-indexed).
    #[php(prop, name = "pageNumber")]
    #[serde(default = "crate::serde_defaults::ocr_element_page_number")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Parent element ID for hierarchical relationships.
    ///
    /// When hierarchy output is enabled, this resolves to another emitted element's
    /// `backend_metadata["element_id"]` value.
    #[php(prop, name = "parentId")]
    #[serde(alias = "parentId")]
    pub parent_id: Option<String>,
    /// Backend-specific metadata that doesn't fit the unified schema.
    #[doc = "PHP: initialize this field through fromJson() and read it via get_backend_metadata(); its value type cannot be
exposed as a directly settable ext-php-rs property."]
    #[serde(alias = "backendMetadata")]
    #[serde(default)]
    pub backend_metadata: HashMap<String, serde_json::Value>,
}

impl Default for OcrElement {
    fn default() -> Self {
        <xberg::OcrElement as Default>::default().into()
    }
}

#[php_impl]
impl OcrElement {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        text: String,
        geometry: &OcrBoundingGeometry,
        confidence: &OcrConfidence,
        level: String,
        pageNumber: u32,
        rotation: Option<&OcrRotation>,
        parentId: Option<String>,
    ) -> Self {
        Self {
            text,
            geometry: geometry.clone(),
            confidence: confidence.clone(),
            level,
            rotation: rotation.cloned(),
            page_number: pageNumber,
            parent_id: parentId,
            backend_metadata: Default::default(),
        }
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }

    pub fn get_geometry(&self) -> OcrBoundingGeometry {
        self.geometry.clone()
    }

    pub fn get_confidence(&self) -> OcrConfidence {
        self.confidence.clone()
    }

    pub fn get_level(&self) -> String {
        self.level.clone()
    }

    pub fn get_rotation(&self) -> Option<OcrRotation> {
        self.rotation.clone().map(Into::into)
    }

    pub fn set_rotation(&mut self, value: Option<&OcrRotation>) {
        self.rotation = value.map(|value| value.clone().into());
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_parent_id(&self) -> Option<String> {
        self.parent_id.clone()
    }

    pub fn get_backend_metadata(&self) -> Option<String> {
        serde_json::to_string(&self.backend_metadata).ok()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrElementConfig")]
#[serde(default)]
pub struct OcrElementConfig {
    /// Whether to include OCR elements in the extraction result.
    ///
    /// When true, the `ocr_elements` field in `ExtractedDocument` will be populated.
    #[php(prop, name = "includeElements")]
    #[serde(alias = "includeElements")]
    #[serde(default)]
    pub include_elements: bool,
    /// Minimum hierarchical level to include.
    ///
    /// Elements below this level (e.g., words when min_level is Line) will be excluded.
    #[php(prop, name = "minLevel")]
    #[serde(default = "crate::serde_defaults::ocr_element_config_min_level")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "minLevel")]
    pub min_level: String,
    /// Minimum recognition confidence threshold (0.0-1.0).
    ///
    /// Elements with confidence below this threshold will be filtered out.
    #[php(prop, name = "minConfidence")]
    #[serde(alias = "minConfidence")]
    #[serde(default)]
    pub min_confidence: f64,
    /// Whether to build hierarchical relationships between elements.
    ///
    /// When true, emitted elements receive an `element_id` metadata value and `parent_id`
    /// references are populated only when a spatially containing parent is also emitted.
    #[php(prop, name = "buildHierarchy")]
    #[serde(alias = "buildHierarchy")]
    #[serde(default)]
    pub build_hierarchy: bool,
}

impl Default for OcrElementConfig {
    fn default() -> Self {
        <xberg::OcrElementConfig as Default>::default().into()
    }
}

#[php_impl]
impl OcrElementConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(includeElements: bool, minLevel: String, minConfidence: f64, buildHierarchy: bool) -> Self {
        Self {
            include_elements: includeElements,
            min_level: minLevel,
            min_confidence: minConfidence,
            build_hierarchy: buildHierarchy,
        }
    }

    pub fn get_include_elements(&self) -> bool {
        self.include_elements.clone()
    }

    pub fn get_min_level(&self) -> String {
        self.min_level.clone()
    }

    pub fn get_min_confidence(&self) -> f64 {
        self.min_confidence.clone()
    }

    pub fn get_build_hierarchy(&self) -> bool {
        self.build_hierarchy.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrExtractionResult")]
#[serde(default)]
pub struct OcrExtractionResult {
    /// Recognized text content
    #[php(prop, name = "content")]
    pub content: String,
    /// Original MIME type of the processed image
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
    /// OCR processing metadata (confidence scores, language, etc.)
    #[doc = "PHP: initialize this field through fromJson() and read it via get_metadata(); its value type cannot be
exposed as a directly settable ext-php-rs property."]
    pub metadata: HashMap<String, serde_json::Value>,
    /// Tables detected and extracted via OCR
    pub tables: Vec<OcrTable>,
    /// Structured OCR elements with bounding boxes and confidence scores.
    /// Available when TSV output is requested or table detection is enabled.
    #[serde(alias = "ocrElements")]
    #[serde(default)]
    pub ocr_elements: Option<Vec<OcrElement>>,
}

impl Default for OcrExtractionResult {
    fn default() -> Self {
        <xberg::OcrExtractionResult as Default>::default().into()
    }
}

#[php_impl]
impl OcrExtractionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        mimeType: String,
        tables: &ext_php_rs::types::ZendHashTable,
        ocrElements: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let mut tables_core_result: Vec<OcrTable> = Vec::new();
        for (_, item) in tables.iter() {
            if let Some(parsed) = <&OcrTable as ext_php_rs::convert::FromZval>::from_zval(item) {
                tables_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to OcrTable".to_string(),
                ));
            }
        }
        let tables_core: Vec<OcrTable> = tables_core_result;
        let ocrElements_core: Option<Vec<OcrElement>> = if let Some(ht) = ocrElements {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&OcrElement as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to OcrElement".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            content,
            mime_type: mimeType,
            metadata: Default::default(),
            tables: tables_core,
            ocr_elements: ocrElements_core,
        })
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }

    pub fn get_metadata(&self) -> Option<String> {
        serde_json::to_string(&self.metadata).ok()
    }

    pub fn get_tables(&self) -> Vec<OcrTable> {
        self.tables.clone()
    }

    pub fn get_ocr_elements(&self) -> Option<Vec<OcrElement>> {
        self.ocr_elements.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrMetadata")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct OcrMetadata {
    /// OCR language code(s) used
    #[php(prop, name = "language")]
    pub language: String,
    /// Tesseract Page Segmentation Mode (PSM)
    #[php(prop, name = "psm")]
    pub psm: i32,
    /// Output format (e.g., "text", "hocr")
    #[php(prop, name = "outputFormat")]
    #[serde(alias = "outputFormat")]
    pub output_format: String,
    /// Number of tables detected
    #[php(prop, name = "tableCount")]
    #[serde(alias = "tableCount")]
    pub table_count: u32,
    /// Number of rows in the detected table (if a single table was found).
    #[php(prop, name = "tableRows")]
    #[serde(alias = "tableRows")]
    pub table_rows: Option<u32>,
    /// Number of columns in the detected table (if a single table was found).
    #[php(prop, name = "tableCols")]
    #[serde(alias = "tableCols")]
    pub table_cols: Option<u32>,
}

impl Default for OcrMetadata {
    fn default() -> Self {
        <xberg::OcrMetadata as Default>::default().into()
    }
}

#[php_impl]
impl OcrMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        language: String,
        psm: i32,
        outputFormat: String,
        tableCount: u32,
        tableRows: Option<u32>,
        tableCols: Option<u32>,
    ) -> Self {
        Self {
            language,
            psm,
            output_format: outputFormat,
            table_count: tableCount,
            table_rows: tableRows,
            table_cols: tableCols,
        }
    }

    pub fn get_language(&self) -> String {
        self.language.clone()
    }

    pub fn get_psm(&self) -> i32 {
        self.psm.clone()
    }

    pub fn get_output_format(&self) -> String {
        self.output_format.clone()
    }

    pub fn get_table_count(&self) -> u32 {
        self.table_count.clone()
    }

    pub fn get_table_rows(&self) -> Option<u32> {
        self.table_rows.clone()
    }

    pub fn get_table_cols(&self) -> Option<u32> {
        self.table_cols.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OcrPipelineConfig")]
#[serde(default)]
pub struct OcrPipelineConfig {
    /// Ordered list of backends to try. Sorted by priority (descending) at runtime.
    pub stages: Vec<OcrPipelineStage>,
    /// Quality thresholds for deciding whether to accept a result or try the next backend.
    #[serde(alias = "qualityThresholds")]
    #[serde(default)]
    pub quality_thresholds: OcrQualityThresholds,
}

#[php_impl]
impl OcrPipelineConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(stages: &ext_php_rs::types::ZendHashTable, qualityThresholds: &OcrQualityThresholds) -> PhpResult<Self> {
        let mut stages_core_result: Vec<OcrPipelineStage> = Vec::new();
        for (_, item) in stages.iter() {
            if let Some(parsed) = <&OcrPipelineStage as ext_php_rs::convert::FromZval>::from_zval(item) {
                stages_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to OcrPipelineStage".to_string(),
                ));
            }
        }
        let stages_core: Vec<OcrPipelineStage> = stages_core_result;
        Ok(Self {
            stages: stages_core,
            quality_thresholds: qualityThresholds.clone(),
        })
    }

    pub fn get_stages(&self) -> Vec<OcrPipelineStage> {
        self.stages.clone()
    }

    pub fn get_quality_thresholds(&self) -> OcrQualityThresholds {
        self.quality_thresholds.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OcrPipelineStage")]
#[serde(default)]
pub struct OcrPipelineStage {
    /// Backend name: "tesseract", "paddleocr", "paddle-ocr", "sceptre", "vlm", or a custom registered name.
    /// Sceptre uses ONNX Runtime on desktop/server and tract on Android/iOS; browser WebAssembly has a separate
    /// byte-fed engine because the normal async OCR registry assumes native model storage.
    #[php(prop, name = "backend")]
    pub backend: String,
    /// Priority weight (higher = tried first). Stages are sorted by priority descending.
    #[php(prop, name = "priority")]
    pub priority: u32,
    /// Language override for this stage (undefined = use parent OcrConfig.language).
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs: `["eng", "deu"]`. When deserializing from a config file,
    /// JSON body, or the REST/MCP API, a single string is also accepted,
    /// either as one code ("eng") or "+"-joined ("eng+deu").
    #[php(prop, name = "language")]
    #[serde(default)]
    pub language: Option<Vec<String>>,
    /// Tesseract-specific config override for this stage.
    #[serde(alias = "tesseractConfig")]
    #[serde(default)]
    pub tesseract_config: Option<TesseractConfig>,
    /// PaddleOCR-specific config for this stage.
    #[serde(alias = "paddleOcrConfig")]
    #[serde(default)]
    pub paddle_ocr_config: Option<serde_json::Value>,
    /// VLM config override for this pipeline stage.
    #[serde(alias = "vlmConfig")]
    #[serde(default)]
    pub vlm_config: Option<LlmConfig>,
    /// Arbitrary per-call options passed through to the backend unchanged.
    ///
    /// Backends that support runtime tuning (mode switching, preprocessing
    /// flags, inference parameters, etc.) read this value and deserialize
    /// the keys they care about. Keys unknown to the backend are silently
    /// ignored, so options from different backends can coexist in the same
    /// config without conflict.
    ///
    /// Example (custom backend):
    /// ```json
    /// { "mode": "fast", "enable_layout": true }
    /// ```
    #[serde(alias = "backendOptions")]
    #[serde(default)]
    pub backend_options: Option<serde_json::Value>,
}

#[php_impl]
impl OcrPipelineStage {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        backend: String,
        priority: u32,
        language: Option<Vec<String>>,
        tesseractConfig: Option<&TesseractConfig>,
        paddleOcrConfig: Option<String>,
        vlmConfig: Option<&LlmConfig>,
        backendOptions: Option<String>,
    ) -> PhpResult<Self> {
        Ok(Self {
            backend,
            priority,
            language,
            tesseract_config: tesseractConfig.cloned(),
            paddle_ocr_config: paddleOcrConfig
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            vlm_config: vlmConfig.cloned(),
            backend_options: backendOptions
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
        })
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }

    pub fn get_priority(&self) -> u32 {
        self.priority.clone()
    }

    pub fn get_language(&self) -> Option<Vec<String>> {
        self.language.clone()
    }

    pub fn get_tesseract_config(&self) -> Option<TesseractConfig> {
        self.tesseract_config.clone().map(Into::into)
    }

    pub fn set_tesseract_config(&mut self, value: Option<&TesseractConfig>) {
        self.tesseract_config = value.map(|value| value.clone().into());
    }

    pub fn get_paddle_ocr_config(&self) -> Option<String> {
        self.paddle_ocr_config
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_vlm_config(&self) -> Option<LlmConfig> {
        self.vlm_config.clone().map(Into::into)
    }

    pub fn set_vlm_config(&mut self, value: Option<&LlmConfig>) {
        self.vlm_config = value.map(|value| value.clone().into());
    }

    pub fn get_backend_options(&self) -> Option<String> {
        self.backend_options
            .as_ref()
            .and_then(|v| serde_json::to_string(v).ok())
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrPoint")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct OcrPoint {
    /// Horizontal coordinate.
    #[php(prop, name = "x")]
    pub x: u32,
    /// Vertical coordinate.
    #[php(prop, name = "y")]
    pub y: u32,
}

impl Default for OcrPoint {
    fn default() -> Self {
        <xberg::OcrPoint as Default>::default().into()
    }
}

#[php_impl]
impl OcrPoint {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(x: u32, y: u32) -> Self {
        Self { x, y }
    }

    pub fn get_x(&self) -> u32 {
        self.x.clone()
    }

    pub fn get_y(&self) -> u32 {
        self.y.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\OcrQualityThresholds")]
#[serde(default)]
pub struct OcrQualityThresholds {
    /// Minimum total non-whitespace characters to consider text substantive.
    #[php(prop, name = "minTotalNonWhitespace")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_total_non_whitespace")]
    #[serde(alias = "minTotalNonWhitespace")]
    pub min_total_non_whitespace: i64,
    /// Minimum non-whitespace characters per page on average.
    #[php(prop, name = "minNonWhitespacePerPage")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_non_whitespace_per_page")]
    #[serde(alias = "minNonWhitespacePerPage")]
    pub min_non_whitespace_per_page: f64,
    /// Minimum character count for a word to be "meaningful".
    #[php(prop, name = "minMeaningfulWordLen")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_meaningful_word_len")]
    #[serde(alias = "minMeaningfulWordLen")]
    pub min_meaningful_word_len: i64,
    /// Minimum count of meaningful words before text is accepted.
    #[php(prop, name = "minMeaningfulWords")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_meaningful_words")]
    #[serde(alias = "minMeaningfulWords")]
    pub min_meaningful_words: i64,
    /// Minimum alphanumeric ratio (non-whitespace chars that are alphanumeric).
    #[php(prop, name = "minAlnumRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_alnum_ratio")]
    #[serde(alias = "minAlnumRatio")]
    pub min_alnum_ratio: f64,
    /// Minimum Unicode replacement characters (U+FFFD) to trigger OCR fallback.
    #[php(prop, name = "minGarbageChars")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_garbage_chars")]
    #[serde(alias = "minGarbageChars")]
    pub min_garbage_chars: i64,
    /// Maximum fraction of short (1-2 char) words before text is considered fragmented.
    #[php(prop, name = "maxFragmentedWordRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_max_fragmented_word_ratio")]
    #[serde(alias = "maxFragmentedWordRatio")]
    pub max_fragmented_word_ratio: f64,
    /// Critical fragmentation threshold — triggers OCR regardless of meaningful words.
    /// Normal English text has ~20-30% short words. 80%+ is definitive garbage.
    #[php(prop, name = "criticalFragmentedWordRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_critical_fragmented_word_ratio")]
    #[serde(alias = "criticalFragmentedWordRatio")]
    pub critical_fragmented_word_ratio: f64,
    /// Maximum fraction of short (1-2 char) words an *OCR result* may carry before the page
    /// is reported as suspected recognition noise.
    ///
    /// This is a different decision, and a different operating point, from
    /// `Self.max_fragmented_word_ratio` / `Self.critical_fragmented_word_ratio`: those
    /// ask "is the native text bad enough that we should OCR this page?", and are tuned to
    /// only fire on text that is already definitively broken (0.6 / 0.8). Here we are judging
    /// what OCR *produced*, where the failure mode is an engine run over line art — a scanned
    /// plat, an engineering drawing, a signature flourish — returning confident-looking
    /// strings that are not words.
    ///
    /// Measured over the 16 pages of a recorded municipal ordinance (13 prose pages, 3 scanned
    /// survey drawings): prose ran 0.04-0.28, the drawings 0.42-0.47. The default sits in that
    /// gap with margin on both sides. By default, crossing the threshold emits a processing
    /// warning but retains the recognized text. Set `Self.discard_suspected_ocr_noise` to
    /// `true` to restore destructive filtering.
    #[php(prop, name = "maxOcrOutputFragmentedWordRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_max_ocr_output_fragmented_word_ratio")]
    #[serde(alias = "maxOcrOutputFragmentedWordRatio")]
    pub max_ocr_output_fragmented_word_ratio: f64,
    /// Minimum mean OCR confidence (0-100) below which a page is reported as suspected noise.
    ///
    /// This is the engine's own uncertainty about what it read, and it is a far sharper
    /// instrument than any statistic derived from the output text. Measured per page over a
    /// recorded municipal ordinance (10 prose pages, 6 scanned survey/architectural drawings)
    /// with Tesseract 5.5.3:
    ///
    /// ```text
    /// prose      86.3 - 95.3
    /// drawings   18.5 - 64.3
    /// ```
    ///
    /// The default sits in that gap with ~11 points of margin on each side. Compare the
    /// short-word ratio, which separated the same two groups by 0.09 on a 0-1 scale.
    ///
    /// A backend that reports no confidence (no `mean_text_conf` in its result metadata)
    /// skips this check entirely and falls back to
    /// `Self.max_ocr_output_fragmented_word_ratio`. By default, the signal emits a warning
    /// without discarding content. Set to 0.0 to disable the signal.
    #[php(prop, name = "minOcrMeanConfidence")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_ocr_mean_confidence")]
    #[serde(alias = "minOcrMeanConfidence")]
    pub min_ocr_mean_confidence: f64,
    /// Minimum word count before `Self.max_ocr_output_fragmented_word_ratio` may report a
    /// page. Short pages (a signature block, an exhibit title) are legitimately dominated by
    /// short words, so the ratio is not meaningful on them and the signal stays disabled.
    #[php(prop, name = "minWordsForOcrOutputCheck")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_words_for_ocr_output_check")]
    #[serde(alias = "minWordsForOcrOutputCheck")]
    pub min_words_for_ocr_output_check: i64,
    /// Maximum fraction of a Tesseract page's dictionary-checkable words that
    /// `TesseractAPI.is_valid_word` may reject before the page is treated as recognition
    /// noise, supplementing (never replacing) `Self.max_ocr_output_fragmented_word_ratio`.
    ///
    /// Tesseract-only: other backends never report this ratio (see
    /// `dictionary_invalid_word_ratio` in `ocr.processor.execution`), so this threshold is
    /// simply never consulted for them.
    ///
    /// Calibration owed, same as `ocr.types.TesseractConfig.min_confidence`:
    /// unlike `Self.max_ocr_output_fragmented_word_ratio` (measured across a recorded
    /// ordinance's prose vs. drawing pages), this ratio has not yet had a page-level
    /// measurement run over a labeled corpus. The default therefore disables the check
    /// entirely (`1.01`, above the `[0.0, 1.0]` range a ratio can reach) rather than guess an
    /// operating point. Do not lower this without running that measurement first.
    #[php(prop, name = "maxOcrOutputDictInvalidWordRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_max_ocr_output_dict_invalid_word_ratio")]
    #[serde(alias = "maxOcrOutputDictInvalidWordRatio")]
    pub max_ocr_output_dict_invalid_word_ratio: f64,
    /// Discard non-empty OCR text when any configured recognition-noise signal fires.
    ///
    /// Defaults to `false`: suspected noise is retained and surfaced through
    /// `processing_warnings`. Set to `true` to preserve the legacy destructive behavior.
    #[php(prop, name = "discardSuspectedOcrNoise")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_discard_suspected_ocr_noise")]
    #[serde(alias = "discardSuspectedOcrNoise")]
    pub discard_suspected_ocr_noise: bool,
    /// Minimum average word length. Below this with enough words indicates garbled extraction.
    #[php(prop, name = "minAvgWordLength")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_avg_word_length")]
    #[serde(alias = "minAvgWordLength")]
    pub min_avg_word_length: f64,
    /// Minimum word count before average word length check applies.
    #[php(prop, name = "minWordsForAvgLengthCheck")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_words_for_avg_length_check")]
    #[serde(alias = "minWordsForAvgLengthCheck")]
    pub min_words_for_avg_length_check: i64,
    /// Minimum consecutive word repetition ratio to detect column scrambling.
    #[php(prop, name = "minConsecutiveRepeatRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_consecutive_repeat_ratio")]
    #[serde(alias = "minConsecutiveRepeatRatio")]
    pub min_consecutive_repeat_ratio: f64,
    /// Minimum word count before consecutive repetition check is applied.
    #[php(prop, name = "minWordsForRepeatCheck")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_words_for_repeat_check")]
    #[serde(alias = "minWordsForRepeatCheck")]
    pub min_words_for_repeat_check: i64,
    /// Minimum character count for "substantive markdown" OCR skip gate.
    #[php(prop, name = "substantiveMinChars")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_substantive_min_chars")]
    #[serde(alias = "substantiveMinChars")]
    pub substantive_min_chars: i64,
    /// Minimum character count for "non-text content" OCR skip gate.
    #[php(prop, name = "nonTextMinChars")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_non_text_min_chars")]
    #[serde(alias = "nonTextMinChars")]
    pub non_text_min_chars: i64,
    /// Alphanumeric+whitespace ratio threshold for skip decisions.
    #[php(prop, name = "alnumWsRatioThreshold")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_alnum_ws_ratio_threshold")]
    #[serde(alias = "alnumWsRatioThreshold")]
    pub alnum_ws_ratio_threshold: f64,
    /// Minimum quality score (0.0-1.0) for a pipeline stage result to be accepted.
    /// If the result from a backend scores below this, try the next backend.
    #[php(prop, name = "pipelineMinQuality")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_pipeline_min_quality")]
    #[serde(alias = "pipelineMinQuality")]
    pub pipeline_min_quality: f64,
    /// Minimum fraction of non-whitespace characters that are undecodable
    /// (Unicode Private Use Area, replacement characters, or non-whitespace
    /// control characters) before a page's text layer is treated as
    /// unreadable and routed to OCR (issue #1254). Gated by
    /// `min_total_non_whitespace` so short snippets with a stray symbol or
    /// two do not trip this check.
    #[php(prop, name = "minUndecodableRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_undecodable_ratio")]
    #[serde(alias = "minUndecodableRatio")]
    pub min_undecodable_ratio: f64,
    /// Whether to route a page to OCR when xberg_native_pdf reports that a high
    /// fraction of its text was fabricated rather than read from the file
    /// (`MappingProvenance.Fallback`, xberg_native_pdf 0.3.75+, issue #1254). This is
    /// a direct fact from the extractor's ISO 32000-1 §9.10.2 mapping cascade,
    /// distinct from the character-heuristic proxy behind `min_undecodable_ratio`.
    /// Defaults to `true`.
    #[php(prop, name = "enableProvenanceOcrRouting")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_enable_provenance_ocr_routing")]
    #[serde(alias = "enableProvenanceOcrRouting")]
    pub enable_provenance_ocr_routing: bool,
    /// Minimum fraction of a page's non-whitespace characters with
    /// `MappingProvenance.Fallback` provenance before the page is treated as
    /// having a fabricated text layer and routed to OCR (issue #1254). Gated by
    /// `min_total_non_whitespace` so a short page with a stray fallback
    /// character cannot trip it. Only used when `enable_provenance_ocr_routing`
    /// is `true`.
    #[php(prop, name = "minProvenanceFallbackRatio")]
    #[serde(default = "crate::serde_defaults::ocr_quality_thresholds_min_provenance_fallback_ratio")]
    #[serde(alias = "minProvenanceFallbackRatio")]
    pub min_provenance_fallback_ratio: f64,
}

impl Default for OcrQualityThresholds {
    fn default() -> Self {
        <xberg::OcrQualityThresholds as Default>::default().into()
    }
}

#[php_impl]
impl OcrQualityThresholds {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        minTotalNonWhitespace: i64,
        minNonWhitespacePerPage: f64,
        minMeaningfulWordLen: i64,
        minMeaningfulWords: i64,
        minAlnumRatio: f64,
        minGarbageChars: i64,
        maxFragmentedWordRatio: f64,
        criticalFragmentedWordRatio: f64,
        maxOcrOutputFragmentedWordRatio: f64,
        minOcrMeanConfidence: f64,
        minWordsForOcrOutputCheck: i64,
        maxOcrOutputDictInvalidWordRatio: f64,
        discardSuspectedOcrNoise: bool,
        minAvgWordLength: f64,
        minWordsForAvgLengthCheck: i64,
        minConsecutiveRepeatRatio: f64,
        minWordsForRepeatCheck: i64,
        substantiveMinChars: i64,
        nonTextMinChars: i64,
        alnumWsRatioThreshold: f64,
        pipelineMinQuality: f64,
        minUndecodableRatio: f64,
        enableProvenanceOcrRouting: bool,
        minProvenanceFallbackRatio: f64,
    ) -> Self {
        Self {
            min_total_non_whitespace: minTotalNonWhitespace,
            min_non_whitespace_per_page: minNonWhitespacePerPage,
            min_meaningful_word_len: minMeaningfulWordLen,
            min_meaningful_words: minMeaningfulWords,
            min_alnum_ratio: minAlnumRatio,
            min_garbage_chars: minGarbageChars,
            max_fragmented_word_ratio: maxFragmentedWordRatio,
            critical_fragmented_word_ratio: criticalFragmentedWordRatio,
            max_ocr_output_fragmented_word_ratio: maxOcrOutputFragmentedWordRatio,
            min_ocr_mean_confidence: minOcrMeanConfidence,
            min_words_for_ocr_output_check: minWordsForOcrOutputCheck,
            max_ocr_output_dict_invalid_word_ratio: maxOcrOutputDictInvalidWordRatio,
            discard_suspected_ocr_noise: discardSuspectedOcrNoise,
            min_avg_word_length: minAvgWordLength,
            min_words_for_avg_length_check: minWordsForAvgLengthCheck,
            min_consecutive_repeat_ratio: minConsecutiveRepeatRatio,
            min_words_for_repeat_check: minWordsForRepeatCheck,
            substantive_min_chars: substantiveMinChars,
            non_text_min_chars: nonTextMinChars,
            alnum_ws_ratio_threshold: alnumWsRatioThreshold,
            pipeline_min_quality: pipelineMinQuality,
            min_undecodable_ratio: minUndecodableRatio,
            enable_provenance_ocr_routing: enableProvenanceOcrRouting,
            min_provenance_fallback_ratio: minProvenanceFallbackRatio,
        }
    }

    pub fn get_min_total_non_whitespace(&self) -> i64 {
        self.min_total_non_whitespace.clone()
    }

    pub fn get_min_non_whitespace_per_page(&self) -> f64 {
        self.min_non_whitespace_per_page.clone()
    }

    pub fn get_min_meaningful_word_len(&self) -> i64 {
        self.min_meaningful_word_len.clone()
    }

    pub fn get_min_meaningful_words(&self) -> i64 {
        self.min_meaningful_words.clone()
    }

    pub fn get_min_alnum_ratio(&self) -> f64 {
        self.min_alnum_ratio.clone()
    }

    pub fn get_min_garbage_chars(&self) -> i64 {
        self.min_garbage_chars.clone()
    }

    pub fn get_max_fragmented_word_ratio(&self) -> f64 {
        self.max_fragmented_word_ratio.clone()
    }

    pub fn get_critical_fragmented_word_ratio(&self) -> f64 {
        self.critical_fragmented_word_ratio.clone()
    }

    pub fn get_max_ocr_output_fragmented_word_ratio(&self) -> f64 {
        self.max_ocr_output_fragmented_word_ratio.clone()
    }

    pub fn get_min_ocr_mean_confidence(&self) -> f64 {
        self.min_ocr_mean_confidence.clone()
    }

    pub fn get_min_words_for_ocr_output_check(&self) -> i64 {
        self.min_words_for_ocr_output_check.clone()
    }

    pub fn get_max_ocr_output_dict_invalid_word_ratio(&self) -> f64 {
        self.max_ocr_output_dict_invalid_word_ratio.clone()
    }

    pub fn get_discard_suspected_ocr_noise(&self) -> bool {
        self.discard_suspected_ocr_noise.clone()
    }

    pub fn get_min_avg_word_length(&self) -> f64 {
        self.min_avg_word_length.clone()
    }

    pub fn get_min_words_for_avg_length_check(&self) -> i64 {
        self.min_words_for_avg_length_check.clone()
    }

    pub fn get_min_consecutive_repeat_ratio(&self) -> f64 {
        self.min_consecutive_repeat_ratio.clone()
    }

    pub fn get_min_words_for_repeat_check(&self) -> i64 {
        self.min_words_for_repeat_check.clone()
    }

    pub fn get_substantive_min_chars(&self) -> i64 {
        self.substantive_min_chars.clone()
    }

    pub fn get_non_text_min_chars(&self) -> i64 {
        self.non_text_min_chars.clone()
    }

    pub fn get_alnum_ws_ratio_threshold(&self) -> f64 {
        self.alnum_ws_ratio_threshold.clone()
    }

    pub fn get_pipeline_min_quality(&self) -> f64 {
        self.pipeline_min_quality.clone()
    }

    pub fn get_min_undecodable_ratio(&self) -> f64 {
        self.min_undecodable_ratio.clone()
    }

    pub fn get_enable_provenance_ocr_routing(&self) -> bool {
        self.enable_provenance_ocr_routing.clone()
    }

    pub fn get_min_provenance_fallback_ratio(&self) -> f64 {
        self.min_provenance_fallback_ratio.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> OcrQualityThresholds {
        xberg::OcrQualityThresholds::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OcrRotation")]
#[serde(default)]
pub struct OcrRotation {
    /// Rotation angle in degrees (0, 90, 180, 270 for PaddleOCR).
    #[php(prop, name = "angleDegrees")]
    #[serde(alias = "angleDegrees")]
    pub angle_degrees: f64,
    /// Confidence score for the rotation detection.
    #[php(prop, name = "confidence")]
    pub confidence: Option<f64>,
}

#[php_impl]
impl OcrRotation {
    #[php(constructor)]
    pub fn new(angleDegrees: f64, confidence: Option<f64>) -> Self {
        Self {
            angle_degrees: angleDegrees,
            confidence,
        }
    }

    pub fn get_angle_degrees(&self) -> f64 {
        self.angle_degrees.clone()
    }

    pub fn get_confidence(&self) -> Option<f64> {
        self.confidence.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OcrTable")]
#[serde(default)]
pub struct OcrTable {
    /// Table cells as a 2D vector (rows × columns)
    pub cells: Vec<Vec<String>>,
    /// Markdown representation of the table
    #[php(prop, name = "markdown")]
    pub markdown: String,
    /// Page number where the table was found (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Bounding box of the table in pixel coordinates (from OCR word positions).
    #[serde(alias = "boundingBox")]
    #[serde(default)]
    pub bounding_box: Option<OcrTableBoundingBox>,
}

#[php_impl]
impl OcrTable {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        cells: Vec<Vec<String>>,
        markdown: String,
        pageNumber: u32,
        boundingBox: Option<&OcrTableBoundingBox>,
    ) -> Self {
        Self {
            cells,
            markdown,
            page_number: pageNumber,
            bounding_box: boundingBox.cloned(),
        }
    }

    pub fn get_cells(&self) -> Vec<Vec<String>> {
        self.cells.clone()
    }

    pub fn get_markdown(&self) -> String {
        self.markdown.clone()
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_bounding_box(&self) -> Option<OcrTableBoundingBox> {
        self.bounding_box.clone().map(Into::into)
    }

    pub fn set_bounding_box(&mut self, value: Option<&OcrTableBoundingBox>) {
        self.bounding_box = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OcrTableBoundingBox")]
#[serde(default)]
pub struct OcrTableBoundingBox {
    /// Left x-coordinate (pixels)
    #[php(prop, name = "left")]
    pub left: u32,
    /// Top y-coordinate (pixels)
    #[php(prop, name = "top")]
    pub top: u32,
    /// Right x-coordinate (pixels)
    #[php(prop, name = "right")]
    pub right: u32,
    /// Bottom y-coordinate (pixels)
    #[php(prop, name = "bottom")]
    pub bottom: u32,
}

#[php_impl]
impl OcrTableBoundingBox {
    #[php(constructor)]
    pub fn new(left: u32, top: u32, right: u32, bottom: u32) -> Self {
        Self {
            left,
            top,
            right,
            bottom,
        }
    }

    pub fn get_left(&self) -> u32 {
        self.left.clone()
    }

    pub fn get_top(&self) -> u32 {
        self.top.clone()
    }

    pub fn get_right(&self) -> u32 {
        self.right.clone()
    }

    pub fn get_bottom(&self) -> u32 {
        self.bottom.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\OrientationResult")]
#[serde(default)]
pub struct OrientationResult {
    /// Detected orientation in degrees (0, 90, 180, or 270).
    #[php(prop, name = "degrees")]
    pub degrees: u32,
    /// Confidence score (0.0-1.0).
    #[php(prop, name = "confidence")]
    pub confidence: f32,
}

#[php_impl]
impl OrientationResult {
    #[php(constructor)]
    pub fn new(degrees: u32, confidence: f32) -> Self {
        Self { degrees, confidence }
    }

    pub fn get_degrees(&self) -> u32 {
        self.degrees.clone()
    }

    pub fn get_confidence(&self) -> f32 {
        self.confidence.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PaddleOcrConfig")]
#[serde(default)]
pub struct PaddleOcrConfig {
    /// Language code (e.g., "en", "ch", "jpn", "kor", "deu", "fra")
    #[php(prop, name = "language")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_language")]
    pub language: String,
    /// Optional Hugging Face Hub cache root for model files.
    ///
    /// When unset, the standard `HF_HUB_CACHE`, legacy
    /// `HUGGINGFACE_HUB_CACHE`, and `HF_HOME` conventions are used.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Enable angle classification for rotated text (default: false).
    /// Can misfire on short text regions, rotating crops incorrectly before recognition.
    #[php(prop, name = "useAngleCls")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_use_angle_cls")]
    #[serde(alias = "useAngleCls")]
    pub use_angle_cls: bool,
    /// Enable table structure detection (default: **false**, unlike `TesseractConfig`
    /// which defaults to `true` — see `crate.ocr.types.TesseractConfig`).
    ///
    /// PaddleOCR has no per-word table-candidate confidence carve-out the way Tesseract's
    /// TSV does (every recognised word is a clustering candidate), so unfiltered clustering
    /// over-fabricates tables on ordinary prose. This stays off until it clears a
    /// cell-scored accuracy measurement (see the pinning test at the bottom of this file).
    /// Consequence for callers: switching an existing config from the Tesseract backend to
    /// PaddleOCR with stock defaults silently produces **no** OCR tables — enable this
    /// explicitly if you need them.
    ///
    /// When enabled, this only clusters and reconstructs a grid from word boxes PaddleOCR
    /// has already produced (see `crate.paddle_ocr.backend.PaddleOcrBackend.process_image`)
    /// — it does not run any additional model inference, so the added per-page cost is the
    /// CPU-only clustering/reconstruction pass, not another ONNX call.
    #[php(prop, name = "enableTableDetection")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_enable_table_detection")]
    #[serde(alias = "enableTableDetection")]
    pub enable_table_detection: bool,
    /// Database threshold for text detection (default: 0.3)
    /// Range: 0.0-1.0, higher values require more confident detections
    #[php(prop, name = "detDbThresh")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_det_db_thresh")]
    #[serde(alias = "detDbThresh")]
    pub det_db_thresh: f32,
    /// Box threshold for text bounding box refinement (default: 0.5)
    /// Range: 0.0-1.0
    #[php(prop, name = "detDbBoxThresh")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_det_db_box_thresh")]
    #[serde(alias = "detDbBoxThresh")]
    pub det_db_box_thresh: f32,
    /// Unclip ratio for expanding text bounding boxes (default: 1.6)
    /// Controls the expansion of detected text regions
    #[php(prop, name = "detDbUnclipRatio")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_det_db_unclip_ratio")]
    #[serde(alias = "detDbUnclipRatio")]
    pub det_db_unclip_ratio: f32,
    /// Maximum side length for detection image (default: 1024)
    /// Larger images may be resized to this limit for faster inference
    #[php(prop, name = "detLimitSideLen")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_det_limit_side_len")]
    #[serde(alias = "detLimitSideLen")]
    pub det_limit_side_len: u32,
    /// Batch size for recognition inference (default: 6)
    /// Number of text regions to process simultaneously
    #[php(prop, name = "recBatchNum")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_rec_batch_num")]
    #[serde(alias = "recBatchNum")]
    pub rec_batch_num: u32,
    /// Padding in pixels added around the image before detection (default: 10).
    /// Large values can include surrounding content like table gridlines.
    #[php(prop, name = "padding")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_padding")]
    pub padding: u32,
    /// Minimum recognition confidence score for text lines (default: 0.5).
    /// Text regions with recognition confidence below this threshold are discarded.
    /// Matches PaddleOCR Python's `drop_score` parameter.
    /// Range: 0.0-1.0
    #[php(prop, name = "dropScore")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_drop_score")]
    #[serde(alias = "dropScore")]
    pub drop_score: f32,
    /// Model tier controlling detection/recognition model size and accuracy trade-off.
    ///
    /// For PP-OCRv5 (`model_version = "pp-ocrv5"`):
    /// - `"mobile"` (default): Lightweight models (~4.5MB detection, ~16.5MB recognition), fast download and inference
    /// - `"server"`: Large, high-accuracy models (~88MB detection, ~84MB recognition), best for GPU or complex documents
    ///
    /// For PP-OCRv6 (`model_version = "pp-ocrv6"`, the default):
    /// - `"small"`: ~9.9MB detection, full 18,708-char CJK+Latin+JA/KO recognition dictionary.
    ///   The default `"mobile"` resolves here, so this is what an unconfigured extraction uses.
    /// - `"medium"`: ~62MB detection, same dictionary. Higher accuracy, substantially slower on
    ///   CPU. A legacy `"server"` tier, or any unrecognised value, resolves here.
    /// - `"tiny"`: ~1.8MB detection, but a reduced 6,904-char (~zh/en) dictionary — it cannot
    ///   read the scripts the other two cover.
    ///
    /// Note PaddleOCR pages do not run concurrently: the ONNX session is held behind a mutex, so
    /// the thread budget goes to intra-op parallelism and wall time scales with page count times
    /// per-page inference. Tier choice therefore dominates throughput on multi-page documents.
    #[php(prop, name = "modelTier")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_model_tier")]
    #[serde(alias = "modelTier")]
    pub model_tier: String,
    /// Model generation: `"pp-ocrv6"` (default) or `"pp-ocrv5"`.
    ///
    /// PP-OCRv6 adds a unified CJK+Latin+JA/KO recognition model with `medium`/`small`/`tiny`
    /// tiers (see `model_tier`). Scripts outside the v6 unified coverage (Arabic, Cyrillic,
    /// Devanagari, Greek, Tamil, Telugu, Thai) transparently fall back to the PP-OCRv5
    /// per-script recognition models. Defaults to `"pp-ocrv6"`; the default `model_tier`
    /// (`"mobile"`) resolves to the v6 `"small"` tier. Select `"pp-ocrv5"` to pin the
    /// legacy per-script/unified fleet.
    #[php(prop, name = "modelVersion")]
    #[serde(default = "crate::serde_defaults::paddle_ocr_config_model_version")]
    #[serde(alias = "modelVersion")]
    pub model_version: String,
    /// Explicit inference engine choice.
    ///
    /// `None` (the default) resolves to the compiled default: `ort` when the
    /// `paddle-ocr-ort` feature is compiled in, otherwise `tract`. An explicit choice
    /// is validated against the compiled features when the OCR engine is constructed
    /// (see `crate.paddle_ocr.backend.effective_backend`); requesting an engine
    /// whose feature is not compiled in is a clear configuration error rather than a
    /// silent fallback.
    #[php(prop, name = "inferenceBackend")]
    #[serde(skip_serializing_if = "Option::is_none")]
    #[serde(alias = "inferenceBackend")]
    #[serde(default)]
    pub inference_backend: Option<String>,
}

impl Default for PaddleOcrConfig {
    fn default() -> Self {
        <xberg::PaddleOcrConfig as Default>::default().into()
    }
}

#[php_impl]
impl PaddleOcrConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        language: String,
        useAngleCls: bool,
        enableTableDetection: bool,
        detDbThresh: f32,
        detDbBoxThresh: f32,
        detDbUnclipRatio: f32,
        detLimitSideLen: u32,
        recBatchNum: u32,
        padding: u32,
        dropScore: f32,
        modelTier: String,
        modelVersion: String,
        cacheDir: Option<String>,
        inferenceBackend: Option<String>,
    ) -> Self {
        Self {
            language,
            cache_dir: cacheDir,
            use_angle_cls: useAngleCls,
            enable_table_detection: enableTableDetection,
            det_db_thresh: detDbThresh,
            det_db_box_thresh: detDbBoxThresh,
            det_db_unclip_ratio: detDbUnclipRatio,
            det_limit_side_len: detLimitSideLen,
            rec_batch_num: recBatchNum,
            padding,
            drop_score: dropScore,
            model_tier: modelTier,
            model_version: modelVersion,
            inference_backend: inferenceBackend,
        }
    }

    pub fn get_language(&self) -> String {
        self.language.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_use_angle_cls(&self) -> bool {
        self.use_angle_cls.clone()
    }

    pub fn get_enable_table_detection(&self) -> bool {
        self.enable_table_detection.clone()
    }

    pub fn get_det_db_thresh(&self) -> f32 {
        self.det_db_thresh.clone()
    }

    pub fn get_det_db_box_thresh(&self) -> f32 {
        self.det_db_box_thresh.clone()
    }

    pub fn get_det_db_unclip_ratio(&self) -> f32 {
        self.det_db_unclip_ratio.clone()
    }

    pub fn get_det_limit_side_len(&self) -> u32 {
        self.det_limit_side_len.clone()
    }

    pub fn get_rec_batch_num(&self) -> u32 {
        self.rec_batch_num.clone()
    }

    pub fn get_padding(&self) -> u32 {
        self.padding.clone()
    }

    pub fn get_drop_score(&self) -> f32 {
        self.drop_score.clone()
    }

    pub fn get_model_tier(&self) -> String {
        self.model_tier.clone()
    }

    pub fn get_model_version(&self) -> String {
        self.model_version.clone()
    }

    pub fn get_inference_backend(&self) -> Option<String> {
        self.inference_backend.clone().map(Into::into)
    }

    pub fn with_cache_dir(&self, path: String) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_cache_dir(std::path::PathBuf::from(path)).into()
    }

    pub fn with_table_detection(&self, enable: bool) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_table_detection(enable).into()
    }

    pub fn with_angle_cls(&self, enable: bool) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_angle_cls(enable).into()
    }

    pub fn with_det_db_thresh(&self, threshold: f32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_det_db_thresh(threshold).into()
    }

    pub fn with_det_db_box_thresh(&self, threshold: f32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_det_db_box_thresh(threshold).into()
    }

    pub fn with_det_db_unclip_ratio(&self, ratio: f32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_det_db_unclip_ratio(ratio).into()
    }

    pub fn with_det_limit_side_len(&self, length: u32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_det_limit_side_len(length).into()
    }

    pub fn with_rec_batch_num(&self, batchSize: u32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_rec_batch_num(batchSize).into()
    }

    pub fn with_drop_score(&self, score: f32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_drop_score(score).into()
    }

    pub fn with_padding(&self, padding: u32) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_padding(padding).into()
    }

    pub fn with_model_tier(&self, tier: String) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_model_tier(tier).into()
    }

    pub fn with_model_version(&self, version: String) -> PaddleOcrConfig {
        #[allow(clippy::needless_update)]
            let core_self = xberg::PaddleOcrConfig {
                language: self.language.clone(),
                cache_dir: self.cache_dir.clone().map(Into::into),
                use_angle_cls: self.use_angle_cls,
                enable_table_detection: self.enable_table_detection,
                det_db_thresh: self.det_db_thresh,
                det_db_box_thresh: self.det_db_box_thresh,
                det_db_unclip_ratio: self.det_db_unclip_ratio,
                det_limit_side_len: self.det_limit_side_len,
                rec_batch_num: self.rec_batch_num,
                padding: self.padding,
                drop_score: self.drop_score,
                model_tier: self.model_tier.clone(),
                model_version: self.model_version.clone(),
                inference_backend: self.inference_backend.as_deref().map(|s| match s { "ort" => xberg::PaddleInferenceBackend::Ort,"tract" => xberg::PaddleInferenceBackend::Tract,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PaddleInferenceBackend::Ort
    }
     })
    ,
                ..Default::default()
            };
        core_self.with_model_version(version).into()
    }

    /// Creates a default configuration with English language support.
    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> PaddleOcrConfig {
        xberg::PaddleOcrConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PaddleOcrVlBackendOptions")]
#[serde(default)]
pub struct PaddleOcrVlBackendOptions {
    /// Optional per-call recognition task; the backend constructor's task is used when absent.
    #[php(prop, name = "task")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub task: Option<String>,
    /// Optional local model directory, which takes precedence over `model_id`.
    #[php(prop, name = "modelPath")]
    #[serde(alias = "modelPath")]
    pub model_path: Option<String>,
    /// Optional Hugging Face repository identifier.
    #[php(prop, name = "modelId")]
    #[serde(alias = "modelId")]
    pub model_id: Option<String>,
    /// Optional immutable Hugging Face model revision.
    #[php(prop, name = "hfRevision")]
    #[serde(alias = "hfRevision")]
    pub hf_revision: Option<String>,
    /// Optional Hugging Face cache root.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Optional per-call device override.
    #[php(prop, name = "device")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub device: Option<String>,
}

impl Default for PaddleOcrVlBackendOptions {
    fn default() -> Self {
        <xberg::candle_ocr::PaddleOcrVlBackendOptions as Default>::default().into()
    }
}

#[php_impl]
impl PaddleOcrVlBackendOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_task(&self) -> Option<String> {
        self.task.clone().map(Into::into)
    }

    pub fn get_model_path(&self) -> Option<String> {
        self.model_path.clone()
    }

    pub fn get_model_id(&self) -> Option<String> {
        self.model_id.clone()
    }

    pub fn get_hf_revision(&self) -> Option<String> {
        self.hf_revision.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_device(&self) -> Option<String> {
        self.device.clone().map(Into::into)
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageBoundary")]
#[serde(default)]
pub struct PageBoundary {
    /// Byte offset where this page starts in the content string (UTF-8 valid boundary, inclusive)
    #[php(prop, name = "byteStart")]
    #[serde(alias = "byteStart")]
    pub byte_start: i64,
    /// Byte offset where this page ends in the content string (UTF-8 valid boundary, exclusive)
    #[php(prop, name = "byteEnd")]
    #[serde(alias = "byteEnd")]
    pub byte_end: i64,
    /// Page number (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
}

#[php_impl]
impl PageBoundary {
    #[php(constructor)]
    pub fn new(byteStart: i64, byteEnd: i64, pageNumber: u32) -> Self {
        Self {
            byte_start: byteStart,
            byte_end: byteEnd,
            page_number: pageNumber,
        }
    }

    pub fn get_byte_start(&self) -> i64 {
        self.byte_start.clone()
    }

    pub fn get_byte_end(&self) -> i64 {
        self.byte_end.clone()
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageClassification")]
#[serde(default)]
pub struct PageClassification {
    /// 1-indexed page number this classification belongs to.
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Labels assigned to the page. Single-label classification yields exactly one
    /// entry; multi-label classification yields any subset of the configured label set.
    pub labels: Vec<ClassificationLabel>,
}

#[php_impl]
impl PageClassification {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(pageNumber: u32, labels: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut labels_core_result: Vec<ClassificationLabel> = Vec::new();
        for (_, item) in labels.iter() {
            if let Some(parsed) = <&ClassificationLabel as ext_php_rs::convert::FromZval>::from_zval(item) {
                labels_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ClassificationLabel".to_string(),
                ));
            }
        }
        let labels_core: Vec<ClassificationLabel> = labels_core_result;
        Ok(Self {
            page_number: pageNumber,
            labels: labels_core,
        })
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_labels(&self) -> Vec<ClassificationLabel> {
        self.labels.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageClassificationConfig")]
#[serde(default)]
pub struct PageClassificationConfig {
    /// Minijinja prompt template. Receives `{{ labels }}` (joined list), `{{ page_text }}`
    /// and `{{ multi_label }}` variables. `None` lets the backend pick a sensible default.
    #[php(prop, name = "promptTemplate")]
    #[serde(alias = "promptTemplate")]
    pub prompt_template: Option<String>,
    /// The set of labels the classifier may emit. Must contain at least one entry.
    #[php(prop, name = "labels")]
    pub labels: Vec<String>,
    /// Allow multiple labels per page. Single-label mode returns at most one label.
    #[php(prop, name = "multiLabel")]
    #[serde(alias = "multiLabel")]
    #[serde(default)]
    pub multi_label: bool,
    /// LLM configuration used for classification.
    pub llm: LlmConfig,
}

#[php_impl]
impl PageClassificationConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(labels: Vec<String>, multiLabel: bool, llm: &LlmConfig, promptTemplate: Option<String>) -> Self {
        Self {
            prompt_template: promptTemplate,
            labels,
            multi_label: multiLabel,
            llm: llm.clone(),
        }
    }

    pub fn get_prompt_template(&self) -> Option<String> {
        self.prompt_template.clone()
    }

    pub fn get_labels(&self) -> Vec<String> {
        self.labels.clone()
    }

    pub fn get_multi_label(&self) -> bool {
        self.multi_label.clone()
    }

    pub fn get_llm(&self) -> LlmConfig {
        self.llm.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PageConfig")]
#[serde(default)]
pub struct PageConfig {
    /// Extract pages as separate array (ExtractedDocument.pages)
    #[php(prop, name = "extractPages")]
    #[serde(default = "crate::serde_defaults::page_config_extract_pages")]
    #[serde(alias = "extractPages")]
    pub extract_pages: bool,
    /// Insert page markers in main content string
    #[php(prop, name = "insertPageMarkers")]
    #[serde(default = "crate::serde_defaults::page_config_insert_page_markers")]
    #[serde(alias = "insertPageMarkers")]
    pub insert_page_markers: bool,
    /// Page marker format (use {page_num} placeholder)
    /// Default: "\n\n<!-- PAGE {page_num} -->\n\n"
    #[php(prop, name = "markerFormat")]
    #[serde(default = "crate::serde_defaults::page_config_marker_format")]
    #[serde(alias = "markerFormat")]
    pub marker_format: String,
}

impl Default for PageConfig {
    fn default() -> Self {
        <xberg::PageConfig as Default>::default().into()
    }
}

#[php_impl]
impl PageConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(extractPages: bool, insertPageMarkers: bool, markerFormat: String) -> Self {
        Self {
            extract_pages: extractPages,
            insert_page_markers: insertPageMarkers,
            marker_format: markerFormat,
        }
    }

    pub fn get_extract_pages(&self) -> bool {
        self.extract_pages.clone()
    }

    pub fn get_insert_page_markers(&self) -> bool {
        self.insert_page_markers.clone()
    }

    pub fn get_marker_format(&self) -> String {
        self.marker_format.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> PageConfig {
        xberg::PageConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageContent")]
#[serde(default)]
pub struct PageContent {
    /// Page number (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Text content for this page
    #[php(prop, name = "content")]
    pub content: String,
    /// Tables found on this page (uses Arc for memory efficiency)
    ///
    /// Serializes as `Vec<Table>` for JSON compatibility while maintaining
    /// Arc semantics in-memory for zero-copy sharing.
    #[serde(default)]
    pub tables: Vec<Table>,
    /// Indices into `ExtractedDocument.images` for images found on this page.
    ///
    /// Each value is a zero-based index into the top-level `images` collection.
    /// Only populated when `extract_images = true` in the extraction config.
    #[php(prop, name = "imageIndices")]
    #[serde(alias = "imageIndices")]
    #[serde(default)]
    pub image_indices: Vec<u32>,
    /// OCR image preprocessing applied to this page's raster.
    #[serde(alias = "imagePreprocessing")]
    #[serde(default)]
    pub image_preprocessing: Option<ImagePreprocessingMetadata>,
    /// Hierarchy information for the page (when hierarchy extraction is enabled)
    ///
    /// Contains text hierarchy levels (H1-H6) extracted from the page content.
    pub hierarchy: Option<PageHierarchy>,
    /// Whether this page is blank (no meaningful text content)
    ///
    /// Determined during extraction based on text content analysis.
    /// A page is blank if it has fewer than 3 non-whitespace characters
    /// and contains no tables or images.
    #[php(prop, name = "isBlank")]
    #[serde(alias = "isBlank")]
    pub is_blank: Option<bool>,
    /// Layout detection regions for this page (when layout detection is enabled).
    ///
    /// Contains detected layout regions with class, confidence, bounding box,
    /// and area fraction. Only populated when layout detection is configured.
    #[serde(alias = "layoutRegions")]
    pub layout_regions: Option<Vec<LayoutRegion>>,
    /// Speaker notes for this slide (PPTX only).
    ///
    /// Contains the text from the slide's notes pane (`ppt/notesSlides/notesSlide{N}.xml`).
    /// Only populated when the source is a PPTX file and notes are present.
    #[php(prop, name = "speakerNotes")]
    #[serde(alias = "speakerNotes")]
    pub speaker_notes: Option<String>,
    /// Section name this slide belongs to (PPTX only).
    ///
    /// PowerPoint sections group slides into logical chapters (`<p:sectionLst>` in
    /// `ppt/presentation.xml`). Only populated when the source is a PPTX file and
    /// the slide belongs to a named section.
    #[php(prop, name = "sectionName")]
    #[serde(alias = "sectionName")]
    pub section_name: Option<String>,
    /// Sheet name for this page (XLSX/ODS only).
    ///
    /// Each spreadsheet sheet maps to one `PageContent` entry. This field carries the
    /// sheet's display name as it appears in the workbook. `None` for all non-spreadsheet
    /// formats and for sheets with an empty name.
    #[php(prop, name = "sheetName")]
    #[serde(alias = "sheetName")]
    pub sheet_name: Option<String>,
    /// Aggregate OCR confidence for this page. `None` when the page was not OCR'd.
    #[serde(alias = "ocrConfidence")]
    #[serde(default)]
    pub ocr_confidence: Option<PageOcrConfidence>,
}

#[php_impl]
impl PageContent {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        pageNumber: u32,
        content: String,
        tables: &ext_php_rs::types::ZendHashTable,
        imageIndices: Vec<u32>,
        imagePreprocessing: Option<&ImagePreprocessingMetadata>,
        hierarchy: Option<&PageHierarchy>,
        isBlank: Option<bool>,
        layoutRegions: Option<&ext_php_rs::types::ZendHashTable>,
        speakerNotes: Option<String>,
        sectionName: Option<String>,
        sheetName: Option<String>,
        ocrConfidence: Option<&PageOcrConfidence>,
    ) -> PhpResult<Self> {
        let mut tables_core_result: Vec<Table> = Vec::new();
        for (_, item) in tables.iter() {
            if let Some(parsed) = <&Table as ext_php_rs::convert::FromZval>::from_zval(item) {
                tables_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to Table".to_string(),
                ));
            }
        }
        let tables_core: Vec<Table> = tables_core_result;
        let layoutRegions_core: Option<Vec<LayoutRegion>> = if let Some(ht) = layoutRegions {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&LayoutRegion as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to LayoutRegion".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            page_number: pageNumber,
            content,
            tables: tables_core,
            image_indices: imageIndices,
            image_preprocessing: imagePreprocessing.cloned(),
            hierarchy: hierarchy.cloned(),
            is_blank: isBlank,
            layout_regions: layoutRegions_core,
            speaker_notes: speakerNotes,
            section_name: sectionName,
            sheet_name: sheetName,
            ocr_confidence: ocrConfidence.cloned(),
        })
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_tables(&self) -> Vec<Table> {
        self.tables.clone()
    }

    pub fn get_image_indices(&self) -> Vec<u32> {
        self.image_indices.clone()
    }

    pub fn get_image_preprocessing(&self) -> Option<ImagePreprocessingMetadata> {
        self.image_preprocessing.clone().map(Into::into)
    }

    pub fn set_image_preprocessing(&mut self, value: Option<&ImagePreprocessingMetadata>) {
        self.image_preprocessing = value.map(|value| value.clone().into());
    }

    pub fn get_hierarchy(&self) -> Option<PageHierarchy> {
        self.hierarchy.clone().map(Into::into)
    }

    pub fn set_hierarchy(&mut self, value: Option<&PageHierarchy>) {
        self.hierarchy = value.map(|value| value.clone().into());
    }

    pub fn get_is_blank(&self) -> Option<bool> {
        self.is_blank.clone()
    }

    pub fn get_layout_regions(&self) -> Option<Vec<LayoutRegion>> {
        self.layout_regions.clone()
    }

    pub fn get_speaker_notes(&self) -> Option<String> {
        self.speaker_notes.clone()
    }

    pub fn get_section_name(&self) -> Option<String> {
        self.section_name.clone()
    }

    pub fn get_sheet_name(&self) -> Option<String> {
        self.sheet_name.clone()
    }

    pub fn get_ocr_confidence(&self) -> Option<PageOcrConfidence> {
        self.ocr_confidence.clone().map(Into::into)
    }

    pub fn set_ocr_confidence(&mut self, value: Option<&PageOcrConfidence>) {
        self.ocr_confidence = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PageDimensions")]
#[serde(default)]
pub struct PageDimensions {
    /// Page width in points or pixels.
    #[php(prop, name = "width")]
    pub width: f64,
    /// Page height in points or pixels.
    #[php(prop, name = "height")]
    pub height: f64,
}

impl Default for PageDimensions {
    fn default() -> Self {
        <xberg::PageDimensions as Default>::default().into()
    }
}

#[php_impl]
impl PageDimensions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(width: f64, height: f64) -> Self {
        Self { width, height }
    }

    pub fn get_width(&self) -> f64 {
        self.width.clone()
    }

    pub fn get_height(&self) -> f64 {
        self.height.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageHierarchy")]
#[serde(default)]
pub struct PageHierarchy {
    /// Number of hierarchy blocks on this page
    #[php(prop, name = "blockCount")]
    #[serde(alias = "blockCount")]
    pub block_count: u32,
    /// Hierarchical blocks with heading levels
    #[serde(default)]
    pub blocks: Vec<HierarchicalBlock>,
}

#[php_impl]
impl PageHierarchy {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(blockCount: u32, blocks: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut blocks_core_result: Vec<HierarchicalBlock> = Vec::new();
        for (_, item) in blocks.iter() {
            if let Some(parsed) = <&HierarchicalBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                blocks_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to HierarchicalBlock".to_string(),
                ));
            }
        }
        let blocks_core: Vec<HierarchicalBlock> = blocks_core_result;
        Ok(Self {
            block_count: blockCount,
            blocks: blocks_core,
        })
    }

    pub fn get_block_count(&self) -> u32 {
        self.block_count.clone()
    }

    pub fn get_blocks(&self) -> Vec<HierarchicalBlock> {
        self.blocks.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageInfo")]
#[serde(default)]
pub struct PageInfo {
    /// Page number (1-indexed)
    #[php(prop, name = "number")]
    pub number: u32,
    /// Page title (usually for presentations)
    #[php(prop, name = "title")]
    pub title: Option<String>,
    /// Dimensions in points (PDF) or pixels (images).
    #[serde(default)]
    pub dimensions: Option<PageDimensions>,
    /// Number of images on this page
    #[php(prop, name = "imageCount")]
    #[serde(alias = "imageCount")]
    pub image_count: Option<u32>,
    /// Number of tables on this page
    #[php(prop, name = "tableCount")]
    #[serde(alias = "tableCount")]
    pub table_count: Option<u32>,
    /// Whether this page is hidden (e.g., in presentations)
    #[php(prop, name = "hidden")]
    pub hidden: Option<bool>,
    /// Whether this page is blank (no meaningful text, no images, no tables)
    ///
    /// A page is considered blank if it has fewer than 3 non-whitespace characters
    /// and contains no tables or images. This is useful for filtering out empty pages
    /// in scanned documents or PDFs with blank separator pages.
    #[php(prop, name = "isBlank")]
    #[serde(alias = "isBlank")]
    pub is_blank: Option<bool>,
    /// Whether this page contains non-trivial vector graphics (paths, shapes, curves)
    ///
    /// Indicates the presence of vector-drawn content such as charts, diagrams,
    /// or geometric shapes (e.g., from Adobe InDesign, LaTeX TikZ). These are
    /// invisible to `ExtractedDocument.images` since they are not embedded as raster
    /// XObjects. Set to `true` when path count exceeds a heuristic threshold,
    /// signaling that downstream consumers may want to rasterize the page to
    /// capture this content.
    ///
    /// Only populated for PDFs; `None` for other document types.
    #[php(prop, name = "hasVectorGraphics")]
    #[serde(alias = "hasVectorGraphics")]
    #[serde(default)]
    pub has_vector_graphics: bool,
}

#[php_impl]
impl PageInfo {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        number: u32,
        hasVectorGraphics: bool,
        title: Option<String>,
        dimensions: Option<&PageDimensions>,
        imageCount: Option<u32>,
        tableCount: Option<u32>,
        hidden: Option<bool>,
        isBlank: Option<bool>,
    ) -> Self {
        Self {
            number,
            title,
            dimensions: dimensions.cloned(),
            image_count: imageCount,
            table_count: tableCount,
            hidden,
            is_blank: isBlank,
            has_vector_graphics: hasVectorGraphics,
        }
    }

    pub fn get_number(&self) -> u32 {
        self.number.clone()
    }

    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    pub fn get_dimensions(&self) -> Option<PageDimensions> {
        self.dimensions.clone().map(Into::into)
    }

    pub fn set_dimensions(&mut self, value: Option<&PageDimensions>) {
        self.dimensions = value.map(|value| value.clone().into());
    }

    pub fn get_image_count(&self) -> Option<u32> {
        self.image_count.clone()
    }

    pub fn get_table_count(&self) -> Option<u32> {
        self.table_count.clone()
    }

    pub fn get_hidden(&self) -> Option<bool> {
        self.hidden.clone()
    }

    pub fn get_is_blank(&self) -> Option<bool> {
        self.is_blank.clone()
    }

    pub fn get_has_vector_graphics(&self) -> bool {
        self.has_vector_graphics.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PageOcrConfidence")]
#[serde(default)]
pub struct PageOcrConfidence {
    /// Aggregate legibility score in `0.0..=1.0`, or `None` when the backend that
    /// produced this page does not report a calibrated legibility scale.
    ///
    /// Backends differ in what their confidence numbers mean (see `OcrConfidence`'s
    /// per-backend normalization), and not every backend maps onto a 0.0-1.0 legibility
    /// scale at all. When a backend has no such calibrated scale, this is `None` rather
    /// than a misleading number, and scores must never be compared across backends.
    #[php(prop, name = "score")]
    pub score: Option<f64>,
    /// Number of words the score was averaged over, AFTER noise filtering.
    ///
    /// A small `word_count` means the average is based on little evidence, so a high
    /// `score` next to a small `word_count` is not representative of the whole page.
    #[php(prop, name = "wordCount")]
    #[serde(alias = "wordCount")]
    pub word_count: u32,
    /// Name of the OCR backend that produced the page text.
    #[php(prop, name = "backend")]
    pub backend: String,
}

impl Default for PageOcrConfidence {
    fn default() -> Self {
        <xberg::PageOcrConfidence as Default>::default().into()
    }
}

#[php_impl]
impl PageOcrConfidence {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(wordCount: u32, backend: String, score: Option<f64>) -> Self {
        Self {
            score,
            word_count: wordCount,
            backend,
        }
    }

    pub fn get_score(&self) -> Option<f64> {
        self.score.clone()
    }

    pub fn get_word_count(&self) -> u32 {
        self.word_count.clone()
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageRange")]
#[serde(default)]
pub struct PageRange {
    /// Start page (0-indexed, inclusive).
    #[php(prop, name = "start")]
    pub start: u32,
    /// End page (0-indexed, inclusive).
    #[php(prop, name = "end")]
    pub end: u32,
}

#[php_impl]
impl PageRange {
    #[php(constructor)]
    pub fn new(start: u32, end: u32) -> Self {
        Self { start, end }
    }

    pub fn get_start(&self) -> u32 {
        self.start.clone()
    }

    pub fn get_end(&self) -> u32 {
        self.end.clone()
    }

    pub fn page_count(&self) -> u32 {
        let core_self = xberg::PageRange {
            start: self.start,
            end: self.end,
        };
        core_self.page_count()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageSignals")]
#[serde(default)]
pub struct PageSignals {
    /// 1-indexed page number.
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// First ~500 characters of extracted text.
    #[php(prop, name = "textExcerpt")]
    #[serde(alias = "textExcerpt")]
    pub text_excerpt: String,
    /// `true` if page starts with letterhead-like content (ALL CAPS line in first 5 lines
    /// or a logo-image bbox at top).
    #[php(prop, name = "startsWithLetterheadLike")]
    #[serde(alias = "startsWithLetterheadLike")]
    pub starts_with_letterhead_like: bool,
    /// `true` if text contains "Page 1" or "1 of N" pattern.
    #[php(prop, name = "hasPageNumberOneMarker")]
    #[serde(alias = "hasPageNumberOneMarker")]
    pub has_page_number_one_marker: bool,
    /// `true` if text contains signature indicators ("Sincerely", "Signed") or
    /// a signature image bbox.
    #[php(prop, name = "hasSignatureBlock")]
    #[serde(alias = "hasSignatureBlock")]
    pub has_signature_block: bool,
    /// Text density: characters per page area, normalised to `[0.0, 1.0]`.
    #[php(prop, name = "layoutTextDensity")]
    #[serde(alias = "layoutTextDensity")]
    pub layout_text_density: f32,
}

#[php_impl]
impl PageSignals {
    #[php(constructor)]
    pub fn new(
        pageNumber: u32,
        textExcerpt: String,
        startsWithLetterheadLike: bool,
        hasPageNumberOneMarker: bool,
        hasSignatureBlock: bool,
        layoutTextDensity: f32,
    ) -> Self {
        Self {
            page_number: pageNumber,
            text_excerpt: textExcerpt,
            starts_with_letterhead_like: startsWithLetterheadLike,
            has_page_number_one_marker: hasPageNumberOneMarker,
            has_signature_block: hasSignatureBlock,
            layout_text_density: layoutTextDensity,
        }
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_text_excerpt(&self) -> String {
        self.text_excerpt.clone()
    }

    pub fn get_starts_with_letterhead_like(&self) -> bool {
        self.starts_with_letterhead_like.clone()
    }

    pub fn get_has_page_number_one_marker(&self) -> bool {
        self.has_page_number_one_marker.clone()
    }

    pub fn get_has_signature_block(&self) -> bool {
        self.has_signature_block.clone()
    }

    pub fn get_layout_text_density(&self) -> f32 {
        self.layout_text_density.clone()
    }

    /// Derive signals from raw page text.
    ///
    /// Callers that already have structured per-page data (e.g. from a PDF extractor)
    /// can set individual fields directly.  This constructor is for callers that only
    /// have the plain-text content of a page (e.g. from `PageContent`).
    ///
    /// # Arguments
    ///
    /// * `page_number` — 1-indexed page number.
    /// * `text` — Full extracted text for the page.
    /// * `layout_text_density` — Pre-computed text density in `[0.0, 1.0]`.  Pass `0.0`
    ///   when unknown (disables density-shift detection for this page).
    ///
    /// # Heuristics
    ///
    /// All signal derivations are *conservative starting points*.  Each is documented
    /// inline.  They err on the side of fewer false positives; tune thresholds via
    /// `MultidocThresholds` rather than by changing these heuristics.
    #[php(name = "fromPageText")]
    pub fn from_page_text(pageNumber: u32, text: String, layoutTextDensity: f32) -> PageSignals {
        xberg::PageSignals::from_page_text(pageNumber, &text, layoutTextDensity).into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageSpan")]
#[serde(default)]
pub struct PageSpan {
    /// Page number (1-indexed).
    #[php(prop, name = "page")]
    pub page: u32,
    /// Bounding box on this page, if known.
    pub bbox: Option<BoundingBox>,
}

#[php_impl]
impl PageSpan {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(page: u32, bbox: Option<&BoundingBox>) -> Self {
        Self {
            page,
            bbox: bbox.cloned(),
        }
    }

    pub fn get_page(&self) -> u32 {
        self.page.clone()
    }

    pub fn get_bbox(&self) -> Option<BoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&BoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PageStructure")]
#[serde(default)]
pub struct PageStructure {
    /// Total number of pages/slides/sheets
    #[php(prop, name = "totalCount")]
    #[serde(alias = "totalCount")]
    pub total_count: u32,
    /// Type of paginated unit
    #[php(prop, name = "unitType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "unitType")]
    pub unit_type: String,
    /// Character offset boundaries for each page
    ///
    /// Maps character ranges in the extracted content to page numbers.
    /// Used for chunk page range calculation.
    pub boundaries: Option<Vec<PageBoundary>>,
    /// Detailed per-page metadata (optional, only when needed)
    pub pages: Option<Vec<PageInfo>>,
}

#[php_impl]
impl PageStructure {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        totalCount: u32,
        unitType: String,
        boundaries: Option<&ext_php_rs::types::ZendHashTable>,
        pages: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let boundaries_core: Option<Vec<PageBoundary>> = if let Some(ht) = boundaries {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageBoundary as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageBoundary".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let pages_core: Option<Vec<PageInfo>> = if let Some(ht) = pages {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageInfo as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageInfo".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            total_count: totalCount,
            unit_type: unitType,
            boundaries: boundaries_core,
            pages: pages_core,
        })
    }

    pub fn get_total_count(&self) -> u32 {
        self.total_count.clone()
    }

    pub fn get_unit_type(&self) -> String {
        self.unit_type.clone()
    }

    pub fn get_boundaries(&self) -> Option<Vec<PageBoundary>> {
        self.boundaries.clone()
    }

    pub fn get_pages(&self) -> Option<Vec<PageInfo>> {
        self.pages.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PatternMatch")]
#[serde(default)]
pub struct PatternMatch {
    /// Inclusive byte-offset start of the match in the source text.
    #[php(prop, name = "start")]
    pub start: i64,
    /// Exclusive byte-offset end of the match.
    #[php(prop, name = "end")]
    pub end: i64,
    /// Category the match belongs to.
    #[php(prop, name = "category")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub category: String,
    /// Matched substring (owned copy — pattern engine returns owned data so the
    /// caller can free the original text if needed before replacement).
    #[php(prop, name = "text")]
    pub text: String,
}

#[php_impl]
impl PatternMatch {
    #[php(constructor)]
    pub fn new(start: i64, end: i64, category: String, text: String) -> Self {
        Self {
            start,
            end,
            category,
            text,
        }
    }

    pub fn get_start(&self) -> i64 {
        self.start.clone()
    }

    pub fn get_end(&self) -> i64 {
        self.end.clone()
    }

    pub fn get_category(&self) -> String {
        self.category.clone()
    }

    pub fn get_text(&self) -> String {
        self.text.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PdfAnnotation")]
#[serde(default)]
pub struct PdfAnnotation {
    /// The type of annotation.
    #[php(prop, name = "annotationType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "annotationType")]
    pub annotation_type: String,
    /// Text content of the annotation (e.g., comment text, link URL).
    #[php(prop, name = "content")]
    pub content: Option<String>,
    /// Page number where the annotation appears (1-indexed).
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Bounding box of the annotation on the page.
    #[serde(alias = "boundingBox")]
    pub bounding_box: Option<BoundingBox>,
    /// Author/creator of the annotation (PDF `/T` entry).
    #[php(prop, name = "author")]
    #[serde(default)]
    pub author: Option<String>,
    /// Last modification date of the annotation (PDF `/M` entry), as a raw
    /// PDF date string (e.g. `"D:20240115120000Z"`).
    #[php(prop, name = "modified")]
    #[serde(default)]
    pub modified: Option<String>,
    /// Annotation colour (PDF `/C` entry), normalised to a CSS-compatible
    /// `#rrggbb` hex string. Gray and CMYK colour spaces are converted to RGB.
    #[php(prop, name = "color")]
    #[serde(default)]
    pub color: Option<String>,
    /// Subject of the annotation (PDF `/Subj` entry).
    #[php(prop, name = "subject")]
    #[serde(default)]
    pub subject: Option<String>,
    /// Per-line bounding boxes derived from the annotation's `/QuadPoints`
    /// entry. Present for text markup annotations (Highlight, Underline,
    /// StrikeOut, Squiggly), one box per marked line/run of text.
    #[serde(alias = "quadPoints")]
    #[serde(default)]
    pub quad_points: Option<Vec<BoundingBox>>,
    /// The document text covered by `Self.quad_points`, recovered from the
    /// page content underneath the marked-up region. Populated for
    /// Highlight, Underline, StrikeOut, and Squiggly annotations when the
    /// underlying text could be recovered.
    #[php(prop, name = "markedText")]
    #[serde(alias = "markedText")]
    #[serde(default)]
    pub marked_text: Option<String>,
}

#[php_impl]
impl PdfAnnotation {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        annotationType: String,
        pageNumber: u32,
        content: Option<String>,
        boundingBox: Option<&BoundingBox>,
        author: Option<String>,
        modified: Option<String>,
        color: Option<String>,
        subject: Option<String>,
        quadPoints: Option<&ext_php_rs::types::ZendHashTable>,
        markedText: Option<String>,
    ) -> PhpResult<Self> {
        let quadPoints_core: Option<Vec<BoundingBox>> = if let Some(ht) = quadPoints {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&BoundingBox as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to BoundingBox".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            annotation_type: annotationType,
            content,
            page_number: pageNumber,
            bounding_box: boundingBox.cloned(),
            author,
            modified,
            color,
            subject,
            quad_points: quadPoints_core,
            marked_text: markedText,
        })
    }

    pub fn get_annotation_type(&self) -> String {
        self.annotation_type.clone()
    }

    pub fn get_content(&self) -> Option<String> {
        self.content.clone()
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_bounding_box(&self) -> Option<BoundingBox> {
        self.bounding_box.clone().map(Into::into)
    }

    pub fn set_bounding_box(&mut self, value: Option<&BoundingBox>) {
        self.bounding_box = value.map(|value| value.clone().into());
    }

    pub fn get_author(&self) -> Option<String> {
        self.author.clone()
    }

    pub fn get_modified(&self) -> Option<String> {
        self.modified.clone()
    }

    pub fn get_color(&self) -> Option<String> {
        self.color.clone()
    }

    pub fn get_subject(&self) -> Option<String> {
        self.subject.clone()
    }

    pub fn get_quad_points(&self) -> Option<Vec<BoundingBox>> {
        self.quad_points.clone()
    }

    pub fn get_marked_text(&self) -> Option<String> {
        self.marked_text.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PdfConfig")]
#[serde(default)]
pub struct PdfConfig {
    /// Extract images from PDF
    #[php(prop, name = "extractImages")]
    #[serde(default = "crate::serde_defaults::pdf_config_extract_images")]
    #[serde(alias = "extractImages")]
    pub extract_images: bool,
    /// Extract tables from PDF.
    ///
    /// When `true` (default), runs the native engine's grid detector and, if it
    /// finds nothing, falls back to the heuristic text-layer reconstruction in
    /// `pdf.native.table.extract_tables_heuristic`. Set to `false` to skip
    /// both passes — `tables` will then be empty in the result.
    #[php(prop, name = "extractTables")]
    #[serde(default = "crate::serde_defaults::pdf_config_extract_tables")]
    #[serde(alias = "extractTables")]
    pub extract_tables: bool,
    /// List of passwords to try when opening encrypted PDFs
    #[php(prop, name = "passwords")]
    #[serde(default)]
    pub passwords: Option<Vec<String>>,
    /// Extract PDF metadata
    #[php(prop, name = "extractMetadata")]
    #[serde(default = "crate::serde_defaults::pdf_config_extract_metadata")]
    #[serde(alias = "extractMetadata")]
    pub extract_metadata: bool,
    /// Hierarchy extraction configuration (undefined = hierarchy extraction disabled)
    #[serde(default)]
    pub hierarchy: Option<HierarchyConfig>,
    /// Extract PDF annotations (text notes, highlights, links, stamps).
    /// Default: false
    #[php(prop, name = "extractAnnotations")]
    #[serde(default = "crate::serde_defaults::pdf_config_extract_annotations")]
    #[serde(alias = "extractAnnotations")]
    pub extract_annotations: bool,
    /// Top margin fraction (0.0–1.0) of page height to exclude headers/running heads.
    /// Ignored when `ContentFilterConfig.include_headers` is `true`.
    /// Effective nonzero margins require per-page OCR so geometry can be filtered;
    /// document-capable OCR backends use their image-processing path in that case.
    /// Default: 0.0 (disabled; set explicitly to filter header content)
    #[php(prop, name = "topMarginFraction")]
    #[serde(alias = "topMarginFraction")]
    #[serde(default)]
    pub top_margin_fraction: Option<f32>,
    /// Bottom margin fraction (0.0–1.0) of page height to exclude footers/page numbers.
    /// Ignored when `ContentFilterConfig.include_footers` is `true`.
    /// Effective nonzero margins require per-page OCR so geometry can be filtered;
    /// document-capable OCR backends use their image-processing path in that case.
    /// Default: 0.0 (disabled; set explicitly to filter footer content)
    #[php(prop, name = "bottomMarginFraction")]
    #[serde(alias = "bottomMarginFraction")]
    #[serde(default)]
    pub bottom_margin_fraction: Option<f32>,
    /// Allow single-column pseudo tables in extraction results.
    ///
    /// By default, tables with fewer than 2 columns (layout-guided) or 3 columns
    /// (heuristic) are rejected. When `true`, the minimum column count is relaxed
    /// to 1, allowing single-column structured data (glossaries, itemized lists)
    /// to be emitted as tables. Other quality filters (density, sparsity, prose
    /// detection) still apply.
    #[php(prop, name = "allowSingleColumnTables")]
    #[serde(default = "crate::serde_defaults::pdf_config_allow_single_column_tables")]
    #[serde(alias = "allowSingleColumnTables")]
    pub allow_single_column_tables: bool,
    /// Perform OCR on inline images extracted from PDF pages and attach the
    /// recognized text to each `ExtractedImage.ocr_result`. Uses the backend
    /// selected by `ExtractionConfig.ocr`, or the default OCR backend when no
    /// OCR configuration is supplied. Requires the `ocr` or `ocr-pipeline`
    /// feature. Per-image failures degrade gracefully (the image is returned
    /// without OCR text rather than failing the whole extraction). Default:
    /// `false`.
    #[php(prop, name = "ocrInlineImages")]
    #[serde(default = "crate::serde_defaults::pdf_config_ocr_inline_images")]
    #[serde(alias = "ocrInlineImages")]
    pub ocr_inline_images: bool,
    /// Extract AcroForm and XFA form fields into `ExtractedDocument.form_fields`.
    ///
    /// When `true` (default), reads the document's interactive form structure
    /// (field names, types, values, widget geometry). Cheap and strictly
    /// additive — non-form PDFs simply yield an empty list. Set to `false` to
    /// skip the form pass entirely.
    #[php(prop, name = "extractFormFields")]
    #[serde(default = "crate::serde_defaults::pdf_config_extract_form_fields")]
    #[serde(alias = "extractFormFields")]
    pub extract_form_fields: bool,
    /// Reorder extracted text by layout-detected reading order.
    ///
    /// When `true`, projects text spans onto layout-detected regions, performs
    /// column detection, and emits spans in natural reading order (important
    /// for multi-column academic PDFs). It also repairs 90/180/270-degree
    /// rotated text runs — sideways tables and captions — that otherwise read
    /// word-reversed and glued (GH#1358); see
    /// `crate.extractors.pdf.reading_order` for the rotation-handling
    /// details and its limits. Requires the `layout-detection` feature and a
    /// page for which layout detection actually produces hints: a page with
    /// no detected regions falls back to the original, unrepaired extraction
    /// order even with this enabled. Independent of
    /// `LayoutStrategy`, which only
    /// controls whether layout detection runs at all — enabling `Always` or
    /// `Auto` alone does not turn reordering on. Defaults to `false`.
    #[php(prop, name = "readingOrder")]
    #[serde(default = "crate::serde_defaults::pdf_config_reading_order")]
    #[serde(alias = "readingOrder")]
    pub reading_order: bool,
    /// Which engine parses and renders this PDF.
    ///
    /// Defaults to `PdfBackend.Native`. Selecting `PdfBackend.Pdfium`
    /// requires the `pdf-pdfium` feature and is rejected otherwise; the
    /// pdfium engine is also deliberately narrower in scope than `Native` --
    /// see `PdfBackend` and `extractors.pdf.pdfium_engine` for details.
    #[php(prop, name = "backend")]
    #[serde(default = "crate::serde_defaults::pdf_config_backend")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub backend: String,
}

impl Default for PdfConfig {
    fn default() -> Self {
        <xberg::PdfConfig as Default>::default().into()
    }
}

#[php_impl]
impl PdfConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        extractImages: bool,
        extractTables: bool,
        extractMetadata: bool,
        extractAnnotations: bool,
        allowSingleColumnTables: bool,
        ocrInlineImages: bool,
        extractFormFields: bool,
        readingOrder: bool,
        backend: String,
        passwords: Option<Vec<String>>,
        hierarchy: Option<&HierarchyConfig>,
        topMarginFraction: Option<f32>,
        bottomMarginFraction: Option<f32>,
    ) -> Self {
        Self {
            extract_images: extractImages,
            extract_tables: extractTables,
            passwords,
            extract_metadata: extractMetadata,
            hierarchy: hierarchy.cloned(),
            extract_annotations: extractAnnotations,
            top_margin_fraction: topMarginFraction,
            bottom_margin_fraction: bottomMarginFraction,
            allow_single_column_tables: allowSingleColumnTables,
            ocr_inline_images: ocrInlineImages,
            extract_form_fields: extractFormFields,
            reading_order: readingOrder,
            backend,
        }
    }

    pub fn get_extract_images(&self) -> bool {
        self.extract_images.clone()
    }

    pub fn get_extract_tables(&self) -> bool {
        self.extract_tables.clone()
    }

    pub fn get_passwords(&self) -> Option<Vec<String>> {
        self.passwords.clone()
    }

    pub fn get_extract_metadata(&self) -> bool {
        self.extract_metadata.clone()
    }

    pub fn get_hierarchy(&self) -> Option<HierarchyConfig> {
        self.hierarchy.clone().map(Into::into)
    }

    pub fn set_hierarchy(&mut self, value: Option<&HierarchyConfig>) {
        self.hierarchy = value.map(|value| value.clone().into());
    }

    pub fn get_extract_annotations(&self) -> bool {
        self.extract_annotations.clone()
    }

    pub fn get_top_margin_fraction(&self) -> Option<f32> {
        self.top_margin_fraction.clone()
    }

    pub fn get_bottom_margin_fraction(&self) -> Option<f32> {
        self.bottom_margin_fraction.clone()
    }

    pub fn get_allow_single_column_tables(&self) -> bool {
        self.allow_single_column_tables.clone()
    }

    pub fn get_ocr_inline_images(&self) -> bool {
        self.ocr_inline_images.clone()
    }

    pub fn get_extract_form_fields(&self) -> bool {
        self.extract_form_fields.clone()
    }

    pub fn get_reading_order(&self) -> bool {
        self.reading_order.clone()
    }

    pub fn get_backend(&self) -> String {
        self.backend.clone()
    }

    pub fn validate(&self) -> PhpResult<()> {
        #[allow(clippy::needless_update)]
        let core_self = xberg::PdfConfig {
            extract_images: self.extract_images,
            extract_tables: self.extract_tables,
            passwords: self.passwords.clone(),
            extract_metadata: self.extract_metadata,
            hierarchy: self.hierarchy.clone().map(Into::into),
            extract_annotations: self.extract_annotations,
            top_margin_fraction: self.top_margin_fraction,
            bottom_margin_fraction: self.bottom_margin_fraction,
            allow_single_column_tables: self.allow_single_column_tables,
            ocr_inline_images: self.ocr_inline_images,
            extract_form_fields: self.extract_form_fields,
            reading_order: self.reading_order,
            backend: match self.backend.as_str() {
                "native" => xberg::PdfBackend::Native,
                "pdfium" => xberg::PdfBackend::Pdfium,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
                        "invalid value {other:?} for field 'backend' of PdfBackend: expected one of [native, pdfium]"
                    ))
                    .throw()
                    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            ..Default::default()
        };
        let result = core_self
            .validate()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> PdfConfig {
        xberg::PdfConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PdfFormField")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct PdfFormField {
    /// Partial field name (the leaf name within the field hierarchy).
    #[php(prop, name = "name")]
    pub name: String,
    /// Fully-qualified field name (dotted path from the form root).
    #[php(prop, name = "fullName")]
    #[serde(alias = "fullName")]
    pub full_name: String,
    /// Classified field type.
    #[php(prop, name = "fieldType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "fieldType")]
    pub field_type: String,
    /// Current field value, if any.
    #[php(prop, name = "value")]
    #[serde(default)]
    pub value: Option<String>,
    /// Default field value, if any.
    #[php(prop, name = "defaultValue")]
    #[serde(alias = "defaultValue")]
    #[serde(default)]
    pub default_value: Option<String>,
    /// Raw field-flags bitmask (read-only, required, multiline, …).
    #[php(prop, name = "flags")]
    #[serde(default)]
    pub flags: u32,
    /// 1-indexed page the field's widget appears on. Currently always `None` for
    /// AcroForm fields; page assignment is a deferred enhancement requiring spatial
    /// analysis of widget annotations per page.
    #[php(prop, name = "page")]
    #[serde(default)]
    pub page: Option<u32>,
    /// Widget bounding box on its page, if known.
    #[serde(default)]
    pub bbox: Option<BoundingBox>,
    /// Maximum input length for text fields, if specified.
    #[php(prop, name = "maxLength")]
    #[serde(alias = "maxLength")]
    #[serde(default)]
    pub max_length: Option<u32>,
    /// Tooltip / alternate field description, if present.
    #[php(prop, name = "tooltip")]
    #[serde(default)]
    pub tooltip: Option<String>,
}

#[php_impl]
impl PdfFormField {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        name: String,
        fullName: String,
        fieldType: String,
        flags: u32,
        value: Option<String>,
        defaultValue: Option<String>,
        page: Option<u32>,
        bbox: Option<&BoundingBox>,
        maxLength: Option<u32>,
        tooltip: Option<String>,
    ) -> Self {
        Self {
            name,
            full_name: fullName,
            field_type: fieldType,
            value,
            default_value: defaultValue,
            flags,
            page,
            bbox: bbox.cloned(),
            max_length: maxLength,
            tooltip,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_full_name(&self) -> String {
        self.full_name.clone()
    }

    pub fn get_field_type(&self) -> String {
        self.field_type.clone()
    }

    pub fn get_value(&self) -> Option<String> {
        self.value.clone()
    }

    pub fn get_default_value(&self) -> Option<String> {
        self.default_value.clone()
    }

    pub fn get_flags(&self) -> u32 {
        self.flags.clone()
    }

    pub fn get_page(&self) -> Option<u32> {
        self.page.clone()
    }

    pub fn get_bbox(&self) -> Option<BoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&BoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }

    pub fn get_max_length(&self) -> Option<u32> {
        self.max_length.clone()
    }

    pub fn get_tooltip(&self) -> Option<String> {
        self.tooltip.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PdfMetadata")]
#[serde(default)]
pub struct PdfMetadata {
    /// PDF version (e.g., "1.7", "2.0")
    #[php(prop, name = "pdfVersion")]
    #[serde(alias = "pdfVersion")]
    pub pdf_version: Option<String>,
    /// PDF producer (application that created the PDF)
    #[php(prop, name = "producer")]
    pub producer: Option<String>,
    /// Whether the PDF is encrypted/password-protected
    #[php(prop, name = "isEncrypted")]
    #[serde(alias = "isEncrypted")]
    pub is_encrypted: Option<bool>,
    /// First page width in points (1/72 inch)
    #[php(prop, name = "width")]
    pub width: Option<i64>,
    /// First page height in points (1/72 inch)
    #[php(prop, name = "height")]
    pub height: Option<i64>,
    /// Total number of pages in the PDF document
    #[php(prop, name = "pageCount")]
    #[serde(alias = "pageCount")]
    pub page_count: Option<u32>,
    /// How strongly the document's most scan-like page resembles a scan, in `[0.0, 1.0]`.
    ///
    /// `None` when the document could not be inspected. A full-page raster with no
    /// visible text scores at least `0.85`; a born-digital slide with a full-bleed
    /// background image scores `0.50`.
    #[php(prop, name = "scannedConfidence")]
    #[serde(alias = "scannedConfidence")]
    pub scanned_confidence: Option<f32>,
    /// Pages that look like scans (1-indexed), using the default confidence threshold.
    ///
    /// `None` when the document could not be inspected; empty when no page qualifies.
    #[php(prop, name = "scannedPages")]
    #[serde(alias = "scannedPages")]
    pub scanned_pages: Option<Vec<u32>>,
    /// Pages the `auto` layout strategy skipped (1-indexed).
    ///
    /// `None` unless layout detection ran with `LayoutStrategy.Auto`; empty
    /// when the gate selected every page.
    #[php(prop, name = "layoutGatedPages")]
    #[serde(alias = "layoutGatedPages")]
    pub layout_gated_pages: Option<Vec<u32>>,
    /// Why the `auto` layout gate selected or skipped each page.
    ///
    /// Index `i` is page `i + 1`. Snake_case values such as `multi_column`,
    /// `table_grid`, or `plain_text` (the skip reason). `None` unless layout
    /// detection ran with `LayoutStrategy.Auto`.
    #[php(prop, name = "layoutGateReasons")]
    #[serde(alias = "layoutGateReasons")]
    pub layout_gate_reasons: Option<Vec<String>>,
}

impl Default for PdfMetadata {
    fn default() -> Self {
        <xberg::pdf::metadata::PdfMetadata as Default>::default().into()
    }
}

#[php_impl]
impl PdfMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_pdf_version(&self) -> Option<String> {
        self.pdf_version.clone()
    }

    pub fn get_producer(&self) -> Option<String> {
        self.producer.clone()
    }

    pub fn get_is_encrypted(&self) -> Option<bool> {
        self.is_encrypted.clone()
    }

    pub fn get_width(&self) -> Option<i64> {
        self.width.clone()
    }

    pub fn get_height(&self) -> Option<i64> {
        self.height.clone()
    }

    pub fn get_page_count(&self) -> Option<u32> {
        self.page_count.clone()
    }

    pub fn get_scanned_confidence(&self) -> Option<f32> {
        self.scanned_confidence.clone()
    }

    pub fn get_scanned_pages(&self) -> Option<Vec<u32>> {
        self.scanned_pages.clone()
    }

    pub fn get_layout_gated_pages(&self) -> Option<Vec<u32>> {
        self.layout_gated_pages.clone()
    }

    pub fn get_layout_gate_reasons(&self) -> Option<Vec<String>> {
        self.layout_gate_reasons.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PixelDimensions")]
#[serde(default)]
pub struct PixelDimensions {
    /// Width in pixels.
    #[php(prop, name = "width")]
    pub width: i64,
    /// Height in pixels.
    #[php(prop, name = "height")]
    pub height: i64,
}

impl Default for PixelDimensions {
    fn default() -> Self {
        <xberg::PixelDimensions as Default>::default().into()
    }
}

#[php_impl]
impl PixelDimensions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(width: i64, height: i64) -> Self {
        Self { width, height }
    }

    pub fn get_width(&self) -> i64 {
        self.width.clone()
    }

    pub fn get_height(&self) -> i64 {
        self.height.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PostProcessorConfig")]
#[serde(default)]
pub struct PostProcessorConfig {
    /// Enable post-processors
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::post_processor_config_enabled")]
    pub enabled: bool,
    /// Whitelist of processor names to run (undefined = all enabled)
    #[php(prop, name = "enabledProcessors")]
    #[serde(alias = "enabledProcessors")]
    #[serde(default)]
    pub enabled_processors: Option<Vec<String>>,
    /// Blacklist of processor names to skip (undefined = none disabled)
    #[php(prop, name = "disabledProcessors")]
    #[serde(alias = "disabledProcessors")]
    #[serde(default)]
    pub disabled_processors: Option<Vec<String>>,
    /// Pre-computed AHashSet for O(1) enabled processor lookup
    #[php(prop, name = "enabledSet")]
    #[serde(alias = "enabledSet")]
    pub enabled_set: Option<Vec<String>>,
    /// Pre-computed AHashSet for O(1) disabled processor lookup
    #[php(prop, name = "disabledSet")]
    #[serde(alias = "disabledSet")]
    pub disabled_set: Option<Vec<String>>,
}

impl Default for PostProcessorConfig {
    fn default() -> Self {
        <xberg::PostProcessorConfig as Default>::default().into()
    }
}

#[php_impl]
impl PostProcessorConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        enabled: bool,
        enabledProcessors: Option<Vec<String>>,
        disabledProcessors: Option<Vec<String>>,
        enabledSet: Option<Vec<String>>,
        disabledSet: Option<Vec<String>>,
    ) -> Self {
        Self {
            enabled,
            enabled_processors: enabledProcessors,
            disabled_processors: disabledProcessors,
            enabled_set: enabledSet,
            disabled_set: disabledSet,
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_enabled_processors(&self) -> Option<Vec<String>> {
        self.enabled_processors.clone()
    }

    pub fn get_disabled_processors(&self) -> Option<Vec<String>> {
        self.disabled_processors.clone()
    }

    pub fn get_enabled_set(&self) -> Option<Vec<String>> {
        self.enabled_set.clone()
    }

    pub fn get_disabled_set(&self) -> Option<Vec<String>> {
        self.disabled_set.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> PostProcessorConfig {
        xberg::PostProcessorConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PptxAppProperties")]
#[serde(default)]
pub struct PptxAppProperties {
    /// Application name (e.g., "Microsoft Office PowerPoint")
    #[php(prop, name = "application")]
    pub application: Option<String>,
    /// Application version
    #[php(prop, name = "appVersion")]
    #[serde(alias = "appVersion")]
    pub app_version: Option<String>,
    /// Total editing time in minutes
    #[php(prop, name = "totalTime")]
    #[serde(alias = "totalTime")]
    pub total_time: Option<i32>,
    /// Company name
    #[php(prop, name = "company")]
    pub company: Option<String>,
    /// Document security level
    #[php(prop, name = "docSecurity")]
    #[serde(alias = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[php(prop, name = "scaleCrop")]
    #[serde(alias = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[php(prop, name = "linksUpToDate")]
    #[serde(alias = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[php(prop, name = "sharedDoc")]
    #[serde(alias = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[php(prop, name = "hyperlinksChanged")]
    #[serde(alias = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
    /// Number of slides
    #[php(prop, name = "slides")]
    pub slides: Option<i32>,
    /// Number of notes
    #[php(prop, name = "notes")]
    pub notes: Option<i32>,
    /// Number of hidden slides
    #[php(prop, name = "hiddenSlides")]
    #[serde(alias = "hiddenSlides")]
    pub hidden_slides: Option<i32>,
    /// Number of multimedia clips
    #[php(prop, name = "multimediaClips")]
    #[serde(alias = "multimediaClips")]
    pub multimedia_clips: Option<i32>,
    /// Presentation format (e.g., "Widescreen", "Standard")
    #[php(prop, name = "presentationFormat")]
    #[serde(alias = "presentationFormat")]
    pub presentation_format: Option<String>,
    /// Slide titles
    #[php(prop, name = "slideTitles")]
    #[serde(alias = "slideTitles")]
    pub slide_titles: Vec<String>,
}

impl Default for PptxAppProperties {
    fn default() -> Self {
        <xberg::extraction::office_metadata::app_properties::PptxAppProperties as Default>::default().into()
    }
}

#[php_impl]
impl PptxAppProperties {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        slideTitles: Vec<String>,
        application: Option<String>,
        appVersion: Option<String>,
        totalTime: Option<i32>,
        company: Option<String>,
        docSecurity: Option<i32>,
        scaleCrop: Option<bool>,
        linksUpToDate: Option<bool>,
        sharedDoc: Option<bool>,
        hyperlinksChanged: Option<bool>,
        slides: Option<i32>,
        notes: Option<i32>,
        hiddenSlides: Option<i32>,
        multimediaClips: Option<i32>,
        presentationFormat: Option<String>,
    ) -> Self {
        Self {
            application,
            app_version: appVersion,
            total_time: totalTime,
            company,
            doc_security: docSecurity,
            scale_crop: scaleCrop,
            links_up_to_date: linksUpToDate,
            shared_doc: sharedDoc,
            hyperlinks_changed: hyperlinksChanged,
            slides,
            notes,
            hidden_slides: hiddenSlides,
            multimedia_clips: multimediaClips,
            presentation_format: presentationFormat,
            slide_titles: slideTitles,
        }
    }

    pub fn get_application(&self) -> Option<String> {
        self.application.clone()
    }

    pub fn get_app_version(&self) -> Option<String> {
        self.app_version.clone()
    }

    pub fn get_total_time(&self) -> Option<i32> {
        self.total_time.clone()
    }

    pub fn get_company(&self) -> Option<String> {
        self.company.clone()
    }

    pub fn get_doc_security(&self) -> Option<i32> {
        self.doc_security.clone()
    }

    pub fn get_scale_crop(&self) -> Option<bool> {
        self.scale_crop.clone()
    }

    pub fn get_links_up_to_date(&self) -> Option<bool> {
        self.links_up_to_date.clone()
    }

    pub fn get_shared_doc(&self) -> Option<bool> {
        self.shared_doc.clone()
    }

    pub fn get_hyperlinks_changed(&self) -> Option<bool> {
        self.hyperlinks_changed.clone()
    }

    pub fn get_slides(&self) -> Option<i32> {
        self.slides.clone()
    }

    pub fn get_notes(&self) -> Option<i32> {
        self.notes.clone()
    }

    pub fn get_hidden_slides(&self) -> Option<i32> {
        self.hidden_slides.clone()
    }

    pub fn get_multimedia_clips(&self) -> Option<i32> {
        self.multimedia_clips.clone()
    }

    pub fn get_presentation_format(&self) -> Option<String> {
        self.presentation_format.clone()
    }

    pub fn get_slide_titles(&self) -> Vec<String> {
        self.slide_titles.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PptxExtractionResult")]
#[serde(default)]
pub struct PptxExtractionResult {
    /// Extracted text content from all slides
    #[php(prop, name = "content")]
    pub content: String,
    /// Presentation metadata
    pub metadata: PptxMetadata,
    /// Total number of slides
    #[php(prop, name = "slideCount")]
    #[serde(alias = "slideCount")]
    pub slide_count: i64,
    /// Total number of embedded images
    #[php(prop, name = "imageCount")]
    #[serde(alias = "imageCount")]
    pub image_count: i64,
    /// Total number of tables
    #[php(prop, name = "tableCount")]
    #[serde(alias = "tableCount")]
    pub table_count: i64,
    /// Extracted images from the presentation
    pub images: Vec<ExtractedImage>,
    /// Slide structure with boundaries (when page tracking is enabled)
    #[serde(alias = "pageStructure")]
    pub page_structure: Option<PageStructure>,
    /// Per-slide content (when page tracking is enabled)
    #[serde(alias = "pageContents")]
    pub page_contents: Option<Vec<PageContent>>,
    /// Structured document representation
    pub document: Option<DocumentStructure>,
    /// Hyperlinks discovered in slides.
    #[serde(default)]
    pub hyperlinks: Vec<PresentationHyperlink>,
    /// Office metadata extracted from docProps/core.xml and docProps/app.xml.
    ///
    /// Contains keys like "title", "author", "created_by", "subject", "keywords",
    /// "modified_by", "created_at", "modified_at", etc.
    #[php(prop, name = "officeMetadata")]
    #[serde(alias = "officeMetadata")]
    #[serde(default)]
    pub office_metadata: HashMap<String, String>,
    /// Slide comments as revisions.
    ///
    /// Each `<p:cm>` element in `ppt/comments/comment{N}.xml` becomes a
    /// `DocumentRevision { kind: Comment }` with author (resolved from
    /// `ppt/commentAuthors.xml`), ISO-8601 timestamp, and
    /// `RevisionAnchor.Slide { index }`. `None` when no comment XML parts exist.
    #[serde(default)]
    pub revisions: Option<Vec<DocumentRevision>>,
}

#[php_impl]
impl PptxExtractionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        metadata: &PptxMetadata,
        slideCount: i64,
        imageCount: i64,
        tableCount: i64,
        images: &ext_php_rs::types::ZendHashTable,
        hyperlinks: &ext_php_rs::types::ZendHashTable,
        officeMetadata: HashMap<String, String>,
        pageStructure: Option<&PageStructure>,
        pageContents: Option<&ext_php_rs::types::ZendHashTable>,
        document: Option<&DocumentStructure>,
        revisions: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let mut images_core_result: Vec<ExtractedImage> = Vec::new();
        for (_, item) in images.iter() {
            if let Some(parsed) = <&ExtractedImage as ext_php_rs::convert::FromZval>::from_zval(item) {
                images_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ExtractedImage".to_string(),
                ));
            }
        }
        let images_core: Vec<ExtractedImage> = images_core_result;
        let pageContents_core: Option<Vec<PageContent>> = if let Some(ht) = pageContents {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageContent as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageContent".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let mut hyperlinks_core_result: Vec<PresentationHyperlink> = Vec::new();
        for (_, item) in hyperlinks.iter() {
            if let Some(parsed) = <&PresentationHyperlink as ext_php_rs::convert::FromZval>::from_zval(item) {
                hyperlinks_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to PresentationHyperlink".to_string(),
                ));
            }
        }
        let hyperlinks_core: Vec<PresentationHyperlink> = hyperlinks_core_result;
        let revisions_core: Option<Vec<DocumentRevision>> = if let Some(ht) = revisions {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&DocumentRevision as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to DocumentRevision".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            content,
            metadata: metadata.clone(),
            slide_count: slideCount,
            image_count: imageCount,
            table_count: tableCount,
            images: images_core,
            page_structure: pageStructure.cloned(),
            page_contents: pageContents_core,
            document: document.cloned(),
            hyperlinks: hyperlinks_core,
            office_metadata: officeMetadata,
            revisions: revisions_core,
        })
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_metadata(&self) -> PptxMetadata {
        self.metadata.clone()
    }

    pub fn get_slide_count(&self) -> i64 {
        self.slide_count.clone()
    }

    pub fn get_image_count(&self) -> i64 {
        self.image_count.clone()
    }

    pub fn get_table_count(&self) -> i64 {
        self.table_count.clone()
    }

    pub fn get_images(&self) -> Vec<ExtractedImage> {
        self.images.clone()
    }

    pub fn get_page_structure(&self) -> Option<PageStructure> {
        self.page_structure.clone().map(Into::into)
    }

    pub fn set_page_structure(&mut self, value: Option<&PageStructure>) {
        self.page_structure = value.map(|value| value.clone().into());
    }

    pub fn get_page_contents(&self) -> Option<Vec<PageContent>> {
        self.page_contents.clone()
    }

    pub fn get_document(&self) -> Option<DocumentStructure> {
        self.document.clone().map(Into::into)
    }

    pub fn set_document(&mut self, value: Option<&DocumentStructure>) {
        self.document = value.map(|value| value.clone().into());
    }

    pub fn get_hyperlinks(&self) -> Vec<PresentationHyperlink> {
        self.hyperlinks.clone()
    }

    pub fn get_office_metadata(&self) -> HashMap<String, String> {
        self.office_metadata.clone()
    }

    pub fn get_revisions(&self) -> Option<Vec<DocumentRevision>> {
        self.revisions.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PptxMetadata")]
#[serde(default)]
pub struct PptxMetadata {
    /// Total number of slides in the presentation
    #[php(prop, name = "slideCount")]
    #[serde(alias = "slideCount")]
    pub slide_count: u32,
    /// Names of slides (if available)
    #[php(prop, name = "slideNames")]
    #[serde(alias = "slideNames")]
    pub slide_names: Vec<String>,
    /// Number of embedded images
    #[php(prop, name = "imageCount")]
    #[serde(alias = "imageCount")]
    pub image_count: Option<u32>,
    /// Number of tables
    #[php(prop, name = "tableCount")]
    #[serde(alias = "tableCount")]
    pub table_count: Option<u32>,
}

impl Default for PptxMetadata {
    fn default() -> Self {
        <xberg::PptxMetadata as Default>::default().into()
    }
}

#[php_impl]
impl PptxMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(slideCount: u32, slideNames: Vec<String>, imageCount: Option<u32>, tableCount: Option<u32>) -> Self {
        Self {
            slide_count: slideCount,
            slide_names: slideNames,
            image_count: imageCount,
            table_count: tableCount,
        }
    }

    pub fn get_slide_count(&self) -> u32 {
        self.slide_count.clone()
    }

    pub fn get_slide_names(&self) -> Vec<String> {
        self.slide_names.clone()
    }

    pub fn get_image_count(&self) -> Option<u32> {
        self.image_count.clone()
    }

    pub fn get_table_count(&self) -> Option<u32> {
        self.table_count.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PreprocessingOptions")]
#[serde(default)]
pub struct PreprocessingOptions {
    /// Enable HTML preprocessing globally
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::preprocessing_options_enabled")]
    pub enabled: bool,
    /// Preprocessing preset level (Minimal, Standard, Aggressive)
    #[php(prop, name = "preset")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub preset: String,
    /// Remove navigation elements (nav, breadcrumbs, menus, sidebars)
    #[php(prop, name = "removeNavigation")]
    #[serde(default = "crate::serde_defaults::preprocessing_options_remove_navigation")]
    #[serde(alias = "removeNavigation")]
    pub remove_navigation: bool,
    /// Remove form elements (forms, inputs, buttons, etc.)
    #[php(prop, name = "removeForms")]
    #[serde(default = "crate::serde_defaults::preprocessing_options_remove_forms")]
    #[serde(alias = "removeForms")]
    pub remove_forms: bool,
}

impl Default for PreprocessingOptions {
    fn default() -> Self {
        <html_to_markdown_rs::PreprocessingOptions as Default>::default().into()
    }
}

#[php_impl]
impl PreprocessingOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(enabled: bool, preset: String, removeNavigation: bool, removeForms: bool) -> Self {
        Self {
            enabled,
            preset,
            remove_navigation: removeNavigation,
            remove_forms: removeForms,
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_preset(&self) -> String {
        self.preset.clone()
    }

    pub fn get_remove_navigation(&self) -> bool {
        self.remove_navigation.clone()
    }

    pub fn get_remove_forms(&self) -> bool {
        self.remove_forms.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PresentationHyperlink")]
#[serde(default)]
pub struct PresentationHyperlink {
    /// Link destination.
    #[php(prop, name = "url")]
    pub url: String,
    /// Optional visible label.
    #[php(prop, name = "label")]
    pub label: Option<String>,
}

impl Default for PresentationHyperlink {
    fn default() -> Self {
        <xberg::PresentationHyperlink as Default>::default().into()
    }
}

#[php_impl]
impl PresentationHyperlink {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(url: String, label: Option<String>) -> Self {
        Self { url, label }
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }

    pub fn get_label(&self) -> Option<String> {
        self.label.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Preset")]
#[serde(default)]
pub struct Preset {
    /// Stable, URL-safe preset identifier (lowercase snake_case).
    #[php(prop, name = "id")]
    pub id: String,
    /// Monotonic version string (e.g. `v1`).
    #[php(prop, name = "version")]
    pub version: String,
    /// Human-readable schema name forwarded to the LLM as the response/tool name.
    #[php(prop, name = "schemaName")]
    #[serde(alias = "schemaName")]
    pub schema_name: String,
    /// One-line preset description shown in the registry UI.
    #[php(prop, name = "description")]
    pub description: String,
    /// Top-level category for grouping in the playground.
    #[php(prop, name = "category")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub category: String,
    /// Free-form tags used for search/filtering. May be empty.
    #[php(prop, name = "tags")]
    #[serde(default)]
    pub tags: Vec<String>,
    /// JSON Schema (Draft 2020-12) describing the structured output shape.
    pub schema: serde_json::Value,
    /// Instruction primer sent to the model.
    #[php(prop, name = "systemPrompt")]
    #[serde(alias = "systemPrompt")]
    pub system_prompt: String,
    /// Optional mustache-style template merged with caller-supplied context.
    #[php(prop, name = "contextTemplate")]
    #[serde(alias = "contextTemplate")]
    #[serde(default)]
    pub context_template: Option<String>,
    /// Strategy for merging per-batch outputs across paginated calls.
    #[php(prop, name = "mergeMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "mergeMode")]
    pub merge_mode: String,
    /// Default call mode suggested for this preset; heuristics may override.
    #[php(prop, name = "preferredCallMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "preferredCallMode")]
    pub preferred_call_mode: String,
    /// When true, the prompt asks the model to wrap each field as
    /// `{value, page, bbox, confidence}` for downstream citation overlays.
    #[php(prop, name = "emitCitations")]
    #[serde(alias = "emitCitations")]
    pub emit_citations: bool,
    /// Optional bundled sample (input file + reference output) for preview.
    #[serde(default)]
    pub sample: Option<PresetSample>,
    /// Stable sha256 fingerprint of the canonical preset file contents.
    ///
    /// Populated at registry load — not present in the on-disk JSON files.
    /// Used as a cache-invalidation token by the worker pipeline.
    #[php(prop, name = "fingerprint")]
    #[serde(default)]
    pub fingerprint: String,
}

#[php_impl]
impl Preset {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        id: String,
        version: String,
        schemaName: String,
        description: String,
        category: String,
        tags: Vec<String>,
        schema: String,
        systemPrompt: String,
        mergeMode: String,
        preferredCallMode: String,
        emitCitations: bool,
        fingerprint: String,
        contextTemplate: Option<String>,
        sample: Option<&PresetSample>,
    ) -> PhpResult<Self> {
        Ok(Self {
            id,
            version,
            schema_name: schemaName,
            description,
            category,
            tags,
            schema: serde_json::from_str(&schema).map_err(|e| PhpException::default(e.to_string()))?,
            system_prompt: systemPrompt,
            context_template: contextTemplate,
            merge_mode: mergeMode,
            preferred_call_mode: preferredCallMode,
            emit_citations: emitCitations,
            sample: sample.cloned(),
            fingerprint,
        })
    }

    pub fn get_id(&self) -> String {
        self.id.clone()
    }

    pub fn get_version(&self) -> String {
        self.version.clone()
    }

    pub fn get_schema_name(&self) -> String {
        self.schema_name.clone()
    }

    pub fn get_description(&self) -> String {
        self.description.clone()
    }

    pub fn get_category(&self) -> String {
        self.category.clone()
    }

    pub fn get_tags(&self) -> Vec<String> {
        self.tags.clone()
    }

    pub fn get_schema(&self) -> Option<String> {
        serde_json::to_string(&self.schema).ok()
    }

    pub fn get_system_prompt(&self) -> String {
        self.system_prompt.clone()
    }

    pub fn get_context_template(&self) -> Option<String> {
        self.context_template.clone()
    }

    pub fn get_merge_mode(&self) -> String {
        self.merge_mode.clone()
    }

    pub fn get_preferred_call_mode(&self) -> String {
        self.preferred_call_mode.clone()
    }

    pub fn get_emit_citations(&self) -> bool {
        self.emit_citations.clone()
    }

    pub fn get_sample(&self) -> Option<PresetSample> {
        self.sample.clone().map(Into::into)
    }

    pub fn set_sample(&mut self, value: Option<&PresetSample>) {
        self.sample = value.map(|value| value.clone().into());
    }

    pub fn get_fingerprint(&self) -> String {
        self.fingerprint.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PresetSample")]
#[serde(default)]
pub struct PresetSample {
    /// Path to the sample input file, relative to the preset directory.
    #[php(prop, name = "inputPath")]
    #[serde(alias = "inputPath")]
    pub input_path: String,
    /// Path to the reference structured output, relative to the preset directory.
    #[php(prop, name = "outputPath")]
    #[serde(alias = "outputPath")]
    pub output_path: String,
}

#[php_impl]
impl PresetSample {
    #[php(constructor)]
    pub fn new(inputPath: String, outputPath: String) -> Self {
        Self {
            input_path: inputPath,
            output_path: outputPath,
        }
    }

    pub fn get_input_path(&self) -> String {
        self.input_path.clone()
    }

    pub fn get_output_path(&self) -> String {
        self.output_path.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PresetSummary")]
#[serde(default)]
pub struct PresetSummary {
    /// Preset identifier matching `Preset.id`.
    #[php(prop, name = "id")]
    pub id: String,
    /// Preset version matching `Preset.version`.
    #[php(prop, name = "version")]
    pub version: String,
    /// Schema name matching `Preset.schema_name`.
    #[php(prop, name = "schemaName")]
    #[serde(alias = "schemaName")]
    pub schema_name: String,
    /// One-line preset description.
    #[php(prop, name = "description")]
    pub description: String,
    /// Top-level category.
    #[php(prop, name = "category")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub category: String,
    /// Free-form tags.
    #[php(prop, name = "tags")]
    pub tags: Vec<String>,
    /// Default call mode.
    #[php(prop, name = "preferredCallMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "preferredCallMode")]
    pub preferred_call_mode: String,
    /// Whether the preset prompts the model for citations.
    #[php(prop, name = "emitCitations")]
    #[serde(alias = "emitCitations")]
    pub emit_citations: bool,
    /// Stable fingerprint matching `Preset.fingerprint`.
    #[php(prop, name = "fingerprint")]
    pub fingerprint: String,
}

#[php_impl]
impl PresetSummary {
    #[php(constructor)]
    pub fn new(
        id: String,
        version: String,
        schemaName: String,
        description: String,
        category: String,
        tags: Vec<String>,
        preferredCallMode: String,
        emitCitations: bool,
        fingerprint: String,
    ) -> Self {
        Self {
            id,
            version,
            schema_name: schemaName,
            description,
            category,
            tags,
            preferred_call_mode: preferredCallMode,
            emit_citations: emitCitations,
            fingerprint,
        }
    }

    pub fn get_id(&self) -> String {
        self.id.clone()
    }

    pub fn get_version(&self) -> String {
        self.version.clone()
    }

    pub fn get_schema_name(&self) -> String {
        self.schema_name.clone()
    }

    pub fn get_description(&self) -> String {
        self.description.clone()
    }

    pub fn get_category(&self) -> String {
        self.category.clone()
    }

    pub fn get_tags(&self) -> Vec<String> {
        self.tags.clone()
    }

    pub fn get_preferred_call_mode(&self) -> String {
        self.preferred_call_mode.clone()
    }

    pub fn get_emit_citations(&self) -> bool {
        self.emit_citations.clone()
    }

    pub fn get_fingerprint(&self) -> String {
        self.fingerprint.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ProcessingWarning")]
#[serde(default)]
pub struct ProcessingWarning {
    /// The pipeline stage or feature that produced this warning
    /// (e.g., "embedding", "chunking", "language_detection", "output_format").
    #[php(prop, name = "source")]
    pub source: String,
    /// Human-readable description of what went wrong.
    #[php(prop, name = "message")]
    pub message: String,
}

#[php_impl]
impl ProcessingWarning {
    #[php(constructor)]
    pub fn new(source: String, message: String) -> Self {
        Self { source, message }
    }

    pub fn get_source(&self) -> String {
        self.source.clone()
    }

    pub fn get_message(&self) -> String {
        self.message.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\PropertyChange")]
#[serde(default)]
pub struct PropertyChange {
    /// Property name, such as `"bold"`, `"italic"`, `"font_size"`, or `"font_color"`.
    #[php(prop, name = "name")]
    pub name: String,
    /// Value before the change, when available.
    #[php(prop, name = "from")]
    pub from: Option<String>,
    /// Value after the change, when available.
    #[php(prop, name = "to")]
    pub to: Option<String>,
}

#[php_impl]
impl PropertyChange {
    #[php(constructor)]
    pub fn new(name: String, from: Option<String>, to: Option<String>) -> Self {
        Self { name, from, to }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_from(&self) -> Option<String> {
        self.from.clone()
    }

    pub fn get_to(&self) -> Option<String> {
        self.to.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ProxyConfig")]
#[serde(default)]
pub struct ProxyConfig {
    /// Proxy URL (e.g. "http://proxy:8080", "socks5://proxy:1080").
    #[php(prop, name = "url")]
    pub url: String,
    /// Optional username for proxy authentication.
    #[php(prop, name = "username")]
    pub username: Option<String>,
    /// Optional password for proxy authentication.
    #[php(prop, name = "password")]
    pub password: Option<String>,
}

impl Default for ProxyConfig {
    fn default() -> Self {
        <crawlberg::ProxyConfig as Default>::default().into()
    }
}

#[php_impl]
impl ProxyConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(url: String, username: Option<String>, password: Option<String>) -> Self {
        Self {
            url,
            username,
            password,
        }
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }

    pub fn get_username(&self) -> Option<String> {
        self.username.clone()
    }

    pub fn get_password(&self) -> Option<String> {
        self.password.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\PstMetadata")]
#[serde(default)]
pub struct PstMetadata {
    /// Total number of email messages found in the PST archive.
    #[php(prop, name = "messageCount")]
    #[serde(alias = "messageCount")]
    pub message_count: i64,
}

impl Default for PstMetadata {
    fn default() -> Self {
        <xberg::PstMetadata as Default>::default().into()
    }
}

#[php_impl]
impl PstMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(messageCount: i64) -> Self {
        Self {
            message_count: messageCount,
        }
    }

    pub fn get_message_count(&self) -> i64 {
        self.message_count.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\QrBoundingBox")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct QrBoundingBox {
    /// Horizontal pixel offset of the bounding box top-left corner.
    #[php(prop, name = "x")]
    pub x: u32,
    /// Vertical pixel offset of the bounding box top-left corner.
    #[php(prop, name = "y")]
    pub y: u32,
    /// Width of the bounding box in pixels.
    #[php(prop, name = "width")]
    pub width: u32,
    /// Height of the bounding box in pixels.
    #[php(prop, name = "height")]
    pub height: u32,
}

#[php_impl]
impl QrBoundingBox {
    #[php(constructor)]
    pub fn new(x: u32, y: u32, width: u32, height: u32) -> Self {
        Self { x, y, width, height }
    }

    pub fn get_x(&self) -> u32 {
        self.x.clone()
    }

    pub fn get_y(&self) -> u32 {
        self.y.clone()
    }

    pub fn get_width(&self) -> u32 {
        self.width.clone()
    }

    pub fn get_height(&self) -> u32 {
        self.height.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\QrCode")]
#[serde(default)]
pub struct QrCode {
    /// Decoded payload (text, URL, vCard string, …).
    #[php(prop, name = "payload")]
    pub payload: String,
    /// Detector-reported confidence in `[0.0, 1.0]`. `None` when the decoder
    /// does not expose confidence (the default `rqrr` backend always reports
    /// `Some` because successful decode implies high confidence).
    #[php(prop, name = "confidence")]
    pub confidence: Option<f32>,
    /// Bounding box of the QR code inside the source image, in pixel coordinates
    /// (`x`, `y` of the top-left corner; `width`, `height` of the rectangle).
    /// `None` if the decoder did not report a bounding box.
    pub bbox: Option<QrBoundingBox>,
}

#[php_impl]
impl QrCode {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(payload: String, confidence: Option<f32>, bbox: Option<&QrBoundingBox>) -> Self {
        Self {
            payload,
            confidence,
            bbox: bbox.cloned(),
        }
    }

    pub fn get_payload(&self) -> String {
        self.payload.clone()
    }

    pub fn get_confidence(&self) -> Option<f32> {
        self.confidence.clone()
    }

    pub fn get_bbox(&self) -> Option<QrBoundingBox> {
        self.bbox.clone().map(Into::into)
    }

    pub fn set_bbox(&mut self, value: Option<&QrBoundingBox>) {
        self.bbox = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\RakeParams")]
#[serde(default)]
pub struct RakeParams {
    /// Minimum word length to consider (default: 1).
    #[php(prop, name = "minWordLength")]
    #[serde(default = "crate::serde_defaults::rake_params_min_word_length")]
    #[serde(alias = "minWordLength")]
    pub min_word_length: i64,
    /// Maximum words in a keyword phrase (default: 3).
    #[php(prop, name = "maxWordsPerPhrase")]
    #[serde(default = "crate::serde_defaults::rake_params_max_words_per_phrase")]
    #[serde(alias = "maxWordsPerPhrase")]
    pub max_words_per_phrase: i64,
}

impl Default for RakeParams {
    fn default() -> Self {
        <xberg::RakeParams as Default>::default().into()
    }
}

#[php_impl]
impl RakeParams {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(minWordLength: i64, maxWordsPerPhrase: i64) -> Self {
        Self {
            min_word_length: minWordLength,
            max_words_per_phrase: maxWordsPerPhrase,
        }
    }

    pub fn get_min_word_length(&self) -> i64 {
        self.min_word_length.clone()
    }

    pub fn get_max_words_per_phrase(&self) -> i64 {
        self.max_words_per_phrase.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> RakeParams {
        xberg::RakeParams::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RecognizedTable")]
#[serde(default)]
pub struct RecognizedTable {
    /// Detection bbox that this table corresponds to (for matching).
    #[serde(alias = "detectionBbox")]
    pub detection_bbox: BBox,
    /// Table cells as a 2D vector (rows × columns).
    pub cells: Vec<Vec<String>>,
    /// Rendered markdown table.
    #[php(prop, name = "markdown")]
    pub markdown: String,
}

#[php_impl]
impl RecognizedTable {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(detectionBbox: &BBox, cells: Vec<Vec<String>>, markdown: String) -> Self {
        Self {
            detection_bbox: detectionBbox.clone(),
            cells,
            markdown,
        }
    }

    pub fn get_detection_bbox(&self) -> BBox {
        self.detection_bbox.clone()
    }

    pub fn get_cells(&self) -> Vec<Vec<String>> {
        self.cells.clone()
    }

    pub fn get_markdown(&self) -> String {
        self.markdown.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\RedactionConfig")]
#[serde(default)]
pub struct RedactionConfig {
    /// Categories to redact. Empty means "every category supported by the engine."
    #[php(prop, name = "categories")]
    #[serde(default)]
    pub categories: Vec<String>,
    /// Strategy applied to every match.
    #[php(prop, name = "strategy")]
    #[serde(default = "crate::serde_defaults::redaction_config_strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub strategy: String,
    /// Optional NER backend — required to redact PERSON / ORGANIZATION / LOCATION
    /// categories (the pure-Rust pattern engine only covers regex-detectable PII).
    pub ner: Option<NerConfig>,
    /// When `true`, chunk byte ranges are kept consistent with the rewritten content by
    /// adjusting `byte_start` / `byte_end` after replacement. When `false`, chunk byte
    /// ranges still refer to the *original* content offsets — useful when downstream
    /// consumers want to map findings back to the original document.
    #[php(prop, name = "preserveOffsets")]
    #[serde(default = "crate::serde_defaults::redaction_config_preserve_offsets")]
    #[serde(alias = "preserveOffsets")]
    pub preserve_offsets: bool,
    /// Arbitrary user-supplied literal terms to redact.
    ///
    /// Each term is treated as a regex hit against the document, surfacing as
    /// `PiiCategory.Custom(label)` in `RedactionFinding`
    /// where `label` is the per-term label (defaulting to the literal value itself).
    /// Case-insensitive by default; set `RedactionTerm.case_sensitive` for exact match.
    ///
    /// Use this when you need to redact tenant-specific tokens (employee IDs,
    /// project codes, internal product names) without writing a custom plugin.
    #[serde(alias = "customTerms")]
    #[serde(default)]
    pub custom_terms: Vec<RedactionTerm>,
    /// Arbitrary user-supplied regex patterns to redact.
    ///
    /// Same surfacing semantics as `custom_terms`: each
    /// hit becomes a `PiiCategory.Custom(label)` finding. Patterns are validated
    /// at config-construction time via `RedactionConfig.validate`.
    #[serde(alias = "customPatterns")]
    #[serde(default)]
    pub custom_patterns: Vec<RedactionPattern>,
}

impl Default for RedactionConfig {
    fn default() -> Self {
        <xberg::RedactionConfig as Default>::default().into()
    }
}

#[php_impl]
impl RedactionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        categories: Vec<String>,
        strategy: String,
        preserveOffsets: bool,
        customTerms: &ext_php_rs::types::ZendHashTable,
        customPatterns: &ext_php_rs::types::ZendHashTable,
        ner: Option<&NerConfig>,
    ) -> PhpResult<Self> {
        let mut customTerms_core_result: Vec<RedactionTerm> = Vec::new();
        for (_, item) in customTerms.iter() {
            if let Some(parsed) = <&RedactionTerm as ext_php_rs::convert::FromZval>::from_zval(item) {
                customTerms_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to RedactionTerm".to_string(),
                ));
            }
        }
        let customTerms_core: Vec<RedactionTerm> = customTerms_core_result;
        let mut customPatterns_core_result: Vec<RedactionPattern> = Vec::new();
        for (_, item) in customPatterns.iter() {
            if let Some(parsed) = <&RedactionPattern as ext_php_rs::convert::FromZval>::from_zval(item) {
                customPatterns_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to RedactionPattern".to_string(),
                ));
            }
        }
        let customPatterns_core: Vec<RedactionPattern> = customPatterns_core_result;
        Ok(Self {
            categories,
            strategy,
            ner: ner.cloned(),
            preserve_offsets: preserveOffsets,
            custom_terms: customTerms_core,
            custom_patterns: customPatterns_core,
        })
    }

    pub fn get_categories(&self) -> Vec<String> {
        self.categories.clone()
    }

    pub fn get_strategy(&self) -> String {
        self.strategy.clone()
    }

    pub fn get_ner(&self) -> Option<NerConfig> {
        self.ner.clone().map(Into::into)
    }

    pub fn set_ner(&mut self, value: Option<&NerConfig>) {
        self.ner = value.map(|value| value.clone().into());
    }

    pub fn get_preserve_offsets(&self) -> bool {
        self.preserve_offsets.clone()
    }

    pub fn get_custom_terms(&self) -> Vec<RedactionTerm> {
        self.custom_terms.clone()
    }

    pub fn get_custom_patterns(&self) -> Vec<RedactionPattern> {
        self.custom_patterns.clone()
    }

    pub fn validate(&self) -> PhpResult<()> {
        #[allow(clippy::needless_update)]
            let core_self = xberg::RedactionConfig {
                categories: self.categories.clone().into_iter().map(|s| match s.as_str() { "email" => xberg::PiiCategory::Email,"phone" => xberg::PiiCategory::Phone,"ssn" => xberg::PiiCategory::Ssn,"credit_card" => xberg::PiiCategory::CreditCard,"postal_code" => xberg::PiiCategory::PostalCode,"ip_address" => xberg::PiiCategory::IpAddress,"iban" => xberg::PiiCategory::Iban,"swift_bic" => xberg::PiiCategory::SwiftBic,"date_of_birth" => xberg::PiiCategory::DateOfBirth,"person" => xberg::PiiCategory::Person,"organization" => xberg::PiiCategory::Organization,"location" => xberg::PiiCategory::Location,"custom" => xberg::PiiCategory::Custom(Default::default()),other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'categories' of PiiCategory: expected one of [email, phone, ssn, credit_card, postal_code, ip_address, iban, swift_bic, date_of_birth, person, organization, location, custom]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        xberg::PiiCategory::Email
    }
     }
    ).collect(),
                strategy: match self.strategy.as_str() { "mask" => xberg::RedactionStrategy::Mask,"hash" => xberg::RedactionStrategy::Hash,"token_replace" => xberg::RedactionStrategy::TokenReplace,"drop" => xberg::RedactionStrategy::Drop,other => {
        ext_php_rs::exception::PhpException::default(format!(
            "invalid value {other:?} for field 'strategy' of RedactionStrategy: expected one of [mask, hash, token_replace, drop]"
        ))
        .throw()
        .expect("failed to throw PHP exception for invalid enum string value");
        Default::default()
    }
     }
    ,
                ner: self.ner.clone().map(Into::into),
                preserve_offsets: self.preserve_offsets,
                custom_terms: self.custom_terms.clone().into_iter().map(Into::into).collect(),
                custom_patterns: self.custom_patterns.clone().into_iter().map(Into::into).collect(),
                ..Default::default()
            };
        let result = core_self
            .validate()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> RedactionConfig {
        xberg::RedactionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RedactionFinding")]
#[serde(default)]
pub struct RedactionFinding {
    /// Byte-offset start in the original (pre-redaction) `ExtractedDocument.content`.
    #[php(prop, name = "start")]
    pub start: u32,
    /// Byte-offset end (exclusive) in the original `ExtractedDocument.content`.
    #[php(prop, name = "end")]
    pub end: u32,
    /// PII category that fired this redaction.
    #[php(prop, name = "category")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub category: String,
    /// Strategy applied to this finding (mask, hash, token-replace, drop).
    #[php(prop, name = "strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub strategy: String,
    /// String that replaced the original mention. Always present; for `Drop` the
    /// replacement is the empty string.
    #[php(prop, name = "replacementToken")]
    #[serde(alias = "replacementToken")]
    pub replacement_token: String,
}

#[php_impl]
impl RedactionFinding {
    #[php(constructor)]
    pub fn new(start: u32, end: u32, category: String, strategy: String, replacementToken: String) -> Self {
        Self {
            start,
            end,
            category,
            strategy,
            replacement_token: replacementToken,
        }
    }

    pub fn get_start(&self) -> u32 {
        self.start.clone()
    }

    pub fn get_end(&self) -> u32 {
        self.end.clone()
    }

    pub fn get_category(&self) -> String {
        self.category.clone()
    }

    pub fn get_strategy(&self) -> String {
        self.strategy.clone()
    }

    pub fn get_replacement_token(&self) -> String {
        self.replacement_token.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RedactionPattern")]
#[serde(default)]
pub struct RedactionPattern {
    /// Custom category label surfaced in `RedactionFinding.category`.
    #[php(prop, name = "label")]
    pub label: String,
    /// Regex pattern (Rust `regex` crate dialect — no look-around).
    #[php(prop, name = "pattern")]
    pub pattern: String,
    /// When `true`, match case-sensitively; otherwise prepend `(?i)` to the regex.
    #[php(prop, name = "caseSensitive")]
    #[serde(alias = "caseSensitive")]
    pub case_sensitive: bool,
}

#[php_impl]
impl RedactionPattern {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(label: String, pattern: String, caseSensitive: bool) -> Self {
        Self {
            label,
            pattern,
            case_sensitive: caseSensitive,
        }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_pattern(&self) -> String {
        self.pattern.clone()
    }

    pub fn get_case_sensitive(&self) -> bool {
        self.case_sensitive.clone()
    }

    /// Build a pattern with the given label (case-insensitive by default).
    #[php(name = "labeled")]
    pub fn labeled(label: String, pattern: String) -> RedactionPattern {
        xberg::RedactionPattern::labeled(label, pattern).into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RedactionReport")]
#[serde(default)]
pub struct RedactionReport {
    /// Individual redaction findings in original-source byte order.
    pub findings: Vec<RedactionFinding>,
    /// Total number of redactions applied across the document.
    #[php(prop, name = "totalRedacted")]
    #[serde(alias = "totalRedacted")]
    pub total_redacted: u32,
}

#[php_impl]
impl RedactionReport {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(findings: &ext_php_rs::types::ZendHashTable, totalRedacted: u32) -> PhpResult<Self> {
        let mut findings_core_result: Vec<RedactionFinding> = Vec::new();
        for (_, item) in findings.iter() {
            if let Some(parsed) = <&RedactionFinding as ext_php_rs::convert::FromZval>::from_zval(item) {
                findings_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to RedactionFinding".to_string(),
                ));
            }
        }
        let findings_core: Vec<RedactionFinding> = findings_core_result;
        Ok(Self {
            findings: findings_core,
            total_redacted: totalRedacted,
        })
    }

    pub fn get_findings(&self) -> Vec<RedactionFinding> {
        self.findings.clone()
    }

    pub fn get_total_redacted(&self) -> u32 {
        self.total_redacted.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RedactionTerm")]
#[serde(default)]
pub struct RedactionTerm {
    /// Custom category label surfaced in `RedactionFinding.category`.
    #[php(prop, name = "label")]
    pub label: String,
    /// Literal value to match. Regex metacharacters are escaped automatically.
    #[php(prop, name = "value")]
    pub value: String,
    /// When `true`, match the value as-is; otherwise match ASCII-case-insensitively.
    #[php(prop, name = "caseSensitive")]
    #[serde(alias = "caseSensitive")]
    pub case_sensitive: bool,
}

#[php_impl]
impl RedactionTerm {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(label: String, value: String, caseSensitive: bool) -> Self {
        Self {
            label,
            value,
            case_sensitive: caseSensitive,
        }
    }

    pub fn get_label(&self) -> String {
        self.label.clone()
    }

    pub fn get_value(&self) -> String {
        self.value.clone()
    }

    pub fn get_case_sensitive(&self) -> bool {
        self.case_sensitive.clone()
    }

    /// Build a term whose label is the literal value itself (case-insensitive).
    #[php(name = "literal")]
    pub fn literal(value: String) -> RedactionTerm {
        xberg::RedactionTerm::literal(value).into()
    }

    /// Build a term with a custom label.
    #[php(name = "labeled")]
    pub fn labeled(label: String, value: String) -> RedactionTerm {
        xberg::RedactionTerm::labeled(label, value).into()
    }
}

#[derive(Clone)]
#[php_class]
#[php(name = "Xberg\\Registry")]
pub struct Registry {
    inner: Arc<std::sync::Mutex<xberg::Registry>>,
}

#[php_impl]
impl Registry {
    /// Look up a preset by its identifier.
    pub fn get(&self, id: String) -> Option<Preset> {
        self.inner.lock().unwrap().get(&id).map(|v| v.clone().into())
    }

    /// Materialize a `PresetSummary` list for the public registry endpoint.
    pub fn summaries(&self) -> Vec<PresetSummary> {
        self.inner
            .lock()
            .unwrap()
            .summaries()
            .into_iter()
            .map(Into::into)
            .collect()
    }

    /// Number of presets currently loaded.
    pub fn len(&self) -> i64 {
        self.inner.lock().unwrap().len() as i64
    }

    /// Whether the registry contains zero presets.
    pub fn is_empty(&self) -> bool {
        self.inner.lock().unwrap().is_empty()
    }

    /// Read raw sample bytes for `<preset_id>` from
    /// `library/<id>/samples/<name>`. Returns `None` when the file is absent.
    pub fn sample_bytes(&self, presetId: String, name: String) -> Option<Vec<u8>> {
        self.inner
            .lock()
            .unwrap()
            .sample_bytes(&presetId, &name)
            .map(Into::into)
    }

    /// Load additional preset files from a runtime directory and insert them
    /// into this registry.
    ///
    /// Reads every `*.json` file directly under `dir` (non-recursive),
    /// validates each against the meta-schema, and inserts it. Files that fail
    /// validation are rejected — the error is returned immediately and the
    /// registry is left in a partially-updated state. Existing entries with the
    /// same id are overwritten.
    ///
    /// Returns the number of presets successfully loaded from `dir`.
    ///
    /// # Use case
    ///
    /// This is the injection point for downstream catalogs that add curated
    /// presets on top of the single embedded OSS preset.
    pub fn extend_from_dir(&self, dir: String) -> PhpResult<i64> {
        let result = self
            .inner
            .lock()
            .unwrap()
            .extend_from_dir(std::path::Path::new(&dir))
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result as i64)
    }

    /// Build the registry from preset files embedded at compile time under
    /// `src/presets/library/`. Validates every file against the meta-schema.
    #[php(name = "loadEmbedded")]
    pub fn load_embedded() -> PhpResult<Registry> {
        xberg::Registry::load_embedded()
            .map(|val| Self {
                inner: Arc::new(std::sync::Mutex::new(val)),
            })
            .map_err(|e| PhpException::default(e.to_string()))
    }

    /// Return the global registry, loading it on first access.
    ///
    /// # Panics
    ///
    /// Panics if any embedded preset is malformed. The build-time validation
    /// test ensures this cannot happen for the embedded presets; a panic here
    /// indicates a build artifact problem, not a runtime error.
    #[php(name = "global")]
    pub fn global() -> Registry {
        Self {
            inner: Arc::new(std::sync::Mutex::new(xberg::Registry::global().clone())),
        }
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\RerankedDocument")]
#[serde(default)]
pub struct RerankedDocument {
    /// Position of this document in the original input `documents` slice.
    #[php(prop, name = "index")]
    pub index: i64,
    /// Relevance score in `[0, 1]`. Higher means more relevant to the query.
    #[php(prop, name = "score")]
    pub score: f32,
    /// The document text.
    #[php(prop, name = "document")]
    pub document: String,
}

#[php_impl]
impl RerankedDocument {
    #[php(constructor)]
    pub fn new(index: i64, score: f32, document: String) -> Self {
        Self { index, score, document }
    }

    pub fn get_index(&self) -> i64 {
        self.index.clone()
    }

    pub fn get_score(&self) -> f32 {
        self.score.clone()
    }

    pub fn get_document(&self) -> String {
        self.document.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\RerankerConfig")]
#[serde(default)]
pub struct RerankerConfig {
    /// The reranker model to use (defaults to "balanced" preset if not specified).
    pub model: RerankerModelType,
    /// Return at most this many documents. `None` returns all.
    ///
    /// Applied after sorting by score, so the highest-scoring documents are kept.
    #[php(prop, name = "topK")]
    #[serde(alias = "topK")]
    #[serde(default)]
    pub top_k: Option<i64>,
    /// Batch size for local ONNX cross-encoder inference.
    #[php(prop, name = "batchSize")]
    #[serde(default = "crate::serde_defaults::reranker_config_batch_size")]
    #[serde(alias = "batchSize")]
    pub batch_size: i64,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `RerankerModelType.Llm` and `RerankerModelType.Plugin`, which download no model.
    #[php(prop, name = "showDownloadProgress")]
    #[serde(default = "crate::serde_defaults::reranker_config_show_download_progress")]
    #[serde(alias = "showDownloadProgress")]
    pub show_download_progress: bool,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the reranker ONNX model.
    ///
    /// Controls which execution provider (CPU, CUDA, CoreML, TensorRT) is used for
    /// local inference. Defaults to `None` (auto-select per platform).
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single `rerank()` call when
    /// using `RerankerModelType.Plugin`.
    ///
    /// Applies only to the in-process plugin path — protects against hung
    /// host-language backends. On timeout, the dispatcher returns
    /// `Plugin` instead of blocking forever.
    ///
    /// `None` disables the timeout. The default (60 seconds) is conservative
    /// for common in-process inference; increase for large document sets on slow
    /// hardware.
    #[php(prop, name = "maxRerankDurationSecs")]
    #[serde(alias = "maxRerankDurationSecs")]
    pub max_rerank_duration_secs: Option<i64>,
}

impl Default for RerankerConfig {
    fn default() -> Self {
        <xberg::RerankerConfig as Default>::default().into()
    }
}

#[php_impl]
impl RerankerConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: &RerankerModelType,
        batchSize: i64,
        showDownloadProgress: bool,
        topK: Option<i64>,
        cacheDir: Option<String>,
        acceleration: Option<&AccelerationConfig>,
        maxRerankDurationSecs: Option<i64>,
    ) -> Self {
        Self {
            model: model.clone(),
            top_k: topK,
            batch_size: batchSize,
            show_download_progress: showDownloadProgress,
            cache_dir: cacheDir,
            acceleration: acceleration.cloned(),
            max_rerank_duration_secs: maxRerankDurationSecs,
        }
    }

    pub fn get_model(&self) -> RerankerModelType {
        self.model.clone()
    }

    pub fn get_top_k(&self) -> Option<i64> {
        self.top_k.clone()
    }

    pub fn get_batch_size(&self) -> i64 {
        self.batch_size.clone()
    }

    pub fn get_show_download_progress(&self) -> bool {
        self.show_download_progress.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_max_rerank_duration_secs(&self) -> Option<i64> {
        self.max_rerank_duration_secs.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> RerankerConfig {
        xberg::RerankerConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\ResolvedPreset")]
#[serde(default)]
pub struct ResolvedPreset {
    /// Source preset identifier.
    #[php(prop, name = "id")]
    pub id: String,
    /// Source preset version.
    #[php(prop, name = "version")]
    pub version: String,
    /// Fingerprint of the source preset file, used as a cache token.
    #[php(prop, name = "fingerprint")]
    pub fingerprint: String,
    /// Schema name forwarded to the LLM.
    #[php(prop, name = "schemaName")]
    #[serde(alias = "schemaName")]
    pub schema_name: String,
    /// Effective JSON Schema (caller override or the preset's own).
    pub schema: serde_json::Value,
    /// System prompt with rendered context appended.
    #[php(prop, name = "systemPrompt")]
    #[serde(alias = "systemPrompt")]
    pub system_prompt: String,
    /// Merge strategy for paginated outputs.
    #[php(prop, name = "mergeMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "mergeMode")]
    pub merge_mode: String,
    /// Preferred call mode.
    #[php(prop, name = "preferredCallMode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "preferredCallMode")]
    pub preferred_call_mode: String,
    /// Whether the prompt asks for per-field citations.
    #[php(prop, name = "emitCitations")]
    #[serde(alias = "emitCitations")]
    pub emit_citations: bool,
}

#[php_impl]
impl ResolvedPreset {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        id: String,
        version: String,
        fingerprint: String,
        schemaName: String,
        schema: String,
        systemPrompt: String,
        mergeMode: String,
        preferredCallMode: String,
        emitCitations: bool,
    ) -> PhpResult<Self> {
        Ok(Self {
            id,
            version,
            fingerprint,
            schema_name: schemaName,
            schema: serde_json::from_str(&schema).map_err(|e| PhpException::default(e.to_string()))?,
            system_prompt: systemPrompt,
            merge_mode: mergeMode,
            preferred_call_mode: preferredCallMode,
            emit_citations: emitCitations,
        })
    }

    pub fn get_id(&self) -> String {
        self.id.clone()
    }

    pub fn get_version(&self) -> String {
        self.version.clone()
    }

    pub fn get_fingerprint(&self) -> String {
        self.fingerprint.clone()
    }

    pub fn get_schema_name(&self) -> String {
        self.schema_name.clone()
    }

    pub fn get_schema(&self) -> Option<String> {
        serde_json::to_string(&self.schema).ok()
    }

    pub fn get_system_prompt(&self) -> String {
        self.system_prompt.clone()
    }

    pub fn get_merge_mode(&self) -> String {
        self.merge_mode.clone()
    }

    pub fn get_preferred_call_mode(&self) -> String {
        self.preferred_call_mode.clone()
    }

    pub fn get_emit_citations(&self) -> bool {
        self.emit_citations.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\RevisionDelta")]
#[serde(default)]
pub struct RevisionDelta {
    /// Line-level content changes for this revision.
    pub content: Vec<DiffLine>,
    /// Cell-level table changes for this revision.
    #[serde(alias = "tableChanges")]
    pub table_changes: Vec<CellChange>,
    /// Formatting or metadata property changes for this revision.
    #[serde(alias = "propertyChanges")]
    #[serde(default)]
    pub property_changes: Vec<PropertyChange>,
}

impl Default for RevisionDelta {
    fn default() -> Self {
        <xberg::RevisionDelta as Default>::default().into()
    }
}

#[php_impl]
impl RevisionDelta {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: &ext_php_rs::types::ZendHashTable,
        tableChanges: &ext_php_rs::types::ZendHashTable,
        propertyChanges: &ext_php_rs::types::ZendHashTable,
    ) -> PhpResult<Self> {
        let mut content_core_result: Vec<DiffLine> = Vec::new();
        for (_, item) in content.iter() {
            if let Some(parsed) = <&DiffLine as ext_php_rs::convert::FromZval>::from_zval(item) {
                content_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to DiffLine".to_string(),
                ));
            }
        }
        let content_core: Vec<DiffLine> = content_core_result;
        let mut tableChanges_core_result: Vec<CellChange> = Vec::new();
        for (_, item) in tableChanges.iter() {
            if let Some(parsed) = <&CellChange as ext_php_rs::convert::FromZval>::from_zval(item) {
                tableChanges_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to CellChange".to_string(),
                ));
            }
        }
        let tableChanges_core: Vec<CellChange> = tableChanges_core_result;
        let mut propertyChanges_core_result: Vec<PropertyChange> = Vec::new();
        for (_, item) in propertyChanges.iter() {
            if let Some(parsed) = <&PropertyChange as ext_php_rs::convert::FromZval>::from_zval(item) {
                propertyChanges_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to PropertyChange".to_string(),
                ));
            }
        }
        let propertyChanges_core: Vec<PropertyChange> = propertyChanges_core_result;
        Ok(Self {
            content: content_core,
            table_changes: tableChanges_core,
            property_changes: propertyChanges_core,
        })
    }

    pub fn get_content(&self) -> Vec<DiffLine> {
        self.content.clone()
    }

    pub fn get_table_changes(&self) -> Vec<CellChange> {
        self.table_changes.clone()
    }

    pub fn get_property_changes(&self) -> Vec<PropertyChange> {
        self.property_changes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SecurityLimits")]
#[serde(default)]
pub struct SecurityLimits {
    /// Maximum uncompressed size for archives (500 MB)
    #[php(prop, name = "maxArchiveSize")]
    #[serde(default = "crate::serde_defaults::security_limits_max_archive_size")]
    #[serde(alias = "maxArchiveSize")]
    pub max_archive_size: i64,
    /// Maximum compression ratio before flagging as potential bomb (100:1)
    #[php(prop, name = "maxCompressionRatio")]
    #[serde(default = "crate::serde_defaults::security_limits_max_compression_ratio")]
    #[serde(alias = "maxCompressionRatio")]
    pub max_compression_ratio: i64,
    /// Maximum number of files in archive (10,000)
    #[php(prop, name = "maxFilesInArchive")]
    #[serde(default = "crate::serde_defaults::security_limits_max_files_in_archive")]
    #[serde(alias = "maxFilesInArchive")]
    pub max_files_in_archive: i64,
    /// Maximum nesting depth for structures (1024)
    #[php(prop, name = "maxNestingDepth")]
    #[serde(default = "crate::serde_defaults::security_limits_max_nesting_depth")]
    #[serde(alias = "maxNestingDepth")]
    pub max_nesting_depth: i64,
    /// Maximum length of any single XML entity / attribute / token (1 MiB).
    /// This is a per-token cap, NOT a total cap — billion-laughs class
    /// attacks where a single entity expands to hundreds of MB are caught
    /// here, while normal long text content (a paragraph, a CDATA block) is
    /// caught by `max_content_size` instead.
    #[php(prop, name = "maxEntityLength")]
    #[serde(default = "crate::serde_defaults::security_limits_max_entity_length")]
    #[serde(alias = "maxEntityLength")]
    pub max_entity_length: i64,
    /// Maximum string growth and decoded image allocation per document (100 MB)
    #[php(prop, name = "maxContentSize")]
    #[serde(default = "crate::serde_defaults::security_limits_max_content_size")]
    #[serde(alias = "maxContentSize")]
    pub max_content_size: i64,
    /// Maximum iterations per operation
    #[php(prop, name = "maxIterations")]
    #[serde(default = "crate::serde_defaults::security_limits_max_iterations")]
    #[serde(alias = "maxIterations")]
    pub max_iterations: i64,
    /// Maximum XML depth (1024 levels)
    #[php(prop, name = "maxXmlDepth")]
    #[serde(default = "crate::serde_defaults::security_limits_max_xml_depth")]
    #[serde(alias = "maxXmlDepth")]
    pub max_xml_depth: i64,
    /// Maximum aggregate table cells per document (100,000).
    ///
    /// Raise this for trusted large tabular inputs. Higher values permit
    /// proportionally more parsing work and output allocation.
    #[php(prop, name = "maxTableCells")]
    #[serde(default = "crate::serde_defaults::security_limits_max_table_cells")]
    #[serde(alias = "maxTableCells")]
    pub max_table_cells: i64,
    /// Maximum number of pages (or slides, or frames) in a single document.
    /// `None` means unlimited.
    ///
    /// Checked once the count is known and before any per-page work (OCR, layout
    /// detection, rendering) starts. Byte-size limits do not bound page count: a
    /// scanned page can compress to a few kilobytes, so a document well under
    /// `max_content_size` or `max_archive_size` can still hold thousands of pages
    /// of per-page work. Defaults to `None` (unlimited) because a real ceiling
    /// here is workload-specific and a low default would silently reject
    /// legitimate large documents; callers that want a ceiling set this
    /// explicitly.
    ///
    /// Enforced for: PDF (`extractors.pdf`, page count via `xberg_native_pdf`/`lopdf`),
    /// PPTX (`extraction.pptx`, slide count from the archive's slide parts),
    /// Keynote (`extractors.iwork.keynote`, slide count from `Index/Slide-*.iwa`
    /// entry names), ODP (`extractors.odp`, `draw:page` count in `content.xml`),
    /// and multi-frame TIFF images built with the `ocr` feature
    /// (`extractors.image`, frame count via the `tiff` crate). Not enforced for
    /// any other format, including DOCX, ODT, XLSX, legacy PPT/DOC, Pages/Numbers,
    /// and TIFF images when the `ocr` feature is disabled: those formats either
    /// have no fixed "page" the crate can count without doing the expensive work
    /// itself (DOCX/ODT page count is a layout outcome, not a stored value), or
    /// have no per-page pipeline to gate at all. Setting `max_pages` on a
    /// document of an unenforced format is silently a no-op, not a guarantee.
    #[php(prop, name = "maxPages")]
    #[serde(alias = "maxPages")]
    pub max_pages: Option<i64>,
}

impl Default for SecurityLimits {
    fn default() -> Self {
        <xberg::SecurityLimits as Default>::default().into()
    }
}

#[php_impl]
impl SecurityLimits {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        maxArchiveSize: i64,
        maxCompressionRatio: i64,
        maxFilesInArchive: i64,
        maxNestingDepth: i64,
        maxEntityLength: i64,
        maxContentSize: i64,
        maxIterations: i64,
        maxXmlDepth: i64,
        maxTableCells: i64,
        maxPages: Option<i64>,
    ) -> Self {
        Self {
            max_archive_size: maxArchiveSize,
            max_compression_ratio: maxCompressionRatio,
            max_files_in_archive: maxFilesInArchive,
            max_nesting_depth: maxNestingDepth,
            max_entity_length: maxEntityLength,
            max_content_size: maxContentSize,
            max_iterations: maxIterations,
            max_xml_depth: maxXmlDepth,
            max_table_cells: maxTableCells,
            max_pages: maxPages,
        }
    }

    pub fn get_max_archive_size(&self) -> i64 {
        self.max_archive_size.clone()
    }

    pub fn get_max_compression_ratio(&self) -> i64 {
        self.max_compression_ratio.clone()
    }

    pub fn get_max_files_in_archive(&self) -> i64 {
        self.max_files_in_archive.clone()
    }

    pub fn get_max_nesting_depth(&self) -> i64 {
        self.max_nesting_depth.clone()
    }

    pub fn get_max_entity_length(&self) -> i64 {
        self.max_entity_length.clone()
    }

    pub fn get_max_content_size(&self) -> i64 {
        self.max_content_size.clone()
    }

    pub fn get_max_iterations(&self) -> i64 {
        self.max_iterations.clone()
    }

    pub fn get_max_xml_depth(&self) -> i64 {
        self.max_xml_depth.clone()
    }

    pub fn get_max_table_cells(&self) -> i64 {
        self.max_table_cells.clone()
    }

    pub fn get_max_pages(&self) -> Option<i64> {
        self.max_pages.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> SecurityLimits {
        xberg::SecurityLimits::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\ServerConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct ServerConfig {
    /// Server host address (e.g., "127.0.0.1", "0.0.0.0")
    #[php(prop, name = "host")]
    #[serde(default = "crate::serde_defaults::server_config_host")]
    pub host: String,
    /// Server port number
    #[php(prop, name = "port")]
    #[serde(default = "crate::serde_defaults::server_config_port")]
    pub port: u16,
    /// CORS allowed origins. Empty vector means allow all origins.
    ///
    /// If this is an empty vector, the server will accept requests from any origin.
    /// If populated with specific origins (e.g., `"https://example.com"`), only
    /// those origins will be allowed.
    #[php(prop, name = "corsOrigins")]
    #[serde(alias = "corsOrigins")]
    #[serde(default)]
    pub cors_origins: Vec<String>,
    /// Maximum size of request body in bytes (default: 100 MB)
    #[php(prop, name = "maxRequestBodyBytes")]
    #[serde(default = "crate::serde_defaults::server_config_max_request_body_bytes")]
    #[serde(alias = "maxRequestBodyBytes")]
    pub max_request_body_bytes: i64,
    /// Maximum size of multipart fields in bytes (default: 100 MB)
    #[php(prop, name = "maxMultipartFieldBytes")]
    #[serde(default = "crate::serde_defaults::server_config_max_multipart_field_bytes")]
    #[serde(alias = "maxMultipartFieldBytes")]
    pub max_multipart_field_bytes: i64,
}

impl Default for ServerConfig {
    fn default() -> Self {
        <xberg::ServerConfig as Default>::default().into()
    }
}

#[php_impl]
impl ServerConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        host: String,
        port: u16,
        corsOrigins: Vec<String>,
        maxRequestBodyBytes: i64,
        maxMultipartFieldBytes: i64,
    ) -> Self {
        Self {
            host,
            port,
            cors_origins: corsOrigins,
            max_request_body_bytes: maxRequestBodyBytes,
            max_multipart_field_bytes: maxMultipartFieldBytes,
        }
    }

    pub fn get_host(&self) -> String {
        self.host.clone()
    }

    pub fn get_port(&self) -> u16 {
        self.port.clone()
    }

    pub fn get_cors_origins(&self) -> Vec<String> {
        self.cors_origins.clone()
    }

    pub fn get_max_request_body_bytes(&self) -> i64 {
        self.max_request_body_bytes.clone()
    }

    pub fn get_max_multipart_field_bytes(&self) -> i64 {
        self.max_multipart_field_bytes.clone()
    }

    pub fn listen_addr(&self) -> String {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ServerConfig {
            host: self.host.clone(),
            port: self.port,
            cors_origins: self.cors_origins.clone(),
            max_request_body_bytes: self.max_request_body_bytes as usize,
            max_multipart_field_bytes: self.max_multipart_field_bytes as usize,
            ..Default::default()
        };
        core_self.listen_addr()
    }

    pub fn cors_allows_all(&self) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ServerConfig {
            host: self.host.clone(),
            port: self.port,
            cors_origins: self.cors_origins.clone(),
            max_request_body_bytes: self.max_request_body_bytes as usize,
            max_multipart_field_bytes: self.max_multipart_field_bytes as usize,
            ..Default::default()
        };
        core_self.cors_allows_all()
    }

    pub fn is_origin_allowed(&self, origin: String) -> bool {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ServerConfig {
            host: self.host.clone(),
            port: self.port,
            cors_origins: self.cors_origins.clone(),
            max_request_body_bytes: self.max_request_body_bytes as usize,
            max_multipart_field_bytes: self.max_multipart_field_bytes as usize,
            ..Default::default()
        };
        core_self.is_origin_allowed(&origin)
    }

    pub fn max_request_body_mb(&self) -> i64 {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ServerConfig {
            host: self.host.clone(),
            port: self.port,
            cors_origins: self.cors_origins.clone(),
            max_request_body_bytes: self.max_request_body_bytes as usize,
            max_multipart_field_bytes: self.max_multipart_field_bytes as usize,
            ..Default::default()
        };
        core_self.max_request_body_mb() as i64
    }

    pub fn max_multipart_field_mb(&self) -> i64 {
        #[allow(clippy::needless_update)]
        let core_self = xberg::ServerConfig {
            host: self.host.clone(),
            port: self.port,
            cors_origins: self.cors_origins.clone(),
            max_request_body_bytes: self.max_request_body_bytes as usize,
            max_multipart_field_bytes: self.max_multipart_field_bytes as usize,
            ..Default::default()
        };
        core_self.max_multipart_field_mb() as i64
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> ServerConfig {
        xberg::ServerConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SitemapUrl")]
#[serde(default)]
pub struct SitemapUrl {
    /// The URL.
    #[php(prop, name = "url")]
    pub url: String,
    /// The last modification date, if present.
    #[php(prop, name = "lastmod")]
    pub lastmod: Option<String>,
    /// The change frequency, if present.
    #[php(prop, name = "changefreq")]
    pub changefreq: Option<String>,
    /// The priority, if present.
    #[php(prop, name = "priority")]
    pub priority: Option<String>,
}

impl Default for SitemapUrl {
    fn default() -> Self {
        <crawlberg::SitemapUrl as Default>::default().into()
    }
}

#[php_impl]
impl SitemapUrl {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(url: String, lastmod: Option<String>, changefreq: Option<String>, priority: Option<String>) -> Self {
        Self {
            url,
            lastmod,
            changefreq,
            priority,
        }
    }

    pub fn get_url(&self) -> String {
        self.url.clone()
    }

    pub fn get_lastmod(&self) -> Option<String> {
        self.lastmod.clone()
    }

    pub fn get_changefreq(&self) -> Option<String> {
        self.changefreq.clone()
    }

    pub fn get_priority(&self) -> Option<String> {
        self.priority.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\SparseEmbedding")]
#[serde(default)]
pub struct SparseEmbedding {
    /// Vocabulary token ids with non-zero weight, ascending.
    #[php(prop, name = "indices")]
    pub indices: Vec<u32>,
    /// Weights parallel to `SparseEmbedding.indices`.
    #[php(prop, name = "values")]
    pub values: Vec<f32>,
}

#[php_impl]
impl SparseEmbedding {
    #[php(constructor)]
    pub fn new(indices: Vec<u32>, values: Vec<f32>) -> Self {
        Self { indices, values }
    }

    pub fn get_indices(&self) -> Vec<u32> {
        self.indices.clone()
    }

    pub fn get_values(&self) -> Vec<f32> {
        self.values.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SparseEmbeddingConfig")]
#[serde(default)]
pub struct SparseEmbeddingConfig {
    /// The sparse-embedding model to use (defaults to the "opensearch-v3-distill" preset).
    pub model: SparseEmbeddingModelType,
    /// Batch size for local ONNX inference.
    ///
    /// SPLADE emits a `[seq, vocab]` logit tensor per document, so memory scales
    /// with batch size — keep this modest.
    #[php(prop, name = "batchSize")]
    #[serde(default = "crate::serde_defaults::sparse_embedding_config_batch_size")]
    #[serde(alias = "batchSize")]
    pub batch_size: i64,
    /// Maximum token sequence length for the tokenizer.
    #[php(prop, name = "maxLength")]
    #[serde(default = "crate::serde_defaults::sparse_embedding_config_max_length")]
    #[serde(alias = "maxLength")]
    pub max_length: i64,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `SparseEmbeddingModelType.Plugin`, which downloads no model.
    #[php(prop, name = "showDownloadProgress")]
    #[serde(default = "crate::serde_defaults::sparse_embedding_config_show_download_progress")]
    #[serde(alias = "showDownloadProgress")]
    pub show_download_progress: bool,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the sparse-embedding ONNX model.
    #[serde(default)]
    pub acceleration: Option<AccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `SparseEmbeddingModelType.Plugin`. `None` disables the timeout.
    #[php(prop, name = "maxEmbedDurationSecs")]
    #[serde(alias = "maxEmbedDurationSecs")]
    pub max_embed_duration_secs: Option<i64>,
}

impl Default for SparseEmbeddingConfig {
    fn default() -> Self {
        <xberg::SparseEmbeddingConfig as Default>::default().into()
    }
}

#[php_impl]
impl SparseEmbeddingConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        model: &SparseEmbeddingModelType,
        batchSize: i64,
        maxLength: i64,
        showDownloadProgress: bool,
        cacheDir: Option<String>,
        acceleration: Option<&AccelerationConfig>,
        maxEmbedDurationSecs: Option<i64>,
    ) -> Self {
        Self {
            model: model.clone(),
            batch_size: batchSize,
            max_length: maxLength,
            show_download_progress: showDownloadProgress,
            cache_dir: cacheDir,
            acceleration: acceleration.cloned(),
            max_embed_duration_secs: maxEmbedDurationSecs,
        }
    }

    pub fn get_model(&self) -> SparseEmbeddingModelType {
        self.model.clone()
    }

    pub fn get_batch_size(&self) -> i64 {
        self.batch_size.clone()
    }

    pub fn get_max_length(&self) -> i64 {
        self.max_length.clone()
    }

    pub fn get_show_download_progress(&self) -> bool {
        self.show_download_progress.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_acceleration(&self) -> Option<AccelerationConfig> {
        self.acceleration.clone().map(Into::into)
    }

    pub fn set_acceleration(&mut self, value: Option<&AccelerationConfig>) {
        self.acceleration = value.map(|value| value.clone().into());
    }

    pub fn get_max_embed_duration_secs(&self) -> Option<i64> {
        self.max_embed_duration_secs.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> SparseEmbeddingConfig {
        xberg::SparseEmbeddingConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\SparseEmbeddingPreset")]
#[serde(default)]
pub struct SparseEmbeddingPreset {
    /// Stable preset name referenced from config.
    #[php(prop, name = "name")]
    pub name: String,
    /// HuggingFace repository hosting the ONNX model.
    #[php(prop, name = "modelRepo")]
    #[serde(alias = "modelRepo")]
    pub model_repo: String,
    /// Path to the ONNX file within the repo.
    #[php(prop, name = "modelFile")]
    #[serde(alias = "modelFile")]
    pub model_file: String,
    /// Sibling files that must be downloaded alongside `model_file`.
    #[php(prop, name = "additionalFiles")]
    #[serde(alias = "additionalFiles")]
    pub additional_files: Vec<String>,
    /// Maximum token sequence length.
    #[php(prop, name = "maxLength")]
    #[serde(alias = "maxLength")]
    pub max_length: i64,
    /// Human-readable description.
    #[php(prop, name = "description")]
    pub description: String,
}

#[php_impl]
impl SparseEmbeddingPreset {
    #[php(constructor)]
    pub fn new(
        name: String,
        modelRepo: String,
        modelFile: String,
        additionalFiles: Vec<String>,
        maxLength: i64,
        description: String,
    ) -> Self {
        Self {
            name,
            model_repo: modelRepo,
            model_file: modelFile,
            additional_files: additionalFiles,
            max_length: maxLength,
            description,
        }
    }

    pub fn get_name(&self) -> String {
        self.name.clone()
    }

    pub fn get_model_repo(&self) -> String {
        self.model_repo.clone()
    }

    pub fn get_model_file(&self) -> String {
        self.model_file.clone()
    }

    pub fn get_additional_files(&self) -> Vec<String> {
        self.additional_files.clone()
    }

    pub fn get_max_length(&self) -> i64 {
        self.max_length.clone()
    }

    pub fn get_description(&self) -> String {
        self.description.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SsrfPolicy")]
#[serde(default)]
pub struct SsrfPolicy {
    /// If true, reject URLs that resolve to private/metadata IP ranges.
    #[php(prop, name = "denyPrivate")]
    #[serde(default = "crate::serde_defaults::ssrf_policy_deny_private")]
    #[serde(alias = "denyPrivate")]
    pub deny_private: bool,
    /// Hostnames and IP ranges permitted regardless of `deny_private`.
    ///
    /// The allowlist is an *override* of `deny_private`, not an intersection with it.
    /// Precedence, in order:
    ///
    /// 1. `deny_private == false` permits everything; the allowlist is not consulted.
    /// 2. A hostname matching an `Exact` or `Suffix` entry is permitted immediately,
    ///    *before* DNS resolution — so the deny-list is never applied to it. This trusts
    ///    the host string: a name that resolves into private space is still permitted.
    /// 3. A literal or resolved IP inside a `Cidr` entry is permitted even though it is
    ///    in the default deny-list.
    /// 4. Otherwise the default deny-list decides.
    ///
    /// An empty allowlist therefore denies nothing by itself — it simply leaves
    /// `deny_private` and the deny-list in sole control.
    #[serde(default)]
    pub allowlist: Vec<HostMatcher>,
    /// Maximum number of HTTP redirects to follow during validation.
    #[php(prop, name = "maxRedirects")]
    #[serde(default = "crate::serde_defaults::ssrf_policy_max_redirects")]
    #[serde(alias = "maxRedirects")]
    pub max_redirects: u8,
    /// Allowed URI schemes. Default: `["http", "https"]`.
    ///
    /// Only `http` and `https` are supported. An empty list denies every URL.
    #[php(prop, name = "schemeAllowlist")]
    #[serde(alias = "schemeAllowlist")]
    pub scheme_allowlist: Vec<String>,
}

impl Default for SsrfPolicy {
    fn default() -> Self {
        <crawlberg::SsrfPolicy as Default>::default().into()
    }
}

#[php_impl]
impl SsrfPolicy {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        denyPrivate: bool,
        allowlist: &ext_php_rs::types::ZendHashTable,
        maxRedirects: u8,
        schemeAllowlist: Vec<String>,
    ) -> PhpResult<Self> {
        let mut allowlist_core_result: Vec<HostMatcher> = Vec::new();
        for (_, item) in allowlist.iter() {
            if let Some(parsed) = <&HostMatcher as ext_php_rs::convert::FromZval>::from_zval(item) {
                allowlist_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to HostMatcher".to_string(),
                ));
            }
        }
        let allowlist_core: Vec<HostMatcher> = allowlist_core_result;
        Ok(Self {
            deny_private: denyPrivate,
            allowlist: allowlist_core,
            max_redirects: maxRedirects,
            scheme_allowlist: schemeAllowlist,
        })
    }

    pub fn get_deny_private(&self) -> bool {
        self.deny_private.clone()
    }

    pub fn get_allowlist(&self) -> Vec<HostMatcher> {
        self.allowlist.clone()
    }

    pub fn get_max_redirects(&self) -> u8 {
        self.max_redirects.clone()
    }

    pub fn get_scheme_allowlist(&self) -> Vec<String> {
        self.scheme_allowlist.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\StructuredData")]
#[serde(default)]
pub struct StructuredData {
    /// Type of structured data
    #[php(prop, name = "dataType")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "dataType")]
    pub data_type: String,
    /// Raw JSON string representation
    #[php(prop, name = "rawJson")]
    #[serde(alias = "rawJson")]
    pub raw_json: String,
    /// Schema type if detectable (e.g., "Article", "Event", "Product")
    #[php(prop, name = "schemaType")]
    #[serde(alias = "schemaType")]
    pub schema_type: Option<String>,
}

#[php_impl]
impl StructuredData {
    #[php(constructor)]
    pub fn new(dataType: String, rawJson: String, schemaType: Option<String>) -> Self {
        Self {
            data_type: dataType,
            raw_json: rawJson,
            schema_type: schemaType,
        }
    }

    pub fn get_data_type(&self) -> String {
        self.data_type.clone()
    }

    pub fn get_raw_json(&self) -> String {
        self.raw_json.clone()
    }

    pub fn get_schema_type(&self) -> Option<String> {
        self.schema_type.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\StructuredDataResult")]
#[serde(default)]
pub struct StructuredDataResult {
    /// The extracted text content, formatted for readability.
    #[php(prop, name = "content")]
    pub content: String,
    /// The source format identifier (e.g. `"json"`, `"yaml"`, `"toml"`).
    #[php(prop, name = "format")]
    pub format: String,
    /// Key-value metadata extracted from recognized text fields.
    #[php(prop, name = "metadata")]
    pub metadata: HashMap<String, String>,
    /// JSON paths of fields that were classified as text-bearing.
    #[php(prop, name = "textFields")]
    #[serde(alias = "textFields")]
    pub text_fields: Vec<String>,
    /// The parsed document as a canonical `serde_json.Value` tree, when the
    /// source format could be represented as one. `None` only for TOML inputs
    /// whose `toml.Value` fails to round-trip through `serde_json.Value`
    /// (xberg-io/xberg#155): the extractor falls back to a raw code block in
    /// that case.
    pub value: Option<serde_json::Value>,
    /// Flattened `path: value` renderings for every leaf field, in traversal
    /// order. Previously computed and discarded (xberg-io/xberg#166); now
    /// surfaced so callers get a full-text view even when the structured
    /// renderer only emits headings/lists for a subset of fields.
    #[php(prop, name = "flattened")]
    pub flattened: Vec<String>,
}

#[php_impl]
impl StructuredDataResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        format: String,
        metadata: HashMap<String, String>,
        textFields: Vec<String>,
        flattened: Vec<String>,
        value: Option<String>,
    ) -> PhpResult<Self> {
        Ok(Self {
            content,
            format,
            metadata,
            text_fields: textFields,
            value: value
                .map(|s| serde_json::from_str(&s))
                .transpose()
                .map_err(|e| PhpException::default(e.to_string()))?,
            flattened,
        })
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_format(&self) -> String {
        self.format.clone()
    }

    pub fn get_metadata(&self) -> HashMap<String, String> {
        self.metadata.clone()
    }

    pub fn get_text_fields(&self) -> Vec<String> {
        self.text_fields.clone()
    }

    pub fn get_value(&self) -> Option<String> {
        self.value.as_ref().and_then(|v| serde_json::to_string(v).ok())
    }

    pub fn get_flattened(&self) -> Vec<String> {
        self.flattened.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\StructuredExtractionConfig")]
#[serde(default)]
pub struct StructuredExtractionConfig {
    /// JSON Schema defining the desired output structure.
    pub schema: serde_json::Value,
    /// Schema name passed to the LLM's structured output mode.
    #[php(prop, name = "schemaName")]
    #[serde(alias = "schemaName")]
    pub schema_name: String,
    /// Optional schema description for the LLM.
    #[php(prop, name = "schemaDescription")]
    #[serde(alias = "schemaDescription")]
    #[serde(default)]
    pub schema_description: Option<String>,
    /// Enable strict mode — output must exactly match the schema.
    #[php(prop, name = "strict")]
    #[serde(default)]
    pub strict: bool,
    /// Custom Jinja2 extraction prompt template. When `None`, a default template is used.
    ///
    /// Available template variables:
    /// - `{{ content }}` — The extracted document text.
    /// - `{{ schema }}` — The JSON schema as a formatted string.
    /// - `{{ schema_name }}` — The schema name.
    /// - `{{ schema_description }}` — The schema description (may be empty).
    #[php(prop, name = "prompt")]
    #[serde(default)]
    pub prompt: Option<String>,
    /// LLM configuration for the extraction.
    pub llm: LlmConfig,
}

#[php_impl]
impl StructuredExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        schema: String,
        schemaName: String,
        strict: bool,
        llm: &LlmConfig,
        schemaDescription: Option<String>,
        prompt: Option<String>,
    ) -> PhpResult<Self> {
        Ok(Self {
            schema: serde_json::from_str(&schema).map_err(|e| PhpException::default(e.to_string()))?,
            schema_name: schemaName,
            schema_description: schemaDescription,
            strict,
            prompt,
            llm: llm.clone(),
        })
    }

    pub fn get_schema(&self) -> Option<String> {
        serde_json::to_string(&self.schema).ok()
    }

    pub fn get_schema_name(&self) -> String {
        self.schema_name.clone()
    }

    pub fn get_schema_description(&self) -> Option<String> {
        self.schema_description.clone()
    }

    pub fn get_strict(&self) -> bool {
        self.strict.clone()
    }

    pub fn get_prompt(&self) -> Option<String> {
        self.prompt.clone()
    }

    pub fn get_llm(&self) -> LlmConfig {
        self.llm.clone()
    }

    /// Default `Self::schema_name` passed to the LLM's structured-output mode.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultSchemaName")]
    pub fn default_schema_name() -> String {
        xberg::StructuredExtractionConfig::default_schema_name()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SummarizationConfig")]
#[serde(default)]
pub struct SummarizationConfig {
    /// Summarisation strategy.
    #[php(prop, name = "strategy")]
    #[serde(default = "crate::serde_defaults::summarization_config_strategy")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub strategy: String,
    /// Maximum summary length in tokens. `None` lets the backend pick a default.
    #[php(prop, name = "maxTokens")]
    #[serde(alias = "maxTokens")]
    pub max_tokens: Option<u32>,
    /// LLM configuration for the abstractive backend. Ignored when
    /// `strategy = Extractive`. Required when `strategy = Abstractive`.
    pub llm: Option<LlmConfig>,
}

impl Default for SummarizationConfig {
    fn default() -> Self {
        <xberg::SummarizationConfig as Default>::default().into()
    }
}

#[php_impl]
impl SummarizationConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(strategy: String, maxTokens: Option<u32>, llm: Option<&LlmConfig>) -> Self {
        Self {
            strategy,
            max_tokens: maxTokens,
            llm: llm.cloned(),
        }
    }

    pub fn get_strategy(&self) -> String {
        self.strategy.clone()
    }

    pub fn get_max_tokens(&self) -> Option<u32> {
        self.max_tokens.clone()
    }

    pub fn get_llm(&self) -> Option<LlmConfig> {
        self.llm.clone().map(Into::into)
    }

    pub fn set_llm(&mut self, value: Option<&LlmConfig>) {
        self.llm = value.map(|value| value.clone().into());
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\SupportedFormat")]
#[serde(default)]
pub struct SupportedFormat {
    /// File extension (without leading dot), e.g., "pdf", "docx"
    #[php(prop, name = "extension")]
    pub extension: String,
    /// MIME type string, e.g., "application/pdf"
    #[php(prop, name = "mimeType")]
    #[serde(alias = "mimeType")]
    pub mime_type: String,
}

#[php_impl]
impl SupportedFormat {
    #[php(constructor)]
    pub fn new(extension: String, mimeType: String) -> Self {
        Self {
            extension,
            mime_type: mimeType,
        }
    }

    pub fn get_extension(&self) -> String {
        self.extension.clone()
    }

    pub fn get_mime_type(&self) -> String {
        self.mime_type.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\SvgOptions")]
#[serde(default)]
pub struct SvgOptions {
    /// Run SVG bytes through `usvg` sanitization (strips external `href` attributes,
    /// JavaScript event handlers, and `foreignObject` elements) even when the
    /// output format is `Native`.  Defaults to `true`.
    #[php(prop, name = "sanitize")]
    #[serde(default = "crate::serde_defaults::svg_options_sanitize")]
    pub sanitize: bool,
    /// Target DPI when rasterizing SVG to a pixel-based format (PNG, JPEG, WebP,
    /// HEIF).  The tree's viewBox is scaled by `render_dpi / 96.0` before the
    /// pixel buffer is allocated.  Defaults to `96.0` (1× CSS pixel density).
    #[php(prop, name = "renderDpi")]
    #[serde(default = "crate::serde_defaults::svg_options_render_dpi")]
    #[serde(alias = "renderDpi")]
    pub render_dpi: f32,
}

impl Default for SvgOptions {
    fn default() -> Self {
        <xberg::core::config::extraction::SvgOptions as Default>::default().into()
    }
}

#[php_impl]
impl SvgOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(sanitize: bool, renderDpi: f32) -> Self {
        Self {
            sanitize,
            render_dpi: renderDpi,
        }
    }

    pub fn get_sanitize(&self) -> bool {
        self.sanitize.clone()
    }

    pub fn get_render_dpi(&self) -> f32 {
        self.render_dpi.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> SvgOptions {
        xberg::core::config::extraction::SvgOptions::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\Table")]
#[serde(default)]
pub struct Table {
    /// Table cells as a 2D vector (rows × columns)
    pub cells: Vec<Vec<String>>,
    /// Markdown representation of the table
    #[php(prop, name = "markdown")]
    pub markdown: String,
    /// Page number where the table was found (1-indexed)
    #[php(prop, name = "pageNumber")]
    #[serde(alias = "pageNumber")]
    pub page_number: u32,
    /// Bounding box of the table's position. Only populated when position data is
    /// available from the producing extractor.
    ///
    /// The coordinate space depends on how the table was produced, and callers must
    /// know which route produced a given `Table` before interpreting this field:
    ///
    /// - Tables extracted from a PDF's native content, and tables recognized on a
    ///   scanned PDF page that went through xberg's OCR pipeline (`--force-ocr` /
    ///   `--ocr-scanned-pages`), are in **PDF points with a bottom-left origin**
    ///   (x0=left, y0=bottom, x1=right, y1=top; y increases upward). For the OCR
    ///   case, the pipeline rescales the backend's raw pixel output into this space
    ///   before it reaches `Table.bounding_box` — see
    ///   `rescale_ocr_bboxes_to_page_points` in `extractors.pdf.ocr`.
    /// - Tables detected by OCR on a standalone image with no backing PDF page (for
    ///   example extracting a bare PNG/JPEG/TIFF) are in **raster pixel coordinates
    ///   with a top-left origin** (x0=left, y0=top, x1=right, y1=bottom; y increases
    ///   downward) — the same convention the OCR backend (Tesseract, PaddleOCR,
    ///   candle-based backends) or the layout detector reported them in. There is no
    ///   PDF page geometry to rescale into for this case, so the raw pixel box is
    ///   passed through unchanged.
    #[serde(alias = "boundingBox")]
    #[serde(default)]
    pub bounding_box: Option<BoundingBox>,
    /// Stable identifier shared by every `tables[]` entry that represents a
    /// fragment of the same physical table.
    ///
    /// Assigned deterministically by the extraction pipeline (e.g. a
    /// sequential `"table-N"` in document order); never derived from
    /// randomness or wall-clock time, so the same input document always
    /// produces the same ids. Consumers can use it to reconcile the markdown
    /// blocks in `content` / `pages[].content` / `chunks[].content` with the
    /// structured entries in `tables[]`. `None` when the extractor did not
    /// assign one.
    ///
    /// Today, same-page fragments of one physical table are already merged
    /// into a single `tables[]` entry before ids are assigned (see PDF table
    /// stitching), so in practice `table_id` is unique per entry rather than
    /// shared across several. A table split across a page boundary is
    /// intentionally *not* linked — its per-page pieces get separate ids.
    /// Sharing one id across page-boundary fragments is a known possible
    /// future extension, not implemented yet.
    #[php(prop, name = "tableId")]
    #[serde(alias = "tableId")]
    #[serde(default)]
    pub table_id: Option<String>,
    /// Paragraph styles carried by individual cells, for the cells that have one.
    ///
    /// Sparse and flat on purpose. A DOCX banner row -- row 0, one cell spanning the grid,
    /// styled `Heading1`..`Heading6` -- is what Word's navigation pane and a `TOC` field treat
    /// as the document outline, but as a table cell it reached consumers as anonymous text
    /// (GH#1587). `cells` keeps the bare text: prefixing it with `#` would put a markdown
    /// heading inside a table cell, which is invalid where it lands and would change text every
    /// existing consumer already reads. This list is the signal instead.
    ///
    /// Entries are only emitted for cells that actually carry a style, so an ordinary table
    /// serialises exactly as it did before. Indices are into `cells`.
    #[serde(alias = "cellStyles")]
    #[serde(default)]
    pub cell_styles: Vec<TableCellStyle>,
    /// Header cells for this fragment, i.e. the first row of `cells`.
    ///
    /// Populated even when this fragment's own header row was merged away or
    /// physically lives in a sibling fragment (see `table_id`), so a single
    /// fragment is interpretable in isolation. `None` when no header row
    /// could be determined.
    #[php(prop, name = "columns")]
    #[serde(default)]
    pub columns: Option<Vec<String>>,
}

impl Default for Table {
    fn default() -> Self {
        <xberg::Table as Default>::default().into()
    }
}

#[php_impl]
impl Table {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        cells: Vec<Vec<String>>,
        markdown: String,
        pageNumber: u32,
        cellStyles: &ext_php_rs::types::ZendHashTable,
        boundingBox: Option<&BoundingBox>,
        tableId: Option<String>,
        columns: Option<Vec<String>>,
    ) -> PhpResult<Self> {
        let mut cellStyles_core_result: Vec<TableCellStyle> = Vec::new();
        for (_, item) in cellStyles.iter() {
            if let Some(parsed) = <&TableCellStyle as ext_php_rs::convert::FromZval>::from_zval(item) {
                cellStyles_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to TableCellStyle".to_string(),
                ));
            }
        }
        let cellStyles_core: Vec<TableCellStyle> = cellStyles_core_result;
        Ok(Self {
            cells,
            markdown,
            page_number: pageNumber,
            bounding_box: boundingBox.cloned(),
            table_id: tableId,
            cell_styles: cellStyles_core,
            columns,
        })
    }

    pub fn get_cells(&self) -> Vec<Vec<String>> {
        self.cells.clone()
    }

    pub fn get_markdown(&self) -> String {
        self.markdown.clone()
    }

    pub fn get_page_number(&self) -> u32 {
        self.page_number.clone()
    }

    pub fn get_bounding_box(&self) -> Option<BoundingBox> {
        self.bounding_box.clone().map(Into::into)
    }

    pub fn set_bounding_box(&mut self, value: Option<&BoundingBox>) {
        self.bounding_box = value.map(|value| value.clone().into());
    }

    pub fn get_table_id(&self) -> Option<String> {
        self.table_id.clone()
    }

    pub fn get_cell_styles(&self) -> Vec<TableCellStyle> {
        self.cell_styles.clone()
    }

    pub fn get_columns(&self) -> Option<Vec<String>> {
        self.columns.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TableCell")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct TableCell {
    /// Cell content as text
    #[php(prop, name = "content")]
    pub content: String,
    /// Row span (number of rows this cell spans)
    #[php(prop, name = "rowSpan")]
    #[serde(default = "crate::serde_defaults::table_cell_row_span")]
    #[serde(alias = "rowSpan")]
    pub row_span: u32,
    /// Column span (number of columns this cell spans)
    #[php(prop, name = "colSpan")]
    #[serde(default = "crate::serde_defaults::table_cell_col_span")]
    #[serde(alias = "colSpan")]
    pub col_span: u32,
    /// Whether this is a header cell
    #[php(prop, name = "isHeader")]
    #[serde(alias = "isHeader")]
    #[serde(default)]
    pub is_header: bool,
}

impl Default for TableCell {
    fn default() -> Self {
        <xberg::TableCell as Default>::default().into()
    }
}

#[php_impl]
impl TableCell {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(content: String, rowSpan: u32, colSpan: u32, isHeader: bool) -> Self {
        Self {
            content,
            row_span: rowSpan,
            col_span: colSpan,
            is_header: isHeader,
        }
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_row_span(&self) -> u32 {
        self.row_span.clone()
    }

    pub fn get_col_span(&self) -> u32 {
        self.col_span.clone()
    }

    pub fn get_is_header(&self) -> bool {
        self.is_header.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TableCellStyle")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct TableCellStyle {
    /// Zero-indexed row of the cell this style belongs to.
    #[php(prop, name = "row")]
    pub row: u32,
    /// Zero-indexed column of the cell this style belongs to.
    #[php(prop, name = "col")]
    pub col: u32,
    /// Outline level 1-6 when the style resolves to a heading, otherwise `None`.
    #[php(prop, name = "headingLevel")]
    #[serde(alias = "headingLevel")]
    #[serde(default)]
    pub heading_level: Option<u8>,
    /// Human-readable style name, e.g. `heading 2`.
    #[php(prop, name = "styleName")]
    #[serde(alias = "styleName")]
    #[serde(default)]
    pub style_name: Option<String>,
}

impl Default for TableCellStyle {
    fn default() -> Self {
        <xberg::TableCellStyle as Default>::default().into()
    }
}

#[php_impl]
impl TableCellStyle {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(row: u32, col: u32, headingLevel: Option<u8>, styleName: Option<String>) -> Self {
        Self {
            row,
            col,
            heading_level: headingLevel,
            style_name: styleName,
        }
    }

    pub fn get_row(&self) -> u32 {
        self.row.clone()
    }

    pub fn get_col(&self) -> u32 {
        self.col.clone()
    }

    pub fn get_heading_level(&self) -> Option<u8> {
        self.heading_level.clone()
    }

    pub fn get_style_name(&self) -> Option<String> {
        self.style_name.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\TableDiff")]
#[serde(default)]
pub struct TableDiff {
    /// Zero-based index of the table in both `a.tables` and `b.tables`.
    #[php(prop, name = "fromIndex")]
    #[serde(alias = "fromIndex")]
    pub from_index: i64,
    /// Zero-based index in `b.tables` (equal to `from_index` for same-dimension tables).
    #[php(prop, name = "toIndex")]
    #[serde(alias = "toIndex")]
    pub to_index: i64,
    /// Cell-level changes within the table.
    #[serde(alias = "cellChanges")]
    pub cell_changes: Vec<CellChange>,
}

#[php_impl]
impl TableDiff {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(fromIndex: i64, toIndex: i64, cellChanges: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut cellChanges_core_result: Vec<CellChange> = Vec::new();
        for (_, item) in cellChanges.iter() {
            if let Some(parsed) = <&CellChange as ext_php_rs::convert::FromZval>::from_zval(item) {
                cellChanges_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to CellChange".to_string(),
                ));
            }
        }
        let cellChanges_core: Vec<CellChange> = cellChanges_core_result;
        Ok(Self {
            from_index: fromIndex,
            to_index: toIndex,
            cell_changes: cellChanges_core,
        })
    }

    pub fn get_from_index(&self) -> i64 {
        self.from_index.clone()
    }

    pub fn get_to_index(&self) -> i64 {
        self.to_index.clone()
    }

    pub fn get_cell_changes(&self) -> Vec<CellChange> {
        self.cell_changes.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TableGrid")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct TableGrid {
    /// Number of rows in the table.
    #[php(prop, name = "rows")]
    pub rows: u32,
    /// Number of columns in the table.
    #[php(prop, name = "cols")]
    pub cols: u32,
    /// All cells in row-major order.
    pub cells: Vec<GridCell>,
}

impl Default for TableGrid {
    fn default() -> Self {
        <xberg::TableGrid as Default>::default().into()
    }
}

#[php_impl]
impl TableGrid {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(rows: u32, cols: u32, cells: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut cells_core_result: Vec<GridCell> = Vec::new();
        for (_, item) in cells.iter() {
            if let Some(parsed) = <&GridCell as ext_php_rs::convert::FromZval>::from_zval(item) {
                cells_core_result.push(parsed.clone());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to GridCell".to_string(),
                ));
            }
        }
        let cells_core: Vec<GridCell> = cells_core_result;
        Ok(Self {
            rows,
            cols,
            cells: cells_core,
        })
    }

    pub fn get_rows(&self) -> u32 {
        self.rows.clone()
    }

    pub fn get_cols(&self) -> u32 {
        self.cols.clone()
    }

    pub fn get_cells(&self) -> Vec<GridCell> {
        self.cells.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TesseractConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct TesseractConfig {
    /// Language code(s) for OCR recognition. For Tesseract, languages are joined with "+".
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs (Python, Node, PHP, WASM, etc.): `["eng", "deu"]`. When
    /// deserializing from a config file, JSON body, or the REST/MCP API, a
    /// single string is also accepted, either as one code ("eng") or
    /// "+"-joined ("eng+deu").
    #[php(prop, name = "language")]
    pub language: Vec<String>,
    /// Page Segmentation Mode (1-13).
    ///
    /// PSM 0 is rejected: Tesseract's `PSM_OSD_ONLY` performs orientation and script
    /// detection with no character recognition, so it cannot satisfy a text-extraction
    /// request and previously yielded an empty document (GH#1586).
    ///
    /// `None` (the default) means the caller made no explicit choice: the extraction
    /// pipeline applies its own context-appropriate PSM (whole-image PSM 11, vertical-
    /// language PSM 5, layout-region PSM 6, or the sparse-text retry's PSM 3) exactly as
    /// it would with no `TesseractConfig` at all — see issue #1573. Setting any other
    /// field on this struct no longer changes that behaviour.
    ///
    /// Common explicit values:
    /// - 3: Fully automatic page segmentation (native engine default)
    /// - 6: Assume a single uniform block of text (WASM engine default — avoids
    ///   layout-analysis hang)
    /// - 11: Sparse text with no particular order
    #[php(prop, name = "psm")]
    pub psm: Option<i32>,
    /// Output format ("text" or "markdown")
    #[php(prop, name = "outputFormat")]
    #[serde(default = "crate::serde_defaults::tesseract_config_output_format")]
    #[serde(alias = "outputFormat")]
    pub output_format: String,
    /// OCR Engine Mode (0-3).
    ///
    /// - 0: Legacy engine only
    /// - 1: Neural nets (LSTM) only (usually best)
    /// - 2: Legacy + LSTM
    /// - 3: Default (based on what's available)
    #[php(prop, name = "oem")]
    #[serde(default = "crate::serde_defaults::tesseract_config_oem")]
    pub oem: i32,
    /// Minimum confidence threshold (0.0-100.0).
    ///
    /// Words with confidence below this threshold may be rejected or flagged.
    #[php(prop, name = "minConfidence")]
    #[serde(default = "crate::serde_defaults::tesseract_config_min_confidence")]
    #[serde(alias = "minConfidence")]
    pub min_confidence: f64,
    /// Image preprocessing configuration.
    ///
    /// Controls how images are preprocessed before OCR. Can significantly
    /// improve quality for scanned documents or low-quality images.
    pub preprocessing: Option<ImagePreprocessingConfig>,
    /// Enable automatic table detection and reconstruction
    #[php(prop, name = "enableTableDetection")]
    #[serde(default = "crate::serde_defaults::tesseract_config_enable_table_detection")]
    #[serde(alias = "enableTableDetection")]
    pub enable_table_detection: bool,
    /// Minimum confidence threshold for table detection (0.0-1.0)
    #[php(prop, name = "tableMinConfidence")]
    #[serde(default = "crate::serde_defaults::tesseract_config_table_min_confidence")]
    #[serde(alias = "tableMinConfidence")]
    pub table_min_confidence: f64,
    /// Column threshold for table detection (pixels)
    #[php(prop, name = "tableColumnThreshold")]
    #[serde(default = "crate::serde_defaults::tesseract_config_table_column_threshold")]
    #[serde(alias = "tableColumnThreshold")]
    pub table_column_threshold: i32,
    /// Row threshold ratio for table detection (0.0-1.0)
    #[php(prop, name = "tableRowThresholdRatio")]
    #[serde(default = "crate::serde_defaults::tesseract_config_table_row_threshold_ratio")]
    #[serde(alias = "tableRowThresholdRatio")]
    pub table_row_threshold_ratio: f64,
    /// Enable OCR result caching
    #[php(prop, name = "useCache")]
    #[serde(default = "crate::serde_defaults::tesseract_config_use_cache")]
    #[serde(alias = "useCache")]
    pub use_cache: bool,
    /// Use pre-adapted templates for character classification
    #[php(prop, name = "classifyUsePreAdaptedTemplates")]
    #[serde(default = "crate::serde_defaults::tesseract_config_classify_use_pre_adapted_templates")]
    #[serde(alias = "classifyUsePreAdaptedTemplates")]
    pub classify_use_pre_adapted_templates: bool,
    /// Enable N-gram language model.
    ///
    /// Kept on by default (see `Self.default` and
    /// `ocr.types.TesseractConfig.language_model_ngram_on` for the rationale);
    /// keep this field's default in sync with the internal struct's.
    #[php(prop, name = "languageModelNgramOn")]
    #[serde(default = "crate::serde_defaults::tesseract_config_language_model_ngram_on")]
    #[serde(alias = "languageModelNgramOn")]
    pub language_model_ngram_on: bool,
    /// Don't reject good words during block-level processing
    #[php(prop, name = "tesseditDontBlkrejGoodWds")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_dont_blkrej_good_wds")]
    #[serde(alias = "tesseditDontBlkrejGoodWds")]
    pub tessedit_dont_blkrej_good_wds: bool,
    /// Don't reject good words during row-level processing
    #[php(prop, name = "tesseditDontRowrejGoodWds")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_dont_rowrej_good_wds")]
    #[serde(alias = "tesseditDontRowrejGoodWds")]
    pub tessedit_dont_rowrej_good_wds: bool,
    /// Enable dictionary correction
    #[php(prop, name = "tesseditEnableDictCorrection")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_enable_dict_correction")]
    #[serde(alias = "tesseditEnableDictCorrection")]
    pub tessedit_enable_dict_correction: bool,
    /// Whitelist of allowed characters (empty = all allowed)
    #[php(prop, name = "tesseditCharWhitelist")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_char_whitelist")]
    #[serde(alias = "tesseditCharWhitelist")]
    pub tessedit_char_whitelist: String,
    /// Blacklist of forbidden characters (empty = none forbidden)
    #[php(prop, name = "tesseditCharBlacklist")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_char_blacklist")]
    #[serde(alias = "tesseditCharBlacklist")]
    pub tessedit_char_blacklist: String,
    /// Use primary language params model
    #[php(prop, name = "tesseditUsePrimaryParamsModel")]
    #[serde(default = "crate::serde_defaults::tesseract_config_tessedit_use_primary_params_model")]
    #[serde(alias = "tesseditUsePrimaryParamsModel")]
    pub tessedit_use_primary_params_model: bool,
    /// Variable-width space detection
    #[php(prop, name = "textordSpaceSizeIsVariable")]
    #[serde(default = "crate::serde_defaults::tesseract_config_textord_space_size_is_variable")]
    #[serde(alias = "textordSpaceSizeIsVariable")]
    pub textord_space_size_is_variable: bool,
    /// Use adaptive thresholding method
    #[php(prop, name = "thresholdingMethod")]
    #[serde(default = "crate::serde_defaults::tesseract_config_thresholding_method")]
    #[serde(alias = "thresholdingMethod")]
    pub thresholding_method: bool,
}

impl Default for TesseractConfig {
    fn default() -> Self {
        <xberg::TesseractConfig as Default>::default().into()
    }
}

#[php_impl]
impl TesseractConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        language: Vec<String>,
        outputFormat: String,
        oem: i32,
        minConfidence: f64,
        enableTableDetection: bool,
        tableMinConfidence: f64,
        tableColumnThreshold: i32,
        tableRowThresholdRatio: f64,
        useCache: bool,
        classifyUsePreAdaptedTemplates: bool,
        languageModelNgramOn: bool,
        tesseditDontBlkrejGoodWds: bool,
        tesseditDontRowrejGoodWds: bool,
        tesseditEnableDictCorrection: bool,
        tesseditCharWhitelist: String,
        tesseditCharBlacklist: String,
        tesseditUsePrimaryParamsModel: bool,
        textordSpaceSizeIsVariable: bool,
        thresholdingMethod: bool,
        psm: Option<i32>,
        preprocessing: Option<&ImagePreprocessingConfig>,
    ) -> Self {
        Self {
            language,
            psm,
            output_format: outputFormat,
            oem,
            min_confidence: minConfidence,
            preprocessing: preprocessing.cloned(),
            enable_table_detection: enableTableDetection,
            table_min_confidence: tableMinConfidence,
            table_column_threshold: tableColumnThreshold,
            table_row_threshold_ratio: tableRowThresholdRatio,
            use_cache: useCache,
            classify_use_pre_adapted_templates: classifyUsePreAdaptedTemplates,
            language_model_ngram_on: languageModelNgramOn,
            tessedit_dont_blkrej_good_wds: tesseditDontBlkrejGoodWds,
            tessedit_dont_rowrej_good_wds: tesseditDontRowrejGoodWds,
            tessedit_enable_dict_correction: tesseditEnableDictCorrection,
            tessedit_char_whitelist: tesseditCharWhitelist,
            tessedit_char_blacklist: tesseditCharBlacklist,
            tessedit_use_primary_params_model: tesseditUsePrimaryParamsModel,
            textord_space_size_is_variable: textordSpaceSizeIsVariable,
            thresholding_method: thresholdingMethod,
        }
    }

    pub fn get_language(&self) -> Vec<String> {
        self.language.clone()
    }

    pub fn get_psm(&self) -> Option<i32> {
        self.psm.clone()
    }

    pub fn get_output_format(&self) -> String {
        self.output_format.clone()
    }

    pub fn get_oem(&self) -> i32 {
        self.oem.clone()
    }

    pub fn get_min_confidence(&self) -> f64 {
        self.min_confidence.clone()
    }

    pub fn get_preprocessing(&self) -> Option<ImagePreprocessingConfig> {
        self.preprocessing.clone().map(Into::into)
    }

    pub fn set_preprocessing(&mut self, value: Option<&ImagePreprocessingConfig>) {
        self.preprocessing = value.map(|value| value.clone().into());
    }

    pub fn get_enable_table_detection(&self) -> bool {
        self.enable_table_detection.clone()
    }

    pub fn get_table_min_confidence(&self) -> f64 {
        self.table_min_confidence.clone()
    }

    pub fn get_table_column_threshold(&self) -> i32 {
        self.table_column_threshold.clone()
    }

    pub fn get_table_row_threshold_ratio(&self) -> f64 {
        self.table_row_threshold_ratio.clone()
    }

    pub fn get_use_cache(&self) -> bool {
        self.use_cache.clone()
    }

    pub fn get_classify_use_pre_adapted_templates(&self) -> bool {
        self.classify_use_pre_adapted_templates.clone()
    }

    pub fn get_language_model_ngram_on(&self) -> bool {
        self.language_model_ngram_on.clone()
    }

    pub fn get_tessedit_dont_blkrej_good_wds(&self) -> bool {
        self.tessedit_dont_blkrej_good_wds.clone()
    }

    pub fn get_tessedit_dont_rowrej_good_wds(&self) -> bool {
        self.tessedit_dont_rowrej_good_wds.clone()
    }

    pub fn get_tessedit_enable_dict_correction(&self) -> bool {
        self.tessedit_enable_dict_correction.clone()
    }

    pub fn get_tessedit_char_whitelist(&self) -> String {
        self.tessedit_char_whitelist.clone()
    }

    pub fn get_tessedit_char_blacklist(&self) -> String {
        self.tessedit_char_blacklist.clone()
    }

    pub fn get_tessedit_use_primary_params_model(&self) -> bool {
        self.tessedit_use_primary_params_model.clone()
    }

    pub fn get_textord_space_size_is_variable(&self) -> bool {
        self.textord_space_size_is_variable.clone()
    }

    pub fn get_thresholding_method(&self) -> bool {
        self.thresholding_method.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TesseractConfig {
        xberg::TesseractConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\TextAnnotation")]
#[serde(default)]
pub struct TextAnnotation {
    /// Start byte offset in the node's text content (inclusive).
    #[php(prop, name = "start")]
    pub start: u32,
    /// End byte offset in the node's text content (exclusive).
    #[php(prop, name = "end")]
    pub end: u32,
    /// Annotation type.
    pub kind: AnnotationKind,
}

#[php_impl]
impl TextAnnotation {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(start: u32, end: u32, kind: &AnnotationKind) -> Self {
        Self {
            start,
            end,
            kind: kind.clone(),
        }
    }

    pub fn get_start(&self) -> u32 {
        self.start.clone()
    }

    pub fn get_end(&self) -> u32 {
        self.end.clone()
    }

    pub fn get_kind(&self) -> AnnotationKind {
        self.kind.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\TextExtractionResult")]
#[serde(default)]
pub struct TextExtractionResult {
    /// Extracted text content
    #[php(prop, name = "content")]
    pub content: String,
    /// Number of lines
    #[php(prop, name = "lineCount")]
    #[serde(alias = "lineCount")]
    pub line_count: i64,
    /// Number of words
    #[php(prop, name = "wordCount")]
    #[serde(alias = "wordCount")]
    pub word_count: i64,
    /// Number of characters
    #[php(prop, name = "characterCount")]
    #[serde(alias = "characterCount")]
    pub character_count: i64,
    /// Markdown headers (text only, Markdown files only)
    #[php(prop, name = "headers")]
    pub headers: Option<Vec<String>>,
    /// Markdown links (Markdown files only).
    pub links: Option<Vec<MarkdownLink>>,
    /// Code blocks (Markdown files only).
    #[serde(alias = "codeBlocks")]
    pub code_blocks: Option<Vec<MarkdownCodeBlock>>,
}

#[php_impl]
impl TextExtractionResult {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        content: String,
        lineCount: i64,
        wordCount: i64,
        characterCount: i64,
        headers: Option<Vec<String>>,
        links: Option<&ext_php_rs::types::ZendHashTable>,
        codeBlocks: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let links_core: Option<Vec<MarkdownLink>> = if let Some(ht) = links {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&MarkdownLink as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to MarkdownLink".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let codeBlocks_core: Option<Vec<MarkdownCodeBlock>> = if let Some(ht) = codeBlocks {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&MarkdownCodeBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to MarkdownCodeBlock".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            content,
            line_count: lineCount,
            word_count: wordCount,
            character_count: characterCount,
            headers,
            links: links_core,
            code_blocks: codeBlocks_core,
        })
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_line_count(&self) -> i64 {
        self.line_count.clone()
    }

    pub fn get_word_count(&self) -> i64 {
        self.word_count.clone()
    }

    pub fn get_character_count(&self) -> i64 {
        self.character_count.clone()
    }

    pub fn get_headers(&self) -> Option<Vec<String>> {
        self.headers.clone()
    }

    pub fn get_links(&self) -> Option<Vec<MarkdownLink>> {
        self.links.clone()
    }

    pub fn get_code_blocks(&self) -> Option<Vec<MarkdownCodeBlock>> {
        self.code_blocks.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TextMetadata")]
#[serde(default)]
pub struct TextMetadata {
    /// Number of lines in the document
    #[php(prop, name = "lineCount")]
    #[serde(alias = "lineCount")]
    pub line_count: u32,
    /// Number of words
    #[php(prop, name = "wordCount")]
    #[serde(alias = "wordCount")]
    pub word_count: u32,
    /// Number of characters
    #[php(prop, name = "characterCount")]
    #[serde(alias = "characterCount")]
    pub character_count: u32,
    /// Markdown headers (headings text only, for Markdown files)
    #[php(prop, name = "headers")]
    pub headers: Option<Vec<String>>,
    /// Markdown links (for Markdown files).
    pub links: Option<Vec<MarkdownLink>>,
    /// Code blocks (for Markdown files).
    #[serde(alias = "codeBlocks")]
    pub code_blocks: Option<Vec<MarkdownCodeBlock>>,
}

impl Default for TextMetadata {
    fn default() -> Self {
        <xberg::TextMetadata as Default>::default().into()
    }
}

#[php_impl]
impl TextMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        lineCount: u32,
        wordCount: u32,
        characterCount: u32,
        headers: Option<Vec<String>>,
        links: Option<&ext_php_rs::types::ZendHashTable>,
        codeBlocks: Option<&ext_php_rs::types::ZendHashTable>,
    ) -> PhpResult<Self> {
        let links_core: Option<Vec<MarkdownLink>> = if let Some(ht) = links {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&MarkdownLink as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to MarkdownLink".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        let codeBlocks_core: Option<Vec<MarkdownCodeBlock>> = if let Some(ht) = codeBlocks {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&MarkdownCodeBlock as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to MarkdownCodeBlock".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            line_count: lineCount,
            word_count: wordCount,
            character_count: characterCount,
            headers,
            links: links_core,
            code_blocks: codeBlocks_core,
        })
    }

    pub fn get_line_count(&self) -> u32 {
        self.line_count.clone()
    }

    pub fn get_word_count(&self) -> u32 {
        self.word_count.clone()
    }

    pub fn get_character_count(&self) -> u32 {
        self.character_count.clone()
    }

    pub fn get_headers(&self) -> Option<Vec<String>> {
        self.headers.clone()
    }

    pub fn get_links(&self) -> Option<Vec<MarkdownLink>> {
        self.links.clone()
    }

    pub fn get_code_blocks(&self) -> Option<Vec<MarkdownCodeBlock>> {
        self.code_blocks.clone()
    }
}

#[derive(Clone)]
#[php_class]
#[php(name = "Xberg\\TokenCounter")]
pub struct TokenCounter {
    inner: Arc<xberg::TokenCounter>,
}

#[php_impl]
impl TokenCounter {
    /// Create a fresh counter with no previous state.
    #[php(name = "new")]
    pub fn new() -> TokenCounter {
        Self {
            inner: Arc::new(xberg::TokenCounter::new()),
        }
    }
}

impl Default for TokenCounter {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TokenReductionConfig")]
#[serde(default)]
pub struct TokenReductionConfig {
    /// Reduction intensity level.
    #[php(prop, name = "level")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub level: String,
    /// ISO 639-1 language code hint for stopword selection (e.g. `"en"`, `"de"`).
    #[php(prop, name = "languageHint")]
    #[serde(alias = "languageHint")]
    pub language_hint: Option<String>,
    /// Preserve Markdown formatting tokens during reduction.
    #[php(prop, name = "preserveMarkdown")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_preserve_markdown")]
    #[serde(alias = "preserveMarkdown")]
    pub preserve_markdown: bool,
    /// Preserve code block contents unchanged.
    #[php(prop, name = "preserveCode")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_preserve_code")]
    #[serde(alias = "preserveCode")]
    pub preserve_code: bool,
    /// Cosine similarity threshold below which sentences are considered dissimilar.
    #[php(prop, name = "semanticThreshold")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_semantic_threshold")]
    #[serde(alias = "semanticThreshold")]
    pub semantic_threshold: f32,
    /// Use Rayon parallel iterators for multi-core processing.
    #[php(prop, name = "enableParallel")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_enable_parallel")]
    #[serde(alias = "enableParallel")]
    pub enable_parallel: bool,
    /// Use SIMD-optimized text scanning where available.
    #[php(prop, name = "useSimd")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_use_simd")]
    #[serde(alias = "useSimd")]
    pub use_simd: bool,
    /// Per-language custom stopword lists (`language_code → stopword_list`).
    #[php(prop, name = "customStopwords")]
    #[serde(alias = "customStopwords")]
    pub custom_stopwords: Option<HashMap<String, Vec<String>>>,
    /// Regex patterns whose matched text is always preserved unchanged.
    #[php(prop, name = "preservePatterns")]
    #[serde(alias = "preservePatterns")]
    pub preserve_patterns: Vec<String>,
    /// Target fraction of text to retain (0.0–1.0); `None` = no fixed target.
    #[php(prop, name = "targetReduction")]
    #[serde(alias = "targetReduction")]
    pub target_reduction: Option<f32>,
    /// Group semantically similar sentences and emit only one per cluster.
    #[php(prop, name = "enableSemanticClustering")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_enable_semantic_clustering")]
    #[serde(alias = "enableSemanticClustering")]
    pub enable_semantic_clustering: bool,
    /// Skip removal of words with "important" characteristics (all-caps
    /// acronyms, words containing digits, mixed-case identifiers, very long
    /// words) during the `Aggressive`/`Maximum` common-word removal pass.
    ///
    /// `true` (the default) protects those words even when they would
    /// otherwise be dropped as low-value filler; `false` lets the frequency/
    /// length heuristics apply uniformly to every word, including ones that
    /// look like acronyms or technical terms (#269).
    #[php(prop, name = "preserveImportantWords")]
    #[serde(default = "crate::serde_defaults::token_reduction_config_preserve_important_words")]
    #[serde(alias = "preserveImportantWords")]
    pub preserve_important_words: bool,
}

impl Default for TokenReductionConfig {
    fn default() -> Self {
        <xberg::TokenReductionConfig as Default>::default().into()
    }
}

#[php_impl]
impl TokenReductionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        level: String,
        preserveMarkdown: bool,
        preserveCode: bool,
        semanticThreshold: f32,
        enableParallel: bool,
        useSimd: bool,
        preservePatterns: Vec<String>,
        enableSemanticClustering: bool,
        preserveImportantWords: bool,
        languageHint: Option<String>,
        customStopwords: Option<HashMap<String, Vec<String>>>,
        targetReduction: Option<f32>,
    ) -> Self {
        Self {
            level,
            language_hint: languageHint,
            preserve_markdown: preserveMarkdown,
            preserve_code: preserveCode,
            semantic_threshold: semanticThreshold,
            enable_parallel: enableParallel,
            use_simd: useSimd,
            custom_stopwords: customStopwords,
            preserve_patterns: preservePatterns,
            target_reduction: targetReduction,
            enable_semantic_clustering: enableSemanticClustering,
            preserve_important_words: preserveImportantWords,
        }
    }

    pub fn get_level(&self) -> String {
        self.level.clone()
    }

    pub fn get_language_hint(&self) -> Option<String> {
        self.language_hint.clone()
    }

    pub fn get_preserve_markdown(&self) -> bool {
        self.preserve_markdown.clone()
    }

    pub fn get_preserve_code(&self) -> bool {
        self.preserve_code.clone()
    }

    pub fn get_semantic_threshold(&self) -> f32 {
        self.semantic_threshold.clone()
    }

    pub fn get_enable_parallel(&self) -> bool {
        self.enable_parallel.clone()
    }

    pub fn get_use_simd(&self) -> bool {
        self.use_simd.clone()
    }

    pub fn get_custom_stopwords(&self) -> Option<HashMap<String, Vec<String>>> {
        self.custom_stopwords.clone()
    }

    pub fn get_preserve_patterns(&self) -> Vec<String> {
        self.preserve_patterns.clone()
    }

    pub fn get_target_reduction(&self) -> Option<f32> {
        self.target_reduction.clone()
    }

    pub fn get_enable_semantic_clustering(&self) -> bool {
        self.enable_semantic_clustering.clone()
    }

    pub fn get_preserve_important_words(&self) -> bool {
        self.preserve_important_words.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TokenReductionConfig {
        xberg::TokenReductionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TokenReductionOptions")]
#[serde(default)]
pub struct TokenReductionOptions {
    /// Reduction mode: "off", "light", "moderate", "aggressive", "maximum"
    #[php(prop, name = "mode")]
    #[serde(default = "crate::serde_defaults::token_reduction_options_mode")]
    pub mode: String,
    /// Preserve important words (capitalized, technical terms)
    #[php(prop, name = "preserveImportantWords")]
    #[serde(default = "crate::serde_defaults::token_reduction_options_preserve_important_words")]
    #[serde(alias = "preserveImportantWords")]
    pub preserve_important_words: bool,
}

impl Default for TokenReductionOptions {
    fn default() -> Self {
        <xberg::TokenReductionOptions as Default>::default().into()
    }
}

#[php_impl]
impl TokenReductionOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(mode: String, preserveImportantWords: bool) -> Self {
        Self {
            mode,
            preserve_important_words: preserveImportantWords,
        }
    }

    pub fn get_mode(&self) -> String {
        self.mode.clone()
    }

    pub fn get_preserve_important_words(&self) -> bool {
        self.preserve_important_words.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TokenReductionOptions {
        xberg::TokenReductionOptions::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TranscriptionConfig")]
#[serde(default)]
pub struct TranscriptionConfig {
    /// Master switch. When `false`, the transcription pipeline is not run.
    ///
    /// The extractor is registered for audio/video MIME types whenever the `transcription`
    /// feature is compiled in, independently of this flag, so an audio/video input with
    /// `enabled = false` fails with an `XbergError.Transcription` explaining how to turn
    /// transcription on — it does not fall through to another extractor.
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::transcription_config_enabled")]
    pub enabled: bool,
    /// Whisper model size to use.
    ///
    /// Smaller = faster + lower memory. `tiny` is the pragmatic default for
    /// first-time users and CI.
    #[php(prop, name = "model")]
    #[serde(default = "crate::serde_defaults::transcription_config_model")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub model: String,
    /// Optional language hint (ISO-639-1 code, e.g. "en", "de").
    ///
    /// When `None` (default), the current engine falls back to English.
    /// For deterministic production output, always set this explicitly.
    #[php(prop, name = "language")]
    #[serde(default)]
    pub language: Option<String>,
    /// Whether to request segment-level timestamps.
    ///
    /// When `true`, the decoder prompt omits `<|notimestamps|>` so the model emits
    /// `<|x.xx|>` tokens, and each transcript segment becomes its own paragraph element
    /// carrying `start_ms` / `end_ms` attributes. When `false` (default), all segment
    /// text is joined into a single flat paragraph with no timing attributes.
    #[php(prop, name = "timestamps")]
    #[serde(default = "crate::serde_defaults::transcription_config_timestamps")]
    pub timestamps: bool,
    /// Hard safety limit on input duration (milliseconds).
    ///
    /// Files longer than this are rejected after decode, before model work.
    /// Default: 30 minutes. Set to `None` to disable (not recommended for
    /// untrusted input).
    #[php(prop, name = "maxDurationMs")]
    #[serde(alias = "maxDurationMs")]
    pub max_duration_ms: Option<i64>,
    /// Hard safety limit on input size (bytes).
    ///
    /// Default: 512 MiB. Protects against pathological or malicious uploads.
    #[php(prop, name = "maxBytes")]
    #[serde(alias = "maxBytes")]
    pub max_bytes: Option<i64>,
    /// Wall-clock timeout for the entire transcription operation (ms).
    ///
    /// Bounds audio decode, model resolution/download, and inference together. On expiry
    /// the extraction fails with an `XbergError.Transcription`. `None` disables the bound
    /// and lets the operation run unbounded (not recommended for untrusted input).
    ///
    /// Enforced on the async extraction path only; the size and duration caps
    /// (`max_bytes`, `max_duration_ms`) are checked on every path.
    ///
    /// Default: 10 minutes.
    #[php(prop, name = "timeoutMs")]
    #[serde(alias = "timeoutMs")]
    pub timeout_ms: Option<i64>,
    /// Optional alternate Hugging Face cache root for Whisper models.
    ///
    /// When unset, hf-hub follows `HF_HUB_CACHE`, `HUGGINGFACE_HUB_CACHE`,
    /// `HF_HOME`, XDG, and platform defaults. Files remain in the standard
    /// content-addressed snapshot layout and are not copied into an Xberg cache.
    #[php(prop, name = "modelCacheDir")]
    #[serde(alias = "modelCacheDir")]
    #[serde(default)]
    pub model_cache_dir: Option<String>,
    /// Allow network access to download models from Hugging Face Hub.
    ///
    /// When `false`, only previously cached models may be used. Useful for
    /// air-gapped or fully offline deployments.
    #[php(prop, name = "allowNetwork")]
    #[serde(default = "crate::serde_defaults::transcription_config_allow_network")]
    #[serde(alias = "allowNetwork")]
    pub allow_network: bool,
    /// Request SHA256 verification of downloaded model files.
    ///
    /// Defaults to `false` because the resolver downloads from mutable Hugging
    /// Face refs unless callers pin and verify models out-of-band. Explicit
    /// `true` requests are rejected by the model resolver until pinned checksum
    /// metadata is available.
    #[php(prop, name = "verifyHash")]
    #[serde(default = "crate::serde_defaults::transcription_config_verify_hash")]
    #[serde(alias = "verifyHash")]
    pub verify_hash: bool,
}

impl Default for TranscriptionConfig {
    fn default() -> Self {
        <xberg::TranscriptionConfig as Default>::default().into()
    }
}

#[php_impl]
impl TranscriptionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        enabled: bool,
        model: String,
        timestamps: bool,
        allowNetwork: bool,
        verifyHash: bool,
        language: Option<String>,
        maxDurationMs: Option<i64>,
        maxBytes: Option<i64>,
        timeoutMs: Option<i64>,
        modelCacheDir: Option<String>,
    ) -> Self {
        Self {
            enabled,
            model,
            language,
            timestamps,
            max_duration_ms: maxDurationMs,
            max_bytes: maxBytes,
            timeout_ms: timeoutMs,
            model_cache_dir: modelCacheDir,
            allow_network: allowNetwork,
            verify_hash: verifyHash,
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_model(&self) -> String {
        self.model.clone()
    }

    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    pub fn get_timestamps(&self) -> bool {
        self.timestamps.clone()
    }

    pub fn get_max_duration_ms(&self) -> Option<i64> {
        self.max_duration_ms.clone()
    }

    pub fn get_max_bytes(&self) -> Option<i64> {
        self.max_bytes.clone()
    }

    pub fn get_timeout_ms(&self) -> Option<i64> {
        self.timeout_ms.clone()
    }

    pub fn get_model_cache_dir(&self) -> Option<String> {
        self.model_cache_dir.clone()
    }

    pub fn get_allow_network(&self) -> bool {
        self.allow_network.clone()
    }

    pub fn get_verify_hash(&self) -> bool {
        self.verify_hash.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TranscriptionConfig {
        xberg::TranscriptionConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\Translation")]
#[serde(default)]
pub struct Translation {
    /// BCP-47 language tag the translation was produced into (e.g. `"de"`, `"fr-CA"`).
    #[php(prop, name = "targetLang")]
    #[serde(alias = "targetLang")]
    pub target_lang: String,
    /// BCP-47 source language. `None` when the translation backend was asked to detect.
    #[php(prop, name = "sourceLang")]
    #[serde(alias = "sourceLang")]
    pub source_lang: Option<String>,
    /// Translated plain-text body. Matches the shape of `ExtractedDocument.content`.
    #[php(prop, name = "content")]
    pub content: String,
    /// Translated markup body (Markdown / HTML / etc.) when `preserve_markup` was
    /// enabled on the config. `None` otherwise.
    #[php(prop, name = "formattedContent")]
    #[serde(alias = "formattedContent")]
    pub formatted_content: Option<String>,
}

#[php_impl]
impl Translation {
    #[php(constructor)]
    pub fn new(
        targetLang: String,
        content: String,
        sourceLang: Option<String>,
        formattedContent: Option<String>,
    ) -> Self {
        Self {
            target_lang: targetLang,
            source_lang: sourceLang,
            content,
            formatted_content: formattedContent,
        }
    }

    pub fn get_target_lang(&self) -> String {
        self.target_lang.clone()
    }

    pub fn get_source_lang(&self) -> Option<String> {
        self.source_lang.clone()
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_formatted_content(&self) -> Option<String> {
        self.formatted_content.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\TranslationConfig")]
#[serde(default)]
pub struct TranslationConfig {
    /// BCP-47 language tag for the target language (e.g. `"de"`, `"fr-CA"`).
    #[php(prop, name = "targetLang")]
    #[serde(alias = "targetLang")]
    pub target_lang: String,
    /// Optional explicit source language. `None` asks the backend to auto-detect.
    #[php(prop, name = "sourceLang")]
    #[serde(alias = "sourceLang")]
    pub source_lang: Option<String>,
    /// Translate the formatted (Markdown/HTML) rendition alongside plain text when
    /// `formatted_content` is present.
    #[php(prop, name = "preserveMarkup")]
    #[serde(alias = "preserveMarkup")]
    #[serde(default)]
    pub preserve_markup: bool,
    /// LLM configuration used for translation.
    pub llm: LlmConfig,
}

#[php_impl]
impl TranslationConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(targetLang: String, preserveMarkup: bool, llm: &LlmConfig, sourceLang: Option<String>) -> Self {
        Self {
            target_lang: targetLang,
            source_lang: sourceLang,
            preserve_markup: preserveMarkup,
            llm: llm.clone(),
        }
    }

    pub fn get_target_lang(&self) -> String {
        self.target_lang.clone()
    }

    pub fn get_source_lang(&self) -> Option<String> {
        self.source_lang.clone()
    }

    pub fn get_preserve_markup(&self) -> bool {
        self.preserve_markup.clone()
    }

    pub fn get_llm(&self) -> LlmConfig {
        self.llm.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TreeSitterConfig")]
#[serde(default)]
pub struct TreeSitterConfig {
    /// Enable code intelligence processing (default: true).
    ///
    /// When `false`, tree-sitter analysis is completely skipped even if
    /// the config section is present.
    #[php(prop, name = "enabled")]
    #[serde(default = "crate::serde_defaults::tree_sitter_config_enabled")]
    pub enabled: bool,
    /// Custom cache directory for downloaded grammars.
    ///
    /// When `None`, uses the default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
    ///
    /// Consumed both by the CLI (`tree-sitter download --from-config`,
    /// `cache warm`) and by `CodeExtractor` at
    /// extraction time, so that a configured cache directory is honoured
    /// wherever grammars are looked up or downloaded, not only during an
    /// explicit CLI download.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    #[serde(default)]
    pub cache_dir: Option<String>,
    /// Languages to pre-download on init (e.g., `["python", "rust"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands as a pre-download hint. Extraction itself does
    /// not read this field: a given source file always processes with a
    /// single, already auto-detected language, so there is nothing for a
    /// language allowlist to gate at extraction time.
    #[php(prop, name = "languages")]
    #[serde(default)]
    pub languages: Option<Vec<String>>,
    /// Language groups to pre-download (e.g., `["web", "systems", "scripting"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands, for the same reason as `languages` above.
    #[php(prop, name = "groups")]
    #[serde(default)]
    pub groups: Option<Vec<String>>,
    /// Processing options for code analysis.
    #[serde(default)]
    pub process: TreeSitterProcessConfig,
}

impl Default for TreeSitterConfig {
    fn default() -> Self {
        <xberg::TreeSitterConfig as Default>::default().into()
    }
}

#[php_impl]
impl TreeSitterConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        enabled: bool,
        process: &TreeSitterProcessConfig,
        cacheDir: Option<String>,
        languages: Option<Vec<String>>,
        groups: Option<Vec<String>>,
    ) -> Self {
        Self {
            enabled,
            cache_dir: cacheDir,
            languages,
            groups,
            process: process.clone(),
        }
    }

    pub fn get_enabled(&self) -> bool {
        self.enabled.clone()
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_languages(&self) -> Option<Vec<String>> {
        self.languages.clone()
    }

    pub fn get_groups(&self) -> Option<Vec<String>> {
        self.groups.clone()
    }

    pub fn get_process(&self) -> TreeSitterProcessConfig {
        self.process.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TreeSitterConfig {
        xberg::TreeSitterConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TreeSitterProcessConfig")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct TreeSitterProcessConfig {
    /// Extract structural items (functions, classes, structs, etc.). Default: true.
    #[php(prop, name = "structure")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_structure")]
    pub structure: bool,
    /// Extract import statements. Default: true.
    #[php(prop, name = "imports")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_imports")]
    pub imports: bool,
    /// Extract export statements. Default: true.
    #[php(prop, name = "exports")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_exports")]
    pub exports: bool,
    /// Extract comments. Default: false.
    #[php(prop, name = "comments")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_comments")]
    pub comments: bool,
    /// Extract docstrings. Default: false.
    #[php(prop, name = "docstrings")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_docstrings")]
    pub docstrings: bool,
    /// Extract symbol definitions. Default: false.
    #[php(prop, name = "symbols")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_symbols")]
    pub symbols: bool,
    /// Include parse diagnostics. Default: false.
    #[php(prop, name = "diagnostics")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_diagnostics")]
    pub diagnostics: bool,
    /// Extract a hierarchical key/value data tree from data-format files
    /// (JSON, YAML, TOML, XML, CSV, etc.). Default: false.
    #[php(prop, name = "dataExtraction")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_data_extraction")]
    #[serde(alias = "dataExtraction")]
    pub data_extraction: bool,
    /// Maximum chunk size in bytes. `None` disables chunking.
    #[php(prop, name = "chunkMaxSize")]
    #[serde(alias = "chunkMaxSize")]
    #[serde(default)]
    pub chunk_max_size: Option<i64>,
    /// Content rendering mode for code extraction.
    #[php(prop, name = "contentMode")]
    #[serde(default = "crate::serde_defaults::tree_sitter_process_config_content_mode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    #[serde(alias = "contentMode")]
    pub content_mode: String,
}

impl Default for TreeSitterProcessConfig {
    fn default() -> Self {
        <xberg::TreeSitterProcessConfig as Default>::default().into()
    }
}

#[php_impl]
impl TreeSitterProcessConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        structure: bool,
        imports: bool,
        exports: bool,
        comments: bool,
        docstrings: bool,
        symbols: bool,
        diagnostics: bool,
        dataExtraction: bool,
        contentMode: String,
        chunkMaxSize: Option<i64>,
    ) -> Self {
        Self {
            structure,
            imports,
            exports,
            comments,
            docstrings,
            symbols,
            diagnostics,
            data_extraction: dataExtraction,
            chunk_max_size: chunkMaxSize,
            content_mode: contentMode,
        }
    }

    pub fn get_structure(&self) -> bool {
        self.structure.clone()
    }

    pub fn get_imports(&self) -> bool {
        self.imports.clone()
    }

    pub fn get_exports(&self) -> bool {
        self.exports.clone()
    }

    pub fn get_comments(&self) -> bool {
        self.comments.clone()
    }

    pub fn get_docstrings(&self) -> bool {
        self.docstrings.clone()
    }

    pub fn get_symbols(&self) -> bool {
        self.symbols.clone()
    }

    pub fn get_diagnostics(&self) -> bool {
        self.diagnostics.clone()
    }

    pub fn get_data_extraction(&self) -> bool {
        self.data_extraction.clone()
    }

    pub fn get_chunk_max_size(&self) -> Option<i64> {
        self.chunk_max_size.clone()
    }

    pub fn get_content_mode(&self) -> String {
        self.content_mode.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> TreeSitterProcessConfig {
        xberg::TreeSitterProcessConfig::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\TrocrBackendOptions")]
#[serde(default)]
pub struct TrocrBackendOptions {
    /// Optional model variant; the backend constructor's variant is used when absent.
    #[php(prop, name = "variant")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub variant: Option<String>,
    /// Optional per-call device override.
    #[php(prop, name = "device")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub device: Option<String>,
    /// Optional Hugging Face cache root.
    #[php(prop, name = "cacheDir")]
    #[serde(alias = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Optional immutable Hugging Face model revision.
    #[php(prop, name = "hfRevision")]
    #[serde(alias = "hfRevision")]
    pub hf_revision: Option<String>,
}

impl Default for TrocrBackendOptions {
    fn default() -> Self {
        <xberg::candle_ocr::TrocrBackendOptions as Default>::default().into()
    }
}

#[php_impl]
impl TrocrBackendOptions {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    pub fn get_variant(&self) -> Option<String> {
        self.variant.clone().map(Into::into)
    }

    pub fn get_device(&self) -> Option<String> {
        self.device.clone().map(Into::into)
    }

    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }

    pub fn get_hf_revision(&self) -> Option<String> {
        self.hf_revision.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\UrlExtractionConfig")]
#[serde(default)]
pub struct UrlExtractionConfig {
    /// URL extraction mode.
    #[php(prop, name = "mode")]
    #[serde(skip_serializing_if = "String::is_empty")]
    pub mode: String,
    /// Crawlberg crawl configuration used for HTTP(S) URL extraction.
    pub crawl: CrawlConfig,
    /// Optional regex filter for document-discovered URLs.
    #[php(prop, name = "documentUrlPattern")]
    #[serde(alias = "documentUrlPattern")]
    pub document_url_pattern: Option<String>,
    /// Maximum URLs to follow per extraction result.
    #[php(prop, name = "maxDocumentUrlsPerResult")]
    #[serde(alias = "maxDocumentUrlsPerResult")]
    pub max_document_urls_per_result: Option<u32>,
    /// Maximum URLs followed across the whole extraction call.
    #[php(prop, name = "maxTotalUrls")]
    #[serde(alias = "maxTotalUrls")]
    pub max_total_urls: Option<u32>,
    /// Allow bare local filesystem path inputs.
    #[php(prop, name = "allowLocalFileInputs")]
    #[serde(default = "crate::serde_defaults::url_extraction_config_allow_local_file_inputs")]
    #[serde(alias = "allowLocalFileInputs")]
    pub allow_local_file_inputs: bool,
    /// Allow local `file://` URI inputs.
    #[php(prop, name = "allowFileUris")]
    #[serde(default = "crate::serde_defaults::url_extraction_config_allow_file_uris")]
    #[serde(alias = "allowFileUris")]
    pub allow_file_uris: bool,
}

impl Default for UrlExtractionConfig {
    fn default() -> Self {
        <xberg::UrlExtractionConfig as Default>::default().into()
    }
}

#[php_impl]
impl UrlExtractionConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        mode: String,
        allowLocalFileInputs: bool,
        allowFileUris: bool,
        documentUrlPattern: Option<String>,
        maxDocumentUrlsPerResult: Option<u32>,
        maxTotalUrls: Option<u32>,
    ) -> Self {
        Self {
            mode,
            crawl: Default::default(),
            document_url_pattern: documentUrlPattern,
            max_document_urls_per_result: maxDocumentUrlsPerResult,
            max_total_urls: maxTotalUrls,
            allow_local_file_inputs: allowLocalFileInputs,
            allow_file_uris: allowFileUris,
        }
    }

    pub fn get_mode(&self) -> String {
        self.mode.clone()
    }

    pub fn get_crawl(&self) -> CrawlConfig {
        self.crawl.clone()
    }

    pub fn get_document_url_pattern(&self) -> Option<String> {
        self.document_url_pattern.clone()
    }

    pub fn get_max_document_urls_per_result(&self) -> Option<u32> {
        self.max_document_urls_per_result.clone()
    }

    pub fn get_max_total_urls(&self) -> Option<u32> {
        self.max_total_urls.clone()
    }

    pub fn get_allow_local_file_inputs(&self) -> bool {
        self.allow_local_file_inputs.clone()
    }

    pub fn get_allow_file_uris(&self) -> bool {
        self.allow_file_uris.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> UrlExtractionConfig {
        xberg::UrlExtractionConfig::default().into()
    }

    /// Default `Self::crawl` policy.
    ///
    /// Public and on the type rather than a free private `fn` because generated bindings
    /// have to call it to reproduce the default, and a private one is out of their reach.
    #[php(name = "defaultXbergCrawlConfig")]
    pub fn default_xberg_crawl_config() -> CrawlConfig {
        xberg::UrlExtractionConfig::default_xberg_crawl_config().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\UserChunkConfig")]
#[serde(default)]
pub struct UserChunkConfig {
    /// User-specified page ranges (overrides automatic chunking).
    #[serde(alias = "pageRanges")]
    pub page_ranges: Option<Vec<PageRange>>,
    /// User-specified pages per chunk (overrides automatic calculation).
    #[php(prop, name = "pagesPerChunk")]
    #[serde(alias = "pagesPerChunk")]
    pub pages_per_chunk: Option<u32>,
    /// Force chunking even for small documents.
    #[php(prop, name = "forceChunking")]
    #[serde(alias = "forceChunking")]
    pub force_chunking: bool,
    /// Disable chunking even for large documents.
    #[php(prop, name = "disableChunking")]
    #[serde(alias = "disableChunking")]
    pub disable_chunking: bool,
}

impl Default for UserChunkConfig {
    fn default() -> Self {
        <xberg::UserChunkConfig as Default>::default().into()
    }
}

#[php_impl]
impl UserChunkConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        forceChunking: bool,
        disableChunking: bool,
        pageRanges: Option<&ext_php_rs::types::ZendHashTable>,
        pagesPerChunk: Option<u32>,
    ) -> PhpResult<Self> {
        let pageRanges_core: Option<Vec<PageRange>> = if let Some(ht) = pageRanges {
            let mut result = Vec::new();
            for (_, item) in ht.iter() {
                if let Some(parsed) = <&PageRange as ext_php_rs::convert::FromZval>::from_zval(item) {
                    result.push(parsed.clone());
                } else {
                    return Err(ext_php_rs::exception::PhpException::default(
                        "Failed to convert array element to PageRange".to_string(),
                    ));
                }
            }
            Some(result)
        } else {
            None
        };
        Ok(Self {
            page_ranges: pageRanges_core,
            pages_per_chunk: pagesPerChunk,
            force_chunking: forceChunking,
            disable_chunking: disableChunking,
        })
    }

    pub fn get_page_ranges(&self) -> Option<Vec<PageRange>> {
        self.page_ranges.clone()
    }

    pub fn get_pages_per_chunk(&self) -> Option<u32> {
        self.pages_per_chunk.clone()
    }

    pub fn get_force_chunking(&self) -> bool {
        self.force_chunking.clone()
    }

    pub fn get_disable_chunking(&self) -> bool {
        self.disable_chunking.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\XlsxAppProperties")]
#[serde(default)]
pub struct XlsxAppProperties {
    /// Application name (e.g., "Microsoft Excel")
    #[php(prop, name = "application")]
    pub application: Option<String>,
    /// Application version
    #[php(prop, name = "appVersion")]
    #[serde(alias = "appVersion")]
    pub app_version: Option<String>,
    /// Document security level
    #[php(prop, name = "docSecurity")]
    #[serde(alias = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[php(prop, name = "scaleCrop")]
    #[serde(alias = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[php(prop, name = "linksUpToDate")]
    #[serde(alias = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[php(prop, name = "sharedDoc")]
    #[serde(alias = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[php(prop, name = "hyperlinksChanged")]
    #[serde(alias = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
    /// Company name
    #[php(prop, name = "company")]
    pub company: Option<String>,
    /// Worksheet names
    #[php(prop, name = "worksheetNames")]
    #[serde(alias = "worksheetNames")]
    pub worksheet_names: Vec<String>,
}

impl Default for XlsxAppProperties {
    fn default() -> Self {
        <xberg::extraction::office_metadata::app_properties::XlsxAppProperties as Default>::default().into()
    }
}

#[php_impl]
impl XlsxAppProperties {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(
        worksheetNames: Vec<String>,
        application: Option<String>,
        appVersion: Option<String>,
        docSecurity: Option<i32>,
        scaleCrop: Option<bool>,
        linksUpToDate: Option<bool>,
        sharedDoc: Option<bool>,
        hyperlinksChanged: Option<bool>,
        company: Option<String>,
    ) -> Self {
        Self {
            application,
            app_version: appVersion,
            doc_security: docSecurity,
            scale_crop: scaleCrop,
            links_up_to_date: linksUpToDate,
            shared_doc: sharedDoc,
            hyperlinks_changed: hyperlinksChanged,
            company,
            worksheet_names: worksheetNames,
        }
    }

    pub fn get_application(&self) -> Option<String> {
        self.application.clone()
    }

    pub fn get_app_version(&self) -> Option<String> {
        self.app_version.clone()
    }

    pub fn get_doc_security(&self) -> Option<i32> {
        self.doc_security.clone()
    }

    pub fn get_scale_crop(&self) -> Option<bool> {
        self.scale_crop.clone()
    }

    pub fn get_links_up_to_date(&self) -> Option<bool> {
        self.links_up_to_date.clone()
    }

    pub fn get_shared_doc(&self) -> Option<bool> {
        self.shared_doc.clone()
    }

    pub fn get_hyperlinks_changed(&self) -> Option<bool> {
        self.hyperlinks_changed.clone()
    }

    pub fn get_company(&self) -> Option<String> {
        self.company.clone()
    }

    pub fn get_worksheet_names(&self) -> Vec<String> {
        self.worksheet_names.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\XmlExtractionResult")]
#[serde(default)]
pub struct XmlExtractionResult {
    /// Extracted text content (XML structure filtered out)
    #[php(prop, name = "content")]
    pub content: String,
    /// Total number of XML elements processed
    #[php(prop, name = "elementCount")]
    #[serde(alias = "elementCount")]
    pub element_count: i64,
    /// List of unique element names found (sorted)
    #[php(prop, name = "uniqueElements")]
    #[serde(alias = "uniqueElements")]
    pub unique_elements: Vec<String>,
}

#[php_impl]
impl XmlExtractionResult {
    #[php(constructor)]
    pub fn new(content: String, elementCount: i64, uniqueElements: Vec<String>) -> Self {
        Self {
            content,
            element_count: elementCount,
            unique_elements: uniqueElements,
        }
    }

    pub fn get_content(&self) -> String {
        self.content.clone()
    }

    pub fn get_element_count(&self) -> i64 {
        self.element_count.clone()
    }

    pub fn get_unique_elements(&self) -> Vec<String> {
        self.unique_elements.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\XmlMetadata")]
#[serde(default)]
pub struct XmlMetadata {
    /// Total number of XML elements processed
    #[php(prop, name = "elementCount")]
    #[serde(alias = "elementCount")]
    pub element_count: u32,
    /// List of unique element tag names (sorted)
    #[php(prop, name = "uniqueElements")]
    #[serde(alias = "uniqueElements")]
    pub unique_elements: Vec<String>,
}

impl Default for XmlMetadata {
    fn default() -> Self {
        <xberg::XmlMetadata as Default>::default().into()
    }
}

#[php_impl]
impl XmlMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(elementCount: u32, uniqueElements: Vec<String>) -> Self {
        Self {
            element_count: elementCount,
            unique_elements: uniqueElements,
        }
    }

    pub fn get_element_count(&self) -> u32 {
        self.element_count.clone()
    }

    pub fn get_unique_elements(&self) -> Vec<String> {
        self.unique_elements.clone()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[php_class]
#[php(name = "Xberg\\YakeParams")]
#[serde(default)]
pub struct YakeParams {
    /// Window size for co-occurrence analysis (default: 2).
    ///
    /// Controls the context window for computing co-occurrence statistics.
    #[php(prop, name = "windowSize")]
    #[serde(default = "crate::serde_defaults::yake_params_window_size")]
    #[serde(alias = "windowSize")]
    pub window_size: i64,
}

impl Default for YakeParams {
    fn default() -> Self {
        <xberg::YakeParams as Default>::default().into()
    }
}

#[php_impl]
impl YakeParams {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(windowSize: i64) -> Self {
        Self {
            window_size: windowSize,
        }
    }

    pub fn get_window_size(&self) -> i64 {
        self.window_size.clone()
    }

    #[allow(clippy::should_implement_trait)]
    #[php(name = "default")]
    pub fn default() -> YakeParams {
        xberg::YakeParams::default().into()
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize, Default)]
#[php_class]
#[php(name = "Xberg\\YearRange")]
#[serde(default)]
#[allow(clippy::similar_names)]
pub struct YearRange {
    /// Earliest (minimum) year in the range.
    #[php(prop, name = "min")]
    pub min: Option<u32>,
    /// Latest (maximum) year in the range.
    #[php(prop, name = "max")]
    pub max: Option<u32>,
    /// All individual years present in the collection.
    #[php(prop, name = "years")]
    #[serde(default)]
    pub years: Vec<u32>,
}

#[php_impl]
impl YearRange {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))
    }

    #[php(constructor)]
    pub fn new(years: Vec<u32>, min: Option<u32>, max: Option<u32>) -> Self {
        Self { min, max, years }
    }

    pub fn get_min(&self) -> Option<u32> {
        self.min.clone()
    }

    pub fn get_max(&self) -> Option<u32> {
        self.max.clone()
    }

    pub fn get_years(&self) -> Vec<u32> {
        self.years.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\AnnotationKind")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct AnnotationKind {
    #[serde(rename = "annotation_type")]
    annotation_type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

#[php_impl]
impl AnnotationKind {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.annotation_type_tag.as_str(),
            "bold"
                | "italic"
                | "underline"
                | "strikethrough"
                | "code"
                | "subscript"
                | "superscript"
                | "link"
                | "highlight"
                | "color"
                | "font_size"
                | "custom"
        ) {
            return Err(PhpException::default(format!(
                "Invalid AnnotationKind tag: {}",
                value.annotation_type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "link")]
    pub fn _factory_link(url: String, title: Option<String>) -> Self {
        xberg::AnnotationKind::Link { url, title }.into()
    }

    #[php(name = "color")]
    pub fn _factory_color(value: String) -> Self {
        xberg::AnnotationKind::Color { value }.into()
    }

    #[php(name = "fontSize")]
    pub fn _factory_font_size(value: String) -> Self {
        xberg::AnnotationKind::FontSize { value }.into()
    }

    #[php(name = "custom")]
    pub fn _factory_custom(name: String, value: Option<String>) -> Self {
        xberg::AnnotationKind::Custom { name, value }.into()
    }

    #[php(getter)]
    pub fn get_annotation_type_tag(&self) -> String {
        self.annotation_type_tag.clone()
    }

    #[php(getter)]
    pub fn get_url(&self) -> Option<String> {
        self.url.clone()
    }

    #[php(getter)]
    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    #[php(getter)]
    pub fn get_value(&self) -> Option<String> {
        self.value.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\AssetCategory")]
pub struct AssetCategory {}

#[php_impl]
impl AssetCategory {
    pub const DOCUMENT: &str = "document";
    pub const IMAGE: &str = "image";
    pub const AUDIO: &str = "audio";
    pub const VIDEO: &str = "video";
    pub const FONT: &str = "font";
    pub const STYLESHEET: &str = "stylesheet";
    pub const SCRIPT: &str = "script";
    pub const ARCHIVE: &str = "archive";
    pub const DATA: &str = "data";
    pub const OTHER: &str = "other";
}

#[php_class]
#[php(name = "Xberg\\AuthConfig")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct AuthConfig {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub username: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub password: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub token: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

#[php_impl]
impl AuthConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "basic" | "bearer" | "header") {
            return Err(PhpException::default(format!(
                "Invalid AuthConfig tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "basic")]
    pub fn _factory_basic(username: String, password: String) -> Self {
        crawlberg::AuthConfig::Basic { username, password }.into()
    }

    #[php(name = "bearer")]
    pub fn _factory_bearer(token: String) -> Self {
        crawlberg::AuthConfig::Bearer { token }.into()
    }

    #[php(name = "header")]
    pub fn _factory_header(name: String, value: String) -> Self {
        crawlberg::AuthConfig::Header { name, value }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_username(&self) -> Option<String> {
        self.username.clone()
    }

    #[php(getter)]
    pub fn get_password(&self) -> Option<String> {
        self.password.clone()
    }

    #[php(getter)]
    pub fn get_token(&self) -> Option<String> {
        self.token.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    #[php(getter)]
    pub fn get_value(&self) -> Option<String> {
        self.value.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\BlockType")]
pub struct BlockType {}

#[php_impl]
impl BlockType {
    pub const PARAGRAPH: &str = "paragraph";
    pub const HEADING: &str = "heading";
    pub const BLOCKQUOTE: &str = "blockquote";
    pub const CODEBLOCK: &str = "code_block";
    pub const LISTITEM: &str = "list_item";
    pub const ORDEREDLIST: &str = "ordered_list";
    pub const BULLETLIST: &str = "bullet_list";
    pub const TASKLIST: &str = "task_list";
    pub const DEFINITIONLIST: &str = "definition_list";
    pub const DEFINITIONTERM: &str = "definition_term";
    pub const DEFINITIONDESCRIPTION: &str = "definition_description";
    pub const DIV: &str = "div";
    pub const SECTION: &str = "section";
    pub const THEMATICBREAK: &str = "thematic_break";
    pub const RAWBLOCK: &str = "raw_block";
    pub const MATHDISPLAY: &str = "math_display";
}

#[php_class]
#[php(name = "Xberg\\BoundaryReason")]
pub struct BoundaryReason {}

#[php_impl]
impl BoundaryReason {
    pub const START: &str = "start";
    pub const PAGEONEMARKER: &str = "page_one_marker";
    pub const LETTERHEADRESET: &str = "letterhead_reset";
    pub const DENSITYSHIFT: &str = "density_shift";
    pub const END: &str = "end";
}

#[php_class]
#[php(name = "Xberg\\BrowserBackend")]
pub struct BrowserBackend {}

#[php_impl]
impl BrowserBackend {
    pub const CHROMIUMOXIDE: &str = "chromiumoxide";
    pub const NATIVE: &str = "native";
}

#[php_class]
#[php(name = "Xberg\\BrowserMode")]
pub struct BrowserMode {}

#[php_impl]
impl BrowserMode {
    pub const AUTO: &str = "auto";
    pub const ALWAYS: &str = "always";
    pub const NEVER: &str = "never";
    pub const STEALTH: &str = "stealth";
}

#[php_class]
#[php(name = "Xberg\\BrowserWait")]
pub struct BrowserWait {}

#[php_impl]
impl BrowserWait {
    pub const NETWORKIDLE: &str = "network_idle";
    pub const SELECTOR: &str = "selector";
    pub const FIXED: &str = "fixed";
}

#[php_class]
#[php(name = "Xberg\\CallMode")]
pub struct CallMode {}

#[php_impl]
impl CallMode {
    pub const TEXTONLY: &str = "text_only";
    pub const VISIONONLY: &str = "vision_only";
    pub const TEXTPLUSVISION: &str = "text_plus_vision";
}

#[php_class]
#[php(name = "Xberg\\CandleDevicePreference")]
pub struct CandleDevicePreference {}

#[php_impl]
impl CandleDevicePreference {
    pub const AUTO: &str = "auto";
    pub const CPU: &str = "cpu";
    pub const CUDA: &str = "cuda";
    pub const METAL: &str = "metal";
}

#[php_class]
#[php(name = "Xberg\\CandleTrocrVariant")]
pub struct CandleTrocrVariant {}

#[php_impl]
impl CandleTrocrVariant {
    pub const BASEPRINTED: &str = "base-printed";
    pub const LARGEPRINTED: &str = "large-printed";
    pub const BASEHANDWRITTEN: &str = "base-handwritten";
    pub const LARGEHANDWRITTEN: &str = "large-handwritten";
}

#[php_class]
#[php(name = "Xberg\\ChunkSizing")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct ChunkSizing {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub cache_dir: Option<String>,
}

#[php_impl]
impl ChunkSizing {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "characters" | "tokenizer") {
            return Err(PhpException::default(format!(
                "Invalid ChunkSizing tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "tokenizer")]
    pub fn _factory_tokenizer(model: String, cacheDir: Option<String>) -> Self {
        xberg::ChunkSizing::Tokenizer {
            model,
            cache_dir: cacheDir.map(std::path::PathBuf::from),
        }
        .into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_model(&self) -> Option<String> {
        self.model.clone()
    }

    #[php(getter)]
    pub fn get_cache_dir(&self) -> Option<String> {
        self.cache_dir.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\ChunkType")]
pub struct ChunkType {}

#[php_impl]
impl ChunkType {
    pub const HEADING: &str = "heading";
    pub const PARTYLIST: &str = "party_list";
    pub const DEFINITIONS: &str = "definitions";
    pub const OPERATIVECLAUSE: &str = "operative_clause";
    pub const SIGNATUREBLOCK: &str = "signature_block";
    pub const SCHEDULE: &str = "schedule";
    pub const TABLELIKE: &str = "table_like";
    pub const FORMULA: &str = "formula";
    pub const CODEBLOCK: &str = "code_block";
    pub const FUNCTION_: &str = "function";
    pub const CLASS_: &str = "class";
    pub const MODULE: &str = "module";
    pub const IMAGE: &str = "image";
    pub const ORGCHART: &str = "org_chart";
    pub const DIAGRAM: &str = "diagram";
    pub const UNKNOWN: &str = "unknown";
}

#[php_class]
#[php(name = "Xberg\\ChunkerType")]
pub struct ChunkerType {}

#[php_impl]
impl ChunkerType {
    pub const TEXT: &str = "text";
    pub const MARKDOWN: &str = "markdown";
    pub const YAML: &str = "yaml";
    pub const SEMANTIC: &str = "semantic";
}

#[php_class]
#[php(name = "Xberg\\ChunkingReason")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct ChunkingReason {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub size_bytes: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub threshold_bytes: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page_count: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub threshold: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub force_ocr: Option<bool>,
}

#[php_impl]
impl ChunkingReason {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.type_tag.as_str(),
            "LargeFile" | "ManyPages" | "OcrRequired" | "LargeAndManyPages"
        ) {
            return Err(PhpException::default(format!(
                "Invalid ChunkingReason tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "largeFile")]
    pub fn _factory_large_file(sizeBytes: i64, thresholdBytes: i64) -> Self {
        xberg::ChunkingReason::LargeFile {
            size_bytes: sizeBytes as u64,
            threshold_bytes: thresholdBytes as u64,
        }
        .into()
    }

    #[php(name = "manyPages")]
    pub fn _factory_many_pages(pageCount: u32, threshold: u32) -> Self {
        xberg::ChunkingReason::ManyPages {
            page_count: pageCount,
            threshold,
        }
        .into()
    }

    #[php(name = "ocrRequired")]
    pub fn _factory_ocr_required(pageCount: u32, forceOcr: bool) -> Self {
        xberg::ChunkingReason::OcrRequired {
            page_count: pageCount,
            force_ocr: forceOcr,
        }
        .into()
    }

    #[php(name = "largeAndManyPages")]
    pub fn _factory_large_and_many_pages(sizeBytes: i64, pageCount: u32) -> Self {
        xberg::ChunkingReason::LargeAndManyPages {
            size_bytes: sizeBytes as u64,
            page_count: pageCount,
        }
        .into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_size_bytes(&self) -> Option<i64> {
        self.size_bytes
    }

    #[php(getter)]
    pub fn get_threshold_bytes(&self) -> Option<i64> {
        self.threshold_bytes
    }

    #[php(getter)]
    pub fn get_page_count(&self) -> Option<u32> {
        self.page_count
    }

    #[php(getter)]
    pub fn get_threshold(&self) -> Option<u32> {
        self.threshold
    }

    #[php(getter)]
    pub fn get_force_ocr(&self) -> Option<bool> {
        self.force_ocr
    }
}

#[php_class]
#[php(name = "Xberg\\CodeBlockStyle")]
pub struct CodeBlockStyle {}

#[php_impl]
impl CodeBlockStyle {
    pub const INDENTED: &str = "Indented";
    pub const BACKTICKS: &str = "Backticks";
    pub const TILDES: &str = "Tildes";
}

#[php_class]
#[php(name = "Xberg\\CodeContentMode")]
pub struct CodeContentMode {}

#[php_impl]
impl CodeContentMode {
    pub const CHUNKS: &str = "chunks";
    pub const RAW: &str = "raw";
    pub const STRUCTURE: &str = "structure";
}

#[php_class]
#[php(name = "Xberg\\CodeDataNodeKind")]
pub struct CodeDataNodeKind {}

#[php_impl]
impl CodeDataNodeKind {
    pub const KEYVALUE: &str = "key_value";
    pub const ELEMENT: &str = "element";
    pub const SEQUENCE: &str = "sequence";
}

#[php_class]
#[php(name = "Xberg\\ConfidenceSemantics")]
pub struct ConfidenceSemantics {}

#[php_impl]
impl ConfidenceSemantics {
    pub const LEGIBILITY: &str = "Legibility";
    pub const UNCALIBRATED: &str = "Uncalibrated";
    pub const NONE: &str = "None";
}

#[php_class]
#[php(name = "Xberg\\ContentFilterKind")]
pub struct ContentFilterKind {}

#[php_impl]
impl ContentFilterKind {
    pub const BM25: &str = "bm25";
}

#[php_class]
#[php(name = "Xberg\\ContentLayer")]
pub struct ContentLayer {}

#[php_impl]
impl ContentLayer {
    pub const BODY: &str = "body";
    pub const HEADER: &str = "header";
    pub const FOOTER: &str = "footer";
    pub const FOOTNOTE: &str = "footnote";
}

#[php_class]
#[php(name = "Xberg\\CrawlStrategyKind")]
pub struct CrawlStrategyKind {}

#[php_impl]
impl CrawlStrategyKind {
    pub const BFS: &str = "bfs";
    pub const DFS: &str = "dfs";
    pub const BESTFIRST: &str = "best_first";
    pub const ADAPTIVE: &str = "adaptive";
}

#[php_class]
#[php(name = "Xberg\\CredentialProviderConfig")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct CredentialProviderConfig {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub tenant_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub client_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub client_secret: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub scope: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub service_account_key_file: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub role_arn: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub token_file: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub session_name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
}

#[php_impl]
impl CredentialProviderConfig {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.type_tag.as_str(),
            "azure_ad" | "vertex_oauth2" | "vertex_adc" | "bedrock_web_identity"
        ) {
            return Err(PhpException::default(format!(
                "Invalid CredentialProviderConfig tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "azureAd")]
    pub fn _factory_azure_ad(tenantId: String, clientId: String, clientSecret: String, scope: Option<String>) -> Self {
        xberg::CredentialProviderConfig::AzureAd {
            tenant_id: tenantId,
            client_id: clientId,
            client_secret: clientSecret,
            scope,
        }
        .into()
    }

    #[php(name = "vertexOauth2")]
    pub fn _factory_vertex_oauth2(serviceAccountKeyFile: String, scope: Option<String>) -> Self {
        xberg::CredentialProviderConfig::VertexOauth2 {
            service_account_key_file: serviceAccountKeyFile,
            scope,
        }
        .into()
    }

    #[php(name = "vertexAdc")]
    pub fn _factory_vertex_adc(scope: Option<String>) -> Self {
        xberg::CredentialProviderConfig::VertexAdc { scope }.into()
    }

    #[php(name = "bedrockWebIdentity")]
    pub fn _factory_bedrock_web_identity(
        roleArn: String,
        tokenFile: String,
        sessionName: Option<String>,
        region: Option<String>,
    ) -> Self {
        xberg::CredentialProviderConfig::BedrockWebIdentity {
            role_arn: roleArn,
            token_file: tokenFile,
            session_name: sessionName,
            region,
        }
        .into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_tenant_id(&self) -> Option<String> {
        self.tenant_id.clone()
    }

    #[php(getter)]
    pub fn get_client_id(&self) -> Option<String> {
        self.client_id.clone()
    }

    #[php(getter)]
    pub fn get_client_secret(&self) -> Option<String> {
        self.client_secret.clone()
    }

    #[php(getter)]
    pub fn get_scope(&self) -> Option<String> {
        self.scope.clone()
    }

    #[php(getter)]
    pub fn get_service_account_key_file(&self) -> Option<String> {
        self.service_account_key_file.clone()
    }

    #[php(getter)]
    pub fn get_role_arn(&self) -> Option<String> {
        self.role_arn.clone()
    }

    #[php(getter)]
    pub fn get_token_file(&self) -> Option<String> {
        self.token_file.clone()
    }

    #[php(getter)]
    pub fn get_session_name(&self) -> Option<String> {
        self.session_name.clone()
    }

    #[php(getter)]
    pub fn get_region(&self) -> Option<String> {
        self.region.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\DiffLine")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct DiffLine {
    #[serde(rename = "kind")]
    kind_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub context: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub added: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub removed: Option<String>,
}

#[php_impl]
impl DiffLine {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.kind_tag.as_str(), "context" | "added" | "removed") {
            return Err(PhpException::default(format!(
                "Invalid DiffLine tag: {}",
                value.kind_tag,
            )));
        }
        Ok(value)
    }

    #[php(getter)]
    pub fn get_kind_tag(&self) -> String {
        self.kind_tag.clone()
    }

    #[php(getter)]
    pub fn get_context(&self) -> Option<String> {
        self.context.clone()
    }

    #[php(getter)]
    pub fn get_added(&self) -> Option<String> {
        self.added.clone()
    }

    #[php(getter)]
    pub fn get_removed(&self) -> Option<String> {
        self.removed.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\DocumentContentEncoding")]
pub struct DocumentContentEncoding {}

#[php_impl]
impl DocumentContentEncoding {
    pub const BASE64: &str = "base64";
}

#[php_class]
#[php(name = "Xberg\\ElementType")]
pub struct ElementType {}

#[php_impl]
impl ElementType {
    pub const TITLE: &str = "title";
    pub const NARRATIVETEXT: &str = "narrative_text";
    pub const HEADING: &str = "heading";
    pub const LISTITEM: &str = "list_item";
    pub const TABLE: &str = "table";
    pub const IMAGE: &str = "image";
    pub const PAGEBREAK: &str = "page_break";
    pub const CODEBLOCK: &str = "code_block";
    pub const FORMULA: &str = "formula";
    pub const BLOCKQUOTE: &str = "block_quote";
    pub const FOOTER: &str = "footer";
    pub const HEADER: &str = "header";
}

#[php_class]
#[php(name = "Xberg\\EmbeddingModelType")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct EmbeddingModelType {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub dimensions: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub llm: Option<LlmConfig>,
}

#[php_impl]
impl EmbeddingModelType {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "preset" | "custom" | "llm" | "plugin") {
            return Err(PhpException::default(format!(
                "Invalid EmbeddingModelType tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "preset")]
    pub fn _factory_preset(name: String) -> Self {
        xberg::EmbeddingModelType::Preset { name }.into()
    }

    #[php(name = "custom")]
    pub fn _factory_custom(modelId: String, dimensions: i64) -> Self {
        xberg::EmbeddingModelType::Custom {
            model_id: modelId,
            dimensions: dimensions as usize,
        }
        .into()
    }

    #[php(name = "llm")]
    pub fn _factory_llm(llm: &LlmConfig) -> Self {
        xberg::EmbeddingModelType::Llm {
            llm: Box::new(llm.clone().into()),
        }
        .into()
    }

    #[php(name = "plugin")]
    pub fn _factory_plugin(name: String) -> Self {
        xberg::EmbeddingModelType::Plugin { name }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    #[php(getter)]
    pub fn get_model_id(&self) -> Option<String> {
        self.model_id.clone()
    }

    #[php(getter)]
    pub fn get_dimensions(&self) -> Option<i64> {
        self.dimensions
    }

    #[php(getter)]
    pub fn get_llm(&self) -> Option<LlmConfig> {
        self.llm.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\EmbeddingsEmbeddingBackend")]
pub struct EmbeddingsEmbeddingBackend {}

#[php_impl]
impl EmbeddingsEmbeddingBackend {
    pub const ONNX: &str = "onnx";
    pub const STATIC_: &str = "static";
}

#[php_class]
#[php(name = "Xberg\\EntityCategory")]
pub struct EntityCategory {}

#[php_impl]
impl EntityCategory {
    pub const PERSON: &str = "person";
    pub const ORGANIZATION: &str = "organization";
    pub const LOCATION: &str = "location";
    pub const DATE: &str = "date";
    pub const TIME: &str = "time";
    pub const MONEY: &str = "money";
    pub const PERCENT: &str = "percent";
    pub const EMAIL: &str = "email";
    pub const PHONE: &str = "phone";
    pub const URL: &str = "url";
    pub const CUSTOM: &str = "custom";
}

#[php_class]
#[php(name = "Xberg\\ExecutionProviderType")]
pub struct ExecutionProviderType {}

#[php_impl]
impl ExecutionProviderType {
    pub const AUTO: &str = "auto";
    pub const CPU: &str = "cpu";
    pub const COREML: &str = "coreml";
    pub const CUDA: &str = "cuda";
    pub const TENSORRT: &str = "tensorrt";
}

#[php_class]
#[php(name = "Xberg\\ExtractInputKind")]
pub struct ExtractInputKind {}

#[php_impl]
impl ExtractInputKind {
    pub const BYTES: &str = "bytes";
    pub const URI: &str = "uri";
}

#[php_class]
#[php(name = "Xberg\\ExtractionMethod")]
pub struct ExtractionMethod {}

#[php_impl]
impl ExtractionMethod {
    pub const NATIVE: &str = "native";
    pub const OCR: &str = "ocr";
    pub const MIXED: &str = "mixed";
}

#[php_class]
#[php(name = "Xberg\\FormFieldType")]
pub struct FormFieldType {}

#[php_impl]
impl FormFieldType {
    pub const TEXT: &str = "text";
    pub const CHECKBOX: &str = "checkbox";
    pub const RADIO: &str = "radio";
    pub const CHOICE: &str = "choice";
    pub const SIGNATURE: &str = "signature";
    pub const BUTTON: &str = "button";
    pub const UNKNOWN: &str = "unknown";
}

#[php_class]
#[php(name = "Xberg\\FormatMetadata")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct FormatMetadata {
    #[serde(rename = "format_type")]
    format_type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub pdf: Option<PdfMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub docx: Option<DocxMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub excel: Option<ExcelMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub email: Option<EmailMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub pptx: Option<PptxMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub archive: Option<ArchiveMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub image: Option<ImageMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub xml: Option<XmlMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub text: Option<TextMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub html: Option<HtmlMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub ocr: Option<OcrMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub csv: Option<CsvMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub bibtex: Option<BibtexMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub citation: Option<CitationMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub fiction_book: Option<FictionBookMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub dbf: Option<DbfMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub jats: Option<JatsMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub epub: Option<EpubMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub pst: Option<PstMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub audio: Option<AudioMetadata>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub code: Option<CodeMetadata>,
}

#[php_impl]
impl FormatMetadata {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.format_type_tag.as_str(),
            "pdf"
                | "docx"
                | "excel"
                | "email"
                | "pptx"
                | "archive"
                | "image"
                | "xml"
                | "text"
                | "html"
                | "ocr"
                | "csv"
                | "bibtex"
                | "citation"
                | "fiction_book"
                | "dbf"
                | "jats"
                | "epub"
                | "pst"
                | "audio"
                | "code"
        ) {
            return Err(PhpException::default(format!(
                "Invalid FormatMetadata tag: {}",
                value.format_type_tag,
            )));
        }
        Ok(value)
    }

    #[php(getter)]
    pub fn get_format_type_tag(&self) -> String {
        self.format_type_tag.clone()
    }

    #[php(getter)]
    pub fn get_pdf(&self) -> Option<PdfMetadata> {
        self.pdf.clone()
    }

    #[php(getter)]
    pub fn get_docx(&self) -> Option<DocxMetadata> {
        self.docx.clone()
    }

    #[php(getter)]
    pub fn get_excel(&self) -> Option<ExcelMetadata> {
        self.excel.clone()
    }

    #[php(getter)]
    pub fn get_email(&self) -> Option<EmailMetadata> {
        self.email.clone()
    }

    #[php(getter)]
    pub fn get_pptx(&self) -> Option<PptxMetadata> {
        self.pptx.clone()
    }

    #[php(getter)]
    pub fn get_archive(&self) -> Option<ArchiveMetadata> {
        self.archive.clone()
    }

    #[php(getter)]
    pub fn get_image(&self) -> Option<ImageMetadata> {
        self.image.clone()
    }

    #[php(getter)]
    pub fn get_xml(&self) -> Option<XmlMetadata> {
        self.xml.clone()
    }

    #[php(getter)]
    pub fn get_text(&self) -> Option<TextMetadata> {
        self.text.clone()
    }

    #[php(getter)]
    pub fn get_html(&self) -> Option<HtmlMetadata> {
        self.html.clone()
    }

    #[php(getter)]
    pub fn get_ocr(&self) -> Option<OcrMetadata> {
        self.ocr.clone()
    }

    #[php(getter)]
    pub fn get_csv(&self) -> Option<CsvMetadata> {
        self.csv.clone()
    }

    #[php(getter)]
    pub fn get_bibtex(&self) -> Option<BibtexMetadata> {
        self.bibtex.clone()
    }

    #[php(getter)]
    pub fn get_citation(&self) -> Option<CitationMetadata> {
        self.citation.clone()
    }

    #[php(getter)]
    pub fn get_fiction_book(&self) -> Option<FictionBookMetadata> {
        self.fiction_book.clone()
    }

    #[php(getter)]
    pub fn get_dbf(&self) -> Option<DbfMetadata> {
        self.dbf.clone()
    }

    #[php(getter)]
    pub fn get_jats(&self) -> Option<JatsMetadata> {
        self.jats.clone()
    }

    #[php(getter)]
    pub fn get_epub(&self) -> Option<EpubMetadata> {
        self.epub.clone()
    }

    #[php(getter)]
    pub fn get_pst(&self) -> Option<PstMetadata> {
        self.pst.clone()
    }

    #[php(getter)]
    pub fn get_audio(&self) -> Option<AudioMetadata> {
        self.audio.clone()
    }

    #[php(getter)]
    pub fn get_code(&self) -> Option<CodeMetadata> {
        self.code.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\FormulaModel")]
pub struct FormulaModel {}

#[php_impl]
impl FormulaModel {
    pub const LATEXOCR: &str = "latex_ocr";
}

#[php_class]
#[php(name = "Xberg\\GlmOcrLayoutMode")]
pub struct GlmOcrLayoutMode {}

#[php_impl]
impl GlmOcrLayoutMode {
    pub const WHOLEPAGE: &str = "whole_page";
    pub const PAIRED: &str = "paired";
}

#[php_class]
#[php(name = "Xberg\\GlmOcrTaskKind")]
pub struct GlmOcrTaskKind {}

#[php_impl]
impl GlmOcrTaskKind {
    pub const OCR: &str = "ocr";
    pub const TABLE: &str = "table";
    pub const FORMULA: &str = "formula";
    pub const CHART: &str = "chart";
    pub const CAPTION: &str = "caption";
}

#[php_class]
#[php(name = "Xberg\\HeadingStyle")]
pub struct HeadingStyle {}

#[php_impl]
impl HeadingStyle {
    pub const UNDERLINED: &str = "Underlined";
    pub const ATX: &str = "Atx";
    pub const ATXCLOSED: &str = "AtxClosed";
}

#[php_class]
#[php(name = "Xberg\\HighlightStyle")]
pub struct HighlightStyle {}

#[php_impl]
impl HighlightStyle {
    pub const DOUBLEEQUAL: &str = "DoubleEqual";
    pub const HTML: &str = "Html";
    pub const BOLD: &str = "Bold";
    pub const NONE: &str = "None";
}

#[php_class]
#[php(name = "Xberg\\HostMatcher")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct HostMatcher {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

#[php_impl]
impl HostMatcher {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "exact" | "suffix" | "cidr") {
            return Err(PhpException::default(format!(
                "Invalid HostMatcher tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "exact")]
    pub fn _factory_exact(value: String) -> Self {
        crawlberg::HostMatcher::Exact { value }.into()
    }

    #[php(name = "suffix")]
    pub fn _factory_suffix(value: String) -> Self {
        crawlberg::HostMatcher::Suffix { value }.into()
    }

    #[php(name = "cidr")]
    pub fn _factory_cidr(value: String) -> Self {
        crawlberg::HostMatcher::Cidr { value }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_value(&self) -> Option<String> {
        self.value.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\HtmlTheme")]
pub struct HtmlTheme {}

#[php_impl]
impl HtmlTheme {
    pub const DEFAULT_: &str = "default";
    pub const GITHUB: &str = "github";
    pub const DARK: &str = "dark";
    pub const LIGHT: &str = "light";
    pub const UNSTYLED: &str = "unstyled";
}

#[php_class]
#[php(name = "Xberg\\HtmlToMarkdownRsOutputFormat")]
pub struct HtmlToMarkdownRsOutputFormat {}

#[php_impl]
impl HtmlToMarkdownRsOutputFormat {
    pub const MARKDOWN: &str = "Markdown";
    pub const DJOT: &str = "Djot";
    pub const PLAIN: &str = "Plain";
}

#[php_class]
#[php(name = "Xberg\\ImageKind")]
pub struct ImageKind {}

#[php_impl]
impl ImageKind {
    pub const PHOTOGRAPH: &str = "photograph";
    pub const DIAGRAM: &str = "diagram";
    pub const CHART: &str = "chart";
    pub const DRAWING: &str = "drawing";
    pub const TEXTBLOCK: &str = "text_block";
    pub const DECORATION: &str = "decoration";
    pub const LOGO: &str = "logo";
    pub const ICON: &str = "icon";
    pub const TILEFRAGMENT: &str = "tile_fragment";
    pub const MASK: &str = "mask";
    pub const PAGERASTER: &str = "page_raster";
    pub const UNKNOWN: &str = "unknown";
}

#[php_class]
#[php(name = "Xberg\\ImageOutputFormat")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct ImageOutputFormat {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub quality: Option<u8>,
}

#[php_impl]
impl ImageOutputFormat {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.type_tag.as_str(),
            "native" | "png" | "jpeg" | "webp" | "heif" | "svg"
        ) {
            return Err(PhpException::default(format!(
                "Invalid ImageOutputFormat tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "jpeg")]
    pub fn _factory_jpeg(quality: u8) -> Self {
        xberg::core::config::extraction::ImageOutputFormat::Jpeg { quality }.into()
    }

    #[php(name = "webp")]
    pub fn _factory_webp(quality: u8) -> Self {
        xberg::core::config::extraction::ImageOutputFormat::Webp { quality }.into()
    }

    #[php(name = "heif")]
    pub fn _factory_heif(quality: u8) -> Self {
        xberg::core::config::extraction::ImageOutputFormat::Heif { quality }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_quality(&self) -> Option<u8> {
        self.quality
    }
}

#[php_class]
#[php(name = "Xberg\\ImageType")]
pub struct ImageType {}

#[php_impl]
impl ImageType {
    pub const DATAURI: &str = "data-uri";
    pub const INLINESVG: &str = "inline-svg";
    pub const EXTERNAL: &str = "external";
    pub const RELATIVE: &str = "relative";
}

#[php_class]
#[php(name = "Xberg\\InlineType")]
pub struct InlineType {}

#[php_impl]
impl InlineType {
    pub const TEXT: &str = "text";
    pub const STRONG: &str = "strong";
    pub const EMPHASIS: &str = "emphasis";
    pub const HIGHLIGHT: &str = "highlight";
    pub const SUBSCRIPT: &str = "subscript";
    pub const SUPERSCRIPT: &str = "superscript";
    pub const INSERT: &str = "insert";
    pub const DELETE: &str = "delete";
    pub const CODE: &str = "code";
    pub const LINK: &str = "link";
    pub const IMAGE: &str = "image";
    pub const SPAN: &str = "span";
    pub const MATH: &str = "math";
    pub const RAWINLINE: &str = "raw_inline";
    pub const FOOTNOTEREF: &str = "footnote_ref";
    pub const SYMBOL: &str = "symbol";
}

#[php_class]
#[php(name = "Xberg\\JupyterCellRendering")]
pub struct JupyterCellRendering {}

#[php_impl]
impl JupyterCellRendering {
    pub const SOURCE: &str = "source";
    pub const OUTPUTS: &str = "outputs";
    pub const BOTH: &str = "both";
}

#[php_class]
#[php(name = "Xberg\\KeywordAlgorithm")]
pub struct KeywordAlgorithm {}

#[php_impl]
impl KeywordAlgorithm {
    pub const YAKE: &str = "yake";
    pub const RAKE: &str = "rake";
}

#[php_class]
#[php(name = "Xberg\\LateInteractionModelType")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct LateInteractionModelType {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_file: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub additional_files: Option<Vec<String>>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub max_length: Option<i64>,
}

#[php_impl]
impl LateInteractionModelType {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "preset" | "custom" | "plugin") {
            return Err(PhpException::default(format!(
                "Invalid LateInteractionModelType tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "preset")]
    pub fn _factory_preset(name: String) -> Self {
        xberg::LateInteractionModelType::Preset { name }.into()
    }

    #[php(name = "custom")]
    pub fn _factory_custom(
        modelId: String,
        modelFile: Option<String>,
        additionalFiles: Vec<String>,
        maxLength: Option<i64>,
    ) -> Self {
        xberg::LateInteractionModelType::Custom {
            model_id: modelId,
            model_file: modelFile,
            additional_files: additionalFiles,
            max_length: maxLength,
        }
        .into()
    }

    #[php(name = "plugin")]
    pub fn _factory_plugin(name: String) -> Self {
        xberg::LateInteractionModelType::Plugin { name }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    #[php(getter)]
    pub fn get_model_id(&self) -> Option<String> {
        self.model_id.clone()
    }

    #[php(getter)]
    pub fn get_model_file(&self) -> Option<String> {
        self.model_file.clone()
    }

    #[php(getter)]
    pub fn get_additional_files(&self) -> Option<Vec<String>> {
        self.additional_files.clone()
    }

    #[php(getter)]
    pub fn get_max_length(&self) -> Option<i64> {
        self.max_length
    }
}

#[php_class]
#[php(name = "Xberg\\LayoutClass")]
pub struct LayoutClass {}

#[php_impl]
impl LayoutClass {
    pub const CAPTION: &str = "caption";
    pub const CHART: &str = "chart";
    pub const FOOTNOTE: &str = "footnote";
    pub const FORMULA: &str = "formula";
    pub const LISTITEM: &str = "list_item";
    pub const PAGEFOOTER: &str = "page_footer";
    pub const PAGEHEADER: &str = "page_header";
    pub const PICTURE: &str = "picture";
    pub const SECTIONHEADER: &str = "section_header";
    pub const TABLE: &str = "table";
    pub const TEXT: &str = "text";
    pub const TITLE: &str = "title";
    pub const DOCUMENTINDEX: &str = "document_index";
    pub const CODE: &str = "code";
    pub const CHECKBOXSELECTED: &str = "checkbox_selected";
    pub const CHECKBOXUNSELECTED: &str = "checkbox_unselected";
    pub const FORM: &str = "form";
    pub const KEYVALUEREGION: &str = "key_value_region";
}

#[php_class]
#[php(name = "Xberg\\LayoutStrategy")]
pub struct LayoutStrategy {}

#[php_impl]
impl LayoutStrategy {
    pub const ALWAYS: &str = "always";
    pub const AUTO: &str = "auto";
}

#[php_class]
#[php(name = "Xberg\\LinkStyle")]
pub struct LinkStyle {}

#[php_impl]
impl LinkStyle {
    pub const INLINE: &str = "Inline";
    pub const REFERENCE: &str = "Reference";
}

#[php_class]
#[php(name = "Xberg\\LinkType")]
pub struct LinkType {}

#[php_impl]
impl LinkType {
    pub const ANCHOR: &str = "anchor";
    pub const INTERNAL: &str = "internal";
    pub const EXTERNAL: &str = "external";
    pub const EMAIL: &str = "email";
    pub const PHONE: &str = "phone";
    pub const OTHER: &str = "other";
}

#[php_class]
#[php(name = "Xberg\\ListIndentType")]
pub struct ListIndentType {}

#[php_impl]
impl ListIndentType {
    pub const SPACES: &str = "Spaces";
    pub const TABS: &str = "Tabs";
}

#[php_class]
#[php(name = "Xberg\\ListType")]
pub struct ListType {}

#[php_impl]
impl ListType {
    pub const BULLET: &str = "Bullet";
    pub const NUMBERED: &str = "Numbered";
    pub const LETTERED: &str = "Lettered";
    pub const INDENTED: &str = "Indented";
}

#[php_class]
#[php(name = "Xberg\\MergeMode")]
pub struct MergeMode {}

#[php_impl]
impl MergeMode {
    pub const OBJECTMERGE: &str = "object_merge";
    pub const ARRAYCONCAT: &str = "array_concat";
    pub const OBJECTFIRST: &str = "object_first";
}

#[php_class]
#[php(name = "Xberg\\MimeDetectionPolicy")]
pub struct MimeDetectionPolicy {}

#[php_impl]
impl MimeDetectionPolicy {
    pub const PREFERCONTENT: &str = "prefer_content";
    pub const TRUSTEXTENSION: &str = "trust_extension";
    pub const CONTENTONLY: &str = "content_only";
}

#[php_class]
#[php(name = "Xberg\\NerBackendKind")]
pub struct NerBackendKind {}

#[php_impl]
impl NerBackendKind {
    pub const ONNX: &str = "onnx";
    pub const LLM: &str = "llm";
}

#[php_class]
#[php(name = "Xberg\\NewlineStyle")]
pub struct NewlineStyle {}

#[php_impl]
impl NewlineStyle {
    pub const SPACES: &str = "Spaces";
    pub const BACKSLASH: &str = "Backslash";
}

#[php_class]
#[php(name = "Xberg\\NoChunkingReason")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct NoChunkingReason {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub size_bytes: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub threshold_bytes: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub page_count: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub threshold: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub text_coverage: Option<f32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub avg_chars_per_page: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub mime_type: Option<String>,
}

#[php_impl]
impl NoChunkingReason {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.type_tag.as_str(),
            "SmallFile"
                | "FewPages"
                | "TextLayerDetected"
                | "FormatNotChunkable"
                | "ChunkingDisabled"
                | "FastTextExtraction"
        ) {
            return Err(PhpException::default(format!(
                "Invalid NoChunkingReason tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "smallFile")]
    pub fn _factory_small_file(sizeBytes: i64, thresholdBytes: i64) -> Self {
        xberg::NoChunkingReason::SmallFile {
            size_bytes: sizeBytes as u64,
            threshold_bytes: thresholdBytes as u64,
        }
        .into()
    }

    #[php(name = "fewPages")]
    pub fn _factory_few_pages(pageCount: u32, threshold: u32) -> Self {
        xberg::NoChunkingReason::FewPages {
            page_count: pageCount,
            threshold,
        }
        .into()
    }

    #[php(name = "textLayerDetected")]
    pub fn _factory_text_layer_detected(textCoverage: f32, avgCharsPerPage: u32) -> Self {
        xberg::NoChunkingReason::TextLayerDetected {
            text_coverage: textCoverage,
            avg_chars_per_page: avgCharsPerPage,
        }
        .into()
    }

    #[php(name = "formatNotChunkable")]
    pub fn _factory_format_not_chunkable(mimeType: String) -> Self {
        xberg::NoChunkingReason::FormatNotChunkable { mime_type: mimeType }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_size_bytes(&self) -> Option<i64> {
        self.size_bytes
    }

    #[php(getter)]
    pub fn get_threshold_bytes(&self) -> Option<i64> {
        self.threshold_bytes
    }

    #[php(getter)]
    pub fn get_page_count(&self) -> Option<u32> {
        self.page_count
    }

    #[php(getter)]
    pub fn get_threshold(&self) -> Option<u32> {
        self.threshold
    }

    #[php(getter)]
    pub fn get_text_coverage(&self) -> Option<f32> {
        self.text_coverage
    }

    #[php(getter)]
    pub fn get_avg_chars_per_page(&self) -> Option<u32> {
        self.avg_chars_per_page
    }

    #[php(getter)]
    pub fn get_mime_type(&self) -> Option<String> {
        self.mime_type.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\NodeContent")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct NodeContent {
    #[serde(rename = "node_type")]
    node_type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub level: Option<u8>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub ordered: Option<bool>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub grid: Option<TableGrid>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub image_index: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub src: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub language: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub heading_level: Option<u8>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub heading_text: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub number: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub term: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub kind: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub format: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub entries: Option<Vec<KeyValueAttribute>>,
}

#[php_impl]
impl NodeContent {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.node_type_tag.as_str(),
            "title"
                | "heading"
                | "paragraph"
                | "list"
                | "list_item"
                | "table"
                | "image"
                | "code"
                | "quote"
                | "formula"
                | "footnote"
                | "comment"
                | "group"
                | "page_break"
                | "slide"
                | "definition_list"
                | "definition_item"
                | "citation"
                | "admonition"
                | "raw_block"
                | "metadata_block"
        ) {
            return Err(PhpException::default(format!(
                "Invalid NodeContent tag: {}",
                value.node_type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "title")]
    pub fn _factory_title(text: String) -> Self {
        xberg::NodeContent::Title { text }.into()
    }

    #[php(name = "heading")]
    pub fn _factory_heading(level: u8, text: String) -> Self {
        xberg::NodeContent::Heading { level, text }.into()
    }

    #[php(name = "paragraph")]
    pub fn _factory_paragraph(text: String) -> Self {
        xberg::NodeContent::Paragraph { text }.into()
    }

    #[php(name = "list")]
    pub fn _factory_list(ordered: bool) -> Self {
        xberg::NodeContent::List { ordered }.into()
    }

    #[php(name = "listItem")]
    pub fn _factory_list_item(text: String) -> Self {
        xberg::NodeContent::ListItem { text }.into()
    }

    #[php(name = "table")]
    pub fn _factory_table(grid: &TableGrid) -> Self {
        xberg::NodeContent::Table {
            grid: grid.clone().into(),
        }
        .into()
    }

    #[php(name = "image")]
    pub fn _factory_image(description: Option<String>, imageIndex: Option<u32>, src: Option<String>) -> Self {
        xberg::NodeContent::Image {
            description,
            image_index: imageIndex,
            src,
        }
        .into()
    }

    #[php(name = "code")]
    pub fn _factory_code(text: String, language: Option<String>) -> Self {
        xberg::NodeContent::Code { text, language }.into()
    }

    #[php(name = "formula")]
    pub fn _factory_formula(text: String) -> Self {
        xberg::NodeContent::Formula { text }.into()
    }

    #[php(name = "footnote")]
    pub fn _factory_footnote(text: String) -> Self {
        xberg::NodeContent::Footnote { text }.into()
    }

    #[php(name = "comment")]
    pub fn _factory_comment(text: String) -> Self {
        xberg::NodeContent::Comment { text }.into()
    }

    #[php(name = "group")]
    pub fn _factory_group(label: Option<String>, headingLevel: Option<u8>, headingText: Option<String>) -> Self {
        xberg::NodeContent::Group {
            label,
            heading_level: headingLevel,
            heading_text: headingText,
        }
        .into()
    }

    #[php(name = "slide")]
    pub fn _factory_slide(number: u32, title: Option<String>) -> Self {
        xberg::NodeContent::Slide { number, title }.into()
    }

    #[php(name = "definitionItem")]
    pub fn _factory_definition_item(term: String, definition: String) -> Self {
        xberg::NodeContent::DefinitionItem { term, definition }.into()
    }

    #[php(name = "citation")]
    pub fn _factory_citation(key: String, text: String) -> Self {
        xberg::NodeContent::Citation { key, text }.into()
    }

    #[php(name = "admonition")]
    pub fn _factory_admonition(kind: String, title: Option<String>) -> Self {
        xberg::NodeContent::Admonition { kind, title }.into()
    }

    #[php(name = "rawBlock")]
    pub fn _factory_raw_block(format: String, content: String) -> Self {
        xberg::NodeContent::RawBlock { format, content }.into()
    }

    #[php(name = "metadataBlock")]
    pub fn _factory_metadata_block(entries: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut entries_core_result: Vec<xberg::KeyValueAttribute> = Vec::new();
        for (_, item) in entries.iter() {
            if let Some(parsed) = <&KeyValueAttribute as ext_php_rs::convert::FromZval>::from_zval(item) {
                entries_core_result.push(parsed.clone().into());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to KeyValueAttribute".to_string(),
                ));
            }
        }
        let entries_core: Vec<xberg::KeyValueAttribute> = entries_core_result;
        Ok(xberg::NodeContent::MetadataBlock { entries: entries_core }.into())
    }

    #[php(getter)]
    pub fn get_node_type_tag(&self) -> String {
        self.node_type_tag.clone()
    }

    #[php(getter)]
    pub fn get_text(&self) -> Option<String> {
        self.text.clone()
    }

    #[php(getter)]
    pub fn get_level(&self) -> Option<u8> {
        self.level
    }

    #[php(getter)]
    pub fn get_ordered(&self) -> Option<bool> {
        self.ordered
    }

    #[php(getter)]
    pub fn get_grid(&self) -> Option<TableGrid> {
        self.grid.clone()
    }

    #[php(getter)]
    pub fn get_description(&self) -> Option<String> {
        self.description.clone()
    }

    #[php(getter)]
    pub fn get_image_index(&self) -> Option<u32> {
        self.image_index
    }

    #[php(getter)]
    pub fn get_src(&self) -> Option<String> {
        self.src.clone()
    }

    #[php(getter)]
    pub fn get_language(&self) -> Option<String> {
        self.language.clone()
    }

    #[php(getter)]
    pub fn get_label(&self) -> Option<String> {
        self.label.clone()
    }

    #[php(getter)]
    pub fn get_heading_level(&self) -> Option<u8> {
        self.heading_level
    }

    #[php(getter)]
    pub fn get_heading_text(&self) -> Option<String> {
        self.heading_text.clone()
    }

    #[php(getter)]
    pub fn get_number(&self) -> Option<u32> {
        self.number
    }

    #[php(getter)]
    pub fn get_title(&self) -> Option<String> {
        self.title.clone()
    }

    #[php(getter)]
    pub fn get_term(&self) -> Option<String> {
        self.term.clone()
    }

    #[php(getter)]
    pub fn get_definition(&self) -> Option<String> {
        self.definition.clone()
    }

    #[php(getter)]
    pub fn get_key(&self) -> Option<String> {
        self.key.clone()
    }

    #[php(getter)]
    pub fn get_kind(&self) -> Option<String> {
        self.kind.clone()
    }

    #[php(getter)]
    pub fn get_format(&self) -> Option<String> {
        self.format.clone()
    }

    #[php(getter)]
    pub fn get_content(&self) -> Option<String> {
        self.content.clone()
    }

    #[php(getter)]
    pub fn get_entries(&self) -> Option<Vec<KeyValueAttribute>> {
        self.entries.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\OcrBackendType")]
pub struct OcrBackendType {}

#[php_impl]
impl OcrBackendType {
    pub const TESSERACT: &str = "Tesseract";
    pub const PADDLEOCR: &str = "PaddleOCR";
    pub const CANDLE: &str = "Candle";
    pub const CUSTOM: &str = "Custom";
}

#[php_class]
#[php(name = "Xberg\\OcrBoundingGeometry")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct OcrBoundingGeometry {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub left: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub top: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub width: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub height: Option<u32>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub points: Option<Vec<OcrPoint>>,
}

#[php_impl]
impl OcrBoundingGeometry {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "rectangle" | "quadrilateral") {
            return Err(PhpException::default(format!(
                "Invalid OcrBoundingGeometry tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "rectangle")]
    pub fn _factory_rectangle(left: u32, top: u32, width: u32, height: u32) -> Self {
        xberg::OcrBoundingGeometry::Rectangle {
            left,
            top,
            width,
            height,
        }
        .into()
    }

    #[php(name = "quadrilateral")]
    pub fn _factory_quadrilateral(points: &ext_php_rs::types::ZendHashTable) -> PhpResult<Self> {
        let mut points_core_result: Vec<xberg::OcrPoint> = Vec::new();
        for (_, item) in points.iter() {
            if let Some(parsed) = <&OcrPoint as ext_php_rs::convert::FromZval>::from_zval(item) {
                points_core_result.push(parsed.clone().into());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to OcrPoint".to_string(),
                ));
            }
        }
        let points_core: Vec<xberg::OcrPoint> = points_core_result;
        Ok(xberg::OcrBoundingGeometry::Quadrilateral { points: points_core }.into())
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_left(&self) -> Option<u32> {
        self.left
    }

    #[php(getter)]
    pub fn get_top(&self) -> Option<u32> {
        self.top
    }

    #[php(getter)]
    pub fn get_width(&self) -> Option<u32> {
        self.width
    }

    #[php(getter)]
    pub fn get_height(&self) -> Option<u32> {
        self.height
    }

    #[php(getter)]
    pub fn get_points(&self) -> Option<Vec<OcrPoint>> {
        self.points.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\OcrElementLevel")]
pub struct OcrElementLevel {}

#[php_impl]
impl OcrElementLevel {
    pub const WORD: &str = "word";
    pub const LINE: &str = "line";
    pub const BLOCK: &str = "block";
    pub const PAGE: &str = "page";
}

#[php_class]
#[php(name = "Xberg\\OcrStrategy")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct OcrStrategy {
    #[serde(rename = "mode")]
    mode_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub min_confidence: Option<f64>,
}

#[php_impl]
impl OcrStrategy {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.mode_tag.as_str(), "auto" | "scanned_pages") {
            return Err(PhpException::default(format!(
                "Invalid OcrStrategy tag: {}",
                value.mode_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "scannedPages")]
    pub fn _factory_scanned_pages(minConfidence: f64) -> Self {
        xberg::OcrStrategy::ScannedPages {
            min_confidence: minConfidence,
        }
        .into()
    }

    #[php(getter)]
    pub fn get_mode_tag(&self) -> String {
        self.mode_tag.clone()
    }

    #[php(getter)]
    pub fn get_min_confidence(&self) -> Option<f64> {
        self.min_confidence
    }
}

#[php_class]
#[php(name = "Xberg\\OutputFormat")]
pub struct OutputFormat {}

#[php_impl]
impl OutputFormat {
    pub const PLAIN: &str = "plain";
    pub const MARKDOWN: &str = "markdown";
    pub const DJOT: &str = "djot";
    pub const HTML: &str = "html";
    pub const JSON: &str = "json";
    pub const DOCTAGS: &str = "doctags";
    pub const CUSTOM: &str = "custom";
}

#[php_class]
#[php(name = "Xberg\\PSMMode")]
pub struct PSMMode {}

#[php_impl]
impl PSMMode {
    pub const OSDONLY: &str = "OsdOnly";
    pub const AUTOOSD: &str = "AutoOsd";
    pub const AUTOONLY: &str = "AutoOnly";
    pub const AUTO: &str = "Auto";
    pub const SINGLECOLUMN: &str = "SingleColumn";
    pub const SINGLEBLOCKVERTICAL: &str = "SingleBlockVertical";
    pub const SINGLEBLOCK: &str = "SingleBlock";
    pub const SINGLELINE: &str = "SingleLine";
    pub const SINGLEWORD: &str = "SingleWord";
    pub const CIRCLEWORD: &str = "CircleWord";
    pub const SINGLECHAR: &str = "SingleChar";
}

#[php_class]
#[php(name = "Xberg\\PaddleInferenceBackend")]
pub struct PaddleInferenceBackend {}

#[php_impl]
impl PaddleInferenceBackend {
    pub const ORT: &str = "ort";
    pub const TRACT: &str = "tract";
}

#[php_class]
#[php(name = "Xberg\\PaddleLanguage")]
pub struct PaddleLanguage {}

#[php_impl]
impl PaddleLanguage {
    pub const ENGLISH: &str = "English";
    pub const CHINESE: &str = "Chinese";
    pub const JAPANESE: &str = "Japanese";
    pub const KOREAN: &str = "Korean";
    pub const GERMAN: &str = "German";
    pub const FRENCH: &str = "French";
    pub const LATIN: &str = "Latin";
    pub const CYRILLIC: &str = "Cyrillic";
    pub const TRADITIONALCHINESE: &str = "TraditionalChinese";
    pub const THAI: &str = "Thai";
    pub const GREEK: &str = "Greek";
    pub const EASTSLAVIC: &str = "EastSlavic";
    pub const ARABIC: &str = "Arabic";
    pub const DEVANAGARI: &str = "Devanagari";
    pub const TAMIL: &str = "Tamil";
    pub const TELUGU: &str = "Telugu";
}

#[php_class]
#[php(name = "Xberg\\PaddleOcrVlTaskKind")]
pub struct PaddleOcrVlTaskKind {}

#[php_impl]
impl PaddleOcrVlTaskKind {
    pub const OCR: &str = "ocr";
    pub const TABLE: &str = "table";
    pub const FORMULA: &str = "formula";
    pub const CHART: &str = "chart";
}

#[php_class]
#[php(name = "Xberg\\PageOrientationHandling")]
pub struct PageOrientationHandling {}

#[php_impl]
impl PageOrientationHandling {
    pub const SELFCORRECTING: &str = "SelfCorrecting";
    pub const RECOGNISESROTATEDTEXT: &str = "RecognisesRotatedText";
    pub const REQUIRESUPRIGHT: &str = "RequiresUpright";
}

#[php_class]
#[php(name = "Xberg\\PageUnitType")]
pub struct PageUnitType {}

#[php_impl]
impl PageUnitType {
    pub const PAGE: &str = "page";
    pub const SLIDE: &str = "slide";
    pub const SHEET: &str = "sheet";
}

#[php_class]
#[php(name = "Xberg\\PdfAnnotationType")]
pub struct PdfAnnotationType {}

#[php_impl]
impl PdfAnnotationType {
    pub const TEXT: &str = "text";
    pub const HIGHLIGHT: &str = "highlight";
    pub const LINK: &str = "link";
    pub const STAMP: &str = "stamp";
    pub const UNDERLINE: &str = "underline";
    pub const STRIKEOUT: &str = "strike_out";
    pub const SQUIGGLY: &str = "squiggly";
    pub const INK: &str = "ink";
    pub const SQUARE: &str = "square";
    pub const CIRCLE: &str = "circle";
    pub const POLYGON: &str = "polygon";
    pub const POLYLINE: &str = "poly_line";
    pub const LINE: &str = "line";
    pub const CARET: &str = "caret";
    pub const FILEATTACHMENT: &str = "file_attachment";
    pub const SOUND: &str = "sound";
    pub const MOVIE: &str = "movie";
    pub const OTHER: &str = "other";
}

#[php_class]
#[php(name = "Xberg\\PdfBackend")]
pub struct PdfBackend {}

#[php_impl]
impl PdfBackend {
    pub const NATIVE: &str = "native";
    pub const PDFIUM: &str = "pdfium";
}

#[php_class]
#[php(name = "Xberg\\PiiCategory")]
pub struct PiiCategory {}

#[php_impl]
impl PiiCategory {
    pub const EMAIL: &str = "email";
    pub const PHONE: &str = "phone";
    pub const SSN: &str = "ssn";
    pub const CREDITCARD: &str = "credit_card";
    pub const POSTALCODE: &str = "postal_code";
    pub const IPADDRESS: &str = "ip_address";
    pub const IBAN: &str = "iban";
    pub const SWIFTBIC: &str = "swift_bic";
    pub const DATEOFBIRTH: &str = "date_of_birth";
    pub const PERSON: &str = "person";
    pub const ORGANIZATION: &str = "organization";
    pub const LOCATION: &str = "location";
    pub const CUSTOM: &str = "custom";
}

#[php_class]
#[php(name = "Xberg\\PreprocessingPreset")]
pub struct PreprocessingPreset {}

#[php_impl]
impl PreprocessingPreset {
    pub const MINIMAL: &str = "Minimal";
    pub const STANDARD: &str = "Standard";
    pub const AGGRESSIVE: &str = "Aggressive";
}

#[php_class]
#[php(name = "Xberg\\PresetCategory")]
pub struct PresetCategory {}

#[php_impl]
impl PresetCategory {
    pub const FINANCE: &str = "finance";
    pub const IDENTITY: &str = "identity";
    pub const LEGAL: &str = "legal";
    pub const LOGISTICS: &str = "logistics";
    pub const MEDICAL: &str = "medical";
    pub const HR: &str = "hr";
    pub const OTHER: &str = "other";
}

#[php_class]
#[php(name = "Xberg\\ProbeStatus")]
pub struct ProbeStatus {}

#[php_impl]
impl ProbeStatus {
    pub const PASS: &str = "pass";
    pub const WARN: &str = "warn";
    pub const FAIL: &str = "fail";
    pub const SKIP: &str = "skip";
}

#[php_class]
#[php(name = "Xberg\\ProcessingStage")]
pub struct ProcessingStage {}

#[php_impl]
impl ProcessingStage {
    pub const EARLY: &str = "Early";
    pub const MIDDLE: &str = "Middle";
    pub const LATE: &str = "Late";
}

#[php_class]
#[php(name = "Xberg\\RedactionStrategy")]
pub struct RedactionStrategy {}

#[php_impl]
impl RedactionStrategy {
    pub const MASK: &str = "mask";
    pub const HASH: &str = "hash";
    pub const TOKENREPLACE: &str = "token_replace";
    pub const DROP: &str = "drop";
}

#[php_class]
#[php(name = "Xberg\\ReductionLevel")]
pub struct ReductionLevel {}

#[php_impl]
impl ReductionLevel {
    pub const OFF: &str = "Off";
    pub const LIGHT: &str = "Light";
    pub const MODERATE: &str = "Moderate";
    pub const AGGRESSIVE: &str = "Aggressive";
    pub const MAXIMUM: &str = "Maximum";
}

#[php_class]
#[php(name = "Xberg\\RegionKind")]
pub struct RegionKind {}

#[php_impl]
impl RegionKind {
    pub const FIGURE: &str = "Figure";
    pub const DENSETABLE: &str = "DenseTable";
    pub const COMPLEXLAYOUT: &str = "ComplexLayout";
    pub const CAPTION: &str = "Caption";
}

#[php_class]
#[php(name = "Xberg\\RelationshipKind")]
pub struct RelationshipKind {}

#[php_impl]
impl RelationshipKind {
    pub const FOOTNOTEREFERENCE: &str = "footnote_reference";
    pub const CITATIONREFERENCE: &str = "citation_reference";
    pub const INTERNALLINK: &str = "internal_link";
    pub const CAPTION: &str = "caption";
    pub const LABEL: &str = "label";
    pub const TOCENTRY: &str = "toc_entry";
    pub const CROSSREFERENCE: &str = "cross_reference";
}

#[php_class]
#[php(name = "Xberg\\RerankerHead")]
pub struct RerankerHead {}

#[php_impl]
impl RerankerHead {
    pub const CROSSENCODER: &str = "cross_encoder";
    pub const QWEN3GENERATIVE: &str = "qwen3_generative";
}

#[php_class]
#[php(name = "Xberg\\RerankerModelType")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct RerankerModelType {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_file: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub additional_files: Option<Vec<String>>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub max_length: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub head: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub llm: Option<LlmConfig>,
}

#[php_impl]
impl RerankerModelType {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "preset" | "custom" | "llm" | "plugin") {
            return Err(PhpException::default(format!(
                "Invalid RerankerModelType tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "preset")]
    pub fn _factory_preset(name: String) -> Self {
        xberg::RerankerModelType::Preset { name }.into()
    }

    #[php(name = "custom")]
    pub fn _factory_custom(
        modelId: String,
        modelFile: Option<String>,
        additionalFiles: Vec<String>,
        maxLength: Option<i64>,
        head: String,
    ) -> Self {
        let head_core: xberg::RerankerHead =
            serde_json::from_str(&serde_json::to_string(&head).unwrap_or_default()).unwrap_or_default();
        xberg::RerankerModelType::Custom {
            model_id: modelId,
            model_file: modelFile,
            additional_files: additionalFiles,
            max_length: maxLength,
            head: head_core,
        }
        .into()
    }

    #[php(name = "llm")]
    pub fn _factory_llm(llm: &LlmConfig) -> Self {
        xberg::RerankerModelType::Llm {
            llm: Box::new(llm.clone().into()),
        }
        .into()
    }

    #[php(name = "plugin")]
    pub fn _factory_plugin(name: String) -> Self {
        xberg::RerankerModelType::Plugin { name }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    #[php(getter)]
    pub fn get_model_id(&self) -> Option<String> {
        self.model_id.clone()
    }

    #[php(getter)]
    pub fn get_model_file(&self) -> Option<String> {
        self.model_file.clone()
    }

    #[php(getter)]
    pub fn get_additional_files(&self) -> Option<Vec<String>> {
        self.additional_files.clone()
    }

    #[php(getter)]
    pub fn get_max_length(&self) -> Option<i64> {
        self.max_length
    }

    #[php(getter)]
    pub fn get_head(&self) -> Option<String> {
        self.head.clone()
    }

    #[php(getter)]
    pub fn get_llm(&self) -> Option<LlmConfig> {
        self.llm.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\ResultFormat")]
pub struct ResultFormat {}

#[php_impl]
impl ResultFormat {
    pub const UNIFIED: &str = "unified";
    pub const ELEMENTBASED: &str = "element_based";
}

#[php_class]
#[php(name = "Xberg\\RevisionAnchor")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct RevisionAnchor {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub index: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub row: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub col: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub table_index: Option<i64>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

#[php_impl]
impl RevisionAnchor {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(
            value.type_tag.as_str(),
            "paragraph" | "table_cell" | "page" | "slide" | "sheet"
        ) {
            return Err(PhpException::default(format!(
                "Invalid RevisionAnchor tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "paragraph")]
    pub fn _factory_paragraph(index: i64) -> Self {
        xberg::RevisionAnchor::Paragraph { index: index as usize }.into()
    }

    #[php(name = "tableCell")]
    pub fn _factory_table_cell(row: i64, col: i64, tableIndex: i64) -> Self {
        xberg::RevisionAnchor::TableCell {
            row: row as usize,
            col: col as usize,
            table_index: tableIndex as usize,
        }
        .into()
    }

    #[php(name = "page")]
    pub fn _factory_page(index: i64) -> Self {
        xberg::RevisionAnchor::Page { index: index as usize }.into()
    }

    #[php(name = "slide")]
    pub fn _factory_slide(index: i64) -> Self {
        xberg::RevisionAnchor::Slide { index: index as usize }.into()
    }

    #[php(name = "sheet")]
    pub fn _factory_sheet(index: i64, name: Option<String>) -> Self {
        xberg::RevisionAnchor::Sheet {
            index: index as usize,
            name,
        }
        .into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_index(&self) -> Option<i64> {
        self.index
    }

    #[php(getter)]
    pub fn get_row(&self) -> Option<i64> {
        self.row
    }

    #[php(getter)]
    pub fn get_col(&self) -> Option<i64> {
        self.col
    }

    #[php(getter)]
    pub fn get_table_index(&self) -> Option<i64> {
        self.table_index
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }
}

#[php_class]
#[php(name = "Xberg\\RevisionKind")]
pub struct RevisionKind {}

#[php_impl]
impl RevisionKind {
    pub const INSERTION: &str = "insertion";
    pub const DELETION: &str = "deletion";
    pub const FORMATCHANGE: &str = "format_change";
    pub const COMMENT: &str = "comment";
}

#[php_class]
#[php(name = "Xberg\\SchemaCompliance")]
pub struct SchemaCompliance {}

#[php_impl]
impl SchemaCompliance {
    pub const ALLVALID: &str = "all_valid";
    pub const PARTIALVALID: &str = "partial_valid";
    pub const ALLINVALID: &str = "all_invalid";
}

#[php_class]
#[php(name = "Xberg\\SparseEmbeddingModelType")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct SparseEmbeddingModelType {
    #[serde(rename = "type")]
    type_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_id: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub model_file: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub additional_files: Option<Vec<String>>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub max_length: Option<i64>,
}

#[php_impl]
impl SparseEmbeddingModelType {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.type_tag.as_str(), "preset" | "custom" | "plugin") {
            return Err(PhpException::default(format!(
                "Invalid SparseEmbeddingModelType tag: {}",
                value.type_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "preset")]
    pub fn _factory_preset(name: String) -> Self {
        xberg::SparseEmbeddingModelType::Preset { name }.into()
    }

    #[php(name = "custom")]
    pub fn _factory_custom(
        modelId: String,
        modelFile: Option<String>,
        additionalFiles: Vec<String>,
        maxLength: Option<i64>,
    ) -> Self {
        xberg::SparseEmbeddingModelType::Custom {
            model_id: modelId,
            model_file: modelFile,
            additional_files: additionalFiles,
            max_length: maxLength,
        }
        .into()
    }

    #[php(name = "plugin")]
    pub fn _factory_plugin(name: String) -> Self {
        xberg::SparseEmbeddingModelType::Plugin { name }.into()
    }

    #[php(getter)]
    pub fn get_type_tag(&self) -> String {
        self.type_tag.clone()
    }

    #[php(getter)]
    pub fn get_name(&self) -> Option<String> {
        self.name.clone()
    }

    #[php(getter)]
    pub fn get_model_id(&self) -> Option<String> {
        self.model_id.clone()
    }

    #[php(getter)]
    pub fn get_model_file(&self) -> Option<String> {
        self.model_file.clone()
    }

    #[php(getter)]
    pub fn get_additional_files(&self) -> Option<Vec<String>> {
        self.additional_files.clone()
    }

    #[php(getter)]
    pub fn get_max_length(&self) -> Option<i64> {
        self.max_length
    }
}

#[php_class]
#[php(name = "Xberg\\StructuredDataType")]
pub struct StructuredDataType {}

#[php_impl]
impl StructuredDataType {
    pub const JSONLD: &str = "json-ld";
    pub const MICRODATA: &str = "microdata";
    pub const RDFA: &str = "rdfa";
}

#[php_class]
#[php(name = "Xberg\\SummaryStrategy")]
pub struct SummaryStrategy {}

#[php_impl]
impl SummaryStrategy {
    pub const EXTRACTIVE: &str = "extractive";
    pub const ABSTRACTIVE: &str = "abstractive";
}

#[php_class]
#[php(name = "Xberg\\TableChunkingMode")]
pub struct TableChunkingMode {}

#[php_impl]
impl TableChunkingMode {
    pub const SPLIT: &str = "split";
    pub const REPEATHEADER: &str = "repeat_header";
}

#[php_class]
#[php(name = "Xberg\\TableModel")]
pub struct TableModel {}

#[php_impl]
impl TableModel {
    pub const TATR: &str = "tatr";
    pub const SLANETWIRED: &str = "slanet_wired";
    pub const SLANETWIRELESS: &str = "slanet_wireless";
    pub const SLANETPLUS: &str = "slanet_plus";
    pub const SLANETAUTO: &str = "slanet_auto";
    pub const DISABLED: &str = "disabled";
}

#[php_class]
#[php(name = "Xberg\\TableOverlapPreference")]
pub struct TableOverlapPreference {}

#[php_impl]
impl TableOverlapPreference {
    pub const CONTENT: &str = "content";
    pub const NATIVE: &str = "native";
    pub const LAYOUT: &str = "layout";
}

#[php_class]
#[php(name = "Xberg\\TextDirection")]
pub struct TextDirection {}

#[php_impl]
impl TextDirection {
    pub const LEFTTORIGHT: &str = "ltr";
    pub const RIGHTTOLEFT: &str = "rtl";
    pub const AUTO: &str = "auto";
}

#[php_class]
#[php(name = "Xberg\\TierStrategy")]
pub struct TierStrategy {}

#[php_impl]
impl TierStrategy {
    pub const AUTO: &str = "auto";
    pub const TIER2: &str = "tier2";
}

#[php_class]
#[php(name = "Xberg\\UriKind")]
pub struct UriKind {}

#[php_impl]
impl UriKind {
    pub const HYPERLINK: &str = "hyperlink";
    pub const IMAGE: &str = "image";
    pub const ANCHOR: &str = "anchor";
    pub const CITATION: &str = "citation";
    pub const REFERENCE: &str = "reference";
    pub const EMAIL: &str = "email";
}

#[php_class]
#[php(name = "Xberg\\UrlEscapeStyle")]
pub struct UrlEscapeStyle {}

#[php_impl]
impl UrlEscapeStyle {
    pub const ANGLE: &str = "Angle";
    pub const PERCENT: &str = "Percent";
}

#[php_class]
#[php(name = "Xberg\\UrlExtractionMode")]
pub struct UrlExtractionMode {}

#[php_impl]
impl UrlExtractionMode {
    pub const AUTO: &str = "auto";
    pub const DOCUMENT: &str = "document";
    pub const CRAWL: &str = "crawl";
}

#[php_class]
#[php(name = "Xberg\\VlmFallbackPolicy")]
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(default)]
pub struct VlmFallbackPolicy {
    #[serde(rename = "mode")]
    mode_tag: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub quality_threshold: Option<f64>,
}

#[php_impl]
impl VlmFallbackPolicy {
    #[php(name = "from_json")]
    pub fn from_json(json: String) -> PhpResult<Self> {
        let value: Self = serde_json::from_str(&json).map_err(|e| PhpException::default(e.to_string()))?;
        if !matches!(value.mode_tag.as_str(), "disabled" | "on_low_quality" | "always") {
            return Err(PhpException::default(format!(
                "Invalid VlmFallbackPolicy tag: {}",
                value.mode_tag,
            )));
        }
        Ok(value)
    }

    #[php(name = "onLowQuality")]
    pub fn _factory_on_low_quality(qualityThreshold: f64) -> Self {
        xberg::VlmFallbackPolicy::OnLowQuality {
            quality_threshold: qualityThreshold,
        }
        .into()
    }

    #[php(getter)]
    pub fn get_mode_tag(&self) -> String {
        self.mode_tag.clone()
    }

    #[php(getter)]
    pub fn get_quality_threshold(&self) -> Option<f64> {
        self.quality_threshold
    }
}

#[php_class]
#[php(name = "Xberg\\WhisperModel")]
pub struct WhisperModel {}

#[php_impl]
impl WhisperModel {
    pub const TINY: &str = "tiny";
    pub const BASE: &str = "base";
    pub const SMALL: &str = "small";
    pub const MEDIUM: &str = "medium";
    pub const LARGEV3: &str = "large_v3";
}

#[php_class]
#[php(name = "Xberg\\WhitespaceMode")]
pub struct WhitespaceMode {}

#[php_impl]
impl WhitespaceMode {
    pub const NORMALIZED: &str = "Normalized";
    pub const STRICT: &str = "Strict";
}

#[php_class]
#[php(name = "Xberg\\XbergApi")]
pub struct XbergApi;

#[php_impl]
impl XbergApi {
    /// Classify a document's chunks and return the updated document.
    ///
    /// This owned form preserves the mutations when the document crosses a language-binding boundary.
    /// Rust callers that already own a mutable document can use `classify_chunks` to avoid moving it.
    ///
    /// # Errors
    ///
    /// Returns the same validation and LLM errors as `classify_chunks`.
    #[php(name = "classifyChunksOwned")]
    pub fn classify_chunks_owned(
        result: Option<&ExtractedDocument>,
        config: &ChunkClassificationConfig,
    ) -> PhpResult<ExtractedDocument> {
        let result_core: Option<xberg::ExtractedDocument> = result.map(|v| v.clone().into());
        let config_core: xberg::ChunkClassificationConfig = config.clone().into();
        WORKER_RUNTIME.block_on(async {
            let result =
                xberg::text::classification::classify_chunks_owned(result_core.unwrap_or_default(), &config_core)
                    .await
                    .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
            Ok(result.into())
        })
    }

    /// Probe the backends and settings in `config` and report what will actually
    /// execute on this host.
    ///
    /// Runs no downloads and no billable API calls. Backends that are not compiled
    /// in or whose models are not cached report `Skip` rather than failing.
    #[php(name = "doctor")]
    pub fn doctor(config: Option<&ExtractionConfig>) -> DoctorReport {
        let config_core: Option<xberg::ExtractionConfig> = config.map(|v| v.clone().into());
        xberg::doctor(&config_core.unwrap_or_default()).into()
    }

    /// Extract content from a single bytes or URI input.
    #[php(name = "extract")]
    pub fn extract(input: Option<&ExtractInput>, config: Option<&ExtractionConfig>) -> PhpResult<ExtractionResult> {
        let input_core: Option<xberg::ExtractInput> = input.map(|v| v.clone().into());
        let config_core: Option<xberg::ExtractionConfig> = config.map(|v| v.clone().into());
        WORKER_RUNTIME.block_on(async {
            let result = xberg::extract(input_core.unwrap_or_default(), &config_core.unwrap_or_default())
                .await
                .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
            Ok(result.into())
        })
    }

    /// Extract content from multiple bytes or URI inputs.
    #[php(name = "extractBatch")]
    pub fn extract_batch(
        inputs: &ext_php_rs::types::ZendHashTable,
        config: Option<&ExtractionConfig>,
    ) -> PhpResult<ExtractionResult> {
        let mut inputs_core_result: Vec<xberg::ExtractInput> = Vec::new();
        for (_, item) in inputs.iter() {
            if let Some(parsed) = <&ExtractInput as ext_php_rs::convert::FromZval>::from_zval(item) {
                inputs_core_result.push(parsed.clone().into());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to ExtractInput".to_string(),
                ));
            }
        }
        let inputs_core: Vec<xberg::ExtractInput> = inputs_core_result;
        let config_core: Option<xberg::ExtractionConfig> = config.map(|v| v.clone().into());
        WORKER_RUNTIME.block_on(async {
            let result = xberg::extract_batch(inputs_core, &config_core.unwrap_or_default())
                .await
                .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
            Ok(result.into())
        })
    }

    /// Find unmarked claims in markdown text.
    ///
    /// Returns lines that assert a claim but carry neither a footnote citation anchor (`[^...]`)
    /// nor an inference marker (`[*inference*]`).
    ///
    /// The heuristic is simple: a line that contains alphabetic words, ends with sentence punctuation,
    /// and is not a heading, blank line, or markup-only line is considered a claim.
    /// Exclude lines that appear in the citation block (after `---` + `<!-- citations ... -->`).
    ///
    /// # Arguments
    ///
    /// * `markdown` - The markdown text to search
    ///
    /// # Returns
    ///
    /// A vector of trimmed line text strings for unmarked claims.
    ///
    /// # Examples
    ///
    /// ```rust
    /// let text = r#"This is a claim without citation.
    /// Another claim with citation.[^1]
    /// This is a claim with inference.[*inference*]
    ///
    /// [^1]: Citation"#;
    /// let unmarked = find_unmarked_claims(text);
    /// assert_eq!(unmarked.len(), 1);
    /// assert!(unmarked[0].contains("without citation"));
    /// ```
    #[php(name = "findUnmarkedClaims")]
    pub fn find_unmarked_claims(markdown: String) -> Vec<String> {
        xberg::find_unmarked_claims(&markdown)
    }

    /// List names of all registered document extractors.
    #[php(name = "listDocumentExtractors")]
    pub fn list_document_extractors() -> PhpResult<Vec<String>> {
        let result = xberg::list_document_extractors()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List the names of all registered embedding backends.
    ///
    /// Used by `xberg-cli`, the api/mcp endpoints, and generated language
    /// bindings.
    #[php(name = "listEmbeddingBackends")]
    pub fn list_embedding_backends() -> PhpResult<Vec<String>> {
        let result = xberg::list_embedding_backends()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List all registered OCR backends.
    ///
    /// Returns the names of all OCR backends currently registered in the global registry.
    ///
    /// # Returns
    ///
    /// A vector of OCR backend names.
    ///
    /// # Example
    ///
    /// ```rust
    /// use xberg::plugins::list_ocr_backends;
    ///
    /// let backends = list_ocr_backends()?;
    /// for name in backends {
    ///     println!("Registered OCR backend: {}", name);
    /// }
    /// ```
    #[php(name = "listOcrBackends")]
    pub fn list_ocr_backends() -> PhpResult<Vec<String>> {
        let result =
            xberg::list_ocr_backends().map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List all registered post-processor names.
    ///
    /// Returns a vector of all post-processor names currently registered in the
    /// global registry.
    ///
    /// # Returns
    ///
    /// - `Ok(Vec<String>)` - Vector of post-processor names
    /// - `Err(...)` if the registry lock is poisoned
    ///
    /// # Example
    ///
    /// ```rust
    /// use xberg::plugins::list_post_processors;
    ///
    /// let processors = list_post_processors()?;
    /// for name in processors {
    ///     println!("Registered post-processor: {}", name);
    /// }
    /// ```
    #[php(name = "listPostProcessors")]
    pub fn list_post_processors() -> PhpResult<Vec<String>> {
        let result =
            xberg::list_post_processors().map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List names of all registered renderers.
    ///
    /// # Errors
    ///
    /// Returns an error if the registry lock is poisoned.
    #[php(name = "listRenderers")]
    pub fn list_renderers() -> PhpResult<Vec<String>> {
        let result =
            xberg::list_renderers().map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List the names of all registered reranker backends.
    ///
    /// Used by `xberg-cli`, the api/mcp endpoints, and generated language
    /// bindings.
    #[php(name = "listRerankerBackends")]
    pub fn list_reranker_backends() -> PhpResult<Vec<String>> {
        let result =
            xberg::list_reranker_backends().map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List all supported document formats.
    ///
    /// Returns every file extension Xberg recognizes together with its
    /// corresponding MIME type, derived from the central format registry.
    /// Formats that have no registered file extension (such as source code,
    /// which is detected dynamically) are not included.
    ///
    /// The static `EXT_TO_MIME` table lists every format the *codebase* knows how
    /// to describe, regardless of which Cargo features were compiled in. Advertising
    /// that table directly would claim support for extractors that may not exist in
    /// this build (see GH#1387). To keep the advertised catalogue honest, the table
    /// is intersected with the document extractor registry: an extension is only
    /// included if some registered extractor actually claims its MIME type in this
    /// build. This can never drift from reality and automatically covers
    /// third-party extractors registered at runtime.
    ///
    /// The list is sorted alphabetically by file extension.
    ///
    /// # Returns
    ///
    /// A vector of `SupportedFormat` entries sorted by extension, limited to
    /// formats with a registered extractor in this build.
    ///
    /// # Example
    ///
    /// ```
    /// use xberg::core::mime::list_supported_formats;
    ///
    /// let formats = list_supported_formats();
    /// assert!(!formats.is_empty());
    /// ```
    #[php(name = "listSupportedFormats")]
    pub fn list_supported_formats() -> Vec<SupportedFormat> {
        xberg::list_supported_formats().into_iter().map(Into::into).collect()
    }

    /// List the names of all registered tokenizer backends.
    ///
    /// Used by `xberg-cli`, the api/mcp endpoints, and generated language
    /// bindings.
    #[php(name = "listTokenizerBackends")]
    pub fn list_tokenizer_backends() -> PhpResult<Vec<String>> {
        let result = xberg::list_tokenizer_backends()
            .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// List names of all registered validators.
    #[php(name = "listValidators")]
    pub fn list_validators() -> PhpResult<Vec<String>> {
        let result =
            xberg::list_validators().map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
        Ok(result)
    }

    /// Discover all pages and sitemaps reachable from `uri` without extracting document content.
    ///
    /// Builds a `crawlberg::CrawlEngine` from `config.crawl`, calls
    /// `CrawlEngine::map`, and returns the set of discovered URLs as a
    /// `crawlberg::MapResult` (re-exported as `MapResult`).
    ///
    /// Use this when you need the URL inventory of a site before committing to
    /// full document extraction — e.g. to build a crawl queue or validate scope.
    ///
    /// # Errors
    ///
    /// Returns `Validation` if the crawl configuration fails
    /// validation or if the map operation itself fails.
    #[php(name = "mapUrl")]
    pub fn map_url(uri: String, config: Option<&UrlExtractionConfig>) -> PhpResult<MapResult> {
        let config_core: Option<xberg::UrlExtractionConfig> = config.map(|v| v.clone().into());
        WORKER_RUNTIME.block_on(async {
            let result = xberg::map_url(&uri, &config_core.unwrap_or_default())
                .await
                .map_err(|e| ext_php_rs::exception::PhpException::default(e.to_string()))?;
            Ok(result.into())
        })
    }

    /// Rank a set of documents against a query by MaxSim score, descending.
    ///
    /// Mirrors the sort/truncate shape of `crate::reranking`'s `build_results`,
    /// minus top-k truncation (callers slice the returned `Vec` themselves).
    ///
    /// Pure CPU primitive — available without ONNX Runtime.
    #[php(name = "maxSimRank")]
    pub fn max_sim_rank(
        query: &MultiVectorEmbedding,
        docs: &ext_php_rs::types::ZendHashTable,
    ) -> PhpResult<Vec<LateInteractionMatch>> {
        let query_core: xberg::MultiVectorEmbedding = query.clone().into();
        let mut docs_core_result: Vec<xberg::MultiVectorEmbedding> = Vec::new();
        for (_, item) in docs.iter() {
            if let Some(parsed) = <&MultiVectorEmbedding as ext_php_rs::convert::FromZval>::from_zval(item) {
                docs_core_result.push(parsed.clone().into());
            } else {
                return Err(ext_php_rs::exception::PhpException::default(
                    "Failed to convert array element to MultiVectorEmbedding".to_string(),
                ));
            }
        }
        let docs_core: Vec<xberg::MultiVectorEmbedding> = docs_core_result;
        Ok(xberg::max_sim_rank(&query_core, &docs_core[..])
            .into_iter()
            .map(Into::into)
            .collect())
    }

    /// Score a query against a document using ColBERT's MaxSim operator: for each
    /// query token vector, take the maximum dot product against any document
    /// token vector, then sum across query tokens.
    ///
    /// Returns `0.0` if `query` and `doc` have mismatched dimensionality, if either
    /// has zero tokens, or if either is not well-formed per
    /// `MultiVectorEmbedding::is_well_formed` (its `data` length does not match
    /// `num_tokens * dim`).
    ///
    /// Pure CPU primitive — available without ONNX Runtime.
    #[php(name = "maxSimScore")]
    pub fn max_sim_score(query: &MultiVectorEmbedding, doc: &MultiVectorEmbedding) -> f64 {
        let query_core: xberg::MultiVectorEmbedding = query.clone().into();
        let doc_core: xberg::MultiVectorEmbedding = doc.clone().into();
        xberg::max_sim_score(&query_core, &doc_core)
    }

    /// Verify that an excerpt appears verbatim in source text.
    ///
    /// Performs exact matching by default. Also tries whitespace-normalized matching
    /// (collapsing runs of whitespace on both sides) since PDF-extracted text often
    /// has irregular spacing.
    ///
    /// # Arguments
    ///
    /// * `excerpt` - The text snippet to find
    /// * `source_text` - The full source text to search
    ///
    /// # Returns
    ///
    /// `true` if the excerpt appears (exactly or with normalized whitespace), `false` otherwise.
    ///
    /// # Examples
    ///
    /// ```rust
    /// let source = "The document states: Exact quoted text.";
    /// let excerpt = "Exact quoted text";
    /// assert!(verify_excerpt(excerpt, source));
    ///
    /// // Whitespace normalization
    /// let source2 = "Text with  irregular   spacing.";
    /// let excerpt2 = "Text with irregular spacing";
    /// assert!(verify_excerpt(excerpt2, source2));
    /// ```
    #[php(name = "verifyExcerpt")]
    pub fn verify_excerpt(excerpt: String, sourceText: String) -> bool {
        xberg::verify_excerpt(&excerpt, &sourceText)
    }

    #[php(name = "registerOcrBackend")]
    pub fn register_ocr_backend(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_ocr_backend(backend)
    }

    #[php(name = "unregisterOcrBackend")]
    pub fn unregister_ocr_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_ocr_backend(name)
    }

    #[php(name = "clearOcrBackends")]
    pub fn clear_ocr_backends() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_ocr_backends()
    }

    #[php(name = "registerPostProcessor")]
    pub fn register_post_processor(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_post_processor(backend)
    }

    #[php(name = "unregisterPostProcessor")]
    pub fn unregister_post_processor(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_post_processor(name)
    }

    #[php(name = "clearPostProcessors")]
    pub fn clear_post_processors() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_post_processors()
    }

    #[php(name = "registerValidator")]
    pub fn register_validator(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_validator(backend)
    }

    #[php(name = "unregisterValidator")]
    pub fn unregister_validator(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_validator(name)
    }

    #[php(name = "clearValidators")]
    pub fn clear_validators() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_validators()
    }

    #[php(name = "registerDocumentExtractor")]
    pub fn register_document_extractor(
        backend: &mut ext_php_rs::types::ZendObject,
    ) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_document_extractor(backend)
    }

    #[php(name = "unregisterDocumentExtractor")]
    pub fn unregister_document_extractor(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_document_extractor(name)
    }

    #[php(name = "clearDocumentExtractors")]
    pub fn clear_document_extractors() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_document_extractors()
    }

    #[php(name = "registerEmbeddingBackend")]
    pub fn register_embedding_backend(
        backend: &mut ext_php_rs::types::ZendObject,
    ) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_embedding_backend(backend)
    }

    #[php(name = "unregisterEmbeddingBackend")]
    pub fn unregister_embedding_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_embedding_backend(name)
    }

    #[php(name = "clearEmbeddingBackends")]
    pub fn clear_embedding_backends() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_embedding_backends()
    }

    #[php(name = "registerRenderer")]
    pub fn register_renderer(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_renderer(backend)
    }

    #[php(name = "unregisterRenderer")]
    pub fn unregister_renderer(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_renderer(name)
    }

    #[php(name = "clearRenderers")]
    pub fn clear_renderers() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_renderers()
    }

    #[php(name = "registerRerankerBackend")]
    pub fn register_reranker_backend(
        backend: &mut ext_php_rs::types::ZendObject,
    ) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_reranker_backend(backend)
    }

    #[php(name = "unregisterRerankerBackend")]
    pub fn unregister_reranker_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_reranker_backend(name)
    }

    #[php(name = "clearRerankerBackends")]
    pub fn clear_reranker_backends() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_reranker_backends()
    }

    #[php(name = "registerTokenizerBackend")]
    pub fn register_tokenizer_backend(
        backend: &mut ext_php_rs::types::ZendObject,
    ) -> ext_php_rs::prelude::PhpResult<()> {
        crate::register_tokenizer_backend(backend)
    }

    #[php(name = "unregisterTokenizerBackend")]
    pub fn unregister_tokenizer_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
        crate::unregister_tokenizer_backend(name)
    }

    #[php(name = "clearTokenizerBackends")]
    pub fn clear_tokenizer_backends() -> ext_php_rs::prelude::PhpResult<()> {
        crate::clear_tokenizer_backends()
    }
}

/// Wrapper that bridges a foreign Php object to the `OcrBackend` trait.
pub struct PhpOcrBackendBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpOcrBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpOcrBackendBridge")
    }
}

impl PhpOcrBackendBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpOcrBackendBridge {}
unsafe impl Sync for PhpOcrBackendBridge {}

impl Drop for PhpOcrBackendBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpOcrBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpOcrBackendBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridge {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "process_image",
                            [
                                ext_php_rs::types::Zval::try_from(format!("{:?}", image_bytes)).unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val)
                            {
                                Ok(native.clone().into())
                            } else {
                                let json_str = val.string().unwrap_or_default();
                                serde_json::from_str(&json_str)
                                    .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                            }
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'process_image' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "process_image",
                        [
                            ext_php_rs::types::Zval::try_from(format!("{:?}", image_bytes)).unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val) {
                            Ok(native.clone().into())
                        } else {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'process_image' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }

    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "process_image_file").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultProcessImageFile(self)
                .process_image_file(path, config)
                .await;
        }
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "process_image_file",
                            [
                                ext_php_rs::types::Zval::try_from(path.to_string_lossy().to_string())
                                    .unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val)
                            {
                                Ok(native.clone().into())
                            } else {
                                let json_str = val.string().unwrap_or_default();
                                serde_json::from_str(&json_str)
                                    .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                            }
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'process_image_file' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "process_image_file",
                        [
                            ext_php_rs::types::Zval::try_from(path.to_string_lossy().to_string()).unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val) {
                            Ok(native.clone().into())
                        } else {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'process_image_file' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }

    fn supports_language(&self, lang: &str) -> bool {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "supports_language",
                [ext_php_rs::types::Zval::try_from(lang).unwrap_or_default()]
                    .iter()
                    .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                    .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "supports_language",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "supports_language", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn backend_type(&self) -> xberg::OcrBackendType {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("backend_type", vec![]) };
        match result {
            Ok(val) => match val.string() {
                Some(json_str) => match serde_json::from_str(&json_str) {
                    Ok(v) => v,
                    Err(e) => {
                        tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "backend_type", error = %e, "host returned an unparseable value; returning default");
                        Default::default()
                    }
                },
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "backend_type",
                        "host returned a non-string value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "backend_type", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn supported_languages(&self) -> Vec<String> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "supported_languages").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultSupportedLanguages(self).supported_languages();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("supported_languages", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <Vec<String> as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpOcrBackendBridge",
                            method = "supported_languages",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "supported_languages", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn supports_table_detection(&self) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "supports_table_detection").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultSupportsTableDetection(self).supports_table_detection();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("supports_table_detection", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "supports_table_detection",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "supports_table_detection", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn supports_document_processing(&self) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "supports_document_processing").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultSupportsDocumentProcessing(self).supports_document_processing();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("supports_document_processing", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "supports_document_processing",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "supports_document_processing", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn emits_structured_markdown(&self) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "emits_structured_markdown").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultEmitsStructuredMarkdown(self).emits_structured_markdown();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("emits_structured_markdown", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "emits_structured_markdown",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "emits_structured_markdown", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "confidence_semantics").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultConfidenceSemantics(self).confidence_semantics();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("confidence_semantics", vec![]) };
        match result {
            Ok(val) => match val.string() {
                Some(json_str) => match serde_json::from_str(&json_str) {
                    Ok(v) => v,
                    Err(e) => {
                        tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "confidence_semantics", error = %e, "host returned an unparseable value; returning default");
                        Default::default()
                    }
                },
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "confidence_semantics",
                        "host returned a non-string value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "confidence_semantics", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "page_orientation_handling").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultPageOrientationHandling(self).page_orientation_handling();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("page_orientation_handling", vec![]) };
        match result {
            Ok(val) => match val.string() {
                Some(json_str) => match serde_json::from_str(&json_str) {
                    Ok(v) => v,
                    Err(e) => {
                        tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "page_orientation_handling", error = %e, "host returned an unparseable value; returning default");
                        Default::default()
                    }
                },
                None => {
                    tracing::warn!(
                        wrapper = "PhpOcrBackendBridge",
                        method = "page_orientation_handling",
                        "host returned a non-string value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpOcrBackendBridge", method = "page_orientation_handling", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "process_document").is_some()
        }) {
            return PhpOcrBackendBridgeDefaultProcessDocument(self)
                .process_document(_path, _config)
                .await;
        }
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "process_document",
                            [
                                ext_php_rs::types::Zval::try_from(_path.to_string_lossy().to_string())
                                    .unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*_config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val)
                            {
                                Ok(native.clone().into())
                            } else {
                                let json_str = val.string().unwrap_or_default();
                                serde_json::from_str(&json_str)
                                    .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                            }
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'process_document' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "process_document",
                        [
                            ext_php_rs::types::Zval::try_from(_path.to_string_lossy().to_string()).unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(OcrConfig::from((*_config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val) {
                            Ok(native.clone().into())
                        } else {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'process_document' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }
}

/// Runs `process_image_file`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultProcessImageFile<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultProcessImageFile<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultProcessImageFile<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supported_languages`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultSupportedLanguages<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultSupportedLanguages<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultSupportedLanguages<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supports_table_detection`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultSupportsTableDetection<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultSupportsTableDetection<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultSupportsTableDetection<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supports_document_processing`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultSupportsDocumentProcessing<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultSupportsDocumentProcessing<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultSupportsDocumentProcessing<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `emits_structured_markdown`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultEmitsStructuredMarkdown<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultEmitsStructuredMarkdown<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultEmitsStructuredMarkdown<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `confidence_semantics`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultConfidenceSemantics<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultConfidenceSemantics<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultConfidenceSemantics<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `page_orientation_handling`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultPageOrientationHandling<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultPageOrientationHandling<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultPageOrientationHandling<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `process_document`'s Rust default body for [`PhpOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpOcrBackendBridgeDefaultProcessDocument<'a>(&'a PhpOcrBackendBridge);
impl xberg::plugins::Plugin for PhpOcrBackendBridgeDefaultProcessDocument<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for PhpOcrBackendBridgeDefaultProcessDocument<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
}

#[php_function]
pub fn register_ocr_backend(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpOcrBackendBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::OcrBackend> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_ocr_backend_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_ocr_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::ocr_backend::unregister_ocr_backend(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_ocr_backends() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::ocr_backend::clear_ocr_backends()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `PostProcessor` trait.
pub struct PhpPostProcessorBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpPostProcessorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpPostProcessorBridge")
    }
}

impl PhpPostProcessorBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpPostProcessorBridge {}
unsafe impl Sync for PhpPostProcessorBridge {}

impl Drop for PhpPostProcessorBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpPostProcessorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpPostProcessorBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::PostProcessor for PhpPostProcessorBridge {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "process",
                            [
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*result).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'process' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "process",
                        [
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*result).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        let json_str = val.string().unwrap_or_default();
                        serde_json::from_str(&json_str)
                            .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'process' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }

    fn processing_stage(&self) -> xberg::ProcessingStage {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("processing_stage", vec![]) };
        match result {
            Ok(val) => match val.string() {
                Some(json_str) => match serde_json::from_str(&json_str) {
                    Ok(v) => v,
                    Err(e) => {
                        tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "processing_stage", error = %e, "host returned an unparseable value; returning default");
                        Default::default()
                    }
                },
                None => {
                    tracing::warn!(
                        wrapper = "PhpPostProcessorBridge",
                        method = "processing_stage",
                        "host returned a non-string value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "processing_stage", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "should_process").is_some()
        }) {
            return PhpPostProcessorBridgeDefaultShouldProcess(self).should_process(_result, _config);
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "should_process",
                [
                    ext_php_rs::convert::IntoZval::into_zval(
                        ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*_result).clone())),
                        false,
                    )
                    .unwrap_or_default(),
                    ext_php_rs::convert::IntoZval::into_zval(
                        ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*_config).clone())),
                        false,
                    )
                    .unwrap_or_default(),
                ]
                .iter()
                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpPostProcessorBridge",
                        method = "should_process",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "should_process", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "estimated_duration_ms").is_some()
        }) {
            return PhpPostProcessorBridgeDefaultEstimatedDurationMs(self).estimated_duration_ms(_result);
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "estimated_duration_ms",
                [ext_php_rs::convert::IntoZval::into_zval(
                    ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*_result).clone())),
                    false,
                )
                .unwrap_or_default()]
                .iter()
                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as u64,
                None => {
                    tracing::warn!(
                        wrapper = "PhpPostProcessorBridge",
                        method = "estimated_duration_ms",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "estimated_duration_ms", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn priority(&self) -> i32 {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "priority").is_some()
        }) {
            return PhpPostProcessorBridgeDefaultPriority(self).priority();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("priority", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as i32,
                None => {
                    tracing::warn!(
                        wrapper = "PhpPostProcessorBridge",
                        method = "priority",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpPostProcessorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }
}

/// Runs `should_process`'s Rust default body for [`PhpPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpPostProcessorBridgeDefaultShouldProcess<'a>(&'a PhpPostProcessorBridge);
impl xberg::plugins::Plugin for PhpPostProcessorBridgeDefaultShouldProcess<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for PhpPostProcessorBridgeDefaultShouldProcess<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        xberg::PostProcessor::estimated_duration_ms(self.0, _result)
    }
    fn priority(&self) -> i32 {
        xberg::PostProcessor::priority(self.0)
    }
}

/// Runs `estimated_duration_ms`'s Rust default body for [`PhpPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpPostProcessorBridgeDefaultEstimatedDurationMs<'a>(&'a PhpPostProcessorBridge);
impl xberg::plugins::Plugin for PhpPostProcessorBridgeDefaultEstimatedDurationMs<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for PhpPostProcessorBridgeDefaultEstimatedDurationMs<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::PostProcessor::should_process(self.0, _result, _config)
    }
    fn priority(&self) -> i32 {
        xberg::PostProcessor::priority(self.0)
    }
}

/// Runs `priority`'s Rust default body for [`PhpPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpPostProcessorBridgeDefaultPriority<'a>(&'a PhpPostProcessorBridge);
impl xberg::plugins::Plugin for PhpPostProcessorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for PhpPostProcessorBridgeDefaultPriority<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::PostProcessor::should_process(self.0, _result, _config)
    }
    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        xberg::PostProcessor::estimated_duration_ms(self.0, _result)
    }
}

#[php_function]
pub fn register_post_processor(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpPostProcessorBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::PostProcessor> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_post_processor_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_post_processor(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::post_processor::unregister_post_processor(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_post_processors() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::post_processor::clear_post_processors()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `Validator` trait.
pub struct PhpValidatorBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpValidatorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpValidatorBridge")
    }
}

impl PhpValidatorBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpValidatorBridge {}
unsafe impl Sync for PhpValidatorBridge {}

impl Drop for PhpValidatorBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpValidatorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpValidatorBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpValidatorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::Validator for PhpValidatorBridge {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "validate",
                            [
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*result).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'validate' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "validate",
                        [
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*result).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        let json_str = val.string().unwrap_or_default();
                        serde_json::from_str(&json_str)
                            .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'validate' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }

    fn should_validate(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "should_validate").is_some()
        }) {
            return PhpValidatorBridgeDefaultShouldValidate(self).should_validate(_result, _config);
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "should_validate",
                [
                    ext_php_rs::convert::IntoZval::into_zval(
                        ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*_result).clone())),
                        false,
                    )
                    .unwrap_or_default(),
                    ext_php_rs::convert::IntoZval::into_zval(
                        ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*_config).clone())),
                        false,
                    )
                    .unwrap_or_default(),
                ]
                .iter()
                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpValidatorBridge",
                        method = "should_validate",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpValidatorBridge", method = "should_validate", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn priority(&self) -> i32 {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "priority").is_some()
        }) {
            return PhpValidatorBridgeDefaultPriority(self).priority();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("priority", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as i32,
                None => {
                    tracing::warn!(
                        wrapper = "PhpValidatorBridge",
                        method = "priority",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpValidatorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }
}

/// Runs `should_validate`'s Rust default body for [`PhpValidatorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpValidatorBridgeDefaultShouldValidate<'a>(&'a PhpValidatorBridge);
impl xberg::plugins::Plugin for PhpValidatorBridgeDefaultShouldValidate<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::Validator for PhpValidatorBridgeDefaultShouldValidate<'_> {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::Validator::validate(self.0, result, config).await
    }
    fn priority(&self) -> i32 {
        xberg::Validator::priority(self.0)
    }
}

/// Runs `priority`'s Rust default body for [`PhpValidatorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpValidatorBridgeDefaultPriority<'a>(&'a PhpValidatorBridge);
impl xberg::plugins::Plugin for PhpValidatorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::Validator for PhpValidatorBridgeDefaultPriority<'_> {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::Validator::validate(self.0, result, config).await
    }
    fn should_validate(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::Validator::should_validate(self.0, _result, _config)
    }
}

#[php_function]
pub fn register_validator(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpValidatorBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::Validator> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_validator_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_validator(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::validator::unregister_validator(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_validators() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::validator::clear_validators()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `DocumentExtractor` trait.
pub struct PhpDocumentExtractorBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpDocumentExtractorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpDocumentExtractorBridge")
    }
}

impl PhpDocumentExtractorBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpDocumentExtractorBridge {}
unsafe impl Sync for PhpDocumentExtractorBridge {}

impl Drop for PhpDocumentExtractorBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpDocumentExtractorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpDocumentExtractorBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpDocumentExtractorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::DocumentExtractor for PhpDocumentExtractorBridge {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "extract",
                            [
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractInput::from(input)),
                                    false,
                                )
                                .unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(
                                    ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                    false,
                                )
                                .unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val)
                            {
                                Ok(native.clone().into())
                            } else {
                                let json_str = val.string().unwrap_or_default();
                                serde_json::from_str(&json_str)
                                    .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                            }
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'extract' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "extract",
                        [
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractInput::from(input)),
                                false,
                            )
                            .unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(
                                ext_php_rs::types::ZendClassObject::new(ExtractionConfig::from((*config).clone())),
                                false,
                            )
                            .unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        if let Some(native) = <&ExtractedDocument as ext_php_rs::convert::FromZval>::from_zval(&val) {
                            Ok(native.clone().into())
                        } else {
                            let json_str = val.string().unwrap_or_default();
                            serde_json::from_str(&json_str)
                                .map_err(|e| xberg::XbergError::Other(format!("Deserialize error: {}", e)))
                        }
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'extract' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }

    fn supported_mime_types(&self) -> &[&str] {
        let __types: Vec<String> = {
            // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
            let result = unsafe { (*self.inner).try_call_method("supported_mime_types", vec![]) };
            match result {
                Ok(val) => {
                    // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                    // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                    match <Vec<String> as ext_php_rs::convert::FromZval>::from_zval(&val) {
                        Some(v) => v,
                        None => {
                            tracing::warn!(
                                wrapper = "PhpDocumentExtractorBridge",
                                method = "supported_mime_types",
                                "host returned an undecodable value; returning default"
                            );
                            Default::default()
                        }
                    }
                }
                // This method's Rust signature is infallible, so a PHP exception cannot
                // propagate. Log before substituting the default — a silent default is
                // indistinguishable from a real result to the caller.
                Err(e) => {
                    tracing::warn!(wrapper = "PhpDocumentExtractorBridge", method = "supported_mime_types", error = %e, "host callback threw; returning default");
                    Default::default()
                }
            }
        };
        let __strs: Vec<&'static str> = __types
            .into_iter()
            .map(|s| -> &'static str { Box::leak(s.into_boxed_str()) })
            .collect();
        Box::leak(__strs.into_boxed_slice())
    }

    fn priority(&self) -> i32 {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "priority").is_some()
        }) {
            return PhpDocumentExtractorBridgeDefaultPriority(self).priority();
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("priority", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as i32,
                None => {
                    tracing::warn!(
                        wrapper = "PhpDocumentExtractorBridge",
                        method = "priority",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpDocumentExtractorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn can_handle(&self, _path: &std::path::Path, _mime_type: &str) -> bool {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "can_handle").is_some()
        }) {
            return PhpDocumentExtractorBridgeDefaultCanHandle(self).can_handle(_path, _mime_type);
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "can_handle",
                [
                    ext_php_rs::types::Zval::try_from(_path.to_string_lossy().to_string()).unwrap_or_default(),
                    ext_php_rs::types::Zval::try_from(_mime_type).unwrap_or_default(),
                ]
                .iter()
                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v != 0,
                None => {
                    tracing::warn!(
                        wrapper = "PhpDocumentExtractorBridge",
                        method = "can_handle",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpDocumentExtractorBridge", method = "can_handle", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }
}

/// Runs `priority`'s Rust default body for [`PhpDocumentExtractorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpDocumentExtractorBridgeDefaultPriority<'a>(&'a PhpDocumentExtractorBridge);
impl xberg::plugins::Plugin for PhpDocumentExtractorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::DocumentExtractor for PhpDocumentExtractorBridgeDefaultPriority<'_> {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::DocumentExtractor::extract(self.0, input, config).await
    }
    fn supported_mime_types(&self) -> &[&str] {
        xberg::DocumentExtractor::supported_mime_types(self.0)
    }
    fn can_handle(&self, _path: &std::path::Path, _mime_type: &str) -> bool {
        xberg::DocumentExtractor::can_handle(self.0, _path, _mime_type)
    }
}

/// Runs `can_handle`'s Rust default body for [`PhpDocumentExtractorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpDocumentExtractorBridgeDefaultCanHandle<'a>(&'a PhpDocumentExtractorBridge);
impl xberg::plugins::Plugin for PhpDocumentExtractorBridgeDefaultCanHandle<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::DocumentExtractor for PhpDocumentExtractorBridgeDefaultCanHandle<'_> {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::DocumentExtractor::extract(self.0, input, config).await
    }
    fn supported_mime_types(&self) -> &[&str] {
        xberg::DocumentExtractor::supported_mime_types(self.0)
    }
    fn priority(&self) -> i32 {
        xberg::DocumentExtractor::priority(self.0)
    }
}

#[php_function]
pub fn register_document_extractor(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpDocumentExtractorBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::DocumentExtractor> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_document_extractor_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_document_extractor(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::document_extractor::unregister_document_extractor(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_document_extractors() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::document_extractor::clear_document_extractors()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `EmbeddingBackend` trait.
pub struct PhpEmbeddingBackendBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpEmbeddingBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpEmbeddingBackendBridge")
    }
}

impl PhpEmbeddingBackendBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpEmbeddingBackendBridge {}
unsafe impl Sync for PhpEmbeddingBackendBridge {}

impl Drop for PhpEmbeddingBackendBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpEmbeddingBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpEmbeddingBackendBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpEmbeddingBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::EmbeddingBackend for PhpEmbeddingBackendBridge {
    fn dimensions(&self) -> usize {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("dimensions", vec![]) };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as usize,
                None => {
                    tracing::warn!(
                        wrapper = "PhpEmbeddingBackendBridge",
                        method = "dimensions",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpEmbeddingBackendBridge", method = "dimensions", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    async fn embed(&self, texts: Vec<String>) -> std::result::Result<Vec<Vec<f32>>, xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "embed",
                            [ext_php_rs::convert::IntoZval::into_zval(texts.clone(), false).unwrap_or_default()]
                                .iter()
                                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                                .collect(),
                        )
                    } {
                        Ok(val) => {
                            // The return type is php-native (String/primitive/Vec of those, recursively) —
                            // decode the PHP array/scalar directly via `FromZval`, no JSON round-trip (#1304).
                            <Vec<Vec<f32>> as ext_php_rs::convert::FromZval>::from_zval(&val).ok_or_else(|| {
                                xberg::XbergError::Other(format!(
                                    "Plugin '{}' failed to decode native PHP return value for method 'embed'",
                                    cached_name
                                ))
                            })
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'embed' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "embed",
                        [ext_php_rs::convert::IntoZval::into_zval(texts.clone(), false).unwrap_or_default()]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                    )
                } {
                    Ok(val) => {
                        // The return type is php-native (String/primitive/Vec of those, recursively) —
                        // decode the PHP array/scalar directly via `FromZval`, no JSON round-trip (#1304).
                        <Vec<Vec<f32>> as ext_php_rs::convert::FromZval>::from_zval(&val).ok_or_else(|| {
                            xberg::XbergError::Other(format!(
                                "Plugin '{}' failed to decode native PHP return value for method 'embed'",
                                cached_name
                            ))
                        })
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'embed' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }
}

#[php_function]
pub fn register_embedding_backend(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpEmbeddingBackendBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::EmbeddingBackend> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_embedding_backend_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_embedding_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::embedding_backend::unregister_embedding_backend(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_embedding_backends() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::embedding_backend::clear_embedding_backends()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `Renderer` trait.
pub struct PhpRendererBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpRendererBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpRendererBridge")
    }
}

impl PhpRendererBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpRendererBridge {}
unsafe impl Sync for PhpRendererBridge {}

impl Drop for PhpRendererBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpRendererBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpRendererBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpRendererBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

impl xberg::Renderer for PhpRendererBridge {
    fn render_result(&self, result: &xberg::ExtractedDocument) -> std::result::Result<String, xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "render_result").is_some()
        }) {
            return PhpRendererBridgeDefaultRenderResult(self).render_result(result);
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "render_result",
                [ext_php_rs::convert::IntoZval::into_zval(
                    ext_php_rs::types::ZendClassObject::new(ExtractedDocument::from((*result).clone())),
                    false,
                )
                .unwrap_or_default()]
                .iter()
                .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                .collect(),
            )
        };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                <String as ext_php_rs::convert::FromZval>::from_zval(&val).ok_or_else(|| {
                    xberg::XbergError::Other(
                        "Failed to decode native PHP return value for method 'render_result'".to_string(),
                    )
                })
            }
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

/// Runs `render_result`'s Rust default body for [`PhpRendererBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct PhpRendererBridgeDefaultRenderResult<'a>(&'a PhpRendererBridge);
impl xberg::plugins::Plugin for PhpRendererBridgeDefaultRenderResult<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
impl xberg::Renderer for PhpRendererBridgeDefaultRenderResult<'_> {}

#[php_function]
pub fn register_renderer(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpRendererBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::Renderer> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_renderer_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_renderer(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::renderer::unregister_renderer(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_renderers() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::renderer::clear_renderers()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `RerankerBackend` trait.
pub struct PhpRerankerBackendBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpRerankerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpRerankerBackendBridge")
    }
}

impl PhpRerankerBackendBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpRerankerBackendBridge {}
unsafe impl Sync for PhpRerankerBackendBridge {}

impl Drop for PhpRerankerBackendBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpRerankerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpRerankerBackendBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpRerankerBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

#[async_trait::async_trait]
impl xberg::RerankerBackend for PhpRerankerBackendBridge {
    async fn rerank(&self, query: String, documents: Vec<String>) -> std::result::Result<Vec<f32>, xberg::XbergError> {
        let inner_obj = self.inner;
        let cached_name = self.cached_name.clone();
        let query = query.to_string();

        // SAFETY: PHP objects are single-threaded within a request.
        // Use block_in_place if already in a tokio context; otherwise use block_on.
        if let Ok(handle) = tokio::runtime::Handle::try_current() {
            tokio::task::block_in_place(|| {
                handle.block_on(async {
                    match unsafe {
                        (*inner_obj).try_call_method(
                            "rerank",
                            [
                                ext_php_rs::types::Zval::try_from(query).unwrap_or_default(),
                                ext_php_rs::convert::IntoZval::into_zval(documents.clone(), false).unwrap_or_default(),
                            ]
                            .iter()
                            .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                            .collect(),
                        )
                    } {
                        Ok(val) => {
                            // The return type is php-native (String/primitive/Vec of those, recursively) —
                            // decode the PHP array/scalar directly via `FromZval`, no JSON round-trip (#1304).
                            <Vec<f32> as ext_php_rs::convert::FromZval>::from_zval(&val).ok_or_else(|| {
                                xberg::XbergError::Other(format!(
                                    "Plugin '{}' failed to decode native PHP return value for method 'rerank'",
                                    cached_name
                                ))
                            })
                        }
                        Err(e) => Err(xberg::XbergError::Other(format!(
                            "Plugin '{}' method 'rerank' failed: {}",
                            cached_name, e
                        ))),
                    }
                })
            })
        } else {
            WORKER_RUNTIME.block_on(async {
                match unsafe {
                    (*inner_obj).try_call_method(
                        "rerank",
                        [
                            ext_php_rs::types::Zval::try_from(query).unwrap_or_default(),
                            ext_php_rs::convert::IntoZval::into_zval(documents.clone(), false).unwrap_or_default(),
                        ]
                        .iter()
                        .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                        .collect(),
                    )
                } {
                    Ok(val) => {
                        // The return type is php-native (String/primitive/Vec of those, recursively) —
                        // decode the PHP array/scalar directly via `FromZval`, no JSON round-trip (#1304).
                        <Vec<f32> as ext_php_rs::convert::FromZval>::from_zval(&val).ok_or_else(|| {
                            xberg::XbergError::Other(format!(
                                "Plugin '{}' failed to decode native PHP return value for method 'rerank'",
                                cached_name
                            ))
                        })
                    }
                    Err(e) => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' method 'rerank' failed: {}",
                        cached_name, e
                    ))),
                }
            })
        }
    }
}

#[php_function]
pub fn register_reranker_backend(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpRerankerBackendBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::RerankerBackend> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_reranker_backend_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_reranker_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::reranker_backend::unregister_reranker_backend(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_reranker_backends() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::reranker_backend::clear_reranker_backends()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

/// Wrapper that bridges a foreign Php object to the `TokenizerBackend` trait.
pub struct PhpTokenizerBackendBridge {
    inner: *mut ext_php_rs::types::ZendObject,
    cached_name: String,
}

impl std::fmt::Debug for PhpTokenizerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PhpTokenizerBackendBridge")
    }
}

impl PhpTokenizerBackendBridge {
    /// Create a new bridge wrapping a PHP object.
    ///
    /// Validates that the PHP object provides all required methods.
    pub fn new(php_obj: &mut ext_php_rs::types::ZendObject) -> Self {
        // Validation of required methods is done in the registration function below.
        // Skipping debug_assert in constructor to avoid type issues with get_property.

        // Increment refcount to keep the object alive while registered.
        php_obj.inc_count();

        // Extract and cache name
        let cached_name = php_obj
            .try_call_method("name", vec![])
            .ok()
            .and_then(|v| v.string())
            .unwrap_or("unknown".into())
            .to_string();

        Self {
            inner: php_obj as *mut _,
            cached_name,
        }
    }
}

// SAFETY: PHP is single-threaded within a request context.
// The raw pointer to ZendObject is managed with inc_count/dec_count pairs.
unsafe impl Send for PhpTokenizerBackendBridge {}
unsafe impl Sync for PhpTokenizerBackendBridge {}

impl Drop for PhpTokenizerBackendBridge {
    fn drop(&mut self) {
        // SAFETY: Decrement refcount when the bridge is dropped.
        // The object pointer is guaranteed valid until this point.
        unsafe {
            if !self.inner.is_null() {
                (*self.inner).dec_count();
            }
        }
    }
}

impl xberg::plugins::Plugin for PhpTokenizerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("version", vec![]) };
        match result {
            Ok(val) => {
                // The return type is php-native (String/primitive/Vec of those, recursively) — decode
                // the PHP array/scalar directly via `FromZval`, no JSON round-trip needed (#1304).
                match <String as ext_php_rs::convert::FromZval>::from_zval(&val) {
                    Some(v) => v,
                    None => {
                        tracing::warn!(
                            wrapper = "PhpTokenizerBackendBridge",
                            method = "version",
                            "host returned an undecodable value; returning default"
                        );
                        Default::default()
                    }
                }
            }
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpTokenizerBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "initialize").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("initialize", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        if !({
            // SAFETY: PHP objects are single-threaded; reads are safe within a request.
            let __class = unsafe { (*self.inner).get_class_name().unwrap_or_default() };
            ext_php_rs::zend::Function::try_from_method(&__class, "shutdown").is_some()
        }) {
            return Ok(());
        }
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe { (*self.inner).try_call_method("shutdown", vec![]) };
        match result {
            Ok(_) => Ok(()),
            Err(e) => Err(xberg::XbergError::Other(e.to_string())),
        }
    }
}

impl xberg::TokenizerBackend for PhpTokenizerBackendBridge {
    fn count_tokens(&self, text: &str) -> usize {
        // SAFETY: PHP objects are single-threaded; method calls are safe within a request.
        let result = unsafe {
            (*self.inner).try_call_method(
                "count_tokens",
                [ext_php_rs::types::Zval::try_from(text).unwrap_or_default()]
                    .iter()
                    .map(|z| z as &dyn ext_php_rs::convert::IntoZvalDyn)
                    .collect(),
            )
        };
        match result {
            Ok(val) => match val.long() {
                Some(v) => v as usize,
                None => {
                    tracing::warn!(
                        wrapper = "PhpTokenizerBackendBridge",
                        method = "count_tokens",
                        "host returned a non-integer value; returning default"
                    );
                    Default::default()
                }
            },
            // This method's Rust signature is infallible, so a PHP exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "PhpTokenizerBackendBridge", method = "count_tokens", error = %e, "host callback threw; returning default");
                Default::default()
            }
        }
    }
}

#[php_function]
pub fn register_tokenizer_backend(backend: &mut ext_php_rs::types::ZendObject) -> ext_php_rs::prelude::PhpResult<()> {
    let wrapper = PhpTokenizerBackendBridge::new(backend);
    let arc: std::sync::Arc<dyn xberg::TokenizerBackend> = std::sync::Arc::new(wrapper);

    let registry = xberg::plugins::registry::get_tokenizer_backend_registry();
    let mut registry = registry.write();
    registry
        .register(arc)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("Failed to register backend: {}", e)))?;
    Ok(())
}

#[php_function]
pub fn unregister_tokenizer_backend(name: String) -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::tokenizer_backend::unregister_tokenizer_backend(&name)
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[php_function]
pub fn clear_tokenizer_backends() -> ext_php_rs::prelude::PhpResult<()> {
    xberg::plugins::tokenizer_backend::clear_tokenizer_backends()
        .map_err(|e| ext_php_rs::exception::PhpException::default(format!("{}", e)))
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<AccelerationConfig> for xberg::AccelerationConfig {
    fn from(val: AccelerationConfig) -> Self {
        Self {
            provider: match val.provider.as_str() {
                "auto" => xberg::ExecutionProviderType::Auto,
                "cpu" => xberg::ExecutionProviderType::Cpu,
                "coreml" => xberg::ExecutionProviderType::CoreMl,
                "cuda" => xberg::ExecutionProviderType::Cuda,
                "tensorrt" => xberg::ExecutionProviderType::TensorRt,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'provider' of ExecutionProviderType: expected one of [auto, cpu, coreml, cuda, tensorrt]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            device_id: val.device_id,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::AccelerationConfig> for AccelerationConfig {
    fn from(val: xberg::AccelerationConfig) -> Self {
        Self {
            provider: serde_json::to_value(val.provider)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            device_id: val.device_id,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ArchiveEntry> for xberg::ArchiveEntry {
    fn from(val: ArchiveEntry) -> Self {
        Self {
            path: val.path,
            mime_type: val.mime_type,
            result: Box::new(val.result.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ArchiveEntry> for ArchiveEntry {
    fn from(val: xberg::ArchiveEntry) -> Self {
        Self {
            path: val.path.to_string(),
            mime_type: val.mime_type.to_string(),
            result: (*val.result).into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ArchiveMetadata> for xberg::ArchiveMetadata {
    fn from(val: ArchiveMetadata) -> Self {
        Self {
            format: val.format.into(),
            file_count: val.file_count,
            file_list: val.file_list.into_iter().collect(),
            total_size: val.total_size as u64,
            compressed_size: val.compressed_size.map(|v| v as u64),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ArchiveMetadata> for ArchiveMetadata {
    fn from(val: xberg::ArchiveMetadata) -> Self {
        Self {
            format: val.format.to_string(),
            file_count: val.file_count,
            file_list: val.file_list.into_iter().collect(),
            total_size: val.total_size as i64,
            compressed_size: val.compressed_size.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<Attributes> for xberg::Attributes {
    fn from(val: Attributes) -> Self {
        Self {
            id: val.id,
            classes: val.classes.into_iter().collect(),
            key_values: val.key_values.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Attributes> for Attributes {
    fn from(val: xberg::Attributes) -> Self {
        Self {
            id: val.id.map(|v| v.to_string()),
            classes: val.classes.into_iter().collect(),
            key_values: val.key_values.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<AudioMetadata> for xberg::AudioMetadata {
    fn from(val: AudioMetadata) -> Self {
        Self {
            duration_ms: val.duration_ms.map(|v| v as u64),
            codec: val.codec,
            container: val.container,
            sample_rate_hz: val.sample_rate_hz,
            channels: val.channels,
            bitrate: val.bitrate,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::AudioMetadata> for AudioMetadata {
    fn from(val: xberg::AudioMetadata) -> Self {
        Self {
            duration_ms: val.duration_ms.map(|v| v as i64),
            codec: val.codec.map(|v| v.to_string()),
            container: val.container.map(|v| v.to_string()),
            sample_rate_hz: val.sample_rate_hz,
            channels: val.channels,
            bitrate: val.bitrate,
        }
    }
}

impl From<xberg::BBox> for BBox {
    fn from(val: xberg::BBox) -> Self {
        Self {
            x1: val.x1,
            y1: val.y1,
            x2: val.x2,
            y2: val.y2,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<BedrockConfig> for xberg::BedrockConfig {
    fn from(val: BedrockConfig) -> Self {
        Self {
            region: val.region,
            cross_region_prefix: val.cross_region_prefix,
            access_key_id: val.access_key_id,
            secret_access_key: val.secret_access_key,
            session_token: val.session_token,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::BedrockConfig> for BedrockConfig {
    fn from(val: xberg::BedrockConfig) -> Self {
        Self {
            region: val.region.map(|v| v.to_string()),
            cross_region_prefix: val.cross_region_prefix.map(|v| v.to_string()),
            access_key_id: val.access_key_id.map(|v| v.to_string()),
            secret_access_key: val.secret_access_key.map(|v| v.to_string()),
            session_token: val.session_token.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BibtexMetadata> for xberg::BibtexMetadata {
    fn from(val: BibtexMetadata) -> Self {
        Self {
            entry_count: val.entry_count as usize,
            citation_keys: val.citation_keys.into_iter().collect(),
            authors: val.authors.into_iter().collect(),
            year_range: val.year_range.map(Into::into),
            entry_types: val
                .entry_types
                .map(|m| m.into_iter().map(|(k, v)| (k, v as usize)).collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::BibtexMetadata> for BibtexMetadata {
    fn from(val: xberg::BibtexMetadata) -> Self {
        Self {
            entry_count: val.entry_count as i64,
            citation_keys: val.citation_keys.into_iter().collect(),
            authors: val.authors.into_iter().collect(),
            year_range: val.year_range.map(Into::into),
            entry_types: val
                .entry_types
                .as_ref()
                .map(|m| m.iter().map(|(k, v)| (k.clone(), *v as i64)).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<BoundingBox> for xberg::BoundingBox {
    fn from(val: BoundingBox) -> Self {
        Self {
            x0: val.x0,
            y0: val.y0,
            x1: val.x1,
            y1: val.y1,
            ..Default::default()
        }
    }
}

impl From<xberg::BoundingBox> for BoundingBox {
    fn from(val: xberg::BoundingBox) -> Self {
        Self {
            x0: val.x0,
            y0: val.y0,
            x1: val.x1,
            y1: val.y1,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<BrowserConfig> for crawlberg::BrowserConfig {
    fn from(val: BrowserConfig) -> Self {
        Self {
            mode: match val.mode.as_str() {
                "auto" => crawlberg::BrowserMode::Auto,
                "always" => crawlberg::BrowserMode::Always,
                "never" => crawlberg::BrowserMode::Never,
                "stealth" => crawlberg::BrowserMode::Stealth,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'mode' of BrowserMode: expected one of [auto, always, never, stealth]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            backend: match val.backend.as_str() {
                "chromiumoxide" => crawlberg::BrowserBackend::Chromiumoxide,
                "native" => crawlberg::BrowserBackend::Native,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'backend' of BrowserBackend: expected one of [chromiumoxide, native]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            endpoint: val.endpoint,
            timeout: val
                .timeout
                .map(|v| std::time::Duration::from_millis(v as u64))
                .unwrap_or_else(|| crawlberg::BrowserConfig::default().timeout),
            wait: match val.wait.as_str() {
                "network_idle" => crawlberg::BrowserWait::NetworkIdle,
                "selector" => crawlberg::BrowserWait::Selector,
                "fixed" => crawlberg::BrowserWait::Fixed,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'wait' of BrowserWait: expected one of [network_idle, selector, fixed]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            wait_selector: val.wait_selector,
            extra_wait: val.extra_wait.map(|v| std::time::Duration::from_millis(v as u64)),
            proxy: val.proxy.map(Into::into),
            block_url_patterns: val.block_url_patterns.into_iter().collect(),
            eval_script: val.eval_script,
            robots_user_agent: val.robots_user_agent,
            capture_network_events: val.capture_network_events,
            session_affinity: val.session_affinity,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<crawlberg::BrowserConfig> for BrowserConfig {
    fn from(val: crawlberg::BrowserConfig) -> Self {
        Self {
            mode: serde_json::to_value(val.mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            backend: serde_json::to_value(val.backend)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            endpoint: val.endpoint.map(|v| v.to_string()),
            timeout: Some(val.timeout.as_millis() as u64 as i64),
            wait: serde_json::to_value(val.wait)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            wait_selector: val.wait_selector.map(|v| v.to_string()),
            extra_wait: val.extra_wait.map(|d| d.as_millis() as u64 as i64),
            proxy: val.proxy.map(Into::into),
            block_url_patterns: val.block_url_patterns.into_iter().collect(),
            eval_script: val.eval_script.map(|v| v.to_string()),
            robots_user_agent: val.robots_user_agent.map(|v| v.to_string()),
            capture_network_events: val.capture_network_events,
            session_affinity: val.session_affinity,
        }
    }
}

impl From<xberg::CacheStats> for CacheStats {
    fn from(val: xberg::CacheStats) -> Self {
        Self {
            total_files: val.total_files as i64,
            total_size_mb: val.total_size_mb,
            available_space_mb: val.available_space_mb,
            oldest_file_age_days: val.oldest_file_age_days,
            newest_file_age_days: val.newest_file_age_days,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<CaptioningConfig> for xberg::CaptioningConfig {
    fn from(val: CaptioningConfig) -> Self {
        Self {
            llm: val.llm.into(),
            prompt: val.prompt,
            min_image_area: val.min_image_area,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CaptioningConfig> for CaptioningConfig {
    fn from(val: xberg::CaptioningConfig) -> Self {
        Self {
            llm: val.llm.into(),
            prompt: val.prompt.map(|v| v.to_string()),
            min_image_area: val.min_image_area,
        }
    }
}

impl From<CellChange> for xberg::CellChange {
    fn from(val: CellChange) -> Self {
        Self {
            row: val.row as usize,
            col: val.col as usize,
            from: val.from,
            to: val.to,
        }
    }
}

impl From<xberg::CellChange> for CellChange {
    fn from(val: xberg::CellChange) -> Self {
        Self {
            row: val.row as i64,
            col: val.col as i64,
            from: val.from.to_string(),
            to: val.to.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Chunk> for xberg::Chunk {
    fn from(val: Chunk) -> Self {
        Self {
            content: val.content,
            chunk_type: match val.chunk_type.as_str() {
                "heading" => xberg::ChunkType::Heading,
                "party_list" => xberg::ChunkType::PartyList,
                "definitions" => xberg::ChunkType::Definitions,
                "operative_clause" => xberg::ChunkType::OperativeClause,
                "signature_block" => xberg::ChunkType::SignatureBlock,
                "schedule" => xberg::ChunkType::Schedule,
                "table_like" => xberg::ChunkType::TableLike,
                "formula" => xberg::ChunkType::Formula,
                "code_block" => xberg::ChunkType::CodeBlock,
                "function" => xberg::ChunkType::Function,
                "class" => xberg::ChunkType::Class,
                "module" => xberg::ChunkType::Module,
                "image" => xberg::ChunkType::Image,
                "org_chart" => xberg::ChunkType::OrgChart,
                "diagram" => xberg::ChunkType::Diagram,
                "unknown" => xberg::ChunkType::Unknown,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'chunk_type' of ChunkType: expected one of [heading, party_list, definitions, operative_clause, signature_block, schedule, table_like, formula, code_block, function, class, module, image, org_chart, diagram, unknown]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            embedding: val.embedding.map(|v| v.into_iter().collect()),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Chunk> for Chunk {
    fn from(val: xberg::Chunk) -> Self {
        Self {
            content: val.content.to_string(),
            chunk_type: serde_json::to_value(val.chunk_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            embedding: val.embedding.map(|v| v.into_iter().collect()),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ChunkClassificationConfig> for xberg::ChunkClassificationConfig {
    fn from(val: ChunkClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template,
            definitions: val.definitions.into_iter().map(Into::into).collect(),
            llm: val.llm.into(),
            batch_size: val.batch_size as usize,
            max_concurrency: val.max_concurrency as usize,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkClassificationConfig> for ChunkClassificationConfig {
    fn from(val: xberg::ChunkClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template.map(|v| v.to_string()),
            definitions: val.definitions.into_iter().map(Into::into).collect(),
            llm: val.llm.into(),
            batch_size: val.batch_size as i64,
            max_concurrency: val.max_concurrency as i64,
        }
    }
}

impl From<ChunkClassificationDefinition> for xberg::ChunkClassificationDefinition {
    fn from(val: ChunkClassificationDefinition) -> Self {
        Self {
            label: val.label,
            description: val.description,
        }
    }
}

impl From<xberg::ChunkClassificationDefinition> for ChunkClassificationDefinition {
    fn from(val: xberg::ChunkClassificationDefinition) -> Self {
        Self {
            label: val.label.to_string(),
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ChunkClassificationEnrichmentConfig> for ChunkClassificationEnrichmentConfig {
    fn from(val: xberg::ChunkClassificationEnrichmentConfig) -> Self {
        Self {
            config: val.config.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ChunkInfo> for ChunkInfo {
    fn from(val: xberg::ChunkInfo) -> Self {
        Self {
            index: val.index,
            pages: val.pages.into(),
            estimated_time_ms: val.estimated_time_ms as i64,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ChunkMetadata> for xberg::ChunkMetadata {
    fn from(val: ChunkMetadata) -> Self {
        Self {
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
            token_count: val.token_count.map(|v| v as usize),
            chunk_index: val.chunk_index as usize,
            total_chunks: val.total_chunks as usize,
            first_page: val.first_page,
            last_page: val.last_page,
            heading_context: val.heading_context.map(Into::into),
            heading_path: val.heading_path.into_iter().collect(),
            image_indices: val.image_indices.into_iter().collect(),
            node_ids: val.node_ids.into_iter().collect(),
            page_spans: val.page_spans.into_iter().map(Into::into).collect(),
            classifications: val.classifications.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkMetadata> for ChunkMetadata {
    fn from(val: xberg::ChunkMetadata) -> Self {
        Self {
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
            token_count: val.token_count.map(|v| v as i64),
            chunk_index: val.chunk_index as i64,
            total_chunks: val.total_chunks as i64,
            first_page: val.first_page,
            last_page: val.last_page,
            heading_context: val.heading_context.map(Into::into),
            heading_path: val.heading_path.into_iter().collect(),
            image_indices: val.image_indices.into_iter().collect(),
            node_ids: val.node_ids.into_iter().collect(),
            page_spans: val.page_spans.into_iter().map(Into::into).collect(),
            classifications: val.classifications.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ChunkingConfig> for xberg::ChunkingConfig {
    fn from(val: ChunkingConfig) -> Self {
        Self {
            max_characters: val.max_characters as usize,
            overlap: val.overlap as usize,
            trim: val.trim,
            chunker_type: match val.chunker_type.as_str() {
                "text" => xberg::ChunkerType::Text,
                "markdown" => xberg::ChunkerType::Markdown,
                "yaml" => xberg::ChunkerType::Yaml,
                "semantic" => xberg::ChunkerType::Semantic,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'chunker_type' of ChunkerType: expected one of [text, markdown, yaml, semantic]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            embedding: val.embedding.map(Into::into),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            preset: val.preset,
            sizing: val.sizing.into(),
            topic_threshold: val.topic_threshold,
            table_chunking: match val.table_chunking.as_str() {
                "split" => xberg::TableChunkingMode::Split,
                "repeat_header" => xberg::TableChunkingMode::RepeatHeader,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'table_chunking' of TableChunkingMode: expected one of [split, repeat_header]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkingConfig> for ChunkingConfig {
    fn from(val: xberg::ChunkingConfig) -> Self {
        Self {
            max_characters: val.max_characters as i64,
            overlap: val.overlap as i64,
            trim: val.trim,
            chunker_type: serde_json::to_value(val.chunker_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            embedding: val.embedding.map(Into::into),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            preset: val.preset.map(|v| v.to_string()),
            sizing: val.sizing.into(),
            topic_threshold: val.topic_threshold,
            table_chunking: serde_json::to_value(val.table_chunking)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Citation> for Citation {
    fn from(val: xberg::Citation) -> Self {
        Self {
            label: val.label.to_string(),
            source: val.source.to_string(),
            locator: val.locator.map(|v| v.to_string()),
            excerpt: val.excerpt.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<CitationMetadata> for xberg::CitationMetadata {
    fn from(val: CitationMetadata) -> Self {
        Self {
            citation_count: val.citation_count as usize,
            format: val.format,
            authors: val.authors.into_iter().collect(),
            year_range: val.year_range.map(Into::into),
            dois: val.dois.into_iter().collect(),
            keywords: val.keywords.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CitationMetadata> for CitationMetadata {
    fn from(val: xberg::CitationMetadata) -> Self {
        Self {
            citation_count: val.citation_count as i64,
            format: val.format.map(|v| v.to_string()),
            authors: val.authors.into_iter().collect(),
            year_range: val.year_range.map(Into::into),
            dois: val.dois.into_iter().collect(),
            keywords: val.keywords.into_iter().collect(),
        }
    }
}

impl From<ClassificationLabel> for xberg::ClassificationLabel {
    fn from(val: ClassificationLabel) -> Self {
        Self {
            label: val.label,
            confidence: val.confidence,
        }
    }
}

impl From<xberg::ClassificationLabel> for ClassificationLabel {
    fn from(val: xberg::ClassificationLabel) -> Self {
        Self {
            label: val.label.to_string(),
            confidence: val.confidence,
        }
    }
}

impl From<CodeChunkInfo> for xberg::CodeChunkInfo {
    fn from(val: CodeChunkInfo) -> Self {
        Self {
            text: val.text,
            context_path: val.context_path.into_iter().collect(),
            node_types: val.node_types.into_iter().collect(),
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

impl From<xberg::CodeChunkInfo> for CodeChunkInfo {
    fn from(val: xberg::CodeChunkInfo) -> Self {
        Self {
            text: val.text.to_string(),
            context_path: val.context_path.into_iter().collect(),
            node_types: val.node_types.into_iter().collect(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

impl From<CodeDataAttribute> for xberg::CodeDataAttribute {
    fn from(val: CodeDataAttribute) -> Self {
        Self {
            name: val.name,
            value: val.value,
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

impl From<xberg::CodeDataAttribute> for CodeDataAttribute {
    fn from(val: xberg::CodeDataAttribute) -> Self {
        Self {
            name: val.name.to_string(),
            value: val.value.to_string(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<CodeDataNode> for xberg::CodeDataNode {
    fn from(val: CodeDataNode) -> Self {
        Self {
            kind: match val.kind.as_str() {
                "key_value" => xberg::CodeDataNodeKind::KeyValue,
                "element" => xberg::CodeDataNodeKind::Element,
                "sequence" => xberg::CodeDataNodeKind::Sequence,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'kind' of CodeDataNodeKind: expected one of [key_value, element, sequence]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            key: val.key,
            value: val.value,
            attributes: val.attributes.into_iter().map(Into::into).collect(),
            children: val.children.into_iter().map(Into::into).collect(),
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CodeDataNode> for CodeDataNode {
    fn from(val: xberg::CodeDataNode) -> Self {
        Self {
            kind: serde_json::to_value(val.kind)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            key: val.key.map(|v| v.to_string()),
            value: val.value.map(|v| v.to_string()),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
            children: val.children.into_iter().map(Into::into).collect(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<CodeMetadata> for xberg::CodeMetadata {
    fn from(val: CodeMetadata) -> Self {
        Self {
            chunks: val.chunks.into_iter().map(Into::into).collect(),
            data: val.data.map(Into::into),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::CodeMetadata> for CodeMetadata {
    fn from(val: xberg::CodeMetadata) -> Self {
        Self {
            chunks: val.chunks.into_iter().map(Into::into).collect(),
            data: val.data.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<ConcurrencyConfig> for xberg::ConcurrencyConfig {
    fn from(val: ConcurrencyConfig) -> Self {
        Self {
            max_threads: val.max_threads.map(|v| v as usize),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ConcurrencyConfig> for ConcurrencyConfig {
    fn from(val: xberg::ConcurrencyConfig) -> Self {
        Self {
            max_threads: val.max_threads.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<ContentConfig> for crawlberg::ContentConfig {
    fn from(val: ContentConfig) -> Self {
        Self {
            output_format: val.output_format,
            preprocessing_preset: val.preprocessing_preset,
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            skip_images: val.skip_images,
            max_depth: val.max_depth.map(|v| v as usize),
            wrap: val.wrap,
            wrap_width: val.wrap_width as usize,
            include_document_structure: val.include_document_structure,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::ContentConfig> for ContentConfig {
    fn from(val: crawlberg::ContentConfig) -> Self {
        Self {
            output_format: val.output_format.to_string(),
            preprocessing_preset: val.preprocessing_preset.to_string(),
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            skip_images: val.skip_images,
            max_depth: val.max_depth.map(|v| v as i64),
            wrap: val.wrap,
            wrap_width: val.wrap_width as i64,
            include_document_structure: val.include_document_structure,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ContentFilterConfig> for xberg::ContentFilterConfig {
    fn from(val: ContentFilterConfig) -> Self {
        Self {
            include_headers: val.include_headers,
            include_footers: val.include_footers,
            include_footnotes: val.include_footnotes,
            strip_repeating_text: val.strip_repeating_text,
            include_watermarks: val.include_watermarks,
            ..Default::default()
        }
    }
}

impl From<xberg::ContentFilterConfig> for ContentFilterConfig {
    fn from(val: xberg::ContentFilterConfig) -> Self {
        Self {
            include_headers: val.include_headers,
            include_footers: val.include_footers,
            include_footnotes: val.include_footnotes,
            strip_repeating_text: val.strip_repeating_text,
            include_watermarks: val.include_watermarks,
        }
    }
}

impl From<ContributorRole> for xberg::ContributorRole {
    fn from(val: ContributorRole) -> Self {
        Self {
            name: val.name,
            role: val.role,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ContributorRole> for ContributorRole {
    fn from(val: xberg::ContributorRole) -> Self {
        Self {
            name: val.name.to_string(),
            role: val.role.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ConversionOptions> for html_to_markdown_rs::ConversionOptions {
    fn from(val: ConversionOptions) -> Self {
        Self {
            heading_style: match val.heading_style.as_str() {
                "Underlined" | "underlined" => html_to_markdown_rs::HeadingStyle::Underlined,
                "Atx" | "atx" => html_to_markdown_rs::HeadingStyle::Atx,
                "AtxClosed" | "atxclosed" => html_to_markdown_rs::HeadingStyle::AtxClosed,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'heading_style' of HeadingStyle: expected one of [Underlined, Atx, AtxClosed]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            list_indent_type: match val.list_indent_type.as_str() {
                "Spaces" | "spaces" => html_to_markdown_rs::ListIndentType::Spaces,
                "Tabs" | "tabs" => html_to_markdown_rs::ListIndentType::Tabs,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'list_indent_type' of ListIndentType: expected one of [Spaces, Tabs]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            list_indent_width: val.list_indent_width as usize,
            bullets: val.bullets,
            strong_em_symbol: val.strong_em_symbol.chars().next().unwrap_or('*'),
            escape_asterisks: val.escape_asterisks,
            escape_underscores: val.escape_underscores,
            escape_misc: val.escape_misc,
            escape_ascii: val.escape_ascii,
            code_language: val.code_language,
            autolinks: val.autolinks,
            default_title: val.default_title,
            br_in_tables: val.br_in_tables,
            compact_tables: val.compact_tables,
            highlight_style: match val.highlight_style.as_str() {
                "DoubleEqual" | "doubleequal" => html_to_markdown_rs::HighlightStyle::DoubleEqual,
                "Html" | "html" => html_to_markdown_rs::HighlightStyle::Html,
                "Bold" | "bold" => html_to_markdown_rs::HighlightStyle::Bold,
                "None" | "none" => html_to_markdown_rs::HighlightStyle::None,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'highlight_style' of HighlightStyle: expected one of [DoubleEqual, Html, Bold, None]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            extract_metadata: val.extract_metadata,
            whitespace_mode: match val.whitespace_mode.as_str() {
                "Normalized" | "normalized" => html_to_markdown_rs::WhitespaceMode::Normalized,
                "Strict" | "strict" => html_to_markdown_rs::WhitespaceMode::Strict,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'whitespace_mode' of WhitespaceMode: expected one of [Normalized, Strict]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            strip_newlines: val.strip_newlines,
            wrap: val.wrap,
            wrap_width: val.wrap_width as usize,
            convert_as_inline: val.convert_as_inline,
            sub_symbol: val.sub_symbol,
            sup_symbol: val.sup_symbol,
            newline_style: match val.newline_style.as_str() {
                "Spaces" | "spaces" => html_to_markdown_rs::NewlineStyle::Spaces,
                "Backslash" | "backslash" => html_to_markdown_rs::NewlineStyle::Backslash,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'newline_style' of NewlineStyle: expected one of [Spaces, Backslash]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            code_block_style: match val.code_block_style.as_str() {
                "Indented" | "indented" => html_to_markdown_rs::CodeBlockStyle::Indented,
                "Backticks" | "backticks" => html_to_markdown_rs::CodeBlockStyle::Backticks,
                "Tildes" | "tildes" => html_to_markdown_rs::CodeBlockStyle::Tildes,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'code_block_style' of CodeBlockStyle: expected one of [Indented, Backticks, Tildes]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            keep_inline_images_in: val.keep_inline_images_in.into_iter().collect(),
            preprocessing: val.preprocessing.into(),
            encoding: val.encoding,
            debug: val.debug,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            skip_images: val.skip_images,
            url_escape_style: match val.url_escape_style.as_str() {
                "Angle" | "angle" => html_to_markdown_rs::UrlEscapeStyle::Angle,
                "Percent" | "percent" => html_to_markdown_rs::UrlEscapeStyle::Percent,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'url_escape_style' of UrlEscapeStyle: expected one of [Angle, Percent]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            link_style: match val.link_style.as_str() {
                "Inline" | "inline" => html_to_markdown_rs::LinkStyle::Inline,
                "Reference" | "reference" => html_to_markdown_rs::LinkStyle::Reference,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'link_style' of LinkStyle: expected one of [Inline, Reference]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            output_format: match val.output_format.as_str() {
                "Markdown" | "markdown" => html_to_markdown_rs::OutputFormat::Markdown,
                "Djot" | "djot" => html_to_markdown_rs::OutputFormat::Djot,
                "Plain" | "plain" => html_to_markdown_rs::OutputFormat::Plain,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'output_format' of HtmlToMarkdownRsOutputFormat: expected one of [Markdown, Djot, Plain]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            include_document_structure: val.include_document_structure,
            extract_images: val.extract_images,
            max_image_size: val.max_image_size as u64,
            capture_svg: val.capture_svg,
            infer_dimensions: val.infer_dimensions,
            max_depth: val.max_depth.map(|v| v as usize),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            tier_strategy: match val.tier_strategy.as_str() {
                "auto" => html_to_markdown_rs::TierStrategy::Auto,
                "tier2" => html_to_markdown_rs::TierStrategy::Tier2,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'tier_strategy' of TierStrategy: expected one of [auto, tier2]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<html_to_markdown_rs::ConversionOptions> for ConversionOptions {
    fn from(val: html_to_markdown_rs::ConversionOptions) -> Self {
        Self {
            heading_style: serde_json::to_value(val.heading_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            list_indent_type: serde_json::to_value(val.list_indent_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            list_indent_width: val.list_indent_width as i64,
            bullets: val.bullets.to_string(),
            strong_em_symbol: val.strong_em_symbol.to_string(),
            escape_asterisks: val.escape_asterisks,
            escape_underscores: val.escape_underscores,
            escape_misc: val.escape_misc,
            escape_ascii: val.escape_ascii,
            code_language: val.code_language.to_string(),
            autolinks: val.autolinks,
            default_title: val.default_title,
            br_in_tables: val.br_in_tables,
            compact_tables: val.compact_tables,
            highlight_style: serde_json::to_value(val.highlight_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            extract_metadata: val.extract_metadata,
            whitespace_mode: serde_json::to_value(val.whitespace_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            strip_newlines: val.strip_newlines,
            wrap: val.wrap,
            wrap_width: val.wrap_width as i64,
            convert_as_inline: val.convert_as_inline,
            sub_symbol: val.sub_symbol.to_string(),
            sup_symbol: val.sup_symbol.to_string(),
            newline_style: serde_json::to_value(val.newline_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            code_block_style: serde_json::to_value(val.code_block_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            keep_inline_images_in: val.keep_inline_images_in.into_iter().collect(),
            preprocessing: val.preprocessing.into(),
            encoding: val.encoding.to_string(),
            debug: val.debug,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            skip_images: val.skip_images,
            url_escape_style: serde_json::to_value(val.url_escape_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            link_style: serde_json::to_value(val.link_style)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            output_format: serde_json::to_value(val.output_format)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            include_document_structure: val.include_document_structure,
            extract_images: val.extract_images,
            max_image_size: val.max_image_size as i64,
            capture_svg: val.capture_svg,
            infer_dimensions: val.infer_dimensions,
            max_depth: val.max_depth.map(|v| v as i64),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            tier_strategy: serde_json::to_value(val.tier_strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<CoreProperties> for xberg::CoreProperties {
    fn from(val: CoreProperties) -> Self {
        Self {
            title: val.title,
            subject: val.subject,
            creator: val.creator,
            keywords: val.keywords,
            description: val.description,
            last_modified_by: val.last_modified_by,
            revision: val.revision,
            created: val.created,
            modified: val.modified,
            category: val.category,
            content_status: val.content_status,
            language: val.language,
            identifier: val.identifier,
            version: val.version,
            last_printed: val.last_printed,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CoreProperties> for CoreProperties {
    fn from(val: xberg::CoreProperties) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            creator: val.creator.map(|v| v.to_string()),
            keywords: val.keywords.map(|v| v.to_string()),
            description: val.description.map(|v| v.to_string()),
            last_modified_by: val.last_modified_by.map(|v| v.to_string()),
            revision: val.revision.map(|v| v.to_string()),
            created: val.created.map(|v| v.to_string()),
            modified: val.modified.map(|v| v.to_string()),
            category: val.category.map(|v| v.to_string()),
            content_status: val.content_status.map(|v| v.to_string()),
            language: val.language.map(|v| v.to_string()),
            identifier: val.identifier.map(|v| v.to_string()),
            version: val.version.map(|v| v.to_string()),
            last_printed: val.last_printed.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<CrawlConfig> for crawlberg::CrawlConfig {
    fn from(val: CrawlConfig) -> Self {
        Self {
            max_depth: val.max_depth.map(|v| v as usize),
            max_pages: val.max_pages.map(|v| v as usize),
            max_links_per_page: val.max_links_per_page.map(|v| v as usize),
            max_concurrent: val.max_concurrent.map(|v| v as usize),
            crawl_strategy: match val.crawl_strategy.as_str() { "bfs" => crawlberg::CrawlStrategyKind::Bfs,"dfs" => crawlberg::CrawlStrategyKind::Dfs,"best_first" => crawlberg::CrawlStrategyKind::BestFirst,"adaptive" => crawlberg::CrawlStrategyKind::Adaptive,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'crawl_strategy' of CrawlStrategyKind: expected one of [bfs, dfs, best_first, adaptive]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 }
,
            content_filter: val.content_filter.as_deref().map(|s| match s { "bm25" => crawlberg::ContentFilterKind::Bm25,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'content_filter' of ContentFilterKind: expected one of [bm25]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    crawlberg::ContentFilterKind::Bm25
}
 })
,
            bm25_query: val.bm25_query,
            bm25_threshold: val.bm25_threshold,
            respect_robots_txt: val.respect_robots_txt,
            soft_http_errors: val.soft_http_errors,
            user_agent: val.user_agent,
            stay_on_domain: val.stay_on_domain,
            allow_subdomains: val.allow_subdomains,
            include_paths: val.include_paths.into_iter().collect(),
            exclude_paths: val.exclude_paths.into_iter().collect(),
            custom_headers: val.custom_headers.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            request_timeout: val.request_timeout.map(|v| std::time::Duration::from_millis(v as u64)).unwrap_or_else(|| crawlberg::CrawlConfig::default().request_timeout),
            rate_limit_ms: val.rate_limit_ms.map(|v| v as u64),
            max_redirects: val.max_redirects as usize,
            retry_count: val.retry_count as usize,
            retry_codes: val.retry_codes.into_iter().collect(),
            cookies_enabled: val.cookies_enabled,
            auth: val.auth.map(Into::into),
            max_body_size: val.max_body_size.map(|v| v as usize),
            remove_tags: val.remove_tags.into_iter().collect(),
            content: val.content.into(),
            map_limit: val.map_limit.map(|v| v as usize),
            map_search: val.map_search,
            download_assets: val.download_assets,
            asset_types: val.asset_types.into_iter().map(|s| match s.as_str() { "document" => crawlberg::AssetCategory::Document,"image" => crawlberg::AssetCategory::Image,"audio" => crawlberg::AssetCategory::Audio,"video" => crawlberg::AssetCategory::Video,"font" => crawlberg::AssetCategory::Font,"stylesheet" => crawlberg::AssetCategory::Stylesheet,"script" => crawlberg::AssetCategory::Script,"archive" => crawlberg::AssetCategory::Archive,"data" => crawlberg::AssetCategory::Data,"other" => crawlberg::AssetCategory::Other,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'asset_types' of AssetCategory: expected one of [document, image, audio, video, font, stylesheet, script, archive, data, other]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 }
).collect(),
            max_asset_size: val.max_asset_size.map(|v| v as usize),
            browser: val.browser.into(),
            proxy: val.proxy.map(Into::into),
            user_agents: val.user_agents.into_iter().collect(),
            capture_screenshot: val.capture_screenshot,
            follow_document_urls: val.follow_document_urls,
            document_url_depth: val.document_url_depth,
            download_documents: val.download_documents,
            document_max_size: val.document_max_size.map(|v| v as usize),
            document_mime_types: val.document_mime_types.into_iter().collect(),
            document_output_dir: val.document_output_dir.map(Into::into),
            document_content_encoding: val.document_content_encoding.as_deref().map(|s| match s { "base64" => crawlberg::DocumentContentEncoding::Base64,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'document_content_encoding' of DocumentContentEncoding: expected one of [base64]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    crawlberg::DocumentContentEncoding::Base64
}
 })
,
            warc_output: val.warc_output.map(Into::into),
            browser_profile: val.browser_profile,
            save_browser_profile: val.save_browser_profile,
            ssrf: val.ssrf.into(),
            ssrf_deny_private_explicit: val.ssrf_deny_private_explicit,
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<crawlberg::CrawlConfig> for CrawlConfig {
    fn from(val: crawlberg::CrawlConfig) -> Self {
        Self {
            max_depth: val.max_depth.map(|v| v as i64),
            max_pages: val.max_pages.map(|v| v as i64),
            max_links_per_page: val.max_links_per_page.map(|v| v as i64),
            max_concurrent: val.max_concurrent.map(|v| v as i64),
            crawl_strategy: serde_json::to_value(val.crawl_strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            content_filter: val.content_filter.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            bm25_query: val.bm25_query.map(|v| v.to_string()),
            bm25_threshold: val.bm25_threshold,
            respect_robots_txt: val.respect_robots_txt,
            soft_http_errors: val.soft_http_errors,
            user_agent: val.user_agent.map(|v| v.to_string()),
            stay_on_domain: val.stay_on_domain,
            allow_subdomains: val.allow_subdomains,
            include_paths: val.include_paths.into_iter().collect(),
            exclude_paths: val.exclude_paths.into_iter().collect(),
            custom_headers: val
                .custom_headers
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            request_timeout: Some(val.request_timeout.as_millis() as u64 as i64),
            rate_limit_ms: val.rate_limit_ms.map(|v| v as i64),
            max_redirects: val.max_redirects as i64,
            retry_count: val.retry_count as i64,
            retry_codes: val.retry_codes.into_iter().collect(),
            cookies_enabled: val.cookies_enabled,
            auth: val.auth.map(Into::into),
            max_body_size: val.max_body_size.map(|v| v as i64),
            remove_tags: val.remove_tags.into_iter().collect(),
            content: val.content.into(),
            map_limit: val.map_limit.map(|v| v as i64),
            map_search: val.map_search.map(|v| v.to_string()),
            download_assets: val.download_assets,
            asset_types: val
                .asset_types
                .iter()
                .map(|v| {
                    serde_json::to_value(v)
                        .ok()
                        .and_then(|s| s.as_str().map(String::from))
                        .unwrap_or_default()
                })
                .collect(),
            max_asset_size: val.max_asset_size.map(|v| v as i64),
            browser: val.browser.into(),
            proxy: val.proxy.map(Into::into),
            user_agents: val.user_agents.into_iter().collect(),
            capture_screenshot: val.capture_screenshot,
            follow_document_urls: val.follow_document_urls,
            document_url_depth: val.document_url_depth,
            download_documents: val.download_documents,
            document_max_size: val.document_max_size.map(|v| v as i64),
            document_mime_types: val.document_mime_types.into_iter().collect(),
            document_output_dir: val.document_output_dir.map(|p| p.to_string_lossy().to_string()),
            document_content_encoding: val.document_content_encoding.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            warc_output: val.warc_output.map(|p| p.to_string_lossy().to_string()),
            browser_profile: val.browser_profile.map(|v| v.to_string()),
            save_browser_profile: val.save_browser_profile,
            ssrf: val.ssrf.into(),
            ssrf_deny_private_explicit: val.ssrf_deny_private_explicit,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<CsvConfig> for xberg::CsvConfig {
    fn from(val: CsvConfig) -> Self {
        Self {
            delimiter: val.delimiter,
            comment_prefixes: val.comment_prefixes.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CsvConfig> for CsvConfig {
    fn from(val: xberg::CsvConfig) -> Self {
        Self {
            delimiter: val.delimiter.map(|v| v.to_string()),
            comment_prefixes: val.comment_prefixes.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<CsvMetadata> for xberg::CsvMetadata {
    fn from(val: CsvMetadata) -> Self {
        Self {
            row_count: val.row_count,
            column_count: val.column_count,
            delimiter: val.delimiter,
            has_header: val.has_header,
            column_types: val.column_types.map(|v| v.into_iter().collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CsvMetadata> for CsvMetadata {
    fn from(val: xberg::CsvMetadata) -> Self {
        Self {
            row_count: val.row_count,
            column_count: val.column_count,
            delimiter: val.delimiter.map(|v| v.to_string()),
            has_header: val.has_header,
            column_types: val.column_types.map(|v| v.into_iter().collect()),
        }
    }
}

impl From<DbfFieldInfo> for xberg::DbfFieldInfo {
    fn from(val: DbfFieldInfo) -> Self {
        Self {
            name: val.name,
            field_type: val.field_type,
        }
    }
}

impl From<xberg::DbfFieldInfo> for DbfFieldInfo {
    fn from(val: xberg::DbfFieldInfo) -> Self {
        Self {
            name: val.name.to_string(),
            field_type: val.field_type.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<DbfMetadata> for xberg::DbfMetadata {
    fn from(val: DbfMetadata) -> Self {
        Self {
            record_count: val.record_count as usize,
            field_count: val.field_count as usize,
            fields: val.fields.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DbfMetadata> for DbfMetadata {
    fn from(val: xberg::DbfMetadata) -> Self {
        Self {
            record_count: val.record_count as i64,
            field_count: val.field_count as i64,
            fields: val.fields.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::candle_ocr::DeepseekOcrBackendOptions> for DeepseekOcrBackendOptions {
    fn from(val: xberg::candle_ocr::DeepseekOcrBackendOptions) -> Self {
        Self {
            model_path: val.model_path.map(|v| v.to_string()),
            device: val.device.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            version: val.version,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::api::DetectResponse> for DetectResponse {
    fn from(val: xberg::api::DetectResponse) -> Self {
        Self {
            mime_type: val.mime_type.to_string(),
            filename: val.filename.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DetectionResult> for DetectionResult {
    fn from(val: xberg::DetectionResult) -> Self {
        Self {
            page_width: val.page_width,
            page_height: val.page_height,
            detections: val.detections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DiffHunk> for DiffHunk {
    fn from(val: xberg::DiffHunk) -> Self {
        Self {
            from_line: val.from_line as i64,
            from_count: val.from_count as i64,
            to_line: val.to_line as i64,
            to_count: val.to_count as i64,
            lines: val.lines.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<DiffOptions> for xberg::DiffOptions {
    fn from(val: DiffOptions) -> Self {
        Self {
            include_metadata: val.include_metadata,
            include_embedded: val.include_embedded,
            max_content_chars: val.max_content_chars.map(|v| v as usize),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DiffOptions> for DiffOptions {
    fn from(val: xberg::DiffOptions) -> Self {
        Self {
            include_metadata: val.include_metadata,
            include_embedded: val.include_embedded,
            max_content_chars: val.max_content_chars.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<DjotAttributeGroup> for xberg::DjotAttributeGroup {
    fn from(val: DjotAttributeGroup) -> Self {
        Self {
            identifier: val.identifier,
            attributes: val.attributes.into(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DjotAttributeGroup> for DjotAttributeGroup {
    fn from(val: xberg::DjotAttributeGroup) -> Self {
        Self {
            identifier: val.identifier.to_string(),
            attributes: val.attributes.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DjotContent> for xberg::DjotContent {
    fn from(val: DjotContent) -> Self {
        Self {
            plain_text: val.plain_text,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into(),
            tables: val.tables.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            footnotes: val.footnotes.into_iter().map(Into::into).collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DjotContent> for DjotContent {
    fn from(val: xberg::DjotContent) -> Self {
        Self {
            plain_text: val.plain_text.to_string(),
            blocks: val.blocks.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into(),
            tables: val.tables.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            footnotes: val.footnotes.into_iter().map(Into::into).collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DjotImage> for xberg::DjotImage {
    fn from(val: DjotImage) -> Self {
        Self {
            src: val.src,
            alt: val.alt,
            title: val.title,
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DjotImage> for DjotImage {
    fn from(val: xberg::DjotImage) -> Self {
        Self {
            src: val.src.to_string(),
            alt: val.alt.to_string(),
            title: val.title.map(|v| v.to_string()),
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DjotLink> for xberg::DjotLink {
    fn from(val: DjotLink) -> Self {
        Self {
            url: val.url,
            text: val.text,
            title: val.title,
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DjotLink> for DjotLink {
    fn from(val: xberg::DjotLink) -> Self {
        Self {
            url: val.url.to_string(),
            text: val.text.to_string(),
            title: val.title.map(|v| v.to_string()),
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DoctorCheck> for xberg::DoctorCheck {
    fn from(val: DoctorCheck) -> Self {
        Self {
            name: val.name,
            status: match val.status.as_str() {
                "pass" => xberg::ProbeStatus::Pass,
                "warn" => xberg::ProbeStatus::Warn,
                "fail" => xberg::ProbeStatus::Fail,
                "skip" => xberg::ProbeStatus::Skip,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'status' of ProbeStatus: expected one of [pass, warn, fail, skip]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::ProbeStatus::Pass
                }
            },
            message: val.message,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DoctorCheck> for DoctorCheck {
    fn from(val: xberg::DoctorCheck) -> Self {
        Self {
            name: val.name.to_string(),
            status: serde_json::to_value(val.status)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<DoctorReport> for xberg::DoctorReport {
    fn from(val: DoctorReport) -> Self {
        Self {
            checks: val.checks.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DoctorReport> for DoctorReport {
    fn from(val: xberg::DoctorReport) -> Self {
        Self {
            checks: val.checks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DocumentBoundary> for DocumentBoundary {
    fn from(val: xberg::DocumentBoundary) -> Self {
        Self {
            start_page: val.start_page,
            end_page: val.end_page,
            confidence: val.confidence,
            reason: serde_json::to_value(val.reason)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<DocumentCounts> for xberg::DocumentCounts {
    fn from(val: DocumentCounts) -> Self {
        Self {
            pages: val.pages as usize,
            tables: val.tables as usize,
            images: val.images as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::DocumentCounts> for DocumentCounts {
    fn from(val: xberg::DocumentCounts) -> Self {
        Self {
            pages: val.pages as i64,
            tables: val.tables as i64,
            images: val.images as i64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DocumentMetadata> for DocumentMetadata {
    fn from(val: xberg::DocumentMetadata) -> Self {
        Self {
            mime_type: val.mime_type.to_string(),
            size_bytes: val.size_bytes as i64,
            page_count: val.page_count,
            force_ocr: val.force_ocr,
            user_chunk_config: val.user_chunk_config.map(Into::into),
            chunking_enabled: val.chunking_enabled,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentNode> for xberg::DocumentNode {
    fn from(val: DocumentNode) -> Self {
        Self {
            id: val.id,
            content: val.content.into(),
            parent: (val.parent).map(xberg::NodeIndex),
            children: (val.children.into_iter().collect::<Vec<_>>())
                .into_iter()
                .map(xberg::NodeIndex)
                .collect(),
            content_layer: match val.content_layer.as_str() {
                "body" => xberg::ContentLayer::Body,
                "header" => xberg::ContentLayer::Header,
                "footer" => xberg::ContentLayer::Footer,
                "footnote" => xberg::ContentLayer::Footnote,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'content_layer' of ContentLayer: expected one of [body, header, footer, footnote]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            page: val.page,
            page_end: val.page_end,
            bbox: val.bbox.map(Into::into),
            annotations: val.annotations.into_iter().map(Into::into).collect(),
            attributes: val
                .attributes
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentNode> for DocumentNode {
    fn from(val: xberg::DocumentNode) -> Self {
        Self {
            id: val.id.to_string(),
            content: val.content.into(),
            parent: val.parent.map(|v| v.0),
            children: val
                .children
                .iter()
                .map(|v| v.0)
                .collect::<Vec<_>>()
                .into_iter()
                .collect(),
            content_layer: serde_json::to_value(val.content_layer)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            page: val.page,
            page_end: val.page_end,
            bbox: val.bbox.map(Into::into),
            annotations: val.annotations.into_iter().map(Into::into).collect(),
            attributes: val
                .attributes
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentRelationship> for xberg::DocumentRelationship {
    fn from(val: DocumentRelationship) -> Self {
        Self {
            source: xberg::NodeIndex(val.source),
            target: xberg::NodeIndex(val.target),
            kind: match val.kind.as_str() {
                "footnote_reference" => xberg::RelationshipKind::FootnoteReference,
                "citation_reference" => xberg::RelationshipKind::CitationReference,
                "internal_link" => xberg::RelationshipKind::InternalLink,
                "caption" => xberg::RelationshipKind::Caption,
                "label" => xberg::RelationshipKind::Label,
                "toc_entry" => xberg::RelationshipKind::TocEntry,
                "cross_reference" => xberg::RelationshipKind::CrossReference,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'kind' of RelationshipKind: expected one of [footnote_reference, citation_reference, internal_link, caption, label, toc_entry, cross_reference]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::RelationshipKind::FootnoteReference
                }
            },
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DocumentRelationship> for DocumentRelationship {
    fn from(val: xberg::DocumentRelationship) -> Self {
        Self {
            source: val.source.0,
            target: val.target.0,
            kind: serde_json::to_value(val.kind)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentRevision> for xberg::DocumentRevision {
    fn from(val: DocumentRevision) -> Self {
        Self {
            revision_id: val.revision_id,
            author: val.author,
            timestamp: val.timestamp,
            kind: match val.kind.as_str() {
                "insertion" => xberg::RevisionKind::Insertion,
                "deletion" => xberg::RevisionKind::Deletion,
                "format_change" => xberg::RevisionKind::FormatChange,
                "comment" => xberg::RevisionKind::Comment,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'kind' of RevisionKind: expected one of [insertion, deletion, format_change, comment]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::RevisionKind::Insertion
                }
            },
            anchor: val.anchor.map(Into::into),
            delta: val.delta.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentRevision> for DocumentRevision {
    fn from(val: xberg::DocumentRevision) -> Self {
        Self {
            revision_id: val.revision_id.to_string(),
            author: val.author.map(|v| v.to_string()),
            timestamp: val.timestamp.map(|v| v.to_string()),
            kind: serde_json::to_value(val.kind)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            anchor: val.anchor.map(Into::into),
            delta: val.delta.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<DocumentStructure> for xberg::DocumentStructure {
    fn from(val: DocumentStructure) -> Self {
        Self {
            nodes: val.nodes.into_iter().map(Into::into).collect(),
            source_format: val.source_format,
            relationships: val.relationships.into_iter().map(Into::into).collect(),
            node_types: val.node_types.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentStructure> for DocumentStructure {
    fn from(val: xberg::DocumentStructure) -> Self {
        Self {
            nodes: val.nodes.into_iter().map(Into::into).collect(),
            source_format: val.source_format.map(|v| v.to_string()),
            relationships: val.relationships.into_iter().map(Into::into).collect(),
            node_types: val.node_types.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentSummary> for xberg::DocumentSummary {
    fn from(val: DocumentSummary) -> Self {
        Self {
            text: val.text,
            strategy: match val.strategy.as_str() {
                "extractive" => xberg::SummaryStrategy::Extractive,
                "abstractive" => xberg::SummaryStrategy::Abstractive,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'strategy' of SummaryStrategy: expected one of [extractive, abstractive]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            token_count: val.token_count,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DocumentSummary> for DocumentSummary {
    fn from(val: xberg::DocumentSummary) -> Self {
        Self {
            text: val.text.to_string(),
            strategy: serde_json::to_value(val.strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            token_count: val.token_count,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<DocxAppProperties> for xberg::DocxAppProperties {
    fn from(val: DocxAppProperties) -> Self {
        Self {
            application: val.application,
            app_version: val.app_version,
            template: val.template,
            total_time: val.total_time,
            pages: val.pages,
            words: val.words,
            characters: val.characters,
            characters_with_spaces: val.characters_with_spaces,
            lines: val.lines,
            paragraphs: val.paragraphs,
            company: val.company,
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DocxAppProperties> for DocxAppProperties {
    fn from(val: xberg::DocxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            template: val.template.map(|v| v.to_string()),
            total_time: val.total_time,
            pages: val.pages,
            words: val.words,
            characters: val.characters,
            characters_with_spaces: val.characters_with_spaces,
            lines: val.lines,
            paragraphs: val.paragraphs,
            company: val.company.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<DocxMetadata> for xberg::DocxMetadata {
    fn from(val: DocxMetadata) -> Self {
        Self {
            core_properties: val.core_properties.map(Into::into),
            app_properties: val.app_properties.map(Into::into),
            custom_properties: val
                .custom_properties
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v)).collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocxMetadata> for DocxMetadata {
    fn from(val: xberg::DocxMetadata) -> Self {
        Self {
            core_properties: val.core_properties.map(Into::into),
            app_properties: val.app_properties.map(Into::into),
            custom_properties: val
                .custom_properties
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v)).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Element> for xberg::Element {
    fn from(val: Element) -> Self {
        Self {
            element_id: val.element_id,
            element_type: match val.element_type.as_str() {
                "title" => xberg::ElementType::Title,
                "narrative_text" => xberg::ElementType::NarrativeText,
                "heading" => xberg::ElementType::Heading,
                "list_item" => xberg::ElementType::ListItem,
                "table" => xberg::ElementType::Table,
                "image" => xberg::ElementType::Image,
                "page_break" => xberg::ElementType::PageBreak,
                "code_block" => xberg::ElementType::CodeBlock,
                "formula" => xberg::ElementType::Formula,
                "block_quote" => xberg::ElementType::BlockQuote,
                "footer" => xberg::ElementType::Footer,
                "header" => xberg::ElementType::Header,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'element_type' of ElementType: expected one of [title, narrative_text, heading, list_item, table, image, page_break, code_block, formula, block_quote, footer, header]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::ElementType::Title
                }
            },
            text: val.text,
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Element> for Element {
    fn from(val: xberg::Element) -> Self {
        Self {
            element_id: val.element_id.to_string(),
            element_type: serde_json::to_value(val.element_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            text: val.text.to_string(),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ElementMetadata> for xberg::ElementMetadata {
    fn from(val: ElementMetadata) -> Self {
        Self {
            page_number: val.page_number,
            filename: val.filename,
            coordinates: val.coordinates.map(Into::into),
            element_index: val.element_index.map(|v| v as usize),
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ElementMetadata> for ElementMetadata {
    fn from(val: xberg::ElementMetadata) -> Self {
        Self {
            page_number: val.page_number,
            filename: val.filename.map(|v| v.to_string()),
            coordinates: val.coordinates.map(Into::into),
            element_index: val.element_index.map(|v| v as i64),
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmailAttachment> for EmailAttachment {
    fn from(val: xberg::EmailAttachment) -> Self {
        Self {
            name: val.name.map(|v| v.to_string()),
            filename: val.filename.map(|v| v.to_string()),
            mime_type: val.mime_type.map(|v| v.to_string()),
            size: val.size.map(|v| v as i64),
            is_image: val.is_image,
            data: val.data.map(|v| v.to_vec().into()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<EmailConfig> for xberg::EmailConfig {
    fn from(val: EmailConfig) -> Self {
        Self {
            msg_fallback_codepage: val.msg_fallback_codepage,
            ..Default::default()
        }
    }
}

impl From<xberg::EmailConfig> for EmailConfig {
    fn from(val: xberg::EmailConfig) -> Self {
        Self {
            msg_fallback_codepage: val.msg_fallback_codepage,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmailExtractionResult> for EmailExtractionResult {
    fn from(val: xberg::EmailExtractionResult) -> Self {
        Self {
            subject: val.subject.map(|v| v.to_string()),
            from_email: val.from_email.map(|v| v.to_string()),
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            date: val.date.map(|v| v.to_string()),
            message_id: val.message_id.map(|v| v.to_string()),
            plain_text: val.plain_text.map(|v| v.to_string()),
            html_content: val.html_content.map(|v| v.to_string()),
            content: val.content.to_string(),
            attachments: val.attachments.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<EmailMetadata> for xberg::EmailMetadata {
    fn from(val: EmailMetadata) -> Self {
        Self {
            from_email: val.from_email,
            from_name: val.from_name,
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            message_id: val.message_id,
            attachments: val.attachments.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::EmailMetadata> for EmailMetadata {
    fn from(val: xberg::EmailMetadata) -> Self {
        Self {
            from_email: val.from_email.map(|v| v.to_string()),
            from_name: val.from_name.map(|v| v.to_string()),
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            message_id: val.message_id.map(|v| v.to_string()),
            attachments: val.attachments.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::EmbeddedChanges> for EmbeddedChanges {
    fn from(val: xberg::EmbeddedChanges) -> Self {
        Self {
            added: val.added.into_iter().map(Into::into).collect(),
            removed: val.removed.into_iter().map(Into::into).collect(),
            changed: val.changed.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::EmbeddedDiff> for EmbeddedDiff {
    fn from(val: xberg::EmbeddedDiff) -> Self {
        Self {
            path: val.path.to_string(),
            diff: (*val.diff).into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::pdf::embedded_files::EmbeddedFile> for EmbeddedFile {
    fn from(val: xberg::pdf::embedded_files::EmbeddedFile) -> Self {
        Self {
            name: val.name.to_string(),
            data: val.data.to_vec().into(),
            compressed_size: val.compressed_size as i64,
            mime_type: val.mime_type.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<EmbeddingConfig> for xberg::EmbeddingConfig {
    fn from(val: EmbeddingConfig) -> Self {
        Self {
            model: val.model.into(),
            normalize: val.normalize,
            batch_size: val.batch_size as usize,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(Into::into),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as u64),
            max_sequence_length: val.max_sequence_length.map(|v| v as usize),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmbeddingConfig> for EmbeddingConfig {
    fn from(val: xberg::EmbeddingConfig) -> Self {
        Self {
            model: val.model.into(),
            normalize: val.normalize,
            batch_size: val.batch_size as i64,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
            max_sequence_length: val.max_sequence_length.map(|v| v as i64),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Entity> for xberg::Entity {
    fn from(val: Entity) -> Self {
        Self {
            category: match val.category.as_str() {
                "person" => xberg::EntityCategory::Person,
                "organization" => xberg::EntityCategory::Organization,
                "location" => xberg::EntityCategory::Location,
                "date" => xberg::EntityCategory::Date,
                "time" => xberg::EntityCategory::Time,
                "money" => xberg::EntityCategory::Money,
                "percent" => xberg::EntityCategory::Percent,
                "email" => xberg::EntityCategory::Email,
                "phone" => xberg::EntityCategory::Phone,
                "url" => xberg::EntityCategory::Url,
                "custom" => xberg::EntityCategory::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'category' of EntityCategory: expected one of [person, organization, location, date, time, money, percent, email, phone, url, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::EntityCategory::Person
                }
            },
            text: val.text,
            start: val.start,
            end: val.end,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Entity> for Entity {
    fn from(val: xberg::Entity) -> Self {
        Self {
            category: serde_json::to_value(val.category)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            text: val.text.to_string(),
            start: val.start,
            end: val.end,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<EpubMetadata> for xberg::EpubMetadata {
    fn from(val: EpubMetadata) -> Self {
        Self {
            coverage: val.coverage,
            dc_format: val.dc_format,
            relation: val.relation,
            source: val.source,
            dc_type: val.dc_type,
            cover_image: val.cover_image,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::EpubMetadata> for EpubMetadata {
    fn from(val: xberg::EpubMetadata) -> Self {
        Self {
            coverage: val.coverage.map(|v| v.to_string()),
            dc_format: val.dc_format.map(|v| v.to_string()),
            relation: val.relation.map(|v| v.to_string()),
            source: val.source.map(|v| v.to_string()),
            dc_type: val.dc_type.map(|v| v.to_string()),
            cover_image: val.cover_image.map(|v| v.to_string()),
        }
    }
}

impl From<ErrorMetadata> for xberg::ErrorMetadata {
    fn from(val: ErrorMetadata) -> Self {
        Self {
            error_type: val.error_type,
            message: val.message,
        }
    }
}

impl From<xberg::ErrorMetadata> for ErrorMetadata {
    fn from(val: xberg::ErrorMetadata) -> Self {
        Self {
            error_type: val.error_type.to_string(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<ExcelMetadata> for xberg::ExcelMetadata {
    fn from(val: ExcelMetadata) -> Self {
        Self {
            sheet_count: val.sheet_count,
            sheet_names: val.sheet_names.map(|v| v.into_iter().collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExcelMetadata> for ExcelMetadata {
    fn from(val: xberg::ExcelMetadata) -> Self {
        Self {
            sheet_count: val.sheet_count,
            sheet_names: val.sheet_names.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExcelSheet> for ExcelSheet {
    fn from(val: xberg::ExcelSheet) -> Self {
        Self {
            name: val.name.to_string(),
            markdown: val.markdown.to_string(),
            row_count: val.row_count as i64,
            col_count: val.col_count as i64,
            cell_count: val.cell_count as i64,
            table_cells: val.table_cells.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExcelWorkbook> for ExcelWorkbook {
    fn from(val: xberg::ExcelWorkbook) -> Self {
        Self {
            sheets: val.sheets.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ExtractInput> for xberg::ExtractInput {
    fn from(val: ExtractInput) -> Self {
        Self {
            kind: match val.kind.as_str() {
                "bytes" => xberg::ExtractInputKind::Bytes,
                "uri" => xberg::ExtractInputKind::Uri,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
                        "invalid value {other:?} for field 'kind' of ExtractInputKind: expected one of [bytes, uri]"
                    ))
                    .throw()
                    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::ExtractInputKind::Bytes
                }
            },
            bytes: val.bytes.map(|v| v.into()),
            uri: val.uri,
            mime_type: val.mime_type,
            filename: val.filename,
            config: val.config.map(Into::into),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractInput> for ExtractInput {
    fn from(val: xberg::ExtractInput) -> Self {
        Self {
            kind: serde_json::to_value(val.kind)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            bytes: val.bytes.map(|v| v.to_vec().into()),
            uri: val.uri.map(|v| v.to_string()),
            mime_type: val.mime_type.map(|v| v.to_string()),
            filename: val.filename.map(|v| v.to_string()),
            config: val.config.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ExtractedDocument> for xberg::ExtractedDocument {
    fn from(val: ExtractedDocument) -> Self {
        let mut __result = xberg::ExtractedDocument::default();
        __result.content = val.content;
        __result.mime_type = val.mime_type.into();
        __result.metadata = val.metadata.into();
        __result.extraction_method = val.extraction_method.as_deref().map(|s| match s { "native" => xberg::ExtractionMethod::Native,"ocr" => xberg::ExtractionMethod::Ocr,"mixed" => xberg::ExtractionMethod::Mixed,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'extraction_method' of ExtractionMethod: expected one of [native, ocr, mixed]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    xberg::ExtractionMethod::Native
}
 })
;
        __result.tables = val.tables.into_iter().map(Into::into).collect();
        __result.counts = val.counts.into();
        __result.detected_languages = val.detected_languages.map(|v| v.into_iter().collect());
        __result.detected_language_confidences = val
            .detected_language_confidences
            .map(|v| v.into_iter().map(Into::into).collect());
        __result.chunks = val.chunks.map(|v| v.into_iter().map(Into::into).collect());
        __result.images = val.images.map(|v| v.into_iter().map(Into::into).collect());
        __result.pages = val.pages.map(|v| v.into_iter().map(Into::into).collect());
        __result.elements = val.elements.map(|v| v.into_iter().map(Into::into).collect());
        __result.djot_content = val.djot_content.map(Into::into);
        __result.ocr_elements = val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect());
        __result.document = val.document.map(Into::into);
        __result.extracted_keywords = val.extracted_keywords.map(|v| v.into_iter().map(Into::into).collect());
        __result.quality_score = val.quality_score;
        __result.processing_warnings = val.processing_warnings.into_iter().map(Into::into).collect();
        __result.annotations = val.annotations.map(|v| v.into_iter().map(Into::into).collect());
        __result.children = val.children.map(|v| v.into_iter().map(Into::into).collect());
        __result.uris = val.uris.map(|v| v.into_iter().map(Into::into).collect());
        __result.revisions = val.revisions.map(|v| v.into_iter().map(Into::into).collect());
        __result.structured_output = val.structured_output;
        __result.code_intelligence = val.code_intelligence;
        __result.llm_usage = val.llm_usage.map(|v| v.into_iter().map(Into::into).collect());
        __result.entities = val.entities.map(|v| v.into_iter().map(Into::into).collect());
        __result.summary = val.summary.map(Into::into);
        __result.extraction_confidence = val.extraction_confidence.map(Into::into);
        __result.translation = val.translation.map(Into::into);
        __result.page_classifications = val
            .page_classifications
            .map(|v| v.into_iter().map(Into::into).collect());
        __result.redaction_report = val.redaction_report.map(Into::into);
        __result.formulas = val.formulas.into_iter().map(Into::into).collect();
        __result.form_fields = val.form_fields.into_iter().map(Into::into).collect();
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractedDocument> for ExtractedDocument {
    fn from(val: xberg::ExtractedDocument) -> Self {
        Self {
            content: val.content.to_string(),
            mime_type: val.mime_type.to_string(),
            metadata: val.metadata.into(),
            extraction_method: val.extraction_method.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            tables: val.tables.into_iter().map(Into::into).collect(),
            counts: val.counts.into(),
            detected_languages: val.detected_languages.map(|v| v.into_iter().collect()),
            detected_language_confidences: val
                .detected_language_confidences
                .map(|v| v.into_iter().map(Into::into).collect()),
            chunks: val.chunks.map(|v| v.into_iter().map(Into::into).collect()),
            images: val.images.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
            elements: val.elements.map(|v| v.into_iter().map(Into::into).collect()),
            djot_content: val.djot_content.map(Into::into),
            ocr_elements: val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            extracted_keywords: val.extracted_keywords.map(|v| v.into_iter().map(Into::into).collect()),
            quality_score: val.quality_score,
            processing_warnings: val.processing_warnings.into_iter().map(Into::into).collect(),
            annotations: val.annotations.map(|v| v.into_iter().map(Into::into).collect()),
            children: val.children.map(|v| v.into_iter().map(Into::into).collect()),
            uris: val.uris.map(|v| v.into_iter().map(Into::into).collect()),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
            structured_output: val.structured_output,
            #[cfg(feature = "tree-sitter")]
            code_intelligence: val.code_intelligence,
            llm_usage: val.llm_usage.map(|v| v.into_iter().map(Into::into).collect()),
            entities: val.entities.map(|v| v.into_iter().map(Into::into).collect()),
            summary: val.summary.map(Into::into),
            #[cfg(feature = "heuristics")]
            extraction_confidence: val.extraction_confidence.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classifications: val
                .page_classifications
                .map(|v| v.into_iter().map(Into::into).collect()),
            redaction_report: val.redaction_report.map(Into::into),
            formulas: val.formulas.into_iter().map(Into::into).collect(),
            form_fields: val.form_fields.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ExtractedImage> for xberg::ExtractedImage {
    fn from(val: ExtractedImage) -> Self {
        Self {
            data: val.data.into(),
            format: val.format.into(),
            image_index: val.image_index,
            page_number: val.page_number,
            width: val.width,
            height: val.height,
            colorspace: val.colorspace,
            bits_per_component: val.bits_per_component,
            is_mask: val.is_mask,
            description: val.description,
            ocr_result: val.ocr_result.map(Into::into).map(Box::new),
            bounding_box: val.bounding_box.map(Into::into),
            source_path: val.source_path,
            image_kind: val.image_kind.as_deref().map(|s| match s { "photograph" => xberg::ImageKind::Photograph,"diagram" => xberg::ImageKind::Diagram,"chart" => xberg::ImageKind::Chart,"drawing" => xberg::ImageKind::Drawing,"text_block" => xberg::ImageKind::TextBlock,"decoration" => xberg::ImageKind::Decoration,"logo" => xberg::ImageKind::Logo,"icon" => xberg::ImageKind::Icon,"tile_fragment" => xberg::ImageKind::TileFragment,"mask" => xberg::ImageKind::Mask,"page_raster" => xberg::ImageKind::PageRaster,"unknown" => xberg::ImageKind::Unknown,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'image_kind' of ImageKind: expected one of [photograph, diagram, chart, drawing, text_block, decoration, logo, icon, tile_fragment, mask, page_raster, unknown]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    xberg::ImageKind::Photograph
}
 })
,
            kind_confidence: val.kind_confidence,
            cluster_id: val.cluster_id,
            caption: val.caption,
            qr_codes: val.qr_codes.map(|v| v.into_iter().map(Into::into).collect()),
            data_base64: val.data_base64,
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractedImage> for ExtractedImage {
    fn from(val: xberg::ExtractedImage) -> Self {
        Self {
            data: val.data.to_vec().into(),
            format: val.format.to_string(),
            image_index: val.image_index,
            page_number: val.page_number,
            width: val.width,
            height: val.height,
            colorspace: val.colorspace.map(|v| v.to_string()),
            bits_per_component: val.bits_per_component,
            is_mask: val.is_mask,
            description: val.description.map(|v| v.to_string()),
            ocr_result: val.ocr_result.map(|v| (*v).into()),
            bounding_box: val.bounding_box.map(Into::into),
            source_path: val.source_path.map(|v| v.to_string()),
            image_kind: val.image_kind.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            kind_confidence: val.kind_confidence,
            cluster_id: val.cluster_id,
            caption: val.caption.map(|v| v.to_string()),
            qr_codes: val.qr_codes.map(|v| v.into_iter().map(Into::into).collect()),
            data_base64: val.data_base64.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ExtractedUri> for xberg::ExtractedUri {
    fn from(val: ExtractedUri) -> Self {
        Self {
            url: val.url,
            label: val.label,
            page: val.page,
            kind: match val.kind.as_str() {
                "hyperlink" => xberg::UriKind::Hyperlink,
                "image" => xberg::UriKind::Image,
                "anchor" => xberg::UriKind::Anchor,
                "citation" => xberg::UriKind::Citation,
                "reference" => xberg::UriKind::Reference,
                "email" => xberg::UriKind::Email,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'kind' of UriKind: expected one of [hyperlink, image, anchor, citation, reference, email]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::UriKind::Hyperlink
                }
            },
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExtractedUri> for ExtractedUri {
    fn from(val: xberg::ExtractedUri) -> Self {
        Self {
            url: val.url.to_string(),
            label: val.label.map(|v| v.to_string()),
            page: val.page,
            kind: serde_json::to_value(val.kind)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ExtractionConfidence> for xberg::ExtractionConfidence {
    fn from(val: ExtractionConfidence) -> Self {
        Self {
            text_coverage: val.text_coverage,
            ocr_aggregate: val.ocr_aggregate,
            schema_compliance: match val.schema_compliance.as_str() {
                "all_valid" => xberg::SchemaCompliance::AllValid,
                "partial_valid" => xberg::SchemaCompliance::PartialValid,
                "all_invalid" => xberg::SchemaCompliance::AllInvalid,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'schema_compliance' of SchemaCompliance: expected one of [all_valid, partial_valid, all_invalid]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::SchemaCompliance::AllValid
                }
            },
            combined: val.combined,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExtractionConfidence> for ExtractionConfidence {
    fn from(val: xberg::ExtractionConfidence) -> Self {
        Self {
            text_coverage: val.text_coverage,
            ocr_aggregate: val.ocr_aggregate,
            schema_compliance: serde_json::to_value(val.schema_compliance)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            combined: val.combined,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ExtractionConfig> for xberg::ExtractionConfig {
    fn from(val: ExtractionConfig) -> Self {
        Self {
            mime_detection_policy: match val.mime_detection_policy.as_str() {
                "prefer_content" => xberg::MimeDetectionPolicy::PreferContent,
                "trust_extension" => xberg::MimeDetectionPolicy::TrustExtension,
                "content_only" => xberg::MimeDetectionPolicy::ContentOnly,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'mime_detection_policy' of MimeDetectionPolicy: expected one of [prefer_content, trust_extension, content_only]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            use_cache: val.use_cache,
            enable_quality_processing: val.enable_quality_processing,
            ocr: val.ocr.map(Into::into),
            force_ocr: val.force_ocr,
            ocr_strategy: val.ocr_strategy.into(),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: val.disable_ocr,
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            html_options: val.html_options.map(Into::into),
            html_output: val.html_output.map(Into::into),
            extraction_timeout_secs: val.extraction_timeout_secs.map(|v| v as u64),
            max_concurrent_extractions: val.max_concurrent_extractions.map(|v| v as usize),
            result_format: match val.result_format.as_str() {
                "unified" => xberg::ResultFormat::Unified,
                "element_based" => xberg::ResultFormat::ElementBased,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'result_format' of ResultFormat: expected one of [unified, element_based]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            security_limits: val.security_limits.map(Into::into),
            max_embedded_file_bytes: val.max_embedded_file_bytes.map(|v| v as u64),
            output_format: match val.output_format.as_str() {
                "plain" => xberg::OutputFormat::Plain,
                "markdown" => xberg::OutputFormat::Markdown,
                "djot" => xberg::OutputFormat::Djot,
                "html" => xberg::OutputFormat::Html,
                "json" => xberg::OutputFormat::Json,
                "doctags" => xberg::OutputFormat::DocTags,
                "custom" => xberg::OutputFormat::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            escape_markdown: val.escape_markdown,
            table_anchors: val.table_anchors,
            jupyter_cell_rendering: match val.jupyter_cell_rendering.as_str() {
                "source" => xberg::JupyterCellRendering::Source,
                "outputs" => xberg::JupyterCellRendering::Outputs,
                "both" => xberg::JupyterCellRendering::Both,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'jupyter_cell_rendering' of JupyterCellRendering: expected one of [source, outputs, both]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            apply_notebook_cell_tags: val.apply_notebook_cell_tags,
            layout: val.layout.map(Into::into),
            transcription: val.transcription.map(Into::into),
            use_layout_for_markdown: val.use_layout_for_markdown,
            include_document_structure: val.include_document_structure,
            acceleration: val.acceleration.map(Into::into),
            cache_namespace: val.cache_namespace,
            cache_ttl_secs: val.cache_ttl_secs.map(|v| v as u64),
            email: val.email.map(Into::into),
            csv: val.csv.map(Into::into),
            geojson: val.geojson.map(Into::into),
            concurrency: val.concurrency.map(Into::into),
            url: val.url.into(),
            max_archive_depth: val.max_archive_depth as usize,
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractionConfig> for ExtractionConfig {
    fn from(val: xberg::ExtractionConfig) -> Self {
        Self {
            mime_detection_policy: serde_json::to_value(val.mime_detection_policy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            use_cache: val.use_cache,
            enable_quality_processing: val.enable_quality_processing,
            ocr: val.ocr.map(Into::into),
            force_ocr: val.force_ocr,
            ocr_strategy: val.ocr_strategy.into(),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: val.disable_ocr,
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            #[cfg(feature = "pdf")]
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            #[cfg(feature = "html")]
            html_options: val.html_options.map(Into::into),
            #[cfg(feature = "html")]
            html_output: val.html_output.map(Into::into),
            extraction_timeout_secs: val.extraction_timeout_secs.map(|v| v as i64),
            max_concurrent_extractions: val.max_concurrent_extractions.map(|v| v as i64),
            result_format: serde_json::to_value(val.result_format)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            security_limits: val.security_limits.map(Into::into),
            max_embedded_file_bytes: val.max_embedded_file_bytes.map(|v| v as i64),
            output_format: serde_json::to_value(val.output_format)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            escape_markdown: val.escape_markdown,
            table_anchors: val.table_anchors,
            jupyter_cell_rendering: serde_json::to_value(val.jupyter_cell_rendering)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            apply_notebook_cell_tags: val.apply_notebook_cell_tags,
            #[cfg(feature = "layout-types")]
            layout: val.layout.map(Into::into),
            #[cfg(feature = "transcription-types")]
            transcription: val.transcription.map(Into::into),
            use_layout_for_markdown: val.use_layout_for_markdown,
            include_document_structure: val.include_document_structure,
            acceleration: val.acceleration.map(Into::into),
            cache_namespace: val.cache_namespace.map(|v| v.to_string()),
            cache_ttl_secs: val.cache_ttl_secs.map(|v| v as i64),
            email: val.email.map(Into::into),
            csv: val.csv.map(Into::into),
            geojson: val.geojson.map(Into::into),
            concurrency: val.concurrency.map(Into::into),
            url: val.url.into(),
            max_archive_depth: val.max_archive_depth as i64,
            #[cfg(feature = "tree-sitter")]
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ExtractionDiff> for ExtractionDiff {
    fn from(val: xberg::ExtractionDiff) -> Self {
        Self {
            content_diff: val.content_diff.into_iter().map(Into::into).collect(),
            tables_added: val.tables_added.into_iter().map(Into::into).collect(),
            tables_removed: val.tables_removed.into_iter().map(Into::into).collect(),
            tables_changed: val.tables_changed.into_iter().map(Into::into).collect(),
            metadata_changed: val.metadata_changed,
            embedded_changes: val.embedded_changes.into(),
        }
    }
}

impl From<ExtractionErrorItem> for xberg::ExtractionErrorItem {
    fn from(val: ExtractionErrorItem) -> Self {
        Self {
            index: val.index as usize,
            code: val.code,
            error_type: val.error_type,
            source: val.source,
            message: val.message,
        }
    }
}

impl From<xberg::ExtractionErrorItem> for ExtractionErrorItem {
    fn from(val: xberg::ExtractionErrorItem) -> Self {
        Self {
            index: val.index as i64,
            code: val.code,
            error_type: val.error_type.to_string(),
            source: val.source.to_string(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ExtractionResult> for xberg::ExtractionResult {
    fn from(val: ExtractionResult) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            errors: val.errors.into_iter().map(Into::into).collect(),
            summary: val.summary.into(),
            crawl_final_urls: val.crawl_final_urls.into_iter().collect(),
            crawl_redirect_count: val.crawl_redirect_count as usize,
            crawl_unique_normalized_urls: val.crawl_unique_normalized_urls.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ExtractionResult> for ExtractionResult {
    fn from(val: xberg::ExtractionResult) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            errors: val.errors.into_iter().map(Into::into).collect(),
            summary: val.summary.into(),
            crawl_final_urls: val.crawl_final_urls.into_iter().collect(),
            crawl_redirect_count: val.crawl_redirect_count as i64,
            crawl_unique_normalized_urls: val.crawl_unique_normalized_urls.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ExtractionSummary> for xberg::ExtractionSummary {
    fn from(val: ExtractionSummary) -> Self {
        Self {
            inputs: val.inputs as usize,
            results: val.results as usize,
            errors: val.errors as usize,
            remote_urls: val.remote_urls as usize,
            pages_crawled: val.pages_crawled as usize,
            documents_downloaded: val.documents_downloaded as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::ExtractionSummary> for ExtractionSummary {
    fn from(val: xberg::ExtractionSummary) -> Self {
        Self {
            inputs: val.inputs as i64,
            results: val.results as i64,
            errors: val.errors as i64,
            remote_urls: val.remote_urls as i64,
            pages_crawled: val.pages_crawled as i64,
            documents_downloaded: val.documents_downloaded as i64,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<FictionBookMetadata> for xberg::FictionBookMetadata {
    fn from(val: FictionBookMetadata) -> Self {
        Self {
            genres: val.genres.into_iter().collect(),
            sequences: val.sequences.into_iter().collect(),
            annotation: val.annotation,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::FictionBookMetadata> for FictionBookMetadata {
    fn from(val: xberg::FictionBookMetadata) -> Self {
        Self {
            genres: val.genres.into_iter().collect(),
            sequences: val.sequences.into_iter().collect(),
            annotation: val.annotation.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<FileExtractionConfig> for xberg::FileExtractionConfig {
    fn from(val: FileExtractionConfig) -> Self {
        Self {
            mime_detection_policy: val.mime_detection_policy.as_deref().map(|s| match s { "prefer_content" => xberg::MimeDetectionPolicy::PreferContent,"trust_extension" => xberg::MimeDetectionPolicy::TrustExtension,"content_only" => xberg::MimeDetectionPolicy::ContentOnly,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'mime_detection_policy' of MimeDetectionPolicy: expected one of [prefer_content, trust_extension, content_only]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            enable_quality_processing: val.enable_quality_processing,
            ocr: val.ocr.map(Into::into),
            force_ocr: val.force_ocr,
            ocr_strategy: val.ocr_strategy.map(Into::into),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: val.disable_ocr,
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            html_options: val.html_options.map(Into::into),
            html_output: val.html_output.map(Into::into),
            result_format: val.result_format.as_deref().map(|s| match s { "unified" => xberg::ResultFormat::Unified,"element_based" => xberg::ResultFormat::ElementBased,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'result_format' of ResultFormat: expected one of [unified, element_based]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            output_format: val.output_format.as_deref().map(|s| match s { "plain" => xberg::OutputFormat::Plain,"markdown" => xberg::OutputFormat::Markdown,"djot" => xberg::OutputFormat::Djot,"html" => xberg::OutputFormat::Html,"json" => xberg::OutputFormat::Json,"doctags" => xberg::OutputFormat::DocTags,"custom" => xberg::OutputFormat::Custom(Default::default()),other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            include_document_structure: val.include_document_structure,
            layout: val.layout.map(Into::into),
            transcription: val.transcription.map(Into::into),
            timeout_secs: val.timeout_secs.map(|v| v as u64),
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            url: val.url.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::FileExtractionConfig> for FileExtractionConfig {
    fn from(val: xberg::FileExtractionConfig) -> Self {
        Self {
            mime_detection_policy: val.mime_detection_policy.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            enable_quality_processing: val.enable_quality_processing,
            ocr: val.ocr.map(Into::into),
            force_ocr: val.force_ocr,
            ocr_strategy: val.ocr_strategy.map(Into::into),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: val.disable_ocr,
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            #[cfg(feature = "pdf")]
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            #[cfg(feature = "html")]
            html_options: val.html_options.map(Into::into),
            #[cfg(feature = "html")]
            html_output: val.html_output.map(Into::into),
            result_format: val.result_format.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            output_format: val.output_format.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            include_document_structure: val.include_document_structure,
            #[cfg(feature = "layout-types")]
            layout: val.layout.map(Into::into),
            #[cfg(feature = "transcription-types")]
            transcription: val.transcription.map(Into::into),
            timeout_secs: val.timeout_secs.map(|v| v as i64),
            #[cfg(feature = "tree-sitter")]
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            url: val.url.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Footnote> for xberg::Footnote {
    fn from(val: Footnote) -> Self {
        Self {
            label: val.label,
            content: val.content.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::Footnote> for Footnote {
    fn from(val: xberg::Footnote) -> Self {
        Self {
            label: val.label.to_string(),
            content: val.content.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<xberg::FootnoteAnchor> for FootnoteAnchor {
    fn from(val: xberg::FootnoteAnchor) -> Self {
        Self {
            label: val.label.to_string(),
            offset: val.offset as i64,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<FootnoteConfig> for xberg::FootnoteConfig {
    fn from(val: FootnoteConfig) -> Self {
        Self {
            parse_citations: val.parse_citations,
            ..Default::default()
        }
    }
}

impl From<xberg::FootnoteConfig> for FootnoteConfig {
    fn from(val: xberg::FootnoteConfig) -> Self {
        Self {
            parse_citations: val.parse_citations,
        }
    }
}

impl From<xberg::FootnoteDefinition> for FootnoteDefinition {
    fn from(val: xberg::FootnoteDefinition) -> Self {
        Self {
            label: val.label.to_string(),
            content: val.content.to_string(),
            offset: val.offset as i64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<FormattedBlock> for xberg::FormattedBlock {
    fn from(val: FormattedBlock) -> Self {
        Self {
            block_type: match val.block_type.as_str() {
                "paragraph" => xberg::BlockType::Paragraph,
                "heading" => xberg::BlockType::Heading,
                "blockquote" => xberg::BlockType::Blockquote,
                "code_block" => xberg::BlockType::CodeBlock,
                "list_item" => xberg::BlockType::ListItem,
                "ordered_list" => xberg::BlockType::OrderedList,
                "bullet_list" => xberg::BlockType::BulletList,
                "task_list" => xberg::BlockType::TaskList,
                "definition_list" => xberg::BlockType::DefinitionList,
                "definition_term" => xberg::BlockType::DefinitionTerm,
                "definition_description" => xberg::BlockType::DefinitionDescription,
                "div" => xberg::BlockType::Div,
                "section" => xberg::BlockType::Section,
                "thematic_break" => xberg::BlockType::ThematicBreak,
                "raw_block" => xberg::BlockType::RawBlock,
                "math_display" => xberg::BlockType::MathDisplay,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'block_type' of BlockType: expected one of [paragraph, heading, blockquote, code_block, list_item, ordered_list, bullet_list, task_list, definition_list, definition_term, definition_description, div, section, thematic_break, raw_block, math_display]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::BlockType::Paragraph
                }
            },
            level: val.level.map(|v| v as usize),
            inline_content: val.inline_content.into_iter().map(Into::into).collect(),
            attributes: val.attributes.map(Into::into),
            language: val.language,
            code: val.code,
            children: val.children.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::FormattedBlock> for FormattedBlock {
    fn from(val: xberg::FormattedBlock) -> Self {
        Self {
            block_type: serde_json::to_value(val.block_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            level: val.level.map(|v| v as i64),
            inline_content: val.inline_content.into_iter().map(Into::into).collect(),
            attributes: val.attributes.map(Into::into),
            language: val.language.map(|v| v.to_string()),
            code: val.code.map(|v| v.to_string()),
            children: val.children.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Formula> for xberg::Formula {
    fn from(val: Formula) -> Self {
        Self {
            latex: val.latex,
            bbox: val.bbox.map(Into::into),
            page: val.page,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::Formula> for Formula {
    fn from(val: xberg::Formula) -> Self {
        Self {
            latex: val.latex.to_string(),
            bbox: val.bbox.map(Into::into),
            page: val.page,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<GeoJsonExtractionConfig> for xberg::GeoJsonExtractionConfig {
    fn from(val: GeoJsonExtractionConfig) -> Self {
        Self {
            include_full_coordinates: val.include_full_coordinates,
            ..Default::default()
        }
    }
}

impl From<xberg::GeoJsonExtractionConfig> for GeoJsonExtractionConfig {
    fn from(val: xberg::GeoJsonExtractionConfig) -> Self {
        Self {
            include_full_coordinates: val.include_full_coordinates,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::candle_ocr::GlmOcrBackendOptions> for GlmOcrBackendOptions {
    fn from(val: xberg::candle_ocr::GlmOcrBackendOptions) -> Self {
        Self {
            task: val.task.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            device: val.device.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            layout_mode: val.layout_mode.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            enable_chart_understanding: val.enable_chart_understanding,
            cache_dir: val.cache_dir.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<GridCell> for xberg::GridCell {
    fn from(val: GridCell) -> Self {
        Self {
            content: val.content,
            row: val.row,
            col: val.col,
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
            bbox: val.bbox.map(Into::into),
            heading_level: val.heading_level,
            style_name: val.style_name,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::GridCell> for GridCell {
    fn from(val: xberg::GridCell) -> Self {
        Self {
            content: val.content.to_string(),
            row: val.row,
            col: val.col,
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
            bbox: val.bbox.map(Into::into),
            heading_level: val.heading_level,
            style_name: val.style_name.map(|v| v.to_string()),
        }
    }
}

impl From<HeaderMetadata> for xberg::HeaderMetadata {
    fn from(val: HeaderMetadata) -> Self {
        Self {
            level: val.level,
            text: val.text,
            id: val.id,
            depth: val.depth,
            html_offset: val.html_offset,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::HeaderMetadata> for HeaderMetadata {
    fn from(val: xberg::HeaderMetadata) -> Self {
        Self {
            level: val.level,
            text: val.text.to_string(),
            id: val.id.map(|v| v.to_string()),
            depth: val.depth,
            html_offset: val.html_offset,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<HeadingContext> for xberg::HeadingContext {
    fn from(val: HeadingContext) -> Self {
        Self {
            headings: val.headings.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::HeadingContext> for HeadingContext {
    fn from(val: xberg::HeadingContext) -> Self {
        Self {
            headings: val.headings.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<HeadingLevel> for xberg::HeadingLevel {
    fn from(val: HeadingLevel) -> Self {
        Self {
            level: val.level,
            text: val.text,
        }
    }
}

impl From<xberg::HeadingLevel> for HeadingLevel {
    fn from(val: xberg::HeadingLevel) -> Self {
        Self {
            level: val.level,
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<HeuristicsConfig> for xberg::HeuristicsConfig {
    fn from(val: HeuristicsConfig) -> Self {
        Self {
            enable_pdf_text_heuristics: val.enable_pdf_text_heuristics,
            text_layer_threshold: val.text_layer_threshold,
            file_size_threshold_bytes: val.file_size_threshold_bytes as u64,
            page_count_threshold: val.page_count_threshold,
            target_pages_per_chunk: val.target_pages_per_chunk,
            max_pages_per_chunk: val.max_pages_per_chunk,
            disk_processing_threshold_bytes: val.disk_processing_threshold_bytes as u64,
            min_chars_per_page: val.min_chars_per_page,
            max_xlsx_sheet_count: val.max_xlsx_sheet_count,
            max_xlsx_workbook_cells: val.max_xlsx_workbook_cells as u64,
            max_pptx_embedded_count: val.max_pptx_embedded_count,
            ..Default::default()
        }
    }
}

impl From<xberg::HeuristicsConfig> for HeuristicsConfig {
    fn from(val: xberg::HeuristicsConfig) -> Self {
        Self {
            enable_pdf_text_heuristics: val.enable_pdf_text_heuristics,
            text_layer_threshold: val.text_layer_threshold,
            file_size_threshold_bytes: val.file_size_threshold_bytes as i64,
            page_count_threshold: val.page_count_threshold,
            target_pages_per_chunk: val.target_pages_per_chunk,
            max_pages_per_chunk: val.max_pages_per_chunk,
            disk_processing_threshold_bytes: val.disk_processing_threshold_bytes as i64,
            min_chars_per_page: val.min_chars_per_page,
            max_xlsx_sheet_count: val.max_xlsx_sheet_count,
            max_xlsx_workbook_cells: val.max_xlsx_workbook_cells as i64,
            max_pptx_embedded_count: val.max_pptx_embedded_count,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<HierarchicalBlock> for xberg::HierarchicalBlock {
    fn from(val: HierarchicalBlock) -> Self {
        Self {
            text: val.text,
            font_size: val.font_size,
            level: val.level,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::HierarchicalBlock> for HierarchicalBlock {
    fn from(val: xberg::HierarchicalBlock) -> Self {
        Self {
            text: val.text.to_string(),
            font_size: val.font_size,
            level: val.level.to_string(),
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<HierarchicalBoundingBox> for xberg::HierarchicalBoundingBox {
    fn from(val: HierarchicalBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
            ..Default::default()
        }
    }
}

impl From<xberg::HierarchicalBoundingBox> for HierarchicalBoundingBox {
    fn from(val: xberg::HierarchicalBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<HierarchyConfig> for xberg::HierarchyConfig {
    fn from(val: HierarchyConfig) -> Self {
        Self {
            enabled: val.enabled,
            k_clusters: val.k_clusters as usize,
            include_bbox: val.include_bbox,
            ..Default::default()
        }
    }
}

impl From<xberg::HierarchyConfig> for HierarchyConfig {
    fn from(val: xberg::HierarchyConfig) -> Self {
        Self {
            enabled: val.enabled,
            k_clusters: val.k_clusters as i64,
            include_bbox: val.include_bbox,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<HtmlMetadata> for xberg::HtmlMetadata {
    fn from(val: HtmlMetadata) -> Self {
        Self {
            title: val.title,
            description: val.description,
            keywords: val.keywords.into_iter().collect(),
            author: val.author,
            canonical_url: val.canonical_url,
            base_href: val.base_href,
            language: val.language,
            text_direction: val.text_direction.as_deref().map(|s| match s {
                "ltr" => xberg::TextDirection::LeftToRight,
                "rtl" => xberg::TextDirection::RightToLeft,
                "auto" => xberg::TextDirection::Auto,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'text_direction' of TextDirection: expected one of [ltr, rtl, auto]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::TextDirection::LeftToRight
                }
            }),
            open_graph: val.open_graph.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            twitter_card: val
                .twitter_card
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            meta_tags: val.meta_tags.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            headers: val.headers.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            structured_data: val.structured_data.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::HtmlMetadata> for HtmlMetadata {
    fn from(val: xberg::HtmlMetadata) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            description: val.description.map(|v| v.to_string()),
            keywords: val.keywords.into_iter().collect(),
            author: val.author.map(|v| v.to_string()),
            canonical_url: val.canonical_url.map(|v| v.to_string()),
            base_href: val.base_href.map(|v| v.to_string()),
            language: val.language.map(|v| v.to_string()),
            text_direction: val.text_direction.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            open_graph: val.open_graph.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            twitter_card: val
                .twitter_card
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            meta_tags: val.meta_tags.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            headers: val.headers.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            structured_data: val.structured_data.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<HtmlOutputConfig> for xberg::HtmlOutputConfig {
    fn from(val: HtmlOutputConfig) -> Self {
        Self {
            css: val.css,
            css_file: val.css_file.map(Into::into),
            theme: match val.theme.as_str() {
                "default" => xberg::HtmlTheme::Default,
                "github" => xberg::HtmlTheme::GitHub,
                "dark" => xberg::HtmlTheme::Dark,
                "light" => xberg::HtmlTheme::Light,
                "unstyled" => xberg::HtmlTheme::Unstyled,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'theme' of HtmlTheme: expected one of [default, github, dark, light, unstyled]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            class_prefix: val.class_prefix,
            embed_css: val.embed_css,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::HtmlOutputConfig> for HtmlOutputConfig {
    fn from(val: xberg::HtmlOutputConfig) -> Self {
        Self {
            css: val.css.map(|v| v.to_string()),
            css_file: val.css_file.map(|p| p.to_string_lossy().to_string()),
            theme: serde_json::to_value(val.theme)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            class_prefix: val.class_prefix.to_string(),
            embed_css: val.embed_css,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ImageDimensions> for xberg::ImageDimensions {
    fn from(val: ImageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
            ..Default::default()
        }
    }
}

impl From<xberg::ImageDimensions> for ImageDimensions {
    fn from(val: xberg::ImageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ImageDpi> for xberg::ImageDpi {
    fn from(val: ImageDpi) -> Self {
        Self {
            horizontal: val.horizontal,
            vertical: val.vertical,
            ..Default::default()
        }
    }
}

impl From<xberg::ImageDpi> for ImageDpi {
    fn from(val: xberg::ImageDpi) -> Self {
        Self {
            horizontal: val.horizontal,
            vertical: val.vertical,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ImageExtractionConfig> for xberg::ImageExtractionConfig {
    fn from(val: ImageExtractionConfig) -> Self {
        Self {
            extract_images: val.extract_images,
            target_dpi: val.target_dpi,
            max_image_dimension: val.max_image_dimension,
            inject_placeholders: val.inject_placeholders,
            auto_adjust_dpi: val.auto_adjust_dpi,
            min_dpi: val.min_dpi,
            max_dpi: val.max_dpi,
            max_images_per_page: val.max_images_per_page,
            classify: val.classify,
            include_page_rasters: val.include_page_rasters,
            run_ocr_on_images: val.run_ocr_on_images,
            ocr_text_only: val.ocr_text_only,
            append_ocr_text: val.append_ocr_text,
            output_format: val.output_format.into(),
            #[cfg(feature = "svg")]
            svg: val.svg.into(),
            include_data_base64: val.include_data_base64,
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ImageExtractionConfig> for ImageExtractionConfig {
    fn from(val: xberg::ImageExtractionConfig) -> Self {
        Self {
            extract_images: val.extract_images,
            target_dpi: val.target_dpi,
            max_image_dimension: val.max_image_dimension,
            inject_placeholders: val.inject_placeholders,
            auto_adjust_dpi: val.auto_adjust_dpi,
            min_dpi: val.min_dpi,
            max_dpi: val.max_dpi,
            max_images_per_page: val.max_images_per_page,
            classify: val.classify,
            include_page_rasters: val.include_page_rasters,
            run_ocr_on_images: val.run_ocr_on_images,
            ocr_text_only: val.ocr_text_only,
            append_ocr_text: val.append_ocr_text,
            output_format: val.output_format.into(),
            #[cfg(feature = "svg")]
            svg: val.svg.into(),
            include_data_base64: val.include_data_base64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ImageMetadata> for xberg::ImageMetadata {
    fn from(val: ImageMetadata) -> Self {
        Self {
            width: val.width,
            height: val.height,
            format: val.format,
            exif: val.exif.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImageMetadata> for ImageMetadata {
    fn from(val: xberg::ImageMetadata) -> Self {
        Self {
            width: val.width,
            height: val.height,
            format: val.format.to_string(),
            exif: val.exif.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ImageMetadataType> for xberg::ImageMetadataType {
    fn from(val: ImageMetadataType) -> Self {
        Self {
            src: val.src,
            alt: val.alt,
            title: val.title,
            dimensions: val.dimensions.map(Into::into),
            image_type: match val.image_type.as_str() {
                "data-uri" => xberg::ImageType::DataUri,
                "inline-svg" => xberg::ImageType::InlineSvg,
                "external" => xberg::ImageType::External,
                "relative" => xberg::ImageType::Relative,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'image_type' of ImageType: expected one of [data-uri, inline-svg, external, relative]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::ImageType::DataUri
                }
            },
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImageMetadataType> for ImageMetadataType {
    fn from(val: xberg::ImageMetadataType) -> Self {
        Self {
            src: val.src.to_string(),
            alt: val.alt.map(|v| v.to_string()),
            title: val.title.map(|v| v.to_string()),
            dimensions: val.dimensions.map(Into::into),
            image_type: serde_json::to_value(val.image_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ImagePreprocessingConfig> for xberg::ImagePreprocessingConfig {
    fn from(val: ImagePreprocessingConfig) -> Self {
        Self {
            target_dpi: val.target_dpi,
            auto_rotate: val.auto_rotate,
            deskew: val.deskew,
            denoise: val.denoise,
            contrast_enhance: val.contrast_enhance,
            binarization_method: val.binarization_method,
            invert_colors: val.invert_colors,
            ..Default::default()
        }
    }
}

impl From<xberg::ImagePreprocessingConfig> for ImagePreprocessingConfig {
    fn from(val: xberg::ImagePreprocessingConfig) -> Self {
        Self {
            target_dpi: val.target_dpi,
            auto_rotate: val.auto_rotate,
            deskew: val.deskew,
            denoise: val.denoise,
            contrast_enhance: val.contrast_enhance,
            binarization_method: val.binarization_method.to_string(),
            invert_colors: val.invert_colors,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ImagePreprocessingMetadata> for xberg::ImagePreprocessingMetadata {
    fn from(val: ImagePreprocessingMetadata) -> Self {
        Self {
            original_dimensions: val.original_dimensions.into(),
            original_dpi: val.original_dpi.into(),
            target_dpi: val.target_dpi,
            scale_factor: val.scale_factor,
            auto_adjusted: val.auto_adjusted,
            final_dpi: val.final_dpi,
            new_dimensions: val.new_dimensions.map(Into::into),
            resample_method: val.resample_method,
            dimension_clamped: val.dimension_clamped,
            calculated_dpi: val.calculated_dpi,
            skipped_resize: val.skipped_resize,
            resize_error: val.resize_error,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImagePreprocessingMetadata> for ImagePreprocessingMetadata {
    fn from(val: xberg::ImagePreprocessingMetadata) -> Self {
        Self {
            original_dimensions: val.original_dimensions.into(),
            original_dpi: val.original_dpi.into(),
            target_dpi: val.target_dpi,
            scale_factor: val.scale_factor,
            auto_adjusted: val.auto_adjusted,
            final_dpi: val.final_dpi,
            new_dimensions: val.new_dimensions.map(Into::into),
            resample_method: val.resample_method.to_string(),
            dimension_clamped: val.dimension_clamped,
            calculated_dpi: val.calculated_dpi,
            skipped_resize: val.skipped_resize,
            resize_error: val.resize_error.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<InlineElement> for xberg::InlineElement {
    fn from(val: InlineElement) -> Self {
        Self {
            element_type: match val.element_type.as_str() {
                "text" => xberg::InlineType::Text,
                "strong" => xberg::InlineType::Strong,
                "emphasis" => xberg::InlineType::Emphasis,
                "highlight" => xberg::InlineType::Highlight,
                "subscript" => xberg::InlineType::Subscript,
                "superscript" => xberg::InlineType::Superscript,
                "insert" => xberg::InlineType::Insert,
                "delete" => xberg::InlineType::Delete,
                "code" => xberg::InlineType::Code,
                "link" => xberg::InlineType::Link,
                "image" => xberg::InlineType::Image,
                "span" => xberg::InlineType::Span,
                "math" => xberg::InlineType::Math,
                "raw_inline" => xberg::InlineType::RawInline,
                "footnote_ref" => xberg::InlineType::FootnoteRef,
                "symbol" => xberg::InlineType::Symbol,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'element_type' of InlineType: expected one of [text, strong, emphasis, highlight, subscript, superscript, insert, delete, code, link, image, span, math, raw_inline, footnote_ref, symbol]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::InlineType::Text
                }
            },
            content: val.content,
            attributes: val.attributes.map(Into::into),
            metadata: val
                .metadata
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::InlineElement> for InlineElement {
    fn from(val: xberg::InlineElement) -> Self {
        Self {
            element_type: serde_json::to_value(val.element_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            content: val.content.to_string(),
            attributes: val.attributes.map(Into::into),
            metadata: val
                .metadata
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JatsMetadata> for xberg::JatsMetadata {
    fn from(val: JatsMetadata) -> Self {
        Self {
            copyright: val.copyright,
            license: val.license,
            history_dates: val
                .history_dates
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            contributor_roles: val.contributor_roles.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::JatsMetadata> for JatsMetadata {
    fn from(val: xberg::JatsMetadata) -> Self {
        Self {
            copyright: val.copyright.map(|v| v.to_string()),
            license: val.license.map(|v| v.to_string()),
            history_dates: val
                .history_dates
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            contributor_roles: val.contributor_roles.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<KeyValueAttribute> for xberg::KeyValueAttribute {
    fn from(val: KeyValueAttribute) -> Self {
        Self {
            key: val.key,
            value: val.value,
            ..Default::default()
        }
    }
}

impl From<xberg::KeyValueAttribute> for KeyValueAttribute {
    fn from(val: xberg::KeyValueAttribute) -> Self {
        Self {
            key: val.key.to_string(),
            value: val.value.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Keyword> for xberg::Keyword {
    fn from(val: Keyword) -> Self {
        Self {
            text: val.text,
            score: val.score,
            algorithm: match val.algorithm.as_str() {
                #[cfg(any(
                    all(target_os = "macos", target_arch = "x86_64"),
                    not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                    target_os = "windows",
                    feature = "keywords-yake"
                ))]
                "yake" => xberg::KeywordAlgorithm::Yake,
                #[cfg(any(
                    all(target_os = "macos", target_arch = "x86_64"),
                    not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                    target_os = "windows",
                    feature = "keywords-rake"
                ))]
                "rake" => xberg::KeywordAlgorithm::Rake,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'algorithm' of KeywordAlgorithm: expected one of [yake, rake]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::KeywordAlgorithm::Yake
                }
            },
            positions: val.positions.map(|v| v.into_iter().map(|x| x as usize).collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Keyword> for Keyword {
    fn from(val: xberg::Keyword) -> Self {
        Self {
            text: val.text.to_string(),
            score: val.score,
            algorithm: serde_json::to_value(val.algorithm)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            positions: val.positions.as_ref().map(|v| v.iter().map(|&x| x as i64).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<KeywordConfig> for xberg::KeywordConfig {
    fn from(val: KeywordConfig) -> Self {
        Self {
            algorithm: match val.algorithm.as_str() {
                #[cfg(any(
                    all(target_os = "macos", target_arch = "x86_64"),
                    not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                    target_os = "windows",
                    feature = "keywords-yake"
                ))]
                "yake" => xberg::KeywordAlgorithm::Yake,
                #[cfg(any(
                    all(target_os = "macos", target_arch = "x86_64"),
                    not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                    target_os = "windows",
                    feature = "keywords-rake"
                ))]
                "rake" => xberg::KeywordAlgorithm::Rake,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'algorithm' of KeywordAlgorithm: expected one of [yake, rake]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::KeywordAlgorithm::Yake
                }
            },
            max_keywords: val.max_keywords as usize,
            min_score: val.min_score,
            ngram_range: val.ngram_range.into(),
            language: val.language,
            yake_params: val.yake_params.map(Into::into),
            rake_params: val.rake_params.map(Into::into),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::KeywordConfig> for KeywordConfig {
    fn from(val: xberg::KeywordConfig) -> Self {
        Self {
            algorithm: serde_json::to_value(val.algorithm)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            max_keywords: val.max_keywords as i64,
            min_score: val.min_score,
            ngram_range: val.ngram_range.into(),
            language: val.language.map(|v| v.to_string()),
            #[cfg(feature = "keywords-yake")]
            yake_params: val.yake_params.map(Into::into),
            #[cfg(feature = "keywords-rake")]
            rake_params: val.rake_params.map(Into::into),
        }
    }
}

impl From<LanguageConfidence> for xberg::LanguageConfidence {
    fn from(val: LanguageConfidence) -> Self {
        Self {
            language: val.language,
            confidence: val.confidence,
            proportion: val.proportion,
            script: val.script,
            reliable: val.reliable,
        }
    }
}

impl From<xberg::LanguageConfidence> for LanguageConfidence {
    fn from(val: xberg::LanguageConfidence) -> Self {
        Self {
            language: val.language.to_string(),
            confidence: val.confidence,
            proportion: val.proportion,
            script: val.script.to_string(),
            reliable: val.reliable,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<LanguageDetectionConfig> for xberg::LanguageDetectionConfig {
    fn from(val: LanguageDetectionConfig) -> Self {
        Self {
            enabled: val.enabled,
            min_confidence: val.min_confidence,
            detect_multiple: val.detect_multiple,
            ..Default::default()
        }
    }
}

impl From<xberg::LanguageDetectionConfig> for LanguageDetectionConfig {
    fn from(val: xberg::LanguageDetectionConfig) -> Self {
        Self {
            enabled: val.enabled,
            min_confidence: val.min_confidence,
            detect_multiple: val.detect_multiple,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<LateInteractionConfig> for xberg::LateInteractionConfig {
    fn from(val: LateInteractionConfig) -> Self {
        Self {
            model: val.model.into(),
            batch_size: val.batch_size as usize,
            max_length: val.max_length as usize,
            query_max_length: val.query_max_length as usize,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(Into::into),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as u64),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LateInteractionConfig> for LateInteractionConfig {
    fn from(val: xberg::LateInteractionConfig) -> Self {
        Self {
            model: val.model.into(),
            batch_size: val.batch_size as i64,
            max_length: val.max_length as i64,
            query_max_length: val.query_max_length as i64,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
        }
    }
}

impl From<LateInteractionMatch> for xberg::LateInteractionMatch {
    fn from(val: LateInteractionMatch) -> Self {
        Self {
            index: val.index as usize,
            score: val.score,
        }
    }
}

impl From<xberg::LateInteractionMatch> for LateInteractionMatch {
    fn from(val: xberg::LateInteractionMatch) -> Self {
        Self {
            index: val.index as i64,
            score: val.score,
        }
    }
}

impl From<xberg::LateInteractionPreset> for LateInteractionPreset {
    fn from(val: xberg::LateInteractionPreset) -> Self {
        Self {
            name: val.name.to_string(),
            model_repo: val.model_repo.to_string(),
            model_file: val.model_file.to_string(),
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as i64,
            query_max_length: val.query_max_length as i64,
            dim: val.dim as i64,
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LayoutDetection> for LayoutDetection {
    fn from(val: xberg::LayoutDetection) -> Self {
        Self {
            class_name: serde_json::to_value(val.class_name)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            confidence: val.confidence,
            bbox: val.bbox.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<LayoutDetectionConfig> for xberg::LayoutDetectionConfig {
    fn from(val: LayoutDetectionConfig) -> Self {
        Self {
            strategy: match val.strategy.as_str() {
                "always" => xberg::LayoutStrategy::Always,
                "auto" => xberg::LayoutStrategy::Auto,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
                        "invalid value {other:?} for field 'strategy' of LayoutStrategy: expected one of [always, auto]"
                    ))
                    .throw()
                    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            confidence_threshold: val.confidence_threshold,
            apply_heuristics: val.apply_heuristics,
            table_model: match val.table_model.as_str() {
                "tatr" => xberg::TableModel::Tatr,
                "slanet_wired" => xberg::TableModel::SlanetWired,
                "slanet_wireless" => xberg::TableModel::SlanetWireless,
                "slanet_plus" => xberg::TableModel::SlanetPlus,
                "slanet_auto" => xberg::TableModel::SlanetAuto,
                "disabled" => xberg::TableModel::Disabled,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'table_model' of TableModel: expected one of [tatr, slanet_wired, slanet_wireless, slanet_plus, slanet_auto, disabled]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            formula_model: val.formula_model.as_deref().map(|s| match s {
                "latex_ocr" => xberg::core::config::layout::FormulaModel::LatexOcr,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
                        "invalid value {other:?} for field 'formula_model' of FormulaModel: expected one of [latex_ocr]"
                    ))
                    .throw()
                    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::core::config::layout::FormulaModel::LatexOcr
                }
            }),
            table_overlap_preference: match val.table_overlap_preference.as_str() {
                "content" => xberg::core::config::layout::TableOverlapPreference::Content,
                "native" => xberg::core::config::layout::TableOverlapPreference::Native,
                "layout" => xberg::core::config::layout::TableOverlapPreference::Layout,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'table_overlap_preference' of TableOverlapPreference: expected one of [content, native, layout]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            acceleration: val.acceleration.map(Into::into),
            enable_chart_understanding: val.enable_chart_understanding,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LayoutDetectionConfig> for LayoutDetectionConfig {
    fn from(val: xberg::LayoutDetectionConfig) -> Self {
        Self {
            strategy: serde_json::to_value(val.strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            confidence_threshold: val.confidence_threshold,
            apply_heuristics: val.apply_heuristics,
            table_model: serde_json::to_value(val.table_model)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            formula_model: val.formula_model.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            table_overlap_preference: serde_json::to_value(val.table_overlap_preference)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            acceleration: val.acceleration.map(Into::into),
            enable_chart_understanding: val.enable_chart_understanding,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<LayoutRegion> for xberg::LayoutRegion {
    fn from(val: LayoutRegion) -> Self {
        Self {
            class_name: val.class_name,
            confidence: val.confidence,
            bounding_box: val.bounding_box.into(),
            area_fraction: val.area_fraction,
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::LayoutRegion> for LayoutRegion {
    fn from(val: xberg::LayoutRegion) -> Self {
        Self {
            class_name: val.class_name.to_string(),
            confidence: val.confidence,
            bounding_box: val.bounding_box.into(),
            area_fraction: val.area_fraction,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<LinkMetadata> for xberg::LinkMetadata {
    fn from(val: LinkMetadata) -> Self {
        Self {
            href: val.href,
            text: val.text,
            title: val.title,
            link_type: match val.link_type.as_str() {
                "anchor" => xberg::LinkType::Anchor,
                "internal" => xberg::LinkType::Internal,
                "external" => xberg::LinkType::External,
                "email" => xberg::LinkType::Email,
                "phone" => xberg::LinkType::Phone,
                "other" => xberg::LinkType::Other,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'link_type' of LinkType: expected one of [anchor, internal, external, email, phone, other]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::LinkType::Anchor
                }
            },
            rel: val.rel.into_iter().collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LinkMetadata> for LinkMetadata {
    fn from(val: xberg::LinkMetadata) -> Self {
        Self {
            href: val.href.to_string(),
            text: val.text.to_string(),
            title: val.title.map(|v| v.to_string()),
            link_type: serde_json::to_value(val.link_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            rel: val.rel.into_iter().collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<LlmBudgetConfig> for xberg::LlmBudgetConfig {
    fn from(val: LlmBudgetConfig) -> Self {
        Self {
            global_limit: val.global_limit,
            model_limits: val.model_limits.map(|m| m.into_iter().collect()),
            enforcement: val.enforcement,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmBudgetConfig> for LlmBudgetConfig {
    fn from(val: xberg::LlmBudgetConfig) -> Self {
        Self {
            global_limit: val.global_limit,
            model_limits: val.model_limits.map(|m| m.into_iter().collect()),
            enforcement: val.enforcement.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<LlmCacheConfig> for xberg::LlmCacheConfig {
    fn from(val: LlmCacheConfig) -> Self {
        Self {
            max_entries: val.max_entries.map(|v| v as usize),
            ttl_seconds: val.ttl_seconds.map(|v| v as u64),
            backend: val.backend,
            backend_config: val
                .backend_config
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LlmCacheConfig> for LlmCacheConfig {
    fn from(val: xberg::LlmCacheConfig) -> Self {
        Self {
            max_entries: val.max_entries.map(|v| v as i64),
            ttl_seconds: val.ttl_seconds.map(|v| v as i64),
            backend: val.backend.map(|v| v.to_string()),
            backend_config: val
                .backend_config
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<LlmConfig> for xberg::LlmConfig {
    fn from(val: LlmConfig) -> Self {
        Self {
            model: val.model,
            api_key: val.api_key,
            base_url: val.base_url,
            timeout_secs: val.timeout_secs.map(|v| v as u64),
            max_retries: val.max_retries,
            temperature: val.temperature,
            max_tokens: val.max_tokens.map(|v| v as u64),
            top_p: val.top_p,
            stop: val.stop.map(|v| v.into_iter().collect()),
            seed: val.seed,
            presence_penalty: val.presence_penalty,
            frequency_penalty: val.frequency_penalty,
            reasoning_effort: val.reasoning_effort,
            extra_body: val.extra_body,
            load_env: val.load_env,
            headers: val
                .headers
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            providers: val.providers.map(|v| v.into_iter().map(Into::into).collect()),
            cache: val.cache.map(Into::into).map(Box::new),
            budget: val.budget.map(Into::into).map(Box::new),
            rate_limit: val.rate_limit.map(Into::into).map(Box::new),
            cost_tracking: val.cost_tracking,
            tracing: val.tracing,
            cooldown_secs: val.cooldown_secs.map(|v| v as u64),
            health_check_secs: val.health_check_secs.map(|v| v as u64),
            bedrock: val.bedrock.map(Into::into).map(Box::new),
            credential_provider: val.credential_provider.map(Into::into).map(Box::new),
            max_concurrency: val.max_concurrency.map(|v| v as usize),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LlmConfig> for LlmConfig {
    fn from(val: xberg::LlmConfig) -> Self {
        Self {
            model: val.model.to_string(),
            api_key: val.api_key.map(|v| v.to_string()),
            base_url: val.base_url.map(|v| v.to_string()),
            timeout_secs: val.timeout_secs.map(|v| v as i64),
            max_retries: val.max_retries,
            temperature: val.temperature,
            max_tokens: val.max_tokens.map(|v| v as i64),
            top_p: val.top_p,
            stop: val.stop.map(|v| v.into_iter().collect()),
            seed: val.seed,
            presence_penalty: val.presence_penalty,
            frequency_penalty: val.frequency_penalty,
            reasoning_effort: val.reasoning_effort.map(|v| v.to_string()),
            extra_body: val.extra_body,
            load_env: val.load_env,
            headers: val
                .headers
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            providers: val.providers.map(|v| v.into_iter().map(Into::into).collect()),
            cache: val.cache.map(|v| (*v).into()),
            budget: val.budget.map(|v| (*v).into()),
            rate_limit: val.rate_limit.map(|v| (*v).into()),
            cost_tracking: val.cost_tracking,
            tracing: val.tracing,
            cooldown_secs: val.cooldown_secs.map(|v| v as i64),
            health_check_secs: val.health_check_secs.map(|v| v as i64),
            bedrock: val.bedrock.map(|v| (*v).into()),
            credential_provider: val.credential_provider.map(|v| (*v).into()),
            max_concurrency: val.max_concurrency.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<LlmProviderConfig> for xberg::LlmProviderConfig {
    fn from(val: LlmProviderConfig) -> Self {
        Self {
            name: val.name,
            base_url: val.base_url,
            auth_header: val.auth_header,
            model_prefixes: val.model_prefixes.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmProviderConfig> for LlmProviderConfig {
    fn from(val: xberg::LlmProviderConfig) -> Self {
        Self {
            name: val.name.to_string(),
            base_url: val.base_url.to_string(),
            auth_header: val.auth_header.map(|v| v.to_string()),
            model_prefixes: val.model_prefixes.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<LlmRateLimitConfig> for xberg::LlmRateLimitConfig {
    fn from(val: LlmRateLimitConfig) -> Self {
        Self {
            rpm: val.rpm,
            tpm: val.tpm.map(|v| v as u64),
            window_seconds: val.window_seconds.map(|v| v as u64),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmRateLimitConfig> for LlmRateLimitConfig {
    fn from(val: xberg::LlmRateLimitConfig) -> Self {
        Self {
            rpm: val.rpm,
            tpm: val.tpm.map(|v| v as i64),
            window_seconds: val.window_seconds.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<LlmUsage> for xberg::LlmUsage {
    fn from(val: LlmUsage) -> Self {
        Self {
            model: val.model,
            source: val.source,
            input_tokens: val.input_tokens.map(|v| v as u64),
            output_tokens: val.output_tokens.map(|v| v as u64),
            total_tokens: val.total_tokens.map(|v| v as u64),
            estimated_cost: val.estimated_cost,
            finish_reason: val.finish_reason,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmUsage> for LlmUsage {
    fn from(val: xberg::LlmUsage) -> Self {
        Self {
            model: val.model.to_string(),
            source: val.source.to_string(),
            input_tokens: val.input_tokens.map(|v| v as i64),
            output_tokens: val.output_tokens.map(|v| v as i64),
            total_tokens: val.total_tokens.map(|v| v as i64),
            estimated_cost: val.estimated_cost,
            finish_reason: val.finish_reason.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<MapResult> for crawlberg::MapResult {
    fn from(val: MapResult) -> Self {
        Self {
            urls: val.urls.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<crawlberg::MapResult> for MapResult {
    fn from(val: crawlberg::MapResult) -> Self {
        Self {
            urls: val.urls.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<MarkdownCodeBlock> for xberg::MarkdownCodeBlock {
    fn from(val: MarkdownCodeBlock) -> Self {
        Self {
            language: val.language,
            code: val.code,
            ..Default::default()
        }
    }
}

impl From<xberg::MarkdownCodeBlock> for MarkdownCodeBlock {
    fn from(val: xberg::MarkdownCodeBlock) -> Self {
        Self {
            language: val.language.to_string(),
            code: val.code.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<MarkdownLink> for xberg::MarkdownLink {
    fn from(val: MarkdownLink) -> Self {
        Self {
            text: val.text,
            url: val.url,
            ..Default::default()
        }
    }
}

impl From<xberg::MarkdownLink> for MarkdownLink {
    fn from(val: xberg::MarkdownLink) -> Self {
        Self {
            text: val.text.to_string(),
            url: val.url.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<Metadata> for xberg::Metadata {
    fn from(val: Metadata) -> Self {
        Self {
            title: val.title,
            subject: val.subject,
            authors: val.authors.map(|v| v.into_iter().collect()),
            keywords: val.keywords.map(|v| v.into_iter().collect()),
            language: val.language,
            created_at: val.created_at,
            modified_at: val.modified_at,
            created_by: val.created_by,
            modified_by: val.modified_by,
            pages: val.pages.map(Into::into),
            format: val.format.map(Into::into),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            json_schema: val.json_schema,
            error: val.error.map(Into::into),
            extraction_duration_ms: val.extraction_duration_ms.map(|v| v as u64),
            category: val.category,
            tags: val.tags.map(|v| v.into_iter().collect()),
            document_version: val.document_version,
            abstract_text: val.abstract_text,
            output_format: val.output_format,
            ocr_used: val.ocr_used,
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v)).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Metadata> for Metadata {
    fn from(val: xberg::Metadata) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            authors: val.authors.map(|v| v.into_iter().collect()),
            keywords: val.keywords.map(|v| v.into_iter().collect()),
            language: val.language.map(|v| v.to_string()),
            created_at: val.created_at.map(|v| v.to_string()),
            modified_at: val.modified_at.map(|v| v.to_string()),
            created_by: val.created_by.map(|v| v.to_string()),
            modified_by: val.modified_by.map(|v| v.to_string()),
            pages: val.pages.map(Into::into),
            format: val.format.map(Into::into),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            json_schema: val.json_schema,
            error: val.error.map(Into::into),
            extraction_duration_ms: val.extraction_duration_ms.map(|v| v as i64),
            category: val.category.map(|v| v.to_string()),
            tags: val.tags.map(|v| v.into_iter().collect()),
            document_version: val.document_version.map(|v| v.to_string()),
            abstract_text: val.abstract_text.map(|v| v.to_string()),
            output_format: val.output_format.map(|v| v.to_string()),
            ocr_used: val.ocr_used,
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v)).collect(),
        }
    }
}

impl From<xberg::ModelPaths> for ModelPaths {
    fn from(val: xberg::ModelPaths) -> Self {
        Self {
            det_model: val.det_model.to_string_lossy().to_string(),
            cls_model: val.cls_model.to_string_lossy().to_string(),
            rec_model: val.rec_model.to_string_lossy().to_string(),
            dict_file: val.dict_file.to_string_lossy().to_string(),
        }
    }
}

impl From<MultiVectorEmbedding> for xberg::MultiVectorEmbedding {
    fn from(val: MultiVectorEmbedding) -> Self {
        Self {
            num_tokens: val.num_tokens,
            dim: val.dim,
            data: val.data.into_iter().collect(),
        }
    }
}

impl From<xberg::MultiVectorEmbedding> for MultiVectorEmbedding {
    fn from(val: xberg::MultiVectorEmbedding) -> Self {
        Self {
            num_tokens: val.num_tokens,
            dim: val.dim,
            data: val.data.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::MultidocInput> for MultidocInput {
    fn from(val: xberg::MultidocInput) -> Self {
        Self {
            page_count: val.page_count,
            pages: val.pages.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<MultidocThresholds> for xberg::MultidocThresholds {
    fn from(val: MultidocThresholds) -> Self {
        Self {
            density_shift_threshold: val.density_shift_threshold,
            bigram_overlap_min: val.bigram_overlap_min,
            ..Default::default()
        }
    }
}

impl From<xberg::MultidocThresholds> for MultidocThresholds {
    fn from(val: xberg::MultidocThresholds) -> Self {
        Self {
            density_shift_threshold: val.density_shift_threshold,
            bigram_overlap_min: val.bigram_overlap_min,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<NerConfig> for xberg::NerConfig {
    fn from(val: NerConfig) -> Self {
        Self {
            backend: match val.backend.as_str() { "onnx" => xberg::NerBackendKind::Onnx,"llm" => xberg::NerBackendKind::Llm,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'backend' of NerBackendKind: expected one of [onnx, llm]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 }
,
            categories: val.categories.into_iter().map(|s| match s.as_str() { "person" => xberg::EntityCategory::Person,"organization" => xberg::EntityCategory::Organization,"location" => xberg::EntityCategory::Location,"date" => xberg::EntityCategory::Date,"time" => xberg::EntityCategory::Time,"money" => xberg::EntityCategory::Money,"percent" => xberg::EntityCategory::Percent,"email" => xberg::EntityCategory::Email,"phone" => xberg::EntityCategory::Phone,"url" => xberg::EntityCategory::Url,"custom" => xberg::EntityCategory::Custom(Default::default()),other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'categories' of EntityCategory: expected one of [person, organization, location, date, time, money, percent, email, phone, url, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    xberg::EntityCategory::Person
}
 }
).collect(),
            model: val.model,
            llm: val.llm.map(Into::into),
            custom_labels: val.custom_labels.into_iter().collect(),
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::NerConfig> for NerConfig {
    fn from(val: xberg::NerConfig) -> Self {
        Self {
            backend: serde_json::to_value(val.backend)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            categories: val
                .categories
                .iter()
                .map(|v| {
                    serde_json::to_value(v)
                        .ok()
                        .and_then(|s| s.as_str().map(String::from))
                        .unwrap_or_default()
                })
                .collect(),
            model: val.model.map(|v| v.to_string()),
            llm: val.llm.map(Into::into),
            custom_labels: val.custom_labels.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<NgramRange> for xberg::NgramRange {
    fn from(val: NgramRange) -> Self {
        Self {
            min: val.min as usize,
            max: val.max as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::NgramRange> for NgramRange {
    fn from(val: xberg::NgramRange) -> Self {
        Self {
            min: val.min as i64,
            max: val.max as i64,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<OcrConfidence> for xberg::OcrConfidence {
    fn from(val: OcrConfidence) -> Self {
        Self {
            detection: val.detection,
            recognition: val.recognition,
            ..Default::default()
        }
    }
}

impl From<xberg::OcrConfidence> for OcrConfidence {
    fn from(val: xberg::OcrConfidence) -> Self {
        Self {
            detection: val.detection,
            recognition: val.recognition,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<OcrConfig> for xberg::OcrConfig {
    fn from(val: OcrConfig) -> Self {
        Self {
            enabled: val.enabled,
            backend: val.backend,
            language: val.language.into_iter().collect(),
            tesseract_config: val.tesseract_config.map(Into::into),
            output_format: val.output_format.as_deref().map(|s| match s { "plain" => xberg::OutputFormat::Plain,"markdown" => xberg::OutputFormat::Markdown,"djot" => xberg::OutputFormat::Djot,"html" => xberg::OutputFormat::Html,"json" => xberg::OutputFormat::Json,"doctags" => xberg::OutputFormat::DocTags,"custom" => xberg::OutputFormat::Custom(Default::default()),other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'output_format' of OutputFormat: expected one of [plain, markdown, djot, html, json, doctags, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            paddle_ocr_config: val.paddle_ocr_config,
            backend_options: val.backend_options,
            element_config: val.element_config.map(Into::into),
            quality_thresholds: val.quality_thresholds.map(Into::into),
            pipeline: val.pipeline.map(Into::into),
            auto_rotate: val.auto_rotate,
            vlm_fallback: val.vlm_fallback.into(),
            vlm_config: val.vlm_config.map(Into::into),
            vlm_prompt: val.vlm_prompt,
            acceleration: val.acceleration.map(Into::into),
            security_limits: val.security_limits.map(Into::into),
            tessdata_bytes: val.tessdata_bytes.map(|m| m.into_iter().map(|(k, v)| (k, v.to_vec().into())).collect()),
            tessdata_path: val.tessdata_path.map(Into::into),
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrConfig> for OcrConfig {
    fn from(val: xberg::OcrConfig) -> Self {
        Self {
            enabled: val.enabled,
            backend: val.backend.to_string(),
            language: val.language.into_iter().collect(),
            tesseract_config: val.tesseract_config.map(Into::into),
            output_format: val.output_format.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            paddle_ocr_config: val.paddle_ocr_config,
            backend_options: val.backend_options,
            element_config: val.element_config.map(Into::into),
            quality_thresholds: val.quality_thresholds.map(Into::into),
            pipeline: val.pipeline.map(Into::into),
            auto_rotate: val.auto_rotate,
            vlm_fallback: val.vlm_fallback.into(),
            vlm_config: val.vlm_config.map(Into::into),
            vlm_prompt: val.vlm_prompt.map(|v| v.to_string()),
            acceleration: val.acceleration.map(Into::into),
            security_limits: val.security_limits.map(Into::into),
            tessdata_bytes: val
                .tessdata_bytes
                .map(|m| m.into_iter().map(|(k, v)| (k, v.to_vec().into())).collect()),
            tessdata_path: val.tessdata_path.map(|p| p.to_string_lossy().to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<OcrElement> for xberg::OcrElement {
    fn from(val: OcrElement) -> Self {
        Self {
            text: val.text,
            geometry: val.geometry.into(),
            confidence: val.confidence.into(),
            level: match val.level.as_str() {
                "word" => xberg::OcrElementLevel::Word,
                "line" => xberg::OcrElementLevel::Line,
                "block" => xberg::OcrElementLevel::Block,
                "page" => xberg::OcrElementLevel::Page,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'level' of OcrElementLevel: expected one of [word, line, block, page]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            rotation: val.rotation.map(Into::into),
            page_number: val.page_number,
            parent_id: val.parent_id,
            backend_metadata: val.backend_metadata.into_iter().map(|(k, v)| (k.into(), v)).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrElement> for OcrElement {
    fn from(val: xberg::OcrElement) -> Self {
        Self {
            text: val.text.to_string(),
            geometry: val.geometry.into(),
            confidence: val.confidence.into(),
            level: serde_json::to_value(val.level)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            rotation: val.rotation.map(Into::into),
            page_number: val.page_number,
            parent_id: val.parent_id.map(|v| v.to_string()),
            backend_metadata: val.backend_metadata.into_iter().map(|(k, v)| (k.into(), v)).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<OcrElementConfig> for xberg::OcrElementConfig {
    fn from(val: OcrElementConfig) -> Self {
        Self {
            include_elements: val.include_elements,
            min_level: match val.min_level.as_str() {
                "word" => xberg::OcrElementLevel::Word,
                "line" => xberg::OcrElementLevel::Line,
                "block" => xberg::OcrElementLevel::Block,
                "page" => xberg::OcrElementLevel::Page,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'min_level' of OcrElementLevel: expected one of [word, line, block, page]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            min_confidence: val.min_confidence,
            build_hierarchy: val.build_hierarchy,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::OcrElementConfig> for OcrElementConfig {
    fn from(val: xberg::OcrElementConfig) -> Self {
        Self {
            include_elements: val.include_elements,
            min_level: serde_json::to_value(val.min_level)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            min_confidence: val.min_confidence,
            build_hierarchy: val.build_hierarchy,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrExtractionResult> for OcrExtractionResult {
    fn from(val: xberg::OcrExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            mime_type: val.mime_type.to_string(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v)).collect(),
            tables: val.tables.into_iter().map(Into::into).collect(),
            ocr_elements: val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<OcrMetadata> for xberg::OcrMetadata {
    fn from(val: OcrMetadata) -> Self {
        Self {
            language: val.language,
            psm: val.psm,
            output_format: val.output_format,
            table_count: val.table_count,
            table_rows: val.table_rows,
            table_cols: val.table_cols,
            ..Default::default()
        }
    }
}

impl From<xberg::OcrMetadata> for OcrMetadata {
    fn from(val: xberg::OcrMetadata) -> Self {
        Self {
            language: val.language.to_string(),
            psm: val.psm,
            output_format: val.output_format.to_string(),
            table_count: val.table_count,
            table_rows: val.table_rows,
            table_cols: val.table_cols,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<OcrPipelineConfig> for xberg::OcrPipelineConfig {
    fn from(val: OcrPipelineConfig) -> Self {
        Self {
            stages: val.stages.into_iter().map(Into::into).collect(),
            quality_thresholds: val.quality_thresholds.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::OcrPipelineConfig> for OcrPipelineConfig {
    fn from(val: xberg::OcrPipelineConfig) -> Self {
        Self {
            stages: val.stages.into_iter().map(Into::into).collect(),
            quality_thresholds: val.quality_thresholds.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<OcrPipelineStage> for xberg::OcrPipelineStage {
    fn from(val: OcrPipelineStage) -> Self {
        Self {
            backend: val.backend,
            priority: val.priority,
            language: val.language.map(|v| v.into_iter().collect()),
            tesseract_config: val.tesseract_config.map(Into::into),
            paddle_ocr_config: val.paddle_ocr_config,
            vlm_config: val.vlm_config.map(Into::into),
            backend_options: val.backend_options,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrPipelineStage> for OcrPipelineStage {
    fn from(val: xberg::OcrPipelineStage) -> Self {
        Self {
            backend: val.backend.to_string(),
            priority: val.priority,
            language: val.language.map(|v| v.into_iter().collect()),
            tesseract_config: val.tesseract_config.map(Into::into),
            paddle_ocr_config: val.paddle_ocr_config,
            vlm_config: val.vlm_config.map(Into::into),
            backend_options: val.backend_options,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<OcrPoint> for xberg::OcrPoint {
    fn from(val: OcrPoint) -> Self {
        Self {
            x: val.x,
            y: val.y,
            ..Default::default()
        }
    }
}

impl From<xberg::OcrPoint> for OcrPoint {
    fn from(val: xberg::OcrPoint) -> Self {
        Self { x: val.x, y: val.y }
    }
}

#[allow(clippy::needless_update)]
impl From<OcrQualityThresholds> for xberg::OcrQualityThresholds {
    fn from(val: OcrQualityThresholds) -> Self {
        Self {
            min_total_non_whitespace: val.min_total_non_whitespace as usize,
            min_non_whitespace_per_page: val.min_non_whitespace_per_page,
            min_meaningful_word_len: val.min_meaningful_word_len as usize,
            min_meaningful_words: val.min_meaningful_words as usize,
            min_alnum_ratio: val.min_alnum_ratio,
            min_garbage_chars: val.min_garbage_chars as usize,
            max_fragmented_word_ratio: val.max_fragmented_word_ratio,
            critical_fragmented_word_ratio: val.critical_fragmented_word_ratio,
            max_ocr_output_fragmented_word_ratio: val.max_ocr_output_fragmented_word_ratio,
            min_ocr_mean_confidence: val.min_ocr_mean_confidence,
            min_words_for_ocr_output_check: val.min_words_for_ocr_output_check as usize,
            max_ocr_output_dict_invalid_word_ratio: val.max_ocr_output_dict_invalid_word_ratio,
            discard_suspected_ocr_noise: val.discard_suspected_ocr_noise,
            min_avg_word_length: val.min_avg_word_length,
            min_words_for_avg_length_check: val.min_words_for_avg_length_check as usize,
            min_consecutive_repeat_ratio: val.min_consecutive_repeat_ratio,
            min_words_for_repeat_check: val.min_words_for_repeat_check as usize,
            substantive_min_chars: val.substantive_min_chars as usize,
            non_text_min_chars: val.non_text_min_chars as usize,
            alnum_ws_ratio_threshold: val.alnum_ws_ratio_threshold,
            pipeline_min_quality: val.pipeline_min_quality,
            min_undecodable_ratio: val.min_undecodable_ratio,
            enable_provenance_ocr_routing: val.enable_provenance_ocr_routing,
            min_provenance_fallback_ratio: val.min_provenance_fallback_ratio,
            ..Default::default()
        }
    }
}

impl From<xberg::OcrQualityThresholds> for OcrQualityThresholds {
    fn from(val: xberg::OcrQualityThresholds) -> Self {
        Self {
            min_total_non_whitespace: val.min_total_non_whitespace as i64,
            min_non_whitespace_per_page: val.min_non_whitespace_per_page,
            min_meaningful_word_len: val.min_meaningful_word_len as i64,
            min_meaningful_words: val.min_meaningful_words as i64,
            min_alnum_ratio: val.min_alnum_ratio,
            min_garbage_chars: val.min_garbage_chars as i64,
            max_fragmented_word_ratio: val.max_fragmented_word_ratio,
            critical_fragmented_word_ratio: val.critical_fragmented_word_ratio,
            max_ocr_output_fragmented_word_ratio: val.max_ocr_output_fragmented_word_ratio,
            min_ocr_mean_confidence: val.min_ocr_mean_confidence,
            min_words_for_ocr_output_check: val.min_words_for_ocr_output_check as i64,
            max_ocr_output_dict_invalid_word_ratio: val.max_ocr_output_dict_invalid_word_ratio,
            discard_suspected_ocr_noise: val.discard_suspected_ocr_noise,
            min_avg_word_length: val.min_avg_word_length,
            min_words_for_avg_length_check: val.min_words_for_avg_length_check as i64,
            min_consecutive_repeat_ratio: val.min_consecutive_repeat_ratio,
            min_words_for_repeat_check: val.min_words_for_repeat_check as i64,
            substantive_min_chars: val.substantive_min_chars as i64,
            non_text_min_chars: val.non_text_min_chars as i64,
            alnum_ws_ratio_threshold: val.alnum_ws_ratio_threshold,
            pipeline_min_quality: val.pipeline_min_quality,
            min_undecodable_ratio: val.min_undecodable_ratio,
            enable_provenance_ocr_routing: val.enable_provenance_ocr_routing,
            min_provenance_fallback_ratio: val.min_provenance_fallback_ratio,
        }
    }
}

impl From<OcrRotation> for xberg::OcrRotation {
    fn from(val: OcrRotation) -> Self {
        Self {
            angle_degrees: val.angle_degrees,
            confidence: val.confidence,
        }
    }
}

impl From<xberg::OcrRotation> for OcrRotation {
    fn from(val: xberg::OcrRotation) -> Self {
        Self {
            angle_degrees: val.angle_degrees,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::OcrTable> for OcrTable {
    fn from(val: xberg::OcrTable) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown.to_string(),
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
        }
    }
}

impl From<xberg::OcrTableBoundingBox> for OcrTableBoundingBox {
    fn from(val: xberg::OcrTableBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
        }
    }
}

impl From<xberg::OrientationResult> for OrientationResult {
    fn from(val: xberg::OrientationResult) -> Self {
        Self {
            degrees: val.degrees,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<PaddleOcrConfig> for xberg::PaddleOcrConfig {
    fn from(val: PaddleOcrConfig) -> Self {
        Self {
            language: val.language,
            cache_dir: val.cache_dir.map(Into::into),
            use_angle_cls: val.use_angle_cls,
            enable_table_detection: val.enable_table_detection,
            det_db_thresh: val.det_db_thresh,
            det_db_box_thresh: val.det_db_box_thresh,
            det_db_unclip_ratio: val.det_db_unclip_ratio,
            det_limit_side_len: val.det_limit_side_len,
            rec_batch_num: val.rec_batch_num,
            padding: val.padding,
            drop_score: val.drop_score,
            model_tier: val.model_tier,
            model_version: val.model_version,
            inference_backend: val.inference_backend.as_deref().map(|s| match s {
                "ort" => xberg::PaddleInferenceBackend::Ort,
                "tract" => xberg::PaddleInferenceBackend::Tract,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'inference_backend' of PaddleInferenceBackend: expected one of [ort, tract]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PaddleInferenceBackend::Ort
                }
            }),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PaddleOcrConfig> for PaddleOcrConfig {
    fn from(val: xberg::PaddleOcrConfig) -> Self {
        Self {
            language: val.language.to_string(),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            use_angle_cls: val.use_angle_cls,
            enable_table_detection: val.enable_table_detection,
            det_db_thresh: val.det_db_thresh,
            det_db_box_thresh: val.det_db_box_thresh,
            det_db_unclip_ratio: val.det_db_unclip_ratio,
            det_limit_side_len: val.det_limit_side_len,
            rec_batch_num: val.rec_batch_num,
            padding: val.padding,
            drop_score: val.drop_score,
            model_tier: val.model_tier.to_string(),
            model_version: val.model_version.to_string(),
            inference_backend: val.inference_backend.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::candle_ocr::PaddleOcrVlBackendOptions> for PaddleOcrVlBackendOptions {
    fn from(val: xberg::candle_ocr::PaddleOcrVlBackendOptions) -> Self {
        Self {
            task: val.task.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            model_path: val.model_path.map(|v| v.to_string()),
            model_id: val.model_id.map(|v| v.to_string()),
            hf_revision: val.hf_revision.map(|v| v.to_string()),
            cache_dir: val.cache_dir.map(|v| v.to_string()),
            device: val.device.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
        }
    }
}

impl From<PageBoundary> for xberg::PageBoundary {
    fn from(val: PageBoundary) -> Self {
        Self {
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
            page_number: val.page_number,
        }
    }
}

impl From<xberg::PageBoundary> for PageBoundary {
    fn from(val: xberg::PageBoundary) -> Self {
        Self {
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
            page_number: val.page_number,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageClassification> for xberg::PageClassification {
    fn from(val: PageClassification) -> Self {
        Self {
            page_number: val.page_number,
            labels: val.labels.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageClassification> for PageClassification {
    fn from(val: xberg::PageClassification) -> Self {
        Self {
            page_number: val.page_number,
            labels: val.labels.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageClassificationConfig> for xberg::PageClassificationConfig {
    fn from(val: PageClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template,
            labels: val.labels.into_iter().collect(),
            multi_label: val.multi_label,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageClassificationConfig> for PageClassificationConfig {
    fn from(val: xberg::PageClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template.map(|v| v.to_string()),
            labels: val.labels.into_iter().collect(),
            multi_label: val.multi_label,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PageConfig> for xberg::PageConfig {
    fn from(val: PageConfig) -> Self {
        Self {
            extract_pages: val.extract_pages,
            insert_page_markers: val.insert_page_markers,
            marker_format: val.marker_format,
            ..Default::default()
        }
    }
}

impl From<xberg::PageConfig> for PageConfig {
    fn from(val: xberg::PageConfig) -> Self {
        Self {
            extract_pages: val.extract_pages,
            insert_page_markers: val.insert_page_markers,
            marker_format: val.marker_format.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<PageContent> for xberg::PageContent {
    fn from(val: PageContent) -> Self {
        Self {
            page_number: val.page_number,
            content: val.content,
            tables: val.tables.into_iter().map(|v| std::sync::Arc::new(v.into())).collect(),
            image_indices: val.image_indices.into_iter().collect(),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            hierarchy: val.hierarchy.map(Into::into),
            is_blank: val.is_blank,
            layout_regions: val.layout_regions.map(|v| v.into_iter().map(Into::into).collect()),
            speaker_notes: val.speaker_notes,
            section_name: val.section_name,
            sheet_name: val.sheet_name,
            ocr_confidence: val.ocr_confidence.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageContent> for PageContent {
    fn from(val: xberg::PageContent) -> Self {
        Self {
            page_number: val.page_number,
            content: val.content.to_string(),
            tables: val.tables.into_iter().map(|v| (*v).clone().into()).collect(),
            image_indices: val.image_indices.into_iter().collect(),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            hierarchy: val.hierarchy.map(Into::into),
            is_blank: val.is_blank,
            layout_regions: val.layout_regions.map(|v| v.into_iter().map(Into::into).collect()),
            speaker_notes: val.speaker_notes.map(|v| v.to_string()),
            section_name: val.section_name.map(|v| v.to_string()),
            sheet_name: val.sheet_name.map(|v| v.to_string()),
            ocr_confidence: val.ocr_confidence.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PageDimensions> for xberg::PageDimensions {
    fn from(val: PageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
            ..Default::default()
        }
    }
}

impl From<xberg::PageDimensions> for PageDimensions {
    fn from(val: xberg::PageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageHierarchy> for xberg::PageHierarchy {
    fn from(val: PageHierarchy) -> Self {
        Self {
            block_count: val.block_count,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageHierarchy> for PageHierarchy {
    fn from(val: xberg::PageHierarchy) -> Self {
        Self {
            block_count: val.block_count,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageInfo> for xberg::PageInfo {
    fn from(val: PageInfo) -> Self {
        Self {
            number: val.number,
            title: val.title,
            dimensions: val.dimensions.map(Into::into),
            image_count: val.image_count,
            table_count: val.table_count,
            hidden: val.hidden,
            is_blank: val.is_blank,
            has_vector_graphics: val.has_vector_graphics,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageInfo> for PageInfo {
    fn from(val: xberg::PageInfo) -> Self {
        Self {
            number: val.number,
            title: val.title.map(|v| v.to_string()),
            dimensions: val.dimensions.map(Into::into),
            image_count: val.image_count,
            table_count: val.table_count,
            hidden: val.hidden,
            is_blank: val.is_blank,
            has_vector_graphics: val.has_vector_graphics,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PageOcrConfidence> for xberg::PageOcrConfidence {
    fn from(val: PageOcrConfidence) -> Self {
        Self {
            score: val.score,
            word_count: val.word_count,
            backend: val.backend,
            ..Default::default()
        }
    }
}

impl From<xberg::PageOcrConfidence> for PageOcrConfidence {
    fn from(val: xberg::PageOcrConfidence) -> Self {
        Self {
            score: val.score,
            word_count: val.word_count,
            backend: val.backend.to_string(),
        }
    }
}

impl From<xberg::PageRange> for PageRange {
    fn from(val: xberg::PageRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

impl From<PageSignals> for xberg::PageSignals {
    fn from(val: PageSignals) -> Self {
        Self {
            page_number: val.page_number,
            text_excerpt: val.text_excerpt,
            starts_with_letterhead_like: val.starts_with_letterhead_like,
            has_page_number_one_marker: val.has_page_number_one_marker,
            has_signature_block: val.has_signature_block,
            layout_text_density: val.layout_text_density,
        }
    }
}

impl From<xberg::PageSignals> for PageSignals {
    fn from(val: xberg::PageSignals) -> Self {
        Self {
            page_number: val.page_number,
            text_excerpt: val.text_excerpt.to_string(),
            starts_with_letterhead_like: val.starts_with_letterhead_like,
            has_page_number_one_marker: val.has_page_number_one_marker,
            has_signature_block: val.has_signature_block,
            layout_text_density: val.layout_text_density,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageSpan> for xberg::PageSpan {
    fn from(val: PageSpan) -> Self {
        Self {
            page: val.page,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageSpan> for PageSpan {
    fn from(val: xberg::PageSpan) -> Self {
        Self {
            page: val.page,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PageStructure> for xberg::PageStructure {
    fn from(val: PageStructure) -> Self {
        Self {
            total_count: val.total_count,
            unit_type: match val.unit_type.as_str() {
                "page" => xberg::PageUnitType::Page,
                "slide" => xberg::PageUnitType::Slide,
                "sheet" => xberg::PageUnitType::Sheet,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'unit_type' of PageUnitType: expected one of [page, slide, sheet]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PageUnitType::Page
                }
            },
            boundaries: val.boundaries.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageStructure> for PageStructure {
    fn from(val: xberg::PageStructure) -> Self {
        Self {
            total_count: val.total_count,
            unit_type: serde_json::to_value(val.unit_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            boundaries: val.boundaries.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::text::redaction::patterns::PatternMatch> for PatternMatch {
    fn from(val: xberg::text::redaction::patterns::PatternMatch) -> Self {
        Self {
            start: val.start as i64,
            end: val.end as i64,
            category: serde_json::to_value(val.category)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PdfAnnotation> for xberg::PdfAnnotation {
    fn from(val: PdfAnnotation) -> Self {
        Self {
            annotation_type: match val.annotation_type.as_str() {
                "text" => xberg::PdfAnnotationType::Text,
                "highlight" => xberg::PdfAnnotationType::Highlight,
                "link" => xberg::PdfAnnotationType::Link,
                "stamp" => xberg::PdfAnnotationType::Stamp,
                "underline" => xberg::PdfAnnotationType::Underline,
                "strike_out" => xberg::PdfAnnotationType::StrikeOut,
                "squiggly" => xberg::PdfAnnotationType::Squiggly,
                "ink" => xberg::PdfAnnotationType::Ink,
                "square" => xberg::PdfAnnotationType::Square,
                "circle" => xberg::PdfAnnotationType::Circle,
                "polygon" => xberg::PdfAnnotationType::Polygon,
                "poly_line" => xberg::PdfAnnotationType::PolyLine,
                "line" => xberg::PdfAnnotationType::Line,
                "caret" => xberg::PdfAnnotationType::Caret,
                "file_attachment" => xberg::PdfAnnotationType::FileAttachment,
                "sound" => xberg::PdfAnnotationType::Sound,
                "movie" => xberg::PdfAnnotationType::Movie,
                "other" => xberg::PdfAnnotationType::Other,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'annotation_type' of PdfAnnotationType: expected one of [text, highlight, link, stamp, underline, strike_out, squiggly, ink, square, circle, polygon, poly_line, line, caret, file_attachment, sound, movie, other]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PdfAnnotationType::Text
                }
            },
            content: val.content,
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            author: val.author,
            modified: val.modified,
            color: val.color,
            subject: val.subject,
            quad_points: val.quad_points.map(|v| v.into_iter().map(Into::into).collect()),
            marked_text: val.marked_text,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfAnnotation> for PdfAnnotation {
    fn from(val: xberg::PdfAnnotation) -> Self {
        Self {
            annotation_type: serde_json::to_value(val.annotation_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            content: val.content.map(|v| v.to_string()),
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            author: val.author.map(|v| v.to_string()),
            modified: val.modified.map(|v| v.to_string()),
            color: val.color.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            quad_points: val.quad_points.map(|v| v.into_iter().map(Into::into).collect()),
            marked_text: val.marked_text.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<PdfConfig> for xberg::PdfConfig {
    fn from(val: PdfConfig) -> Self {
        Self {
            extract_images: val.extract_images,
            extract_tables: val.extract_tables,
            passwords: val.passwords.map(|v| v.into_iter().collect()),
            extract_metadata: val.extract_metadata,
            hierarchy: val.hierarchy.map(Into::into),
            extract_annotations: val.extract_annotations,
            top_margin_fraction: val.top_margin_fraction,
            bottom_margin_fraction: val.bottom_margin_fraction,
            allow_single_column_tables: val.allow_single_column_tables,
            ocr_inline_images: val.ocr_inline_images,
            extract_form_fields: val.extract_form_fields,
            reading_order: val.reading_order,
            backend: match val.backend.as_str() {
                "native" => xberg::PdfBackend::Native,
                "pdfium" => xberg::PdfBackend::Pdfium,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
                        "invalid value {other:?} for field 'backend' of PdfBackend: expected one of [native, pdfium]"
                    ))
                    .throw()
                    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfConfig> for PdfConfig {
    fn from(val: xberg::PdfConfig) -> Self {
        Self {
            extract_images: val.extract_images,
            extract_tables: val.extract_tables,
            passwords: val.passwords.map(|v| v.into_iter().collect()),
            extract_metadata: val.extract_metadata,
            hierarchy: val.hierarchy.map(Into::into),
            extract_annotations: val.extract_annotations,
            top_margin_fraction: val.top_margin_fraction,
            bottom_margin_fraction: val.bottom_margin_fraction,
            allow_single_column_tables: val.allow_single_column_tables,
            ocr_inline_images: val.ocr_inline_images,
            extract_form_fields: val.extract_form_fields,
            reading_order: val.reading_order,
            backend: serde_json::to_value(val.backend)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PdfFormField> for xberg::PdfFormField {
    fn from(val: PdfFormField) -> Self {
        Self {
            name: val.name,
            full_name: val.full_name,
            field_type: match val.field_type.as_str() {
                "text" => xberg::FormFieldType::Text,
                "checkbox" => xberg::FormFieldType::Checkbox,
                "radio" => xberg::FormFieldType::Radio,
                "choice" => xberg::FormFieldType::Choice,
                "signature" => xberg::FormFieldType::Signature,
                "button" => xberg::FormFieldType::Button,
                "unknown" => xberg::FormFieldType::Unknown,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'field_type' of FormFieldType: expected one of [text, checkbox, radio, choice, signature, button, unknown]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            value: val.value,
            default_value: val.default_value,
            flags: val.flags,
            page: val.page,
            bbox: val.bbox.map(Into::into),
            max_length: val.max_length,
            tooltip: val.tooltip,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfFormField> for PdfFormField {
    fn from(val: xberg::PdfFormField) -> Self {
        Self {
            name: val.name.to_string(),
            full_name: val.full_name.to_string(),
            field_type: serde_json::to_value(val.field_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            value: val.value.map(|v| v.to_string()),
            default_value: val.default_value.map(|v| v.to_string()),
            flags: val.flags,
            page: val.page,
            bbox: val.bbox.map(Into::into),
            max_length: val.max_length,
            tooltip: val.tooltip.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<PdfMetadata> for xberg::pdf::metadata::PdfMetadata {
    fn from(val: PdfMetadata) -> Self {
        Self {
            pdf_version: val.pdf_version,
            producer: val.producer,
            is_encrypted: val.is_encrypted,
            width: val.width,
            height: val.height,
            page_count: val.page_count,
            scanned_confidence: val.scanned_confidence,
            scanned_pages: val.scanned_pages.map(|v| v.into_iter().collect()),
            layout_gated_pages: val.layout_gated_pages.map(|v| v.into_iter().collect()),
            layout_gate_reasons: val.layout_gate_reasons.map(|v| v.into_iter().collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::pdf::metadata::PdfMetadata> for PdfMetadata {
    fn from(val: xberg::pdf::metadata::PdfMetadata) -> Self {
        Self {
            pdf_version: val.pdf_version.map(|v| v.to_string()),
            producer: val.producer.map(|v| v.to_string()),
            is_encrypted: val.is_encrypted,
            width: val.width,
            height: val.height,
            page_count: val.page_count,
            scanned_confidence: val.scanned_confidence,
            scanned_pages: val.scanned_pages.map(|v| v.into_iter().collect()),
            layout_gated_pages: val.layout_gated_pages.map(|v| v.into_iter().collect()),
            layout_gate_reasons: val.layout_gate_reasons.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PixelDimensions> for xberg::PixelDimensions {
    fn from(val: PixelDimensions) -> Self {
        Self {
            width: val.width as usize,
            height: val.height as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::PixelDimensions> for PixelDimensions {
    fn from(val: xberg::PixelDimensions) -> Self {
        Self {
            width: val.width as i64,
            height: val.height as i64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<PostProcessorConfig> for xberg::PostProcessorConfig {
    fn from(val: PostProcessorConfig) -> Self {
        Self {
            enabled: val.enabled,
            enabled_processors: val.enabled_processors.map(|v| v.into_iter().collect()),
            disabled_processors: val.disabled_processors.map(|v| v.into_iter().collect()),
            enabled_set: val.enabled_set.map(|v| v.into_iter().collect()),
            disabled_set: val.disabled_set.map(|v| v.into_iter().collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PostProcessorConfig> for PostProcessorConfig {
    fn from(val: xberg::PostProcessorConfig) -> Self {
        Self {
            enabled: val.enabled,
            enabled_processors: val.enabled_processors.map(|v| v.into_iter().collect()),
            disabled_processors: val.disabled_processors.map(|v| v.into_iter().collect()),
            enabled_set: val.enabled_set.map(|v| v.into_iter().collect()),
            disabled_set: val.disabled_set.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::extraction::office_metadata::app_properties::PptxAppProperties> for PptxAppProperties {
    fn from(val: xberg::extraction::office_metadata::app_properties::PptxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            total_time: val.total_time,
            company: val.company.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            slides: val.slides,
            notes: val.notes,
            hidden_slides: val.hidden_slides,
            multimedia_clips: val.multimedia_clips,
            presentation_format: val.presentation_format.map(|v| v.to_string()),
            slide_titles: val.slide_titles.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PptxExtractionResult> for PptxExtractionResult {
    fn from(val: xberg::PptxExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            metadata: val.metadata.into(),
            slide_count: val.slide_count as i64,
            image_count: val.image_count as i64,
            table_count: val.table_count as i64,
            images: val.images.into_iter().map(Into::into).collect(),
            page_structure: val.page_structure.map(Into::into),
            page_contents: val.page_contents.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            hyperlinks: val.hyperlinks.into_iter().map(Into::into).collect(),
            office_metadata: val
                .office_metadata
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PptxMetadata> for xberg::PptxMetadata {
    fn from(val: PptxMetadata) -> Self {
        Self {
            slide_count: val.slide_count,
            slide_names: val.slide_names.into_iter().collect(),
            image_count: val.image_count,
            table_count: val.table_count,
            ..Default::default()
        }
    }
}

impl From<xberg::PptxMetadata> for PptxMetadata {
    fn from(val: xberg::PptxMetadata) -> Self {
        Self {
            slide_count: val.slide_count,
            slide_names: val.slide_names.into_iter().collect(),
            image_count: val.image_count,
            table_count: val.table_count,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<PreprocessingOptions> for html_to_markdown_rs::PreprocessingOptions {
    fn from(val: PreprocessingOptions) -> Self {
        Self {
            enabled: val.enabled,
            preset: match val.preset.as_str() {
                "Minimal" | "minimal" => html_to_markdown_rs::PreprocessingPreset::Minimal,
                "Standard" | "standard" => html_to_markdown_rs::PreprocessingPreset::Standard,
                "Aggressive" | "aggressive" => html_to_markdown_rs::PreprocessingPreset::Aggressive,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'preset' of PreprocessingPreset: expected one of [Minimal, Standard, Aggressive]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<html_to_markdown_rs::PreprocessingOptions> for PreprocessingOptions {
    fn from(val: html_to_markdown_rs::PreprocessingOptions) -> Self {
        Self {
            enabled: val.enabled,
            preset: serde_json::to_value(val.preset)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PresentationHyperlink> for PresentationHyperlink {
    fn from(val: xberg::PresentationHyperlink) -> Self {
        Self {
            url: val.url.to_string(),
            label: val.label.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Preset> for xberg::Preset {
    fn from(val: Preset) -> Self {
        Self {
            id: val.id,
            version: val.version,
            schema_name: val.schema_name,
            description: val.description,
            category: match val.category.as_str() {
                "finance" => xberg::PresetCategory::Finance,
                "identity" => xberg::PresetCategory::Identity,
                "legal" => xberg::PresetCategory::Legal,
                "logistics" => xberg::PresetCategory::Logistics,
                "medical" => xberg::PresetCategory::Medical,
                "hr" => xberg::PresetCategory::Hr,
                "other" => xberg::PresetCategory::Other,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'category' of PresetCategory: expected one of [finance, identity, legal, logistics, medical, hr, other]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PresetCategory::Finance
                }
            },
            tags: val.tags.into_iter().collect(),
            schema: val.schema,
            system_prompt: val.system_prompt,
            context_template: val.context_template,
            merge_mode: match val.merge_mode.as_str() {
                "object_merge" => xberg::MergeMode::ObjectMerge,
                "array_concat" => xberg::MergeMode::ArrayConcat,
                "object_first" => xberg::MergeMode::ObjectFirst,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'merge_mode' of MergeMode: expected one of [object_merge, array_concat, object_first]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            preferred_call_mode: match val.preferred_call_mode.as_str() {
                "text_only" => xberg::CallMode::TextOnly,
                "vision_only" => xberg::CallMode::VisionOnly,
                "text_plus_vision" => xberg::CallMode::TextPlusVision,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'preferred_call_mode' of CallMode: expected one of [text_only, vision_only, text_plus_vision]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            emit_citations: val.emit_citations,
            sample: val.sample.map(Into::into),
            fingerprint: val.fingerprint,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Preset> for Preset {
    fn from(val: xberg::Preset) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            schema_name: val.schema_name.to_string(),
            description: val.description.to_string(),
            category: serde_json::to_value(val.category)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            tags: val.tags.into_iter().collect(),
            schema: val.schema,
            system_prompt: val.system_prompt.to_string(),
            context_template: val.context_template.map(|v| v.to_string()),
            merge_mode: serde_json::to_value(val.merge_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            preferred_call_mode: serde_json::to_value(val.preferred_call_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            emit_citations: val.emit_citations,
            sample: val.sample.map(Into::into),
            fingerprint: val.fingerprint.to_string(),
        }
    }
}

impl From<PresetSample> for xberg::PresetSample {
    fn from(val: PresetSample) -> Self {
        Self {
            input_path: val.input_path,
            output_path: val.output_path,
        }
    }
}

impl From<xberg::PresetSample> for PresetSample {
    fn from(val: xberg::PresetSample) -> Self {
        Self {
            input_path: val.input_path.to_string(),
            output_path: val.output_path.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PresetSummary> for xberg::PresetSummary {
    fn from(val: PresetSummary) -> Self {
        Self {
            id: val.id,
            version: val.version,
            schema_name: val.schema_name,
            description: val.description,
            category: match val.category.as_str() {
                "finance" => xberg::PresetCategory::Finance,
                "identity" => xberg::PresetCategory::Identity,
                "legal" => xberg::PresetCategory::Legal,
                "logistics" => xberg::PresetCategory::Logistics,
                "medical" => xberg::PresetCategory::Medical,
                "hr" => xberg::PresetCategory::Hr,
                "other" => xberg::PresetCategory::Other,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'category' of PresetCategory: expected one of [finance, identity, legal, logistics, medical, hr, other]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PresetCategory::Finance
                }
            },
            tags: val.tags.into_iter().collect(),
            preferred_call_mode: match val.preferred_call_mode.as_str() {
                "text_only" => xberg::CallMode::TextOnly,
                "vision_only" => xberg::CallMode::VisionOnly,
                "text_plus_vision" => xberg::CallMode::TextPlusVision,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'preferred_call_mode' of CallMode: expected one of [text_only, vision_only, text_plus_vision]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            emit_citations: val.emit_citations,
            fingerprint: val.fingerprint,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PresetSummary> for PresetSummary {
    fn from(val: xberg::PresetSummary) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            schema_name: val.schema_name.to_string(),
            description: val.description.to_string(),
            category: serde_json::to_value(val.category)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            tags: val.tags.into_iter().collect(),
            preferred_call_mode: serde_json::to_value(val.preferred_call_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            emit_citations: val.emit_citations,
            fingerprint: val.fingerprint.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ProcessingWarning> for xberg::ProcessingWarning {
    fn from(val: ProcessingWarning) -> Self {
        Self {
            source: val.source.into(),
            message: val.message.into(),
        }
    }
}

impl From<xberg::ProcessingWarning> for ProcessingWarning {
    fn from(val: xberg::ProcessingWarning) -> Self {
        Self {
            source: val.source.to_string(),
            message: val.message.to_string(),
        }
    }
}

impl From<PropertyChange> for xberg::PropertyChange {
    fn from(val: PropertyChange) -> Self {
        Self {
            name: val.name,
            from: val.from,
            to: val.to,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PropertyChange> for PropertyChange {
    fn from(val: xberg::PropertyChange) -> Self {
        Self {
            name: val.name.to_string(),
            from: val.from.map(|v| v.to_string()),
            to: val.to.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ProxyConfig> for crawlberg::ProxyConfig {
    fn from(val: ProxyConfig) -> Self {
        Self {
            url: val.url,
            username: val.username,
            password: val.password,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::ProxyConfig> for ProxyConfig {
    fn from(val: crawlberg::ProxyConfig) -> Self {
        Self {
            url: val.url.to_string(),
            username: val.username.map(|v| v.to_string()),
            password: val.password.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PstMetadata> for xberg::PstMetadata {
    fn from(val: PstMetadata) -> Self {
        Self {
            message_count: val.message_count as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::PstMetadata> for PstMetadata {
    fn from(val: xberg::PstMetadata) -> Self {
        Self {
            message_count: val.message_count as i64,
        }
    }
}

impl From<QrBoundingBox> for xberg::QrBoundingBox {
    fn from(val: QrBoundingBox) -> Self {
        Self {
            x: val.x,
            y: val.y,
            width: val.width,
            height: val.height,
        }
    }
}

impl From<xberg::QrBoundingBox> for QrBoundingBox {
    fn from(val: xberg::QrBoundingBox) -> Self {
        Self {
            x: val.x,
            y: val.y,
            width: val.width,
            height: val.height,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<QrCode> for xberg::QrCode {
    fn from(val: QrCode) -> Self {
        Self {
            payload: val.payload,
            confidence: val.confidence,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::QrCode> for QrCode {
    fn from(val: xberg::QrCode) -> Self {
        Self {
            payload: val.payload.to_string(),
            confidence: val.confidence,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<RakeParams> for xberg::RakeParams {
    fn from(val: RakeParams) -> Self {
        Self {
            min_word_length: val.min_word_length as usize,
            max_words_per_phrase: val.max_words_per_phrase as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::RakeParams> for RakeParams {
    fn from(val: xberg::RakeParams) -> Self {
        Self {
            min_word_length: val.min_word_length as i64,
            max_words_per_phrase: val.max_words_per_phrase as i64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RecognizedTable> for RecognizedTable {
    fn from(val: xberg::RecognizedTable) -> Self {
        Self {
            detection_bbox: val.detection_bbox.into(),
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<RedactionConfig> for xberg::RedactionConfig {
    fn from(val: RedactionConfig) -> Self {
        Self {
            categories: val.categories.into_iter().map(|s| match s.as_str() { "email" => xberg::PiiCategory::Email,"phone" => xberg::PiiCategory::Phone,"ssn" => xberg::PiiCategory::Ssn,"credit_card" => xberg::PiiCategory::CreditCard,"postal_code" => xberg::PiiCategory::PostalCode,"ip_address" => xberg::PiiCategory::IpAddress,"iban" => xberg::PiiCategory::Iban,"swift_bic" => xberg::PiiCategory::SwiftBic,"date_of_birth" => xberg::PiiCategory::DateOfBirth,"person" => xberg::PiiCategory::Person,"organization" => xberg::PiiCategory::Organization,"location" => xberg::PiiCategory::Location,"custom" => xberg::PiiCategory::Custom(Default::default()),other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'categories' of PiiCategory: expected one of [email, phone, ssn, credit_card, postal_code, ip_address, iban, swift_bic, date_of_birth, person, organization, location, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    xberg::PiiCategory::Email
}
 }
).collect(),
            strategy: match val.strategy.as_str() { "mask" => xberg::RedactionStrategy::Mask,"hash" => xberg::RedactionStrategy::Hash,"token_replace" => xberg::RedactionStrategy::TokenReplace,"drop" => xberg::RedactionStrategy::Drop,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'strategy' of RedactionStrategy: expected one of [mask, hash, token_replace, drop]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 }
,
            ner: val.ner.map(Into::into),
            preserve_offsets: val.preserve_offsets,
            custom_terms: val.custom_terms.into_iter().map(Into::into).collect(),
            custom_patterns: val.custom_patterns.into_iter().map(Into::into).collect(),
            ..Default::default()        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::RedactionConfig> for RedactionConfig {
    fn from(val: xberg::RedactionConfig) -> Self {
        Self {
            categories: val
                .categories
                .iter()
                .map(|v| {
                    serde_json::to_value(v)
                        .ok()
                        .and_then(|s| s.as_str().map(String::from))
                        .unwrap_or_default()
                })
                .collect(),
            strategy: serde_json::to_value(val.strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            ner: val.ner.map(Into::into),
            preserve_offsets: val.preserve_offsets,
            custom_terms: val.custom_terms.into_iter().map(Into::into).collect(),
            custom_patterns: val.custom_patterns.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<RedactionFinding> for xberg::RedactionFinding {
    fn from(val: RedactionFinding) -> Self {
        Self {
            start: val.start,
            end: val.end,
            category: match val.category.as_str() {
                "email" => xberg::PiiCategory::Email,
                "phone" => xberg::PiiCategory::Phone,
                "ssn" => xberg::PiiCategory::Ssn,
                "credit_card" => xberg::PiiCategory::CreditCard,
                "postal_code" => xberg::PiiCategory::PostalCode,
                "ip_address" => xberg::PiiCategory::IpAddress,
                "iban" => xberg::PiiCategory::Iban,
                "swift_bic" => xberg::PiiCategory::SwiftBic,
                "date_of_birth" => xberg::PiiCategory::DateOfBirth,
                "person" => xberg::PiiCategory::Person,
                "organization" => xberg::PiiCategory::Organization,
                "location" => xberg::PiiCategory::Location,
                "custom" => xberg::PiiCategory::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'category' of PiiCategory: expected one of [email, phone, ssn, credit_card, postal_code, ip_address, iban, swift_bic, date_of_birth, person, organization, location, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PiiCategory::Email
                }
            },
            strategy: match val.strategy.as_str() {
                "mask" => xberg::RedactionStrategy::Mask,
                "hash" => xberg::RedactionStrategy::Hash,
                "token_replace" => xberg::RedactionStrategy::TokenReplace,
                "drop" => xberg::RedactionStrategy::Drop,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'strategy' of RedactionStrategy: expected one of [mask, hash, token_replace, drop]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            replacement_token: val.replacement_token,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::RedactionFinding> for RedactionFinding {
    fn from(val: xberg::RedactionFinding) -> Self {
        Self {
            start: val.start,
            end: val.end,
            category: serde_json::to_value(val.category)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            strategy: serde_json::to_value(val.strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            replacement_token: val.replacement_token.to_string(),
        }
    }
}

impl From<RedactionPattern> for xberg::RedactionPattern {
    fn from(val: RedactionPattern) -> Self {
        Self {
            label: val.label,
            pattern: val.pattern,
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RedactionPattern> for RedactionPattern {
    fn from(val: xberg::RedactionPattern) -> Self {
        Self {
            label: val.label.to_string(),
            pattern: val.pattern.to_string(),
            case_sensitive: val.case_sensitive,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<RedactionReport> for xberg::RedactionReport {
    fn from(val: RedactionReport) -> Self {
        Self {
            findings: val.findings.into_iter().map(Into::into).collect(),
            total_redacted: val.total_redacted,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RedactionReport> for RedactionReport {
    fn from(val: xberg::RedactionReport) -> Self {
        Self {
            findings: val.findings.into_iter().map(Into::into).collect(),
            total_redacted: val.total_redacted,
        }
    }
}

impl From<RedactionTerm> for xberg::RedactionTerm {
    fn from(val: RedactionTerm) -> Self {
        Self {
            label: val.label,
            value: val.value,
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RedactionTerm> for RedactionTerm {
    fn from(val: xberg::RedactionTerm) -> Self {
        Self {
            label: val.label.to_string(),
            value: val.value.to_string(),
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RerankedDocument> for RerankedDocument {
    fn from(val: xberg::RerankedDocument) -> Self {
        Self {
            index: val.index as i64,
            score: val.score,
            document: val.document.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<RerankerConfig> for xberg::RerankerConfig {
    fn from(val: RerankerConfig) -> Self {
        Self {
            model: val.model.into(),
            top_k: val.top_k.map(|v| v as usize),
            batch_size: val.batch_size as usize,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(Into::into),
            acceleration: val.acceleration.map(Into::into),
            max_rerank_duration_secs: val.max_rerank_duration_secs.map(|v| v as u64),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::RerankerConfig> for RerankerConfig {
    fn from(val: xberg::RerankerConfig) -> Self {
        Self {
            model: val.model.into(),
            top_k: val.top_k.map(|v| v as i64),
            batch_size: val.batch_size as i64,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_rerank_duration_secs: val.max_rerank_duration_secs.map(|v| v as i64),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ResolvedPreset> for ResolvedPreset {
    fn from(val: xberg::ResolvedPreset) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            fingerprint: val.fingerprint.to_string(),
            schema_name: val.schema_name.to_string(),
            schema: val.schema,
            system_prompt: val.system_prompt.to_string(),
            merge_mode: serde_json::to_value(val.merge_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            preferred_call_mode: serde_json::to_value(val.preferred_call_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            emit_citations: val.emit_citations,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<RevisionDelta> for xberg::RevisionDelta {
    fn from(val: RevisionDelta) -> Self {
        Self {
            content: val.content.into_iter().map(Into::into).collect(),
            table_changes: val.table_changes.into_iter().map(Into::into).collect(),
            property_changes: val.property_changes.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RevisionDelta> for RevisionDelta {
    fn from(val: xberg::RevisionDelta) -> Self {
        Self {
            content: val.content.into_iter().map(Into::into).collect(),
            table_changes: val.table_changes.into_iter().map(Into::into).collect(),
            property_changes: val.property_changes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure)]
impl From<SecurityLimits> for xberg::SecurityLimits {
    fn from(val: SecurityLimits) -> Self {
        Self {
            max_archive_size: val.max_archive_size as usize,
            max_compression_ratio: val.max_compression_ratio as usize,
            max_files_in_archive: val.max_files_in_archive as usize,
            max_nesting_depth: val.max_nesting_depth as usize,
            max_entity_length: val.max_entity_length as usize,
            max_content_size: val.max_content_size as usize,
            max_iterations: val.max_iterations as usize,
            max_xml_depth: val.max_xml_depth as usize,
            max_table_cells: val.max_table_cells as usize,
            max_pages: val.max_pages.map(|v| v as usize),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::SecurityLimits> for SecurityLimits {
    fn from(val: xberg::SecurityLimits) -> Self {
        Self {
            max_archive_size: val.max_archive_size as i64,
            max_compression_ratio: val.max_compression_ratio as i64,
            max_files_in_archive: val.max_files_in_archive as i64,
            max_nesting_depth: val.max_nesting_depth as i64,
            max_entity_length: val.max_entity_length as i64,
            max_content_size: val.max_content_size as i64,
            max_iterations: val.max_iterations as i64,
            max_xml_depth: val.max_xml_depth as i64,
            max_table_cells: val.max_table_cells as i64,
            max_pages: val.max_pages.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ServerConfig> for xberg::ServerConfig {
    fn from(val: ServerConfig) -> Self {
        Self {
            host: val.host,
            port: val.port,
            cors_origins: val.cors_origins.into_iter().collect(),
            max_request_body_bytes: val.max_request_body_bytes as usize,
            max_multipart_field_bytes: val.max_multipart_field_bytes as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::ServerConfig> for ServerConfig {
    fn from(val: xberg::ServerConfig) -> Self {
        Self {
            host: val.host.to_string(),
            port: val.port,
            cors_origins: val.cors_origins.into_iter().collect(),
            max_request_body_bytes: val.max_request_body_bytes as i64,
            max_multipart_field_bytes: val.max_multipart_field_bytes as i64,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<SitemapUrl> for crawlberg::SitemapUrl {
    fn from(val: SitemapUrl) -> Self {
        Self {
            url: val.url,
            lastmod: val.lastmod,
            changefreq: val.changefreq,
            priority: val.priority,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::SitemapUrl> for SitemapUrl {
    fn from(val: crawlberg::SitemapUrl) -> Self {
        Self {
            url: val.url.to_string(),
            lastmod: val.lastmod.map(|v| v.to_string()),
            changefreq: val.changefreq.map(|v| v.to_string()),
            priority: val.priority.map(|v| v.to_string()),
        }
    }
}

impl From<SparseEmbedding> for xberg::SparseEmbedding {
    fn from(val: SparseEmbedding) -> Self {
        Self {
            indices: val.indices.into_iter().collect(),
            values: val.values.into_iter().collect(),
        }
    }
}

impl From<xberg::SparseEmbedding> for SparseEmbedding {
    fn from(val: xberg::SparseEmbedding) -> Self {
        Self {
            indices: val.indices.into_iter().collect(),
            values: val.values.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<SparseEmbeddingConfig> for xberg::SparseEmbeddingConfig {
    fn from(val: SparseEmbeddingConfig) -> Self {
        Self {
            model: val.model.into(),
            batch_size: val.batch_size as usize,
            max_length: val.max_length as usize,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(Into::into),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as u64),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::SparseEmbeddingConfig> for SparseEmbeddingConfig {
    fn from(val: xberg::SparseEmbeddingConfig) -> Self {
        Self {
            model: val.model.into(),
            batch_size: val.batch_size as i64,
            max_length: val.max_length as i64,
            show_download_progress: val.show_download_progress,
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
        }
    }
}

impl From<xberg::SparseEmbeddingPreset> for SparseEmbeddingPreset {
    fn from(val: xberg::SparseEmbeddingPreset) -> Self {
        Self {
            name: val.name.to_string(),
            model_repo: val.model_repo.to_string(),
            model_file: val.model_file.to_string(),
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as i64,
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<SsrfPolicy> for crawlberg::SsrfPolicy {
    fn from(val: SsrfPolicy) -> Self {
        Self {
            deny_private: val.deny_private,
            allowlist: val.allowlist.into_iter().map(Into::into).collect(),
            max_redirects: val.max_redirects,
            scheme_allowlist: val.scheme_allowlist.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<crawlberg::SsrfPolicy> for SsrfPolicy {
    fn from(val: crawlberg::SsrfPolicy) -> Self {
        Self {
            deny_private: val.deny_private,
            allowlist: val.allowlist.into_iter().map(Into::into).collect(),
            max_redirects: val.max_redirects,
            scheme_allowlist: val.scheme_allowlist.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<StructuredData> for xberg::StructuredData {
    fn from(val: StructuredData) -> Self {
        Self {
            data_type: match val.data_type.as_str() {
                "json-ld" => xberg::StructuredDataType::JsonLd,
                "microdata" => xberg::StructuredDataType::Microdata,
                "rdfa" => xberg::StructuredDataType::RDFa,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'data_type' of StructuredDataType: expected one of [json-ld, microdata, rdfa]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::StructuredDataType::JsonLd
                }
            },
            raw_json: val.raw_json,
            schema_type: val.schema_type,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::StructuredData> for StructuredData {
    fn from(val: xberg::StructuredData) -> Self {
        Self {
            data_type: serde_json::to_value(val.data_type)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            raw_json: val.raw_json.to_string(),
            schema_type: val.schema_type.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::extraction::structured::StructuredDataResult> for StructuredDataResult {
    fn from(val: xberg::extraction::structured::StructuredDataResult) -> Self {
        Self {
            content: val.content.to_string(),
            format: val.format.to_string(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            text_fields: val.text_fields.into_iter().collect(),
            value: val.value,
            flattened: val.flattened.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<StructuredExtractionConfig> for xberg::StructuredExtractionConfig {
    fn from(val: StructuredExtractionConfig) -> Self {
        Self {
            schema: val.schema,
            schema_name: val.schema_name,
            schema_description: val.schema_description,
            strict: val.strict,
            prompt: val.prompt,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::StructuredExtractionConfig> for StructuredExtractionConfig {
    fn from(val: xberg::StructuredExtractionConfig) -> Self {
        Self {
            schema: val.schema,
            schema_name: val.schema_name.to_string(),
            schema_description: val.schema_description.map(|v| v.to_string()),
            strict: val.strict,
            prompt: val.prompt.map(|v| v.to_string()),
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<SummarizationConfig> for xberg::SummarizationConfig {
    fn from(val: SummarizationConfig) -> Self {
        Self {
            strategy: match val.strategy.as_str() {
                "extractive" => xberg::SummaryStrategy::Extractive,
                "abstractive" => xberg::SummaryStrategy::Abstractive,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'strategy' of SummaryStrategy: expected one of [extractive, abstractive]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            max_tokens: val.max_tokens,
            llm: val.llm.map(Into::into),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::SummarizationConfig> for SummarizationConfig {
    fn from(val: xberg::SummarizationConfig) -> Self {
        Self {
            strategy: serde_json::to_value(val.strategy)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            max_tokens: val.max_tokens,
            llm: val.llm.map(Into::into),
        }
    }
}

impl From<SupportedFormat> for xberg::SupportedFormat {
    fn from(val: SupportedFormat) -> Self {
        Self {
            extension: val.extension,
            mime_type: val.mime_type,
        }
    }
}

impl From<xberg::SupportedFormat> for SupportedFormat {
    fn from(val: xberg::SupportedFormat) -> Self {
        Self {
            extension: val.extension.to_string(),
            mime_type: val.mime_type.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<SvgOptions> for xberg::core::config::extraction::SvgOptions {
    fn from(val: SvgOptions) -> Self {
        Self {
            sanitize: val.sanitize,
            render_dpi: val.render_dpi,
            ..Default::default()
        }
    }
}

impl From<xberg::core::config::extraction::SvgOptions> for SvgOptions {
    fn from(val: xberg::core::config::extraction::SvgOptions) -> Self {
        Self {
            sanitize: val.sanitize,
            render_dpi: val.render_dpi,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<Table> for xberg::Table {
    fn from(val: Table) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown,
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            table_id: val.table_id,
            cell_styles: val.cell_styles.into_iter().map(Into::into).collect(),
            columns: val.columns.map(|v| v.into_iter().collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Table> for Table {
    fn from(val: xberg::Table) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown.to_string(),
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            table_id: val.table_id.map(|v| v.to_string()),
            cell_styles: val.cell_styles.into_iter().map(Into::into).collect(),
            columns: val.columns.map(|v| v.into_iter().collect()),
        }
    }
}

impl From<xberg::TableCell> for TableCell {
    fn from(val: xberg::TableCell) -> Self {
        Self {
            content: val.content.to_string(),
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<TableCellStyle> for xberg::TableCellStyle {
    fn from(val: TableCellStyle) -> Self {
        Self {
            row: val.row,
            col: val.col,
            heading_level: val.heading_level,
            style_name: val.style_name,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::TableCellStyle> for TableCellStyle {
    fn from(val: xberg::TableCellStyle) -> Self {
        Self {
            row: val.row,
            col: val.col,
            heading_level: val.heading_level,
            style_name: val.style_name.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TableDiff> for TableDiff {
    fn from(val: xberg::TableDiff) -> Self {
        Self {
            from_index: val.from_index as i64,
            to_index: val.to_index as i64,
            cell_changes: val.cell_changes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TableGrid> for xberg::TableGrid {
    fn from(val: TableGrid) -> Self {
        Self {
            rows: val.rows,
            cols: val.cols,
            cells: val.cells.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TableGrid> for TableGrid {
    fn from(val: xberg::TableGrid) -> Self {
        Self {
            rows: val.rows,
            cols: val.cols,
            cells: val.cells.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TesseractConfig> for xberg::TesseractConfig {
    fn from(val: TesseractConfig) -> Self {
        Self {
            language: val.language.into_iter().collect(),
            psm: val.psm,
            output_format: val.output_format,
            oem: val.oem,
            min_confidence: val.min_confidence,
            preprocessing: val.preprocessing.map(Into::into),
            enable_table_detection: val.enable_table_detection,
            table_min_confidence: val.table_min_confidence,
            table_column_threshold: val.table_column_threshold,
            table_row_threshold_ratio: val.table_row_threshold_ratio,
            use_cache: val.use_cache,
            classify_use_pre_adapted_templates: val.classify_use_pre_adapted_templates,
            language_model_ngram_on: val.language_model_ngram_on,
            tessedit_dont_blkrej_good_wds: val.tessedit_dont_blkrej_good_wds,
            tessedit_dont_rowrej_good_wds: val.tessedit_dont_rowrej_good_wds,
            tessedit_enable_dict_correction: val.tessedit_enable_dict_correction,
            tessedit_char_whitelist: val.tessedit_char_whitelist,
            tessedit_char_blacklist: val.tessedit_char_blacklist,
            tessedit_use_primary_params_model: val.tessedit_use_primary_params_model,
            textord_space_size_is_variable: val.textord_space_size_is_variable,
            thresholding_method: val.thresholding_method,
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TesseractConfig> for TesseractConfig {
    fn from(val: xberg::TesseractConfig) -> Self {
        Self {
            language: val.language.into_iter().collect(),
            psm: val.psm,
            output_format: val.output_format.to_string(),
            oem: val.oem,
            min_confidence: val.min_confidence,
            preprocessing: val.preprocessing.map(Into::into),
            enable_table_detection: val.enable_table_detection,
            table_min_confidence: val.table_min_confidence,
            table_column_threshold: val.table_column_threshold,
            table_row_threshold_ratio: val.table_row_threshold_ratio,
            use_cache: val.use_cache,
            classify_use_pre_adapted_templates: val.classify_use_pre_adapted_templates,
            language_model_ngram_on: val.language_model_ngram_on,
            tessedit_dont_blkrej_good_wds: val.tessedit_dont_blkrej_good_wds,
            tessedit_dont_rowrej_good_wds: val.tessedit_dont_rowrej_good_wds,
            tessedit_enable_dict_correction: val.tessedit_enable_dict_correction,
            tessedit_char_whitelist: val.tessedit_char_whitelist.to_string(),
            tessedit_char_blacklist: val.tessedit_char_blacklist.to_string(),
            tessedit_use_primary_params_model: val.tessedit_use_primary_params_model,
            textord_space_size_is_variable: val.textord_space_size_is_variable,
            thresholding_method: val.thresholding_method,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<TextAnnotation> for xberg::TextAnnotation {
    fn from(val: TextAnnotation) -> Self {
        Self {
            start: val.start,
            end: val.end,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TextAnnotation> for TextAnnotation {
    fn from(val: xberg::TextAnnotation) -> Self {
        Self {
            start: val.start,
            end: val.end,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TextExtractionResult> for TextExtractionResult {
    fn from(val: xberg::TextExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            line_count: val.line_count as i64,
            word_count: val.word_count as i64,
            character_count: val.character_count as i64,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<TextMetadata> for xberg::TextMetadata {
    fn from(val: TextMetadata) -> Self {
        Self {
            line_count: val.line_count,
            word_count: val.word_count,
            character_count: val.character_count,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TextMetadata> for TextMetadata {
    fn from(val: xberg::TextMetadata) -> Self {
        Self {
            line_count: val.line_count,
            word_count: val.word_count,
            character_count: val.character_count,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TokenReductionConfig> for xberg::TokenReductionConfig {
    fn from(val: TokenReductionConfig) -> Self {
        Self {
            level: match val.level.as_str() {
                "Off" | "off" => xberg::ReductionLevel::Off,
                "Light" | "light" => xberg::ReductionLevel::Light,
                "Moderate" | "moderate" => xberg::ReductionLevel::Moderate,
                "Aggressive" | "aggressive" => xberg::ReductionLevel::Aggressive,
                "Maximum" | "maximum" => xberg::ReductionLevel::Maximum,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'level' of ReductionLevel: expected one of [Off, Light, Moderate, Aggressive, Maximum]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            language_hint: val.language_hint,
            preserve_markdown: val.preserve_markdown,
            preserve_code: val.preserve_code,
            semantic_threshold: val.semantic_threshold,
            enable_parallel: val.enable_parallel,
            use_simd: val.use_simd,
            custom_stopwords: val.custom_stopwords.map(|m| m.into_iter().collect()),
            preserve_patterns: val.preserve_patterns.into_iter().collect(),
            target_reduction: val.target_reduction,
            enable_semantic_clustering: val.enable_semantic_clustering,
            preserve_important_words: val.preserve_important_words,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::TokenReductionConfig> for TokenReductionConfig {
    fn from(val: xberg::TokenReductionConfig) -> Self {
        Self {
            level: serde_json::to_value(val.level)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            language_hint: val.language_hint.map(|v| v.to_string()),
            preserve_markdown: val.preserve_markdown,
            preserve_code: val.preserve_code,
            semantic_threshold: val.semantic_threshold,
            enable_parallel: val.enable_parallel,
            use_simd: val.use_simd,
            custom_stopwords: val.custom_stopwords.map(|m| m.into_iter().collect()),
            preserve_patterns: val.preserve_patterns.into_iter().collect(),
            target_reduction: val.target_reduction,
            enable_semantic_clustering: val.enable_semantic_clustering,
            preserve_important_words: val.preserve_important_words,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<TokenReductionOptions> for xberg::TokenReductionOptions {
    fn from(val: TokenReductionOptions) -> Self {
        Self {
            mode: val.mode,
            preserve_important_words: val.preserve_important_words,
            ..Default::default()
        }
    }
}

impl From<xberg::TokenReductionOptions> for TokenReductionOptions {
    fn from(val: xberg::TokenReductionOptions) -> Self {
        Self {
            mode: val.mode.to_string(),
            preserve_important_words: val.preserve_important_words,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TranscriptionConfig> for xberg::TranscriptionConfig {
    fn from(val: TranscriptionConfig) -> Self {
        Self {
            enabled: val.enabled,
            model: match val.model.as_str() {
                "tiny" => xberg::WhisperModel::Tiny,
                "base" => xberg::WhisperModel::Base,
                "small" => xberg::WhisperModel::Small,
                "medium" => xberg::WhisperModel::Medium,
                "large_v3" => xberg::WhisperModel::LargeV3,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'model' of WhisperModel: expected one of [tiny, base, small, medium, large_v3]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            language: val.language,
            timestamps: val.timestamps,
            max_duration_ms: val.max_duration_ms.map(|v| v as u64),
            max_bytes: val.max_bytes.map(|v| v as u64),
            timeout_ms: val.timeout_ms.map(|v| v as u64),
            model_cache_dir: val.model_cache_dir.map(Into::into),
            allow_network: val.allow_network,
            verify_hash: val.verify_hash,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::TranscriptionConfig> for TranscriptionConfig {
    fn from(val: xberg::TranscriptionConfig) -> Self {
        Self {
            enabled: val.enabled,
            model: serde_json::to_value(val.model)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            language: val.language.map(|v| v.to_string()),
            timestamps: val.timestamps,
            max_duration_ms: val.max_duration_ms.map(|v| v as i64),
            max_bytes: val.max_bytes.map(|v| v as i64),
            timeout_ms: val.timeout_ms.map(|v| v as i64),
            model_cache_dir: val.model_cache_dir.map(|p| p.to_string_lossy().to_string()),
            allow_network: val.allow_network,
            verify_hash: val.verify_hash,
        }
    }
}

impl From<Translation> for xberg::Translation {
    fn from(val: Translation) -> Self {
        Self {
            target_lang: val.target_lang,
            source_lang: val.source_lang,
            content: val.content,
            formatted_content: val.formatted_content,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Translation> for Translation {
    fn from(val: xberg::Translation) -> Self {
        Self {
            target_lang: val.target_lang.to_string(),
            source_lang: val.source_lang.map(|v| v.to_string()),
            content: val.content.to_string(),
            formatted_content: val.formatted_content.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<TranslationConfig> for xberg::TranslationConfig {
    fn from(val: TranslationConfig) -> Self {
        Self {
            target_lang: val.target_lang,
            source_lang: val.source_lang,
            preserve_markup: val.preserve_markup,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TranslationConfig> for TranslationConfig {
    fn from(val: xberg::TranslationConfig) -> Self {
        Self {
            target_lang: val.target_lang.to_string(),
            source_lang: val.source_lang.map(|v| v.to_string()),
            preserve_markup: val.preserve_markup,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TreeSitterConfig> for xberg::TreeSitterConfig {
    fn from(val: TreeSitterConfig) -> Self {
        Self {
            enabled: val.enabled,
            cache_dir: val.cache_dir.map(Into::into),
            languages: val.languages.map(|v| v.into_iter().collect()),
            groups: val.groups.map(|v| v.into_iter().collect()),
            process: val.process.into(),
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TreeSitterConfig> for TreeSitterConfig {
    fn from(val: xberg::TreeSitterConfig) -> Self {
        Self {
            enabled: val.enabled,
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            languages: val.languages.map(|v| v.into_iter().collect()),
            groups: val.groups.map(|v| v.into_iter().collect()),
            process: val.process.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TreeSitterProcessConfig> for xberg::TreeSitterProcessConfig {
    fn from(val: TreeSitterProcessConfig) -> Self {
        Self {
            structure: val.structure,
            imports: val.imports,
            exports: val.exports,
            comments: val.comments,
            docstrings: val.docstrings,
            symbols: val.symbols,
            diagnostics: val.diagnostics,
            data_extraction: val.data_extraction,
            chunk_max_size: val.chunk_max_size.map(|v| v as usize),
            content_mode: match val.content_mode.as_str() {
                "chunks" => xberg::CodeContentMode::Chunks,
                "raw" => xberg::CodeContentMode::Raw,
                "structure" => xberg::CodeContentMode::Structure,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'content_mode' of CodeContentMode: expected one of [chunks, raw, structure]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::TreeSitterProcessConfig> for TreeSitterProcessConfig {
    fn from(val: xberg::TreeSitterProcessConfig) -> Self {
        Self {
            structure: val.structure,
            imports: val.imports,
            exports: val.exports,
            comments: val.comments,
            docstrings: val.docstrings,
            symbols: val.symbols,
            diagnostics: val.diagnostics,
            data_extraction: val.data_extraction,
            chunk_max_size: val.chunk_max_size.map(|v| v as i64),
            content_mode: serde_json::to_value(val.content_mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::candle_ocr::TrocrBackendOptions> for TrocrBackendOptions {
    fn from(val: xberg::candle_ocr::TrocrBackendOptions) -> Self {
        Self {
            variant: val.variant.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            device: val.device.as_ref().map(|v| {
                serde_json::to_value(v)
                    .ok()
                    .and_then(|s| s.as_str().map(String::from))
                    .unwrap_or_default()
            }),
            cache_dir: val.cache_dir.map(|v| v.to_string()),
            hf_revision: val.hf_revision.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<UrlExtractionConfig> for xberg::UrlExtractionConfig {
    fn from(val: UrlExtractionConfig) -> Self {
        Self {
            mode: match val.mode.as_str() {
                "auto" => xberg::UrlExtractionMode::Auto,
                "document" => xberg::UrlExtractionMode::Document,
                "crawl" => xberg::UrlExtractionMode::Crawl,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'mode' of UrlExtractionMode: expected one of [auto, document, crawl]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            crawl: val.crawl.into(),
            document_url_pattern: val.document_url_pattern,
            max_document_urls_per_result: val.max_document_urls_per_result,
            max_total_urls: val.max_total_urls,
            allow_local_file_inputs: val.allow_local_file_inputs,
            allow_file_uris: val.allow_file_uris,
            ..Default::default()
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::UrlExtractionConfig> for UrlExtractionConfig {
    fn from(val: xberg::UrlExtractionConfig) -> Self {
        Self {
            mode: serde_json::to_value(val.mode)
                .ok()
                .and_then(|s| s.as_str().map(String::from))
                .unwrap_or_default(),
            #[cfg(feature = "url-config-types")]
            crawl: val.crawl.into(),
            document_url_pattern: val.document_url_pattern.map(|v| v.to_string()),
            max_document_urls_per_result: val.max_document_urls_per_result,
            max_total_urls: val.max_total_urls,
            allow_local_file_inputs: val.allow_local_file_inputs,
            allow_file_uris: val.allow_file_uris,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::UserChunkConfig> for UserChunkConfig {
    fn from(val: xberg::UserChunkConfig) -> Self {
        Self {
            page_ranges: val.page_ranges.map(|v| v.into_iter().map(Into::into).collect()),
            pages_per_chunk: val.pages_per_chunk,
            force_chunking: val.force_chunking,
            disable_chunking: val.disable_chunking,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::extraction::office_metadata::app_properties::XlsxAppProperties> for XlsxAppProperties {
    fn from(val: xberg::extraction::office_metadata::app_properties::XlsxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            company: val.company.map(|v| v.to_string()),
            worksheet_names: val.worksheet_names.into_iter().collect(),
        }
    }
}

impl From<xberg::XmlExtractionResult> for XmlExtractionResult {
    fn from(val: xberg::XmlExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            element_count: val.element_count as i64,
            unique_elements: val.unique_elements.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<XmlMetadata> for xberg::XmlMetadata {
    fn from(val: XmlMetadata) -> Self {
        Self {
            element_count: val.element_count,
            unique_elements: val.unique_elements.into_iter().collect(),
            ..Default::default()
        }
    }
}

impl From<xberg::XmlMetadata> for XmlMetadata {
    fn from(val: xberg::XmlMetadata) -> Self {
        Self {
            element_count: val.element_count,
            unique_elements: val.unique_elements.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<YakeParams> for xberg::YakeParams {
    fn from(val: YakeParams) -> Self {
        Self {
            window_size: val.window_size as usize,
            ..Default::default()
        }
    }
}

impl From<xberg::YakeParams> for YakeParams {
    fn from(val: xberg::YakeParams) -> Self {
        Self {
            window_size: val.window_size as i64,
        }
    }
}

impl From<YearRange> for xberg::YearRange {
    fn from(val: YearRange) -> Self {
        Self {
            min: val.min,
            max: val.max,
            years: val.years.into_iter().collect(),
        }
    }
}

impl From<xberg::YearRange> for YearRange {
    fn from(val: xberg::YearRange) -> Self {
        Self {
            min: val.min,
            max: val.max,
            years: val.years.into_iter().collect(),
        }
    }
}

impl From<xberg::AnnotationKind> for AnnotationKind {
    fn from(val: xberg::AnnotationKind) -> Self {
        match val {
            xberg::AnnotationKind::Bold => Self {
                annotation_type_tag: "bold".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Italic => Self {
                annotation_type_tag: "italic".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Underline => Self {
                annotation_type_tag: "underline".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Strikethrough => Self {
                annotation_type_tag: "strikethrough".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Code => Self {
                annotation_type_tag: "code".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Subscript => Self {
                annotation_type_tag: "subscript".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Superscript => Self {
                annotation_type_tag: "superscript".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Link { url, title } => Self {
                annotation_type_tag: "link".to_string(),
                url: Some(url),
                title: title,
                ..Default::default()
            },
            xberg::AnnotationKind::Highlight => Self {
                annotation_type_tag: "highlight".to_string(),
                ..Default::default()
            },
            xberg::AnnotationKind::Color { value } => Self {
                annotation_type_tag: "color".to_string(),
                value: Some(value),
                ..Default::default()
            },
            xberg::AnnotationKind::FontSize { value } => Self {
                annotation_type_tag: "font_size".to_string(),
                value: Some(value),
                ..Default::default()
            },
            xberg::AnnotationKind::Custom { name, value } => Self {
                annotation_type_tag: "custom".to_string(),
                name: Some(name),
                value: value,
                ..Default::default()
            },
        }
    }
}

impl From<AnnotationKind> for xberg::AnnotationKind {
    fn from(val: AnnotationKind) -> Self {
        match val.annotation_type_tag.as_str() {
            "bold" => xberg::AnnotationKind::Bold,
            "italic" => xberg::AnnotationKind::Italic,
            "underline" => xberg::AnnotationKind::Underline,
            "strikethrough" => xberg::AnnotationKind::Strikethrough,
            "code" => xberg::AnnotationKind::Code,
            "subscript" => xberg::AnnotationKind::Subscript,
            "superscript" => xberg::AnnotationKind::Superscript,
            "link" => xberg::AnnotationKind::Link {
                url: val.url.unwrap_or_default(),
                title: val.title,
            },
            "highlight" => xberg::AnnotationKind::Highlight,
            "color" => xberg::AnnotationKind::Color {
                value: val.value.unwrap_or_default(),
            },
            "font_size" => xberg::AnnotationKind::FontSize {
                value: val.value.unwrap_or_default(),
            },
            "custom" => xberg::AnnotationKind::Custom {
                name: val.name.unwrap_or_default(),
                value: val.value,
            },
            _ => xberg::AnnotationKind::default(),
        }
    }
}

impl From<crawlberg::AuthConfig> for AuthConfig {
    fn from(val: crawlberg::AuthConfig) -> Self {
        match val {
            crawlberg::AuthConfig::Basic { username, password } => Self {
                type_tag: "basic".to_string(),
                username: Some(username),
                password: Some(password),
                ..Default::default()
            },
            crawlberg::AuthConfig::Bearer { token } => Self {
                type_tag: "bearer".to_string(),
                token: Some(token),
                ..Default::default()
            },
            crawlberg::AuthConfig::Header { name, value } => Self {
                type_tag: "header".to_string(),
                name: Some(name),
                value: Some(value),
                ..Default::default()
            },
        }
    }
}

impl From<AuthConfig> for crawlberg::AuthConfig {
    fn from(val: AuthConfig) -> Self {
        match val.type_tag.as_str() {
            "basic" => crawlberg::AuthConfig::Basic {
                username: val.username.unwrap_or_default(),
                password: val.password.unwrap_or_default(),
            },
            "bearer" => crawlberg::AuthConfig::Bearer {
                token: val.token.unwrap_or_default(),
            },
            "header" => crawlberg::AuthConfig::Header {
                name: val.name.unwrap_or_default(),
                value: val.value.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::ChunkSizing> for ChunkSizing {
    fn from(val: xberg::ChunkSizing) -> Self {
        match val {
            xberg::ChunkSizing::Characters => Self {
                type_tag: "characters".to_string(),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "chunking-tokenizers"
            ))]
            xberg::ChunkSizing::Tokenizer { model, cache_dir } => Self {
                type_tag: "tokenizer".to_string(),
                model: Some(model),
                cache_dir: cache_dir.map(|p| p.to_string_lossy().into_owned()),
            },
        }
    }
}

impl From<ChunkSizing> for xberg::ChunkSizing {
    fn from(val: ChunkSizing) -> Self {
        match val.type_tag.as_str() {
            "characters" => xberg::ChunkSizing::Characters,
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "chunking-tokenizers"
            ))]
            "tokenizer" => xberg::ChunkSizing::Tokenizer {
                model: val.model.unwrap_or_default(),
                cache_dir: val.cache_dir.map(std::path::PathBuf::from),
            },
            _ => xberg::ChunkSizing::default(),
        }
    }
}

impl From<xberg::ChunkingReason> for ChunkingReason {
    fn from(val: xberg::ChunkingReason) -> Self {
        match val {
            xberg::ChunkingReason::LargeFile {
                size_bytes,
                threshold_bytes,
            } => Self {
                type_tag: "LargeFile".to_string(),
                size_bytes: Some(size_bytes as i64),
                threshold_bytes: Some(threshold_bytes as i64),
                ..Default::default()
            },
            xberg::ChunkingReason::ManyPages { page_count, threshold } => Self {
                type_tag: "ManyPages".to_string(),
                page_count: Some(page_count),
                threshold: Some(threshold),
                ..Default::default()
            },
            xberg::ChunkingReason::OcrRequired { page_count, force_ocr } => Self {
                type_tag: "OcrRequired".to_string(),
                page_count: Some(page_count),
                force_ocr: Some(force_ocr),
                ..Default::default()
            },
            xberg::ChunkingReason::LargeAndManyPages { size_bytes, page_count } => Self {
                type_tag: "LargeAndManyPages".to_string(),
                size_bytes: Some(size_bytes as i64),
                page_count: Some(page_count),
                ..Default::default()
            },
        }
    }
}

impl From<ChunkingReason> for xberg::ChunkingReason {
    fn from(val: ChunkingReason) -> Self {
        match val.type_tag.as_str() {
            "LargeFile" => xberg::ChunkingReason::LargeFile {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                threshold_bytes: val.threshold_bytes.map(|v| v as u64).unwrap_or_default(),
            },
            "ManyPages" => xberg::ChunkingReason::ManyPages {
                page_count: val.page_count.unwrap_or_default(),
                threshold: val.threshold.unwrap_or_default(),
            },
            "OcrRequired" => xberg::ChunkingReason::OcrRequired {
                page_count: val.page_count.unwrap_or_default(),
                force_ocr: val.force_ocr.unwrap_or_default(),
            },
            "LargeAndManyPages" => xberg::ChunkingReason::LargeAndManyPages {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                page_count: val.page_count.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::CredentialProviderConfig> for CredentialProviderConfig {
    fn from(val: xberg::CredentialProviderConfig) -> Self {
        match val {
            xberg::CredentialProviderConfig::AzureAd {
                tenant_id,
                client_id,
                client_secret,
                scope,
            } => Self {
                type_tag: "azure_ad".to_string(),
                tenant_id: Some(tenant_id),
                client_id: Some(client_id),
                client_secret: Some(client_secret),
                scope: scope,
                ..Default::default()
            },
            xberg::CredentialProviderConfig::VertexOauth2 {
                service_account_key_file,
                scope,
            } => Self {
                type_tag: "vertex_oauth2".to_string(),
                service_account_key_file: Some(service_account_key_file),
                scope: scope,
                ..Default::default()
            },
            xberg::CredentialProviderConfig::VertexAdc { scope } => Self {
                type_tag: "vertex_adc".to_string(),
                scope: scope,
                ..Default::default()
            },
            xberg::CredentialProviderConfig::BedrockWebIdentity {
                role_arn,
                token_file,
                session_name,
                region,
            } => Self {
                type_tag: "bedrock_web_identity".to_string(),
                role_arn: Some(role_arn),
                token_file: Some(token_file),
                session_name: session_name,
                region: region,
                ..Default::default()
            },
        }
    }
}

impl From<CredentialProviderConfig> for xberg::CredentialProviderConfig {
    fn from(val: CredentialProviderConfig) -> Self {
        match val.type_tag.as_str() {
            "azure_ad" => xberg::CredentialProviderConfig::AzureAd {
                tenant_id: val.tenant_id.unwrap_or_default(),
                client_id: val.client_id.unwrap_or_default(),
                client_secret: val.client_secret.unwrap_or_default(),
                scope: val.scope,
            },
            "vertex_oauth2" => xberg::CredentialProviderConfig::VertexOauth2 {
                service_account_key_file: val.service_account_key_file.unwrap_or_default(),
                scope: val.scope,
            },
            "vertex_adc" => xberg::CredentialProviderConfig::VertexAdc { scope: val.scope },
            "bedrock_web_identity" => xberg::CredentialProviderConfig::BedrockWebIdentity {
                role_arn: val.role_arn.unwrap_or_default(),
                token_file: val.token_file.unwrap_or_default(),
                session_name: val.session_name,
                region: val.region,
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::DiffLine> for DiffLine {
    fn from(val: xberg::DiffLine) -> Self {
        match val {
            xberg::DiffLine::Context(_0) => Self {
                kind_tag: "context".to_string(),
                context: Some(_0),
                ..Default::default()
            },
            xberg::DiffLine::Added(_0) => Self {
                kind_tag: "added".to_string(),
                added: Some(_0),
                ..Default::default()
            },
            xberg::DiffLine::Removed(_0) => Self {
                kind_tag: "removed".to_string(),
                removed: Some(_0),
                ..Default::default()
            },
        }
    }
}

impl From<DiffLine> for xberg::DiffLine {
    fn from(val: DiffLine) -> Self {
        match val.kind_tag.as_str() {
            "context" => xberg::DiffLine::Context(val.context.unwrap_or_default()),
            "added" => xberg::DiffLine::Added(val.added.unwrap_or_default()),
            "removed" => xberg::DiffLine::Removed(val.removed.unwrap_or_default()),
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::EmbeddingModelType> for EmbeddingModelType {
    fn from(val: xberg::EmbeddingModelType) -> Self {
        match val {
            xberg::EmbeddingModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                name: Some(name),
                ..Default::default()
            },
            xberg::EmbeddingModelType::Custom { model_id, dimensions } => Self {
                type_tag: "custom".to_string(),
                model_id: Some(model_id),
                dimensions: Some(dimensions as i64),
                ..Default::default()
            },
            xberg::EmbeddingModelType::Llm { llm } => Self {
                type_tag: "llm".to_string(),
                llm: Some((*llm).into()),
                ..Default::default()
            },
            xberg::EmbeddingModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                name: Some(name),
                ..Default::default()
            },
        }
    }
}

impl From<EmbeddingModelType> for xberg::EmbeddingModelType {
    fn from(val: EmbeddingModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => xberg::EmbeddingModelType::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => xberg::EmbeddingModelType::Custom {
                model_id: val.model_id.unwrap_or_default(),
                dimensions: val.dimensions.map(|v| v as usize).unwrap_or_default(),
            },
            "llm" => xberg::EmbeddingModelType::Llm {
                llm: Box::new(val.llm.map(Into::into).unwrap_or_default()),
            },
            "plugin" => xberg::EmbeddingModelType::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::FormatMetadata> for FormatMetadata {
    fn from(val: xberg::FormatMetadata) -> Self {
        match val {
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "pdf"
            ))]
            xberg::FormatMetadata::Pdf(_0) => Self {
                format_type_tag: "pdf".to_string(),
                pdf: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::Docx(_0) => Self {
                format_type_tag: "docx".to_string(),
                docx: Some((*_0).into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Excel(_0) => Self {
                format_type_tag: "excel".to_string(),
                excel: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Email(_0) => Self {
                format_type_tag: "email".to_string(),
                email: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Pptx(_0) => Self {
                format_type_tag: "pptx".to_string(),
                pptx: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Archive(_0) => Self {
                format_type_tag: "archive".to_string(),
                archive: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Image(_0) => Self {
                format_type_tag: "image".to_string(),
                image: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Xml(_0) => Self {
                format_type_tag: "xml".to_string(),
                xml: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Text(_0) => Self {
                format_type_tag: "text".to_string(),
                text: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Html(_0) => Self {
                format_type_tag: "html".to_string(),
                html: Some((*_0).into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Ocr(_0) => Self {
                format_type_tag: "ocr".to_string(),
                ocr: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Csv(_0) => Self {
                format_type_tag: "csv".to_string(),
                csv: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::Bibtex(_0) => Self {
                format_type_tag: "bibtex".to_string(),
                bibtex: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::Citation(_0) => Self {
                format_type_tag: "citation".to_string(),
                citation: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::FictionBook(_0) => Self {
                format_type_tag: "fiction_book".to_string(),
                fiction_book: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::Dbf(_0) => Self {
                format_type_tag: "dbf".to_string(),
                dbf: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "xml"
            ))]
            xberg::FormatMetadata::Jats(_0) => Self {
                format_type_tag: "jats".to_string(),
                jats: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            xberg::FormatMetadata::Epub(_0) => Self {
                format_type_tag: "epub".to_string(),
                epub: Some(_0.into()),
                ..Default::default()
            },
            xberg::FormatMetadata::Pst(_0) => Self {
                format_type_tag: "pst".to_string(),
                pst: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "transcription-types"
            ))]
            xberg::FormatMetadata::Audio(_0) => Self {
                format_type_tag: "audio".to_string(),
                audio: Some(_0.into()),
                ..Default::default()
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "tree-sitter"
            ))]
            xberg::FormatMetadata::Code(_0) => Self {
                format_type_tag: "code".to_string(),
                code: Some(_0.into()),
                ..Default::default()
            },
        }
    }
}

impl From<FormatMetadata> for xberg::FormatMetadata {
    fn from(val: FormatMetadata) -> Self {
        match val.format_type_tag.as_str() {
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "pdf"
            ))]
            "pdf" => xberg::FormatMetadata::Pdf(val.pdf.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "docx" => xberg::FormatMetadata::Docx(Box::new(val.docx.map(Into::into).unwrap_or_default())),
            "excel" => xberg::FormatMetadata::Excel(val.excel.map(Into::into).unwrap_or_default()),
            "email" => xberg::FormatMetadata::Email(val.email.map(Into::into).unwrap_or_default()),
            "pptx" => xberg::FormatMetadata::Pptx(val.pptx.map(Into::into).unwrap_or_default()),
            "archive" => xberg::FormatMetadata::Archive(val.archive.map(Into::into).unwrap_or_default()),
            "image" => xberg::FormatMetadata::Image(val.image.map(Into::into).unwrap_or_default()),
            "xml" => xberg::FormatMetadata::Xml(val.xml.map(Into::into).unwrap_or_default()),
            "text" => xberg::FormatMetadata::Text(val.text.map(Into::into).unwrap_or_default()),
            "html" => xberg::FormatMetadata::Html(Box::new(val.html.map(Into::into).unwrap_or_default())),
            "ocr" => xberg::FormatMetadata::Ocr(val.ocr.map(Into::into).unwrap_or_default()),
            "csv" => xberg::FormatMetadata::Csv(val.csv.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "bibtex" => xberg::FormatMetadata::Bibtex(val.bibtex.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "citation" => xberg::FormatMetadata::Citation(val.citation.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "fiction_book" => xberg::FormatMetadata::FictionBook(val.fiction_book.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "dbf" => xberg::FormatMetadata::Dbf(val.dbf.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "xml"
            ))]
            "jats" => xberg::FormatMetadata::Jats(val.jats.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "office"
            ))]
            "epub" => xberg::FormatMetadata::Epub(val.epub.map(Into::into).unwrap_or_default()),
            "pst" => xberg::FormatMetadata::Pst(val.pst.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "transcription-types"
            ))]
            "audio" => xberg::FormatMetadata::Audio(val.audio.map(Into::into).unwrap_or_default()),
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "tree-sitter"
            ))]
            "code" => xberg::FormatMetadata::Code(val.code.map(Into::into).unwrap_or_default()),
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<crawlberg::HostMatcher> for HostMatcher {
    fn from(val: crawlberg::HostMatcher) -> Self {
        match val {
            crawlberg::HostMatcher::Exact { value } => Self {
                type_tag: "exact".to_string(),
                value: Some(value),
            },
            crawlberg::HostMatcher::Suffix { value } => Self {
                type_tag: "suffix".to_string(),
                value: Some(value),
            },
            crawlberg::HostMatcher::Cidr { value } => Self {
                type_tag: "cidr".to_string(),
                value: Some(value),
            },
        }
    }
}

impl From<HostMatcher> for crawlberg::HostMatcher {
    fn from(val: HostMatcher) -> Self {
        match val.type_tag.as_str() {
            "exact" => crawlberg::HostMatcher::Exact {
                value: val.value.unwrap_or_default(),
            },
            "suffix" => crawlberg::HostMatcher::Suffix {
                value: val.value.unwrap_or_default(),
            },
            "cidr" => crawlberg::HostMatcher::Cidr {
                value: val.value.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::core::config::extraction::ImageOutputFormat> for ImageOutputFormat {
    fn from(val: xberg::core::config::extraction::ImageOutputFormat) -> Self {
        match val {
            xberg::core::config::extraction::ImageOutputFormat::Native => Self {
                type_tag: "native".to_string(),
                ..Default::default()
            },
            xberg::core::config::extraction::ImageOutputFormat::Png => Self {
                type_tag: "png".to_string(),
                ..Default::default()
            },
            xberg::core::config::extraction::ImageOutputFormat::Jpeg { quality } => Self {
                type_tag: "jpeg".to_string(),
                quality: Some(quality),
            },
            xberg::core::config::extraction::ImageOutputFormat::Webp { quality } => Self {
                type_tag: "webp".to_string(),
                quality: Some(quality),
            },
            xberg::core::config::extraction::ImageOutputFormat::Heif { quality } => Self {
                type_tag: "heif".to_string(),
                quality: Some(quality),
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "svg"
            ))]
            xberg::core::config::extraction::ImageOutputFormat::Svg => Self {
                type_tag: "svg".to_string(),
                ..Default::default()
            },
        }
    }
}

impl From<ImageOutputFormat> for xberg::core::config::extraction::ImageOutputFormat {
    fn from(val: ImageOutputFormat) -> Self {
        match val.type_tag.as_str() {
            "native" => xberg::core::config::extraction::ImageOutputFormat::Native,
            "png" => xberg::core::config::extraction::ImageOutputFormat::Png,
            "jpeg" => xberg::core::config::extraction::ImageOutputFormat::Jpeg {
                quality: val.quality.unwrap_or_default(),
            },
            "webp" => xberg::core::config::extraction::ImageOutputFormat::Webp {
                quality: val.quality.unwrap_or_default(),
            },
            "heif" => xberg::core::config::extraction::ImageOutputFormat::Heif {
                quality: val.quality.unwrap_or_default(),
            },
            #[cfg(any(
                all(target_os = "macos", target_arch = "x86_64"),
                not(any(target_os = "windows", all(target_os = "macos", target_arch = "x86_64"))),
                target_os = "windows",
                feature = "svg"
            ))]
            "svg" => xberg::core::config::extraction::ImageOutputFormat::Svg,
            _ => xberg::core::config::extraction::ImageOutputFormat::default(),
        }
    }
}

impl From<xberg::LateInteractionModelType> for LateInteractionModelType {
    fn from(val: xberg::LateInteractionModelType) -> Self {
        match val {
            xberg::LateInteractionModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                name: Some(name),
                ..Default::default()
            },
            xberg::LateInteractionModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
            } => Self {
                type_tag: "custom".to_string(),
                model_id: Some(model_id),
                model_file: model_file,
                additional_files: Some(additional_files.into()),
                max_length: max_length,
                ..Default::default()
            },
            xberg::LateInteractionModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                name: Some(name),
                ..Default::default()
            },
        }
    }
}

impl From<LateInteractionModelType> for xberg::LateInteractionModelType {
    fn from(val: LateInteractionModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => xberg::LateInteractionModelType::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => xberg::LateInteractionModelType::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.map(Into::into).unwrap_or_default(),
                max_length: val.max_length,
            },
            "plugin" => xberg::LateInteractionModelType::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::NoChunkingReason> for NoChunkingReason {
    fn from(val: xberg::NoChunkingReason) -> Self {
        match val {
            xberg::NoChunkingReason::SmallFile {
                size_bytes,
                threshold_bytes,
            } => Self {
                type_tag: "SmallFile".to_string(),
                size_bytes: Some(size_bytes as i64),
                threshold_bytes: Some(threshold_bytes as i64),
                ..Default::default()
            },
            xberg::NoChunkingReason::FewPages { page_count, threshold } => Self {
                type_tag: "FewPages".to_string(),
                page_count: Some(page_count),
                threshold: Some(threshold),
                ..Default::default()
            },
            xberg::NoChunkingReason::TextLayerDetected {
                text_coverage,
                avg_chars_per_page,
            } => Self {
                type_tag: "TextLayerDetected".to_string(),
                text_coverage: Some(text_coverage),
                avg_chars_per_page: Some(avg_chars_per_page),
                ..Default::default()
            },
            xberg::NoChunkingReason::FormatNotChunkable { mime_type } => Self {
                type_tag: "FormatNotChunkable".to_string(),
                mime_type: Some(mime_type),
                ..Default::default()
            },
            xberg::NoChunkingReason::ChunkingDisabled => Self {
                type_tag: "ChunkingDisabled".to_string(),
                ..Default::default()
            },
            xberg::NoChunkingReason::FastTextExtraction => Self {
                type_tag: "FastTextExtraction".to_string(),
                ..Default::default()
            },
        }
    }
}

impl From<NoChunkingReason> for xberg::NoChunkingReason {
    fn from(val: NoChunkingReason) -> Self {
        match val.type_tag.as_str() {
            "SmallFile" => xberg::NoChunkingReason::SmallFile {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                threshold_bytes: val.threshold_bytes.map(|v| v as u64).unwrap_or_default(),
            },
            "FewPages" => xberg::NoChunkingReason::FewPages {
                page_count: val.page_count.unwrap_or_default(),
                threshold: val.threshold.unwrap_or_default(),
            },
            "TextLayerDetected" => xberg::NoChunkingReason::TextLayerDetected {
                text_coverage: val.text_coverage.unwrap_or_default(),
                avg_chars_per_page: val.avg_chars_per_page.unwrap_or_default(),
            },
            "FormatNotChunkable" => xberg::NoChunkingReason::FormatNotChunkable {
                mime_type: val.mime_type.unwrap_or_default(),
            },
            "ChunkingDisabled" => xberg::NoChunkingReason::ChunkingDisabled,
            "FastTextExtraction" => xberg::NoChunkingReason::FastTextExtraction,
            _ => xberg::NoChunkingReason::default(),
        }
    }
}

impl From<xberg::NodeContent> for NodeContent {
    fn from(val: xberg::NodeContent) -> Self {
        match val {
            xberg::NodeContent::Title { text } => Self {
                node_type_tag: "title".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Heading { level, text } => Self {
                node_type_tag: "heading".to_string(),
                level: Some(level),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Paragraph { text } => Self {
                node_type_tag: "paragraph".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::List { ordered } => Self {
                node_type_tag: "list".to_string(),
                ordered: Some(ordered),
                ..Default::default()
            },
            xberg::NodeContent::ListItem { text } => Self {
                node_type_tag: "list_item".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Table { grid } => Self {
                node_type_tag: "table".to_string(),
                grid: Some(grid.into()),
                ..Default::default()
            },
            xberg::NodeContent::Image {
                description,
                image_index,
                src,
            } => Self {
                node_type_tag: "image".to_string(),
                description: description,
                image_index: image_index,
                src: src,
                ..Default::default()
            },
            xberg::NodeContent::Code { text, language } => Self {
                node_type_tag: "code".to_string(),
                text: Some(text),
                language: language,
                ..Default::default()
            },
            xberg::NodeContent::Quote => Self {
                node_type_tag: "quote".to_string(),
                ..Default::default()
            },
            xberg::NodeContent::Formula { text } => Self {
                node_type_tag: "formula".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Footnote { text } => Self {
                node_type_tag: "footnote".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Comment { text } => Self {
                node_type_tag: "comment".to_string(),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Group {
                label,
                heading_level,
                heading_text,
            } => Self {
                node_type_tag: "group".to_string(),
                label: label,
                heading_level: heading_level,
                heading_text: heading_text,
                ..Default::default()
            },
            xberg::NodeContent::PageBreak => Self {
                node_type_tag: "page_break".to_string(),
                ..Default::default()
            },
            xberg::NodeContent::Slide { number, title } => Self {
                node_type_tag: "slide".to_string(),
                number: Some(number),
                title: title,
                ..Default::default()
            },
            xberg::NodeContent::DefinitionList => Self {
                node_type_tag: "definition_list".to_string(),
                ..Default::default()
            },
            xberg::NodeContent::DefinitionItem { term, definition } => Self {
                node_type_tag: "definition_item".to_string(),
                term: Some(term),
                definition: Some(definition),
                ..Default::default()
            },
            xberg::NodeContent::Citation { key, text } => Self {
                node_type_tag: "citation".to_string(),
                key: Some(key),
                text: Some(text),
                ..Default::default()
            },
            xberg::NodeContent::Admonition { kind, title } => Self {
                node_type_tag: "admonition".to_string(),
                kind: Some(kind),
                title: title,
                ..Default::default()
            },
            xberg::NodeContent::RawBlock { format, content } => Self {
                node_type_tag: "raw_block".to_string(),
                format: Some(format),
                content: Some(content),
                ..Default::default()
            },
            xberg::NodeContent::MetadataBlock { entries } => Self {
                node_type_tag: "metadata_block".to_string(),
                entries: Some(entries.into_iter().map(Into::into).collect()),
                ..Default::default()
            },
        }
    }
}

impl From<NodeContent> for xberg::NodeContent {
    fn from(val: NodeContent) -> Self {
        match val.node_type_tag.as_str() {
            "title" => xberg::NodeContent::Title {
                text: val.text.unwrap_or_default(),
            },
            "heading" => xberg::NodeContent::Heading {
                level: val.level.unwrap_or_default(),
                text: val.text.unwrap_or_default(),
            },
            "paragraph" => xberg::NodeContent::Paragraph {
                text: val.text.unwrap_or_default(),
            },
            "list" => xberg::NodeContent::List {
                ordered: val.ordered.unwrap_or_default(),
            },
            "list_item" => xberg::NodeContent::ListItem {
                text: val.text.unwrap_or_default(),
            },
            "table" => xberg::NodeContent::Table {
                grid: val.grid.map(Into::into).unwrap_or_default(),
            },
            "image" => xberg::NodeContent::Image {
                description: val.description,
                image_index: val.image_index,
                src: val.src,
            },
            "code" => xberg::NodeContent::Code {
                text: val.text.unwrap_or_default(),
                language: val.language,
            },
            "quote" => xberg::NodeContent::Quote,
            "formula" => xberg::NodeContent::Formula {
                text: val.text.unwrap_or_default(),
            },
            "footnote" => xberg::NodeContent::Footnote {
                text: val.text.unwrap_or_default(),
            },
            "comment" => xberg::NodeContent::Comment {
                text: val.text.unwrap_or_default(),
            },
            "group" => xberg::NodeContent::Group {
                label: val.label,
                heading_level: val.heading_level,
                heading_text: val.heading_text,
            },
            "page_break" => xberg::NodeContent::PageBreak,
            "slide" => xberg::NodeContent::Slide {
                number: val.number.unwrap_or_default(),
                title: val.title,
            },
            "definition_list" => xberg::NodeContent::DefinitionList,
            "definition_item" => xberg::NodeContent::DefinitionItem {
                term: val.term.unwrap_or_default(),
                definition: val.definition.unwrap_or_default(),
            },
            "citation" => xberg::NodeContent::Citation {
                key: val.key.unwrap_or_default(),
                text: val.text.unwrap_or_default(),
            },
            "admonition" => xberg::NodeContent::Admonition {
                kind: val.kind.unwrap_or_default(),
                title: val.title,
            },
            "raw_block" => xberg::NodeContent::RawBlock {
                format: val.format.unwrap_or_default(),
                content: val.content.unwrap_or_default(),
            },
            "metadata_block" => xberg::NodeContent::MetadataBlock {
                entries: val
                    .entries
                    .map(|v| v.into_iter().map(Into::into).collect())
                    .unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::OcrBoundingGeometry> for OcrBoundingGeometry {
    fn from(val: xberg::OcrBoundingGeometry) -> Self {
        match val {
            xberg::OcrBoundingGeometry::Rectangle {
                left,
                top,
                width,
                height,
            } => Self {
                type_tag: "rectangle".to_string(),
                left: Some(left),
                top: Some(top),
                width: Some(width),
                height: Some(height),
                ..Default::default()
            },
            xberg::OcrBoundingGeometry::Quadrilateral { points } => Self {
                type_tag: "quadrilateral".to_string(),
                points: Some(points.into_iter().map(Into::into).collect()),
                ..Default::default()
            },
        }
    }
}

impl From<OcrBoundingGeometry> for xberg::OcrBoundingGeometry {
    fn from(val: OcrBoundingGeometry) -> Self {
        match val.type_tag.as_str() {
            "rectangle" => xberg::OcrBoundingGeometry::Rectangle {
                left: val.left.unwrap_or_default(),
                top: val.top.unwrap_or_default(),
                width: val.width.unwrap_or_default(),
                height: val.height.unwrap_or_default(),
            },
            "quadrilateral" => xberg::OcrBoundingGeometry::Quadrilateral {
                points: val
                    .points
                    .map(|v| v.into_iter().map(Into::into).collect())
                    .unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::OcrStrategy> for OcrStrategy {
    fn from(val: xberg::OcrStrategy) -> Self {
        match val {
            xberg::OcrStrategy::Auto => Self {
                mode_tag: "auto".to_string(),
                ..Default::default()
            },
            xberg::OcrStrategy::ScannedPages { min_confidence } => Self {
                mode_tag: "scanned_pages".to_string(),
                min_confidence: Some(min_confidence),
            },
        }
    }
}

impl From<OcrStrategy> for xberg::OcrStrategy {
    fn from(val: OcrStrategy) -> Self {
        match val.mode_tag.as_str() {
            "auto" => xberg::OcrStrategy::Auto,
            "scanned_pages" => xberg::OcrStrategy::ScannedPages {
                min_confidence: val.min_confidence.unwrap_or_default(),
            },
            _ => xberg::OcrStrategy::default(),
        }
    }
}

impl From<xberg::RerankerModelType> for RerankerModelType {
    fn from(val: xberg::RerankerModelType) -> Self {
        match val {
            xberg::RerankerModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                name: Some(name),
                ..Default::default()
            },
            xberg::RerankerModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
                head,
            } => Self {
                type_tag: "custom".to_string(),
                model_id: Some(model_id),
                model_file: model_file,
                additional_files: Some(additional_files.into()),
                max_length: max_length,
                head: Some(head.into()),
                ..Default::default()
            },
            xberg::RerankerModelType::Llm { llm } => Self {
                type_tag: "llm".to_string(),
                llm: Some((*llm).into()),
                ..Default::default()
            },
            xberg::RerankerModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                name: Some(name),
                ..Default::default()
            },
        }
    }
}

impl From<RerankerModelType> for xberg::RerankerModelType {
    fn from(val: RerankerModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => xberg::RerankerModelType::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => xberg::RerankerModelType::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.map(Into::into).unwrap_or_default(),
                max_length: val.max_length,
                head: val.head.map(Into::into).unwrap_or_default(),
            },
            "llm" => xberg::RerankerModelType::Llm {
                llm: Box::new(val.llm.map(Into::into).unwrap_or_default()),
            },
            "plugin" => xberg::RerankerModelType::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::RevisionAnchor> for RevisionAnchor {
    fn from(val: xberg::RevisionAnchor) -> Self {
        match val {
            xberg::RevisionAnchor::Paragraph { index } => Self {
                type_tag: "paragraph".to_string(),
                index: Some(index as i64),
                ..Default::default()
            },
            xberg::RevisionAnchor::TableCell { row, col, table_index } => Self {
                type_tag: "table_cell".to_string(),
                row: Some(row as i64),
                col: Some(col as i64),
                table_index: Some(table_index as i64),
                ..Default::default()
            },
            xberg::RevisionAnchor::Page { index } => Self {
                type_tag: "page".to_string(),
                index: Some(index as i64),
                ..Default::default()
            },
            xberg::RevisionAnchor::Slide { index } => Self {
                type_tag: "slide".to_string(),
                index: Some(index as i64),
                ..Default::default()
            },
            xberg::RevisionAnchor::Sheet { index, name } => Self {
                type_tag: "sheet".to_string(),
                index: Some(index as i64),
                name: name,
                ..Default::default()
            },
        }
    }
}

impl From<RevisionAnchor> for xberg::RevisionAnchor {
    fn from(val: RevisionAnchor) -> Self {
        match val.type_tag.as_str() {
            "paragraph" => xberg::RevisionAnchor::Paragraph {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "table_cell" => xberg::RevisionAnchor::TableCell {
                row: val.row.map(|v| v as usize).unwrap_or_default(),
                col: val.col.map(|v| v as usize).unwrap_or_default(),
                table_index: val.table_index.map(|v| v as usize).unwrap_or_default(),
            },
            "page" => xberg::RevisionAnchor::Page {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "slide" => xberg::RevisionAnchor::Slide {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "sheet" => xberg::RevisionAnchor::Sheet {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
                name: val.name,
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::SparseEmbeddingModelType> for SparseEmbeddingModelType {
    fn from(val: xberg::SparseEmbeddingModelType) -> Self {
        match val {
            xberg::SparseEmbeddingModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                name: Some(name),
                ..Default::default()
            },
            xberg::SparseEmbeddingModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
            } => Self {
                type_tag: "custom".to_string(),
                model_id: Some(model_id),
                model_file: model_file,
                additional_files: Some(additional_files.into()),
                max_length: max_length,
                ..Default::default()
            },
            xberg::SparseEmbeddingModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                name: Some(name),
                ..Default::default()
            },
        }
    }
}

impl From<SparseEmbeddingModelType> for xberg::SparseEmbeddingModelType {
    fn from(val: SparseEmbeddingModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => xberg::SparseEmbeddingModelType::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => xberg::SparseEmbeddingModelType::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.map(Into::into).unwrap_or_default(),
                max_length: val.max_length,
            },
            "plugin" => xberg::SparseEmbeddingModelType::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => unreachable!("unrecognised tag for flat enum, not constructible from PHP"),
        }
    }
}

impl From<xberg::VlmFallbackPolicy> for VlmFallbackPolicy {
    fn from(val: xberg::VlmFallbackPolicy) -> Self {
        match val {
            xberg::VlmFallbackPolicy::Disabled => Self {
                mode_tag: "disabled".to_string(),
                ..Default::default()
            },
            xberg::VlmFallbackPolicy::OnLowQuality { quality_threshold } => Self {
                mode_tag: "on_low_quality".to_string(),
                quality_threshold: Some(quality_threshold),
            },
            xberg::VlmFallbackPolicy::Always => Self {
                mode_tag: "always".to_string(),
                ..Default::default()
            },
        }
    }
}

impl From<VlmFallbackPolicy> for xberg::VlmFallbackPolicy {
    fn from(val: VlmFallbackPolicy) -> Self {
        match val.mode_tag.as_str() {
            "disabled" => xberg::VlmFallbackPolicy::Disabled,
            "on_low_quality" => xberg::VlmFallbackPolicy::OnLowQuality {
                quality_threshold: val.quality_threshold.unwrap_or_default(),
            },
            "always" => xberg::VlmFallbackPolicy::Always,
            _ => xberg::VlmFallbackPolicy::default(),
        }
    }
}

impl From<BBox> for xberg::BBox {
    fn from(val: BBox) -> Self {
        Self {
            x1: val.x1,
            y1: val.y1,
            x2: val.x2,
            y2: val.y2,
        }
    }
}

impl From<CacheStats> for xberg::CacheStats {
    fn from(val: CacheStats) -> Self {
        Self {
            total_files: val.total_files as usize,
            total_size_mb: val.total_size_mb,
            available_space_mb: val.available_space_mb,
            oldest_file_age_days: val.oldest_file_age_days,
            newest_file_age_days: val.newest_file_age_days,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ChunkClassificationEnrichmentConfig> for xberg::ChunkClassificationEnrichmentConfig {
    fn from(val: ChunkClassificationEnrichmentConfig) -> Self {
        Self {
            config: val.config.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ChunkInfo> for xberg::ChunkInfo {
    fn from(val: ChunkInfo) -> Self {
        Self {
            index: val.index,
            pages: val.pages.into(),
            estimated_time_ms: val.estimated_time_ms as u64,
        }
    }
}

impl From<Citation> for xberg::Citation {
    fn from(val: Citation) -> Self {
        Self {
            label: val.label,
            source: val.source,
            locator: val.locator,
            excerpt: val.excerpt,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<DeepseekOcrBackendOptions> for xberg::candle_ocr::DeepseekOcrBackendOptions {
    fn from(val: DeepseekOcrBackendOptions) -> Self {
        Self {
            model_path: val.model_path,
            device: val.device.as_deref().map(|s| match s {
                "auto" => xberg::candle_ocr::CandleDevicePreference::Auto,
                "cpu" => xberg::candle_ocr::CandleDevicePreference::Cpu,
                "cuda" => xberg::candle_ocr::CandleDevicePreference::Cuda,
                "metal" => xberg::candle_ocr::CandleDevicePreference::Metal,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'device' of CandleDevicePreference: expected one of [auto, cpu, cuda, metal]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            }),
            version: val.version,
            ..Default::default()
        }
    }
}

impl From<DetectResponse> for xberg::api::DetectResponse {
    fn from(val: DetectResponse) -> Self {
        Self {
            mime_type: val.mime_type,
            filename: val.filename,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DetectionResult> for xberg::DetectionResult {
    fn from(val: DetectionResult) -> Self {
        Self {
            page_width: val.page_width,
            page_height: val.page_height,
            detections: val.detections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DiffHunk> for xberg::DiffHunk {
    fn from(val: DiffHunk) -> Self {
        Self {
            from_line: val.from_line as usize,
            from_count: val.from_count as usize,
            to_line: val.to_line as usize,
            to_count: val.to_count as usize,
            lines: val.lines.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentBoundary> for xberg::DocumentBoundary {
    fn from(val: DocumentBoundary) -> Self {
        Self {
            start_page: val.start_page,
            end_page: val.end_page,
            confidence: val.confidence,
            reason: match val.reason.as_str() {
                "start" => xberg::BoundaryReason::Start,
                "page_one_marker" => xberg::BoundaryReason::PageOneMarker,
                "letterhead_reset" => xberg::BoundaryReason::LetterheadReset,
                "density_shift" => xberg::BoundaryReason::DensityShift,
                "end" => xberg::BoundaryReason::End,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'reason' of BoundaryReason: expected one of [start, page_one_marker, letterhead_reset, density_shift, end]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::BoundaryReason::Start
                }
            },
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<DocumentMetadata> for xberg::DocumentMetadata {
    fn from(val: DocumentMetadata) -> Self {
        Self {
            mime_type: val.mime_type,
            size_bytes: val.size_bytes as u64,
            page_count: val.page_count,
            force_ocr: val.force_ocr,
            user_chunk_config: val.user_chunk_config.map(Into::into),
            chunking_enabled: val.chunking_enabled,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<EmailAttachment> for xberg::EmailAttachment {
    fn from(val: EmailAttachment) -> Self {
        Self {
            name: val.name,
            filename: val.filename,
            mime_type: val.mime_type,
            size: val.size.map(|v| v as usize),
            is_image: val.is_image,
            data: val.data.map(|v| v.to_vec().into()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<EmailExtractionResult> for xberg::EmailExtractionResult {
    fn from(val: EmailExtractionResult) -> Self {
        Self {
            subject: val.subject,
            from_email: val.from_email,
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            date: val.date,
            message_id: val.message_id,
            plain_text: val.plain_text,
            html_content: val.html_content,
            content: val.content,
            attachments: val.attachments.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<EmbeddedChanges> for xberg::EmbeddedChanges {
    fn from(val: EmbeddedChanges) -> Self {
        Self {
            added: val.added.into_iter().map(Into::into).collect(),
            removed: val.removed.into_iter().map(Into::into).collect(),
            changed: val.changed.into_iter().map(Into::into).collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<EmbeddedDiff> for xberg::EmbeddedDiff {
    fn from(val: EmbeddedDiff) -> Self {
        Self {
            path: val.path,
            diff: Box::new(val.diff.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<EmbeddedFile> for xberg::pdf::embedded_files::EmbeddedFile {
    fn from(val: EmbeddedFile) -> Self {
        Self {
            name: val.name,
            data: val.data.to_vec().into(),
            compressed_size: val.compressed_size as usize,
            mime_type: val.mime_type,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<ExcelSheet> for xberg::ExcelSheet {
    fn from(val: ExcelSheet) -> Self {
        Self {
            name: val.name,
            markdown: val.markdown,
            row_count: val.row_count as usize,
            col_count: val.col_count as usize,
            cell_count: val.cell_count as usize,
            table_cells: val.table_cells.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ExcelWorkbook> for xberg::ExcelWorkbook {
    fn from(val: ExcelWorkbook) -> Self {
        Self {
            sheets: val.sheets.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<ExtractionDiff> for xberg::ExtractionDiff {
    fn from(val: ExtractionDiff) -> Self {
        Self {
            content_diff: val.content_diff.into_iter().map(Into::into).collect(),
            tables_added: val.tables_added.into_iter().map(Into::into).collect(),
            tables_removed: val.tables_removed.into_iter().map(Into::into).collect(),
            tables_changed: val.tables_changed.into_iter().map(Into::into).collect(),
            metadata_changed: val.metadata_changed,
            embedded_changes: val.embedded_changes.into(),
            ..Default::default()
        }
    }
}

impl From<FootnoteAnchor> for xberg::FootnoteAnchor {
    fn from(val: FootnoteAnchor) -> Self {
        Self {
            label: val.label,
            offset: val.offset as usize,
        }
    }
}

impl From<FootnoteDefinition> for xberg::FootnoteDefinition {
    fn from(val: FootnoteDefinition) -> Self {
        Self {
            label: val.label,
            content: val.content,
            offset: val.offset as usize,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<GlmOcrBackendOptions> for xberg::candle_ocr::GlmOcrBackendOptions {
    fn from(val: GlmOcrBackendOptions) -> Self {
        Self {
            task: val.task.as_deref().map(|s| match s { "ocr" => xberg::candle_ocr::GlmOcrTaskKind::Ocr,"table" => xberg::candle_ocr::GlmOcrTaskKind::Table,"formula" => xberg::candle_ocr::GlmOcrTaskKind::Formula,"chart" => xberg::candle_ocr::GlmOcrTaskKind::Chart,"caption" => xberg::candle_ocr::GlmOcrTaskKind::Caption,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'task' of GlmOcrTaskKind: expected one of [ocr, table, formula, chart, caption]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            device: val.device.as_deref().map(|s| match s { "auto" => xberg::candle_ocr::CandleDevicePreference::Auto,"cpu" => xberg::candle_ocr::CandleDevicePreference::Cpu,"cuda" => xberg::candle_ocr::CandleDevicePreference::Cuda,"metal" => xberg::candle_ocr::CandleDevicePreference::Metal,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'device' of CandleDevicePreference: expected one of [auto, cpu, cuda, metal]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            layout_mode: val.layout_mode.as_deref().map(|s| match s { "whole_page" => xberg::candle_ocr::GlmOcrLayoutMode::WholePage,"paired" => xberg::candle_ocr::GlmOcrLayoutMode::Paired,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'layout_mode' of GlmOcrLayoutMode: expected one of [whole_page, paired]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            enable_chart_understanding: val.enable_chart_understanding,
            cache_dir: val.cache_dir,
            ..Default::default()        }
    }
}

impl From<LateInteractionPreset> for xberg::LateInteractionPreset {
    fn from(val: LateInteractionPreset) -> Self {
        Self {
            name: val.name,
            model_repo: val.model_repo,
            model_file: val.model_file,
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as usize,
            query_max_length: val.query_max_length as usize,
            dim: val.dim as usize,
            description: val.description,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<LayoutDetection> for xberg::LayoutDetection {
    fn from(val: LayoutDetection) -> Self {
        Self {
            class_name: match val.class_name.as_str() {
                "caption" => xberg::LayoutClass::Caption,
                "chart" => xberg::LayoutClass::Chart,
                "footnote" => xberg::LayoutClass::Footnote,
                "formula" => xberg::LayoutClass::Formula,
                "list_item" => xberg::LayoutClass::ListItem,
                "page_footer" => xberg::LayoutClass::PageFooter,
                "page_header" => xberg::LayoutClass::PageHeader,
                "picture" => xberg::LayoutClass::Picture,
                "section_header" => xberg::LayoutClass::SectionHeader,
                "table" => xberg::LayoutClass::Table,
                "text" => xberg::LayoutClass::Text,
                "title" => xberg::LayoutClass::Title,
                "document_index" => xberg::LayoutClass::DocumentIndex,
                "code" => xberg::LayoutClass::Code,
                "checkbox_selected" => xberg::LayoutClass::CheckboxSelected,
                "checkbox_unselected" => xberg::LayoutClass::CheckboxUnselected,
                "form" => xberg::LayoutClass::Form,
                "key_value_region" => xberg::LayoutClass::KeyValueRegion,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'class_name' of LayoutClass: expected one of [caption, chart, footnote, formula, list_item, page_footer, page_header, picture, section_header, table, text, title, document_index, code, checkbox_selected, checkbox_unselected, form, key_value_region]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            confidence: val.confidence,
            bbox: val.bbox.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ModelPaths> for xberg::ModelPaths {
    fn from(val: ModelPaths) -> Self {
        Self {
            det_model: val.det_model.into(),
            cls_model: val.cls_model.into(),
            rec_model: val.rec_model.into(),
            dict_file: val.dict_file.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<MultidocInput> for xberg::MultidocInput {
    fn from(val: MultidocInput) -> Self {
        Self {
            page_count: val.page_count,
            pages: val.pages.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<OcrExtractionResult> for xberg::OcrExtractionResult {
    fn from(val: OcrExtractionResult) -> Self {
        let mut __result = xberg::OcrExtractionResult::default();
        __result.content = val.content;
        __result.mime_type = val.mime_type;
        __result.metadata = val.metadata.into_iter().map(|(k, v)| (k.into(), v)).collect();
        __result.tables = val.tables.into_iter().map(Into::into).collect();
        __result.ocr_elements = val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect());
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<OcrTable> for xberg::OcrTable {
    fn from(val: OcrTable) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown,
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
        }
    }
}

impl From<OcrTableBoundingBox> for xberg::OcrTableBoundingBox {
    fn from(val: OcrTableBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
        }
    }
}

impl From<OrientationResult> for xberg::OrientationResult {
    fn from(val: OrientationResult) -> Self {
        Self {
            degrees: val.degrees,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<PaddleOcrVlBackendOptions> for xberg::candle_ocr::PaddleOcrVlBackendOptions {
    fn from(val: PaddleOcrVlBackendOptions) -> Self {
        Self {
            task: val.task.as_deref().map(|s| match s {
                "ocr" => xberg::candle_ocr::PaddleOcrVlTaskKind::Ocr,
                "table" => xberg::candle_ocr::PaddleOcrVlTaskKind::Table,
                "formula" => xberg::candle_ocr::PaddleOcrVlTaskKind::Formula,
                "chart" => xberg::candle_ocr::PaddleOcrVlTaskKind::Chart,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'task' of PaddleOcrVlTaskKind: expected one of [ocr, table, formula, chart]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            }),
            model_path: val.model_path,
            model_id: val.model_id,
            hf_revision: val.hf_revision,
            cache_dir: val.cache_dir,
            device: val.device.as_deref().map(|s| match s {
                "auto" => xberg::candle_ocr::CandleDevicePreference::Auto,
                "cpu" => xberg::candle_ocr::CandleDevicePreference::Cpu,
                "cuda" => xberg::candle_ocr::CandleDevicePreference::Cuda,
                "metal" => xberg::candle_ocr::CandleDevicePreference::Metal,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'device' of CandleDevicePreference: expected one of [auto, cpu, cuda, metal]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            }),
            ..Default::default()
        }
    }
}

impl From<PageRange> for xberg::PageRange {
    fn from(val: PageRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PatternMatch> for xberg::text::redaction::patterns::PatternMatch {
    fn from(val: PatternMatch) -> Self {
        Self {
            start: val.start as usize,
            end: val.end as usize,
            category: match val.category.as_str() {
                "email" => xberg::PiiCategory::Email,
                "phone" => xberg::PiiCategory::Phone,
                "ssn" => xberg::PiiCategory::Ssn,
                "credit_card" => xberg::PiiCategory::CreditCard,
                "postal_code" => xberg::PiiCategory::PostalCode,
                "ip_address" => xberg::PiiCategory::IpAddress,
                "iban" => xberg::PiiCategory::Iban,
                "swift_bic" => xberg::PiiCategory::SwiftBic,
                "date_of_birth" => xberg::PiiCategory::DateOfBirth,
                "person" => xberg::PiiCategory::Person,
                "organization" => xberg::PiiCategory::Organization,
                "location" => xberg::PiiCategory::Location,
                "custom" => xberg::PiiCategory::Custom(Default::default()),
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'category' of PiiCategory: expected one of [email, phone, ssn, credit_card, postal_code, ip_address, iban, swift_bic, date_of_birth, person, organization, location, custom]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    xberg::PiiCategory::Email
                }
            },
            text: val.text,
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PptxAppProperties> for xberg::extraction::office_metadata::app_properties::PptxAppProperties {
    fn from(val: PptxAppProperties) -> Self {
        Self {
            application: val.application,
            app_version: val.app_version,
            total_time: val.total_time,
            company: val.company,
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            slides: val.slides,
            notes: val.notes,
            hidden_slides: val.hidden_slides,
            multimedia_clips: val.multimedia_clips,
            presentation_format: val.presentation_format,
            slide_titles: val.slide_titles.into_iter().collect(),
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PptxExtractionResult> for xberg::PptxExtractionResult {
    fn from(val: PptxExtractionResult) -> Self {
        Self {
            content: val.content,
            metadata: val.metadata.into(),
            slide_count: val.slide_count as usize,
            image_count: val.image_count as usize,
            table_count: val.table_count as usize,
            images: val.images.into_iter().map(Into::into).collect(),
            page_structure: val.page_structure.map(Into::into),
            page_contents: val.page_contents.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            hyperlinks: val.hyperlinks.into_iter().map(Into::into).collect(),
            office_metadata: val
                .office_metadata
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<PresentationHyperlink> for xberg::PresentationHyperlink {
    fn from(val: PresentationHyperlink) -> Self {
        Self {
            url: val.url,
            label: val.label,
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<RecognizedTable> for xberg::RecognizedTable {
    fn from(val: RecognizedTable) -> Self {
        Self {
            detection_bbox: val.detection_bbox.into(),
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown,
        }
    }
}

impl From<RerankedDocument> for xberg::RerankedDocument {
    fn from(val: RerankedDocument) -> Self {
        Self {
            index: val.index as usize,
            score: val.score,
            document: val.document,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ResolvedPreset> for xberg::ResolvedPreset {
    fn from(val: ResolvedPreset) -> Self {
        Self {
            id: val.id,
            version: val.version,
            fingerprint: val.fingerprint,
            schema_name: val.schema_name,
            schema: val.schema,
            system_prompt: val.system_prompt,
            merge_mode: match val.merge_mode.as_str() {
                "object_merge" => xberg::MergeMode::ObjectMerge,
                "array_concat" => xberg::MergeMode::ArrayConcat,
                "object_first" => xberg::MergeMode::ObjectFirst,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'merge_mode' of MergeMode: expected one of [object_merge, array_concat, object_first]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            preferred_call_mode: match val.preferred_call_mode.as_str() {
                "text_only" => xberg::CallMode::TextOnly,
                "vision_only" => xberg::CallMode::VisionOnly,
                "text_plus_vision" => xberg::CallMode::TextPlusVision,
                other => {
                    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'preferred_call_mode' of CallMode: expected one of [text_only, vision_only, text_plus_vision]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
                    Default::default()
                }
            },
            emit_citations: val.emit_citations,
        }
    }
}

impl From<SparseEmbeddingPreset> for xberg::SparseEmbeddingPreset {
    fn from(val: SparseEmbeddingPreset) -> Self {
        Self {
            name: val.name,
            model_repo: val.model_repo,
            model_file: val.model_file,
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as usize,
            description: val.description,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<StructuredDataResult> for xberg::extraction::structured::StructuredDataResult {
    fn from(val: StructuredDataResult) -> Self {
        Self {
            content: val.content,
            format: val.format.into(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            text_fields: val.text_fields.into_iter().collect(),
            value: val.value,
            flattened: val.flattened.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<TableCell> for xberg::TableCell {
    fn from(val: TableCell) -> Self {
        Self {
            content: val.content,
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
            ..Default::default()
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<TableDiff> for xberg::TableDiff {
    fn from(val: TableDiff) -> Self {
        Self {
            from_index: val.from_index as usize,
            to_index: val.to_index as usize,
            cell_changes: val.cell_changes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<TextExtractionResult> for xberg::TextExtractionResult {
    fn from(val: TextExtractionResult) -> Self {
        Self {
            content: val.content,
            line_count: val.line_count as usize,
            word_count: val.word_count as usize,
            character_count: val.character_count as usize,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::useless_conversion)]
impl From<TrocrBackendOptions> for xberg::candle_ocr::TrocrBackendOptions {
    fn from(val: TrocrBackendOptions) -> Self {
        Self {
            variant: val.variant.as_deref().map(|s| match s { "base-printed" => xberg::candle_ocr::CandleTrocrVariant::BasePrinted,"large-printed" => xberg::candle_ocr::CandleTrocrVariant::LargePrinted,"base-handwritten" => xberg::candle_ocr::CandleTrocrVariant::BaseHandwritten,"large-handwritten" => xberg::candle_ocr::CandleTrocrVariant::LargeHandwritten,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'variant' of CandleTrocrVariant: expected one of [base-printed, large-printed, base-handwritten, large-handwritten]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            device: val.device.as_deref().map(|s| match s { "auto" => xberg::candle_ocr::CandleDevicePreference::Auto,"cpu" => xberg::candle_ocr::CandleDevicePreference::Cpu,"cuda" => xberg::candle_ocr::CandleDevicePreference::Cuda,"metal" => xberg::candle_ocr::CandleDevicePreference::Metal,other => {
    ext_php_rs::exception::PhpException::default(format!(
        "invalid value {other:?} for field 'device' of CandleDevicePreference: expected one of [auto, cpu, cuda, metal]"
    ))
    .throw()
    .expect("failed to throw PHP exception for invalid enum string value");
    Default::default()
}
 })
,
            cache_dir: val.cache_dir,
            hf_revision: val.hf_revision,
            ..Default::default()        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<UserChunkConfig> for xberg::UserChunkConfig {
    fn from(val: UserChunkConfig) -> Self {
        Self {
            page_ranges: val.page_ranges.map(|v| v.into_iter().map(Into::into).collect()),
            pages_per_chunk: val.pages_per_chunk,
            force_chunking: val.force_chunking,
            disable_chunking: val.disable_chunking,
            ..Default::default()
        }
    }
}

#[allow(clippy::needless_update)]
impl From<XlsxAppProperties> for xberg::extraction::office_metadata::app_properties::XlsxAppProperties {
    fn from(val: XlsxAppProperties) -> Self {
        Self {
            application: val.application,
            app_version: val.app_version,
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            company: val.company,
            worksheet_names: val.worksheet_names.into_iter().collect(),
            ..Default::default()
        }
    }
}

impl From<XmlExtractionResult> for xberg::XmlExtractionResult {
    fn from(val: XmlExtractionResult) -> Self {
        Self {
            content: val.content,
            element_count: val.element_count as usize,
            unique_elements: val.unique_elements.into_iter().collect(),
        }
    }
}

/// Convert a `xberg::heuristics::error::HeuristicsError` error to a PHP exception.
#[allow(dead_code)]
fn heuristics_error_to_php_err(e: xberg::heuristics::error::HeuristicsError) -> ext_php_rs::exception::PhpException {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        xberg::heuristics::error::HeuristicsError::ConfigError(..) => {
            ext_php_rs::exception::PhpException::default(format!("[ConfigError] {}", msg))
        }
        xberg::heuristics::error::HeuristicsError::PdfAnalysisError(..) => {
            ext_php_rs::exception::PhpException::default(format!("[PdfAnalysisError] {}", msg))
        }
        _ => ext_php_rs::exception::PhpException::default(msg),
    }
}

/// Convert a `xberg::presets::loader::LoadError` error to a PHP exception.
#[allow(dead_code)]
fn load_error_to_php_err(e: xberg::presets::loader::LoadError) -> ext_php_rs::exception::PhpException {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        xberg::presets::loader::LoadError::Parse { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Parse] {}", msg))
        }
        xberg::presets::loader::LoadError::SchemaValidation { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[SchemaValidation] {}", msg))
        }
        xberg::presets::loader::LoadError::Deserialize { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Deserialize] {}", msg))
        }
        xberg::presets::loader::LoadError::IdMismatch { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[IdMismatch] {}", msg))
        }
        xberg::presets::loader::LoadError::BadMetaSchema(..) => {
            ext_php_rs::exception::PhpException::default(format!("[BadMetaSchema] {}", msg))
        }
        xberg::presets::loader::LoadError::Io(..) => {
            ext_php_rs::exception::PhpException::default(format!("[Io] {}", msg))
        }
        _ => ext_php_rs::exception::PhpException::default(msg),
    }
}

/// Convert a `xberg::presets::resolve::ResolveError` error to a PHP exception.
#[allow(dead_code)]
fn resolve_error_to_php_err(e: xberg::presets::resolve::ResolveError) -> ext_php_rs::exception::PhpException {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        xberg::presets::resolve::ResolveError::SchemaNotObject => {
            ext_php_rs::exception::PhpException::default(format!("[SchemaNotObject] {}", msg))
        }
        _ => ext_php_rs::exception::PhpException::default(msg),
    }
}

/// Convert a `xberg::error::XbergError` error to a PHP exception.
#[allow(dead_code)]
fn xberg_error_to_php_err(e: xberg::error::XbergError) -> ext_php_rs::exception::PhpException {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        xberg::error::XbergError::Io(..) => ext_php_rs::exception::PhpException::default(format!("[Io] {}", msg)),
        xberg::error::XbergError::Parsing { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Parsing] {}", msg))
        }
        xberg::error::XbergError::Ocr { .. } => ext_php_rs::exception::PhpException::default(format!("[Ocr] {}", msg)),
        xberg::error::XbergError::Validation { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Validation] {}", msg))
        }
        xberg::error::XbergError::Cache { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Cache] {}", msg))
        }
        xberg::error::XbergError::ImageProcessing { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[ImageProcessing] {}", msg))
        }
        xberg::error::XbergError::Serialization { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Serialization] {}", msg))
        }
        xberg::error::XbergError::MissingDependency(..) => {
            ext_php_rs::exception::PhpException::default(format!("[MissingDependency] {}", msg))
        }
        xberg::error::XbergError::Plugin { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Plugin] {}", msg))
        }
        xberg::error::XbergError::LockPoisoned(..) => {
            ext_php_rs::exception::PhpException::default(format!("[LockPoisoned] {}", msg))
        }
        xberg::error::XbergError::UnsupportedFormat(..) => {
            ext_php_rs::exception::PhpException::default(format!("[UnsupportedFormat] {}", msg))
        }
        xberg::error::XbergError::Embedding { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Embedding] {}", msg))
        }
        xberg::error::XbergError::Reranking { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Reranking] {}", msg))
        }
        xberg::error::XbergError::Transcription { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Transcription] {}", msg))
        }
        xberg::error::XbergError::Timeout { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Timeout] {}", msg))
        }
        xberg::error::XbergError::Cancelled => {
            ext_php_rs::exception::PhpException::default(format!("[Cancelled] {}", msg))
        }
        xberg::error::XbergError::Security { .. } => {
            ext_php_rs::exception::PhpException::default(format!("[Security] {}", msg))
        }
        xberg::error::XbergError::Other(..) => ext_php_rs::exception::PhpException::default(format!("[Other] {}", msg)),
        _ => ext_php_rs::exception::PhpException::default(msg),
    }
}

mod serde_defaults {
    pub fn acceleration_config_provider() -> String {
        "auto".to_string()
    }
    pub fn browser_config_capture_network_events() -> bool {
        false
    }
    pub fn browser_config_session_affinity() -> bool {
        true
    }
    pub fn chunking_config_max_characters() -> i64 {
        1000
    }
    pub fn chunking_config_overlap() -> i64 {
        200
    }
    pub fn chunking_config_trim() -> bool {
        true
    }
    pub fn chunking_config_table_chunking() -> String {
        "split".to_string()
    }
    pub fn content_config_output_format() -> String {
        "markdown".to_string()
    }
    pub fn content_config_preprocessing_preset() -> String {
        "standard".to_string()
    }
    pub fn content_config_remove_navigation() -> bool {
        true
    }
    pub fn content_config_remove_forms() -> bool {
        true
    }
    pub fn content_config_skip_images() -> bool {
        false
    }
    pub fn content_config_wrap() -> bool {
        false
    }
    pub fn content_config_wrap_width() -> i64 {
        80
    }
    pub fn content_config_include_document_structure() -> bool {
        true
    }
    pub fn content_filter_config_include_headers() -> bool {
        false
    }
    pub fn content_filter_config_include_footers() -> bool {
        false
    }
    pub fn content_filter_config_include_footnotes() -> bool {
        false
    }
    pub fn content_filter_config_strip_repeating_text() -> bool {
        true
    }
    pub fn content_filter_config_include_watermarks() -> bool {
        false
    }
    pub fn conversion_options_list_indent_width() -> i64 {
        2
    }
    pub fn conversion_options_bullets() -> String {
        "-*+".to_string()
    }
    pub fn conversion_options_escape_asterisks() -> bool {
        false
    }
    pub fn conversion_options_escape_underscores() -> bool {
        false
    }
    pub fn conversion_options_escape_misc() -> bool {
        false
    }
    pub fn conversion_options_escape_ascii() -> bool {
        false
    }
    pub fn conversion_options_code_language() -> String {
        "".to_string()
    }
    pub fn conversion_options_autolinks() -> bool {
        true
    }
    pub fn conversion_options_default_title() -> bool {
        false
    }
    pub fn conversion_options_br_in_tables() -> bool {
        false
    }
    pub fn conversion_options_compact_tables() -> bool {
        false
    }
    pub fn conversion_options_extract_metadata() -> bool {
        true
    }
    pub fn conversion_options_strip_newlines() -> bool {
        false
    }
    pub fn conversion_options_wrap() -> bool {
        false
    }
    pub fn conversion_options_wrap_width() -> i64 {
        80
    }
    pub fn conversion_options_convert_as_inline() -> bool {
        false
    }
    pub fn conversion_options_sub_symbol() -> String {
        "".to_string()
    }
    pub fn conversion_options_sup_symbol() -> String {
        "".to_string()
    }
    pub fn conversion_options_encoding() -> String {
        "utf-8".to_string()
    }
    pub fn conversion_options_debug() -> bool {
        false
    }
    pub fn conversion_options_skip_images() -> bool {
        false
    }
    pub fn conversion_options_include_document_structure() -> bool {
        false
    }
    pub fn conversion_options_extract_images() -> bool {
        false
    }
    pub fn conversion_options_max_image_size() -> i64 {
        5242880
    }
    pub fn conversion_options_capture_svg() -> bool {
        false
    }
    pub fn conversion_options_infer_dimensions() -> bool {
        true
    }
    pub fn conversion_options_tier_strategy() -> String {
        "auto".to_string()
    }
    pub fn crawl_config_respect_robots_txt() -> bool {
        false
    }
    pub fn crawl_config_soft_http_errors() -> bool {
        false
    }
    pub fn crawl_config_stay_on_domain() -> bool {
        false
    }
    pub fn crawl_config_allow_subdomains() -> bool {
        false
    }
    pub fn crawl_config_max_redirects() -> i64 {
        10
    }
    pub fn crawl_config_retry_count() -> i64 {
        0
    }
    pub fn crawl_config_cookies_enabled() -> bool {
        false
    }
    pub fn crawl_config_download_assets() -> bool {
        false
    }
    pub fn crawl_config_capture_screenshot() -> bool {
        false
    }
    pub fn crawl_config_follow_document_urls() -> bool {
        false
    }
    pub fn crawl_config_download_documents() -> bool {
        true
    }
    pub fn crawl_config_save_browser_profile() -> bool {
        false
    }
    pub fn crawl_config_ssrf() -> crate::SsrfPolicy {
        crawlberg::SsrfPolicy::from_env().into()
    }
    pub fn diff_options_include_metadata() -> bool {
        true
    }
    pub fn diff_options_include_embedded() -> bool {
        true
    }
    pub fn embedding_config_normalize() -> bool {
        true
    }
    pub fn embedding_config_batch_size() -> i64 {
        32
    }
    pub fn embedding_config_show_download_progress() -> bool {
        false
    }
    pub fn extraction_config_mime_detection_policy() -> String {
        "prefer_content".to_string()
    }
    pub fn extraction_config_use_cache() -> bool {
        true
    }
    pub fn extraction_config_enable_quality_processing() -> bool {
        true
    }
    pub fn extraction_config_force_ocr() -> bool {
        false
    }
    pub fn extraction_config_disable_ocr() -> bool {
        false
    }
    pub fn extraction_config_result_format() -> String {
        "unified".to_string()
    }
    pub fn extraction_config_output_format() -> String {
        "plain".to_string()
    }
    pub fn extraction_config_escape_markdown() -> bool {
        true
    }
    pub fn extraction_config_table_anchors() -> bool {
        false
    }
    pub fn extraction_config_jupyter_cell_rendering() -> String {
        "both".to_string()
    }
    pub fn extraction_config_apply_notebook_cell_tags() -> bool {
        true
    }
    pub fn extraction_config_use_layout_for_markdown() -> bool {
        false
    }
    pub fn extraction_config_include_document_structure() -> bool {
        false
    }
    pub fn extraction_config_max_archive_depth() -> i64 {
        3
    }
    pub fn footnote_config_parse_citations() -> bool {
        true
    }
    pub fn heuristics_config_enable_pdf_text_heuristics() -> bool {
        true
    }
    pub fn heuristics_config_text_layer_threshold() -> f32 {
        0.7
    }
    pub fn heuristics_config_file_size_threshold_bytes() -> i64 {
        10485760
    }
    pub fn heuristics_config_page_count_threshold() -> u32 {
        50
    }
    pub fn heuristics_config_target_pages_per_chunk() -> u32 {
        10
    }
    pub fn heuristics_config_max_pages_per_chunk() -> u32 {
        25
    }
    pub fn heuristics_config_disk_processing_threshold_bytes() -> i64 {
        52428800
    }
    pub fn heuristics_config_min_chars_per_page() -> u32 {
        50
    }
    pub fn heuristics_config_max_xlsx_sheet_count() -> u32 {
        200
    }
    pub fn heuristics_config_max_xlsx_workbook_cells() -> i64 {
        5000000
    }
    pub fn heuristics_config_max_pptx_embedded_count() -> u32 {
        50
    }
    pub fn hierarchy_config_enabled() -> bool {
        true
    }
    pub fn hierarchy_config_k_clusters() -> i64 {
        3
    }
    pub fn hierarchy_config_include_bbox() -> bool {
        true
    }
    pub fn html_output_config_theme() -> String {
        "unstyled".to_string()
    }
    pub fn html_output_config_class_prefix() -> String {
        "kb-".to_string()
    }
    pub fn html_output_config_embed_css() -> bool {
        true
    }
    pub fn image_extraction_config_extract_images() -> bool {
        true
    }
    pub fn image_extraction_config_target_dpi() -> i32 {
        300
    }
    pub fn image_extraction_config_max_image_dimension() -> i32 {
        4096
    }
    pub fn image_extraction_config_inject_placeholders() -> bool {
        true
    }
    pub fn image_extraction_config_auto_adjust_dpi() -> bool {
        true
    }
    pub fn image_extraction_config_min_dpi() -> i32 {
        72
    }
    pub fn image_extraction_config_max_dpi() -> i32 {
        600
    }
    pub fn image_extraction_config_classify() -> bool {
        false
    }
    pub fn image_extraction_config_include_page_rasters() -> bool {
        false
    }
    pub fn image_extraction_config_run_ocr_on_images() -> bool {
        true
    }
    pub fn image_extraction_config_ocr_text_only() -> bool {
        false
    }
    pub fn image_extraction_config_append_ocr_text() -> bool {
        false
    }
    pub fn image_extraction_config_include_data_base64() -> bool {
        false
    }
    pub fn image_preprocessing_config_target_dpi() -> i32 {
        300
    }
    pub fn image_preprocessing_config_auto_rotate() -> bool {
        false
    }
    pub fn image_preprocessing_config_deskew() -> bool {
        true
    }
    pub fn image_preprocessing_config_denoise() -> bool {
        false
    }
    pub fn image_preprocessing_config_contrast_enhance() -> bool {
        false
    }
    pub fn image_preprocessing_config_binarization_method() -> String {
        "otsu".to_string()
    }
    pub fn image_preprocessing_config_invert_colors() -> bool {
        false
    }
    pub fn keyword_config_max_keywords() -> i64 {
        10
    }
    pub fn keyword_config_min_score() -> f32 {
        0.0
    }
    pub fn language_detection_config_enabled() -> bool {
        true
    }
    pub fn language_detection_config_min_confidence() -> f64 {
        0.8
    }
    pub fn language_detection_config_detect_multiple() -> bool {
        false
    }
    pub fn late_interaction_config_batch_size() -> i64 {
        16
    }
    pub fn late_interaction_config_max_length() -> i64 {
        512
    }
    pub fn late_interaction_config_query_max_length() -> i64 {
        32
    }
    pub fn late_interaction_config_show_download_progress() -> bool {
        false
    }
    pub fn layout_detection_config_strategy() -> String {
        "always".to_string()
    }
    pub fn layout_detection_config_apply_heuristics() -> bool {
        true
    }
    pub fn layout_detection_config_table_model() -> String {
        "tatr".to_string()
    }
    pub fn layout_detection_config_table_overlap_preference() -> String {
        "content".to_string()
    }
    pub fn layout_detection_config_enable_chart_understanding() -> bool {
        false
    }
    pub fn multidoc_thresholds_density_shift_threshold() -> f32 {
        0.3
    }
    pub fn multidoc_thresholds_bigram_overlap_min() -> f32 {
        0.1
    }
    pub fn ner_config_backend() -> String {
        "onnx".to_string()
    }
    pub fn ngram_range_min() -> i64 {
        1
    }
    pub fn ngram_range_max() -> i64 {
        3
    }
    pub fn ocr_config_enabled() -> bool {
        true
    }
    pub fn ocr_config_backend() -> String {
        "tesseract".to_string()
    }
    pub fn ocr_config_auto_rotate() -> bool {
        false
    }
    pub fn ocr_element_level() -> String {
        "line".to_string()
    }
    pub fn ocr_element_page_number() -> u32 {
        1
    }
    pub fn ocr_element_config_min_level() -> String {
        "line".to_string()
    }
    pub fn ocr_quality_thresholds_min_total_non_whitespace() -> i64 {
        64
    }
    pub fn ocr_quality_thresholds_min_non_whitespace_per_page() -> f64 {
        32.0
    }
    pub fn ocr_quality_thresholds_min_meaningful_word_len() -> i64 {
        4
    }
    pub fn ocr_quality_thresholds_min_meaningful_words() -> i64 {
        3
    }
    pub fn ocr_quality_thresholds_min_alnum_ratio() -> f64 {
        0.3
    }
    pub fn ocr_quality_thresholds_min_garbage_chars() -> i64 {
        5
    }
    pub fn ocr_quality_thresholds_max_fragmented_word_ratio() -> f64 {
        0.6
    }
    pub fn ocr_quality_thresholds_critical_fragmented_word_ratio() -> f64 {
        0.8
    }
    pub fn ocr_quality_thresholds_max_ocr_output_fragmented_word_ratio() -> f64 {
        0.35
    }
    pub fn ocr_quality_thresholds_min_ocr_mean_confidence() -> f64 {
        75.0
    }
    pub fn ocr_quality_thresholds_min_words_for_ocr_output_check() -> i64 {
        20
    }
    pub fn ocr_quality_thresholds_max_ocr_output_dict_invalid_word_ratio() -> f64 {
        1.01
    }
    pub fn ocr_quality_thresholds_discard_suspected_ocr_noise() -> bool {
        false
    }
    pub fn ocr_quality_thresholds_min_avg_word_length() -> f64 {
        2.0
    }
    pub fn ocr_quality_thresholds_min_words_for_avg_length_check() -> i64 {
        50
    }
    pub fn ocr_quality_thresholds_min_consecutive_repeat_ratio() -> f64 {
        0.08
    }
    pub fn ocr_quality_thresholds_min_words_for_repeat_check() -> i64 {
        50
    }
    pub fn ocr_quality_thresholds_substantive_min_chars() -> i64 {
        100
    }
    pub fn ocr_quality_thresholds_non_text_min_chars() -> i64 {
        20
    }
    pub fn ocr_quality_thresholds_alnum_ws_ratio_threshold() -> f64 {
        0.4
    }
    pub fn ocr_quality_thresholds_pipeline_min_quality() -> f64 {
        0.5
    }
    pub fn ocr_quality_thresholds_min_undecodable_ratio() -> f64 {
        0.5
    }
    pub fn ocr_quality_thresholds_enable_provenance_ocr_routing() -> bool {
        true
    }
    pub fn ocr_quality_thresholds_min_provenance_fallback_ratio() -> f64 {
        0.5
    }
    pub fn paddle_ocr_config_language() -> String {
        "en".to_string()
    }
    pub fn paddle_ocr_config_use_angle_cls() -> bool {
        false
    }
    pub fn paddle_ocr_config_enable_table_detection() -> bool {
        false
    }
    pub fn paddle_ocr_config_det_db_thresh() -> f32 {
        0.3
    }
    pub fn paddle_ocr_config_det_db_box_thresh() -> f32 {
        0.5
    }
    pub fn paddle_ocr_config_det_db_unclip_ratio() -> f32 {
        1.6
    }
    pub fn paddle_ocr_config_det_limit_side_len() -> u32 {
        1024
    }
    pub fn paddle_ocr_config_rec_batch_num() -> u32 {
        6
    }
    pub fn paddle_ocr_config_padding() -> u32 {
        10
    }
    pub fn paddle_ocr_config_drop_score() -> f32 {
        0.5
    }
    pub fn paddle_ocr_config_model_tier() -> String {
        "mobile".to_string()
    }
    pub fn paddle_ocr_config_model_version() -> String {
        "pp-ocrv6".to_string()
    }
    pub fn page_config_extract_pages() -> bool {
        false
    }
    pub fn page_config_insert_page_markers() -> bool {
        false
    }
    pub fn page_config_marker_format() -> String {
        "\n\n<!-- PAGE {page_num} -->\n\n".to_string()
    }
    pub fn pdf_config_extract_images() -> bool {
        false
    }
    pub fn pdf_config_extract_tables() -> bool {
        true
    }
    pub fn pdf_config_extract_metadata() -> bool {
        true
    }
    pub fn pdf_config_extract_annotations() -> bool {
        false
    }
    pub fn pdf_config_allow_single_column_tables() -> bool {
        false
    }
    pub fn pdf_config_ocr_inline_images() -> bool {
        false
    }
    pub fn pdf_config_extract_form_fields() -> bool {
        true
    }
    pub fn pdf_config_reading_order() -> bool {
        false
    }
    pub fn pdf_config_backend() -> String {
        "native".to_string()
    }
    pub fn post_processor_config_enabled() -> bool {
        true
    }
    pub fn preprocessing_options_enabled() -> bool {
        true
    }
    pub fn preprocessing_options_remove_navigation() -> bool {
        true
    }
    pub fn preprocessing_options_remove_forms() -> bool {
        true
    }
    pub fn rake_params_min_word_length() -> i64 {
        1
    }
    pub fn rake_params_max_words_per_phrase() -> i64 {
        3
    }
    pub fn redaction_config_strategy() -> String {
        "mask".to_string()
    }
    pub fn redaction_config_preserve_offsets() -> bool {
        true
    }
    pub fn reranker_config_batch_size() -> i64 {
        32
    }
    pub fn reranker_config_show_download_progress() -> bool {
        false
    }
    pub fn security_limits_max_archive_size() -> i64 {
        524288000
    }
    pub fn security_limits_max_compression_ratio() -> i64 {
        100
    }
    pub fn security_limits_max_files_in_archive() -> i64 {
        10000
    }
    pub fn security_limits_max_nesting_depth() -> i64 {
        1024
    }
    pub fn security_limits_max_entity_length() -> i64 {
        1048576
    }
    pub fn security_limits_max_content_size() -> i64 {
        104857600
    }
    pub fn security_limits_max_iterations() -> i64 {
        10000000
    }
    pub fn security_limits_max_xml_depth() -> i64 {
        1024
    }
    pub fn security_limits_max_table_cells() -> i64 {
        100000
    }
    pub fn server_config_host() -> String {
        "127.0.0.1".to_string()
    }
    pub fn server_config_port() -> u16 {
        8000
    }
    pub fn server_config_max_request_body_bytes() -> i64 {
        104857600
    }
    pub fn server_config_max_multipart_field_bytes() -> i64 {
        104857600
    }
    pub fn sparse_embedding_config_batch_size() -> i64 {
        16
    }
    pub fn sparse_embedding_config_max_length() -> i64 {
        256
    }
    pub fn sparse_embedding_config_show_download_progress() -> bool {
        false
    }
    pub fn ssrf_policy_deny_private() -> bool {
        true
    }
    pub fn ssrf_policy_max_redirects() -> u8 {
        5
    }
    pub fn summarization_config_strategy() -> String {
        "extractive".to_string()
    }
    pub fn svg_options_sanitize() -> bool {
        true
    }
    pub fn svg_options_render_dpi() -> f32 {
        96.0
    }
    pub fn table_cell_row_span() -> u32 {
        1
    }
    pub fn table_cell_col_span() -> u32 {
        1
    }
    pub fn tesseract_config_output_format() -> String {
        "markdown".to_string()
    }
    pub fn tesseract_config_oem() -> i32 {
        3
    }
    pub fn tesseract_config_min_confidence() -> f64 {
        0.0
    }
    pub fn tesseract_config_enable_table_detection() -> bool {
        true
    }
    pub fn tesseract_config_table_min_confidence() -> f64 {
        0.0
    }
    pub fn tesseract_config_table_column_threshold() -> i32 {
        50
    }
    pub fn tesseract_config_table_row_threshold_ratio() -> f64 {
        0.5
    }
    pub fn tesseract_config_use_cache() -> bool {
        true
    }
    pub fn tesseract_config_classify_use_pre_adapted_templates() -> bool {
        true
    }
    pub fn tesseract_config_language_model_ngram_on() -> bool {
        true
    }
    pub fn tesseract_config_tessedit_dont_blkrej_good_wds() -> bool {
        true
    }
    pub fn tesseract_config_tessedit_dont_rowrej_good_wds() -> bool {
        true
    }
    pub fn tesseract_config_tessedit_enable_dict_correction() -> bool {
        true
    }
    pub fn tesseract_config_tessedit_char_whitelist() -> String {
        "".to_string()
    }
    pub fn tesseract_config_tessedit_char_blacklist() -> String {
        "".to_string()
    }
    pub fn tesseract_config_tessedit_use_primary_params_model() -> bool {
        true
    }
    pub fn tesseract_config_textord_space_size_is_variable() -> bool {
        true
    }
    pub fn tesseract_config_thresholding_method() -> bool {
        false
    }
    pub fn token_reduction_config_preserve_markdown() -> bool {
        false
    }
    pub fn token_reduction_config_preserve_code() -> bool {
        true
    }
    pub fn token_reduction_config_semantic_threshold() -> f32 {
        0.3
    }
    pub fn token_reduction_config_enable_parallel() -> bool {
        true
    }
    pub fn token_reduction_config_use_simd() -> bool {
        true
    }
    pub fn token_reduction_config_enable_semantic_clustering() -> bool {
        false
    }
    pub fn token_reduction_config_preserve_important_words() -> bool {
        true
    }
    pub fn token_reduction_options_mode() -> String {
        "off".to_string()
    }
    pub fn token_reduction_options_preserve_important_words() -> bool {
        true
    }
    pub fn transcription_config_enabled() -> bool {
        true
    }
    pub fn transcription_config_model() -> String {
        "tiny".to_string()
    }
    pub fn transcription_config_timestamps() -> bool {
        false
    }
    pub fn transcription_config_allow_network() -> bool {
        true
    }
    pub fn transcription_config_verify_hash() -> bool {
        false
    }
    pub fn tree_sitter_config_enabled() -> bool {
        true
    }
    pub fn tree_sitter_process_config_structure() -> bool {
        true
    }
    pub fn tree_sitter_process_config_imports() -> bool {
        true
    }
    pub fn tree_sitter_process_config_exports() -> bool {
        true
    }
    pub fn tree_sitter_process_config_comments() -> bool {
        false
    }
    pub fn tree_sitter_process_config_docstrings() -> bool {
        false
    }
    pub fn tree_sitter_process_config_symbols() -> bool {
        false
    }
    pub fn tree_sitter_process_config_diagnostics() -> bool {
        false
    }
    pub fn tree_sitter_process_config_data_extraction() -> bool {
        false
    }
    pub fn tree_sitter_process_config_content_mode() -> String {
        "chunks".to_string()
    }
    pub fn url_extraction_config_allow_local_file_inputs() -> bool {
        true
    }
    pub fn url_extraction_config_allow_file_uris() -> bool {
        true
    }
    pub fn yake_params_window_size() -> i64 {
        2
    }
}

static __EXT_PHP_RS_MODULE_STARTUP: ::std::sync::Mutex<::std::option::Option<::ext_php_rs::builders::ModuleStartup>> =
    ::std::sync::Mutex::new(::std::option::Option::None);

unsafe extern "C" fn __ext_php_rs_module_startup(ty: i32, mod_num: i32) -> i32 {
    let startup = match __EXT_PHP_RS_MODULE_STARTUP.lock() {
        Ok(mut guard) => guard.take(),
        Err(_) => return 1,
    };
    match startup {
        Some(s) => match s.startup(ty, mod_num) {
            Ok(_) => 0,
            Err(_) => 1,
        },
        None => 1,
    }
}

#[doc(hidden)]
#[unsafe(no_mangle)]
pub extern "C" fn get_module() -> *mut ::ext_php_rs::zend::ModuleEntry {
    static __EXT_PHP_RS_MODULE_ENTRY: ::ext_php_rs::zend::StaticModuleEntry =
        ::ext_php_rs::zend::StaticModuleEntry::new();
    __EXT_PHP_RS_MODULE_ENTRY.get_or_init(|| {
        let builder =
            ::ext_php_rs::builders::ModuleBuilder::new("xberg", "1.1.6").startup_function(__ext_php_rs_module_startup);
        let builder = builder
            .class::<AccelerationConfig>()
            .class::<ArchiveEntry>()
            .class::<ArchiveMetadata>()
            .class::<Attributes>()
            .class::<AudioMetadata>()
            .class::<BBox>()
            .class::<BedrockConfig>()
            .class::<BibtexMetadata>()
            .class::<BoundingBox>()
            .class::<BrowserConfig>()
            .class::<CacheStats>()
            .class::<CaptioningConfig>()
            .class::<CellChange>()
            .class::<Chunk>()
            .class::<ChunkClassificationConfig>()
            .class::<ChunkClassificationDefinition>()
            .class::<ChunkClassificationEnrichmentConfig>()
            .class::<ChunkInfo>()
            .class::<ChunkMetadata>()
            .class::<ChunkingConfig>()
            .class::<Citation>()
            .class::<CitationMetadata>()
            .class::<ClassificationLabel>()
            .class::<CodeChunkInfo>()
            .class::<CodeDataAttribute>()
            .class::<CodeDataNode>()
            .class::<CodeMetadata>()
            .class::<ConcurrencyConfig>()
            .class::<ContentConfig>()
            .class::<ContentFilterConfig>()
            .class::<ContributorRole>()
            .class::<ConversionOptions>()
            .class::<CoreProperties>()
            .class::<CrawlConfig>()
            .class::<CsvConfig>()
            .class::<CsvMetadata>()
            .class::<DbfFieldInfo>()
            .class::<DbfMetadata>()
            .class::<DeepseekOcrBackendOptions>()
            .class::<DetectResponse>()
            .class::<DetectionResult>()
            .class::<DiffHunk>()
            .class::<DiffOptions>()
            .class::<DjotAttributeGroup>()
            .class::<DjotContent>()
            .class::<DjotImage>()
            .class::<DjotLink>()
            .class::<DoctorCheck>()
            .class::<DoctorReport>()
            .class::<DocumentBoundary>()
            .class::<DocumentCounts>()
            .class::<DocumentMetadata>()
            .class::<DocumentNode>()
            .class::<DocumentRelationship>()
            .class::<DocumentRevision>()
            .class::<DocumentStructure>()
            .class::<DocumentSummary>()
            .class::<DocxAppProperties>()
            .class::<DocxMetadata>()
            .class::<Element>()
            .class::<ElementMetadata>()
            .class::<EmailAttachment>()
            .class::<EmailConfig>()
            .class::<EmailExtractionResult>()
            .class::<EmailMetadata>()
            .class::<EmbeddedChanges>()
            .class::<EmbeddedDiff>()
            .class::<EmbeddedFile>()
            .class::<EmbeddingConfig>()
            .class::<Entity>()
            .class::<EpubMetadata>()
            .class::<ErrorMetadata>()
            .class::<ExcelMetadata>()
            .class::<ExcelSheet>()
            .class::<ExcelWorkbook>()
            .class::<ExtractInput>()
            .class::<ExtractedDocument>()
            .class::<ExtractedImage>()
            .class::<ExtractedUri>()
            .class::<ExtractionConfidence>()
            .class::<ExtractionConfig>()
            .class::<ExtractionDiff>()
            .class::<ExtractionErrorItem>()
            .class::<ExtractionResult>()
            .class::<ExtractionSummary>()
            .class::<FictionBookMetadata>()
            .class::<FileExtractionConfig>()
            .class::<Footnote>()
            .class::<FootnoteAnchor>()
            .class::<FootnoteConfig>()
            .class::<FootnoteDefinition>()
            .class::<FormattedBlock>()
            .class::<Formula>()
            .class::<GeoJsonExtractionConfig>()
            .class::<GlmOcrBackendOptions>()
            .class::<GridCell>()
            .class::<HeaderMetadata>()
            .class::<HeadingContext>()
            .class::<HeadingLevel>()
            .class::<HeuristicsConfig>()
            .class::<HierarchicalBlock>()
            .class::<HierarchicalBoundingBox>()
            .class::<HierarchyConfig>()
            .class::<HtmlMetadata>()
            .class::<HtmlOutputConfig>()
            .class::<ImageDimensions>()
            .class::<ImageDpi>()
            .class::<ImageExtractionConfig>()
            .class::<ImageMetadata>()
            .class::<ImageMetadataType>()
            .class::<ImagePreprocessingConfig>()
            .class::<ImagePreprocessingMetadata>()
            .class::<InlineElement>()
            .class::<JatsMetadata>()
            .class::<KeyValueAttribute>()
            .class::<Keyword>()
            .class::<KeywordConfig>()
            .class::<LanguageConfidence>()
            .class::<LanguageDetectionConfig>()
            .class::<LateInteractionConfig>()
            .class::<LateInteractionMatch>()
            .class::<LateInteractionPreset>()
            .class::<LayoutDetection>()
            .class::<LayoutDetectionConfig>()
            .class::<LayoutRegion>()
            .class::<LinkMetadata>()
            .class::<LlmBudgetConfig>()
            .class::<LlmCacheConfig>()
            .class::<LlmConfig>()
            .class::<LlmProviderConfig>()
            .class::<LlmRateLimitConfig>()
            .class::<LlmUsage>()
            .class::<MapResult>()
            .class::<MarkdownCodeBlock>()
            .class::<MarkdownLink>()
            .class::<MetaSchema>()
            .class::<Metadata>()
            .class::<ModelPaths>()
            .class::<MultiVectorEmbedding>()
            .class::<MultidocInput>()
            .class::<MultidocThresholds>()
            .class::<NerConfig>()
            .class::<NgramRange>()
            .class::<OcrConfidence>()
            .class::<OcrConfig>()
            .class::<OcrElement>()
            .class::<OcrElementConfig>()
            .class::<OcrExtractionResult>()
            .class::<OcrMetadata>()
            .class::<OcrPipelineConfig>()
            .class::<OcrPipelineStage>()
            .class::<OcrPoint>()
            .class::<OcrQualityThresholds>()
            .class::<OcrRotation>()
            .class::<OcrTable>()
            .class::<OcrTableBoundingBox>()
            .class::<OrientationResult>()
            .class::<PaddleOcrConfig>()
            .class::<PaddleOcrVlBackendOptions>()
            .class::<PageBoundary>()
            .class::<PageClassification>()
            .class::<PageClassificationConfig>()
            .class::<PageConfig>()
            .class::<PageContent>()
            .class::<PageDimensions>()
            .class::<PageHierarchy>()
            .class::<PageInfo>()
            .class::<PageOcrConfidence>()
            .class::<PageRange>()
            .class::<PageSignals>()
            .class::<PageSpan>()
            .class::<PageStructure>()
            .class::<PatternMatch>()
            .class::<PdfAnnotation>()
            .class::<PdfConfig>()
            .class::<PdfFormField>()
            .class::<PdfMetadata>()
            .class::<PixelDimensions>()
            .class::<PostProcessorConfig>()
            .class::<PptxAppProperties>()
            .class::<PptxExtractionResult>()
            .class::<PptxMetadata>()
            .class::<PreprocessingOptions>()
            .class::<PresentationHyperlink>()
            .class::<Preset>()
            .class::<PresetSample>()
            .class::<PresetSummary>()
            .class::<ProcessingWarning>()
            .class::<PropertyChange>()
            .class::<ProxyConfig>()
            .class::<PstMetadata>()
            .class::<QrBoundingBox>()
            .class::<QrCode>()
            .class::<RakeParams>()
            .class::<RecognizedTable>()
            .class::<RedactionConfig>()
            .class::<RedactionFinding>()
            .class::<RedactionPattern>()
            .class::<RedactionReport>()
            .class::<RedactionTerm>()
            .class::<Registry>()
            .class::<RerankedDocument>()
            .class::<RerankerConfig>()
            .class::<ResolvedPreset>()
            .class::<RevisionDelta>()
            .class::<SecurityLimits>()
            .class::<ServerConfig>()
            .class::<SitemapUrl>()
            .class::<SparseEmbedding>()
            .class::<SparseEmbeddingConfig>()
            .class::<SparseEmbeddingPreset>()
            .class::<SsrfPolicy>()
            .class::<StructuredData>()
            .class::<StructuredDataResult>()
            .class::<StructuredExtractionConfig>()
            .class::<SummarizationConfig>()
            .class::<SupportedFormat>()
            .class::<SvgOptions>()
            .class::<Table>()
            .class::<TableCell>()
            .class::<TableCellStyle>()
            .class::<TableDiff>()
            .class::<TableGrid>()
            .class::<TesseractConfig>()
            .class::<TextAnnotation>()
            .class::<TextExtractionResult>()
            .class::<TextMetadata>()
            .class::<TokenCounter>()
            .class::<TokenReductionConfig>()
            .class::<TokenReductionOptions>()
            .class::<TranscriptionConfig>()
            .class::<Translation>()
            .class::<TranslationConfig>()
            .class::<TreeSitterConfig>()
            .class::<TreeSitterProcessConfig>()
            .class::<TrocrBackendOptions>()
            .class::<UrlExtractionConfig>()
            .class::<UserChunkConfig>()
            .class::<XlsxAppProperties>()
            .class::<XmlExtractionResult>()
            .class::<XmlMetadata>()
            .class::<YakeParams>()
            .class::<YearRange>()
            .class::<XbergApi>()
            .class::<AnnotationKind>()
            .class::<AssetCategory>()
            .class::<AuthConfig>()
            .class::<BlockType>()
            .class::<BoundaryReason>()
            .class::<BrowserBackend>()
            .class::<BrowserMode>()
            .class::<BrowserWait>()
            .class::<CallMode>()
            .class::<CandleDevicePreference>()
            .class::<CandleTrocrVariant>()
            .class::<ChunkSizing>()
            .class::<ChunkType>()
            .class::<ChunkerType>()
            .class::<ChunkingReason>()
            .class::<CodeBlockStyle>()
            .class::<CodeContentMode>()
            .class::<CodeDataNodeKind>()
            .class::<ConfidenceSemantics>()
            .class::<ContentFilterKind>()
            .class::<ContentLayer>()
            .class::<CrawlStrategyKind>()
            .class::<CredentialProviderConfig>()
            .class::<DiffLine>()
            .class::<DocumentContentEncoding>()
            .class::<ElementType>()
            .class::<EmbeddingModelType>()
            .class::<EmbeddingsEmbeddingBackend>()
            .class::<EntityCategory>()
            .class::<ExecutionProviderType>()
            .class::<ExtractInputKind>()
            .class::<ExtractionMethod>()
            .class::<FormFieldType>()
            .class::<FormatMetadata>()
            .class::<FormulaModel>()
            .class::<GlmOcrLayoutMode>()
            .class::<GlmOcrTaskKind>()
            .class::<HeadingStyle>()
            .class::<HighlightStyle>()
            .class::<HostMatcher>()
            .class::<HtmlTheme>()
            .class::<HtmlToMarkdownRsOutputFormat>()
            .class::<ImageKind>()
            .class::<ImageOutputFormat>()
            .class::<ImageType>()
            .class::<InlineType>()
            .class::<JupyterCellRendering>()
            .class::<KeywordAlgorithm>()
            .class::<LateInteractionModelType>()
            .class::<LayoutClass>()
            .class::<LayoutStrategy>()
            .class::<LinkStyle>()
            .class::<LinkType>()
            .class::<ListIndentType>()
            .class::<ListType>()
            .class::<MergeMode>()
            .class::<MimeDetectionPolicy>()
            .class::<NerBackendKind>()
            .class::<NewlineStyle>()
            .class::<NoChunkingReason>()
            .class::<NodeContent>()
            .class::<OcrBackendType>()
            .class::<OcrBoundingGeometry>()
            .class::<OcrElementLevel>()
            .class::<OcrStrategy>()
            .class::<OutputFormat>()
            .class::<PSMMode>()
            .class::<PaddleInferenceBackend>()
            .class::<PaddleLanguage>()
            .class::<PaddleOcrVlTaskKind>()
            .class::<PageOrientationHandling>()
            .class::<PageUnitType>()
            .class::<PdfAnnotationType>()
            .class::<PdfBackend>()
            .class::<PiiCategory>()
            .class::<PreprocessingPreset>()
            .class::<PresetCategory>()
            .class::<ProbeStatus>()
            .class::<ProcessingStage>()
            .class::<RedactionStrategy>()
            .class::<ReductionLevel>()
            .class::<RegionKind>()
            .class::<RelationshipKind>()
            .class::<RerankerHead>()
            .class::<RerankerModelType>()
            .class::<ResultFormat>()
            .class::<RevisionAnchor>()
            .class::<RevisionKind>()
            .class::<SchemaCompliance>()
            .class::<SparseEmbeddingModelType>()
            .class::<StructuredDataType>()
            .class::<SummaryStrategy>()
            .class::<TableChunkingMode>()
            .class::<TableModel>()
            .class::<TableOverlapPreference>()
            .class::<TextDirection>()
            .class::<TierStrategy>()
            .class::<UriKind>()
            .class::<UrlEscapeStyle>()
            .class::<UrlExtractionMode>()
            .class::<VlmFallbackPolicy>()
            .class::<WhisperModel>()
            .class::<WhitespaceMode>();
        match builder.try_into() {
            Ok((entry, startup)) => {
                *__EXT_PHP_RS_MODULE_STARTUP
                    .lock()
                    .expect("module startup mutex poisoned") = Some(startup);
                entry
            }
            Err(e) => panic!("Failed to build PHP module: {:?}", e),
        }
    })
}
