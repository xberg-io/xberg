// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:8c08fe2cb014af307e65893a1c8bf374c5a3a0d576c27587282a6344b7a0b585
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables)]
#![allow(unsafe_code)]
#![allow(missing_docs)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::map_identity,
    clippy::just_underscores_and_digits,
    clippy::unnecessary_cast,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::redundant_field_names,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::arc_with_non_send_sync,
    clippy::collapsible_if,
    clippy::clone_on_copy,
    clippy::should_implement_trait
)]
#![allow(
    clippy::cast_possible_wrap,
    clippy::cast_possible_truncation,
    clippy::cast_sign_loss,
    clippy::default_trait_access,
    clippy::useless_conversion,
    clippy::unsafe_derive_deserialize,
    clippy::must_use_candidate,
    clippy::return_self_not_must_use,
    clippy::use_self,
    clippy::missing_const_for_fn,
    clippy::missing_errors_doc,
    clippy::needless_pass_by_value,
    clippy::doc_markdown,
    clippy::derive_partial_eq_without_eq,
    clippy::uninlined_format_args,
    clippy::redundant_clone,
    clippy::implicit_clone,
    clippy::redundant_closure_for_method_calls,
    clippy::wildcard_imports,
    clippy::option_if_let_else,
    clippy::too_many_lines
)]

use napi::JsValue;
use napi::bindgen_prelude::{JsObjectValue, Object, ToNapiValue, Unknown};
use napi::*;
use napi_derive::napi;
use std::collections::HashMap;
use std::sync::Arc;
use std::sync::Mutex;
use tokio_util::sync::CancellationToken;
use xberg::Validator;
use xberg::engine::seams::PresetResolver;

static WORKER_POOL: std::sync::LazyLock<tokio::runtime::Runtime> = std::sync::LazyLock::new(|| {
    // 16 MB worker stack: a deep consumer future (e.g. a multi-stage OCR pipeline) overflows the
    // default (~2 MB) worker stack and aborts the process with SIGBUS.
    tokio::runtime::Builder::new_multi_thread()
        .enable_all()
        .thread_stack_size(16 * 1024 * 1024)
        .build()
        .expect("Failed to create Tokio runtime")
});

/// Wrapper for byte arrays that implements custom FromNapiValue to accept Buffer.from(...).
///
/// NAPI v3's default FromNapiValue for `Vec<u8>` expects `Array<number>`, not Buffer.
/// This wrapper provides custom deserialization that accepts Buffer, Uint8Array, or Array,
/// converting them to `Vec<u8>`. Implements Clone and serde traits for use in struct fields.
#[derive(Clone, Debug, Default, serde::Serialize, serde::Deserialize)]
pub struct JsBytes(pub Vec<u8>);

impl From<Vec<u8>> for JsBytes {
    fn from(v: Vec<u8>) -> Self {
        JsBytes(v)
    }
}

impl From<JsBytes> for Vec<u8> {
    fn from(js_bytes: JsBytes) -> Self {
        js_bytes.0
    }
}

impl AsRef<[u8]> for JsBytes {
    fn as_ref(&self) -> &[u8] {
        &self.0
    }
}

impl std::ops::Deref for JsBytes {
    type Target = Vec<u8>;
    fn deref(&self) -> &Self::Target {
        &self.0
    }
}

impl std::ops::DerefMut for JsBytes {
    fn deref_mut(&mut self) -> &mut Self::Target {
        &mut self.0
    }
}

impl napi::bindgen_prelude::FromNapiValue for JsBytes {
    unsafe fn from_napi_value(env: napi::sys::napi_env, napi_val: napi::sys::napi_value) -> napi::Result<Self> {
        use napi::bindgen_prelude::FromNapiValue;

        // Try Buffer first (most common for binary data in JS)
        if let Ok(buffer) = unsafe { napi::bindgen_prelude::Buffer::from_napi_value(env, napi_val) } {
            return Ok(JsBytes(buffer.as_ref().to_vec()));
        }

        // Try Uint8Array
        if let Ok(ua) = unsafe { napi::bindgen_prelude::Uint8Array::from_napi_value(env, napi_val) } {
            return Ok(JsBytes(ua.to_vec()));
        }

        // Fall back to Array[number]
        if let Ok(vec) = unsafe { Vec::<u8>::from_napi_value(env, napi_val) } {
            return Ok(JsBytes(vec));
        }

        Err(napi::Error::new(
            napi::Status::InvalidArg,
            "Expected Buffer, Uint8Array, or Array<number> for bytes field",
        ))
    }
}

impl napi::bindgen_prelude::ToNapiValue for JsBytes {
    unsafe fn to_napi_value(env: napi::sys::napi_env, val: Self) -> napi::Result<napi::sys::napi_value> {
        // Delegate to Vec<u8>'s implementation (which returns an Uint8Array/Buffer).
        unsafe { <Vec<u8> as napi::bindgen_prelude::ToNapiValue>::to_napi_value(env, val.0) }
    }
}

/// Wrapper for trait visitor types (napi::Object<'static>) that implements Clone.
///
/// Object is not Clone. This wrapper uses Arc<Object<'static>> internally for cheap cloning.
/// The .inner field is public for compatibility with generated code that needs to access
/// the underlying Object for trait dispatch.
pub struct JsVisitorRef {
    pub inner: std::sync::Arc<napi::bindgen_prelude::Object<'static>>,
}

impl Clone for JsVisitorRef {
    fn clone(&self) -> Self {
        JsVisitorRef {
            inner: std::sync::Arc::clone(&self.inner),
        }
    }
}

#[allow(clippy::arc_with_non_send_sync)]
impl From<napi::bindgen_prelude::Object<'static>> for JsVisitorRef {
    fn from(visitor: napi::bindgen_prelude::Object<'static>) -> Self {
        JsVisitorRef {
            inner: std::sync::Arc::new(visitor),
        }
    }
}

impl From<JsVisitorRef> for napi::bindgen_prelude::Object<'static> {
    fn from(visitor_ref: JsVisitorRef) -> Self {
        // Object<'static> is Copy (it just holds an env+handle pair), so deref directly.
        *visitor_ref.inner
    }
}

/// Hardware acceleration configuration for ONNX Runtime models.
///
/// Controls which execution provider (CPU, CoreML, CUDA, TensorRT) is used
/// for inference in layout detection and embedding generation.
///
/// # Example
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "AccelerationConfig")]
pub struct JsAccelerationConfig {
    /// Execution provider to use for ONNX inference.
    #[napi(ts_type = "ExecutionProviderType | 'auto' | 'cpu' | 'coreml' | 'cuda' | 'tensorrt'")]
    pub provider: Option<JsExecutionProviderType>,
    /// GPU device ID (for CUDA/TensorRT). Ignored for CPU/CoreML/Auto.
    #[napi(js_name = "deviceId")]
    pub device_id: Option<u32>,
}
impl<'de> serde::Deserialize<'de> for JsAccelerationConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::AccelerationConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A single file extracted from an archive.
///
/// When archives (ZIP, TAR, 7Z, GZIP) are extracted with recursive extraction
/// enabled, each processable file produces its own full `ExtractedDocument`.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ArchiveEntry")]
pub struct JsArchiveEntry {
    /// Archive-relative file path (e.g. "folder/document.pdf").
    pub path: String,
    /// Detected MIME type of the file.
    #[napi(js_name = "mimeType")]
    pub mime_type: String,
    /// Full extraction result for this file.
    pub result: JsExtractedDocument,
}

/// Archive (ZIP/TAR/7Z) metadata.
///
/// Extracted from compressed archive files containing file lists and size information.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ArchiveMetadata")]
pub struct JsArchiveMetadata {
    /// Archive format ("ZIP", "TAR", "7Z", etc.)
    pub format: Option<String>,
    /// Total number of files in the archive
    #[napi(js_name = "fileCount")]
    pub file_count: Option<u32>,
    /// List of file paths within the archive
    #[napi(js_name = "fileList")]
    pub file_list: Option<Vec<String>>,
    /// Total uncompressed size in bytes
    #[napi(js_name = "totalSize")]
    pub total_size: Option<i64>,
    /// Compressed size in bytes (if available)
    #[napi(js_name = "compressedSize")]
    pub compressed_size: Option<i64>,
}

/// Element attributes in Djot.
///
/// Represents the attributes attached to elements using {.class #id key="value"} syntax.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Attributes")]
pub struct JsAttributes {
    /// Element ID (#identifier)
    pub id: Option<String>,
    /// CSS classes (.class1 .class2)
    pub classes: Option<Vec<String>>,
    /// Key-value pairs (key="value")
    #[napi(js_name = "keyValues")]
    pub key_values: Option<Vec<JsKeyValueAttribute>>,
}
impl<'de> serde::Deserialize<'de> for JsAttributes {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Attributes as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Audio/video file metadata.
///
/// Populated from container tags (ID3v2, MP4 atoms, Vorbis comments, etc.) and
/// PCM decode properties. Available when the `transcription-types` feature is enabled.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "AudioMetadata")]
pub struct JsAudioMetadata {
    /// Duration in milliseconds derived from the decoded audio stream.
    #[napi(js_name = "durationMs")]
    pub duration_ms: Option<i64>,
    /// Audio codec (e.g. "mp3", "aac", "opus", "flac").
    pub codec: Option<String>,
    /// Container format (e.g. "mpeg", "mp4", "ogg", "wav").
    pub container: Option<String>,
    /// Sample rate in Hz after decode (always 16000 when resampled for Whisper).
    #[napi(js_name = "sampleRateHz")]
    pub sample_rate_hz: Option<u32>,
    /// Number of audio channels (1 = mono, 2 = stereo).
    pub channels: Option<u16>,
    /// Audio bitrate in kbps from the source file tags/properties.
    pub bitrate: Option<u32>,
}

/// Bounding box in original image coordinates (x1, y1) top-left, (x2, y2) bottom-right.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "BBox")]
pub struct JsBBox {
    /// Left edge (x-coordinate of the top-left corner).
    pub x1: f64,
    /// Top edge (y-coordinate of the top-left corner).
    pub y1: f64,
    /// Right edge (x-coordinate of the bottom-right corner).
    pub x2: f64,
    /// Bottom edge (y-coordinate of the bottom-right corner).
    pub y2: f64,
}

/// AWS Bedrock configuration for `bedrock/`-prefixed models.
///
/// Mirrors liter-llm's `BedrockConfig`. Every field is optional: anything left
/// unset falls back to the standard AWS environment variables
/// (`AWS_DEFAULT_REGION` / `AWS_REGION`, `AWS_ACCESS_KEY_ID`,
/// `AWS_SECRET_ACCESS_KEY`, `AWS_SESSION_TOKEN`, `BEDROCK_CROSS_REGION`) and the
/// default AWS credential chain. Leave the credential fields unset unless you
/// have an explicit reason to pin them.
///
/// # Example
///
/// ```toml
/// [ocr.vlm_config]
/// model = "bedrock/anthropic.claude-3-sonnet-20240229-v1:0"
///
/// [ocr.vlm_config.bedrock]
/// region = "eu-central-1"
/// cross_region_prefix = "eu"
/// ```
///
/// `Debug` is implemented by hand so the three credential fields are never printed.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "BedrockConfig")]
pub struct JsBedrockConfig {
    /// AWS region (e.g. `"us-east-1"`).
    pub region: Option<String>,
    /// Cross-region inference profile prefix (e.g. `"us"`).
    #[napi(js_name = "crossRegionPrefix")]
    pub cross_region_prefix: Option<String>,
    /// Explicit AWS access key ID. Secret — never logged.
    #[napi(js_name = "accessKeyId")]
    pub access_key_id: Option<String>,
    /// Explicit AWS secret access key. Secret — never logged.
    #[napi(js_name = "secretAccessKey")]
    pub secret_access_key: Option<String>,
    /// Explicit AWS session token for temporary credentials. Secret — never logged.
    #[napi(js_name = "sessionToken")]
    pub session_token: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsBedrockConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::BedrockConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// BibTeX bibliography metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "BibtexMetadata")]
pub struct JsBibtexMetadata {
    /// Number of entries in the bibliography.
    #[napi(js_name = "entryCount")]
    pub entry_count: Option<i64>,
    /// BibTeX citation keys (e.g. `"knuth1984"`) for all entries.
    #[napi(js_name = "citationKeys")]
    pub citation_keys: Option<Vec<String>>,
    /// Author names collected across all bibliography entries.
    pub authors: Option<Vec<String>>,
    /// Earliest and latest publication years found in the bibliography.
    #[napi(js_name = "yearRange")]
    pub year_range: Option<JsYearRange>,
    /// Count of entries grouped by BibTeX entry type (e.g. `"article"` → 5).
    #[napi(js_name = "entryTypes")]
    pub entry_types: Option<HashMap<String, i64>>,
}
impl<'de> serde::Deserialize<'de> for JsBibtexMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::BibtexMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Bounding box coordinates for element positioning.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "BoundingBox")]
pub struct JsBoundingBox {
    /// Left x-coordinate
    pub x0: Option<f64>,
    /// Bottom y-coordinate
    pub y0: Option<f64>,
    /// Right x-coordinate
    pub x1: Option<f64>,
    /// Top y-coordinate
    pub y1: Option<f64>,
}

/// Browser fallback configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "BrowserConfig")]
pub struct JsBrowserConfig {
    /// When to use the headless browser fallback.
    #[napi(ts_type = "BrowserMode | 'auto' | 'always' | 'never' | 'stealth'")]
    pub mode: Option<JsBrowserMode>,
    /// Browser backend used to render JavaScript-heavy pages.
    #[napi(ts_type = "BrowserBackend | 'chromiumoxide' | 'native'")]
    pub backend: Option<JsBrowserBackend>,
    /// CDP WebSocket endpoint for connecting to an external browser instance.
    pub endpoint: Option<String>,
    /// Timeout for browser page load and rendering (in milliseconds when serialized).
    pub timeout: Option<i64>,
    /// Wait strategy after browser navigation.
    #[napi(ts_type = "BrowserWait | 'network_idle' | 'selector' | 'fixed'")]
    pub wait: Option<JsBrowserWait>,
    /// CSS selector to wait for when `wait` is `Selector`.
    #[napi(js_name = "waitSelector")]
    pub wait_selector: Option<String>,
    /// Extra time to wait after the wait condition is met.
    #[napi(js_name = "extraWait")]
    pub extra_wait: Option<i64>,
    /// Proxy for browser fetches. Overrides `CrawlConfig.proxy` when set.
    /// Native backend supports http/https only (no SOCKS5).
    pub proxy: Option<JsProxyConfig>,
    /// URL patterns to block before the network request fires. Supports `*`
    /// wildcards. Useful for skipping ads/analytics/large images. Honored by
    /// `BrowserBackend.Native`; chromiumoxide ignores this field today.
    #[napi(js_name = "blockUrlPatterns")]
    pub block_url_patterns: Option<Vec<String>>,
    /// JavaScript snippet evaluated after navigation completes.
    ///
    /// Scraping captures the native backend result in `ScrapeResult.browser.eval_result`.
    /// Interactions run this script before page actions on both browser backends but do
    /// not include the script result in `InteractionResult`.
    #[napi(js_name = "evalScript")]
    pub eval_script: Option<String>,
    /// User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent`
    /// (or crawlberg's default) if unset. Native only.
    #[napi(js_name = "robotsUserAgent")]
    pub robots_user_agent: Option<String>,
    /// Capture the full network event stream into the result. Default false
    /// (only the document event is captured). Native only.
    #[napi(js_name = "captureNetworkEvents")]
    pub capture_network_events: Option<bool>,
    /// Enable session affinity: reuse chromiumoxide Pages for same-domain
    /// requests so cookies + fingerprint + solved challenges persist.
    /// Default: true. When false, each request gets a fresh Page.
    #[napi(js_name = "sessionAffinity")]
    pub session_affinity: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsBrowserConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <crawlberg::BrowserConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Aggregate statistics for a xberg cache directory.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CacheStats")]
pub struct JsCacheStats {
    /// Total number of files currently in the cache directory.
    #[napi(js_name = "totalFiles")]
    pub total_files: i64,
    /// Combined size of all cache files in megabytes.
    #[napi(js_name = "totalSizeMb")]
    pub total_size_mb: f64,
    /// Free disk space available on the cache volume, in megabytes.
    #[napi(js_name = "availableSpaceMb")]
    pub available_space_mb: f64,
    /// Age of the oldest cache file in days (0.0 if the cache is empty).
    #[napi(js_name = "oldestFileAgeDays")]
    pub oldest_file_age_days: f64,
    /// Age of the most recently written cache file in days (0.0 if the cache is empty).
    #[napi(js_name = "newestFileAgeDays")]
    pub newest_file_age_days: f64,
}

/// Configuration for the VLM captioning post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "CaptioningConfig")]
pub struct JsCaptioningConfig {
    /// LLM configuration used for the VLM call.
    pub llm: JsLlmConfig,
    /// Optional custom caption prompt. `None` uses the default `RegionKind.Caption`
    /// prompt that ships with `crate.llm.region_extractor`.
    pub prompt: Option<String>,
    /// Skip images whose `width * height` is below this threshold (in pixels).
    /// Default `1_000` filters out icons and decorations.
    #[napi(js_name = "minImageArea")]
    pub min_image_area: u32,
}
impl<'de> serde::Deserialize<'de> for JsCaptioningConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::CaptioningConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A single changed cell within a table.
///
/// Defined here (rather than only in `crate.diff`) so `RevisionDelta` can
/// reference it unconditionally, without requiring the `diff` Cargo feature.
/// `crate.diff` re-exports this type verbatim.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CellChange")]
pub struct JsCellChange {
    /// Zero-based row index.
    pub row: i64,
    /// Zero-based column index.
    pub col: i64,
    /// Value before the change.
    pub from: String,
    /// Value after the change.
    pub to: String,
}

/// A text chunk with optional embedding and metadata.
///
/// Chunks are created when chunking is enabled in `ExtractionConfig`. Each chunk
/// contains the text content, optional embedding vector (if embedding generation
/// is configured), and metadata about its position in the document.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Chunk")]
pub struct JsChunk {
    /// The text content of this chunk.
    pub content: String,
    /// Semantic structural classification of this chunk.
    ///
    /// Assigned by the heuristic classifier based on content patterns and
    /// heading context. Defaults to `ChunkType.Unknown` when no rule matches.
    #[napi(
        js_name = "chunkType",
        ts_type = "ChunkType | 'heading' | 'party_list' | 'definitions' | 'operative_clause' | 'signature_block' | 'schedule' | 'table_like' | 'formula' | 'code_block' | 'function' | 'class' | 'module' | 'image' | 'org_chart' | 'diagram' | 'unknown'"
    )]
    pub chunk_type: JsChunkType,
    /// Optional embedding vector for this chunk.
    ///
    /// Only populated when `EmbeddingConfig` is provided in chunking configuration.
    /// The dimensionality depends on the chosen embedding model.
    pub embedding: Option<Vec<f64>>,
    /// Optional sparse (SPLADE) learned embedding for this chunk.
    ///
    /// Only populated when sparse-embedding generation is configured for chunking.
    /// `None` otherwise, including on builds without the `sparse-embeddings` feature.
    ///
    /// Uses the crate-root `SparseEmbedding` alias rather than
    /// `crate.sparse_embeddings.SparseEmbedding` directly: the `sparse_embeddings`
    /// module itself only compiles under `sparse-embeddings`/`sparse-embedding-presets`,
    /// while the crate-root alias is always defined (a field-compatible stub on builds
    /// without either feature), so this field — and `Chunk` itself — compiles on every
    /// feature combination, including the crate's default features.
    #[napi(js_name = "sparseEmbedding")]
    pub sparse_embedding: Option<JsSparseEmbedding>,
    /// Optional ColBERT-style multi-vector (late-interaction) embedding for this chunk.
    ///
    /// Only populated when late-interaction embedding generation is configured for
    /// chunking. `None` otherwise, including on builds without the `late-interaction`
    /// feature.
    ///
    /// Uses the crate-root `MultiVectorEmbedding` alias for the same reason
    /// `sparse_embedding` uses `SparseEmbedding` — see that field's docs.
    #[napi(js_name = "lateInteraction")]
    pub late_interaction: Option<JsMultiVectorEmbedding>,
    /// Metadata about this chunk's position and properties.
    pub metadata: JsChunkMetadata,
}
impl<'de> serde::Deserialize<'de> for JsChunk {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Chunk as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for the chunk-classification post-processor.
///
/// Chunk classification is always multi-label: a chunk may match zero, one, or
/// many of the configured definitions. This is the chunk-level equivalent of
/// `PageClassificationConfig`, but scoped to individual chunks
/// (`ExtractedDocument.chunks`) rather than whole pages, and built for large
/// taxonomies where each label needs its own description rather than a bare name.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ChunkClassificationConfig")]
pub struct JsChunkClassificationConfig {
    /// Minijinja prompt template. Receives `{{ definitions }}` (rendered label +
    /// description list) and `{{ chunks }}` (a numbered list of chunk texts in
    /// the current batch) variables. `None` lets the backend pick a sensible
    /// default.
    #[napi(js_name = "promptTemplate")]
    pub prompt_template: Option<String>,
    /// The set of label definitions the classifier may emit. Must contain at
    /// least one entry.
    pub definitions: Vec<JsChunkClassificationDefinition>,
    /// LLM configuration used for classification.
    pub llm: JsLlmConfig,
    /// Number of chunks batched into a single LLM request.
    ///
    /// Larger batches amortize the fixed prompt cost (definitions block) across
    /// more chunks, at the risk of exceeding the model's context window for
    /// very large taxonomies or chunk texts. Defaults to `DEFAULT_BATCH_SIZE`.
    #[napi(js_name = "batchSize")]
    pub batch_size: i64,
    /// Maximum number of in-flight batch requests.
    ///
    /// Bounds concurrency against the configured LLM provider. Defaults to
    /// `DEFAULT_MAX_CONCURRENCY`.
    #[napi(js_name = "maxConcurrency")]
    pub max_concurrency: i64,
}
impl<'de> serde::Deserialize<'de> for JsChunkClassificationConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ChunkClassificationConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A single labeled definition the chunk classifier may emit.
///
/// Unlike `PageClassificationConfig.labels` (bare label names), chunk
/// classification targets potentially large domain taxonomies where every
/// label carries its own semantic description, letting the LLM disambiguate
/// similarly named labels without relying on the label string alone.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ChunkClassificationDefinition")]
pub struct JsChunkClassificationDefinition {
    /// Label name returned in `ChunkMetadata.classifications`.
    pub label: String,
    /// Semantic description of when this label applies. Injected verbatim into
    /// the classification prompt next to the label name.
    pub description: String,
}

/// Chunk-classification enrichment knob: how to multi-label individual chunks.
///
/// Operates on `ExtractedDocument.chunks` in place — the caller must have
/// already produced chunks (e.g. via `ExtractionConfig.chunking`) for this
/// stage to have any effect; a document with no chunks is a no-op.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ChunkClassificationEnrichmentConfig")]
pub struct JsChunkClassificationEnrichmentConfig {
    /// Label-definition set and LLM/batching settings for the chunk-classification stage.
    pub config: JsChunkClassificationConfig,
}

/// Information about a single chunk.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ChunkInfo")]
pub struct JsChunkInfo {
    /// Zero-based chunk index.
    pub index: u32,
    /// Page range for this chunk.
    pub pages: JsPageRange,
    /// Estimated processing time for this chunk in milliseconds.
    #[napi(js_name = "estimatedTimeMs")]
    pub estimated_time_ms: i64,
}

/// Metadata about a chunk's position in the original document.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ChunkMetadata")]
pub struct JsChunkMetadata {
    /// Byte offset where this chunk starts in the original text (UTF-8 valid boundary).
    #[napi(js_name = "byteStart")]
    pub byte_start: i64,
    /// Byte offset where this chunk ends in the original text (UTF-8 valid boundary).
    #[napi(js_name = "byteEnd")]
    pub byte_end: i64,
    /// Number of tokens in this chunk (if available).
    ///
    /// This is calculated by the embedding model's tokenizer if embeddings are enabled.
    #[napi(js_name = "tokenCount")]
    pub token_count: Option<i64>,
    /// Zero-based index of this chunk in the document.
    #[napi(js_name = "chunkIndex")]
    pub chunk_index: i64,
    /// Total number of chunks in the document.
    #[napi(js_name = "totalChunks")]
    pub total_chunks: i64,
    /// First page number this chunk spans (1-indexed).
    ///
    /// Only populated when page tracking is enabled in extraction configuration.
    #[napi(js_name = "firstPage")]
    pub first_page: Option<u32>,
    /// Last page number this chunk spans (1-indexed, equal to first_page for single-page chunks).
    ///
    /// Only populated when page tracking is enabled in extraction configuration.
    #[napi(js_name = "lastPage")]
    pub last_page: Option<u32>,
    /// Heading context when using Markdown chunker.
    ///
    /// Contains the heading hierarchy this chunk falls under.
    /// Only populated when `ChunkerType.Markdown` is used.
    #[napi(js_name = "headingContext")]
    pub heading_context: Option<JsHeadingContext>,
    /// Flattened heading trail from document root to this chunk's section.
    ///
    /// Each element is a heading's text, outermost first. Derived from
    /// `heading_context` when present; empty otherwise.
    /// Provides a binding-friendly, RAG-shaped breadcrumb without requiring
    /// callers to walk the nested `HeadingContext` structure.
    #[napi(js_name = "headingPath")]
    pub heading_path: Vec<String>,
    /// Indices into `ExtractedDocument.images` for images on pages covered by this chunk.
    ///
    /// Contains zero-based indices into the top-level `images` collection for every
    /// image whose `page_number` falls within `[first_page, last_page]`.
    /// Empty when image extraction is disabled or the chunk spans no pages with images.
    #[napi(js_name = "imageIndices")]
    pub image_indices: Vec<u32>,
    /// Ids of the `DocumentNode`s
    /// this chunk was derived from.
    ///
    /// Joins a chunk back to the structured document tree via
    /// `DocumentNode.id`.
    /// Populated from exact node provenance when available, with a textual
    /// containment fallback for rendered chunks that do not retain byte offsets.
    #[napi(js_name = "nodeIds")]
    pub node_ids: Vec<String>,
    /// Per-page bounding-box spans this chunk covers, for viewer highlighting (#1295).
    ///
    /// One entry per page the chunk overlaps, in page order — the first and last entries'
    /// `page` fields equal `first_page`/`last_page`.
    /// Populated whenever page-boundary provenance is available (the same condition under
    /// which `first_page`/`last_page` are populated); each entry's `bbox` is additionally
    /// populated when the document's structured node tree (`ExtractedDocument.document`) is
    /// available, as the union of that page's body-layer node bounding boxes found within this
    /// chunk. Empty when page-boundary provenance is unavailable (mirrors `first_page`/
    /// `last_page` being `None`).
    #[napi(js_name = "pageSpans")]
    pub page_spans: Vec<JsPageSpan>,
    /// Multi-label classification result for this chunk.
    ///
    /// Populated by the chunk-classification post-processor when
    /// `ExtractionConfig.chunk_classification`
    /// is set. A chunk may match zero, one, or many of the configured label
    /// definitions. Empty when chunk classification was not configured.
    pub classifications: Vec<JsClassificationLabel>,
}
impl<'de> serde::Deserialize<'de> for JsChunkMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ChunkMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Chunking configuration.
///
/// Configures text chunking for document content, including chunk size,
/// overlap, trimming behavior, and optional embeddings.
///
/// Use `..Default.default()` when constructing to allow for future field additions:
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ChunkingConfig")]
pub struct JsChunkingConfig {
    /// Maximum size per chunk (in units determined by `sizing`).
    ///
    /// When `sizing` is `Characters` (default), this is the max character count.
    /// When using token-based sizing, this is the max token count.
    ///
    /// Default: 1000
    #[napi(js_name = "maxCharacters")]
    #[serde(rename = "max_chars")]
    pub max_characters: Option<i64>,
    /// Overlap between chunks (in units determined by `sizing`).
    ///
    /// Default: 200
    #[serde(rename = "max_overlap")]
    pub overlap: Option<i64>,
    /// Whether to trim whitespace from chunk boundaries.
    ///
    /// Default: true
    pub trim: Option<bool>,
    /// Type of chunker to use (Text or Markdown).
    ///
    /// Default: Text
    #[napi(
        js_name = "chunkerType",
        ts_type = "ChunkerType | 'text' | 'markdown' | 'yaml' | 'semantic'"
    )]
    pub chunker_type: Option<JsChunkerType>,
    /// Optional embedding configuration for chunk embeddings.
    pub embedding: Option<JsEmbeddingConfig>,
    /// Optional sparse (SPLADE) embedding configuration for chunk embeddings.
    ///
    /// When set, sparse vectors are generated for each chunk's content and attached
    /// via `sparse_embedding`. Requires the `sparse-embeddings`
    /// feature; without it, a warning is emitted and no sparse vectors are attached.
    ///
    /// Config-file only: like `RerankerConfig` and the local-ONNX branch of
    /// `embedding`, this has no CLI flag and no environment variable. Only the secret/identity
    /// fields of LLM-routed configs (model, API key, base URL) get that reach.
    #[napi(js_name = "sparseEmbedding")]
    pub sparse_embedding: Option<JsSparseEmbeddingConfig>,
    /// Optional late-interaction (ColBERT) embedding configuration for chunk embeddings.
    ///
    /// When set, multi-vector embeddings are generated for each chunk's content and
    /// attached via `late_interaction`. Requires the
    /// `late-interaction` feature; without it, a warning is emitted and no
    /// late-interaction vectors are attached.
    ///
    /// Config-file only, for the same reason as `sparse_embedding` above.
    #[napi(js_name = "lateInteraction")]
    pub late_interaction: Option<JsLateInteractionConfig>,
    /// Use a preset configuration (overrides individual settings if provided).
    pub preset: Option<String>,
    /// How to measure chunk size.
    ///
    /// Default: `Characters` (Unicode character count).
    /// Enable `chunking-tiktoken` or `chunking-tokenizers` features for token-based sizing.
    pub sizing: Option<JsChunkSizing>,
    /// Optional cosine similarity threshold for semantic topic boundary detection.
    ///
    /// Only used when `chunker_type` is `Semantic` and an `EmbeddingConfig` is
    /// provided. You almost never need to set this. When omitted, defaults to
    /// `0.75` which works well for most documents. Lower values detect more
    /// topic boundaries (more, smaller chunks); higher values detect fewer.
    /// Range: `0.0..=1.0`.
    #[napi(js_name = "topicThreshold")]
    pub topic_threshold: Option<f64>,
    /// How to handle markdown tables that exceed the chunk size limit.
    ///
    /// Only applies when `chunker_type` is `Markdown`.
    ///
    /// * `Split` (default) — tables are split at row boundaries; continuation
    ///   chunks do not repeat the header.
    /// * `RepeatHeader` — the table header row and separator are prepended to
    ///   every continuation chunk so each chunk is self-contained.
    ///
    /// Default: `Split`
    #[napi(js_name = "tableChunking", ts_type = "TableChunkingMode | 'split' | 'repeat_header'")]
    pub table_chunking: Option<JsTableChunkingMode>,
}
impl<'de> serde::Deserialize<'de> for JsChunkingConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ChunkingConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "chunkingConfigDefault")]
pub fn chunking_config_default() -> JsChunkingConfig {
    xberg::ChunkingConfig::default().into()
}

/// A structured citation from a citation block.
///
/// Parsed from entries like:
/// `[^srcN]: source, locator, excerpt: "text"`
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "Citation")]
pub struct JsCitation {
    /// The label of the citation (e.g., "src1" in `[^src1]: ...`).
    pub label: String,
    /// The source reference (path, URL, or identifier).
    pub source: String,
    /// Optional locator within the source (e.g., "page 3" or "section 2.1").
    pub locator: Option<String>,
    /// Optional excerpt — quoted text from the source.
    pub excerpt: Option<String>,
}

/// Citation file metadata (RIS, PubMed, EndNote).
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "CitationMetadata")]
pub struct JsCitationMetadata {
    /// Total number of citation records in the file.
    #[napi(js_name = "citationCount")]
    pub citation_count: Option<i64>,
    /// Detected citation file format (e.g. `"ris"`, `"pubmed"`, `"endnote"`).
    pub format: Option<String>,
    /// Author names collected across all citation records.
    pub authors: Option<Vec<String>>,
    /// Earliest and latest publication years found in the file.
    #[napi(js_name = "yearRange")]
    pub year_range: Option<JsYearRange>,
    /// DOI identifiers found in the citation records.
    pub dois: Option<Vec<String>>,
    /// Keywords collected from all citation records.
    pub keywords: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsCitationMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::CitationMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A single label + confidence pair.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ClassificationLabel")]
pub struct JsClassificationLabel {
    /// Label name as configured in `PageClassificationConfig.labels`.
    pub label: String,
    /// Backend-reported confidence in `[0.0, 1.0]`. `None` when the backend (e.g. an LLM
    /// prompt without explicit confidence schema) did not report one.
    pub confidence: Option<f64>,
}

/// A single structurally-meaningful code chunk produced by tree-sitter parsing.
///
/// Purpose-built payload owned by xberg — deliberately does not expose the upstream
/// `tree_sitter_language_pack` types, so binding generators never need to resolve an
/// external crate's types across FFI/language boundaries.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CodeChunkInfo")]
pub struct JsCodeChunkInfo {
    /// The raw source text of this chunk.
    pub text: String,
    /// Hierarchical path of enclosing structural items (e.g. `["MyClass", "my_method"]`).
    #[napi(js_name = "contextPath")]
    pub context_path: Vec<String>,
    /// Tree-sitter node kinds that appear at the top level of this chunk (e.g.
    /// `"function_definition"`, `"class_definition"`).
    #[napi(js_name = "nodeTypes")]
    pub node_types: Vec<String>,
    /// Inclusive start byte offset of this chunk in the original source.
    #[napi(js_name = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of this chunk in the original source.
    #[napi(js_name = "byteEnd")]
    pub byte_end: i64,
}

/// An XML-style attribute attached to an `Element` node.
///
/// Populated only for `CodeDataNodeKind.Element`; always empty for `KeyValue` and
/// `Sequence` nodes.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CodeDataAttribute")]
pub struct JsCodeDataAttribute {
    /// Attribute name (e.g. `"class"`, `"href"`).
    pub name: String,
    /// Attribute value as a raw string (quotes stripped).
    pub value: String,
    /// Inclusive start byte offset of the `name="value"` attribute token.
    #[napi(js_name = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of the `name="value"` attribute token.
    #[napi(js_name = "byteEnd")]
    pub byte_end: i64,
}

/// A node in the hierarchical data tree produced by data-format extraction.
///
/// Purpose-built payload owned by xberg — mirrors
/// `tree_sitter_language_pack.DataNode` but flattens its `Span` down to plain byte
/// offsets, so binding generators never need to resolve an external crate's types
/// across FFI/language boundaries.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "CodeDataNode")]
pub struct JsCodeDataNode {
    /// Whether this node is a key/value pair, XML element, or sequence item.
    #[napi(ts_type = "CodeDataNodeKind | 'key_value' | 'element' | 'sequence'")]
    pub kind: JsCodeDataNodeKind,
    /// Key, attribute name, tag name, or positional index (`"0"`, `"1"`, …).
    /// `None` at the document root.
    pub key: Option<String>,
    /// Leaf scalar value, if any. `None` for containers (objects, arrays, XML
    /// elements with child elements).
    pub value: Option<String>,
    /// Attributes on element-shape nodes (XML `STag` attributes). Empty for all
    /// other kinds.
    pub attributes: Vec<JsCodeDataAttribute>,
    /// Children for nested containers and XML element bodies.
    pub children: Vec<JsCodeDataNode>,
    /// Inclusive start byte offset of this node in the original source.
    #[napi(js_name = "byteStart")]
    pub byte_start: i64,
    /// Exclusive end byte offset of this node in the original source.
    #[napi(js_name = "byteEnd")]
    pub byte_end: i64,
}
impl<'de> serde::Deserialize<'de> for JsCodeDataNode {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::CodeDataNode as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Code-format metadata: the structural chunks produced by tree-sitter parsing.
///
/// Wrapped by `FormatMetadata.Code`. Kept as a named struct (rather than an inline
/// enum-variant body) so serde can tag it under internal tagging and utoipa can emit a
/// referenceable `CodeMetadata` component in the OpenAPI schema.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "CodeMetadata")]
pub struct JsCodeMetadata {
    /// Structural code chunks (function/class/module boundaries).
    pub chunks: Option<Vec<JsCodeChunkInfo>>,
    /// Hierarchical key/value data tree extracted from data-format source
    /// (JSON, YAML, TOML, XML, CSV, etc.), when data extraction was enabled.
    pub data: Option<JsCodeDataNode>,
}
impl<'de> serde::Deserialize<'de> for JsCodeMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::CodeMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Controls thread usage for constrained environments.
///
/// Set `max_threads` to cap all internal thread pools (Rayon, ONNX Runtime
/// intra-op) and batch concurrency to a single limit.
///
/// # Default budget when `max_threads` is unset
///
/// Without an explicit `max_threads`, the effective budget is
/// `min(detected_cpu_cores, 8)` — a deliberate ceiling chosen for
/// serverless/shared-tenant defaults, not a full-host auto-scale. On a host
/// with more than 8 cores this means the extra cores go **unused** unless one
/// of the following applies:
///
/// - `max_threads` is set explicitly above 8 (the only way to exceed the
///   ceiling on a bare-metal or VM host with no CPU quota).
/// - The process runs under a Linux cgroup CPU quota (containers, Kubernetes
///   `resources.limits.cpu`); in that case the quota itself is used as the
///   ceiling instead of the hardcoded 8, since the quota already reflects a
///   deliberately-configured resource limit.
///
/// When neither applies and the host has more than 8 cores, a single
/// `WARN`-level log is emitted the first time the budget is resolved,
/// naming the detected core count and the applied cap, so the ceiling is
/// discoverable without reading source.
///
/// # Example
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ConcurrencyConfig")]
pub struct JsConcurrencyConfig {
    /// Maximum number of threads for all internal thread pools.
    ///
    /// Caps Rayon global pool size, ONNX Runtime intra-op threads, and the
    /// combined document/inner-task budget for batch extraction. When `None`,
    /// the effective budget is `min(detected_cpu_cores, 8)` unless a Linux
    /// cgroup CPU quota is present, in which case the quota is used as the
    /// ceiling instead. On hosts with more than 8 cores and no cgroup quota,
    /// set `max_threads` explicitly to use the additional cores — the
    /// default will not scale past 8 on its own.
    #[napi(js_name = "maxThreads")]
    pub max_threads: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsConcurrencyConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ConcurrencyConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Content extraction and conversion configuration.
///
/// Controls how HTML is converted to the output format. Uses
/// html-to-markdown-rs as the conversion engine for all formats
/// (markdown, plain text, djot).
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ContentConfig")]
pub struct JsContentConfig {
    /// Output format: `"markdown"` (default), `"plain"`, `"djot"`.
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<String>,
    /// Preprocessing aggressiveness: `"minimal"`, `"standard"` (default), `"aggressive"`.
    ///
    /// - Minimal: only scripts/styles removed.
    /// - Standard: also removes nav, nav-hinted headers/footers/asides, forms.
    /// - Aggressive: removes all footers/asides unconditionally.
    #[napi(js_name = "preprocessingPreset")]
    pub preprocessing_preset: Option<String>,
    /// Remove navigation elements (nav, breadcrumbs, menus). Default: `true`.
    #[napi(js_name = "removeNavigation")]
    pub remove_navigation: Option<bool>,
    /// Remove form elements. Default: `true`.
    #[napi(js_name = "removeForms")]
    pub remove_forms: Option<bool>,
    /// HTML tag names to strip (render children only, remove the tag wrapper).
    /// Default: `[]`.
    #[napi(js_name = "stripTags")]
    pub strip_tags: Option<Vec<String>>,
    /// HTML tag names to preserve as raw HTML in output.
    #[napi(js_name = "preserveTags")]
    pub preserve_tags: Option<Vec<String>>,
    /// CSS selectors for elements to exclude entirely (element + all content).
    ///
    /// Unlike `strip_tags` (which removes the wrapper but keeps children),
    /// excluded elements and all descendants are dropped. Supports CSS selectors:
    /// `.class`, `#id`, `[attribute]`, compound selectors.
    ///
    /// Default: `["noscript"]`. `<noscript>` fallback content (no-JS notices,
    /// tracking pixels, GTM iframes) is meant for browsers with JavaScript
    /// disabled, not for a markdown reader, and `strip_tags` cannot drop it —
    /// on `preprocessing_preset: "standard"` (crawlberg's only path) it only
    /// removes the wrapper and still renders the children.
    ///
    /// Example: `[".cookie-banner", "#ad-container", "[role='complementary']"]`
    #[napi(js_name = "excludeSelectors")]
    pub exclude_selectors: Option<Vec<String>>,
    /// Skip image elements in output. Default: `false`.
    #[napi(js_name = "skipImages")]
    pub skip_images: Option<bool>,
    /// Max DOM traversal depth. Prevents stack overflow on deeply nested HTML.
    #[napi(js_name = "maxDepth")]
    pub max_depth: Option<i64>,
    /// Enable line wrapping. Default: `false`.
    pub wrap: Option<bool>,
    /// Wrap width when `wrap` is enabled. Default: `80`.
    #[napi(js_name = "wrapWidth")]
    pub wrap_width: Option<i64>,
    /// Include document structure tree in output. Default: `true`.
    #[napi(js_name = "includeDocumentStructure")]
    pub include_document_structure: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsContentConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <crawlberg::ContentConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Cross-extractor content filtering configuration.
///
/// Controls whether "furniture" content (headers, footers, page numbers,
/// watermarks, repeating text) is included in or stripped from extraction
/// results. Applies across all extractors (PDF, DOCX, RTF, ODT, HTML, etc.)
/// with format-specific implementation.
///
/// When `None` on `ExtractionConfig`, each extractor uses its current
/// default behavior unchanged.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ContentFilterConfig")]
pub struct JsContentFilterConfig {
    /// Include running headers in extraction output.
    ///
    /// - PDF: Disables top-margin furniture stripping and prevents the layout
    ///   model from treating `PageHeader`-classified regions as furniture.
    /// - DOCX: Includes document headers in text output.
    /// - RTF/ODT: Headers already included; this is a no-op when true.
    /// - HTML/EPUB: Keeps `<header>` element content.
    ///
    /// Default: `false` (headers are stripped or excluded).
    #[napi(js_name = "includeHeaders")]
    pub include_headers: Option<bool>,
    /// Include running footers in extraction output.
    ///
    /// - PDF: Disables bottom-margin furniture stripping and prevents the layout
    ///   model from treating `PageFooter`-classified regions as furniture.
    /// - DOCX: Includes document footers in text output.
    /// - RTF/ODT: Footers already included; this is a no-op when true.
    /// - HTML/EPUB: Keeps `<footer>` element content.
    ///
    /// Default: `false` (footers are stripped or excluded).
    #[napi(js_name = "includeFooters")]
    pub include_footers: Option<bool>,
    /// Include footnote bodies in extraction output.
    ///
    /// - PDF: Prevents the layout model from treating `Footnote`-classified
    ///   regions as furniture, so footnote bodies survive alongside the main
    ///   text instead of being silently dropped.
    /// - Other formats: No effect currently.
    ///
    /// Default: `false` (footnotes are stripped), matching the existing
    /// `include_headers` / `include_footers` defaults.
    #[napi(js_name = "includeFootnotes")]
    pub include_footnotes: Option<bool>,
    /// Enable the heuristic cross-page repeating text detector.
    ///
    /// When `true` (default), text that repeats verbatim across a supermajority
    /// of pages is classified as furniture and stripped.  Disable this if brand
    /// names or repeated headings are being incorrectly removed by the heuristic.
    ///
    /// This flag also gates a same-page rule: a body paragraph is removed when
    /// its text is also carried by a table detected on the same page (catches
    /// table content that PDF extraction renders both as a table and as body
    /// text). The comparison preserves case and never touches headings, list
    /// items, code blocks, formulas, or captions, so it cannot delete a body
    /// sentence merely for repeating an earlier heading's words (GH#1623).
    ///
    /// Note: when a layout-detection model is active, the model may independently
    /// classify page-header / page-footer / footnote regions as furniture on a
    /// per-page basis. To preserve those regions, set `include_headers = true`,
    /// `include_footers = true`, `include_footnotes = true`, or any combination,
    /// in addition to disabling this flag.
    ///
    /// Primarily affects PDF extraction.
    ///
    /// Default: `true`.
    #[napi(js_name = "stripRepeatingText")]
    pub strip_repeating_text: Option<bool>,
    /// Include watermark text in extraction output.
    ///
    /// - PDF: Keeps watermark artifacts and arXiv identifiers.
    /// - Other formats: No effect currently.
    ///
    /// Default: `false` (watermarks are stripped).
    #[napi(js_name = "includeWatermarks")]
    pub include_watermarks: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsContentFilterConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ContentFilterConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "contentFilterConfigDefault")]
pub fn content_filter_config_default() -> JsContentFilterConfig {
    xberg::ContentFilterConfig::default().into()
}

/// JATS contributor with role.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ContributorRole")]
pub struct JsContributorRole {
    /// Contributor display name.
    pub name: String,
    /// Contributor role (e.g. `"author"`, `"editor"`).
    pub role: Option<String>,
}

/// Main conversion options for HTML to Markdown conversion.
///
/// Use `ConversionOptions.builder()` to construct, or `Default.default()` for defaults.
///
/// # Example
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ConversionOptions")]
pub struct JsConversionOptions {
    /// Heading style to use in Markdown output (ATX `#` or Setext underline).
    #[napi(
        js_name = "headingStyle",
        ts_type = "HeadingStyle | 'Underlined' | 'Atx' | 'AtxClosed'"
    )]
    pub heading_style: Option<JsHeadingStyle>,
    /// How to indent nested list items (spaces or tab).
    #[napi(js_name = "listIndentType", ts_type = "ListIndentType | 'Spaces' | 'Tabs'")]
    pub list_indent_type: Option<JsListIndentType>,
    /// Number of spaces (or tabs) to use for each level of list indentation.
    #[napi(js_name = "listIndentWidth")]
    pub list_indent_width: Option<i64>,
    /// Bullet character(s) to use for unordered list items (e.g. `"-"`, `"*"`).
    pub bullets: Option<String>,
    /// Character used for bold/italic emphasis markers (`*` or `_`).
    #[napi(js_name = "strongEmSymbol")]
    pub strong_em_symbol: Option<String>,
    /// Escape `*` characters in plain text to avoid unintended bold/italic.
    #[napi(js_name = "escapeAsterisks")]
    pub escape_asterisks: Option<bool>,
    /// Escape `_` characters in plain text to avoid unintended bold/italic.
    #[napi(js_name = "escapeUnderscores")]
    pub escape_underscores: Option<bool>,
    /// Escape miscellaneous Markdown metacharacters (`[]()#` etc.) in plain text.
    #[napi(js_name = "escapeMisc")]
    pub escape_misc: Option<bool>,
    /// Escape ASCII characters that have special meaning in certain Markdown dialects.
    #[napi(js_name = "escapeAscii")]
    pub escape_ascii: Option<bool>,
    /// Default language annotation for fenced code blocks that have no language hint.
    #[napi(js_name = "codeLanguage")]
    pub code_language: Option<String>,
    /// Automatically convert bare URLs into Markdown autolinks.
    pub autolinks: Option<bool>,
    /// Emit a default title when no `<title>` tag is present.
    #[napi(js_name = "defaultTitle")]
    pub default_title: Option<bool>,
    /// Render `<br>` elements inside table cells as literal line breaks.
    #[napi(js_name = "brInTables")]
    pub br_in_tables: Option<bool>,
    /// Emit tables without column padding (compact GFM format).
    ///
    /// When `true`, column widths are not computed and cells are emitted with
    /// no trailing spaces. Separator rows use exactly `---` per column.
    /// Produces token-efficient output suitable for RAG / LLM contexts.
    ///
    /// Default `false` (aligned padding preserved).
    #[napi(js_name = "compactTables")]
    pub compact_tables: Option<bool>,
    /// Style used for `<mark>` / highlighted text (e.g. `==text==`).
    #[napi(
        js_name = "highlightStyle",
        ts_type = "HighlightStyle | 'DoubleEqual' | 'Html' | 'Bold' | 'None'"
    )]
    pub highlight_style: Option<JsHighlightStyle>,
    /// Populate `result.metadata` with `<head>` / `<meta>` extraction
    /// (title, description, Open Graph, Twitter Card, JSON-LD, …).
    ///
    /// Default `true`. Disabling skips the metadata pass only — table
    /// extraction into `result.tables` runs unconditionally.
    #[napi(js_name = "extractMetadata")]
    pub extract_metadata: Option<bool>,
    /// Controls how whitespace sequences are normalised in the converted output.
    ///
    /// - `WhitespaceMode.Normalized` (default) — collapses consecutive whitespace characters
    ///   (spaces, tabs, newlines) to a single space, matching browser rendering behaviour.
    /// - `WhitespaceMode.Strict` — preserves all whitespace exactly as it appears in the
    ///   source HTML, including runs of spaces and embedded newlines.
    ///
    /// Choose `Strict` only when the source HTML uses deliberate whitespace (e.g. pre-formatted
    /// content outside `<pre>` tags). For most documents `Normalized` produces cleaner output.
    #[napi(js_name = "whitespaceMode", ts_type = "WhitespaceMode | 'Normalized' | 'Strict'")]
    pub whitespace_mode: Option<JsWhitespaceMode>,
    /// Strip all newlines from the output, producing a single-line result.
    #[napi(js_name = "stripNewlines")]
    pub strip_newlines: Option<bool>,
    /// Wrap long lines at `wrap_width` characters.
    pub wrap: Option<bool>,
    /// Maximum output line width in characters when `wrap` is `true` (default `80`).
    ///
    /// Lines are broken at word boundaries so that no line exceeds this length. A value of `0`
    /// is treated as "no limit" — equivalent to leaving `wrap` disabled. Has no
    /// effect when `wrap` is `false`.
    #[napi(js_name = "wrapWidth")]
    pub wrap_width: Option<i64>,
    /// Treat the entire document as inline content (no block-level wrappers).
    #[napi(js_name = "convertAsInline")]
    pub convert_as_inline: Option<bool>,
    /// Markdown notation for subscript text (e.g. `"~"`).
    #[napi(js_name = "subSymbol")]
    pub sub_symbol: Option<String>,
    /// Markdown notation for superscript text (e.g. `"^"`).
    #[napi(js_name = "supSymbol")]
    pub sup_symbol: Option<String>,
    /// How to encode hard line breaks (`<br>`) in Markdown.
    #[napi(js_name = "newlineStyle", ts_type = "NewlineStyle | 'Spaces' | 'Backslash'")]
    pub newline_style: Option<JsNewlineStyle>,
    /// Style used for fenced code blocks (backticks or tilde).
    #[napi(
        js_name = "codeBlockStyle",
        ts_type = "CodeBlockStyle | 'Indented' | 'Backticks' | 'Tildes'"
    )]
    pub code_block_style: Option<JsCodeBlockStyle>,
    /// HTML tag names whose `<img>` children are kept inline instead of block.
    #[napi(js_name = "keepInlineImagesIn")]
    pub keep_inline_images_in: Option<Vec<String>>,
    /// Options for the HTML pre-processing pass applied before conversion begins.
    ///
    /// Pre-processing runs before the HTML is handed to the converter and can perform operations
    /// such as unwrapping redundant wrapper elements, removing tracking pixels, and normalising
    /// vendor-specific markup. See `PreprocessingOptions` for the full set of knobs.
    ///
    /// Defaults to `PreprocessingOptions.default()`, which enables the standard cleaning
    /// passes. Set individual fields on `PreprocessingOptions` (or construct via
    /// `ConversionOptions.builder`) to opt in or out of specific passes.
    pub preprocessing: Option<JsPreprocessingOptions>,
    /// Expected character encoding of the input HTML (default `"utf-8"`).
    pub encoding: Option<String>,
    /// Emit debug information during conversion.
    pub debug: Option<bool>,
    /// HTML tag names whose content is stripped from the output entirely.
    #[napi(js_name = "stripTags")]
    pub strip_tags: Option<Vec<String>>,
    /// HTML tag names that are preserved verbatim in the output.
    #[napi(js_name = "preserveTags")]
    pub preserve_tags: Option<Vec<String>>,
    /// Skip conversion of `<img>` elements (omit images from output).
    #[napi(js_name = "skipImages")]
    pub skip_images: Option<bool>,
    /// URL encoding strategy for link and image destinations.
    ///
    /// Controls how special characters in URL destinations are escaped:
    /// - `UrlEscapeStyle.Angle` (default) — wraps the destination in angle brackets when it
    ///   contains spaces or newlines. parsers misinterpret `>` inside such a destination.
    /// - `UrlEscapeStyle.Percent` — percent-encodes every character that is not an RFC 3986
    ///   unreserved character or `/`, producing a destination that all Markdown parsers handle
    ///   correctly even when the URL contains `<`, `>`, spaces, or parentheses.
    #[napi(js_name = "urlEscapeStyle", ts_type = "UrlEscapeStyle | 'Angle' | 'Percent'")]
    pub url_escape_style: Option<JsUrlEscapeStyle>,
    /// Link rendering style (inline or reference).
    #[napi(js_name = "linkStyle", ts_type = "LinkStyle | 'Inline' | 'Reference'")]
    pub link_style: Option<JsLinkStyle>,
    /// Target output format (Markdown, plain text, etc.).
    #[napi(
        js_name = "outputFormat",
        ts_type = "HtmlToMarkdownRsOutputFormat | 'Markdown' | 'Djot' | 'Plain'"
    )]
    pub output_format: Option<JsHtmlToMarkdownRsOutputFormat>,
    /// Include structured document tree in result.
    #[napi(js_name = "includeDocumentStructure")]
    pub include_document_structure: Option<bool>,
    /// Extract inline images from data URIs and SVGs.
    #[napi(js_name = "extractImages")]
    pub extract_images: Option<bool>,
    /// Maximum decoded image size in bytes (default 5MB).
    #[napi(js_name = "maxImageSize")]
    pub max_image_size: Option<i64>,
    /// Capture SVG elements as images.
    #[napi(js_name = "captureSvg")]
    pub capture_svg: Option<bool>,
    /// Infer image dimensions from data.
    #[napi(js_name = "inferDimensions")]
    pub infer_dimensions: Option<bool>,
    /// Maximum DOM traversal depth.
    ///
    /// `None` uses the library's internal native-stack safety limit. Explicit
    /// values above that safety limit are clamped to prevent process-aborting
    /// stack overflows on pathologically deep DOM trees.
    #[napi(js_name = "maxDepth")]
    pub max_depth: Option<i64>,
    /// CSS selectors for elements to exclude entirely (element + all content).
    ///
    /// Unlike `strip_tags` (which removes the tag wrapper but keeps children),
    /// excluded elements and all their descendants are dropped from the output.
    /// Supports any CSS selector that `tl` supports: tag names, `.class`,
    /// `#id`, `[attribute]`, etc.
    ///
    /// Invalid selectors are silently skipped at conversion time.
    ///
    /// Example: `vec![".cookie-banner".into(), "#ad-container".into(), "[role='complementary']".into()]`
    #[napi(js_name = "excludeSelectors")]
    pub exclude_selectors: Option<Vec<String>>,
    /// Which conversion tier to use.
    ///
    /// - `TierStrategy.Auto` (default) — automatically choose the best path.
    /// - `TierStrategy.Tier2` — always use the Tier-2 DOM-walk path.
    #[napi(js_name = "tierStrategy", ts_type = "TierStrategy | 'auto' | 'tier_2'")]
    pub tier_strategy: Option<JsTierStrategy>,
}

/// Dublin Core metadata from docProps/core.xml
///
/// Contains standard metadata fields defined by the Dublin Core standard
/// and Office-specific extensions.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CoreProperties")]
pub struct JsCoreProperties {
    /// Document title
    pub title: Option<String>,
    /// Document subject/topic
    pub subject: Option<String>,
    /// Document creator/author
    pub creator: Option<String>,
    /// Keywords or tags
    pub keywords: Option<String>,
    /// Document description/abstract
    pub description: Option<String>,
    /// User who last modified the document
    #[napi(js_name = "lastModifiedBy")]
    pub last_modified_by: Option<String>,
    /// Revision number
    pub revision: Option<String>,
    /// Creation timestamp (ISO 8601)
    pub created: Option<String>,
    /// Last modification timestamp (ISO 8601)
    pub modified: Option<String>,
    /// Document category
    pub category: Option<String>,
    /// Content status (Draft, Final, etc.)
    #[napi(js_name = "contentStatus")]
    pub content_status: Option<String>,
    /// Document language
    pub language: Option<String>,
    /// Unique identifier
    pub identifier: Option<String>,
    /// Document version
    pub version: Option<String>,
    /// Last print timestamp (ISO 8601)
    #[napi(js_name = "lastPrinted")]
    pub last_printed: Option<String>,
}

/// Configuration for crawl, scrape, and map operations.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CrawlConfig")]
pub struct JsCrawlConfig {
    /// Maximum crawl depth (number of link hops from the start URL).
    #[napi(js_name = "maxDepth")]
    pub max_depth: Option<i64>,
    /// Maximum number of pages to crawl.
    #[napi(js_name = "maxPages")]
    pub max_pages: Option<i64>,
    /// Maximum links enqueued from a single page. Defaults to 10000.
    ///
    /// Bounds the work one hostile or pathological page can create; links past the
    /// cap are dropped and a warning is logged.
    #[napi(js_name = "maxLinksPerPage")]
    pub max_links_per_page: Option<i64>,
    /// Maximum number of concurrent requests.
    #[napi(js_name = "maxConcurrent")]
    pub max_concurrent: Option<i64>,
    /// Traversal order. Defaults to breadth-first.
    ///
    /// A frontier or strategy set explicitly on `CrawlEngineBuilder` takes precedence over
    /// this field.
    #[napi(
        js_name = "crawlStrategy",
        ts_type = "CrawlStrategyKind | 'bfs' | 'dfs' | 'best_first' | 'adaptive'"
    )]
    pub crawl_strategy: Option<JsCrawlStrategyKind>,
    /// Content filter applied to each page. `None` keeps every page.
    ///
    /// A content filter set explicitly on `CrawlEngineBuilder` takes precedence.
    #[napi(js_name = "contentFilter", ts_type = "ContentFilterKind | 'bm_25'")]
    pub content_filter: Option<JsContentFilterKind>,
    /// Query the BM25 content filter scores pages against. Required by `ContentFilterKind.Bm25`.
    #[napi(js_name = "bm25Query")]
    pub bm25_query: Option<String>,
    /// Minimum BM25 score a page must reach to be kept. Defaults to `0.0`.
    #[napi(js_name = "bm25Threshold")]
    pub bm25_threshold: Option<f64>,
    /// Whether to respect robots.txt directives.
    #[napi(js_name = "respectRobotsTxt")]
    pub respect_robots_txt: Option<bool>,
    /// When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF blocks)
    /// are surfaced as `ScrapeResult` records with the matching `status_code` rather
    /// than raised as `CrawlError`. Default `false` preserves the historical
    /// throw-on-error contract for direct fetches. Independently of this flag,
    /// 404s reached at the end of a redirect chain are *always* surfaced softly —
    /// the user opted into redirect-following, so receiving a 404 there is part of
    /// the normal flow rather than an unexpected error.
    #[napi(js_name = "softHttpErrors")]
    pub soft_http_errors: Option<bool>,
    /// Custom user-agent string.
    #[napi(js_name = "userAgent")]
    pub user_agent: Option<String>,
    /// Whether to restrict crawling to the same domain.
    #[napi(js_name = "stayOnDomain")]
    pub stay_on_domain: Option<bool>,
    /// Whether to allow subdomains when `stay_on_domain` is true.
    #[napi(js_name = "allowSubdomains")]
    pub allow_subdomains: Option<bool>,
    /// Regex patterns for paths to include during crawling.
    #[napi(js_name = "includePaths")]
    pub include_paths: Option<Vec<String>>,
    /// Regex patterns for paths to exclude during crawling.
    #[napi(js_name = "excludePaths")]
    pub exclude_paths: Option<Vec<String>>,
    /// Custom HTTP headers to send with each request.
    #[napi(js_name = "customHeaders")]
    pub custom_headers: Option<HashMap<String, String>>,
    /// Timeout for individual HTTP requests (in milliseconds when serialized).
    #[napi(js_name = "requestTimeout")]
    pub request_timeout: Option<i64>,
    /// Per-domain rate limit in milliseconds. When set, enforces a minimum delay
    /// between requests to the same domain. Defaults to 200ms when `None`.
    #[napi(js_name = "rateLimitMs")]
    pub rate_limit_ms: Option<i64>,
    /// Maximum number of redirects to follow.
    #[napi(js_name = "maxRedirects")]
    pub max_redirects: Option<i64>,
    /// Number of retry attempts for failed requests.
    #[napi(js_name = "retryCount")]
    pub retry_count: Option<i64>,
    /// HTTP status codes that should trigger a retry.
    #[napi(js_name = "retryCodes")]
    pub retry_codes: Option<Vec<u16>>,
    /// Whether to enable cookie handling.
    #[napi(js_name = "cookiesEnabled")]
    pub cookies_enabled: Option<bool>,
    /// Authentication configuration.
    pub auth: Option<JsAuthConfig>,
    /// Maximum response body size in bytes.
    ///
    /// `None` does not mean unbounded: an unset cap falls back to a 100 MiB safety
    /// ceiling, because HTTP responses are decompressed while being read and a few
    /// hundred compressed bytes can otherwise expand to gigabytes in memory. To read
    /// bodies larger than that, set this explicitly.
    #[napi(js_name = "maxBodySize")]
    pub max_body_size: Option<i64>,
    /// CSS selectors for tags to remove from HTML before processing.
    #[napi(js_name = "removeTags")]
    pub remove_tags: Option<Vec<String>>,
    /// Content extraction and conversion configuration.
    pub content: Option<JsContentConfig>,
    /// Maximum number of URLs to return from a map operation.
    #[napi(js_name = "mapLimit")]
    pub map_limit: Option<i64>,
    /// Search filter for map results (case-insensitive substring match on URLs).
    #[napi(js_name = "mapSearch")]
    pub map_search: Option<String>,
    /// Whether to download assets (CSS, JS, images, etc.) from the page.
    #[napi(js_name = "downloadAssets")]
    pub download_assets: Option<bool>,
    /// Filter for asset categories to download.
    #[napi(
        js_name = "assetTypes",
        ts_type = "Array<AssetCategory | 'document' | 'image' | 'audio' | 'video' | 'font' | 'stylesheet' | 'script' | 'archive' | 'data' | 'other'>"
    )]
    pub asset_types: Option<Vec<JsAssetCategory>>,
    /// Maximum size in bytes for individual asset downloads.
    #[napi(js_name = "maxAssetSize")]
    pub max_asset_size: Option<i64>,
    /// Browser configuration.
    pub browser: Option<JsBrowserConfig>,
    /// Proxy configuration for HTTP requests.
    pub proxy: Option<JsProxyConfig>,
    /// List of user-agent strings for rotation. If non-empty, overrides `user_agent`.
    #[napi(js_name = "userAgents")]
    pub user_agents: Option<Vec<String>>,
    /// Whether to capture a screenshot when using the browser.
    ///
    /// Only supported by `scrape()` with `BrowserBackend.Chromiumoxide` and
    /// `BrowserMode.Always` or `Stealth`. A screenshot is 100–500 KB of PNG per page,
    /// so `crawl()` does not carry screenshots in `CrawlPageResult`/`CrawlResult` at
    /// all — a multi-thousand-page crawl holding one per page in memory is not a safe
    /// default. Setting this with any other configuration (a different backend,
    /// `BrowserMode.Auto`/`Never`, or during `crawl()`) has no effect and logs a
    /// warning rather than silently doing nothing.
    #[napi(js_name = "captureScreenshot")]
    pub capture_screenshot: Option<bool>,
    /// Re-enqueue discovered `LinkType.Document` URLs into the crawl frontier so
    /// the crawl follows links *from* document pages (PDFs, etc.) as it would
    /// from HTML pages. Default: `false` (documents terminate at materialisation).
    #[napi(js_name = "followDocumentUrls")]
    pub follow_document_urls: Option<bool>,
    /// Maximum document-depth (from the seed URL through document links only)
    /// when `follow_document_urls` is true. `None` means inherit `max_depth`.
    /// Independent of `max_depth`: a document URL is enqueued only if BOTH the
    /// outer `max_depth` and (if set) `document_url_depth` permit it.
    #[napi(js_name = "documentUrlDepth")]
    pub document_url_depth: Option<u32>,
    /// Whether to download non-HTML documents (PDF, DOCX, images, code, etc.) instead of skipping them.
    /// Defaults to `true` — unlike `download_assets` and `capture_screenshot`, which default to `false`.
    #[napi(js_name = "downloadDocuments")]
    pub download_documents: Option<bool>,
    /// Maximum size in bytes for document downloads. Defaults to 50 MB.
    #[napi(js_name = "documentMaxSize")]
    pub document_max_size: Option<i64>,
    /// Allowlist of MIME types to download. If empty, uses built-in defaults.
    #[napi(js_name = "documentMimeTypes")]
    pub document_mime_types: Option<Vec<String>>,
    /// Directory to stream downloaded document bytes into instead of holding them in
    /// memory on `DownloadedDocument.content`. When set, `content` is left empty and
    /// `DownloadedDocument.content_path` is populated with `<dir>/<content_hash>.<ext>`.
    /// `None` (default) preserves today's in-memory-only behavior. Has no effect on
    /// wasm32, which has no filesystem — use `document_content_encoding` there instead.
    #[napi(js_name = "documentOutputDir")]
    pub document_output_dir: Option<String>,
    /// Opt-in encoding that duplicates `DownloadedDocument.content` into a serializable
    /// field for language bindings that need the bytes in-memory (`content` itself is
    /// `alef(skip)`ed). `None` (default) means no encoding is produced. Independent of
    /// `document_output_dir` — set both to get a file on disk and an in-memory copy.
    #[napi(js_name = "documentContentEncoding", ts_type = "DocumentContentEncoding | 'base_64'")]
    pub document_content_encoding: Option<JsDocumentContentEncoding>,
    /// Path to write WARC output. If `None`, WARC output is disabled.
    #[napi(js_name = "warcOutput")]
    pub warc_output: Option<String>,
    /// Named browser profile for persistent sessions (cookies, localStorage).
    ///
    /// Chromiumoxide backend only. The native backend runs an in-process JavaScript
    /// engine with no Chrome process and therefore no profile directory, so this is
    /// ignored there and logs a warning. It is also ignored — with a warning — when a
    /// shared browser pool is in use (the pool launches before any per-crawl config
    /// exists) or when connecting to an external CDP endpoint whose process crawlberg
    /// does not own.
    #[napi(js_name = "browserProfile")]
    pub browser_profile: Option<String>,
    /// Whether to save changes back to the browser profile on exit.
    #[napi(js_name = "saveBrowserProfile")]
    pub save_browser_profile: Option<bool>,
    /// SSRF policy for outbound network requests. Default: deny private networks,
    /// allow http/https only, max 5 redirects.
    ///
    /// All policy fields are exposed to language bindings.
    ///
    /// **wasm32 (including Node.js): `deny_private` does not stop hostname-based
    /// requests.** There is no DNS resolution on this target, so only a literal IP host is
    /// checked against the policy — a domain name is always permitted, regardless of
    /// `deny_private`. Under Node, where `fetch` enforces no CORS, this means a service
    /// embedding the wasm binding can be driven to internal hosts by domain name even with
    /// `deny_private = true`. Enforce egress restrictions at the network layer for that
    /// deployment target; do not rely on this field. See `crawlberg.net.validate_url`.
    pub ssrf: Option<JsSsrfPolicy>,
    /// Pins `SsrfPolicy.deny_private` to a caller-chosen value, bypassing the
    /// `CRAWLBERG_ALLOW_PRIVATE_NETWORK` operator override entirely for this config.
    ///
    /// `ssrf.deny_private` is a plain, always-serialized `bool`: several alef-generated
    /// bindings construct `SsrfPolicy.default()` (hardcoding `deny_private: true`)
    /// whenever their caller never touches SSRF settings at all, so `true` on that field
    /// alone cannot distinguish "the caller wants private networks denied" from "the
    /// binding's own structural default landed on `true`". The environment variable
    /// exists precisely to resolve that ambiguity in the common case by treating any
    /// `true` as inconclusive and deferring to the operator.
    ///
    /// Set this field when that default-deferral is wrong for your call — e.g. a test
    /// that must prove `deny_private: true` still denies even while the operator has set
    /// `CRAWLBERG_ALLOW_PRIVATE_NETWORK` suite-wide for every other call. `None` (default)
    /// preserves today's behavior: the environment variable may still flip
    /// `ssrf.deny_private` to `false`. `Some(value)` pins `ssrf.deny_private` to `value`
    /// and the environment variable is not consulted for this config.
    #[napi(js_name = "ssrfDenyPrivateExplicit")]
    pub ssrf_deny_private_explicit: Option<bool>,
}

/// Configuration for CSV/TSV extraction.
///
/// When unset (`ExtractionConfig.csv == None`), the extractor keeps its
/// existing default behavior: the delimiter is auto-detected by sampling the
/// file (comma, tab, pipe, or semicolon), and no line is treated as a comment.
///
/// # Example
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "CsvConfig")]
pub struct JsCsvConfig {
    /// Field delimiter, as a single-character string (e.g. `","`, `";"`,
    /// `"\t"`, `"|"`). When `None` (default), the delimiter is auto-detected
    /// from a sample of the file.
    ///
    /// Must be exactly one ASCII byte when set — `ExtractionConfig.validate`
    /// rejects an empty string or a multi-byte value with a helpful error.
    /// The TSV MIME type (`text/tab-separated-values`) always forces `\t`
    /// regardless of this setting.
    pub delimiter: Option<String>,
    /// Line prefixes that mark a comment line to skip entirely during row
    /// parsing (e.g. `["#"]`). A line is treated as a comment when its
    /// trimmed start matches any of these prefixes exactly.
    ///
    /// Default: empty, meaning no line is treated as a comment (matches the
    /// pre-existing extractor behavior).
    #[napi(js_name = "commentPrefixes")]
    pub comment_prefixes: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsCsvConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::CsvConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// CSV/TSV file metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CsvMetadata")]
pub struct JsCsvMetadata {
    /// Total number of data rows (excluding the header row if present).
    #[napi(js_name = "rowCount")]
    pub row_count: Option<u32>,
    /// Number of columns detected.
    #[napi(js_name = "columnCount")]
    pub column_count: Option<u32>,
    /// Field delimiter character (e.g. `","` or `"\t"`).
    pub delimiter: Option<String>,
    /// Whether the first row was treated as a header.
    #[napi(js_name = "hasHeader")]
    pub has_header: Option<bool>,
    /// Inferred data type for each column (e.g. `"string"`, `"integer"`, `"float"`).
    #[napi(js_name = "columnTypes")]
    pub column_types: Option<Vec<String>>,
}

/// dBASE field information.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DbfFieldInfo")]
pub struct JsDbfFieldInfo {
    /// Field (column) name.
    pub name: String,
    /// dBASE field type character (e.g. `"C"` for character, `"N"` for numeric).
    #[napi(js_name = "fieldType")]
    pub field_type: String,
}

/// dBASE (DBF) file metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "DbfMetadata")]
pub struct JsDbfMetadata {
    /// Total number of data records in the DBF file.
    #[napi(js_name = "recordCount")]
    pub record_count: Option<i64>,
    /// Number of field (column) definitions.
    #[napi(js_name = "fieldCount")]
    pub field_count: Option<i64>,
    /// Descriptor for each field in the table schema.
    pub fields: Option<Vec<JsDbfFieldInfo>>,
}
impl<'de> serde::Deserialize<'de> for JsDbfMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::DbfMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Runtime options accepted by the `candle-deepseek-ocr` backend.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "DeepseekOcrBackendOptions")]
pub struct JsDeepseekOcrBackendOptions {
    /// Local DeepSeek-OCR model directory. The backend requires this option.
    #[napi(js_name = "modelPath")]
    pub model_path: Option<String>,
    /// Optional per-call device override.
    #[napi(ts_type = "CandleDevicePreference | 'auto' | 'cpu' | 'cuda' | 'metal'")]
    pub device: Option<JsCandleDevicePreference>,
    /// DeepSeek-OCR model generation, either 1 or 2. Defaults to 2.
    pub version: Option<u32>,
}
impl<'de> serde::Deserialize<'de> for JsDeepseekOcrBackendOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::candle_ocr::DeepseekOcrBackendOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// MIME type detection response.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DetectResponse")]
pub struct JsDetectResponse {
    /// Detected MIME type
    #[napi(js_name = "mimeType")]
    pub mime_type: String,
    /// Original filename (if provided)
    pub filename: Option<String>,
}

/// Page-level detection result containing all detections and page metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DetectionResult")]
pub struct JsDetectionResult {
    /// Page width in pixels (as seen by the model).
    #[napi(js_name = "pageWidth")]
    pub page_width: u32,
    /// Page height in pixels (as seen by the model).
    #[napi(js_name = "pageHeight")]
    pub page_height: u32,
    /// All layout detections on this page after postprocessing.
    pub detections: Vec<JsLayoutDetection>,
}

/// A single contiguous hunk in a unified diff.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DiffHunk")]
pub struct JsDiffHunk {
    /// Starting line number in the old content (0-indexed).
    #[napi(js_name = "fromLine")]
    pub from_line: i64,
    /// Number of lines from the old content in this hunk.
    #[napi(js_name = "fromCount")]
    pub from_count: i64,
    /// Starting line number in the new content (0-indexed).
    #[napi(js_name = "toLine")]
    pub to_line: i64,
    /// Number of lines from the new content in this hunk.
    #[napi(js_name = "toCount")]
    pub to_count: i64,
    /// Lines that make up this hunk.
    pub lines: Vec<JsDiffLine>,
}

/// Options controlling how two `ExtractedDocument` values are compared.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DiffOptions")]
pub struct JsDiffOptions {
    /// Include metadata changes in the diff. Default: `true`.
    #[napi(js_name = "includeMetadata")]
    pub include_metadata: Option<bool>,
    /// Include embedded-children changes in the diff. Default: `true`.
    #[napi(js_name = "includeEmbedded")]
    pub include_embedded: Option<bool>,
    /// Truncate content to this many characters before diffing.
    ///
    /// Useful for very large documents where only the first N characters matter.
    /// `None` means no truncation.
    #[napi(js_name = "maxContentChars")]
    pub max_content_chars: Option<i64>,
}

#[napi(js_name = "diffOptionsDefault")]
pub fn diff_options_default() -> JsDiffOptions {
    xberg::DiffOptions::default().into()
}

/// Attributes associated with a named Djot element.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DjotAttributeGroup")]
pub struct JsDjotAttributeGroup {
    /// Element identifier used by the Djot attribute map.
    pub identifier: Option<String>,
    /// Attributes associated with the element.
    pub attributes: Option<JsAttributes>,
}

/// Comprehensive Djot document structure with semantic preservation.
///
/// This type captures the full richness of Djot markup, including:
/// - Block-level structures (headings, lists, blockquotes, code blocks, etc.)
/// - Inline formatting (emphasis, strong, highlight, subscript, superscript, etc.)
/// - Attributes (classes, IDs, key-value pairs)
/// - Links, images, footnotes
/// - Math expressions (inline and display)
/// - Tables with full structure
///
/// Available when the `djot` feature is enabled.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "DjotContent")]
pub struct JsDjotContent {
    /// Plain text representation for backwards compatibility
    #[napi(js_name = "plainText")]
    pub plain_text: String,
    /// Structured block-level content
    pub blocks: Vec<JsFormattedBlock>,
    /// Metadata from YAML frontmatter
    pub metadata: JsMetadata,
    /// Extracted tables as structured data
    pub tables: Vec<JsTable>,
    /// Extracted images with metadata
    pub images: Vec<JsDjotImage>,
    /// Extracted links with URLs
    pub links: Vec<JsDjotLink>,
    /// Footnote definitions
    pub footnotes: Vec<JsFootnote>,
    /// Attributes mapped by element identifier (if present)
    pub attributes: Vec<JsDjotAttributeGroup>,
}
impl<'de> serde::Deserialize<'de> for JsDjotContent {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::DjotContent as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Image element in Djot.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DjotImage")]
pub struct JsDjotImage {
    /// Image source URL or path
    pub src: String,
    /// Alternative text
    pub alt: String,
    /// Optional title
    pub title: Option<String>,
    /// Element attributes
    pub attributes: Option<JsAttributes>,
}

/// Link element in Djot.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DjotLink")]
pub struct JsDjotLink {
    /// Link URL
    pub url: String,
    /// Link text content
    pub text: String,
    /// Optional title
    pub title: Option<String>,
    /// Element attributes
    pub attributes: Option<JsAttributes>,
}

/// A single doctor verdict: what was checked, the outcome, and why.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DoctorCheck")]
pub struct JsDoctorCheck {
    /// Check identifier, e.g. `ocr.tesseract` or `layout.rtdetr`.
    pub name: String,
    /// Pass / warn / fail / skip verdict.
    #[napi(ts_type = "ProbeStatus | 'pass' | 'warn' | 'fail' | 'skip'")]
    pub status: JsProbeStatus,
    /// One-line reason or detail (e.g. missing language, resolved path, error).
    pub message: String,
}

/// A `ProbeStatus.Pass` verdict: the checked backend or setting will
/// work as configured on this host.
#[napi(js_name = "doctorCheckPass")]
pub fn doctor_check_pass(name: String, message: String) -> JsDoctorCheck {
    xberg::DoctorCheck::pass(name, message).into()
}
/// A `ProbeStatus.Warn` verdict: the check ran and found something
/// actionable, but nothing is broken. Never fails the report.
#[napi(js_name = "doctorCheckWarn")]
pub fn doctor_check_warn(name: String, message: String) -> JsDoctorCheck {
    xberg::DoctorCheck::warn(name, message).into()
}
/// A `ProbeStatus.Fail` verdict: the configured setup will not work (or
/// will silently degrade) on this host.
#[napi(js_name = "doctorCheckFail")]
pub fn doctor_check_fail(name: String, message: String) -> JsDoctorCheck {
    xberg::DoctorCheck::fail(name, message).into()
}
/// A `ProbeStatus.Skip` verdict: the check cannot run locally; first
/// real use decides, possibly after a download.
#[napi(js_name = "doctorCheckSkip")]
pub fn doctor_check_skip(name: String, message: String) -> JsDoctorCheck {
    xberg::DoctorCheck::skip(name, message).into()
}

/// Aggregate doctor report over all configured backends and settings.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DoctorReport")]
pub struct JsDoctorReport {
    /// Individual check verdicts, in execution order.
    pub checks: Option<Vec<JsDoctorCheck>>,
}

/// Detected document boundary within a PDF.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentBoundary")]
pub struct JsDocumentBoundary {
    /// 1-indexed start page (inclusive).
    #[napi(js_name = "startPage")]
    pub start_page: u32,
    /// 1-indexed end page (inclusive).
    #[napi(js_name = "endPage")]
    pub end_page: u32,
    /// Confidence in this boundary, `[0.0, 1.0]`.
    pub confidence: f64,
    /// Reason for the boundary detection.
    #[napi(ts_type = "BoundaryReason | 'start' | 'page_one_marker' | 'letterhead_reset' | 'density_shift' | 'end'")]
    pub reason: JsBoundaryReason,
}

/// Cheap structural counts for an extracted document.
///
/// Populated on every `ExtractedDocument` returned by `extract` /
/// `extract_batch`, regardless of whether the heavy `pages` / `images`
/// collections are materialized. A caller that only needs "how many pages /
/// tables / images did this document have?" (reporting, cost estimation,
/// progress, quotas) can read these without enabling per-page or per-image
/// extraction.
///
/// The page count comes from the parse (the extractor already walks the page
/// tree); it does not require opting into per-page content. `pages` is `0` for
/// inputs that are not page-addressable (e.g. plain text).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentCounts")]
pub struct JsDocumentCounts {
    /// Total pages in the source document (`0` when not page-addressable).
    pub pages: Option<i64>,
    /// Tables detected in the document.
    pub tables: Option<i64>,
    /// Images detected in the document.
    pub images: Option<i64>,
}

/// Metadata about a document for analysis.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentMetadata")]
pub struct JsDocumentMetadata {
    /// MIME type of the document.
    #[napi(js_name = "mimeType")]
    pub mime_type: String,
    /// File size in bytes.
    #[napi(js_name = "sizeBytes")]
    pub size_bytes: i64,
    /// Page count (if known, e.g., from previous analysis).
    #[napi(js_name = "pageCount")]
    pub page_count: Option<u32>,
    /// Whether OCR is forced regardless of text layer.
    #[napi(js_name = "forceOcr")]
    pub force_ocr: bool,
    /// User-provided chunk configuration overrides.
    #[napi(js_name = "userChunkConfig")]
    pub user_chunk_config: Option<JsUserChunkConfig>,
    /// Whether chunking is enabled for this job.
    #[napi(js_name = "chunkingEnabled")]
    pub chunking_enabled: bool,
}

/// A single node in the document tree.
///
/// Each node has deterministic `id`, typed `content`, optional `parent`/`children`
/// for tree structure, and metadata like page number, bounding box, and content layer.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "DocumentNode")]
pub struct JsDocumentNode {
    /// Deterministic identifier (hash of node type + text + page + position).
    ///
    /// Stable and unique within a single extraction response: every internal
    /// construction path threads the node's position (its index in
    /// `DocumentStructure.nodes`) into the hash, so identical
    /// `(node_type, text, page)` tuples at different positions never collide.
    /// Always serialised — `ChunkMetadata.node_ids` references it to join
    /// chunks back to the nodes they were derived from.
    /// `#[serde(default)]` covers the missing-field case on inbound JSON
    /// (e.g. documents serialised before this field existed).
    pub id: String,
    /// Node content — tagged enum, type-specific data only.
    pub content: JsNodeContent,
    /// Parent node index (`None` = root-level node).
    pub parent: Option<u32>,
    /// Child node indices in reading order.
    pub children: Vec<u32>,
    /// Content layer classification.
    ///
    /// Always serialised — Kotlin-Android (and any other typed binding) treats
    /// the field as non-nullable, so omitting it from the JSON wire would
    /// break consumer deserialisation.  `#[serde(default)]` covers the
    /// missing-field case on inbound JSON.
    #[napi(
        js_name = "contentLayer",
        ts_type = "ContentLayer | 'body' | 'header' | 'footer' | 'footnote'"
    )]
    pub content_layer: JsContentLayer,
    /// Page number where this node starts (1-indexed).
    pub page: Option<u32>,
    /// Page number where this node ends (for multi-page tables/sections).
    #[napi(js_name = "pageEnd")]
    pub page_end: Option<u32>,
    /// Bounding box in document coordinates.
    pub bbox: Option<JsBoundingBox>,
    /// Inline annotations (formatting, links) on this node's text content.
    ///
    /// Only meaningful for text-carrying nodes; empty for containers.
    pub annotations: Vec<JsTextAnnotation>,
    /// Format-specific key-value attributes.
    ///
    /// Extensible bag for miscellaneous data without a dedicated typed field: CSS classes,
    /// LaTeX environment names, Excel cell formulas, slide layout names, etc.
    pub attributes: Option<HashMap<String, String>>,
}
impl<'de> serde::Deserialize<'de> for JsDocumentNode {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::DocumentNode as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A resolved relationship between two nodes in the document tree.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentRelationship")]
pub struct JsDocumentRelationship {
    /// Source node index (the referencing node).
    pub source: u32,
    /// Target node index (the referenced node).
    pub target: u32,
    /// Semantic kind of the relationship.
    #[napi(
        ts_type = "RelationshipKind | 'footnote_reference' | 'citation_reference' | 'internal_link' | 'caption' | 'label' | 'toc_entry' | 'cross_reference'"
    )]
    pub kind: JsRelationshipKind,
}

/// A single tracked change embedded in a document.
///
/// Populated by per-format extractors that understand change-tracking metadata
/// (DOCX `w:ins`/`w:del`/`w:rPrChange`, ODT `text:change-*`, …). Every
/// extractor defaults to `ExtractedDocument.revisions = None` until a
/// format-specific implementation is added.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentRevision")]
pub struct JsDocumentRevision {
    /// Format-specific revision identifier.
    ///
    /// For DOCX this is the `w:id` attribute value on the change element
    /// (e.g. `"42"`). When the attribute is absent a synthetic fallback is
    /// generated (`"docx-ins-0"`, `"docx-del-3"`, …).
    #[napi(js_name = "revisionId")]
    pub revision_id: String,
    /// Display name of the author who made this change, when available.
    pub author: Option<String>,
    /// ISO-8601 timestamp of the change, when available.
    ///
    /// Stored as a plain string so this type remains FFI-friendly and
    /// unconditionally available without the `chrono` optional dep.
    /// DOCX populates this from the `w:date` attribute (e.g.
    /// `"2024-03-15T10:30:00Z"`).
    pub timestamp: Option<String>,
    /// Semantic kind of this revision.
    #[napi(ts_type = "RevisionKind | 'insertion' | 'deletion' | 'format_change' | 'comment'")]
    pub kind: JsRevisionKind,
    /// Best-effort document location for this revision.
    ///
    /// Resolution is format-dependent and may be `None` when the location
    /// cannot be determined (e.g. changes inside table cells before
    /// table-cell anchor support is added).
    pub anchor: Option<JsRevisionAnchor>,
    /// The content changes that make up this revision.
    pub delta: JsRevisionDelta,
}

/// Top-level structured document representation.
///
/// A flat array of nodes with index-based parent/child references forming a tree.
/// Root-level nodes have `parent: None`. Use `body_roots()` and `furniture_roots()`
/// to iterate over top-level content by layer.
///
/// # Validation
///
/// Call `validate()` after construction to verify all node indices are in bounds
/// and parent-child relationships are bidirectionally consistent.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "DocumentStructure")]
pub struct JsDocumentStructure {
    /// All nodes in document/reading order.
    pub nodes: Option<Vec<JsDocumentNode>>,
    /// Origin format identifier (e.g. "docx", "pptx", "html", "pdf").
    ///
    /// Allows renderers to apply format-aware heuristics when converting
    /// the document tree to output formats.
    #[napi(js_name = "sourceFormat")]
    pub source_format: Option<String>,
    /// Resolved relationships between nodes (footnote refs, citations, anchor links, etc.).
    ///
    /// Populated during derivation from the internal document representation.
    /// Empty when no relationships are detected.
    pub relationships: Option<Vec<JsDocumentRelationship>>,
    /// Sorted, deduplicated list of node type names present in this document.
    ///
    /// Each value is the snake_case `node_type` tag of the corresponding
    /// `NodeContent` variant (e.g. `"paragraph"`, `"heading"`, `"table"`, …).
    ///
    /// Computed from `nodes` via `DocumentStructure.finalize_node_types`.
    /// Empty until that method is called (internal construction paths call it
    /// at the end of derivation).
    #[napi(js_name = "nodeTypes")]
    pub node_types: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsDocumentStructure {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::DocumentStructure as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "documentStructureDefault")]
pub fn document_structure_default() -> JsDocumentStructure {
    xberg::DocumentStructure::default().into()
}

/// Summary of an extracted document.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocumentSummary")]
pub struct JsDocumentSummary {
    /// Summary text (plain prose).
    pub text: String,
    /// Strategy that produced this summary.
    #[napi(ts_type = "SummaryStrategy | 'extractive' | 'abstractive'")]
    pub strategy: JsSummaryStrategy,
    /// Approximate token count of the summary, when known.
    #[napi(js_name = "tokenCount")]
    pub token_count: Option<u32>,
}

/// Application properties from docProps/app.xml for DOCX
///
/// Contains Word-specific document statistics and metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocxAppProperties")]
pub struct JsDocxAppProperties {
    /// Application name (e.g., "Microsoft Office Word")
    pub application: Option<String>,
    /// Application version
    #[napi(js_name = "appVersion")]
    pub app_version: Option<String>,
    /// Template filename
    pub template: Option<String>,
    /// Total editing time in minutes
    #[napi(js_name = "totalTime")]
    pub total_time: Option<i32>,
    /// Number of pages
    pub pages: Option<i32>,
    /// Number of words
    pub words: Option<i32>,
    /// Number of characters (excluding spaces)
    pub characters: Option<i32>,
    /// Number of characters (including spaces)
    #[napi(js_name = "charactersWithSpaces")]
    pub characters_with_spaces: Option<i32>,
    /// Number of lines
    pub lines: Option<i32>,
    /// Number of paragraphs
    pub paragraphs: Option<i32>,
    /// Company name
    pub company: Option<String>,
    /// Document security level
    #[napi(js_name = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[napi(js_name = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[napi(js_name = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[napi(js_name = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[napi(js_name = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
}

/// Word document metadata.
///
/// Extracted from DOCX files using shared Office Open XML metadata extraction.
/// Integrates with `office_metadata` module for core/app/custom properties.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DocxMetadata")]
pub struct JsDocxMetadata {
    /// Core properties from docProps/core.xml (Dublin Core metadata)
    ///
    /// Contains title, creator, subject, keywords, dates, etc.
    /// Shared format across DOCX/PPTX/XLSX documents.
    #[napi(js_name = "coreProperties")]
    pub core_properties: Option<JsCoreProperties>,
    /// Application properties from docProps/app.xml (Word-specific statistics)
    ///
    /// Contains word count, page count, paragraph count, editing time, etc.
    /// DOCX-specific variant of Office application properties.
    #[napi(js_name = "appProperties")]
    pub app_properties: Option<JsDocxAppProperties>,
    /// Custom properties from docProps/custom.xml (user-defined properties)
    ///
    /// Contains key-value pairs defined by users or applications.
    /// Values can be strings, numbers, booleans, or dates.
    #[napi(js_name = "customProperties")]
    pub custom_properties: Option<HashMap<String, serde_json::Value>>,
}

/// Semantic element extracted from document.
///
/// Represents a logical unit of content with semantic classification,
/// unique identifier, and metadata for tracking origin and position.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Element")]
pub struct JsElement {
    /// Deterministic element identifier. Empty only when deserializing legacy payloads
    /// that predate this field's wire representation.
    #[napi(js_name = "elementId")]
    pub element_id: String,
    /// Semantic type of this element
    #[napi(
        js_name = "elementType",
        ts_type = "ElementType | 'title' | 'narrative_text' | 'heading' | 'list_item' | 'table' | 'image' | 'page_break' | 'code_block' | 'formula' | 'block_quote' | 'footer' | 'header'"
    )]
    pub element_type: JsElementType,
    /// Text content of the element
    pub text: String,
    /// Metadata about the element
    pub metadata: JsElementMetadata,
}
impl<'de> serde::Deserialize<'de> for JsElement {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Element as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Metadata for a semantic element.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ElementMetadata")]
pub struct JsElementMetadata {
    /// Page number (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: Option<u32>,
    /// Source filename or document name
    pub filename: Option<String>,
    /// Bounding box coordinates if available
    pub coordinates: Option<JsBoundingBox>,
    /// Position index in the element sequence
    #[napi(js_name = "elementIndex")]
    pub element_index: Option<i64>,
    /// Additional custom metadata
    pub additional: HashMap<String, String>,
}

/// Email attachment representation.
///
/// Contains metadata and optionally the content of an email attachment.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmailAttachment")]
pub struct JsEmailAttachment {
    /// Attachment name (from Content-Disposition header)
    pub name: Option<String>,
    /// Filename of the attachment
    pub filename: Option<String>,
    /// MIME type of the attachment
    #[napi(js_name = "mimeType")]
    pub mime_type: Option<String>,
    /// Size in bytes
    pub size: Option<i64>,
    /// Whether this attachment is an image
    #[napi(js_name = "isImage")]
    pub is_image: bool,
    /// Attachment data (if extracted).
    /// Uses `bytes.Bytes` for cheap cloning of large buffers.
    #[napi(ts_type = "Uint8Array | Buffer | Array<number>")]
    pub data: Option<JsBytes>,
}

/// Configuration for email extraction.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "EmailConfig")]
pub struct JsEmailConfig {
    /// Windows codepage number to use when an MSG file contains no codepage property.
    /// Defaults to `None`, which falls back to windows-1252.
    ///
    /// If an unrecognized or invalid codepage number is supplied (including 0),
    /// the behavior silently falls back to windows-1252 — the same as when the
    /// MSG file itself contains an unrecognized codepage. No error or warning is
    /// emitted. Users should verify output when supplying unusual values.
    ///
    /// Common values:
    /// - 1250: Central European (Polish, Czech, Hungarian, etc.)
    /// - 1251: Cyrillic (Russian, Ukrainian, Bulgarian, etc.)
    /// - 1252: Western European (default)
    /// - 1253: Greek
    /// - 1254: Turkish
    /// - 1255: Hebrew
    /// - 1256: Arabic
    /// - 932:  Japanese (Shift-JIS)
    /// - 936:  Simplified Chinese (GBK)
    #[napi(js_name = "msgFallbackCodepage")]
    pub msg_fallback_codepage: Option<u32>,
}
impl<'de> serde::Deserialize<'de> for JsEmailConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::EmailConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Email extraction result.
///
/// Complete representation of an extracted email message (.eml or .msg)
/// including headers, body content, and attachments.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmailExtractionResult")]
pub struct JsEmailExtractionResult {
    /// Email subject line
    pub subject: Option<String>,
    /// Sender email address
    #[napi(js_name = "fromEmail")]
    pub from_email: Option<String>,
    /// Primary recipient email addresses
    #[napi(js_name = "toEmails")]
    pub to_emails: Vec<String>,
    /// CC recipient email addresses
    #[napi(js_name = "ccEmails")]
    pub cc_emails: Vec<String>,
    /// BCC recipient email addresses
    #[napi(js_name = "bccEmails")]
    pub bcc_emails: Vec<String>,
    /// Email date/timestamp
    pub date: Option<String>,
    /// Message-ID header value
    #[napi(js_name = "messageId")]
    pub message_id: Option<String>,
    /// Plain text version of the email body
    #[napi(js_name = "plainText")]
    pub plain_text: Option<String>,
    /// HTML version of the email body
    #[napi(js_name = "htmlContent")]
    pub html_content: Option<String>,
    /// Cleaned/processed text content. Aliased as `cleaned_text` for back-compat.
    pub content: String,
    /// List of email attachments
    pub attachments: Vec<JsEmailAttachment>,
    /// Additional email headers and metadata
    pub metadata: HashMap<String, String>,
}

/// Email metadata extracted from .eml and .msg files.
///
/// Includes sender/recipient information, message ID, and attachment list.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmailMetadata")]
pub struct JsEmailMetadata {
    /// Sender's email address
    #[napi(js_name = "fromEmail")]
    pub from_email: Option<String>,
    /// Sender's display name
    #[napi(js_name = "fromName")]
    pub from_name: Option<String>,
    /// Primary recipients
    #[napi(js_name = "toEmails")]
    pub to_emails: Option<Vec<String>>,
    /// CC recipients
    #[napi(js_name = "ccEmails")]
    pub cc_emails: Option<Vec<String>>,
    /// BCC recipients
    #[napi(js_name = "bccEmails")]
    pub bcc_emails: Option<Vec<String>>,
    /// Message-ID header value
    #[napi(js_name = "messageId")]
    pub message_id: Option<String>,
    /// List of attachment filenames
    pub attachments: Option<Vec<String>>,
}

/// Changes to embedded archive children between two results.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmbeddedChanges")]
pub struct JsEmbeddedChanges {
    /// Children present in `b` but not in `a` (matched by `path`).
    pub added: Option<Vec<JsArchiveEntry>>,
    /// Children present in `a` but not in `b` (matched by `path`).
    pub removed: Option<Vec<JsArchiveEntry>>,
    /// Children present in both but with differing content (matched by `path`).
    ///
    /// Each entry holds the diff of the nested `ExtractedDocument`.
    pub changed: Option<Vec<JsEmbeddedDiff>>,
}

/// Diff for a single embedded archive entry that appears in both results.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmbeddedDiff")]
pub struct JsEmbeddedDiff {
    /// Archive-relative path identifying this entry.
    pub path: String,
    /// The recursive diff of the entry's extraction result.
    pub diff: JsExtractionDiff,
}

/// Embedded file descriptor extracted from the PDF name tree.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmbeddedFile")]
pub struct JsEmbeddedFile {
    /// The filename as stored in the PDF name tree.
    pub name: String,
    /// Raw file bytes from the embedded stream (already decompressed by lopdf).
    #[napi(ts_type = "Uint8Array | Buffer | Array<number>")]
    pub data: JsBytes,
    /// Compressed byte count of the original stream (before decompression).
    ///
    /// Used by callers to compute the decompression ratio and detect zip-bomb-style
    /// attacks that embed a tiny compressed stream expanding to gigabytes of data.
    #[napi(js_name = "compressedSize")]
    pub compressed_size: i64,
    /// MIME type if specified in the filespec, otherwise `None`.
    #[napi(js_name = "mimeType")]
    pub mime_type: Option<String>,
}

/// Embedding configuration for text chunks.
///
/// Configures embedding generation using ONNX models via the vendored embedding engine.
/// Requires the `embeddings` feature to be enabled.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "EmbeddingConfig")]
pub struct JsEmbeddingConfig {
    /// The embedding model to use (defaults to "gte-modernbert-base" preset if not specified)
    pub model: Option<JsEmbeddingModelType>,
    /// Whether to normalize embedding vectors (recommended for cosine similarity)
    pub normalize: Option<bool>,
    /// Batch size for embedding generation
    #[napi(js_name = "batchSize")]
    pub batch_size: Option<i64>,
    /// Show model download progress.
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). Covers both
    /// local backends (ONNX and static/model2vec). A warm Hugging Face cache transfers nothing and
    /// so reports nothing. Ignored by `EmbeddingModelType.Llm` and
    /// `EmbeddingModelType.Plugin`, which download no model.
    #[napi(js_name = "showDownloadProgress")]
    pub show_download_progress: Option<bool>,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows `HF_HUB_CACHE`, `HUGGINGFACE_HUB_CACHE`,
    /// `HF_HOME`, XDG, and platform defaults. Prefer those environment variables
    /// when configuring the cache process-wide.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the embedding ONNX model.
    ///
    /// When set, controls which execution provider (CPU, CUDA, CoreML, TensorRT)
    /// is used for inference. Defaults to `None` (auto-select per platform).
    pub acceleration: Option<JsAccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single `embed()` call when
    /// using `EmbeddingModelType.Plugin`.
    ///
    /// Applies only to the in-process plugin path — protects against hung
    /// host-language backends (e.g. a Python callback deadlocked on the GIL,
    /// a model stuck on CUDA OOM retries, etc.). On timeout, the dispatcher
    /// returns `Plugin` instead of blocking forever.
    ///
    /// `None` disables the timeout. The default (60 seconds) is conservative
    /// for common in-process inference; increase for large batches on slow
    /// hardware.
    #[napi(js_name = "maxEmbedDurationSecs")]
    pub max_embed_duration_secs: Option<i64>,
    /// Maximum number of tokens fed to the tokenizer before truncation when
    /// embedding a chunk with a local ONNX model (Preset/Custom).
    ///
    /// A chunk longer than this many tokens has its tail dropped before
    /// inference, so only the prefix contributes to the stored vector. `None`
    /// falls back to 512 (the historical default). The effective value is
    /// always capped at the model's own `model_max_length`, so raising it past
    /// what the model supports has no effect — set it to match a long-context
    /// model (e.g. 8192 for Jina/Nomic) so long chunks embed in full.
    ///
    /// Ignored by the `Llm` and `Plugin` model types, which own their own
    /// tokenization.
    #[napi(js_name = "maxSequenceLength")]
    pub max_sequence_length: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsEmbeddingConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::EmbeddingConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "embeddingConfigDefault")]
pub fn embedding_config_default() -> JsEmbeddingConfig {
    xberg::EmbeddingConfig::default().into()
}

/// A single named entity detected in the extracted text.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "Entity")]
pub struct JsEntity {
    /// Canonical category the entity belongs to (PERSON, ORG, LOCATION, etc.).
    pub category: JsEntityCategory,
    /// Raw mention text exactly as it appeared in the source.
    pub text: String,
    /// Byte-offset span in `ExtractedDocument.content` where the mention starts.
    pub start: u32,
    /// Byte-offset span in `ExtractedDocument.content` where the mention ends (exclusive).
    pub end: u32,
    /// Backend-reported confidence in `[0.0, 1.0]`. `None` when the backend does not
    /// expose confidence scores.
    pub confidence: Option<f64>,
}

/// EPUB metadata (Dublin Core extensions).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EpubMetadata")]
pub struct JsEpubMetadata {
    /// Dublin Core `coverage` field (geographic or temporal scope).
    pub coverage: Option<String>,
    /// Dublin Core `format` field (media type of the resource).
    #[napi(js_name = "dcFormat")]
    pub dc_format: Option<String>,
    /// Dublin Core `relation` field (related resource identifier).
    pub relation: Option<String>,
    /// Dublin Core `source` field (origin resource identifier).
    pub source: Option<String>,
    /// Dublin Core `type` field (nature or genre of the resource).
    #[napi(js_name = "dcType")]
    pub dc_type: Option<String>,
    /// Path or identifier of the cover image within the EPUB container.
    #[napi(js_name = "coverImage")]
    pub cover_image: Option<String>,
}

/// Error metadata (for batch operations).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ErrorMetadata")]
pub struct JsErrorMetadata {
    /// Machine-readable error type identifier (e.g. "UnsupportedFormat").
    #[napi(js_name = "errorType")]
    pub error_type: String,
    /// Human-readable error description.
    pub message: String,
}

/// Excel/spreadsheet format metadata.
///
/// Identifies the document as a spreadsheet source via the `FormatMetadata.Excel`
/// discriminant. Sheet count and sheet names are stored inside this struct.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExcelMetadata")]
pub struct JsExcelMetadata {
    /// Number of sheets in the workbook.
    #[napi(js_name = "sheetCount")]
    pub sheet_count: Option<u32>,
    /// Names of all sheets in the workbook.
    #[napi(js_name = "sheetNames")]
    pub sheet_names: Option<Vec<String>>,
}

/// Single Excel worksheet.
///
/// Represents one sheet from an Excel workbook with its content
/// converted to Markdown format and dimensional statistics.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ExcelSheet")]
pub struct JsExcelSheet {
    /// Sheet name as it appears in Excel
    pub name: String,
    /// Sheet content converted to Markdown tables
    pub markdown: String,
    /// Number of rows
    #[napi(js_name = "rowCount")]
    pub row_count: i64,
    /// Number of columns
    #[napi(js_name = "colCount")]
    pub col_count: i64,
    /// Total number of non-empty cells
    #[napi(js_name = "cellCount")]
    pub cell_count: i64,
    /// Pre-extracted table cells (2D vector of cell values)
    /// Populated during markdown generation to avoid re-parsing markdown.
    /// undefined for empty sheets.
    #[napi(js_name = "tableCells")]
    pub table_cells: Option<Vec<Vec<String>>>,
}
impl<'de> serde::Deserialize<'de> for JsExcelSheet {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ExcelSheet as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Excel workbook representation.
///
/// Contains all sheets from an Excel file (.xlsx, .xls, etc.) with
/// extracted content and metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ExcelWorkbook")]
pub struct JsExcelWorkbook {
    /// All sheets in the workbook
    pub sheets: Vec<JsExcelSheet>,
    /// Workbook-level metadata (author, creation date, etc.)
    pub metadata: HashMap<String, String>,
    /// Collaborative-edit revision headers from `xl/revisions/revisionHeaders.xml`.
    ///
    /// Populated for legacy shared-workbook `.xlsx` files that contain the
    /// `xl/revisions/` directory. Each `<header>` element maps to one
    /// `DocumentRevision { kind: FormatChange }` carrying the header's `guid`
    /// (→ `revision_id`), `userName` (→ `author`), and `dateTime` (→ `timestamp`).
    /// `anchor` and `delta` are `None`/empty for v1 (per-cell log parsing is a
    /// follow-up). `None` when `xl/revisions/revisionHeaders.xml` is absent.
    pub revisions: Option<Vec<JsDocumentRevision>>,
}
impl<'de> serde::Deserialize<'de> for JsExcelWorkbook {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ExcelWorkbook as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Unified extraction input for all public extraction entry points.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ExtractInput")]
pub struct JsExtractInput {
    /// Source kind. `bytes` requires `bytes`; `uri` requires `uri`.
    #[napi(ts_type = "ExtractInputKind | 'bytes' | 'uri'")]
    pub kind: Option<JsExtractInputKind>,
    /// Raw bytes for `kind = "bytes"`.
    #[napi(ts_type = "Uint8Array | Buffer | Array<number>")]
    pub bytes: Option<JsBytes>,
    /// Local path, `file://` URI, or HTTP(S) URL for `kind = "uri"`.
    pub uri: Option<String>,
    /// MIME type hint.
    #[napi(js_name = "mimeType")]
    pub mime_type: Option<String>,
    /// Filename hint used for MIME detection and metadata.
    pub filename: Option<String>,
    /// Per-input extraction overrides.
    pub config: Option<JsFileExtractionConfig>,
}
impl<'de> serde::Deserialize<'de> for JsExtractInput {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ExtractInput as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "extractInputDefault")]
pub fn extract_input_default() -> JsExtractInput {
    xberg::ExtractInput::default().into()
}
/// Build a bytes input with a MIME type and optional filename hint.
#[napi(js_name = "extractInputFromBytes")]
pub fn extract_input_from_bytes(
    bytes: napi::bindgen_prelude::Buffer,
    mime_type: String,
    filename: Option<String>,
) -> JsExtractInput {
    xberg::ExtractInput::from_bytes(bytes, mime_type, filename).into()
}
/// Build a URI input from a local path, `file://` URI, or HTTP(S) URL.
#[napi(js_name = "extractInputFromUri")]
pub fn extract_input_from_uri(uri: String) -> JsExtractInput {
    xberg::ExtractInput::from_uri(uri).into()
}

/// Document extracted by the core extraction pipeline.
///
/// `extract` and `extract_batch` return an `ExtractionResult` envelope whose
/// `results` field contains these per-document payloads.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractedDocument")]
pub struct JsExtractedDocument {
    /// Plain-text representation of the extracted document content.
    pub content: Option<String>,
    /// MIME type of the source document (e.g. `"application/pdf"`).
    #[napi(js_name = "mimeType")]
    pub mime_type: Option<String>,
    /// Document-level metadata (author, title, dates, format-specific fields).
    pub metadata: Option<JsMetadata>,
    /// Extraction strategy used to produce the returned text.
    ///
    /// Populated when the extractor can reliably distinguish native text extraction,
    /// OCR-only extraction, or mixed native/OCR output.
    #[napi(
        js_name = "extractionMethod",
        ts_type = "ExtractionMethod | 'native' | 'ocr' | 'mixed'"
    )]
    pub extraction_method: Option<JsExtractionMethod>,
    /// Tables extracted from the document, each with structured cell data.
    pub tables: Option<Vec<JsTable>>,
    /// Cheap structural counts (pages, tables, images).
    ///
    /// Always populated by the extraction pipeline, even when the `pages` /
    /// `images` collections are `None`. See `DocumentCounts`.
    pub counts: Option<JsDocumentCounts>,
    /// ISO 639-1 language codes detected in the document content.
    #[napi(js_name = "detectedLanguages")]
    pub detected_languages: Option<Vec<String>>,
    /// Structured per-language detection results: confidence, document share, script,
    /// and reliability, alongside the ISO-code-only `detected_languages` (#261).
    ///
    /// One entry per language in `detected_languages`, in the same order. `None` under
    /// the same conditions as `detected_languages`: detection disabled, empty input
    /// text, or no language met the configured `min_confidence`.
    #[napi(js_name = "detectedLanguageConfidences")]
    pub detected_language_confidences: Option<Vec<JsLanguageConfidence>>,
    /// Text chunks when chunking is enabled.
    ///
    /// When chunking configuration is provided, the content is split into
    /// overlapping chunks for efficient processing. Each chunk contains the text,
    /// optional embeddings (if enabled), and metadata about its position.
    pub chunks: Option<Vec<JsChunk>>,
    /// Extracted images from the document.
    ///
    /// When image extraction is enabled via `ImageExtractionConfig`, this field
    /// contains all images found in the document with their raw data and metadata.
    /// Each image may optionally contain a nested `ocr_result` if OCR was performed.
    pub images: Option<Vec<JsExtractedImage>>,
    /// Per-page content when page extraction is enabled.
    ///
    /// When page extraction is configured, the document is split into per-page content
    /// with tables and images mapped to their respective pages.
    pub pages: Option<Vec<JsPageContent>>,
    /// Semantic elements when element-based result format is enabled.
    ///
    /// When result_format is set to ElementBased, this field contains semantic
    /// elements with type classification, unique identifiers, and metadata for
    /// Unstructured-compatible element-based processing.
    pub elements: Option<Vec<JsElement>>,
    /// Rich Djot content structure (when extracting Djot documents).
    ///
    /// When extracting Djot documents with structured extraction enabled,
    /// this field contains the full semantic structure including:
    /// - Block-level elements with nesting
    /// - Inline formatting with attributes
    /// - Links, images, footnotes
    /// - Math expressions
    /// - Complete attribute information
    ///
    /// The `content` field still contains plain text for backward compatibility.
    ///
    /// Always `None` for non-Djot documents.
    #[napi(js_name = "djotContent")]
    pub djot_content: Option<JsDjotContent>,
    /// OCR elements with full spatial and confidence metadata.
    ///
    /// When OCR is performed with element extraction enabled, this field contains
    /// the structured representation of detected text including:
    /// - Bounding geometry (rectangles or quadrilaterals)
    /// - Confidence scores (detection and recognition)
    /// - Rotation information
    /// - Hierarchical relationships (Tesseract only)
    ///
    /// This field preserves all metadata that would otherwise be lost when
    /// converting to plain text or markdown output formats.
    ///
    /// Only populated when `OcrElementConfig.include_elements` is true.
    #[napi(js_name = "ocrElements")]
    pub ocr_elements: Option<Vec<JsOcrElement>>,
    /// Structured document tree (when document structure extraction is enabled).
    ///
    /// When `include_document_structure` is true in `ExtractionConfig`, this field
    /// contains the full hierarchical representation of the document including:
    /// - Heading-driven section nesting
    /// - Table grids with cell-level metadata
    /// - Content layer classification (body, header, footer, footnote)
    /// - Inline text annotations (formatting, links)
    /// - Bounding boxes and page numbers
    ///
    /// Independent of `result_format` — can be combined with Unified or ElementBased.
    pub document: Option<JsDocumentStructure>,
    /// Extracted keywords when keyword extraction is enabled.
    ///
    /// When keyword extraction (RAKE or YAKE) is configured, this field contains
    /// the extracted keywords with scores, algorithm info, and position data.
    /// Previously stored in `metadata.additional["keywords"]`.
    #[napi(js_name = "extractedKeywords")]
    #[serde(skip)]
    pub extracted_keywords: Option<Vec<JsKeyword>>,
    /// Text cleanliness/readability score from quality analysis.
    ///
    /// A value between 0.0 and 1.0 describing the quality of the text that was
    /// retained. This is not a completeness or recall score: clean text can score
    /// highly even when an extractor omitted or rejected other content. Inspect
    /// `processing_warnings` separately for known degraded or partial extraction.
    /// Previously stored in `metadata.additional["quality_score"]`.
    #[napi(js_name = "qualityScore")]
    pub quality_score: Option<f64>,
    /// Non-fatal warnings collected during processing pipeline stages.
    ///
    /// Captures errors from optional pipeline features (embedding, chunking,
    /// language detection, output formatting) that don't prevent extraction
    /// but may indicate degraded or incomplete results. These warnings are
    /// independent of `quality_score`, which assesses only retained text.
    /// Previously stored as individual keys in `metadata.additional`.
    #[napi(js_name = "processingWarnings")]
    pub processing_warnings: Option<Vec<JsProcessingWarning>>,
    /// PDF annotations extracted from the document.
    ///
    /// When annotation extraction is enabled via `PdfConfig.extract_annotations`,
    /// this field contains text notes, highlights, links, stamps, and other
    /// annotations found in PDF documents.
    pub annotations: Option<Vec<JsPdfAnnotation>>,
    /// Nested extraction results from archive contents.
    ///
    /// When extracting archives, each processable file inside produces its own
    /// full extraction result. Set to `None` for non-archive formats.
    /// Use `max_archive_depth` in config to control recursion depth.
    pub children: Option<Vec<JsArchiveEntry>>,
    /// URIs/links discovered during document extraction.
    ///
    /// Contains hyperlinks, image references, citations, email addresses, and
    /// other URI-like references found in the document. Always extracted when
    /// present in the source document.
    pub uris: Option<Vec<JsExtractedUri>>,
    /// Tracked changes embedded in the source document.
    ///
    /// Populated by per-format extractors that understand change-tracking
    /// metadata (DOCX `w:ins`/`w:del`/`w:rPrChange`, ODT `text:change-*`,
    /// …). Every extractor defaults to `None` until its format-specific
    /// implementation is added. Extractors that do populate this field follow
    /// the "accepted-changes" convention: inserted text is present in
    /// `content`, deleted text is absent — the revision list is the separate
    /// audit trail.
    pub revisions: Option<Vec<JsDocumentRevision>>,
    /// Structured extraction output from LLM-based JSON schema extraction.
    ///
    /// When `structured_extraction` is configured in `ExtractionConfig`, the
    /// extracted document content is sent to a VLM with the provided JSON schema.
    /// The response is parsed and stored here as a JSON value matching the schema.
    #[napi(js_name = "structuredOutput")]
    pub structured_output: Option<serde_json::Value>,
    /// Code intelligence results from tree-sitter analysis.
    ///
    /// Populated when extracting source code files with the `tree-sitter` feature.
    /// Contains metrics, structural analysis, imports/exports, comments,
    /// docstrings, symbols, diagnostics, and optionally chunked code segments.
    ///
    /// Stored as an opaque JSON value so that all language bindings (Go, Java,
    /// C#, …) can deserialize it as a raw JSON object rather than a typed struct.
    /// The underlying type is `tree_sitter_language_pack.ProcessResult`.
    #[napi(js_name = "codeIntelligence")]
    #[serde(skip)]
    pub code_intelligence: Option<serde_json::Value>,
    /// LLM token usage and cost data for all LLM calls made during this extraction.
    ///
    /// Contains one entry per LLM call. Multiple entries are produced when
    /// VLM OCR, structured extraction, or LLM embeddings run during
    /// the same extraction.
    ///
    /// `None` when no LLM was used.
    #[napi(js_name = "llmUsage")]
    pub llm_usage: Option<Vec<JsLlmUsage>>,
    /// Named entities detected in `content` by the NER post-processor.
    ///
    /// `None` when no NER backend is configured. Populated by the `xberg-gliner`
    /// ONNX backend or the LLM-driven backend (see `crates/xberg/src/text/ner/`).
    pub entities: Option<Vec<JsEntity>>,
    /// Summary of `content` produced by the summarisation post-processor.
    ///
    /// `None` when summarisation is not configured. Populated by the TextRank
    /// extractive backend (deterministic, no external service) or by the
    /// liter-llm-driven abstractive backend.
    pub summary: Option<JsDocumentSummary>,
    /// Confidence score computed by the heuristics pipeline.
    ///
    /// Populated when the `heuristics` feature is enabled and confidence
    /// scoring has been performed.  Combines text-coverage, OCR aggregate
    /// confidence, and schema-compliance into a single `[0, 1]` value.
    ///
    /// `None` when confidence scoring is not configured or the feature is
    /// absent.
    #[napi(js_name = "extractionConfidence")]
    #[serde(skip)]
    pub extraction_confidence: Option<JsExtractionConfidence>,
    /// Translation of `content` produced by the translation post-processor.
    ///
    /// `None` when translation is not configured.
    pub translation: Option<JsTranslation>,
    /// Per-page classifications produced by the page-classification post-processor.
    ///
    /// `None` when classification is not configured.
    #[napi(js_name = "pageClassifications")]
    pub page_classifications: Option<Vec<JsPageClassification>>,
    /// Audit report of redactions applied by the redaction post-processor.
    ///
    /// The redaction processor rewrites `content`, `formatted_content`, every
    /// chunk's text, and the textual fields of `entities` / `summary` / `translation` /
    /// `page_classifications` in place. This report describes what was found and how it
    /// was replaced. `None` when redaction is not configured.
    #[napi(js_name = "redactionReport")]
    pub redaction_report: Option<JsRedactionReport>,
    /// Mathematical formulas recognized in the document.
    ///
    /// Populated from every source that produces formulas: layout-guided OCR
    /// (with geometry), VLM OCR (text only), and markup extraction (DOCX,
    /// PPTX, ODT, EPUB, HTML, JATS, LaTeX, Markdown, and related formats,
    /// without geometry). Empty when the document contains no formulas.
    pub formulas: Option<Vec<JsFormula>>,
    /// Form fields extracted from a PDF's AcroForm or XFA structure.
    ///
    /// Populated by the PDF extractor when `PdfConfig.extract_form_fields` is
    /// enabled (default) and the document is a fillable form. Empty otherwise.
    #[napi(js_name = "formFields")]
    pub form_fields: Option<Vec<JsPdfFormField>>,
}

/// Extracted image from a document.
///
/// Contains raw image data, metadata, and optional nested OCR results.
/// Raw bytes allow cross-language compatibility - users can convert to
/// PIL.Image (Python), Sharp (Node.js), or other formats as needed.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ExtractedImage")]
pub struct JsExtractedImage {
    /// Raw image data (PNG, JPEG, WebP, etc. bytes).
    /// Uses `bytes.Bytes` for cheap cloning of large buffers.
    #[napi(ts_type = "Uint8Array | Buffer | Array<number>")]
    pub data: Option<JsBytes>,
    /// Image format (e.g., "jpeg", "png", "webp")
    /// Uses Cow<, str> to avoid allocation for static literals.
    pub format: Option<String>,
    /// Zero-indexed position of this image in the document/page
    #[napi(js_name = "imageIndex")]
    pub image_index: Option<u32>,
    /// Page/slide number where image was found (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: Option<u32>,
    /// Image width in pixels
    pub width: Option<u32>,
    /// Image height in pixels
    pub height: Option<u32>,
    /// Colorspace information (e.g., "RGB", "CMYK", "Gray")
    pub colorspace: Option<String>,
    /// Bits per color component (e.g., 8, 16)
    #[napi(js_name = "bitsPerComponent")]
    pub bits_per_component: Option<u32>,
    /// Whether this image is a mask image
    #[napi(js_name = "isMask")]
    pub is_mask: Option<bool>,
    /// Optional description of the image
    pub description: Option<String>,
    /// Nested OCR extraction result (if image was OCRed)
    ///
    /// When OCR is performed on this image, the result is embedded here
    /// rather than in a separate collection, making the relationship explicit.
    #[napi(js_name = "ocrResult")]
    pub ocr_result: Option<JsExtractedDocument>,
    /// Bounding box of the image on the page (PDF coordinates: x0=left, y0=bottom, x1=right, y1=top).
    /// Only populated for PDF-extracted images when position data is available from the PDF extractor.
    #[napi(js_name = "boundingBox")]
    pub bounding_box: Option<JsBoundingBox>,
    /// Original source path of the image within the document archive (e.g., "media/image1.png" in DOCX).
    /// Used for rendering image references when the binary data is not extracted.
    #[napi(js_name = "sourcePath")]
    pub source_path: Option<String>,
    /// Heuristic classification of what this image likely depicts.
    /// `None` if classification was disabled or inconclusive.
    #[napi(
        js_name = "imageKind",
        ts_type = "ImageKind | 'photograph' | 'diagram' | 'chart' | 'drawing' | 'text_block' | 'decoration' | 'logo' | 'icon' | 'tile_fragment' | 'mask' | 'page_raster' | 'unknown'"
    )]
    pub image_kind: Option<JsImageKind>,
    /// Confidence score for `image_kind`, in the range 0.0 to 1.0.
    #[napi(js_name = "kindConfidence")]
    pub kind_confidence: Option<f64>,
    /// Identifier shared across images that form a single logical figure
    /// (e.g. all raster tiles of one technical drawing). `None` for singletons.
    #[napi(js_name = "clusterId")]
    pub cluster_id: Option<u32>,
    /// VLM-generated caption describing the image, when captioning is configured.
    ///
    /// Populated by the captioning post-processor
    /// (`crates/xberg/src/plugins/processor/builtin/captioning.rs`), which routes
    /// each image through `crate.llm.region_extractor.extract_region_with_vlm` in
    /// caption mode. `None` when captioning is disabled or the VLM declined to caption.
    pub caption: Option<String>,
    /// QR codes decoded from this image, when QR detection is enabled.
    ///
    /// Populated by the QR post-processor (`crates/xberg/src/extractors/qr.rs`) via
    /// the pure-Rust `rqrr` decoder. `None` when QR detection is disabled; an empty
    /// `Some(vec![])` when detection ran but found nothing.
    #[napi(js_name = "qrCodes")]
    pub qr_codes: Option<Vec<JsQrCode>>,
    /// Base64-encoded copy of `data`; populated when `ImageExtractionConfig.include_data_base64`
    /// is `true`. Omitted from JSON by default; use instead of `data` in JSON-only clients.
    #[napi(js_name = "dataBase64")]
    pub data_base64: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsExtractedImage {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ExtractedImage as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A URI extracted from a document.
///
/// Represents any link, reference, or resource pointer found during extraction.
/// The `kind` field classifies the URI semantically, while `label` carries
/// optional human-readable display text.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractedUri")]
pub struct JsExtractedUri {
    /// The URL or path string.
    pub url: String,
    /// Optional display text / label for the link.
    pub label: Option<String>,
    /// Optional page number where the URI was found (1-indexed).
    pub page: Option<u32>,
    /// Semantic classification of the URI.
    #[napi(ts_type = "UriKind | 'hyperlink' | 'image' | 'anchor' | 'citation' | 'reference' | 'email'")]
    pub kind: JsUriKind,
}

/// Combined confidence on `[0, 1]`.
///
/// When OCR did not run, the `ocr_aggregate` weight folds into `text_coverage`
/// so the weighted sum still totals 1.0.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractionConfidence")]
pub struct JsExtractionConfidence {
    /// Fraction of pages with a usable text layer.
    #[napi(js_name = "textCoverage")]
    pub text_coverage: f64,
    /// Mean OCR per-element recognition confidence when OCR ran; `None` when it did not.
    #[napi(js_name = "ocrAggregate")]
    pub ocr_aggregate: Option<f64>,
    /// Whether the merged output validates against the preset schema.
    #[napi(
        js_name = "schemaCompliance",
        ts_type = "SchemaCompliance | 'all_valid' | 'partial_valid' | 'all_invalid'"
    )]
    pub schema_compliance: JsSchemaCompliance,
    /// Weighted blend in `[0, 1]`.  The value compared against the fallback threshold.
    pub combined: f64,
}

/// Main extraction configuration.
///
/// This struct contains all configuration options for the extraction process.
/// It can be loaded from TOML, YAML, or JSON files, or created programmatically.
///
/// # Example
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractionConfig")]
pub struct JsExtractionConfig {
    /// Controls whether MIME inference prefers content, a supported extension, or content alone.
    #[napi(
        js_name = "mimeDetectionPolicy",
        ts_type = "MimeDetectionPolicy | 'prefer_content' | 'trust_extension' | 'content_only'"
    )]
    pub mime_detection_policy: Option<JsMimeDetectionPolicy>,
    /// Enable caching of extraction results
    #[napi(js_name = "useCache")]
    pub use_cache: Option<bool>,
    /// Enable quality post-processing
    #[napi(js_name = "enableQualityProcessing")]
    pub enable_quality_processing: Option<bool>,
    /// OCR configuration.
    ///
    /// `None` does not run OCR for documents that already have usable text. Under
    /// `OcrStrategy.Auto`, a PDF with no text layer at all (a scan) is still routed
    /// to OCR with default settings so it is not returned empty (#1338). Set
    /// `Self.disable_ocr` to hard-disable OCR regardless of the detected content.
    pub ocr: Option<JsOcrConfig>,
    /// Force OCR even for searchable PDFs
    #[napi(js_name = "forceOcr")]
    pub force_ocr: Option<bool>,
    /// Which pages get OCR'd when neither `force_ocr` nor `force_ocr_pages` applies.
    ///
    /// Defaults to `OcrStrategy.Auto`, which OCRs only pages whose native text
    /// fails a quality check. Only applies to PDF documents. Cannot be
    /// `OcrStrategy.ScannedPages` while `disable_ocr` is `true`.
    #[napi(js_name = "ocrStrategy")]
    pub ocr_strategy: Option<JsOcrStrategy>,
    /// Force OCR on specific pages only (1-indexed page numbers, must be >= 1).
    ///
    /// When set, only the listed pages are OCR'd regardless of text layer quality.
    /// Unlisted pages use native text extraction. Ignored when `force_ocr` is `true`.
    /// Only applies to PDF documents. Duplicates are automatically deduplicated.
    /// An `ocr` config is recommended for backend/language selection; defaults are used if absent.
    #[napi(js_name = "forceOcrPages")]
    pub force_ocr_pages: Option<Vec<u32>>,
    /// Disable OCR entirely, even for images.
    ///
    /// When `true`, OCR is skipped for all document types. Images return metadata
    /// only (dimensions, format, EXIF) without text extraction. PDFs use only
    /// native text extraction without OCR fallback.
    ///
    /// Cannot be `true` simultaneously with `force_ocr`.
    #[napi(js_name = "disableOcr")]
    pub disable_ocr: Option<bool>,
    /// Text chunking configuration (undefined = chunking disabled)
    pub chunking: Option<JsChunkingConfig>,
    /// Content filtering configuration (undefined = use extractor defaults).
    ///
    /// Controls whether document "furniture" (headers, footers, watermarks,
    /// repeating text) is included in or stripped from extraction results.
    /// See `ContentFilterConfig` for per-field documentation.
    #[napi(js_name = "contentFilter")]
    pub content_filter: Option<JsContentFilterConfig>,
    /// Image extraction configuration (undefined = no image extraction)
    pub images: Option<JsImageExtractionConfig>,
    /// PDF-specific options (undefined = use defaults)
    #[napi(js_name = "pdfOptions")]
    #[serde(skip)]
    pub pdf_options: Option<JsPdfConfig>,
    /// Token reduction configuration (undefined = no token reduction)
    #[napi(js_name = "tokenReduction")]
    pub token_reduction: Option<JsTokenReductionOptions>,
    /// Language detection configuration (undefined = no language detection)
    #[napi(js_name = "languageDetection")]
    pub language_detection: Option<JsLanguageDetectionConfig>,
    /// Page extraction configuration (undefined = no page tracking)
    pub pages: Option<JsPageConfig>,
    /// Keyword extraction configuration (undefined = no keyword extraction)
    #[serde(skip)]
    pub keywords: Option<JsKeywordConfig>,
    /// Post-processor configuration (undefined = use defaults)
    pub postprocessor: Option<JsPostProcessorConfig>,
    /// HTML to Markdown conversion options (undefined = use defaults)
    ///
    /// Configure how HTML documents are converted to Markdown, including heading styles,
    /// list formatting, code block styles, and preprocessing options.
    #[napi(js_name = "htmlOptions")]
    #[serde(skip)]
    pub html_options: Option<JsConversionOptions>,
    /// Styled HTML output configuration.
    ///
    /// When set alongside `output_format = OutputFormat.Html`, the extraction
    /// pipeline uses `StyledHtmlRenderer`
    /// which emits stable `kb-*` CSS class hooks on every structural element
    /// and optionally embeds theme CSS or user-supplied CSS in a `<style>` block.
    ///
    /// When `None`, the existing plain comrak-based HTML renderer is used.
    #[napi(js_name = "htmlOutput")]
    #[serde(skip)]
    pub html_output: Option<JsHtmlOutputConfig>,
    /// Default per-file timeout in seconds for batch extraction.
    ///
    /// When set, each file in a batch will be canceled after this duration
    /// unless overridden by `FileExtractionConfig.timeout_secs`.
    ///
    /// Defaults to `Some(600)` (10 minutes) to prevent pathological files
    /// (e.g. deeply nested archives, documents with millions of cells) from
    /// running indefinitely and exhausting caller resources, while still
    /// giving slow paths (VLM-based OCR, large scanned documents) enough
    /// headroom to finish. Set to `None` to disable the timeout for trusted
    /// input or long-running workloads.
    #[napi(js_name = "extractionTimeoutSecs")]
    pub extraction_timeout_secs: Option<i64>,
    /// Maximum concurrent document extractions in batch operations.
    ///
    /// This is a ceiling within the configured total thread budget, not an
    /// independent pool size. When unset, the scheduler derives document and
    /// per-document concurrency from `ConcurrencyConfig.max_threads`.
    #[napi(js_name = "maxConcurrentExtractions")]
    pub max_concurrent_extractions: Option<i64>,
    /// Result structure format
    ///
    /// Controls whether results are returned in unified format (default) with all
    /// content in the `content` field, or element-based format with semantic
    /// elements (for Unstructured-compatible output).
    #[napi(js_name = "resultFormat", ts_type = "ResultFormat | 'unified' | 'element_based'")]
    pub result_format: Option<JsResultFormat>,
    /// Security limits for archive extraction.
    ///
    /// Controls maximum archive size, compression ratio, file count, and other
    /// security thresholds to prevent decompression bomb attacks. Also caps
    /// nesting depth, iteration count, entity / token length, total content size,
    /// decoded image allocation, and table cell count for every extraction path
    /// that ingests user-controlled bytes.
    /// When `None`, default limits are used.
    #[napi(js_name = "securityLimits")]
    pub security_limits: Option<JsSecurityLimits>,
    /// Maximum uncompressed size in bytes for a single embedded file before
    /// recursive extraction is attempted (default: 50 MiB).
    ///
    /// Applies to embedded objects inside OOXML containers (DOCX, PPTX) and
    /// to email attachments processed via recursive extraction. Files that
    /// exceed this limit are skipped with a `ProcessingWarning` rather than
    /// passed to the extraction pipeline, preventing a single oversized
    /// embedded object from consuming unbounded memory or time.
    ///
    /// Set to `None` to disable the per-embedded-file cap (falls back to
    /// `security_limits.max_archive_size` as the only guard).
    #[napi(js_name = "maxEmbeddedFileBytes")]
    pub max_embedded_file_bytes: Option<i64>,
    /// Content text format (default: Plain).
    ///
    /// Controls the format of the extracted content:
    /// - `Plain`: Raw extracted text (default)
    /// - `Markdown`: Markdown formatted output
    /// - `Djot`: Djot markup format (requires djot feature)
    /// - `Html`: HTML formatted output
    ///
    /// When set to a structured format, extraction results will include
    /// formatted output. The `formatted_content` field may be populated
    /// when format conversion is applied.
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<JsOutputFormat>,
    /// Escape Markdown special characters in rendered prose (default: `true`).
    ///
    /// When `output_format` is `Markdown` or `Djot`, the renderer backslash-escapes
    /// CommonMark-significant leading characters (e.g. `-`, `#`) so that literal
    /// text such as `#06-18` or `- clause` round-trips safely through a CommonMark
    /// parser instead of being reinterpreted as a heading or list marker.
    ///
    /// Table cell text is never escaped, so escaped prose can look inconsistent
    /// with table cells containing the same characters. Set this to `false` to
    /// disable prose escaping and make `content`, `pages[].content`, and
    /// `chunks[].content` read identically to table cell text — useful for LLM
    /// prompts or search indexing where CommonMark round-tripping does not matter.
    ///
    /// Defaults to `true` to preserve existing behavior.
    #[napi(js_name = "escapeMarkdown")]
    pub escape_markdown: Option<bool>,
    /// Emit an opt-in anchor marker before each table's rendered Markdown
    /// block (default: `false`).
    ///
    /// When `output_format` is `Markdown` (or `Djot`) and this is `true`, the
    /// renderer inserts a `[TABLE:{table_id}]` marker immediately before each
    /// table's Markdown in `content`, `pages[].content`, and
    /// `chunks[].content`, where `table_id` matches the corresponding
    /// entry's `table_id`. This lets a consumer
    /// reconcile a rendered Markdown table block with its structured
    /// `tables[]` entry.
    ///
    /// Defaults to `false` so existing output is byte-identical unless
    /// explicitly enabled.
    #[napi(js_name = "tableAnchors")]
    pub table_anchors: Option<bool>,
    /// Controls how Jupyter notebook (`.ipynb`) code cells are rendered.
    ///
    /// - `Both` (default): code source plus the notebook's saved outputs
    /// - `Source`: only the code source (fenced code blocks)
    /// - `Outputs`: only the saved outputs
    ///
    /// Cells are never executed; `Outputs`/`Both` surface only outputs already
    /// stored in the notebook.
    #[napi(
        js_name = "jupyterCellRendering",
        ts_type = "JupyterCellRendering | 'source' | 'outputs' | 'both'"
    )]
    pub jupyter_cell_rendering: Option<JsJupyterCellRendering>,
    /// Apply Jupyter Book/MyST cell visibility tags while rendering notebooks.
    ///
    /// When enabled, `remove-cell`/`hide-cell`, `remove-input`/`hide-input`,
    /// and `remove-output`/`hide-output` suppress the corresponding saved
    /// source or output. Cells are never executed, and their metadata remains
    /// available even when their rendered content is suppressed.
    ///
    /// Defaults to `true`. Set this to `false` to preserve all saved notebook
    /// content regardless of cell tags.
    #[napi(js_name = "applyNotebookCellTags")]
    pub apply_notebook_cell_tags: Option<bool>,
    /// Layout detection configuration (undefined = layout detection disabled).
    ///
    /// When set, PDF pages and images are analyzed for document structure
    /// (headings, code, formulas, tables, figures, etc.) using RT-DETR models
    /// via ONNX Runtime. For PDFs, layout hints override paragraph classification
    /// in the markdown pipeline. For images, per-region OCR is performed with
    /// markdown formatting based on detected layout classes.
    /// Requires the `layout-detection` feature to run inference; the field is
    /// present whenever the `layout-types` feature is active (which includes
    /// `layout-detection` as well as the no-ORT target groups).
    #[serde(skip)]
    pub layout: Option<JsLayoutDetectionConfig>,
    /// Transcription (speech-to-text) configuration for audio/video files.
    ///
    /// When set and `enabled`, files with audio/video MIME types (mp3, mp4,
    /// m4a, wav, webm, etc.) are routed to the Whisper-based transcription
    /// pipeline. The actual heavy dependencies are only active under the
    /// `transcription` feature; the field is visible under `transcription-types`
    /// (including on WASM and Android targets that use the no-ORT preset).
    ///
    /// Default: `None` (transcription disabled). This is an additive,
    /// non-breaking change.
    #[serde(skip)]
    pub transcription: Option<JsTranscriptionConfig>,
    /// Run layout detection on the non-OCR PDF markdown path.
    ///
    /// When `true` and `layout` is `Some(_)`, layout regions inform reading
    /// order, region grouping, and table detection while native font/tag
    /// semantics remain authoritative for headings, lists, code, and formulas.
    /// OCR layout classification is unchanged. This improves structural output
    /// at the cost of inference latency (~150-300ms/page CPU, ~20-50ms/page
    /// GPU). Default: `false`. Requires the `layout-detection` feature.
    #[napi(js_name = "useLayoutForMarkdown")]
    pub use_layout_for_markdown: Option<bool>,
    /// Enable structured document tree output.
    ///
    /// When true, populates the `document` field on `ExtractedDocument` with a
    /// hierarchical `DocumentStructure` containing heading-driven section nesting,
    /// table grids, content layer classification, and inline annotations.
    ///
    /// Independent of `result_format` — can be combined with Unified or ElementBased.
    #[napi(js_name = "includeDocumentStructure")]
    pub include_document_structure: Option<bool>,
    /// Hardware acceleration configuration for ONNX Runtime models.
    ///
    /// Controls execution provider selection for layout detection and embedding
    /// models. When `None`, uses platform defaults (CoreML on macOS, CUDA on
    /// Linux, CPU on Windows).
    pub acceleration: Option<JsAccelerationConfig>,
    /// Cache namespace for tenant isolation.
    ///
    /// When set, cache entries are stored under `{cache_dir}/{namespace}/`.
    /// Must be alphanumeric, hyphens, or underscores only (max 64 chars).
    /// Different namespaces have isolated cache spaces on the same filesystem.
    #[napi(js_name = "cacheNamespace")]
    pub cache_namespace: Option<String>,
    /// Per-request cache TTL in seconds.
    ///
    /// Overrides the global `max_age_days` for this specific extraction.
    /// When `0`, caching is completely skipped (no read or write).
    /// When `None`, the global TTL applies.
    #[napi(js_name = "cacheTtlSecs")]
    pub cache_ttl_secs: Option<i64>,
    /// Email extraction configuration (undefined = use defaults).
    ///
    /// Currently supports configuring the fallback codepage for MSG files
    /// that do not specify one. See `EmailConfig` for details.
    pub email: Option<JsEmailConfig>,
    /// CSV/TSV extraction configuration (undefined = use defaults).
    ///
    /// Lets callers set an explicit delimiter and declare comment-line
    /// prefixes to skip, instead of relying solely on delimiter
    /// auto-detection. See `CsvConfig` for details.
    pub csv: Option<JsCsvConfig>,
    /// GeoJSON extraction configuration (undefined = bounded summary).
    ///
    /// By default, GeoJSON coordinates are replaced by aggregate counts and bounds
    /// so large geometry arrays do not become unbounded rendered output. Set
    /// `include_full_coordinates`
    /// explicitly to retain the legacy full-coordinate output.
    pub geojson: Option<JsGeoJsonExtractionConfig>,
    /// Concurrency limits for constrained environments (undefined = use defaults).
    ///
    /// Controls Rayon thread pool size, ONNX Runtime intra-op threads, and the
    /// combined document/inner-task budget for batch extraction. See
    /// `ConcurrencyConfig` for details.
    pub concurrency: Option<JsConcurrencyConfig>,
    /// URL ingestion and crawl configuration.
    pub url: Option<JsUrlExtractionConfig>,
    /// Maximum recursion depth for archive extraction (default: 3).
    /// Set to 0 to disable recursive extraction (legacy behavior).
    #[napi(js_name = "maxArchiveDepth")]
    pub max_archive_depth: Option<i64>,
    /// Tree-sitter language pack configuration (undefined = tree-sitter disabled).
    ///
    /// When set, enables code file extraction using tree-sitter parsers.
    /// Controls grammar download behavior and code analysis options.
    #[napi(js_name = "treeSitter")]
    #[serde(skip)]
    pub tree_sitter: Option<JsTreeSitterConfig>,
    /// Structured extraction via LLM (undefined = disabled).
    ///
    /// When set, the extracted document content is sent to an LLM with the
    /// provided JSON schema. The structured response is stored in
    /// `ExtractedDocument.structured_output`.
    #[napi(js_name = "structuredExtraction")]
    pub structured_extraction: Option<JsStructuredExtractionConfig>,
    /// Named-entity recognition configuration. When set, the NER post-processor runs at
    /// the Middle stage and populates `ExtractedDocument.entities`.
    pub ner: Option<JsNerConfig>,
    /// Redaction / anonymisation configuration. When set, the redaction post-processor
    /// runs at the Late stage and rewrites every textual field in `ExtractedDocument`,
    /// emitting an audit trail in `ExtractedDocument.redaction_report`.
    pub redaction: Option<JsRedactionConfig>,
    /// Summarisation configuration. When set, the summarisation post-processor runs at
    /// the Middle stage and populates `ExtractedDocument.summary`.
    pub summarization: Option<JsSummarizationConfig>,
    /// Translation configuration. When set, the translation post-processor runs at the
    /// Middle stage and populates `ExtractedDocument.translation`.
    pub translation: Option<JsTranslationConfig>,
    /// Per-page classification configuration. When set, the classification post-processor
    /// runs at the Middle stage and populates `ExtractedDocument.page_classifications`.
    #[napi(js_name = "pageClassification")]
    pub page_classification: Option<JsPageClassificationConfig>,
    /// Per-chunk multi-label classification configuration. When set, the
    /// chunk-classification post-processor runs at the Middle stage (after
    /// chunking) and populates `ChunkMetadata.classifications` on every chunk.
    #[napi(js_name = "chunkClassification")]
    pub chunk_classification: Option<JsChunkClassificationConfig>,
    /// VLM captioning configuration for extracted images. When set, the captioning
    /// post-processor runs at the Middle stage and writes a caption into each
    /// `ExtractedImage.caption`.
    pub captioning: Option<JsCaptioningConfig>,
    /// Enable QR-code detection in extracted images. When `true`, the QR post-processor
    /// runs at the Middle stage and populates `ExtractedImage.qr_codes`.
    #[napi(js_name = "qrCodes")]
    pub qr_codes: Option<bool>,
}

#[napi(js_name = "extractionConfigDefault")]
pub fn extraction_config_default() -> JsExtractionConfig {
    xberg::ExtractionConfig::default().into()
}

/// The complete diff between two `ExtractedDocument` values.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractionDiff")]
pub struct JsExtractionDiff {
    /// Unified-diff hunks for the `content` field.
    ///
    /// Empty when the content is identical.
    #[napi(js_name = "contentDiff")]
    pub content_diff: Option<Vec<JsDiffHunk>>,
    /// Tables present in `b` but not in `a` (by index position, excess right-side tables).
    #[napi(js_name = "tablesAdded")]
    pub tables_added: Option<Vec<JsTable>>,
    /// Tables present in `a` but not in `b` (by index position, excess left-side tables).
    #[napi(js_name = "tablesRemoved")]
    pub tables_removed: Option<Vec<JsTable>>,
    /// Cell-level changes for table pairs that share the same index and dimensions.
    #[napi(js_name = "tablesChanged")]
    pub tables_changed: Option<Vec<JsTableDiff>>,
    /// Metadata difference, encoded as a JSON object with three top-level keys:
    /// `added` (keys present in `b` but not `a`), `removed` (keys present in `a`
    /// but not `b`), and `changed` (keys whose values differ — each entry is
    /// `{ "from": <value-in-a>, "to": <value-in-b> }`).
    ///
    /// This is NOT RFC 6902 JSON Patch — we deliberately chose a flatter shape
    /// to avoid pulling in a json-patch crate. If you need RFC 6902 semantics
    /// (with JSON Pointer paths) feed `a.metadata` and `b.metadata` to your
    /// preferred json-patch impl directly.
    #[napi(js_name = "metadataChanged")]
    pub metadata_changed: Option<serde_json::Value>,
    /// Changes to embedded archive children.
    #[napi(js_name = "embeddedChanges")]
    pub embedded_changes: Option<JsEmbeddedChanges>,
}

/// Non-fatal per-input extraction error captured by `ExtractionResult`.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractionErrorItem")]
pub struct JsExtractionErrorItem {
    /// Input index in the original request.
    pub index: i64,
    /// Stable numeric error code.
    pub code: u32,
    /// Stable snake_case error kind.
    #[napi(js_name = "errorType")]
    pub error_type: String,
    /// Best-effort source identifier.
    pub source: String,
    /// Error message.
    pub message: String,
}

/// Unified extraction result envelope.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ExtractionResult")]
pub struct JsExtractionResult {
    /// Extracted documents in discovery order.
    pub results: Option<Vec<JsExtractedDocument>>,
    /// Non-fatal per-input errors.
    pub errors: Option<Vec<JsExtractionErrorItem>>,
    /// Aggregate counts for the operation.
    pub summary: Option<JsExtractionSummary>,
    /// Final URLs reached after redirects during URL ingestion.
    #[napi(js_name = "crawlFinalUrls")]
    pub crawl_final_urls: Option<Vec<String>>,
    /// Total redirects followed while fetching or crawling URLs.
    #[napi(js_name = "crawlRedirectCount")]
    pub crawl_redirect_count: Option<i64>,
    /// Unique normalized URLs discovered by crawls.
    #[napi(js_name = "crawlUniqueNormalizedUrls")]
    pub crawl_unique_normalized_urls: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsExtractionResult {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ExtractionResult as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Build an output containing one successful result.
#[napi(js_name = "extractionResultSingle")]
pub fn extraction_result_single(result: JsExtractedDocument) -> JsExtractionResult {
    let result_core: xberg::ExtractedDocument = result.into();

    xberg::ExtractionResult::single(result_core).into()
}

/// Summary for a unified extraction call.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ExtractionSummary")]
pub struct JsExtractionSummary {
    /// Number of inputs submitted by the caller.
    pub inputs: Option<i64>,
    /// Number of extraction results produced.
    pub results: Option<i64>,
    /// Number of per-input errors.
    pub errors: Option<i64>,
    /// Number of URI inputs that resolved to remote HTTP(S) URLs.
    #[napi(js_name = "remoteUrls")]
    pub remote_urls: Option<i64>,
    /// Number of HTML pages crawled or scraped.
    #[napi(js_name = "pagesCrawled")]
    pub pages_crawled: Option<i64>,
    /// Number of downloaded non-HTML documents extracted from URLs.
    #[napi(js_name = "documentsDownloaded")]
    pub documents_downloaded: Option<i64>,
}

/// FictionBook (FB2) metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "FictionBookMetadata")]
pub struct JsFictionBookMetadata {
    /// Genre tags as declared in the FB2 `<genre>` elements.
    pub genres: Option<Vec<String>>,
    /// Book series (sequence) names, if any.
    pub sequences: Option<Vec<String>>,
    /// Short annotation / summary from the FB2 `<annotation>` element.
    pub annotation: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsFictionBookMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::FictionBookMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Per-file extraction configuration overrides for batch processing.
///
/// All fields are `Option<T>` — `None` means "use the batch-level default."
/// This type is used by `config` and `extract_batch`
/// to allow heterogeneous extraction settings within a single batch.
///
/// # Excluded Fields
///
/// The following `ExtractionConfig` fields are batch-level only and
/// cannot be overridden per file:
/// - `max_concurrent_extractions` — controls batch parallelism
/// - `use_cache` — global caching policy
/// - `acceleration` — shared ONNX execution provider
/// - `security_limits` — global archive security policy
///
/// # Example
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "FileExtractionConfig")]
pub struct JsFileExtractionConfig {
    /// Override MIME inference policy for this file.
    #[napi(
        js_name = "mimeDetectionPolicy",
        ts_type = "MimeDetectionPolicy | 'prefer_content' | 'trust_extension' | 'content_only'"
    )]
    pub mime_detection_policy: Option<JsMimeDetectionPolicy>,
    /// Override quality post-processing for this file.
    #[napi(js_name = "enableQualityProcessing")]
    pub enable_quality_processing: Option<bool>,
    /// Override OCR configuration for this file (undefined in the Option = use batch default).
    pub ocr: Option<JsOcrConfig>,
    /// Override force OCR for this file.
    #[napi(js_name = "forceOcr")]
    pub force_ocr: Option<bool>,
    /// Override the OCR page-selection strategy for this file.
    #[napi(js_name = "ocrStrategy")]
    pub ocr_strategy: Option<JsOcrStrategy>,
    /// Override force OCR pages for this file (1-indexed page numbers).
    #[napi(js_name = "forceOcrPages")]
    pub force_ocr_pages: Option<Vec<u32>>,
    /// Override disable OCR for this file.
    #[napi(js_name = "disableOcr")]
    pub disable_ocr: Option<bool>,
    /// Override chunking configuration for this file.
    pub chunking: Option<JsChunkingConfig>,
    /// Override content filtering configuration for this file.
    #[napi(js_name = "contentFilter")]
    pub content_filter: Option<JsContentFilterConfig>,
    /// Override image extraction configuration for this file.
    pub images: Option<JsImageExtractionConfig>,
    /// Override PDF options for this file.
    #[napi(js_name = "pdfOptions")]
    #[serde(skip)]
    pub pdf_options: Option<JsPdfConfig>,
    /// Override token reduction for this file.
    #[napi(js_name = "tokenReduction")]
    pub token_reduction: Option<JsTokenReductionOptions>,
    /// Override language detection for this file.
    #[napi(js_name = "languageDetection")]
    pub language_detection: Option<JsLanguageDetectionConfig>,
    /// Override page extraction for this file.
    pub pages: Option<JsPageConfig>,
    /// Override keyword extraction for this file.
    #[serde(skip)]
    pub keywords: Option<JsKeywordConfig>,
    /// Override post-processor for this file.
    pub postprocessor: Option<JsPostProcessorConfig>,
    /// Override HTML conversion options for this file.
    #[napi(js_name = "htmlOptions")]
    #[serde(skip)]
    pub html_options: Option<JsConversionOptions>,
    /// Override styled HTML output configuration for this file.
    #[napi(js_name = "htmlOutput")]
    #[serde(skip)]
    pub html_output: Option<JsHtmlOutputConfig>,
    /// Override result format for this file.
    #[napi(js_name = "resultFormat", ts_type = "ResultFormat | 'unified' | 'element_based'")]
    pub result_format: Option<JsResultFormat>,
    /// Override output content format for this file.
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<JsOutputFormat>,
    /// Override document structure output for this file.
    #[napi(js_name = "includeDocumentStructure")]
    pub include_document_structure: Option<bool>,
    /// Override layout detection for this file.
    #[serde(skip)]
    pub layout: Option<JsLayoutDetectionConfig>,
    /// Transcription configuration (see ExtractionConfig for docs).
    #[serde(skip)]
    pub transcription: Option<JsTranscriptionConfig>,
    /// Override per-file extraction timeout in seconds.
    ///
    /// When set, the extraction for this file will be canceled after the
    /// specified duration. A timed-out file produces an error result without
    /// affecting other files in the batch.
    #[napi(js_name = "timeoutSecs")]
    pub timeout_secs: Option<i64>,
    /// Override tree-sitter configuration for this file.
    #[napi(js_name = "treeSitter")]
    #[serde(skip)]
    pub tree_sitter: Option<JsTreeSitterConfig>,
    /// Override structured extraction configuration for this file.
    ///
    /// When set, enables LLM-based structured extraction with a JSON schema
    /// for this specific file. The extracted content is sent to a VLM/LLM
    /// and the response is parsed according to the provided schema.
    #[napi(js_name = "structuredExtraction")]
    pub structured_extraction: Option<JsStructuredExtractionConfig>,
    /// Override URL ingestion and crawl configuration for this file.
    pub url: Option<JsUrlExtractionConfig>,
    /// Override named-entity recognition configuration for this file.
    pub ner: Option<JsNerConfig>,
    /// Override redaction configuration for this file.
    pub redaction: Option<JsRedactionConfig>,
    /// Override summarization configuration for this file.
    pub summarization: Option<JsSummarizationConfig>,
    /// Override translation configuration for this file.
    pub translation: Option<JsTranslationConfig>,
    /// Override per-page classification configuration for this file.
    #[napi(js_name = "pageClassification")]
    pub page_classification: Option<JsPageClassificationConfig>,
    /// Override per-chunk classification configuration for this file.
    #[napi(js_name = "chunkClassification")]
    pub chunk_classification: Option<JsChunkClassificationConfig>,
    /// Override VLM captioning configuration for this file.
    pub captioning: Option<JsCaptioningConfig>,
    /// Override QR-code detection for this file.
    #[napi(js_name = "qrCodes")]
    pub qr_codes: Option<bool>,
}

/// Footnote in Djot.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "Footnote")]
pub struct JsFootnote {
    /// Footnote label
    pub label: String,
    /// Footnote content blocks
    pub content: Vec<JsFormattedBlock>,
}

/// A footnote anchor reference in markdown text.
///
/// Represents a `[^label]` use-site (not a definition).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "FootnoteAnchor")]
pub struct JsFootnoteAnchor {
    /// The label of the footnote reference (e.g., "1" in `[^1]`).
    pub label: String,
    /// Byte offset of the anchor in the markdown text.
    pub offset: i64,
}

/// Configuration for markdown footnote and citation parsing.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "FootnoteConfig")]
pub struct JsFootnoteConfig {
    /// Whether to parse the structured citation block (default: true).
    ///
    /// When enabled, the parser will look for and extract citations from
    /// the block after `---` + `<!-- citations ... -->`.
    #[napi(js_name = "parseCitations")]
    pub parse_citations: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsFootnoteConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::FootnoteConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "footnoteConfigDefault")]
pub fn footnote_config_default() -> JsFootnoteConfig {
    xberg::FootnoteConfig::default().into()
}
/// Set whether to parse the citation block.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "footnoteConfigWithParseCitations")]
pub fn footnote_config_with_parse_citations(cfg: JsFootnoteConfig, enabled: bool) -> napi::Result<JsFootnoteConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::FootnoteConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_parse_citations(enabled);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// A footnote definition from markdown text.
///
/// Represents `[^label]: content` declarations (including multi-line continuations).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "FootnoteDefinition")]
pub struct JsFootnoteDefinition {
    /// The label of the footnote (e.g., "1" in `[^1]: ...`).
    pub label: String,
    /// The full content of the footnote definition.
    pub content: String,
    /// Byte offset of the definition line in the markdown text.
    pub offset: i64,
}

/// Block-level element in a Djot document.
///
/// Represents structural elements like headings, paragraphs, lists, code blocks, etc.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "FormattedBlock")]
pub struct JsFormattedBlock {
    /// Type of block element
    #[napi(
        js_name = "blockType",
        ts_type = "BlockType | 'paragraph' | 'heading' | 'blockquote' | 'code_block' | 'list_item' | 'ordered_list' | 'bullet_list' | 'task_list' | 'definition_list' | 'definition_term' | 'definition_description' | 'div' | 'section' | 'thematic_break' | 'raw_block' | 'math_display'"
    )]
    pub block_type: JsBlockType,
    /// Heading level (1-6) for headings, or nesting level for lists
    pub level: Option<i64>,
    /// Inline content within the block
    #[napi(js_name = "inlineContent")]
    pub inline_content: Vec<JsInlineElement>,
    /// Element attributes (classes, IDs, key-value pairs)
    pub attributes: Option<JsAttributes>,
    /// Language identifier for code blocks
    pub language: Option<String>,
    /// Raw code content for code blocks
    pub code: Option<String>,
    /// Nested blocks for containers (blockquotes, list items, divs)
    pub children: Vec<JsFormattedBlock>,
}
impl<'de> serde::Deserialize<'de> for JsFormattedBlock {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::FormattedBlock as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A mathematical formula extracted from a document.
///
/// Three kinds of sources populate this type. Layout-guided OCR detects
/// formula regions and recognizes them; those formulas carry a `bbox` and a
/// `page`. VLM OCR recognizes formulas in transcribed text without layout, so
/// its formulas carry no geometry. Markup extraction (DOCX, PPTX, ODT, EPUB,
/// HTML, JATS, LaTeX, Markdown, and related formats) converts embedded math
/// to LaTeX, also without geometry.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Formula")]
pub struct JsFormula {
    /// LaTeX source of the formula, without surrounding `$$` delimiters.
    ///
    /// Markup converters and formula OCR produce real LaTeX. The native PDF
    /// layout path stores the plain text of a detected formula region, which
    /// keeps the original Unicode math characters instead of LaTeX commands.
    /// To render the formula in Markdown or other formats, wrap it in `$$..$$`.
    pub latex: String,
    /// Bounding box of the formula region on its page. `None` for markup sources.
    ///
    /// PDF OCR sources report PDF point coordinates with the origin at the
    /// bottom-left of the page, comparable to native PDF geometry. Image
    /// sources, and PDF pages whose geometry is unavailable, report pixels of
    /// the image the OCR backend saw. The C FFI reports an absent bbox as a
    /// null pointer.
    pub bbox: Option<JsBoundingBox>,
    /// 1-indexed page number the formula appears on. `None` when the source
    /// format has no page concept. The C FFI reports an absent page as `0`.
    pub page: Option<u32>,
}
impl<'de> serde::Deserialize<'de> for JsFormula {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Formula as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for GeoJSON extraction.
///
/// GeoJSON coordinates can dominate extraction output and duplicate large geometry
/// payloads in rendered content and metadata. The default therefore emits a bounded
/// aggregate summary. Set `Self.include_full_coordinates` only when callers need
/// every coordinate in the extracted text and accept output proportional to the input.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "GeoJsonExtractionConfig")]
pub struct JsGeoJsonExtractionConfig {
    /// Include every coordinate in rendered content and `flattened_fields` metadata.
    ///
    /// Defaults to `false`. When false, extraction reports feature, property, geometry,
    /// position-count, bounds, truncation, and discarded-category metadata and emits a
    /// `ProcessingWarning` for every GeoJSON input.
    #[napi(js_name = "includeFullCoordinates")]
    pub include_full_coordinates: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsGeoJsonExtractionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::GeoJsonExtractionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Runtime options accepted by the `candle-glm-ocr` backend.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "GlmOcrBackendOptions")]
pub struct JsGlmOcrBackendOptions {
    /// Optional recognition task; the backend constructor's task is used when absent.
    #[napi(ts_type = "GlmOcrTaskKind | 'ocr' | 'table' | 'formula' | 'chart' | 'caption'")]
    pub task: Option<JsGlmOcrTaskKind>,
    /// Optional per-call device override.
    #[napi(ts_type = "CandleDevicePreference | 'auto' | 'cpu' | 'cuda' | 'metal'")]
    pub device: Option<JsCandleDevicePreference>,
    /// Optional whole-page or layout-paired dispatch mode.
    #[napi(js_name = "layoutMode", ts_type = "GlmOcrLayoutMode | 'whole_page' | 'paired'")]
    pub layout_mode: Option<JsGlmOcrLayoutMode>,
    /// Whether chart regions use chart understanding instead of captioning.
    #[napi(js_name = "enableChartUnderstanding")]
    pub enable_chart_understanding: Option<bool>,
    /// Optional Hugging Face cache root.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsGlmOcrBackendOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::candle_ocr::GlmOcrBackendOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Individual grid cell with position and span metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "GridCell")]
pub struct JsGridCell {
    /// Cell text content.
    pub content: String,
    /// Zero-indexed row position.
    pub row: u32,
    /// Zero-indexed column position.
    pub col: u32,
    /// Number of rows this cell spans.
    #[napi(js_name = "rowSpan")]
    pub row_span: u32,
    /// Number of columns this cell spans.
    #[napi(js_name = "colSpan")]
    pub col_span: u32,
    /// Whether this is a header cell.
    #[napi(js_name = "isHeader")]
    pub is_header: bool,
    /// Bounding box for this cell (if available).
    pub bbox: Option<JsBoundingBox>,
    /// Outline level (1-6) of the heading style this cell's text carries, when it has one.
    ///
    /// A DOCX banner row -- row 0, one cell spanning the grid, styled `Heading1`..`Heading6` --
    /// is what Word's navigation pane and a `TOC` field treat as the document's outline, but as
    /// a table cell it reached consumers as anonymous text (GH#1587). `content` is deliberately
    /// left as the bare cell text: prefixing it with `#` would put a markdown heading inside a
    /// table cell, which is invalid where it lands and changes text every existing consumer
    /// already reads. This field is the signal instead, so a caller can decide for itself
    /// whether a `heading 2` in a banner row is a section title or a column label.
    #[napi(js_name = "headingLevel")]
    pub heading_level: Option<u8>,
    /// Human-readable name of the paragraph style applied to this cell's text (`heading 2`).
    ///
    /// Carries the style even when it resolves to no outline level, so a caller can key on a
    /// named style this crate does not map to a heading. See `GridCell.heading_level`.
    #[napi(js_name = "styleName")]
    pub style_name: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsGridCell {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::GridCell as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Header/heading element metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HeaderMetadata")]
pub struct JsHeaderMetadata {
    /// Header level: 1 (h1) through 6 (h6)
    pub level: u8,
    /// Normalized text content of the header
    pub text: String,
    /// HTML id attribute if present
    pub id: Option<String>,
    /// Document tree depth at the header element
    pub depth: u32,
    /// Byte offset in original HTML document
    #[napi(js_name = "htmlOffset")]
    pub html_offset: u32,
}

/// Heading context for a chunk within a Markdown document.
///
/// Contains the heading hierarchy from document root to this chunk's section.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HeadingContext")]
pub struct JsHeadingContext {
    /// The heading hierarchy from document root to this chunk's section.
    /// Index 0 is the outermost (h1), last element is the most specific.
    pub headings: Vec<JsHeadingLevel>,
}

/// A single heading in the hierarchy.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HeadingLevel")]
pub struct JsHeadingLevel {
    /// Heading depth (1 = h1, 2 = h2, etc.)
    pub level: u8,
    /// The text content of the heading.
    pub text: String,
}

/// Configuration for document chunking and analysis heuristics.
///
/// Every threshold is a public field so callers can override any subset via
/// struct-update syntax: `HeuristicsConfig { text_layer_threshold: 0.5, ..Default.default() }`.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HeuristicsConfig")]
pub struct JsHeuristicsConfig {
    /// Enable PDF text-layer detection heuristics.
    ///
    /// When `true`, PDFs with a substantial text layer will skip chunking.
    /// Default: `true`.
    #[napi(js_name = "enablePdfTextHeuristics")]
    pub enable_pdf_text_heuristics: Option<bool>,
    /// Minimum fraction of pages that must have text to skip chunking.
    ///
    /// Range `0.0..=1.0`. Default: `0.7` (70 % of pages).
    #[napi(js_name = "textLayerThreshold")]
    pub text_layer_threshold: Option<f64>,
    /// File size threshold in bytes for considering chunking.
    ///
    /// Files smaller than this are processed without chunking.
    /// Default: 10 MiB (10 × 1 024 × 1 024).
    #[napi(js_name = "fileSizeThresholdBytes")]
    pub file_size_threshold_bytes: Option<i64>,
    /// Page count threshold for considering chunking.
    ///
    /// Documents with fewer pages are processed without chunking.
    /// Default: 50.
    #[napi(js_name = "pageCountThreshold")]
    pub page_count_threshold: Option<u32>,
    /// Target number of pages per chunk for optimal parallel processing.
    ///
    /// Default: 10.
    #[napi(js_name = "targetPagesPerChunk")]
    pub target_pages_per_chunk: Option<u32>,
    /// Hard cap on pages per chunk.
    ///
    /// No chunk will exceed this limit. Must be ≥ `target_pages_per_chunk`.
    /// Default: 25.
    #[napi(js_name = "maxPagesPerChunk")]
    pub max_pages_per_chunk: Option<u32>,
    /// File size threshold for disk-based processing.
    ///
    /// Files larger than this are buffered to disk to prevent OOM.
    /// Default: 50 MiB (50 × 1 024 × 1 024).
    #[napi(js_name = "diskProcessingThresholdBytes")]
    pub disk_processing_threshold_bytes: Option<i64>,
    /// Minimum characters per page to consider a page as having text.
    ///
    /// Default: 50.
    #[napi(js_name = "minCharsPerPage")]
    pub min_chars_per_page: Option<u32>,
    /// Maximum sheet count allowed in an XLSX workbook.
    ///
    /// Workbooks beyond this are rejected pre-extraction to avoid OOM /
    /// abusive billing inflation. Default: 200.
    #[napi(js_name = "maxXlsxSheetCount")]
    pub max_xlsx_sheet_count: Option<u32>,
    /// Maximum cell count (sheets × rows × columns approximation) in an XLSX workbook.
    ///
    /// Default: 5 000 000 (≈ 200 sheets × 25 k cells).
    #[napi(js_name = "maxXlsxWorkbookCells")]
    pub max_xlsx_workbook_cells: Option<i64>,
    /// Maximum number of OLE-embedded objects extractable from a single PPTX or DOCX.
    ///
    /// Protects against zip-bomb-style nested-document abuse. Default: 50.
    #[napi(js_name = "maxPptxEmbeddedCount")]
    pub max_pptx_embedded_count: Option<u32>,
}

#[napi(js_name = "heuristicsConfigDefault")]
pub fn heuristics_config_default() -> JsHeuristicsConfig {
    xberg::HeuristicsConfig::default().into()
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HierarchicalBlock")]
pub struct JsHierarchicalBlock {
    /// The text content of this block
    pub text: String,
    /// The font size of the text in this block
    #[napi(js_name = "fontSize")]
    pub font_size: f64,
    /// The hierarchy level of this block (H1-H6 or Body)
    ///
    /// Levels correspond to HTML heading tags:
    /// - "h1": Top-level heading
    /// - "h2": Secondary heading
    /// - "h3": Tertiary heading
    /// - "h4": Quaternary heading
    /// - "h5": Quinary heading
    /// - "h6": Senary heading
    /// - "body": Body text (no heading level)
    pub level: String,
    /// Bounding box information for the block
    ///
    /// Contains left, top, right, and bottom coordinates in PDF units.
    pub bbox: Option<JsHierarchicalBoundingBox>,
}

/// A text block with hierarchy level assignment.
///
/// Represents a block of text with semantic heading information extracted from
/// font size clustering and hierarchical analysis.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HierarchicalBoundingBox")]
pub struct JsHierarchicalBoundingBox {
    /// Left coordinate.
    pub left: Option<f64>,
    /// Top coordinate.
    pub top: Option<f64>,
    /// Right coordinate.
    pub right: Option<f64>,
    /// Bottom coordinate.
    pub bottom: Option<f64>,
}

/// Hierarchy extraction configuration for PDF text structure analysis.
///
/// Enables extraction of document hierarchy levels (H1-H6) based on font size
/// clustering and semantic analysis. When enabled, hierarchical blocks are
/// included in page content.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "HierarchyConfig")]
pub struct JsHierarchyConfig {
    /// Enable hierarchy extraction
    pub enabled: Option<bool>,
    /// Number of font size clusters to use for hierarchy levels (1-7)
    ///
    /// Default: 3, which provides two heading levels plus body text.
    /// Larger values create more fine-grained hierarchy levels.
    #[napi(js_name = "kClusters")]
    pub k_clusters: Option<i64>,
    /// Include bounding box information in hierarchy blocks
    #[napi(js_name = "includeBbox")]
    pub include_bbox: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsHierarchyConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::HierarchyConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "hierarchyConfigDefault")]
pub fn hierarchy_config_default() -> JsHierarchyConfig {
    xberg::HierarchyConfig::default().into()
}

/// HTML metadata extracted from HTML documents.
///
/// Includes document-level metadata, Open Graph data, Twitter Card metadata,
/// and extracted structural elements (headers, links, images, structured data).
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "HtmlMetadata")]
pub struct JsHtmlMetadata {
    /// Document title from `<title>` tag
    pub title: Option<String>,
    /// Document description from `<meta name="description">` tag
    pub description: Option<String>,
    /// Document keywords from `<meta name="keywords">` tag, split on commas
    pub keywords: Option<Vec<String>>,
    /// Document author from `<meta name="author">` tag
    pub author: Option<String>,
    /// Canonical URL from `<link rel="canonical">` tag
    #[napi(js_name = "canonicalUrl")]
    pub canonical_url: Option<String>,
    /// Base URL from `<base href="">` tag for resolving relative URLs
    #[napi(js_name = "baseHref")]
    pub base_href: Option<String>,
    /// Document language from `lang` attribute
    pub language: Option<String>,
    /// Document text direction from `dir` attribute
    #[napi(js_name = "textDirection", ts_type = "TextDirection | 'ltr' | 'rtl' | 'auto'")]
    pub text_direction: Option<JsTextDirection>,
    /// Open Graph metadata (og:* properties) for social media
    /// Keys like "title", "description", "image", "url", etc.
    #[napi(js_name = "openGraph")]
    pub open_graph: Option<HashMap<String, String>>,
    /// Twitter Card metadata (twitter:* properties)
    /// Keys like "card", "site", "creator", "title", "description", "image", etc.
    #[napi(js_name = "twitterCard")]
    pub twitter_card: Option<HashMap<String, String>>,
    /// Additional meta tags not covered by specific fields
    /// Keys are meta name/property attributes, values are content
    #[napi(js_name = "metaTags")]
    pub meta_tags: Option<HashMap<String, String>>,
    /// Extracted header elements with hierarchy
    pub headers: Option<Vec<JsHeaderMetadata>>,
    /// Extracted hyperlinks with type classification
    pub links: Option<Vec<JsLinkMetadata>>,
    /// Extracted images with source and dimensions
    pub images: Option<Vec<JsImageMetadataType>>,
    /// Extracted structured data blocks
    #[napi(js_name = "structuredData")]
    pub structured_data: Option<Vec<JsStructuredData>>,
}
impl<'de> serde::Deserialize<'de> for JsHtmlMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::HtmlMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for styled HTML output.
///
/// When set on `html_output` alongside
/// `output_format = OutputFormat.Html`, the pipeline builds a
/// `StyledHtmlRenderer` instead of
/// the plain comrak-based renderer.
///
/// # Example
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "HtmlOutputConfig")]
pub struct JsHtmlOutputConfig {
    /// Inline CSS string injected into the output after the theme stylesheet.
    /// Concatenated after `css_file` content when both are set.
    pub css: Option<String>,
    /// Path to a CSS file loaded once at renderer construction time.
    /// Concatenated before `css` when both are set.
    #[napi(js_name = "cssFile")]
    pub css_file: Option<String>,
    /// Built-in colour/typography theme. Default: `HtmlTheme.Unstyled`.
    #[napi(ts_type = "HtmlTheme | 'default' | 'github' | 'dark' | 'light' | 'unstyled'")]
    pub theme: Option<JsHtmlTheme>,
    /// CSS class prefix applied to every emitted class name.
    ///
    /// Default: `"kb-"`. Change this if your host application already uses
    /// classes that start with `kb-`.
    #[napi(js_name = "classPrefix")]
    pub class_prefix: Option<String>,
    /// When `true` (default), write the resolved CSS into a `<style>` block
    /// immediately after the opening `<div class="{prefix}doc">`.
    ///
    /// Set to `false` to emit only the structural markup and wire up your
    /// own stylesheet targeting the `kb-*` class names.
    #[napi(js_name = "embedCss")]
    pub embed_css: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsHtmlOutputConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::HtmlOutputConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "htmlOutputConfigDefault")]
pub fn html_output_config_default() -> JsHtmlOutputConfig {
    xberg::HtmlOutputConfig::default().into()
}

/// Image dimensions in pixels.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageDimensions")]
pub struct JsImageDimensions {
    /// Width in pixels.
    pub width: Option<u32>,
    /// Height in pixels.
    pub height: Option<u32>,
}

/// Horizontal and vertical image resolution in dots per inch.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageDpi")]
pub struct JsImageDpi {
    /// Horizontal resolution.
    pub horizontal: Option<f64>,
    /// Vertical resolution.
    pub vertical: Option<f64>,
}

/// Image extraction configuration.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageExtractionConfig")]
pub struct JsImageExtractionConfig {
    /// Extract images from documents
    #[napi(js_name = "extractImages")]
    pub extract_images: Option<bool>,
    /// Target DPI for image normalization
    #[napi(js_name = "targetDpi")]
    pub target_dpi: Option<i32>,
    /// Maximum dimension for images (width or height)
    #[napi(js_name = "maxImageDimension")]
    pub max_image_dimension: Option<i32>,
    /// Whether to inject image reference placeholders into markdown output.
    /// When `true` (default), image references like `![Image 1](embedded:p1_i0)`
    /// are appended to the markdown. Set to `false` to extract images as data
    /// without polluting the markdown output.
    #[napi(js_name = "injectPlaceholders")]
    pub inject_placeholders: Option<bool>,
    /// Automatically adjust DPI based on image content
    #[napi(js_name = "autoAdjustDpi")]
    pub auto_adjust_dpi: Option<bool>,
    /// Minimum DPI threshold
    #[napi(js_name = "minDpi")]
    pub min_dpi: Option<i32>,
    /// Maximum DPI threshold
    #[napi(js_name = "maxDpi")]
    pub max_dpi: Option<i32>,
    /// Maximum number of image objects to extract per PDF page.
    ///
    /// Some PDFs (e.g. technical diagrams stored as thousands of raster fragments)
    /// can trigger extremely long or indefinite extraction times when every image
    /// object on a dense page is decoded individually via the PDF extractor. Setting this
    /// limit causes xberg to stop collecting individual images once the count
    /// per page reaches the cap and emit a warning instead.
    ///
    /// `None` (default) means no limit — all images are extracted.
    #[napi(js_name = "maxImagesPerPage")]
    pub max_images_per_page: Option<u32>,
    /// When `true`, extracted images are classified by kind and grouped
    /// into clusters where they appear to belong to one figure.
    /// Defaults to `false` — opt in explicitly to avoid unexpected ML overhead.
    pub classify: Option<bool>,
    /// When `true`, full-page renders produced during OCR preprocessing are captured
    /// and returned as `ImageKind.PageRaster` entries in `ExtractedDocument.images`.
    ///
    /// **PDF + OCR only.** No rasters are captured for non-PDF inputs or when the
    /// document-level OCR bypass is active (whole-document backend). When OCR is
    /// enabled and this flag is set but the active backend skips per-page rendering,
    /// a `ProcessingWarning` is emitted in `ExtractedDocument.processing_warnings`.
    ///
    /// Defaults to `false`. Enable when downstream consumers need page thumbnails
    /// (e.g. citation previews, visual grounding).
    #[napi(js_name = "includePageRasters")]
    pub include_page_rasters: Option<bool>,
    /// Run OCR on extracted images and include the recognized text in the document content.
    ///
    /// When `true` (default) and `ExtractionConfig.ocr` is configured, extracted images
    /// are processed with the configured OCR backend. Set to `false` to extract images
    /// without OCR processing, even when OCR is enabled.
    #[napi(js_name = "runOcrOnImages")]
    pub run_ocr_on_images: Option<bool>,
    /// When `true`, image OCR results are rendered as plain text without the
    /// `![...](...)` markdown placeholder. Only takes effect when `run_ocr_on_images`
    /// is also `true`.
    #[napi(js_name = "ocrTextOnly")]
    pub ocr_text_only: Option<bool>,
    /// When `true` and `ocr_text_only` is `false`, append the OCR text after
    /// the image placeholder in the rendered output.
    #[napi(js_name = "appendOcrText")]
    pub append_ocr_text: Option<bool>,
    /// Target format for re-encoding extracted images.
    ///
    /// When set to anything other than `Native`, each extracted image is
    /// re-encoded to the requested format before being returned. This lets
    /// callers receive uniform output without duplicating encode logic
    /// downstream.
    ///
    /// Defaults to `Native` — no re-encode pass is performed and
    /// `ExtractedImage.format` reflects the source extractor's output.
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<JsImageOutputFormat>,
    /// SVG-specific knobs for the image-encode pipeline.
    ///
    /// Controls sanitization and rasterization DPI when the source or output
    /// format is SVG.  Only available when the `svg` feature is active.
    #[serde(skip)]
    pub svg: Option<JsSvgOptions>,
    /// When `true`, populate `ExtractedImage.data_base64` with a Base64-encoded
    /// copy of the raw image bytes.
    ///
    /// Useful for JSON-only clients that cannot efficiently parse the default
    /// integer-array serialization of `data`. Defaults to `false`; enabling it
    /// doubles the in-memory image representation for the duration of the response.
    #[napi(js_name = "includeDataBase64")]
    pub include_data_base64: Option<bool>,
}

#[napi(js_name = "imageExtractionConfigDefault")]
pub fn image_extraction_config_default() -> JsImageExtractionConfig {
    xberg::ImageExtractionConfig::default().into()
}

/// Image metadata extracted from image files.
///
/// Includes dimensions, format, and EXIF data.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageMetadata")]
pub struct JsImageMetadata {
    /// Image width in pixels
    pub width: Option<u32>,
    /// Image height in pixels
    pub height: Option<u32>,
    /// Image format (e.g., "PNG", "JPEG", "TIFF")
    pub format: Option<String>,
    /// EXIF metadata tags
    pub exif: Option<HashMap<String, String>>,
}

/// Image element metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageMetadataType")]
pub struct JsImageMetadataType {
    /// Image source (URL, data URI, or SVG content)
    pub src: String,
    /// Alternative text from alt attribute
    pub alt: Option<String>,
    /// Title attribute
    pub title: Option<String>,
    /// Image dimensions if available.
    pub dimensions: Option<JsImageDimensions>,
    /// Image type classification
    #[napi(
        js_name = "imageType",
        ts_type = "ImageType | 'data-uri' | 'inline-svg' | 'external' | 'relative'"
    )]
    pub image_type: JsImageType,
    /// Additional attributes as key-value pairs.
    pub attributes: Vec<JsKeyValueAttribute>,
}

/// Image preprocessing configuration for OCR.
///
/// These settings control how images are preprocessed before OCR to improve
/// text recognition quality. Different preprocessing strategies work better
/// for different document types.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ImagePreprocessingConfig")]
pub struct JsImagePreprocessingConfig {
    /// Target DPI for the image (300 is standard, 600 for small text).
    #[napi(js_name = "targetDpi")]
    pub target_dpi: Option<i32>,
    /// Auto-detect and correct image rotation.
    #[napi(js_name = "autoRotate")]
    pub auto_rotate: Option<bool>,
    /// Correct skew (tilted images). Must be `false` with `none` or `off` binarization.
    pub deskew: Option<bool>,
    /// Remove noise from the image.
    pub denoise: Option<bool>,
    /// Enhance contrast for better text visibility. With `none` or `off` binarization, this
    /// applies background normalization and sharpening while preserving grayscale pixels.
    #[napi(js_name = "contrastEnhance")]
    pub contrast_enhance: Option<bool>,
    /// Binarization method: "none" (alias "off"), "otsu", "sauvola", or "adaptive".
    /// `deskew` must be `false` when this is `none` or `off`.
    #[napi(js_name = "binarizationMethod")]
    pub binarization_method: Option<String>,
    /// Invert colors (white text on black → black on white).
    #[napi(js_name = "invertColors")]
    pub invert_colors: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsImagePreprocessingConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ImagePreprocessingConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "imagePreprocessingConfigDefault")]
pub fn image_preprocessing_config_default() -> JsImagePreprocessingConfig {
    xberg::ImagePreprocessingConfig::default().into()
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImagePreprocessingMetadata")]
pub struct JsImagePreprocessingMetadata {
    /// Original image dimensions in pixels.
    #[napi(js_name = "originalDimensions")]
    pub original_dimensions: JsPixelDimensions,
    /// Original image resolution.
    #[napi(js_name = "originalDpi")]
    pub original_dpi: JsImageDpi,
    /// Target DPI from configuration
    #[napi(js_name = "targetDpi")]
    pub target_dpi: i32,
    /// Scaling factor applied to the image
    #[napi(js_name = "scaleFactor")]
    pub scale_factor: f64,
    /// Whether DPI was auto-adjusted based on content
    #[napi(js_name = "autoAdjusted")]
    pub auto_adjusted: bool,
    /// Final DPI after processing
    #[napi(js_name = "finalDpi")]
    pub final_dpi: i32,
    /// New dimensions after resizing (if resized).
    #[napi(js_name = "newDimensions")]
    pub new_dimensions: Option<JsPixelDimensions>,
    /// Resampling algorithm used ("LANCZOS3", "CATMULLROM", etc.)
    #[napi(js_name = "resampleMethod")]
    pub resample_method: String,
    /// Whether dimensions were clamped to max_image_dimension
    #[napi(js_name = "dimensionClamped")]
    pub dimension_clamped: bool,
    /// Calculated optimal DPI (if auto_adjust_dpi enabled)
    #[napi(js_name = "calculatedDpi")]
    pub calculated_dpi: Option<i32>,
    /// Whether resize was skipped (dimensions already optimal)
    #[napi(js_name = "skippedResize")]
    pub skipped_resize: bool,
    /// Error message if resize failed
    #[napi(js_name = "resizeError")]
    pub resize_error: Option<String>,
}

/// Inline element within a block.
///
/// Represents text with formatting, links, images, etc.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "InlineElement")]
pub struct JsInlineElement {
    /// Type of inline element
    #[napi(
        js_name = "elementType",
        ts_type = "InlineType | 'text' | 'strong' | 'emphasis' | 'highlight' | 'subscript' | 'superscript' | 'insert' | 'delete' | 'code' | 'link' | 'image' | 'span' | 'math' | 'raw_inline' | 'footnote_ref' | 'symbol'"
    )]
    pub element_type: JsInlineType,
    /// Text content
    pub content: String,
    /// Element attributes
    pub attributes: Option<JsAttributes>,
    /// Additional metadata (e.g., href for links, src/alt for images)
    pub metadata: Option<HashMap<String, String>>,
}

/// JATS (Journal Article Tag Suite) metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "JatsMetadata")]
pub struct JsJatsMetadata {
    /// Copyright statement from the article's `<permissions>` element.
    pub copyright: Option<String>,
    /// Open-access license URI from the article's `<license>` element.
    pub license: Option<String>,
    /// Publication history dates keyed by event type (e.g. `"received"`, `"accepted"`).
    #[napi(js_name = "historyDates")]
    pub history_dates: Option<HashMap<String, String>>,
    /// Authors and contributors with their stated roles.
    #[napi(js_name = "contributorRoles")]
    pub contributor_roles: Option<Vec<JsContributorRole>>,
}
impl<'de> serde::Deserialize<'de> for JsJatsMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::JatsMetadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A string key-value attribute.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "KeyValueAttribute")]
pub struct JsKeyValueAttribute {
    /// Attribute name.
    pub key: Option<String>,
    /// Attribute value.
    pub value: Option<String>,
}

/// Extracted keyword with metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "Keyword")]
pub struct JsKeyword {
    /// The keyword text.
    pub text: String,
    /// Relevance score (higher is better, algorithm-specific range).
    pub score: f64,
    /// Algorithm that extracted this keyword.
    #[napi(ts_type = "KeywordAlgorithm | 'yake' | 'rake'")]
    pub algorithm: JsKeywordAlgorithm,
    /// Optional positions where keyword appears in text (character offsets).
    pub positions: Option<Vec<i64>>,
}

/// Keyword extraction configuration.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "KeywordConfig")]
pub struct JsKeywordConfig {
    /// Algorithm to use for extraction.
    #[napi(ts_type = "KeywordAlgorithm | 'yake' | 'rake'")]
    pub algorithm: Option<JsKeywordAlgorithm>,
    /// Maximum number of keywords to extract (default: 10).
    #[napi(js_name = "maxKeywords")]
    pub max_keywords: Option<i64>,
    /// Minimum score threshold (0.0-1.0, default: 0.0).
    ///
    /// Keywords with scores below this threshold are filtered out.
    /// Note: Score ranges differ between algorithms.
    #[napi(js_name = "minScore")]
    pub min_score: Option<f64>,
    /// N-gram range for keyword extraction (min, max).
    ///
    /// (1, 1) = unigrams only
    /// (1, 2) = unigrams and bigrams
    /// (1, 3) = unigrams, bigrams, and trigrams (default)
    #[napi(js_name = "ngramRange")]
    pub ngram_range: Option<JsNgramRange>,
    /// Language code for stopword filtering (e.g., "en", "de", "fr").
    ///
    /// If undefined, no stopword filtering is applied.
    pub language: Option<String>,
    /// YAKE-specific tuning parameters.
    #[napi(js_name = "yakeParams")]
    #[serde(skip)]
    pub yake_params: Option<JsYakeParams>,
    /// RAKE-specific tuning parameters.
    #[napi(js_name = "rakeParams")]
    #[serde(skip)]
    pub rake_params: Option<JsRakeParams>,
}

#[napi(js_name = "keywordConfigDefault")]
pub fn keyword_config_default() -> JsKeywordConfig {
    xberg::KeywordConfig::default().into()
}

/// Structured per-language detection result: confidence, document share, and script —
/// the information the ISO-code-only `detected_languages` list cannot convey (#261).
///
/// Populated by the language-detection processor alongside `detected_languages`, with one
/// entry per language, in the same order as `detected_languages`.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LanguageConfidence")]
pub struct JsLanguageConfidence {
    /// ISO 639-3 language code, matching the corresponding entry in `detected_languages`.
    pub language: String,
    /// Confidence for this language, in `[0.0, 1.0]`.
    ///
    /// In single-language mode this is whatlang's `Info.confidence()` for the whole
    /// document. In multi-language mode this is the average whatlang confidence across
    /// the document's 200-character chunks that were classified as this language.
    pub confidence: f64,
    /// Share of the document's analyzed content classified as this language, in `[0.0, 1.0]`.
    ///
    /// In single-language mode this is always `1.0`. In multi-language mode this is the
    /// fraction of 200-character chunks classified as this language (chunks that did not
    /// meet `min_confidence` for any language are excluded from the count but still count
    /// toward the denominator).
    pub proportion: f64,
    /// Writing system whatlang detected for this language (e.g. `"Latin"`, `"Cyrillic"`).
    pub script: String,
    /// Whether this detection is considered reliable.
    ///
    /// In single-language mode this is whatlang's own `Info.is_reliable()` (confidence
    /// above whatlang's internal 0.9 threshold). In multi-language mode this is the
    /// chunk-averaged `confidence` above that same 0.9 threshold, since whatlang's
    /// `is_reliable()` only applies to a single detection.
    pub reliable: bool,
}

/// Language detection configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LanguageDetectionConfig")]
pub struct JsLanguageDetectionConfig {
    /// Enable language detection
    pub enabled: Option<bool>,
    /// Minimum confidence threshold (0.0-1.0)
    #[napi(js_name = "minConfidence")]
    pub min_confidence: Option<f64>,
    /// Detect multiple languages in the document
    #[napi(js_name = "detectMultiple")]
    pub detect_multiple: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsLanguageDetectionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LanguageDetectionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "languageDetectionConfigDefault")]
pub fn language_detection_config_default() -> JsLanguageDetectionConfig {
    xberg::LanguageDetectionConfig::default().into()
}

/// Configuration for the late-interaction (ColBERT) pipeline.
///
/// Controls which model to use, batching, and download/cache behavior for the
/// local ONNX ColBERT model.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LateInteractionConfig")]
pub struct JsLateInteractionConfig {
    /// The late-interaction model to use (defaults to the "gte-moderncolbert" preset).
    pub model: Option<JsLateInteractionModelType>,
    /// Batch size for local ONNX inference.
    ///
    /// ColBERT emits a `[seq, dim]` multi-vector embedding per document, so
    /// memory scales with batch size — keep this modest.
    #[napi(js_name = "batchSize")]
    pub batch_size: Option<i64>,
    /// Maximum token sequence length for the tokenizer (documents).
    #[napi(js_name = "maxLength")]
    pub max_length: Option<i64>,
    /// Fixed padded length for query augmentation.
    ///
    /// ColBERT queries are padded (with the mask token, kept attention-live)
    /// to exactly this many tokens rather than truncated/left as-is — this is
    /// the "query augmentation" trick from the ColBERT paper.
    #[napi(js_name = "queryMaxLength")]
    pub query_max_length: Option<i64>,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `LateInteractionModelType.Plugin`, which downloads no model.
    #[napi(js_name = "showDownloadProgress")]
    pub show_download_progress: Option<bool>,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the late-interaction ONNX model.
    pub acceleration: Option<JsAccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `LateInteractionModelType.Plugin`. `None` disables the timeout.
    #[napi(js_name = "maxEmbedDurationSecs")]
    pub max_embed_duration_secs: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsLateInteractionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LateInteractionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "lateInteractionConfigDefault")]
pub fn late_interaction_config_default() -> JsLateInteractionConfig {
    xberg::LateInteractionConfig::default().into()
}

/// A single document match returned by `max_sim_rank`, with its position in
/// the input and MaxSim score.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LateInteractionMatch")]
pub struct JsLateInteractionMatch {
    /// Position of this document in the original input slice.
    pub index: i64,
    /// MaxSim relevance score. Higher means more relevant to the query.
    pub score: f64,
}

/// Static metadata for a bundled ColBERT preset (WASM/Android-safe, no ORT).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LateInteractionPreset")]
pub struct JsLateInteractionPreset {
    /// Stable preset name referenced from config.
    pub name: String,
    /// HuggingFace repository hosting the ONNX model.
    #[napi(js_name = "modelRepo")]
    pub model_repo: String,
    /// Path to the ONNX file within the repo.
    #[napi(js_name = "modelFile")]
    pub model_file: String,
    /// Sibling files that must be downloaded alongside `model_file`.
    #[napi(js_name = "additionalFiles")]
    pub additional_files: Vec<String>,
    /// Maximum document token sequence length.
    #[napi(js_name = "maxLength")]
    pub max_length: i64,
    /// Fixed padded query length (ColBERT query augmentation).
    #[napi(js_name = "queryMaxLength")]
    pub query_max_length: i64,
    /// Per-token embedding dimensionality.
    pub dim: i64,
    /// Human-readable description.
    pub description: String,
}

/// A single layout detection result.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LayoutDetection")]
pub struct JsLayoutDetection {
    /// Detected layout class (e.g. `Table`, `Text`, `Title`).
    #[napi(
        js_name = "className",
        ts_type = "LayoutClass | 'caption' | 'chart' | 'footnote' | 'formula' | 'list_item' | 'page_footer' | 'page_header' | 'picture' | 'section_header' | 'table' | 'text' | 'title' | 'document_index' | 'code' | 'checkbox_selected' | 'checkbox_unselected' | 'form' | 'key_value_region'"
    )]
    pub class_name: JsLayoutClass,
    /// Detection confidence score in `[0.0, 1.0]`.
    pub confidence: f64,
    /// Bounding box in image pixel coordinates.
    pub bbox: JsBBox,
}

/// Layout detection configuration.
///
/// Controls layout detection behavior in the extraction pipeline.
/// When set on `ExtractionConfig`, layout detection
/// is enabled for PDF extraction.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LayoutDetectionConfig")]
pub struct JsLayoutDetectionConfig {
    /// Which pages the layout model runs on.
    ///
    /// Defaults to `LayoutStrategy.Always`, the historical behavior:
    /// every page is rendered and inferred. `LayoutStrategy.Auto`
    /// pre-screens pages with cheap signals and skips the model where it
    /// cannot help.
    #[napi(ts_type = "LayoutStrategy | 'always' | 'auto'")]
    pub strategy: Option<JsLayoutStrategy>,
    /// Confidence threshold override (undefined = use model default).
    #[napi(js_name = "confidenceThreshold")]
    pub confidence_threshold: Option<f64>,
    /// Whether to apply postprocessing heuristics (default: true).
    #[napi(js_name = "applyHeuristics")]
    pub apply_heuristics: Option<bool>,
    /// Table structure recognition model.
    ///
    /// Controls which model is used for table cell detection within layout-detected
    /// table regions. Defaults to `TableModel.Tatr`.
    #[napi(
        js_name = "tableModel",
        ts_type = "TableModel | 'tatr' | 'slanet_wired' | 'slanet_wireless' | 'slanet_plus' | 'slanet_auto' | 'disabled'"
    )]
    pub table_model: Option<JsTableModel>,
    /// Formula recognition model for layout-detected formula regions.
    ///
    /// `None` (the default) keeps the plain OCR text of the region. Setting a
    /// model converts each formula region crop to LaTeX. Requires the
    /// `formula-recognition` feature; without it the setting is ignored.
    #[napi(js_name = "formulaModel", ts_type = "FormulaModel | 'latex_ocr'")]
    pub formula_model: Option<JsFormulaModel>,
    /// How to resolve overlapping native vs layout tables.
    ///
    /// When a native table and a layout (TATR/SLANeXT) table overlap on the
    /// same region, this controls which one is kept. Defaults to
    /// `TableOverlapPreference.Content` (historical behavior: keep the table with
    /// more content). Set to `TableOverlapPreference.Native` to favor source
    /// reading order (higher text F1) over the model's cell reflow.
    #[napi(
        js_name = "tableOverlapPreference",
        ts_type = "TableOverlapPreference | 'content' | 'native' | 'layout'"
    )]
    pub table_overlap_preference: Option<JsTableOverlapPreference>,
    /// Hardware acceleration for ONNX models (layout detection + table structure).
    ///
    /// When set, controls which execution provider (CPU, CUDA, CoreML, TensorRT)
    /// is used for inference. Defaults to `None` (auto-select per platform).
    pub acceleration: Option<JsAccelerationConfig>,
    /// Route regions classified as charts to the chart-understanding OCR task.
    ///
    /// When `true`, layout regions detected as charts are sent to the VLM
    /// chart task (data-series/axis recovery) instead of being treated as
    /// generic image regions. Defaults to `false` — chart understanding is
    /// opt-in and has no effect on standard text/table extraction scores.
    #[napi(js_name = "enableChartUnderstanding")]
    pub enable_chart_understanding: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsLayoutDetectionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LayoutDetectionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "layoutDetectionConfigDefault")]
pub fn layout_detection_config_default() -> JsLayoutDetectionConfig {
    xberg::LayoutDetectionConfig::default().into()
}

/// A detected layout region on a page.
///
/// When layout detection is enabled, each page may have layout regions
/// identifying different content types (text, pictures, tables, etc.)
/// with confidence scores and spatial positions.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LayoutRegion")]
pub struct JsLayoutRegion {
    /// Layout class name (e.g. "picture", "table", "text", "section_header").
    #[napi(js_name = "className")]
    pub class_name: Option<String>,
    /// Confidence score from the layout detection model (0.0 to 1.0).
    pub confidence: Option<f64>,
    /// Bounding box in document coordinate space.
    #[napi(js_name = "boundingBox")]
    pub bounding_box: Option<JsBoundingBox>,
    /// Fraction of the page area covered by this region (0.0 to 1.0).
    #[napi(js_name = "areaFraction")]
    pub area_fraction: Option<f64>,
}

/// Link element metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LinkMetadata")]
pub struct JsLinkMetadata {
    /// The href URL value
    pub href: String,
    /// Link text content (normalized)
    pub text: String,
    /// Optional title attribute
    pub title: Option<String>,
    /// Link type classification
    #[napi(
        js_name = "linkType",
        ts_type = "LinkType | 'anchor' | 'internal' | 'external' | 'email' | 'phone' | 'other'"
    )]
    pub link_type: JsLinkType,
    /// Rel attribute values
    pub rel: Vec<String>,
    /// Additional attributes as key-value pairs.
    pub attributes: Vec<JsKeyValueAttribute>,
}

/// Budget enforcement configuration.
///
/// Mirrors liter-llm's `LlmBudgetConfig`. Only takes effect when liter-llm's `tower`
/// feature is compiled in; otherwise the value round-trips through configuration
/// but is not enforced at request time.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LlmBudgetConfig")]
pub struct JsLlmBudgetConfig {
    /// Global spend limit in USD.
    #[napi(js_name = "globalLimit")]
    pub global_limit: Option<f64>,
    /// Per-model spend limits in USD, keyed by model name.
    #[napi(js_name = "modelLimits")]
    pub model_limits: Option<HashMap<String, f64>>,
    /// Enforcement mode: `"hard"` (reject over-budget requests) or `"soft"` (log only).
    pub enforcement: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsLlmBudgetConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LlmBudgetConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Response cache configuration.
///
/// Mirrors liter-llm's `LlmCacheConfig`. Only takes effect when liter-llm's `tower`
/// feature is compiled in; otherwise the value round-trips through configuration
/// but is not consulted at request time.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LlmCacheConfig")]
pub struct JsLlmCacheConfig {
    /// Maximum number of cached entries.
    #[napi(js_name = "maxEntries")]
    pub max_entries: Option<i64>,
    /// Cache entry time-to-live, in seconds.
    #[napi(js_name = "ttlSeconds")]
    pub ttl_seconds: Option<i64>,
    /// Cache backend name (e.g. `"memory"`, or an `opendal` scheme).
    pub backend: Option<String>,
    /// Backend-specific configuration key/value pairs.
    #[napi(js_name = "backendConfig")]
    pub backend_config: Option<HashMap<String, String>>,
}
impl<'de> serde::Deserialize<'de> for JsLlmCacheConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LlmCacheConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for an LLM provider/model via liter-llm.
///
/// Each feature (VLM OCR, VLM embeddings, structured extraction) carries
/// its own `LlmConfig`, allowing different providers per feature.
///
/// # Example
///
/// ```toml
/// [structured_extraction.llm]
/// model = "openai/gpt-4o"
/// api_key = "sk-..."  # or use XBERG_LLM_API_KEY env var
/// ```
///
/// `Debug` is implemented by hand so `api_key`, header values, and the AWS
/// credentials in `BedrockConfig` are never printed.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LlmConfig")]
pub struct JsLlmConfig {
    /// Provider/model string using liter-llm routing format.
    ///
    /// Examples: `"openai/gpt-4o"`, `"anthropic/claude-sonnet-4-20250514"`,
    /// `"groq/llama-3.1-70b-versatile"`.
    pub model: Option<String>,
    /// API key for the provider. When `None`, liter-llm falls back to
    /// the provider's standard environment variable (e.g., `OPENAI_API_KEY`).
    #[napi(js_name = "apiKey")]
    pub api_key: Option<String>,
    /// Custom base URL override for the provider endpoint.
    #[napi(js_name = "baseUrl")]
    pub base_url: Option<String>,
    /// Request timeout in seconds. When `None`, liter-llm's built-in 60s default
    /// applies, except the VLM OCR path which uses a 300s default (a single page
    /// image transcription routinely exceeds 60s). Set explicitly to override.
    #[napi(js_name = "timeoutSecs")]
    pub timeout_secs: Option<i64>,
    /// Maximum retry attempts (default: 3).
    #[napi(js_name = "maxRetries")]
    pub max_retries: Option<u32>,
    /// Sampling temperature for generation tasks.
    pub temperature: Option<f64>,
    /// Maximum tokens to generate.
    #[napi(js_name = "maxTokens")]
    pub max_tokens: Option<i64>,
    /// Nucleus sampling parameter for generation tasks, applied to individual
    /// requests built from this config. Restricts sampling to the smallest set of
    /// tokens whose cumulative probability mass is at least this value; lower is
    /// more focused. Validated to `[0.0, 1.0]` by `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.top_p`. A request-time
    /// parameter like `temperature`/`max_tokens` above, not a client-level
    /// setting.
    #[napi(js_name = "topP")]
    pub top_p: Option<f64>,
    /// Stop sequence(s) that halt token generation, applied to individual requests
    /// built from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.stop`
    /// (`types.common.StopSequence`), which liter-llm represents as either a
    /// single string or a list of strings via an untagged enum. Always expressed
    /// here as a list — even one stop sequence is `["..."]` — so the field has a
    /// single, FFI-friendly shape across every language binding instead of a
    /// single-or-list union type. Converted to liter-llm's
    /// `StopSequence.Multiple` at each request-building call site; see
    /// `llm.client.to_stop_sequence`.
    pub stop: Option<Vec<String>>,
    /// Random seed for reproducible outputs, applied to individual requests built
    /// from this config. Provider support varies — some silently ignore it.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.seed`.
    pub seed: Option<i64>,
    /// Presence penalty for generation tasks, applied to individual requests
    /// built from this config. Positive values discourage the model from
    /// repeating topics already present in the conversation. Validated to
    /// `[-2.0, 2.0]` by `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.presence_penalty`.
    #[napi(js_name = "presencePenalty")]
    pub presence_penalty: Option<f64>,
    /// Frequency penalty for generation tasks, applied to individual requests
    /// built from this config. Positive values discourage the model from
    /// repeating the same tokens verbatim. Validated to `[-2.0, 2.0]` by
    /// `LlmConfig.validate`.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.frequency_penalty`.
    #[napi(js_name = "frequencyPenalty")]
    pub frequency_penalty: Option<f64>,
    /// Reasoning effort level for extended-thinking models, applied to individual
    /// requests built from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.reasoning_effort`
    /// (`types.chat.ReasoningEffort`). A request-time parameter like `temperature`/
    /// `max_tokens` above, not a client-level setting — `into_client_builder` does not
    /// map it. Accepted as a plain string — one of `"low"`, `"medium"`, `"high"`,
    /// `"minimal"`, `"max"` (case-insensitive; liter-llm's own
    /// `#[serde(rename_all = "lowercase")]` spelling) — rather than importing
    /// liter-llm's enum, because this module compiles even when the `liter-llm`
    /// feature is disabled. See `llm.client.parse_reasoning_effort` for the
    /// conversion into `liter_llm.ReasoningEffort`.
    #[napi(js_name = "reasoningEffort")]
    pub reasoning_effort: Option<String>,
    /// Provider-specific extra parameters merged into the request body (guardrails,
    /// safety settings, grounding config, etc.), applied to individual requests built
    /// from this config.
    ///
    /// Mirrors liter-llm's `ChatCompletionRequest.extra_body`. A request-time
    /// parameter like `temperature`/`max_tokens` above, not a client-level setting.
    #[napi(js_name = "extraBody")]
    pub extra_body: Option<serde_json::Value>,
    /// Whether liter-llm should load provider credentials from environment variables.
    ///
    /// Mirrors liter-llm's `ClientConfigBuilder.load_env`. When `None`, liter-llm's
    /// own default behavior applies.
    #[napi(js_name = "loadEnv")]
    pub load_env: Option<bool>,
    /// Extra HTTP headers sent with every request to the provider.
    ///
    /// Mirrors liter-llm's `ClientConfigBuilder.header`, for gateways or providers
    /// that require custom auth/routing headers.
    pub headers: Option<HashMap<String, String>>,
    /// Custom provider configurations, in addition to liter-llm's built-in providers.
    ///
    /// Mirrors liter-llm's `LlmConfig.providers`, for OpenAI-compatible gateways
    /// and self-hosted model servers that are not in the built-in provider catalog.
    pub providers: Option<Vec<JsLlmProviderConfig>>,
    /// Response cache configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.cache`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    ///
    /// Boxed for the same reason as `bedrock` (a4579589ac): `LlmConfig` is the payload
    /// of `EmbeddingModelType.Llm` and `RerankerModelType.Llm`, whose other variants
    /// are tens of bytes. Inlining this and the two sub-configs below pushed that
    /// variant to 480 bytes and tripped `clippy.large_enum_variant` on the
    /// `--features full` leg.
    pub cache: Option<JsLlmCacheConfig>,
    /// Budget enforcement configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.budget`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    pub budget: Option<JsLlmBudgetConfig>,
    /// Per-model rate limiting configuration.
    ///
    /// Mirrors liter-llm's `LlmConfig.rate_limit`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    #[napi(js_name = "rateLimit")]
    pub rate_limit: Option<JsLlmRateLimitConfig>,
    /// Enable per-request cost tracking.
    ///
    /// Mirrors liter-llm's `LlmConfig.cost_tracking`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[napi(js_name = "costTracking")]
    pub cost_tracking: Option<bool>,
    /// Enable OpenTelemetry-compatible tracing spans.
    ///
    /// Mirrors liter-llm's `LlmConfig.tracing`. Only takes effect when liter-llm's
    /// `tower` feature is compiled in; otherwise the value is accepted but unused.
    pub tracing: Option<bool>,
    /// Cooldown duration after transient errors, in seconds.
    ///
    /// Mirrors liter-llm's `LlmConfig.cooldown_secs`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[napi(js_name = "cooldownSecs")]
    pub cooldown_secs: Option<i64>,
    /// Background health check interval, in seconds.
    ///
    /// Mirrors liter-llm's `LlmConfig.health_check_secs`. Only takes effect when
    /// liter-llm's `tower` feature is compiled in; otherwise the value is accepted
    /// but unused.
    #[napi(js_name = "healthCheckSecs")]
    pub health_check_secs: Option<i64>,
    /// AWS Bedrock settings (region, cross-region routing, explicit credentials).
    ///
    /// Only consulted for `bedrock/`-prefixed models. When `None` — or when an
    /// individual field inside it is `None` — liter-llm falls back to the standard
    /// AWS environment variables and the default credential chain.
    pub bedrock: Option<JsBedrockConfig>,
    /// Managed OAuth2/STS credential provider for auth modes liter-llm cannot express via a
    /// static `api_key` — Azure AD, Vertex AI OAuth2, Vertex AI Application Default
    /// Credentials, and AWS STS Web Identity (EKS IRSA) for Bedrock.
    ///
    /// Mirrors liter-llm's `client.ClientConfigBuilder.credential_provider`, which takes an
    /// `Arc<dyn liter_llm.auth.CredentialProvider>` trait object — that cannot appear in a
    /// serde DTO. Every `CredentialProviderConfig` variant is plain data instead, so it
    /// round-trips through TOML/JSON/YAML and every language binding like the rest of
    /// `LlmConfig`.
    ///
    /// Managed credential providers are unavailable on `wasm32`, where liter-llm uses
    /// browser HTTP rather than its native authentication modules. `LlmConfig.validate`
    /// rejects a configured provider on that target instead of silently ignoring it.
    ///
    /// GitHub Copilot's device-flow provider has no variant here: it takes no configuration at
    /// all (`liter_llm.auth.github_copilot.GithubCopilotCredentialProvider.new` accepts only
    /// an HTTP client) and drives an interactive terminal prompt, so it cannot be expressed as
    /// data. A Rust embedder who needs it — or any other fully custom `CredentialProvider` — can
    /// call `xberg.llm.client.create_client_with_credential_provider` directly with a
    /// `liter-llm` dependency of their own.
    #[napi(js_name = "credentialProvider")]
    pub credential_provider: Option<JsCredentialProviderConfig>,
    /// Maximum number of simultaneously in-flight requests to the LLM provider this
    /// config resolves to.
    ///
    /// This is a real, global bound on *provider* concurrency, not a per-extraction
    /// allowance: `xberg.llm.client.create_client` shares one process-wide client
    /// instance per distinct resolved config, so every concurrent extraction that
    /// resolves to the same config shares the one in-flight-request limit this value
    /// configures, instead of each minting its own (GH#1465). `None` means unlimited.
    ///
    /// PDF and image OCR batch sizing are **not** derived from this field, even when the
    /// configured OCR backend or `vlm_fallback` policy can reach a VLM — those call sites
    /// mix CPU-bound raster/OCR work with, at most, occasional remote requests, so they
    /// size their batches from the general thread budget
    /// (`max_threads`) unconditionally (GH#1465). Captioning is
    /// the one feature that *additionally* uses this value to bound its own
    /// per-extraction async request fan-out (issuing only VLM requests, with no CPU
    /// batching to protect), on top of the global provider-side limit described above;
    /// that per-extraction bound clamps a value below 1 up to 1. The global provider-side
    /// limit does not clamp: `Some(0)` reaches liter-llm as-is, and liter-llm rejects it
    /// when building the client (zero permitted in-flight requests is never useful), so it
    /// surfaces as a `create_client` error rather than a silent clamp to 1.
    ///
    /// This field is intentionally last to preserve positional constructor
    /// compatibility in generated language bindings.
    #[napi(js_name = "maxConcurrency")]
    pub max_concurrency: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsLlmConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LlmConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A custom provider configuration entry, in addition to liter-llm's built-in providers.
///
/// Mirrors liter-llm's `LlmProviderConfig`.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LlmProviderConfig")]
pub struct JsLlmProviderConfig {
    /// Provider name, used to key model prefix matching.
    pub name: Option<String>,
    /// Base URL for the provider's OpenAI-compatible API.
    #[napi(js_name = "baseUrl")]
    pub base_url: Option<String>,
    /// Header name used to carry the API key (defaults to `Authorization` when unset).
    #[napi(js_name = "authHeader")]
    pub auth_header: Option<String>,
    /// Model name prefixes routed to this provider (e.g. `["my-provider/"]`).
    #[napi(js_name = "modelPrefixes")]
    pub model_prefixes: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsLlmProviderConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LlmProviderConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Per-model rate limiting configuration.
///
/// Mirrors liter-llm's `LlmRateLimitConfig`. Only takes effect when liter-llm's
/// `tower` feature is compiled in; otherwise the value round-trips through
/// configuration but is not enforced at request time.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "LlmRateLimitConfig")]
pub struct JsLlmRateLimitConfig {
    /// Requests per minute limit.
    pub rpm: Option<u32>,
    /// Tokens per minute limit.
    pub tpm: Option<i64>,
    /// Rate limit window, in seconds.
    #[napi(js_name = "windowSeconds")]
    pub window_seconds: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsLlmRateLimitConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::LlmRateLimitConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Token usage and cost data for a single LLM call made during extraction.
///
/// Populated when VLM OCR, structured extraction, or LLM-based embeddings
/// are used. Multiple entries may be present when multiple LLM calls occur
/// within one extraction (e.g. VLM OCR + structured extraction).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LlmUsage")]
pub struct JsLlmUsage {
    /// The LLM model identifier (e.g. "openai/gpt-4o", "anthropic/claude-sonnet-4-20250514").
    pub model: Option<String>,
    /// The pipeline stage that triggered this LLM call
    /// (e.g. "vlm_ocr", "structured_extraction", "embeddings").
    pub source: Option<String>,
    /// Number of input/prompt tokens consumed.
    #[napi(js_name = "inputTokens")]
    pub input_tokens: Option<i64>,
    /// Number of output/completion tokens generated.
    #[napi(js_name = "outputTokens")]
    pub output_tokens: Option<i64>,
    /// Total tokens (input + output).
    #[napi(js_name = "totalTokens")]
    pub total_tokens: Option<i64>,
    /// Estimated cost in USD based on the provider's published pricing.
    #[napi(js_name = "estimatedCost")]
    pub estimated_cost: Option<f64>,
    /// Why the model stopped generating (e.g. "stop", "length", "content_filter").
    #[napi(js_name = "finishReason")]
    pub finish_reason: Option<String>,
}

/// The result of a map operation, containing discovered URLs.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MapResult")]
pub struct JsMapResult {
    /// The list of discovered URLs.
    pub urls: Option<Vec<JsSitemapUrl>>,
}

/// A fenced code block extracted from Markdown.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MarkdownCodeBlock")]
pub struct JsMarkdownCodeBlock {
    /// Declared language identifier, or an empty string when absent.
    pub language: Option<String>,
    /// Code block content.
    pub code: Option<String>,
}

/// A link extracted from Markdown.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MarkdownLink")]
pub struct JsMarkdownLink {
    /// Visible link text.
    pub text: Option<String>,
    /// Link destination.
    pub url: Option<String>,
}

/// Compiled meta-schema validator over `preset.schema.json`.
#[derive(Clone)]
#[napi(js_name = "MetaSchema")]
pub struct JsMetaSchema {
    inner: Arc<xberg::MetaSchema>,
}

#[napi]
impl JsMetaSchema {
    /// Validate `raw` against the meta-schema and deserialize into a `Preset`,
    /// stamping the fingerprint over the canonical file bytes.
    #[allow(clippy::missing_errors_doc)]
    #[napi(js_name = "parsePreset")]
    pub fn parse_preset(&self, path: String, raw: napi::bindgen_prelude::Buffer) -> Result<JsPreset> {
        let result = self
            .inner
            .parse_preset(&path, &raw)
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(result.into())
    }

    /// Compile the given JSON text as a Draft 2020-12 meta-schema.
    #[allow(clippy::missing_errors_doc)]
    #[napi]
    pub fn compile(meta_schema_json: String) -> Result<JsMetaSchema> {
        xberg::MetaSchema::compile(&meta_schema_json)
            .map(|val| Self { inner: Arc::new(val) })
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
    }
}

/// Extraction result metadata.
///
/// Contains common fields applicable to all formats, format-specific metadata
/// via a discriminated union, and additional custom fields from postprocessors.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Metadata")]
pub struct JsMetadata {
    /// Document title
    pub title: Option<String>,
    /// Document subject or description
    pub subject: Option<String>,
    /// Primary author(s) - always Vec for consistency
    pub authors: Option<Vec<String>>,
    /// Keywords/tags - always Vec for consistency
    pub keywords: Option<Vec<String>>,
    /// Primary language (ISO 639 code)
    pub language: Option<String>,
    /// Creation timestamp (ISO 8601 format)
    #[napi(js_name = "createdAt")]
    pub created_at: Option<String>,
    /// Last modification timestamp (ISO 8601 format)
    #[napi(js_name = "modifiedAt")]
    pub modified_at: Option<String>,
    /// User who created the document
    #[napi(js_name = "createdBy")]
    pub created_by: Option<String>,
    /// User who last modified the document
    #[napi(js_name = "modifiedBy")]
    pub modified_by: Option<String>,
    /// Page/slide/sheet structure with boundaries
    pub pages: Option<JsPageStructure>,
    /// Format-specific metadata (discriminated union)
    ///
    /// Contains detailed metadata specific to the document format.
    /// Serialized as a nested `"format"` object with a `format_type` discriminator field.
    pub format: Option<JsFormatMetadata>,
    /// Image preprocessing metadata (when OCR preprocessing was applied)
    #[napi(js_name = "imagePreprocessing")]
    pub image_preprocessing: Option<JsImagePreprocessingMetadata>,
    /// JSON schema (for structured data extraction)
    #[napi(js_name = "jsonSchema")]
    pub json_schema: Option<serde_json::Value>,
    /// Error metadata (for batch operations)
    pub error: Option<JsErrorMetadata>,
    /// Extraction duration in milliseconds (for benchmarking).
    ///
    /// This field is populated by batch extraction to provide per-file timing
    /// information. It's `None` for single-file extraction (which uses external timing).
    #[napi(js_name = "extractionDurationMs")]
    pub extraction_duration_ms: Option<i64>,
    /// Document category (from frontmatter or classification).
    pub category: Option<String>,
    /// Document tags (from frontmatter).
    pub tags: Option<Vec<String>>,
    /// Document version string (from frontmatter).
    #[napi(js_name = "documentVersion")]
    pub document_version: Option<String>,
    /// Abstract or summary text (from frontmatter).
    #[napi(js_name = "abstractText")]
    pub abstract_text: Option<String>,
    /// Output format identifier (e.g., "markdown", "html", "text").
    ///
    /// Set by the output format pipeline stage when format conversion is applied.
    /// Previously stored in `metadata.additional["output_format"]`.
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<String>,
    /// Whether OCR was used during extraction.
    ///
    /// Set to `true` whenever the extraction pipeline ran an OCR backend
    /// (Tesseract, PaddleOCR, VLM, etc.) and used that output as the primary
    /// or fallback text. `false` means native text extraction was used exclusively.
    #[napi(js_name = "ocrUsed")]
    pub ocr_used: Option<bool>,
    /// Additional custom fields from postprocessors.
    ///
    /// Serialized as a nested `"additional"` object (not flattened at root level).
    /// Uses `Cow<'static, str>` keys so static string keys avoid allocation.
    pub additional: Option<HashMap<String, serde_json::Value>>,
}
impl<'de> serde::Deserialize<'de> for JsMetadata {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Metadata as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Combined paths to all models needed for OCR (backward compatibility).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ModelPaths")]
pub struct JsModelPaths {
    /// Exact path to the detection ONNX model in the Hugging Face snapshot.
    #[napi(js_name = "detModel")]
    pub det_model: String,
    /// Exact path to the classification ONNX model in the Hugging Face snapshot.
    #[napi(js_name = "clsModel")]
    pub cls_model: String,
    /// Exact path to the recognition ONNX model in the Hugging Face snapshot.
    #[napi(js_name = "recModel")]
    pub rec_model: String,
    /// Path to the character dictionary file.
    #[napi(js_name = "dictFile")]
    pub dict_file: String,
}

/// A ColBERT multi-vector embedding: one row per attention-live token.
///
/// `data` is a flat, row-major buffer of length `num_tokens * dim` — row `i`
/// (the embedding for token `i`) occupies `data[i*dim .. (i+1)*dim]`. Flat
/// storage keeps the type FFI-friendly across binding boundaries; use
/// `MultiVectorEmbedding.rows` internally to iterate per-token slices.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MultiVectorEmbedding")]
pub struct JsMultiVectorEmbedding {
    /// Number of attention-live token rows (padding rows are dropped, not
    /// zeroed — see `engine.normalize_tokens`).
    #[napi(js_name = "numTokens")]
    pub num_tokens: u32,
    /// Dimensionality of each per-token vector.
    pub dim: u32,
    /// Flat row-major buffer, length `num_tokens * dim`.
    pub data: Vec<f64>,
}

/// Input signals for multi-document boundary detection.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MultidocInput")]
pub struct JsMultidocInput {
    /// Total number of pages in the PDF.
    #[napi(js_name = "pageCount")]
    pub page_count: u32,
    /// Per-page signals extracted from the PDF.
    pub pages: Vec<JsPageSignals>,
}

/// Thresholds for multi-document boundary detection.
///
/// All fields are public; callers override any subset via struct-update syntax.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "MultidocThresholds")]
pub struct JsMultidocThresholds {
    /// Text density difference threshold for `DensityShift` detection.
    /// Default: 0.3.
    #[napi(js_name = "densityShiftThreshold")]
    pub density_shift_threshold: Option<f64>,
    /// Minimum bigram-overlap ratio below which a density shift is promoted to
    /// a `DensityShift` boundary.  Default: 0.1 (10 % overlap).
    #[napi(js_name = "bigramOverlapMin")]
    pub bigram_overlap_min: Option<f64>,
}

#[napi(js_name = "multidocThresholdsDefault")]
pub fn multidoc_thresholds_default() -> JsMultidocThresholds {
    xberg::MultidocThresholds::default().into()
}

/// Configuration for the NER post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "NerConfig")]
pub struct JsNerConfig {
    /// Backend that runs the entity detection.
    #[napi(ts_type = "NerBackendKind | 'onnx' | 'llm'")]
    pub backend: Option<JsNerBackendKind>,
    /// Entity categories to detect. Defaults to a sensible PERSON/ORG/LOCATION/EMAIL set
    /// when empty.
    pub categories: Option<Vec<JsEntityCategory>>,
    /// Override the default model — only used by `NerBackendKind.Onnx`.
    /// `None` lets the backend pick its pinned default xberg GLiNER model alias.
    pub model: Option<String>,
    /// Optional LLM configuration — only used by `NerBackendKind.Llm`. Token usage
    /// for LLM backends is recorded in `ExtractedDocument.llm_usage`.
    pub llm: Option<JsLlmConfig>,
    /// Arbitrary user-supplied entity labels for zero-shot detection.
    ///
    /// `xberg-gliner` natively supports zero-shot inference over caller-supplied
    /// labels. The LLM backend also honours these
    /// labels by including them in the structured-output schema. Custom labels
    /// surface as `EntityCategory.Custom` in the resulting `Entity` stream.
    ///
    /// Use this when you need domain-specific entity types (e.g. `"Treatment"`,
    /// `"Product"`, `"Vessel"`) without forking GLiNER's taxonomy.
    #[napi(js_name = "customLabels")]
    pub custom_labels: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsNerConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::NerConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Inclusive word-count range used to form keyword candidates.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "NgramRange")]
pub struct JsNgramRange {
    /// Minimum number of words in a candidate.
    pub min: Option<i64>,
    /// Maximum number of words in a candidate.
    pub max: Option<i64>,
}

#[napi(js_name = "ngramRangeDefault")]
pub fn ngram_range_default() -> JsNgramRange {
    xberg::NgramRange::default().into()
}

/// Confidence scores for an OCR element.
///
/// Separates detection confidence (how confident that text exists at this location)
/// from recognition confidence (how confident about the actual text content).
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrConfidence")]
pub struct JsOcrConfidence {
    /// Detection confidence: how confident the OCR engine is that text exists here.
    ///
    /// PaddleOCR provides this as `box_score`, Tesseract doesn't have a direct equivalent.
    /// Range: 0.0 to 1.0 (or undefined if not available).
    pub detection: Option<f64>,
    /// Recognition confidence: how confident about the text content.
    ///
    /// Range: 0.0 to 1.0.
    pub recognition: Option<f64>,
}
impl<'de> serde::Deserialize<'de> for JsOcrConfidence {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrConfidence as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// OCR configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrConfig")]
#[serde_with::serde_as]
pub struct JsOcrConfig {
    /// Whether OCR is enabled.
    ///
    /// Setting `enabled: false` is a shorthand for `disable_ocr: true` on the parent
    /// `ExtractionConfig`. Images return
    /// metadata only; PDFs use native text extraction without OCR fallback.
    ///
    /// Defaults to `true`. When `false`, all other OCR settings are ignored.
    pub enabled: Option<bool>,
    /// OCR backend: tesseract, paddleocr, paddle-ocr, sceptre, or vlm.
    /// Sceptre uses ONNX Runtime on desktop/server and tract on supported mobile builds.
    /// Browser WebAssembly uses the separate byte-fed Sceptre worker API.
    pub backend: Option<String>,
    /// Language code(s) for OCR recognition. Defaults to `["eng"]`. For Tesseract,
    /// languages are joined with "+".
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs (Python, Node, PHP, WASM, etc.): `["eng", "deu"]`. When
    /// deserializing from a config file, JSON body, or the REST/MCP API, a
    /// single string is also accepted, either as one code ("eng") or
    /// "+"-joined ("eng+deu").
    pub language: Option<Vec<String>>,
    /// Tesseract-specific configuration (optional)
    #[napi(js_name = "tesseractConfig")]
    pub tesseract_config: Option<JsTesseractConfig>,
    /// Output format for OCR results (optional, for format conversion)
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<JsOutputFormat>,
    /// PaddleOCR-specific configuration (optional, JSON passthrough).
    ///
    /// Deserialized into a `PaddleOcrConfig`, so any of its fields can be
    /// overridden here — most notably `model_version` (`"pp-ocrv6"` default / `"pp-ocrv5"`) and
    /// `model_tier`. In TOML:
    ///
    /// ```toml
    /// [ocr.paddle_ocr_config]
    /// model_version = "pp-ocrv5"
    /// model_tier = "server"
    /// ```
    ///
    /// The `XBERG_OCR_MODEL_VERSION` / `XBERG_OCR_MODEL_TIER` environment variables set the same two
    /// keys for env-configured servers (issue #1279).
    #[napi(js_name = "paddleOcrConfig")]
    pub paddle_ocr_config: Option<serde_json::Value>,
    /// Arbitrary per-call options passed through to the backend unchanged.
    ///
    /// Custom OCR backends and built-in backends that support runtime tuning
    /// can read this value and deserialize the keys they care about. Keys
    /// unknown to the backend are silently ignored.
    ///
    /// This is the recommended extension point for per-call parameters that
    /// are not covered by the typed fields above (e.g. mode switching,
    /// preprocessing flags, inference batch size).
    ///
    /// **Scope:** when `pipeline` is `None`, this value is propagated to the
    /// primary stage of the auto-constructed pipeline. When `pipeline` is
    /// explicitly set, this field has **no effect** — the caller must set
    /// `OcrPipelineStage.backend_options` directly on the relevant stage(s)
    /// instead.
    ///
    /// Example:
    /// ```json
    /// { "mode": "fast", "enable_layout": true, "timeout_ms": 5000 }
    /// ```
    #[napi(js_name = "backendOptions")]
    pub backend_options: Option<serde_json::Value>,
    /// OCR element extraction configuration
    #[napi(js_name = "elementConfig")]
    pub element_config: Option<JsOcrElementConfig>,
    /// Quality thresholds for the native-text-to-OCR fallback decision.
    /// When undefined, uses compiled defaults (matching previous hardcoded behavior).
    #[napi(js_name = "qualityThresholds")]
    pub quality_thresholds: Option<JsOcrQualityThresholds>,
    /// Multi-backend OCR pipeline configuration. When set, enables weighted
    /// fallback across multiple OCR backends based on output quality.
    /// When undefined, uses the single `backend` field (same as today).
    pub pipeline: Option<JsOcrPipelineConfig>,
    /// Enable automatic page rotation based on orientation detection.
    ///
    /// When enabled, page orientation (0/90/180/270 degrees) is detected with an
    /// ONNX PP-LCNet document-orientation classifier — NOT Tesseract's own
    /// `DetectOrientationScript()`/OSD, which this crate does not call. If the
    /// page is rotated with high confidence, the image is corrected before
    /// recognition. Applies to every OCR backend; the tesseract-only
    /// `TesseractConfig.preprocessing` field of the same name is OR'd in on
    /// top of this one and affects tesseract alone.
    ///
    /// Independent of a page's PDF `/Rotate` entry, which is handled separately
    /// on every OCR route regardless of this setting.
    #[napi(js_name = "autoRotate")]
    pub auto_rotate: Option<bool>,
    /// Ergonomic VLM fallback policy.
    ///
    /// When set to anything other than `VlmFallbackPolicy.Disabled` and
    /// `OcrConfig.pipeline` is `None`, a multi-stage pipeline is synthesised
    /// automatically:
    ///
    /// - `VlmFallbackPolicy.OnLowQuality` → `[classical_stage, vlm_stage]` with the
    ///   `quality_threshold` mapped onto `OcrQualityThresholds.pipeline_min_quality`.
    /// - `VlmFallbackPolicy.Always` → `[vlm_stage]` only.
    ///
    /// Requires `OcrConfig.vlm_config` to be `Some` when not `Disabled`.
    /// When `OcrConfig.pipeline` is explicitly set, this field is ignored.
    #[napi(js_name = "vlmFallback")]
    pub vlm_fallback: Option<JsVlmFallbackPolicy>,
    /// VLM (Vision Language Model) OCR configuration.
    ///
    /// Required when `backend` is `"vlm"` or when `vlm_fallback` is not
    /// `VlmFallbackPolicy.Disabled`. Uses liter-llm to send page images to a
    /// vision model for text extraction.
    #[napi(js_name = "vlmConfig")]
    pub vlm_config: Option<JsLlmConfig>,
    /// Custom Jinja2 prompt template for VLM OCR.
    ///
    /// When `None`, uses the default template. Available variables:
    /// - `{{ language }}` — The document language code (e.g., "eng", "deu").
    #[napi(js_name = "vlmPrompt")]
    pub vlm_prompt: Option<String>,
    /// Hardware acceleration for ONNX Runtime models (e.g. PaddleOCR, layout detection).
    ///
    /// Not user-configurable via config files — injected at runtime from
    /// `ExtractionConfig.acceleration` before each `process_image` call.
    pub acceleration: Option<JsAccelerationConfig>,
    /// Security limits applied when decoding raw image bytes for OCR (GH#1554).
    ///
    /// Not user-configurable via config files — injected at runtime from
    /// `ExtractionConfig.security_limits` before each `process_image` call, the same
    /// pattern `Self.acceleration` uses. `ExtractionConfig.security_limits` is the
    /// single source of truth: this field only ever holds a copy the caller placed here
    /// immediately before dispatch, so the two cannot drift. A backend consulting a
    /// `backend_options` override for this call may still let that override win, but in
    /// the absence of one this field is what backends should fall back to instead of
    /// `SecurityLimits.default()`. `None` means "use `SecurityLimits.default()`", never
    /// "disable the check".
    #[napi(js_name = "securityLimits")]
    pub security_limits: Option<JsSecurityLimits>,
    /// Caller-supplied Tesseract `traineddata` bytes per language code.
    ///
    /// Primary use case is the WASM build, which has no filesystem and cannot
    /// download tessdata at runtime. Native builds typically rely on
    /// `TessdataManager` and ignore this field. When present, the WASM
    /// Tesseract backend prefers these bytes over its compile-time-bundled
    /// English data.
    ///
    /// Skipped by serde to keep config files small — supply via the typed API
    /// at runtime.
    #[napi(
        js_name = "tessdataBytes",
        ts_type = "Record<string, Uint8Array | Buffer | Array<number>>"
    )]
    #[serde_as(as = "HashMap<_, serde_with::Bytes>")]
    pub tessdata_bytes: Option<HashMap<String, JsBytes>>,
    /// Runtime override for tessdata directory path.
    ///
    /// When set, uses this path as the highest-priority tessdata location,
    /// bypassing environment variables and cache directories. Useful for
    /// embedding pre-installed tessdata in applications. When `None`, uses
    /// the standard resolution chain: TESSDATA_PREFIX env, cache dir, system paths.
    #[napi(js_name = "tessdataPath")]
    pub tessdata_path: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsOcrConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "ocrConfigDefault")]
pub fn ocr_config_default() -> JsOcrConfig {
    xberg::OcrConfig::default().into()
}

/// A unified OCR element representing detected text with full metadata.
///
/// This is the primary type for structured OCR output, preserving all information
/// from both Tesseract and PaddleOCR backends.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrElement")]
pub struct JsOcrElement {
    /// The recognized text content.
    pub text: Option<String>,
    /// Bounding geometry (rectangle or quadrilateral).
    pub geometry: Option<JsOcrBoundingGeometry>,
    /// Confidence scores for detection and recognition.
    pub confidence: Option<JsOcrConfidence>,
    /// Hierarchical level (word, line, block, page).
    #[napi(ts_type = "OcrElementLevel | 'word' | 'line' | 'block' | 'page'")]
    pub level: Option<JsOcrElementLevel>,
    /// Rotation information (if detected).
    pub rotation: Option<JsOcrRotation>,
    /// Page number (1-indexed).
    #[napi(js_name = "pageNumber")]
    pub page_number: Option<u32>,
    /// Parent element ID for hierarchical relationships.
    ///
    /// When hierarchy output is enabled, this resolves to another emitted element's
    /// `backend_metadata["element_id"]` value.
    #[napi(js_name = "parentId")]
    pub parent_id: Option<String>,
    /// Backend-specific metadata that doesn't fit the unified schema.
    #[napi(js_name = "backendMetadata")]
    pub backend_metadata: Option<HashMap<String, serde_json::Value>>,
}
impl<'de> serde::Deserialize<'de> for JsOcrElement {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrElement as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for OCR element extraction.
///
/// Controls how OCR elements are extracted and filtered.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrElementConfig")]
pub struct JsOcrElementConfig {
    /// Whether to include OCR elements in the extraction result.
    ///
    /// When true, the `ocr_elements` field in `ExtractedDocument` will be populated.
    #[napi(js_name = "includeElements")]
    pub include_elements: Option<bool>,
    /// Minimum hierarchical level to include.
    ///
    /// Elements below this level (e.g., words when min_level is Line) will be excluded.
    #[napi(
        js_name = "minLevel",
        ts_type = "OcrElementLevel | 'word' | 'line' | 'block' | 'page'"
    )]
    pub min_level: Option<JsOcrElementLevel>,
    /// Minimum recognition confidence threshold (0.0-1.0).
    ///
    /// Elements with confidence below this threshold will be filtered out.
    #[napi(js_name = "minConfidence")]
    pub min_confidence: Option<f64>,
    /// Whether to build hierarchical relationships between elements.
    ///
    /// When true, emitted elements receive an `element_id` metadata value and `parent_id`
    /// references are populated only when a spatially containing parent is also emitted.
    #[napi(js_name = "buildHierarchy")]
    pub build_hierarchy: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsOcrElementConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrElementConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// OCR extraction result.
///
/// Result of performing OCR on an image or scanned document,
/// including recognized text and detected tables.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrExtractionResult")]
pub struct JsOcrExtractionResult {
    /// Recognized text content
    pub content: Option<String>,
    /// Original MIME type of the processed image
    #[napi(js_name = "mimeType")]
    pub mime_type: Option<String>,
    /// OCR processing metadata (confidence scores, language, etc.)
    pub metadata: Option<HashMap<String, serde_json::Value>>,
    /// Tables detected and extracted via OCR
    pub tables: Option<Vec<JsOcrTable>>,
    /// Structured OCR elements with bounding boxes and confidence scores.
    /// Available when TSV output is requested or table detection is enabled.
    #[napi(js_name = "ocrElements")]
    pub ocr_elements: Option<Vec<JsOcrElement>>,
}
impl<'de> serde::Deserialize<'de> for JsOcrExtractionResult {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrExtractionResult as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// OCR processing metadata.
///
/// Captures information about OCR processing configuration and results.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrMetadata")]
pub struct JsOcrMetadata {
    /// OCR language code(s) used
    pub language: Option<String>,
    /// Tesseract Page Segmentation Mode (PSM)
    pub psm: Option<i32>,
    /// Output format (e.g., "text", "hocr")
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<String>,
    /// Number of tables detected
    #[napi(js_name = "tableCount")]
    pub table_count: Option<u32>,
    /// Number of rows in the detected table (if a single table was found).
    #[napi(js_name = "tableRows")]
    pub table_rows: Option<u32>,
    /// Number of columns in the detected table (if a single table was found).
    #[napi(js_name = "tableCols")]
    pub table_cols: Option<u32>,
}

/// Multi-backend OCR pipeline with quality-based fallback.
///
/// Backends are tried in priority order (highest first). After each backend
/// produces output, quality is evaluated. If it meets `quality_thresholds.pipeline_min_quality`,
/// the result is accepted. Otherwise the next backend is tried; if none clears the
/// threshold, an internal selection policy derived from the `OcrConfig` decides which
/// stage's result is returned as the best effort (`vlm_fallback` pipelines prefer their
/// last non-empty stage; explicit and classical pipelines stay score-based).
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrPipelineConfig")]
pub struct JsOcrPipelineConfig {
    /// Ordered list of backends to try. Sorted by priority (descending) at runtime.
    pub stages: Vec<JsOcrPipelineStage>,
    /// Quality thresholds for deciding whether to accept a result or try the next backend.
    #[napi(js_name = "qualityThresholds")]
    pub quality_thresholds: JsOcrQualityThresholds,
}
impl<'de> serde::Deserialize<'de> for JsOcrPipelineConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrPipelineConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A single backend stage in the OCR pipeline.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrPipelineStage")]
pub struct JsOcrPipelineStage {
    /// Backend name: "tesseract", "paddleocr", "paddle-ocr", "sceptre", "vlm", or a custom registered name.
    /// Sceptre uses ONNX Runtime on desktop/server and tract on Android/iOS; browser WebAssembly has a separate
    /// byte-fed engine because the normal async OCR registry assumes native model storage.
    pub backend: String,
    /// Priority weight (higher = tried first). Stages are sorted by priority descending.
    pub priority: u32,
    /// Language override for this stage (undefined = use parent OcrConfig.language).
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs: `["eng", "deu"]`. When deserializing from a config file,
    /// JSON body, or the REST/MCP API, a single string is also accepted,
    /// either as one code ("eng") or "+"-joined ("eng+deu").
    pub language: Option<Vec<String>>,
    /// Tesseract-specific config override for this stage.
    #[napi(js_name = "tesseractConfig")]
    pub tesseract_config: Option<JsTesseractConfig>,
    /// PaddleOCR-specific config for this stage.
    #[napi(js_name = "paddleOcrConfig")]
    pub paddle_ocr_config: Option<serde_json::Value>,
    /// VLM config override for this pipeline stage.
    #[napi(js_name = "vlmConfig")]
    pub vlm_config: Option<JsLlmConfig>,
    /// Arbitrary per-call options passed through to the backend unchanged.
    ///
    /// Backends that support runtime tuning (mode switching, preprocessing
    /// flags, inference parameters, etc.) read this value and deserialize
    /// the keys they care about. Keys unknown to the backend are silently
    /// ignored, so options from different backends can coexist in the same
    /// config without conflict.
    ///
    /// Example (custom backend):
    /// ```json
    /// { "mode": "fast", "enable_layout": true }
    /// ```
    #[napi(js_name = "backendOptions")]
    pub backend_options: Option<serde_json::Value>,
}
impl<'de> serde::Deserialize<'de> for JsOcrPipelineStage {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrPipelineStage as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A point in OCR raster pixel coordinates.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrPoint")]
pub struct JsOcrPoint {
    /// Horizontal coordinate.
    pub x: Option<u32>,
    /// Vertical coordinate.
    pub y: Option<u32>,
}

/// Quality thresholds for OCR fallback decisions and pipeline quality gating.
///
/// Fields default to conservative extraction behavior. Suspected OCR recognition noise is
/// reported but retained unless destructive filtering is explicitly enabled.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrQualityThresholds")]
pub struct JsOcrQualityThresholds {
    /// Minimum total non-whitespace characters to consider text substantive.
    #[napi(js_name = "minTotalNonWhitespace")]
    pub min_total_non_whitespace: Option<i64>,
    /// Minimum non-whitespace characters per page on average.
    #[napi(js_name = "minNonWhitespacePerPage")]
    pub min_non_whitespace_per_page: Option<f64>,
    /// Minimum character count for a word to be "meaningful".
    #[napi(js_name = "minMeaningfulWordLen")]
    pub min_meaningful_word_len: Option<i64>,
    /// Minimum count of meaningful words before text is accepted.
    #[napi(js_name = "minMeaningfulWords")]
    pub min_meaningful_words: Option<i64>,
    /// Minimum alphanumeric ratio (non-whitespace chars that are alphanumeric).
    #[napi(js_name = "minAlnumRatio")]
    pub min_alnum_ratio: Option<f64>,
    /// Minimum Unicode replacement characters (U+FFFD) to trigger OCR fallback.
    #[napi(js_name = "minGarbageChars")]
    pub min_garbage_chars: Option<i64>,
    /// Maximum fraction of short (1-2 char) words before text is considered fragmented.
    #[napi(js_name = "maxFragmentedWordRatio")]
    pub max_fragmented_word_ratio: Option<f64>,
    /// Critical fragmentation threshold — triggers OCR regardless of meaningful words.
    /// Normal English text has ~20-30% short words. 80%+ is definitive garbage.
    #[napi(js_name = "criticalFragmentedWordRatio")]
    pub critical_fragmented_word_ratio: Option<f64>,
    /// Maximum fraction of short (1-2 char) words an *OCR result* may carry before the page
    /// is reported as suspected recognition noise.
    ///
    /// This is a different decision, and a different operating point, from
    /// `Self.max_fragmented_word_ratio` / `Self.critical_fragmented_word_ratio`: those
    /// ask "is the native text bad enough that we should OCR this page?", and are tuned to
    /// only fire on text that is already definitively broken (0.6 / 0.8). Here we are judging
    /// what OCR *produced*, where the failure mode is an engine run over line art — a scanned
    /// plat, an engineering drawing, a signature flourish — returning confident-looking
    /// strings that are not words.
    ///
    /// Measured over the 16 pages of a recorded municipal ordinance (13 prose pages, 3 scanned
    /// survey drawings): prose ran 0.04-0.28, the drawings 0.42-0.47. The default sits in that
    /// gap with margin on both sides. By default, crossing the threshold emits a processing
    /// warning but retains the recognized text. Set `Self.discard_suspected_ocr_noise` to
    /// `true` to restore destructive filtering.
    #[napi(js_name = "maxOcrOutputFragmentedWordRatio")]
    pub max_ocr_output_fragmented_word_ratio: Option<f64>,
    /// Minimum mean OCR confidence (0-100) below which a page is reported as suspected noise.
    ///
    /// This is the engine's own uncertainty about what it read, and it is a far sharper
    /// instrument than any statistic derived from the output text. Measured per page over a
    /// recorded municipal ordinance (10 prose pages, 6 scanned survey/architectural drawings)
    /// with Tesseract 5.5.3:
    ///
    /// ```text
    /// prose      86.3 - 95.3
    /// drawings   18.5 - 64.3
    /// ```
    ///
    /// The default sits in that gap with ~11 points of margin on each side. Compare the
    /// short-word ratio, which separated the same two groups by 0.09 on a 0-1 scale.
    ///
    /// A backend that reports no confidence (no `mean_text_conf` in its result metadata)
    /// skips this check entirely and falls back to
    /// `Self.max_ocr_output_fragmented_word_ratio`. By default, the signal emits a warning
    /// without discarding content. Set to 0.0 to disable the signal.
    #[napi(js_name = "minOcrMeanConfidence")]
    pub min_ocr_mean_confidence: Option<f64>,
    /// Minimum word count before `Self.max_ocr_output_fragmented_word_ratio` may report a
    /// page. Short pages (a signature block, an exhibit title) are legitimately dominated by
    /// short words, so the ratio is not meaningful on them and the signal stays disabled.
    #[napi(js_name = "minWordsForOcrOutputCheck")]
    pub min_words_for_ocr_output_check: Option<i64>,
    /// Maximum fraction of a Tesseract page's dictionary-checkable words that
    /// `TesseractAPI.is_valid_word` may reject before the page is treated as recognition
    /// noise, supplementing (never replacing) `Self.max_ocr_output_fragmented_word_ratio`.
    ///
    /// Tesseract-only: other backends never report this ratio (see
    /// `dictionary_invalid_word_ratio` in `ocr.processor.execution`), so this threshold is
    /// simply never consulted for them.
    ///
    /// Calibration owed, same as `ocr.types.TesseractConfig.min_confidence`:
    /// unlike `Self.max_ocr_output_fragmented_word_ratio` (measured across a recorded
    /// ordinance's prose vs. drawing pages), this ratio has not yet had a page-level
    /// measurement run over a labeled corpus. The default therefore disables the check
    /// entirely (`1.01`, above the `[0.0, 1.0]` range a ratio can reach) rather than guess an
    /// operating point. Do not lower this without running that measurement first.
    #[napi(js_name = "maxOcrOutputDictInvalidWordRatio")]
    pub max_ocr_output_dict_invalid_word_ratio: Option<f64>,
    /// Discard non-empty OCR text when any configured recognition-noise signal fires.
    ///
    /// Defaults to `false`: suspected noise is retained and surfaced through
    /// `processing_warnings`. Set to `true` to preserve the legacy destructive behavior.
    #[napi(js_name = "discardSuspectedOcrNoise")]
    pub discard_suspected_ocr_noise: Option<bool>,
    /// Minimum average word length. Below this with enough words indicates garbled extraction.
    #[napi(js_name = "minAvgWordLength")]
    pub min_avg_word_length: Option<f64>,
    /// Minimum word count before average word length check applies.
    #[napi(js_name = "minWordsForAvgLengthCheck")]
    pub min_words_for_avg_length_check: Option<i64>,
    /// Minimum consecutive word repetition ratio to detect column scrambling.
    #[napi(js_name = "minConsecutiveRepeatRatio")]
    pub min_consecutive_repeat_ratio: Option<f64>,
    /// Minimum word count before consecutive repetition check is applied.
    #[napi(js_name = "minWordsForRepeatCheck")]
    pub min_words_for_repeat_check: Option<i64>,
    /// Minimum character count for "substantive markdown" OCR skip gate.
    #[napi(js_name = "substantiveMinChars")]
    pub substantive_min_chars: Option<i64>,
    /// Minimum character count for "non-text content" OCR skip gate.
    #[napi(js_name = "nonTextMinChars")]
    pub non_text_min_chars: Option<i64>,
    /// Alphanumeric+whitespace ratio threshold for skip decisions.
    #[napi(js_name = "alnumWsRatioThreshold")]
    pub alnum_ws_ratio_threshold: Option<f64>,
    /// Minimum quality score (0.0-1.0) for a pipeline stage result to be accepted.
    /// If the result from a backend scores below this, try the next backend.
    #[napi(js_name = "pipelineMinQuality")]
    pub pipeline_min_quality: Option<f64>,
    /// Minimum fraction of non-whitespace characters that are undecodable
    /// (Unicode Private Use Area, replacement characters, or non-whitespace
    /// control characters) before a page's text layer is treated as
    /// unreadable and routed to OCR (issue #1254). Gated by
    /// `min_total_non_whitespace` so short snippets with a stray symbol or
    /// two do not trip this check.
    #[napi(js_name = "minUndecodableRatio")]
    pub min_undecodable_ratio: Option<f64>,
    /// Whether to route a page to OCR when xberg_native_pdf reports that a high
    /// fraction of its text was fabricated rather than read from the file
    /// (`MappingProvenance.Fallback`, xberg_native_pdf 0.3.75+, issue #1254). This is
    /// a direct fact from the extractor's ISO 32000-1 §9.10.2 mapping cascade,
    /// distinct from the character-heuristic proxy behind `min_undecodable_ratio`.
    /// Defaults to `true`.
    #[napi(js_name = "enableProvenanceOcrRouting")]
    pub enable_provenance_ocr_routing: Option<bool>,
    /// Minimum fraction of a page's non-whitespace characters with
    /// `MappingProvenance.Fallback` provenance before the page is treated as
    /// having a fabricated text layer and routed to OCR (issue #1254). Gated by
    /// `min_total_non_whitespace` so a short page with a stray fallback
    /// character cannot trip it. Only used when `enable_provenance_ocr_routing`
    /// is `true`.
    #[napi(js_name = "minProvenanceFallbackRatio")]
    pub min_provenance_fallback_ratio: Option<f64>,
}
impl<'de> serde::Deserialize<'de> for JsOcrQualityThresholds {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrQualityThresholds as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "ocrQualityThresholdsDefault")]
pub fn ocr_quality_thresholds_default() -> JsOcrQualityThresholds {
    xberg::OcrQualityThresholds::default().into()
}

/// Rotation information for an OCR element.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrRotation")]
pub struct JsOcrRotation {
    /// Rotation angle in degrees (0, 90, 180, 270 for PaddleOCR).
    #[napi(js_name = "angleDegrees")]
    pub angle_degrees: f64,
    /// Confidence score for the rotation detection.
    pub confidence: Option<f64>,
}

/// Table detected via OCR.
///
/// Represents a table structure recognized during OCR processing.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "OcrTable")]
pub struct JsOcrTable {
    /// Table cells as a 2D vector (rows × columns)
    pub cells: Vec<Vec<String>>,
    /// Markdown representation of the table
    pub markdown: String,
    /// Page number where the table was found (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
    /// Bounding box of the table in pixel coordinates (from OCR word positions).
    #[napi(js_name = "boundingBox")]
    pub bounding_box: Option<JsOcrTableBoundingBox>,
}
impl<'de> serde::Deserialize<'de> for JsOcrTable {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::OcrTable as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Bounding box for an OCR-detected table in pixel coordinates.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrTableBoundingBox")]
pub struct JsOcrTableBoundingBox {
    /// Left x-coordinate (pixels)
    pub left: u32,
    /// Top y-coordinate (pixels)
    pub top: u32,
    /// Right x-coordinate (pixels)
    pub right: u32,
    /// Bottom y-coordinate (pixels)
    pub bottom: u32,
}

/// Document orientation detection result.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OrientationResult")]
pub struct JsOrientationResult {
    /// Detected orientation in degrees (0, 90, 180, or 270).
    pub degrees: u32,
    /// Confidence score (0.0-1.0).
    pub confidence: f64,
}

/// Configuration for PaddleOCR backend.
///
/// Configures PaddleOCR text detection and recognition with multi-language support.
/// Uses a builder pattern for convenient configuration.
///
/// # Examples
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PaddleOcrConfig")]
pub struct JsPaddleOcrConfig {
    /// Language code (e.g., "en", "ch", "jpn", "kor", "deu", "fra")
    pub language: Option<String>,
    /// Optional Hugging Face Hub cache root for model files.
    ///
    /// When unset, the standard `HF_HUB_CACHE`, legacy
    /// `HUGGINGFACE_HUB_CACHE`, and `HF_HOME` conventions are used.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Enable angle classification for rotated text (default: false).
    /// Can misfire on short text regions, rotating crops incorrectly before recognition.
    #[napi(js_name = "useAngleCls")]
    pub use_angle_cls: Option<bool>,
    /// Enable table structure detection (default: **false**, unlike `TesseractConfig`
    /// which defaults to `true` — see `crate.ocr.types.TesseractConfig`).
    ///
    /// PaddleOCR has no per-word table-candidate confidence carve-out the way Tesseract's
    /// TSV does (every recognised word is a clustering candidate), so unfiltered clustering
    /// over-fabricates tables on ordinary prose. This stays off until it clears a
    /// cell-scored accuracy measurement (see the pinning test at the bottom of this file).
    /// Consequence for callers: switching an existing config from the Tesseract backend to
    /// PaddleOCR with stock defaults silently produces **no** OCR tables — enable this
    /// explicitly if you need them.
    ///
    /// When enabled, this only clusters and reconstructs a grid from word boxes PaddleOCR
    /// has already produced (see `crate.paddle_ocr.backend.PaddleOcrBackend.process_image`)
    /// — it does not run any additional model inference, so the added per-page cost is the
    /// CPU-only clustering/reconstruction pass, not another ONNX call.
    #[napi(js_name = "enableTableDetection")]
    pub enable_table_detection: Option<bool>,
    /// Database threshold for text detection (default: 0.3)
    /// Range: 0.0-1.0, higher values require more confident detections
    #[napi(js_name = "detDbThresh")]
    pub det_db_thresh: Option<f64>,
    /// Box threshold for text bounding box refinement (default: 0.5)
    /// Range: 0.0-1.0
    #[napi(js_name = "detDbBoxThresh")]
    pub det_db_box_thresh: Option<f64>,
    /// Unclip ratio for expanding text bounding boxes (default: 1.6)
    /// Controls the expansion of detected text regions
    #[napi(js_name = "detDbUnclipRatio")]
    pub det_db_unclip_ratio: Option<f64>,
    /// Maximum side length for detection image (default: 1024)
    /// Larger images may be resized to this limit for faster inference
    #[napi(js_name = "detLimitSideLen")]
    pub det_limit_side_len: Option<u32>,
    /// Batch size for recognition inference (default: 6)
    /// Number of text regions to process simultaneously
    #[napi(js_name = "recBatchNum")]
    pub rec_batch_num: Option<u32>,
    /// Padding in pixels added around the image before detection (default: 10).
    /// Large values can include surrounding content like table gridlines.
    pub padding: Option<u32>,
    /// Minimum recognition confidence score for text lines (default: 0.5).
    /// Text regions with recognition confidence below this threshold are discarded.
    /// Matches PaddleOCR Python's `drop_score` parameter.
    /// Range: 0.0-1.0
    #[napi(js_name = "dropScore")]
    pub drop_score: Option<f64>,
    /// Model tier controlling detection/recognition model size and accuracy trade-off.
    ///
    /// For PP-OCRv5 (`model_version = "pp-ocrv5"`):
    /// - `"mobile"` (default): Lightweight models (~4.5MB detection, ~16.5MB recognition), fast download and inference
    /// - `"server"`: Large, high-accuracy models (~88MB detection, ~84MB recognition), best for GPU or complex documents
    ///
    /// For PP-OCRv6 (`model_version = "pp-ocrv6"`, the default):
    /// - `"small"`: ~9.9MB detection, full 18,708-char CJK+Latin+JA/KO recognition dictionary.
    ///   The default `"mobile"` resolves here, so this is what an unconfigured extraction uses.
    /// - `"medium"`: ~62MB detection, same dictionary. Higher accuracy, substantially slower on
    ///   CPU. A legacy `"server"` tier, or any unrecognised value, resolves here.
    /// - `"tiny"`: ~1.8MB detection, but a reduced 6,904-char (~zh/en) dictionary — it cannot
    ///   read the scripts the other two cover.
    ///
    /// Note PaddleOCR pages do not run concurrently: the ONNX session is held behind a mutex, so
    /// the thread budget goes to intra-op parallelism and wall time scales with page count times
    /// per-page inference. Tier choice therefore dominates throughput on multi-page documents.
    #[napi(js_name = "modelTier")]
    pub model_tier: Option<String>,
    /// Model generation: `"pp-ocrv6"` (default) or `"pp-ocrv5"`.
    ///
    /// PP-OCRv6 adds a unified CJK+Latin+JA/KO recognition model with `medium`/`small`/`tiny`
    /// tiers (see `model_tier`). Scripts outside the v6 unified coverage (Arabic, Cyrillic,
    /// Devanagari, Greek, Tamil, Telugu, Thai) transparently fall back to the PP-OCRv5
    /// per-script recognition models. Defaults to `"pp-ocrv6"`; the default `model_tier`
    /// (`"mobile"`) resolves to the v6 `"small"` tier. Select `"pp-ocrv5"` to pin the
    /// legacy per-script/unified fleet.
    #[napi(js_name = "modelVersion")]
    pub model_version: Option<String>,
    /// Explicit inference engine choice.
    ///
    /// `None` (the default) resolves to the compiled default: `ort` when the
    /// `paddle-ocr-ort` feature is compiled in, otherwise `tract`. An explicit choice
    /// is validated against the compiled features when the OCR engine is constructed
    /// (see `crate.paddle_ocr.backend.effective_backend`); requesting an engine
    /// whose feature is not compiled in is a clear configuration error rather than a
    /// silent fallback.
    #[napi(js_name = "inferenceBackend", ts_type = "PaddleInferenceBackend | 'ort' | 'tract'")]
    pub inference_backend: Option<JsPaddleInferenceBackend>,
}
impl<'de> serde::Deserialize<'de> for JsPaddleOcrConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PaddleOcrConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Sets a custom Hugging Face Hub cache root for model files.
///
/// # Arguments
///
/// * `path` - Path to cache directory
///
/// # Examples
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithCacheDir")]
pub fn paddle_ocr_config_with_cache_dir(cfg: JsPaddleOcrConfig, path: String) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_cache_dir(std::path::PathBuf::from(path));
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Enables or disables table structure detection.
///
/// # Arguments
///
/// * `enable` - Whether to enable table detection
///
/// # Examples
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithTableDetection")]
pub fn paddle_ocr_config_with_table_detection(cfg: JsPaddleOcrConfig, enable: bool) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_table_detection(enable);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Enables or disables angle classification for rotated text.
///
/// # Arguments
///
/// * `enable` - Whether to enable angle classification
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithAngleCls")]
pub fn paddle_ocr_config_with_angle_cls(cfg: JsPaddleOcrConfig, enable: bool) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_angle_cls(enable);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the database threshold for text detection.
///
/// # Arguments
///
/// * `threshold` - Detection threshold (0.0-1.0)
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithDetDbThresh")]
pub fn paddle_ocr_config_with_det_db_thresh(cfg: JsPaddleOcrConfig, threshold: f64) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_det_db_thresh(threshold as f32);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the box threshold for text bounding box refinement.
///
/// # Arguments
///
/// * `threshold` - Box threshold (0.0-1.0)
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithDetDbBoxThresh")]
pub fn paddle_ocr_config_with_det_db_box_thresh(
    cfg: JsPaddleOcrConfig,
    threshold: f64,
) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_det_db_box_thresh(threshold as f32);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the unclip ratio for expanding text bounding boxes.
///
/// # Arguments
///
/// * `ratio` - Unclip ratio (typically 1.5-2.0)
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithDetDbUnclipRatio")]
pub fn paddle_ocr_config_with_det_db_unclip_ratio(
    cfg: JsPaddleOcrConfig,
    ratio: f64,
) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_det_db_unclip_ratio(ratio as f32);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the maximum side length for detection images.
///
/// # Arguments
///
/// * `length` - Maximum side length in pixels
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithDetLimitSideLen")]
pub fn paddle_ocr_config_with_det_limit_side_len(
    cfg: JsPaddleOcrConfig,
    length: u32,
) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_det_limit_side_len(length);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the batch size for recognition inference.
///
/// # Arguments
///
/// * `batch_size` - Number of text regions to process simultaneously
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithRecBatchNum")]
pub fn paddle_ocr_config_with_rec_batch_num(
    cfg: JsPaddleOcrConfig,
    batch_size: u32,
) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_rec_batch_num(batch_size);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the minimum recognition confidence threshold.
///
/// # Arguments
///
/// * `score` - Minimum confidence (0.0-1.0), text below this is dropped
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithDropScore")]
pub fn paddle_ocr_config_with_drop_score(cfg: JsPaddleOcrConfig, score: f64) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_drop_score(score as f32);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets padding in pixels added around images before detection.
///
/// # Arguments
///
/// * `padding` - Padding in pixels (0-100)
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithPadding")]
pub fn paddle_ocr_config_with_padding(cfg: JsPaddleOcrConfig, padding: u32) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_padding(padding);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the model tier controlling detection/recognition model size.
///
/// # Arguments
///
/// * `tier` - `"mobile"` (default, lightweight, faster) or `"server"` (high accuracy, GPU/complex documents)
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithModelTier")]
pub fn paddle_ocr_config_with_model_tier(cfg: JsPaddleOcrConfig, tier: String) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_model_tier(tier);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Sets the model generation.
///
/// # Arguments
///
/// * `version` - `"pp-ocrv6"` (default) or `"pp-ocrv5"`. Under `"pp-ocrv6"`, `model_tier`
///   selects among `"medium"`/`"small"`/`"tiny"`.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "paddleOcrConfigWithModelVersion")]
pub fn paddle_ocr_config_with_model_version(
    cfg: JsPaddleOcrConfig,
    version: String,
) -> napi::Result<JsPaddleOcrConfig> {
    let _json =
        serde_json::to_string(&cfg).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _core: xberg::PaddleOcrConfig =
        serde_json::from_str(&_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    let _result = _core.with_model_version(version);
    let _out_json =
        serde_json::to_string(&_result).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    serde_json::from_str(&_out_json).map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}
/// Creates a default configuration with English language support.
#[napi(js_name = "paddleOcrConfigDefault")]
pub fn paddle_ocr_config_default() -> JsPaddleOcrConfig {
    xberg::PaddleOcrConfig::default().into()
}

/// Runtime options accepted by the `candle-paddleocr-vl` backend.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PaddleOcrVlBackendOptions")]
pub struct JsPaddleOcrVlBackendOptions {
    /// Optional per-call recognition task; the backend constructor's task is used when absent.
    #[napi(ts_type = "PaddleOcrVlTaskKind | 'ocr' | 'table' | 'formula' | 'chart'")]
    pub task: Option<JsPaddleOcrVlTaskKind>,
    /// Optional local model directory, which takes precedence over `model_id`.
    #[napi(js_name = "modelPath")]
    pub model_path: Option<String>,
    /// Optional Hugging Face repository identifier.
    #[napi(js_name = "modelId")]
    pub model_id: Option<String>,
    /// Optional immutable Hugging Face model revision.
    #[napi(js_name = "hfRevision")]
    pub hf_revision: Option<String>,
    /// Optional Hugging Face cache root.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Optional per-call device override.
    #[napi(ts_type = "CandleDevicePreference | 'auto' | 'cpu' | 'cuda' | 'metal'")]
    pub device: Option<JsCandleDevicePreference>,
}
impl<'de> serde::Deserialize<'de> for JsPaddleOcrVlBackendOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::candle_ocr::PaddleOcrVlBackendOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Byte offset boundary for a page.
///
/// Tracks where a specific page's content starts and ends in the main content string,
/// enabling mapping from byte positions to page numbers. Offsets are guaranteed to be
/// at valid UTF-8 character boundaries when using standard String methods (push_str, push, etc.).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageBoundary")]
pub struct JsPageBoundary {
    /// Byte offset where this page starts in the content string (UTF-8 valid boundary, inclusive)
    #[napi(js_name = "byteStart")]
    pub byte_start: i64,
    /// Byte offset where this page ends in the content string (UTF-8 valid boundary, exclusive)
    #[napi(js_name = "byteEnd")]
    pub byte_end: i64,
    /// Page number (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
}

/// Classification result for a single page.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageClassification")]
pub struct JsPageClassification {
    /// 1-indexed page number this classification belongs to.
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
    /// Labels assigned to the page. Single-label classification yields exactly one
    /// entry; multi-label classification yields any subset of the configured label set.
    pub labels: Vec<JsClassificationLabel>,
}

/// Configuration for the page-classification post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PageClassificationConfig")]
pub struct JsPageClassificationConfig {
    /// Minijinja prompt template. Receives `{{ labels }}` (joined list), `{{ page_text }}`
    /// and `{{ multi_label }}` variables. `None` lets the backend pick a sensible default.
    #[napi(js_name = "promptTemplate")]
    pub prompt_template: Option<String>,
    /// The set of labels the classifier may emit. Must contain at least one entry.
    pub labels: Vec<String>,
    /// Allow multiple labels per page. Single-label mode returns at most one label.
    #[napi(js_name = "multiLabel")]
    pub multi_label: bool,
    /// LLM configuration used for classification.
    pub llm: JsLlmConfig,
}
impl<'de> serde::Deserialize<'de> for JsPageClassificationConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PageClassificationConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Page extraction and tracking configuration.
///
/// Controls how pages are extracted, tracked, and represented in the extraction results.
/// When `None`, page tracking is disabled.
///
/// Page range tracking in chunk metadata (first_page/last_page) is automatically enabled
/// when page boundaries are available and chunking is configured.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PageConfig")]
pub struct JsPageConfig {
    /// Extract pages as separate array (ExtractedDocument.pages)
    #[napi(js_name = "extractPages")]
    pub extract_pages: Option<bool>,
    /// Insert page markers in main content string
    #[napi(js_name = "insertPageMarkers")]
    pub insert_page_markers: Option<bool>,
    /// Page marker format (use {page_num} placeholder)
    /// Default: "\n\n<!-- PAGE {page_num} -->\n\n"
    #[napi(js_name = "markerFormat")]
    pub marker_format: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsPageConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PageConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "pageConfigDefault")]
pub fn page_config_default() -> JsPageConfig {
    xberg::PageConfig::default().into()
}

/// Content for a single page/slide.
///
/// When page extraction is enabled, documents are split into per-page content
/// with associated tables and images mapped to each page.
///
/// # Performance
///
/// Uses Arc-wrapped tables and images for memory efficiency:
/// - `Vec<Arc<Table>>` enables zero-copy sharing of table data
/// - `Vec<Arc<ExtractedImage>>` enables zero-copy sharing of image data
/// - Maintains exact JSON compatibility via custom Serialize/Deserialize
///
/// This reduces memory overhead for documents with shared tables/images
/// by avoiding redundant copies during serialization.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PageContent")]
pub struct JsPageContent {
    /// Page number (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
    /// Text content for this page
    pub content: String,
    /// Tables found on this page (uses Arc for memory efficiency)
    ///
    /// Serializes as `Vec<Table>` for JSON compatibility while maintaining
    /// Arc semantics in-memory for zero-copy sharing.
    pub tables: Vec<JsTable>,
    /// Indices into `ExtractedDocument.images` for images found on this page.
    ///
    /// Each value is a zero-based index into the top-level `images` collection.
    /// Only populated when `extract_images = true` in the extraction config.
    #[napi(js_name = "imageIndices")]
    pub image_indices: Vec<u32>,
    /// OCR image preprocessing applied to this page's raster.
    #[napi(js_name = "imagePreprocessing")]
    pub image_preprocessing: Option<JsImagePreprocessingMetadata>,
    /// Hierarchy information for the page (when hierarchy extraction is enabled)
    ///
    /// Contains text hierarchy levels (H1-H6) extracted from the page content.
    pub hierarchy: Option<JsPageHierarchy>,
    /// Whether this page is blank (no meaningful text content)
    ///
    /// Determined during extraction based on text content analysis.
    /// A page is blank if it has fewer than 3 non-whitespace characters
    /// and contains no tables or images.
    #[napi(js_name = "isBlank")]
    pub is_blank: Option<bool>,
    /// Layout detection regions for this page (when layout detection is enabled).
    ///
    /// Contains detected layout regions with class, confidence, bounding box,
    /// and area fraction. Only populated when layout detection is configured.
    #[napi(js_name = "layoutRegions")]
    pub layout_regions: Option<Vec<JsLayoutRegion>>,
    /// Speaker notes for this slide (PPTX only).
    ///
    /// Contains the text from the slide's notes pane (`ppt/notesSlides/notesSlide{N}.xml`).
    /// Only populated when the source is a PPTX file and notes are present.
    #[napi(js_name = "speakerNotes")]
    pub speaker_notes: Option<String>,
    /// Section name this slide belongs to (PPTX only).
    ///
    /// PowerPoint sections group slides into logical chapters (`<p:sectionLst>` in
    /// `ppt/presentation.xml`). Only populated when the source is a PPTX file and
    /// the slide belongs to a named section.
    #[napi(js_name = "sectionName")]
    pub section_name: Option<String>,
    /// Sheet name for this page (XLSX/ODS only).
    ///
    /// Each spreadsheet sheet maps to one `PageContent` entry. This field carries the
    /// sheet's display name as it appears in the workbook. `None` for all non-spreadsheet
    /// formats and for sheets with an empty name.
    #[napi(js_name = "sheetName")]
    pub sheet_name: Option<String>,
    /// Aggregate OCR confidence for this page. `None` when the page was not OCR'd.
    #[napi(js_name = "ocrConfidence")]
    pub ocr_confidence: Option<JsPageOcrConfidence>,
}
impl<'de> serde::Deserialize<'de> for JsPageContent {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PageContent as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Metadata for individual page/slide/sheet.
///
/// Captures per-page information including dimensions, content counts,
/// and visibility state (for presentations).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageDimensions")]
pub struct JsPageDimensions {
    /// Page width in points or pixels.
    pub width: Option<f64>,
    /// Page height in points or pixels.
    pub height: Option<f64>,
}

/// Page hierarchy structure containing heading levels and block information.
///
/// Used when PDF text hierarchy extraction is enabled. Contains hierarchical
/// blocks with heading levels (H1-H6) for semantic document structure.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PageHierarchy")]
pub struct JsPageHierarchy {
    /// Number of hierarchy blocks on this page
    #[napi(js_name = "blockCount")]
    pub block_count: u32,
    /// Hierarchical blocks with heading levels
    pub blocks: Vec<JsHierarchicalBlock>,
}
impl<'de> serde::Deserialize<'de> for JsPageHierarchy {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PageHierarchy as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PageInfo")]
pub struct JsPageInfo {
    /// Page number (1-indexed)
    pub number: u32,
    /// Page title (usually for presentations)
    pub title: Option<String>,
    /// Dimensions in points (PDF) or pixels (images).
    pub dimensions: Option<JsPageDimensions>,
    /// Number of images on this page
    #[napi(js_name = "imageCount")]
    pub image_count: Option<u32>,
    /// Number of tables on this page
    #[napi(js_name = "tableCount")]
    pub table_count: Option<u32>,
    /// Whether this page is hidden (e.g., in presentations)
    pub hidden: Option<bool>,
    /// Whether this page is blank (no meaningful text, no images, no tables)
    ///
    /// A page is considered blank if it has fewer than 3 non-whitespace characters
    /// and contains no tables or images. This is useful for filtering out empty pages
    /// in scanned documents or PDFs with blank separator pages.
    #[napi(js_name = "isBlank")]
    pub is_blank: Option<bool>,
    /// Whether this page contains non-trivial vector graphics (paths, shapes, curves)
    ///
    /// Indicates the presence of vector-drawn content such as charts, diagrams,
    /// or geometric shapes (e.g., from Adobe InDesign, LaTeX TikZ). These are
    /// invisible to `ExtractedDocument.images` since they are not embedded as raster
    /// XObjects. Set to `true` when path count exceeds a heuristic threshold,
    /// signaling that downstream consumers may want to rasterize the page to
    /// capture this content.
    ///
    /// Only populated for PDFs; `None` for other document types.
    #[napi(js_name = "hasVectorGraphics")]
    pub has_vector_graphics: bool,
}
impl<'de> serde::Deserialize<'de> for JsPageInfo {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PageInfo as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Aggregate OCR legibility score for a page, reported by the backend that produced its text.
///
/// This is distinct from `OcrConfidence`, which scores a single detected element (a word or
/// line) using detection/recognition confidence from the OCR engine itself. `PageOcrConfidence`
/// is a page-level summary computed after noise filtering, intended for triage of which pages
/// are worth a closer look, not for comparing OCR engines against each other.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageOcrConfidence")]
pub struct JsPageOcrConfidence {
    /// Aggregate legibility score in `0.0..=1.0`, or `None` when the backend that
    /// produced this page does not report a calibrated legibility scale.
    ///
    /// Backends differ in what their confidence numbers mean (see `OcrConfidence`'s
    /// per-backend normalization), and not every backend maps onto a 0.0-1.0 legibility
    /// scale at all. When a backend has no such calibrated scale, this is `None` rather
    /// than a misleading number, and scores must never be compared across backends.
    pub score: Option<f64>,
    /// Number of words the score was averaged over, AFTER noise filtering.
    ///
    /// A small `word_count` means the average is based on little evidence, so a high
    /// `score` next to a small `word_count` is not representative of the whole page.
    #[napi(js_name = "wordCount")]
    pub word_count: Option<u32>,
    /// Name of the OCR backend that produced the page text.
    pub backend: Option<String>,
}

/// Page range for a chunk (0-indexed, inclusive).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageRange")]
pub struct JsPageRange {
    /// Start page (0-indexed, inclusive).
    pub start: u32,
    /// End page (0-indexed, inclusive).
    pub end: u32,
}

/// Per-page signals extracted from PDF content.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageSignals")]
pub struct JsPageSignals {
    /// 1-indexed page number.
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
    /// First ~500 characters of extracted text.
    #[napi(js_name = "textExcerpt")]
    pub text_excerpt: String,
    /// `true` if page starts with letterhead-like content (ALL CAPS line in first 5 lines
    /// or a logo-image bbox at top).
    #[napi(js_name = "startsWithLetterheadLike")]
    pub starts_with_letterhead_like: bool,
    /// `true` if text contains "Page 1" or "1 of N" pattern.
    #[napi(js_name = "hasPageNumberOneMarker")]
    pub has_page_number_one_marker: bool,
    /// `true` if text contains signature indicators ("Sincerely", "Signed") or
    /// a signature image bbox.
    #[napi(js_name = "hasSignatureBlock")]
    pub has_signature_block: bool,
    /// Text density: characters per page area, normalised to `[0.0, 1.0]`.
    #[napi(js_name = "layoutTextDensity")]
    pub layout_text_density: f64,
}

/// Derive signals from raw page text.
///
/// Callers that already have structured per-page data (e.g. from a PDF extractor)
/// can set individual fields directly.  This constructor is for callers that only
/// have the plain-text content of a page (e.g. from `PageContent`).
///
/// # Arguments
///
/// * `page_number` — 1-indexed page number.
/// * `text` — Full extracted text for the page.
/// * `layout_text_density` — Pre-computed text density in `[0.0, 1.0]`.  Pass `0.0`
///   when unknown (disables density-shift detection for this page).
///
/// # Heuristics
///
/// All signal derivations are *conservative starting points*.  Each is documented
/// inline.  They err on the side of fewer false positives; tune thresholds via
/// `MultidocThresholds` rather than by changing these heuristics.
#[napi(js_name = "pageSignalsFromPageText")]
pub fn page_signals_from_page_text(page_number: u32, text: String, layout_text_density: f64) -> JsPageSignals {
    xberg::PageSignals::from_page_text(page_number, &text, layout_text_density as f32).into()
}

/// A single page covered by a chunk, with an optional bounding box on that page.
///
/// See `ChunkMetadata.page_spans` (#1295) for population semantics.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageSpan")]
pub struct JsPageSpan {
    /// Page number (1-indexed).
    pub page: u32,
    /// Bounding box on this page, if known.
    pub bbox: Option<JsBoundingBox>,
}

/// Unified page structure for documents.
///
/// Supports different page types (PDF pages, PPTX slides, Excel sheets)
/// with character offset boundaries for chunk-to-page mapping.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PageStructure")]
pub struct JsPageStructure {
    /// Total number of pages/slides/sheets
    #[napi(js_name = "totalCount")]
    pub total_count: u32,
    /// Type of paginated unit
    #[napi(js_name = "unitType", ts_type = "PageUnitType | 'page' | 'slide' | 'sheet'")]
    pub unit_type: JsPageUnitType,
    /// Character offset boundaries for each page
    ///
    /// Maps character ranges in the extracted content to page numbers.
    /// Used for chunk page range calculation.
    pub boundaries: Option<Vec<JsPageBoundary>>,
    /// Detailed per-page metadata (optional, only when needed)
    pub pages: Option<Vec<JsPageInfo>>,
}

/// One detected PII span in the input text.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PatternMatch")]
pub struct JsPatternMatch {
    /// Inclusive byte-offset start of the match in the source text.
    pub start: i64,
    /// Exclusive byte-offset end of the match.
    pub end: i64,
    /// Category the match belongs to.
    pub category: JsPiiCategory,
    /// Matched substring (owned copy — pattern engine returns owned data so the
    /// caller can free the original text if needed before replacement).
    pub text: String,
}

/// A PDF annotation extracted from a document page.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PdfAnnotation")]
pub struct JsPdfAnnotation {
    /// The type of annotation.
    #[napi(
        js_name = "annotationType",
        ts_type = "PdfAnnotationType | 'text' | 'highlight' | 'link' | 'stamp' | 'underline' | 'strike_out' | 'squiggly' | 'ink' | 'square' | 'circle' | 'polygon' | 'poly_line' | 'line' | 'caret' | 'file_attachment' | 'sound' | 'movie' | 'other'"
    )]
    pub annotation_type: JsPdfAnnotationType,
    /// Text content of the annotation (e.g., comment text, link URL).
    pub content: Option<String>,
    /// Page number where the annotation appears (1-indexed).
    #[napi(js_name = "pageNumber")]
    pub page_number: u32,
    /// Bounding box of the annotation on the page.
    #[napi(js_name = "boundingBox")]
    pub bounding_box: Option<JsBoundingBox>,
    /// Author/creator of the annotation (PDF `/T` entry).
    pub author: Option<String>,
    /// Last modification date of the annotation (PDF `/M` entry), as a raw
    /// PDF date string (e.g. `"D:20240115120000Z"`).
    pub modified: Option<String>,
    /// Annotation colour (PDF `/C` entry), normalised to a CSS-compatible
    /// `#rrggbb` hex string. Gray and CMYK colour spaces are converted to RGB.
    pub color: Option<String>,
    /// Subject of the annotation (PDF `/Subj` entry).
    pub subject: Option<String>,
    /// Per-line bounding boxes derived from the annotation's `/QuadPoints`
    /// entry. Present for text markup annotations (Highlight, Underline,
    /// StrikeOut, Squiggly), one box per marked line/run of text.
    #[napi(js_name = "quadPoints")]
    pub quad_points: Option<Vec<JsBoundingBox>>,
    /// The document text covered by `Self.quad_points`, recovered from the
    /// page content underneath the marked-up region. Populated for
    /// Highlight, Underline, StrikeOut, and Squiggly annotations when the
    /// underlying text could be recovered.
    #[napi(js_name = "markedText")]
    pub marked_text: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsPdfAnnotation {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PdfAnnotation as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// PDF-specific configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PdfConfig")]
pub struct JsPdfConfig {
    /// Extract images from PDF
    #[napi(js_name = "extractImages")]
    pub extract_images: Option<bool>,
    /// Extract tables from PDF.
    ///
    /// When `true` (default), runs the native engine's grid detector and, if it
    /// finds nothing, falls back to the heuristic text-layer reconstruction in
    /// `pdf.native.table.extract_tables_heuristic`. Set to `false` to skip
    /// both passes — `tables` will then be empty in the result.
    #[napi(js_name = "extractTables")]
    pub extract_tables: Option<bool>,
    /// List of passwords to try when opening encrypted PDFs
    pub passwords: Option<Vec<String>>,
    /// Extract PDF metadata
    #[napi(js_name = "extractMetadata")]
    pub extract_metadata: Option<bool>,
    /// Hierarchy extraction configuration (undefined = hierarchy extraction disabled)
    pub hierarchy: Option<JsHierarchyConfig>,
    /// Extract PDF annotations (text notes, highlights, links, stamps).
    /// Default: false
    #[napi(js_name = "extractAnnotations")]
    pub extract_annotations: Option<bool>,
    /// Top margin fraction (0.0–1.0) of page height to exclude headers/running heads.
    /// Ignored when `ContentFilterConfig.include_headers` is `true`.
    /// Effective nonzero margins require per-page OCR so geometry can be filtered;
    /// document-capable OCR backends use their image-processing path in that case.
    /// Default: 0.0 (disabled; set explicitly to filter header content)
    #[napi(js_name = "topMarginFraction")]
    pub top_margin_fraction: Option<f64>,
    /// Bottom margin fraction (0.0–1.0) of page height to exclude footers/page numbers.
    /// Ignored when `ContentFilterConfig.include_footers` is `true`.
    /// Effective nonzero margins require per-page OCR so geometry can be filtered;
    /// document-capable OCR backends use their image-processing path in that case.
    /// Default: 0.0 (disabled; set explicitly to filter footer content)
    #[napi(js_name = "bottomMarginFraction")]
    pub bottom_margin_fraction: Option<f64>,
    /// Allow single-column pseudo tables in extraction results.
    ///
    /// By default, tables with fewer than 2 columns (layout-guided) or 3 columns
    /// (heuristic) are rejected. When `true`, the minimum column count is relaxed
    /// to 1, allowing single-column structured data (glossaries, itemized lists)
    /// to be emitted as tables. Other quality filters (density, sparsity, prose
    /// detection) still apply.
    #[napi(js_name = "allowSingleColumnTables")]
    pub allow_single_column_tables: Option<bool>,
    /// Perform OCR on inline images extracted from PDF pages and attach the
    /// recognized text to each `ExtractedImage.ocr_result`. Uses the backend
    /// selected by `ExtractionConfig.ocr`, or the default OCR backend when no
    /// OCR configuration is supplied. Requires the `ocr` or `ocr-pipeline`
    /// feature. Per-image failures degrade gracefully (the image is returned
    /// without OCR text rather than failing the whole extraction). Default:
    /// `false`.
    #[napi(js_name = "ocrInlineImages")]
    pub ocr_inline_images: Option<bool>,
    /// Extract AcroForm and XFA form fields into `ExtractedDocument.form_fields`.
    ///
    /// When `true` (default), reads the document's interactive form structure
    /// (field names, types, values, widget geometry). Cheap and strictly
    /// additive — non-form PDFs simply yield an empty list. Set to `false` to
    /// skip the form pass entirely.
    #[napi(js_name = "extractFormFields")]
    pub extract_form_fields: Option<bool>,
    /// Reorder extracted text by layout-detected reading order.
    ///
    /// When `true`, projects text spans onto layout-detected regions, performs
    /// column detection, and emits spans in natural reading order (important
    /// for multi-column academic PDFs). It also repairs 90/180/270-degree
    /// rotated text runs — sideways tables and captions — that otherwise read
    /// word-reversed and glued (GH#1358); see
    /// `crate.extractors.pdf.reading_order` for the rotation-handling
    /// details and its limits. Requires the `layout-detection` feature and a
    /// page for which layout detection actually produces hints: a page with
    /// no detected regions falls back to the original, unrepaired extraction
    /// order even with this enabled. Independent of
    /// `LayoutStrategy`, which only
    /// controls whether layout detection runs at all — enabling `Always` or
    /// `Auto` alone does not turn reordering on. Defaults to `false`.
    #[napi(js_name = "readingOrder")]
    pub reading_order: Option<bool>,
    /// Which engine parses and renders this PDF.
    ///
    /// Defaults to `PdfBackend.Native`. Selecting `PdfBackend.Pdfium`
    /// requires the `pdf-pdfium` feature and is rejected otherwise; the
    /// pdfium engine is also deliberately narrower in scope than `Native` --
    /// see `PdfBackend` and `extractors.pdf.pdfium_engine` for details.
    #[napi(ts_type = "PdfBackend | 'native' | 'pdfium'")]
    pub backend: Option<JsPdfBackend>,
}
impl<'de> serde::Deserialize<'de> for JsPdfConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PdfConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[cfg(feature = "pdf")]
#[napi(js_name = "pdfConfigDefault")]
pub fn pdf_config_default() -> JsPdfConfig {
    xberg::PdfConfig::default().into()
}

/// A form field extracted from a PDF's AcroForm or XFA structure.
///
/// Populated by the PDF extractor when `PdfConfig.extract_form_fields` is
/// enabled and the document is a fillable form. Supports both AcroForm (standard)
/// and XFA (XML Forms Architecture) layers. When both are present, AcroForm fields
/// take priority (canonical fallback per PDF spec), and XFA-only fields are appended.
/// The collection is empty for non-form PDFs and for non-PDF formats.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PdfFormField")]
pub struct JsPdfFormField {
    /// Partial field name (the leaf name within the field hierarchy).
    pub name: String,
    /// Fully-qualified field name (dotted path from the form root).
    #[napi(js_name = "fullName")]
    pub full_name: String,
    /// Classified field type.
    #[napi(
        js_name = "fieldType",
        ts_type = "FormFieldType | 'text' | 'checkbox' | 'radio' | 'choice' | 'signature' | 'button' | 'unknown'"
    )]
    pub field_type: JsFormFieldType,
    /// Current field value, if any.
    pub value: Option<String>,
    /// Default field value, if any.
    #[napi(js_name = "defaultValue")]
    pub default_value: Option<String>,
    /// Raw field-flags bitmask (read-only, required, multiline, …).
    pub flags: u32,
    /// 1-indexed page the field's widget appears on. Currently always `None` for
    /// AcroForm fields; page assignment is a deferred enhancement requiring spatial
    /// analysis of widget annotations per page.
    pub page: Option<u32>,
    /// Widget bounding box on its page, if known.
    pub bbox: Option<JsBoundingBox>,
    /// Maximum input length for text fields, if specified.
    #[napi(js_name = "maxLength")]
    pub max_length: Option<u32>,
    /// Tooltip / alternate field description, if present.
    pub tooltip: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsPdfFormField {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PdfFormField as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// PDF-specific metadata.
///
/// Contains metadata fields specific to PDF documents that are not in the common
/// `Metadata` structure. Common fields like title, authors, keywords, and dates
/// are at the `Metadata` level.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PdfMetadata")]
pub struct JsPdfMetadata {
    /// PDF version (e.g., "1.7", "2.0")
    #[napi(js_name = "pdfVersion")]
    pub pdf_version: Option<String>,
    /// PDF producer (application that created the PDF)
    pub producer: Option<String>,
    /// Whether the PDF is encrypted/password-protected
    #[napi(js_name = "isEncrypted")]
    pub is_encrypted: Option<bool>,
    /// First page width in points (1/72 inch)
    pub width: Option<i64>,
    /// First page height in points (1/72 inch)
    pub height: Option<i64>,
    /// Total number of pages in the PDF document
    #[napi(js_name = "pageCount")]
    pub page_count: Option<u32>,
    /// How strongly the document's most scan-like page resembles a scan, in `[0.0, 1.0]`.
    ///
    /// `None` when the document could not be inspected. A full-page raster with no
    /// visible text scores at least `0.85`; a born-digital slide with a full-bleed
    /// background image scores `0.50`.
    #[napi(js_name = "scannedConfidence")]
    pub scanned_confidence: Option<f64>,
    /// Pages that look like scans (1-indexed), using the default confidence threshold.
    ///
    /// `None` when the document could not be inspected; empty when no page qualifies.
    #[napi(js_name = "scannedPages")]
    pub scanned_pages: Option<Vec<u32>>,
    /// Pages the `auto` layout strategy skipped (1-indexed).
    ///
    /// `None` unless layout detection ran with `LayoutStrategy.Auto`; empty
    /// when the gate selected every page.
    #[napi(js_name = "layoutGatedPages")]
    pub layout_gated_pages: Option<Vec<u32>>,
    /// Why the `auto` layout gate selected or skipped each page.
    ///
    /// Index `i` is page `i + 1`. Snake_case values such as `multi_column`,
    /// `table_grid`, or `plain_text` (the skip reason). `None` unless layout
    /// detection ran with `LayoutStrategy.Auto`.
    #[napi(js_name = "layoutGateReasons")]
    pub layout_gate_reasons: Option<Vec<String>>,
}

/// Image preprocessing metadata.
///
/// Tracks the transformations applied to an image during OCR preprocessing,
/// including DPI normalization, resizing, and resampling.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PixelDimensions")]
pub struct JsPixelDimensions {
    /// Width in pixels.
    pub width: Option<i64>,
    /// Height in pixels.
    pub height: Option<i64>,
}

/// Post-processor configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PostProcessorConfig")]
pub struct JsPostProcessorConfig {
    /// Enable post-processors
    pub enabled: Option<bool>,
    /// Whitelist of processor names to run (undefined = all enabled)
    #[napi(js_name = "enabledProcessors")]
    pub enabled_processors: Option<Vec<String>>,
    /// Blacklist of processor names to skip (undefined = none disabled)
    #[napi(js_name = "disabledProcessors")]
    pub disabled_processors: Option<Vec<String>>,
    /// Pre-computed AHashSet for O(1) enabled processor lookup
    #[napi(js_name = "enabledSet")]
    pub enabled_set: Option<Vec<String>>,
    /// Pre-computed AHashSet for O(1) disabled processor lookup
    #[napi(js_name = "disabledSet")]
    pub disabled_set: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsPostProcessorConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PostProcessorConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "postProcessorConfigDefault")]
pub fn post_processor_config_default() -> JsPostProcessorConfig {
    xberg::PostProcessorConfig::default().into()
}

/// Application properties from docProps/app.xml for PPTX
///
/// Contains PowerPoint-specific document metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PptxAppProperties")]
pub struct JsPptxAppProperties {
    /// Application name (e.g., "Microsoft Office PowerPoint")
    pub application: Option<String>,
    /// Application version
    #[napi(js_name = "appVersion")]
    pub app_version: Option<String>,
    /// Total editing time in minutes
    #[napi(js_name = "totalTime")]
    pub total_time: Option<i32>,
    /// Company name
    pub company: Option<String>,
    /// Document security level
    #[napi(js_name = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[napi(js_name = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[napi(js_name = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[napi(js_name = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[napi(js_name = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
    /// Number of slides
    pub slides: Option<i32>,
    /// Number of notes
    pub notes: Option<i32>,
    /// Number of hidden slides
    #[napi(js_name = "hiddenSlides")]
    pub hidden_slides: Option<i32>,
    /// Number of multimedia clips
    #[napi(js_name = "multimediaClips")]
    pub multimedia_clips: Option<i32>,
    /// Presentation format (e.g., "Widescreen", "Standard")
    #[napi(js_name = "presentationFormat")]
    pub presentation_format: Option<String>,
    /// Slide titles
    #[napi(js_name = "slideTitles")]
    pub slide_titles: Option<Vec<String>>,
}

/// PowerPoint (PPTX) extraction result.
///
/// Contains extracted slide content, metadata, and embedded images/tables.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PptxExtractionResult")]
pub struct JsPptxExtractionResult {
    /// Extracted text content from all slides
    pub content: String,
    /// Presentation metadata
    pub metadata: JsPptxMetadata,
    /// Total number of slides
    #[napi(js_name = "slideCount")]
    pub slide_count: i64,
    /// Total number of embedded images
    #[napi(js_name = "imageCount")]
    pub image_count: i64,
    /// Total number of tables
    #[napi(js_name = "tableCount")]
    pub table_count: i64,
    /// Extracted images from the presentation
    pub images: Vec<JsExtractedImage>,
    /// Slide structure with boundaries (when page tracking is enabled)
    #[napi(js_name = "pageStructure")]
    pub page_structure: Option<JsPageStructure>,
    /// Per-slide content (when page tracking is enabled)
    #[napi(js_name = "pageContents")]
    pub page_contents: Option<Vec<JsPageContent>>,
    /// Structured document representation
    pub document: Option<JsDocumentStructure>,
    /// Hyperlinks discovered in slides.
    pub hyperlinks: Vec<JsPresentationHyperlink>,
    /// Office metadata extracted from docProps/core.xml and docProps/app.xml.
    ///
    /// Contains keys like "title", "author", "created_by", "subject", "keywords",
    /// "modified_by", "created_at", "modified_at", etc.
    #[napi(js_name = "officeMetadata")]
    pub office_metadata: HashMap<String, String>,
    /// Slide comments as revisions.
    ///
    /// Each `<p:cm>` element in `ppt/comments/comment{N}.xml` becomes a
    /// `DocumentRevision { kind: Comment }` with author (resolved from
    /// `ppt/commentAuthors.xml`), ISO-8601 timestamp, and
    /// `RevisionAnchor.Slide { index }`. `None` when no comment XML parts exist.
    pub revisions: Option<Vec<JsDocumentRevision>>,
}
impl<'de> serde::Deserialize<'de> for JsPptxExtractionResult {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::PptxExtractionResult as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// PowerPoint presentation metadata.
///
/// Extracted from PPTX files containing slide counts and presentation details.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PptxMetadata")]
pub struct JsPptxMetadata {
    /// Total number of slides in the presentation
    #[napi(js_name = "slideCount")]
    pub slide_count: Option<u32>,
    /// Names of slides (if available)
    #[napi(js_name = "slideNames")]
    pub slide_names: Option<Vec<String>>,
    /// Number of embedded images
    #[napi(js_name = "imageCount")]
    pub image_count: Option<u32>,
    /// Number of tables
    #[napi(js_name = "tableCount")]
    pub table_count: Option<u32>,
}

/// HTML preprocessing options for document cleanup before conversion.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "PreprocessingOptions")]
pub struct JsPreprocessingOptions {
    /// Enable HTML preprocessing globally
    pub enabled: Option<bool>,
    /// Preprocessing preset level (Minimal, Standard, Aggressive)
    #[napi(ts_type = "PreprocessingPreset | 'Minimal' | 'Standard' | 'Aggressive'")]
    pub preset: Option<JsPreprocessingPreset>,
    /// Remove navigation elements (nav, breadcrumbs, menus, sidebars)
    #[napi(js_name = "removeNavigation")]
    pub remove_navigation: Option<bool>,
    /// Remove form elements (forms, inputs, buttons, etc.)
    #[napi(js_name = "removeForms")]
    pub remove_forms: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsPreprocessingOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <html_to_markdown_rs::PreprocessingOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A hyperlink discovered in a presentation slide.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PresentationHyperlink")]
pub struct JsPresentationHyperlink {
    /// Link destination.
    pub url: Option<String>,
    /// Optional visible label.
    pub label: Option<String>,
}

/// A curated structured-extraction preset loaded from the embedded library.
///
/// Each preset is a JSON file under `src/presets/library/<id>/v1.json` that
/// validates against the meta-schema in `src/presets/preset.schema.json`.
///
/// Downstream catalog consumers can inject presets via
/// `extend_from_dir`. The embedded OSS library
/// ships only the `generic_document` toy preset.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Preset")]
pub struct JsPreset {
    /// Stable, URL-safe preset identifier (lowercase snake_case).
    pub id: String,
    /// Monotonic version string (e.g. `v1`).
    pub version: String,
    /// Human-readable schema name forwarded to the LLM as the response/tool name.
    #[napi(js_name = "schemaName")]
    pub schema_name: String,
    /// One-line preset description shown in the registry UI.
    pub description: String,
    /// Top-level category for grouping in the playground.
    #[napi(ts_type = "PresetCategory | 'finance' | 'identity' | 'legal' | 'logistics' | 'medical' | 'hr' | 'other'")]
    pub category: JsPresetCategory,
    /// Free-form tags used for search/filtering. May be empty.
    pub tags: Vec<String>,
    /// JSON Schema (Draft 2020-12) describing the structured output shape.
    pub schema: serde_json::Value,
    /// Instruction primer sent to the model.
    #[napi(js_name = "systemPrompt")]
    pub system_prompt: String,
    /// Optional mustache-style template merged with caller-supplied context.
    #[napi(js_name = "contextTemplate")]
    pub context_template: Option<String>,
    /// Strategy for merging per-batch outputs across paginated calls.
    #[napi(
        js_name = "mergeMode",
        ts_type = "MergeMode | 'object_merge' | 'array_concat' | 'object_first'"
    )]
    pub merge_mode: JsMergeMode,
    /// Default call mode suggested for this preset; heuristics may override.
    #[napi(
        js_name = "preferredCallMode",
        ts_type = "CallMode | 'text_only' | 'vision_only' | 'text_plus_vision'"
    )]
    pub preferred_call_mode: JsCallMode,
    /// When true, the prompt asks the model to wrap each field as
    /// `{value, page, bbox, confidence}` for downstream citation overlays.
    #[napi(js_name = "emitCitations")]
    pub emit_citations: bool,
    /// Optional bundled sample (input file + reference output) for preview.
    pub sample: Option<JsPresetSample>,
    /// Stable sha256 fingerprint of the canonical preset file contents.
    ///
    /// Populated at registry load — not present in the on-disk JSON files.
    /// Used as a cache-invalidation token by the worker pipeline.
    pub fingerprint: String,
}
impl<'de> serde::Deserialize<'de> for JsPreset {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Preset as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Pointer to a sample input + its reference output bundled with the preset.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PresetSample")]
pub struct JsPresetSample {
    /// Path to the sample input file, relative to the preset directory.
    #[napi(js_name = "inputPath")]
    pub input_path: String,
    /// Path to the reference structured output, relative to the preset directory.
    #[napi(js_name = "outputPath")]
    pub output_path: String,
}

/// Lightweight projection of `Preset` used by the registry list endpoint
/// (omits the full schema and prompt to keep the payload small).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PresetSummary")]
pub struct JsPresetSummary {
    /// Preset identifier matching `Preset.id`.
    pub id: String,
    /// Preset version matching `Preset.version`.
    pub version: String,
    /// Schema name matching `Preset.schema_name`.
    #[napi(js_name = "schemaName")]
    pub schema_name: String,
    /// One-line preset description.
    pub description: String,
    /// Top-level category.
    #[napi(ts_type = "PresetCategory | 'finance' | 'identity' | 'legal' | 'logistics' | 'medical' | 'hr' | 'other'")]
    pub category: JsPresetCategory,
    /// Free-form tags.
    pub tags: Vec<String>,
    /// Default call mode.
    #[napi(
        js_name = "preferredCallMode",
        ts_type = "CallMode | 'text_only' | 'vision_only' | 'text_plus_vision'"
    )]
    pub preferred_call_mode: JsCallMode,
    /// Whether the preset prompts the model for citations.
    #[napi(js_name = "emitCitations")]
    pub emit_citations: bool,
    /// Stable fingerprint matching `Preset.fingerprint`.
    pub fingerprint: String,
}

/// A non-fatal warning from a processing pipeline stage.
///
/// Captures errors from optional features that don't prevent extraction
/// but may indicate degraded or incomplete results. Inspect these independently
/// from `ExtractedDocument.quality_score`, which assesses retained text only.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ProcessingWarning")]
pub struct JsProcessingWarning {
    /// The pipeline stage or feature that produced this warning
    /// (e.g., "embedding", "chunking", "language_detection", "output_format").
    pub source: String,
    /// Human-readable description of what went wrong.
    pub message: String,
}

/// A single run-level or style-level property change.
///
/// Used for revisions that change formatting rather than text content. `from`
/// and `to` store normalized property values when the source format exposes
/// them; either side may be absent when the format only records one side of the
/// change.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PropertyChange")]
pub struct JsPropertyChange {
    /// Property name, such as `"bold"`, `"italic"`, `"font_size"`, or `"font_color"`.
    pub name: String,
    /// Value before the change, when available.
    pub from: Option<String>,
    /// Value after the change, when available.
    pub to: Option<String>,
}

/// Proxy configuration for HTTP requests.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ProxyConfig")]
pub struct JsProxyConfig {
    /// Proxy URL (e.g. "http://proxy:8080", "socks5://proxy:1080").
    pub url: Option<String>,
    /// Optional username for proxy authentication.
    pub username: Option<String>,
    /// Optional password for proxy authentication.
    pub password: Option<String>,
}

/// Outlook PST archive metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PstMetadata")]
pub struct JsPstMetadata {
    /// Total number of email messages found in the PST archive.
    #[napi(js_name = "messageCount")]
    pub message_count: Option<i64>,
}

/// Pixel-space bounding box of a QR code inside its source image.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "QrBoundingBox")]
pub struct JsQrBoundingBox {
    /// Horizontal pixel offset of the bounding box top-left corner.
    pub x: u32,
    /// Vertical pixel offset of the bounding box top-left corner.
    pub y: u32,
    /// Width of the bounding box in pixels.
    pub width: u32,
    /// Height of the bounding box in pixels.
    pub height: u32,
}

/// One QR code decoded from an extracted image.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "QrCode")]
pub struct JsQrCode {
    /// Decoded payload (text, URL, vCard string, …).
    pub payload: String,
    /// Detector-reported confidence in `[0.0, 1.0]`. `None` when the decoder
    /// does not expose confidence (the default `rqrr` backend always reports
    /// `Some` because successful decode implies high confidence).
    pub confidence: Option<f64>,
    /// Bounding box of the QR code inside the source image, in pixel coordinates
    /// (`x`, `y` of the top-left corner; `width`, `height` of the rectangle).
    /// `None` if the decoder did not report a bounding box.
    pub bbox: Option<JsQrBoundingBox>,
}

/// RAKE-specific parameters.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RakeParams")]
pub struct JsRakeParams {
    /// Minimum word length to consider (default: 1).
    #[napi(js_name = "minWordLength")]
    pub min_word_length: Option<i64>,
    /// Maximum words in a keyword phrase (default: 3).
    #[napi(js_name = "maxWordsPerPhrase")]
    pub max_words_per_phrase: Option<i64>,
}

#[cfg(feature = "keywords-rake")]
#[napi(js_name = "rakeParamsDefault")]
pub fn rake_params_default() -> JsRakeParams {
    xberg::RakeParams::default().into()
}

/// Pre-computed table markdown for a table detection region.
///
/// Produced by the TATR-based table structure recognizer and surfaced as part of
/// layout-aware OCR results.  The struct lives here (under `layout-types`, pure-Rust)
/// so that consumers who do not enable `layout-detection` (ORT) can still reference
/// the type in their own code.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RecognizedTable")]
pub struct JsRecognizedTable {
    /// Detection bbox that this table corresponds to (for matching).
    #[napi(js_name = "detectionBbox")]
    pub detection_bbox: JsBBox,
    /// Table cells as a 2D vector (rows × columns).
    pub cells: Vec<Vec<String>>,
    /// Rendered markdown table.
    pub markdown: String,
}

/// Configuration for the redaction post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "RedactionConfig")]
pub struct JsRedactionConfig {
    /// Categories to redact. Empty means "every category supported by the engine."
    pub categories: Option<Vec<JsPiiCategory>>,
    /// Strategy applied to every match.
    #[napi(ts_type = "RedactionStrategy | 'mask' | 'hash' | 'token_replace' | 'drop'")]
    pub strategy: Option<JsRedactionStrategy>,
    /// Optional NER backend — required to redact PERSON / ORGANIZATION / LOCATION
    /// categories (the pure-Rust pattern engine only covers regex-detectable PII).
    pub ner: Option<JsNerConfig>,
    /// When `true`, chunk byte ranges are kept consistent with the rewritten content by
    /// adjusting `byte_start` / `byte_end` after replacement. When `false`, chunk byte
    /// ranges still refer to the *original* content offsets — useful when downstream
    /// consumers want to map findings back to the original document.
    #[napi(js_name = "preserveOffsets")]
    pub preserve_offsets: Option<bool>,
    /// Arbitrary user-supplied literal terms to redact.
    ///
    /// Each term is treated as a regex hit against the document, surfacing as
    /// `PiiCategory.Custom(label)` in `RedactionFinding`
    /// where `label` is the per-term label (defaulting to the literal value itself).
    /// Case-insensitive by default; set `RedactionTerm.case_sensitive` for exact match.
    ///
    /// Use this when you need to redact tenant-specific tokens (employee IDs,
    /// project codes, internal product names) without writing a custom plugin.
    #[napi(js_name = "customTerms")]
    pub custom_terms: Option<Vec<JsRedactionTerm>>,
    /// Arbitrary user-supplied regex patterns to redact.
    ///
    /// Same surfacing semantics as `custom_terms`: each
    /// hit becomes a `PiiCategory.Custom(label)` finding. Patterns are validated
    /// at config-construction time via `RedactionConfig.validate`.
    #[napi(js_name = "customPatterns")]
    pub custom_patterns: Option<Vec<JsRedactionPattern>>,
}
impl<'de> serde::Deserialize<'de> for JsRedactionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::RedactionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "redactionConfigDefault")]
pub fn redaction_config_default() -> JsRedactionConfig {
    xberg::RedactionConfig::default().into()
}

/// One redaction event: which span was rewritten, why, and with what.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RedactionFinding")]
pub struct JsRedactionFinding {
    /// Byte-offset start in the original (pre-redaction) `ExtractedDocument.content`.
    pub start: u32,
    /// Byte-offset end (exclusive) in the original `ExtractedDocument.content`.
    pub end: u32,
    /// PII category that fired this redaction.
    pub category: JsPiiCategory,
    /// Strategy applied to this finding (mask, hash, token-replace, drop).
    #[napi(ts_type = "RedactionStrategy | 'mask' | 'hash' | 'token_replace' | 'drop'")]
    pub strategy: JsRedactionStrategy,
    /// String that replaced the original mention. Always present; for `Drop` the
    /// replacement is the empty string.
    #[napi(js_name = "replacementToken")]
    pub replacement_token: String,
}

/// One user-supplied regex pattern to redact.
///
/// The pattern is compiled with the Rust `regex` crate (no look-around). Case
/// sensitivity is encoded in the pattern via the `(?i)` inline flag when
/// `Self.case_sensitive` is `false`.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "RedactionPattern")]
pub struct JsRedactionPattern {
    /// Custom category label surfaced in `RedactionFinding.category`.
    pub label: String,
    /// Regex pattern (Rust `regex` crate dialect — no look-around).
    pub pattern: String,
    /// When `true`, match case-sensitively; otherwise prepend `(?i)` to the regex.
    #[napi(js_name = "caseSensitive")]
    pub case_sensitive: bool,
}
impl<'de> serde::Deserialize<'de> for JsRedactionPattern {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::RedactionPattern as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Build a pattern with the given label (case-insensitive by default).
#[napi(js_name = "redactionPatternLabeled")]
pub fn redaction_pattern_labeled(label: String, pattern: String) -> JsRedactionPattern {
    xberg::RedactionPattern::labeled(label, pattern).into()
}

/// Audit report describing what the redaction processor found and how it replaced it.
///
/// The redactor returns this alongside the rewritten content so compliance, replay, and
/// audit-log consumers can see exactly what fired. Offsets are relative to the *original*
/// pre-redaction `content` and are intended for audit reconstruction only — the original
/// bytes are dropped at the end of the pipeline.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RedactionReport")]
pub struct JsRedactionReport {
    /// Individual redaction findings in original-source byte order.
    pub findings: Vec<JsRedactionFinding>,
    /// Total number of redactions applied across the document.
    #[napi(js_name = "totalRedacted")]
    pub total_redacted: u32,
}

/// One user-supplied literal term to redact.
///
/// Matched as a regex-escaped substring (so callers do not need to escape
/// metacharacters themselves). Case-insensitive by default — set
/// `Self.case_sensitive` to `true` for exact byte-match semantics.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "RedactionTerm")]
pub struct JsRedactionTerm {
    /// Custom category label surfaced in `RedactionFinding.category`.
    pub label: String,
    /// Literal value to match. Regex metacharacters are escaped automatically.
    pub value: String,
    /// When `true`, match the value as-is; otherwise match ASCII-case-insensitively.
    #[napi(js_name = "caseSensitive")]
    pub case_sensitive: bool,
}
impl<'de> serde::Deserialize<'de> for JsRedactionTerm {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::RedactionTerm as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Build a term whose label is the literal value itself (case-insensitive).
#[napi(js_name = "redactionTermLiteral")]
pub fn redaction_term_literal(value: String) -> JsRedactionTerm {
    xberg::RedactionTerm::literal(value).into()
}
/// Build a term with a custom label.
#[napi(js_name = "redactionTermLabeled")]
pub fn redaction_term_labeled(label: String, value: String) -> JsRedactionTerm {
    xberg::RedactionTerm::labeled(label, value).into()
}

/// Sorted map of preset id → `Preset`.
#[derive(Clone)]
#[napi(js_name = "Registry")]
pub struct JsRegistry {
    inner: Arc<std::sync::Mutex<xberg::Registry>>,
}

#[napi]
impl JsRegistry {
    /// Look up a preset by its identifier.
    #[napi]
    pub fn get(&self, id: String) -> Option<JsPreset> {
        self.inner.lock().unwrap().get(&id).map(|v| v.clone().into())
    }

    /// Materialize a `PresetSummary` list for the public registry endpoint.
    #[napi]
    pub fn summaries(&self) -> Vec<JsPresetSummary> {
        self.inner
            .lock()
            .unwrap()
            .summaries()
            .into_iter()
            .map(Into::into)
            .collect()
    }

    /// Number of presets currently loaded.
    #[napi]
    pub fn len(&self) -> i64 {
        self.inner.lock().unwrap().len() as i64
    }

    /// Whether the registry contains zero presets.
    #[napi(js_name = "isEmpty")]
    pub fn is_empty(&self) -> bool {
        self.inner.lock().unwrap().is_empty()
    }

    /// Read raw sample bytes for `<preset_id>` from
    /// `library/<id>/samples/<name>`. Returns `None` when the file is absent.
    #[napi(js_name = "sampleBytes")]
    pub fn sample_bytes(&self, preset_id: String, name: String) -> Option<napi::bindgen_prelude::Buffer> {
        self.inner
            .lock()
            .unwrap()
            .sample_bytes(&preset_id, &name)
            .map(Into::into)
    }

    /// Load additional preset files from a runtime directory and insert them
    /// into this registry.
    ///
    /// Reads every `*.json` file directly under `dir` (non-recursive),
    /// validates each against the meta-schema, and inserts it. Files that fail
    /// validation are rejected — the error is returned immediately and the
    /// registry is left in a partially-updated state. Existing entries with the
    /// same id are overwritten.
    ///
    /// Returns the number of presets successfully loaded from `dir`.
    ///
    /// # Use case
    ///
    /// This is the injection point for downstream catalogs that add curated
    /// presets on top of the single embedded OSS preset.
    #[allow(clippy::missing_errors_doc)]
    #[napi(js_name = "extendFromDir")]
    pub fn extend_from_dir(&self, dir: String) -> Result<i64> {
        let result = self
            .inner
            .lock()
            .unwrap()
            .extend_from_dir(std::path::Path::new(&dir))
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
        Ok(result as i64)
    }

    /// Build the registry from preset files embedded at compile time under
    /// `src/presets/library/`. Validates every file against the meta-schema.
    #[allow(clippy::missing_errors_doc)]
    #[napi(js_name = "loadEmbedded")]
    pub fn load_embedded() -> Result<JsRegistry> {
        xberg::Registry::load_embedded()
            .map(|val| Self {
                inner: Arc::new(std::sync::Mutex::new(val)),
            })
            .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
    }

    /// Return the global registry, loading it on first access.
    ///
    /// # Panics
    ///
    /// Panics if any embedded preset is malformed. The build-time validation
    /// test ensures this cannot happen for the embedded presets; a panic here
    /// indicates a build artifact problem, not a runtime error.
    #[napi]
    pub fn global() -> JsRegistry {
        Self {
            inner: Arc::new(std::sync::Mutex::new(xberg::Registry::global().clone())),
        }
    }
}

/// A single document returned by the reranker, with its position in the input and score.
///
/// `index` maps back to the caller's original document list, so metadata arrays
/// (e.g. IDs, paths) can be reordered without passing them through the reranker.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RerankedDocument")]
pub struct JsRerankedDocument {
    /// Position of this document in the original input `documents` slice.
    pub index: i64,
    /// Relevance score in `[0, 1]`. Higher means more relevant to the query.
    pub score: f64,
    /// The document text.
    pub document: String,
}

/// Configuration for the reranking pipeline.
///
/// Controls which model to use, how many results to return, and download/cache
/// behavior for local ONNX models.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "RerankerConfig")]
pub struct JsRerankerConfig {
    /// The reranker model to use (defaults to "balanced" preset if not specified).
    pub model: Option<JsRerankerModelType>,
    /// Return at most this many documents. `None` returns all.
    ///
    /// Applied after sorting by score, so the highest-scoring documents are kept.
    #[napi(js_name = "topK")]
    pub top_k: Option<i64>,
    /// Batch size for local ONNX cross-encoder inference.
    #[napi(js_name = "batchSize")]
    pub batch_size: Option<i64>,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `RerankerModelType.Llm` and `RerankerModelType.Plugin`, which download no model.
    #[napi(js_name = "showDownloadProgress")]
    pub show_download_progress: Option<bool>,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the reranker ONNX model.
    ///
    /// Controls which execution provider (CPU, CUDA, CoreML, TensorRT) is used for
    /// local inference. Defaults to `None` (auto-select per platform).
    pub acceleration: Option<JsAccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single `rerank()` call when
    /// using `RerankerModelType.Plugin`.
    ///
    /// Applies only to the in-process plugin path — protects against hung
    /// host-language backends. On timeout, the dispatcher returns
    /// `Plugin` instead of blocking forever.
    ///
    /// `None` disables the timeout. The default (60 seconds) is conservative
    /// for common in-process inference; increase for large document sets on slow
    /// hardware.
    #[napi(js_name = "maxRerankDurationSecs")]
    pub max_rerank_duration_secs: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsRerankerConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::RerankerConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "rerankerConfigDefault")]
pub fn reranker_config_default() -> JsRerankerConfig {
    xberg::RerankerConfig::default().into()
}

/// A preset merged with caller-supplied overrides (custom schema, prompt suffix,
/// context map). Output is what the pipeline orchestrator consumes.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ResolvedPreset")]
pub struct JsResolvedPreset {
    /// Source preset identifier.
    pub id: String,
    /// Source preset version.
    pub version: String,
    /// Fingerprint of the source preset file, used as a cache token.
    pub fingerprint: String,
    /// Schema name forwarded to the LLM.
    #[napi(js_name = "schemaName")]
    pub schema_name: String,
    /// Effective JSON Schema (caller override or the preset's own).
    pub schema: serde_json::Value,
    /// System prompt with rendered context appended.
    #[napi(js_name = "systemPrompt")]
    pub system_prompt: String,
    /// Merge strategy for paginated outputs.
    #[napi(
        js_name = "mergeMode",
        ts_type = "MergeMode | 'object_merge' | 'array_concat' | 'object_first'"
    )]
    pub merge_mode: JsMergeMode,
    /// Preferred call mode.
    #[napi(
        js_name = "preferredCallMode",
        ts_type = "CallMode | 'text_only' | 'vision_only' | 'text_plus_vision'"
    )]
    pub preferred_call_mode: JsCallMode,
    /// Whether the prompt asks for per-field citations.
    #[napi(js_name = "emitCitations")]
    pub emit_citations: bool,
}

/// The content changes that make up a single revision.
///
/// For insertions and deletions the `content` field carries the added/removed
/// lines as `DiffLine.Added` / `DiffLine.Removed` entries. For format
/// changes, `property_changes` carries normalized before/after formatting
/// values when the source document exposes them.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "RevisionDelta")]
pub struct JsRevisionDelta {
    /// Line-level content changes for this revision.
    pub content: Option<Vec<JsDiffLine>>,
    /// Cell-level table changes for this revision.
    #[napi(js_name = "tableChanges")]
    pub table_changes: Option<Vec<JsCellChange>>,
    /// Formatting or metadata property changes for this revision.
    #[napi(js_name = "propertyChanges")]
    pub property_changes: Option<Vec<JsPropertyChange>>,
}
impl<'de> serde::Deserialize<'de> for JsRevisionDelta {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::RevisionDelta as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for security limits across extractors.
///
/// All limits are intentionally conservative to prevent DoS attacks
/// while still supporting legitimate documents.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "SecurityLimits")]
pub struct JsSecurityLimits {
    /// Maximum uncompressed size for archives (500 MB)
    #[napi(js_name = "maxArchiveSize")]
    pub max_archive_size: Option<i64>,
    /// Maximum compression ratio before flagging as potential bomb (100:1)
    #[napi(js_name = "maxCompressionRatio")]
    pub max_compression_ratio: Option<i64>,
    /// Maximum number of files in archive (10,000)
    #[napi(js_name = "maxFilesInArchive")]
    pub max_files_in_archive: Option<i64>,
    /// Maximum nesting depth for structures (1024)
    #[napi(js_name = "maxNestingDepth")]
    pub max_nesting_depth: Option<i64>,
    /// Maximum length of any single XML entity / attribute / token (1 MiB).
    /// This is a per-token cap, NOT a total cap — billion-laughs class
    /// attacks where a single entity expands to hundreds of MB are caught
    /// here, while normal long text content (a paragraph, a CDATA block) is
    /// caught by `max_content_size` instead.
    #[napi(js_name = "maxEntityLength")]
    pub max_entity_length: Option<i64>,
    /// Maximum string growth and decoded image allocation per document (100 MB)
    #[napi(js_name = "maxContentSize")]
    pub max_content_size: Option<i64>,
    /// Maximum iterations per operation
    #[napi(js_name = "maxIterations")]
    pub max_iterations: Option<i64>,
    /// Maximum XML depth (1024 levels)
    #[napi(js_name = "maxXmlDepth")]
    pub max_xml_depth: Option<i64>,
    /// Maximum aggregate table cells per document (100,000).
    ///
    /// Raise this for trusted large tabular inputs. Higher values permit
    /// proportionally more parsing work and output allocation.
    #[napi(js_name = "maxTableCells")]
    pub max_table_cells: Option<i64>,
    /// Maximum number of pages (or slides, or frames) in a single document.
    /// `None` means unlimited.
    ///
    /// Checked once the count is known and before any per-page work (OCR, layout
    /// detection, rendering) starts. Byte-size limits do not bound page count: a
    /// scanned page can compress to a few kilobytes, so a document well under
    /// `max_content_size` or `max_archive_size` can still hold thousands of pages
    /// of per-page work. Defaults to `None` (unlimited) because a real ceiling
    /// here is workload-specific and a low default would silently reject
    /// legitimate large documents; callers that want a ceiling set this
    /// explicitly.
    ///
    /// Enforced for: PDF (`extractors.pdf`, page count via `xberg_native_pdf`/`lopdf`),
    /// PPTX (`extraction.pptx`, slide count from the archive's slide parts),
    /// Keynote (`extractors.iwork.keynote`, slide count from `Index/Slide-*.iwa`
    /// entry names), ODP (`extractors.odp`, `draw:page` count in `content.xml`),
    /// and multi-frame TIFF images built with the `ocr` feature
    /// (`extractors.image`, frame count via the `tiff` crate). Not enforced for
    /// any other format, including DOCX, ODT, XLSX, legacy PPT/DOC, Pages/Numbers,
    /// and TIFF images when the `ocr` feature is disabled: those formats either
    /// have no fixed "page" the crate can count without doing the expensive work
    /// itself (DOCX/ODT page count is a layout outcome, not a stored value), or
    /// have no per-page pipeline to gate at all. Setting `max_pages` on a
    /// document of an unenforced format is silently a no-op, not a guarantee.
    #[napi(js_name = "maxPages")]
    pub max_pages: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsSecurityLimits {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::SecurityLimits as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "securityLimitsDefault")]
pub fn security_limits_default() -> JsSecurityLimits {
    xberg::SecurityLimits::default().into()
}

/// API server configuration.
///
/// This struct holds all configuration options for the Xberg API server,
/// including host/port settings, CORS configuration, and upload limits.
///
/// # Defaults
///
/// - `host`: "127.0.0.1" (localhost only)
/// - `port`: 8000
/// - `cors_origins`: empty vector (allows all origins)
/// - `max_request_body_bytes`: 104_857_600 (100 MB)
/// - `max_multipart_field_bytes`: 104_857_600 (100 MB)
/// - `job_timeout_secs`: 600 (10 minutes)
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "ServerConfig")]
pub struct JsServerConfig {
    /// Server host address (e.g., "127.0.0.1", "0.0.0.0")
    pub host: Option<String>,
    /// Server port number
    pub port: Option<u16>,
    /// CORS allowed origins. Empty vector means allow all origins.
    ///
    /// If this is an empty vector, the server will accept requests from any origin.
    /// If populated with specific origins (e.g., `"https://example.com"`), only
    /// those origins will be allowed.
    #[napi(js_name = "corsOrigins")]
    pub cors_origins: Option<Vec<String>>,
    /// Maximum size of request body in bytes (default: 100 MB)
    #[napi(js_name = "maxRequestBodyBytes")]
    pub max_request_body_bytes: Option<i64>,
    /// Maximum size of multipart fields in bytes (default: 100 MB)
    #[napi(js_name = "maxMultipartFieldBytes")]
    pub max_multipart_field_bytes: Option<i64>,
    /// Fallback timeout, in seconds, for `POST /extract-async` jobs whose request does not
    /// pin down `extraction_timeout_secs` (default: 600, 10 minutes).
    ///
    /// A per-request `extraction_timeout_secs: Some(n)` always overrides this value. An
    /// explicit `extraction_timeout_secs: null` deliberately does NOT mean "run unbounded" —
    /// it still falls back to this server-configured cap, because an unbounded job on a
    /// shared server is a denial-of-service risk.
    #[napi(js_name = "jobTimeoutSecs")]
    pub job_timeout_secs: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsServerConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::ServerConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "serverConfigDefault")]
pub fn server_config_default() -> JsServerConfig {
    xberg::ServerConfig::default().into()
}

/// A URL entry from a sitemap.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "SitemapUrl")]
pub struct JsSitemapUrl {
    /// The URL.
    pub url: Option<String>,
    /// The last modification date, if present.
    pub lastmod: Option<String>,
    /// The change frequency, if present.
    pub changefreq: Option<String>,
    /// The priority, if present.
    pub priority: Option<String>,
}

/// A sparse learned embedding: vocabulary term indices and their weights.
///
/// `indices` are ascending vocabulary token ids; `values[i]` is the weight for
/// `indices[i]`. The two arrays always have equal length. Only strictly-positive
/// terms are retained, so the representation is genuinely sparse.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "SparseEmbedding")]
pub struct JsSparseEmbedding {
    /// Vocabulary token ids with non-zero weight, ascending.
    pub indices: Vec<u32>,
    /// Weights parallel to `SparseEmbedding.indices`.
    pub values: Vec<f64>,
}

/// Configuration for the sparse-embedding pipeline.
///
/// Controls which model to use, batching, and download/cache behavior for the
/// local ONNX SPLADE model.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "SparseEmbeddingConfig")]
pub struct JsSparseEmbeddingConfig {
    /// The sparse-embedding model to use (defaults to the "opensearch-v3-distill" preset).
    pub model: Option<JsSparseEmbeddingModelType>,
    /// Batch size for local ONNX inference.
    ///
    /// SPLADE emits a `[seq, vocab]` logit tensor per document, so memory scales
    /// with batch size — keep this modest.
    #[napi(js_name = "batchSize")]
    pub batch_size: Option<i64>,
    /// Maximum token sequence length for the tokenizer.
    #[napi(js_name = "maxLength")]
    pub max_length: Option<i64>,
    /// Show model download progress (local ONNX path only).
    ///
    /// When enabled, transfer progress for the model, tokenizer and config files is reported at
    /// `info` level on the `xberg.model_download` target while they download (#279). A warm
    /// Hugging Face cache transfers nothing and so reports nothing. Ignored by
    /// `SparseEmbeddingModelType.Plugin`, which downloads no model.
    #[napi(js_name = "showDownloadProgress")]
    pub show_download_progress: Option<bool>,
    /// Optional alternate Hugging Face cache root for model files.
    ///
    /// When unset, hf-hub follows the standard Hugging Face environment and
    /// platform cache conventions.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Hardware acceleration for the sparse-embedding ONNX model.
    pub acceleration: Option<JsAccelerationConfig>,
    /// Maximum wall-clock duration (in seconds) for a single embed call when
    /// using `SparseEmbeddingModelType.Plugin`. `None` disables the timeout.
    #[napi(js_name = "maxEmbedDurationSecs")]
    pub max_embed_duration_secs: Option<i64>,
}
impl<'de> serde::Deserialize<'de> for JsSparseEmbeddingConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::SparseEmbeddingConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "sparseEmbeddingConfigDefault")]
pub fn sparse_embedding_config_default() -> JsSparseEmbeddingConfig {
    xberg::SparseEmbeddingConfig::default().into()
}

/// Static metadata for a bundled SPLADE preset (WASM/Android-safe, no ORT).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "SparseEmbeddingPreset")]
pub struct JsSparseEmbeddingPreset {
    /// Stable preset name referenced from config.
    pub name: String,
    /// HuggingFace repository hosting the ONNX model.
    #[napi(js_name = "modelRepo")]
    pub model_repo: String,
    /// Path to the ONNX file within the repo.
    #[napi(js_name = "modelFile")]
    pub model_file: String,
    /// Sibling files that must be downloaded alongside `model_file`.
    #[napi(js_name = "additionalFiles")]
    pub additional_files: Vec<String>,
    /// Maximum token sequence length.
    #[napi(js_name = "maxLength")]
    pub max_length: i64,
    /// Human-readable description.
    pub description: String,
}

/// SSRF policy configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "SsrfPolicy")]
pub struct JsSsrfPolicy {
    /// If true, reject URLs that resolve to private/metadata IP ranges.
    #[napi(js_name = "denyPrivate")]
    pub deny_private: Option<bool>,
    /// Hostnames and IP ranges permitted regardless of `deny_private`.
    ///
    /// The allowlist is an *override* of `deny_private`, not an intersection with it.
    /// Precedence, in order:
    ///
    /// 1. `deny_private == false` permits everything; the allowlist is not consulted.
    /// 2. A hostname matching an `Exact` or `Suffix` entry is permitted immediately,
    ///    *before* DNS resolution — so the deny-list is never applied to it. This trusts
    ///    the host string: a name that resolves into private space is still permitted.
    /// 3. A literal or resolved IP inside a `Cidr` entry is permitted even though it is
    ///    in the default deny-list.
    /// 4. Otherwise the default deny-list decides.
    ///
    /// An empty allowlist therefore denies nothing by itself — it simply leaves
    /// `deny_private` and the deny-list in sole control.
    pub allowlist: Option<Vec<JsHostMatcher>>,
    /// Maximum number of HTTP redirects to follow during validation.
    #[napi(js_name = "maxRedirects")]
    pub max_redirects: Option<u8>,
    /// Allowed URI schemes. Default: `["http", "https"]`.
    ///
    /// Only `http` and `https` are supported. An empty list denies every URL.
    #[napi(js_name = "schemeAllowlist")]
    pub scheme_allowlist: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsSsrfPolicy {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <crawlberg::SsrfPolicy as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Structured data (Schema.org, microdata, RDFa) block.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "StructuredData")]
pub struct JsStructuredData {
    /// Type of structured data
    #[napi(
        js_name = "dataType",
        ts_type = "StructuredDataType | 'json-ld' | 'microdata' | 'rdfa'"
    )]
    pub data_type: JsStructuredDataType,
    /// Raw JSON string representation
    #[napi(js_name = "rawJson")]
    pub raw_json: String,
    /// Schema type if detectable (e.g., "Article", "Event", "Product")
    #[napi(js_name = "schemaType")]
    pub schema_type: Option<String>,
}

/// Result of parsing a structured data file (JSON, JSONL, YAML, or TOML).
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "StructuredDataResult")]
pub struct JsStructuredDataResult {
    /// The extracted text content, formatted for readability.
    pub content: String,
    /// The source format identifier (e.g. `"json"`, `"yaml"`, `"toml"`).
    pub format: String,
    /// Key-value metadata extracted from recognized text fields.
    pub metadata: HashMap<String, String>,
    /// JSON paths of fields that were classified as text-bearing.
    #[napi(js_name = "textFields")]
    pub text_fields: Vec<String>,
    /// The parsed document as a canonical `serde_json.Value` tree, when the
    /// source format could be represented as one. `None` only for TOML inputs
    /// whose `toml.Value` fails to round-trip through `serde_json.Value`
    /// (xberg-io/xberg#155): the extractor falls back to a raw code block in
    /// that case.
    pub value: Option<serde_json::Value>,
    /// Flattened `path: value` renderings for every leaf field, in traversal
    /// order. Previously computed and discarded (xberg-io/xberg#166); now
    /// surfaced so callers get a full-text view even when the structured
    /// renderer only emits headings/lists for a subset of fields.
    pub flattened: Vec<String>,
}

/// Configuration for LLM-based structured data extraction.
///
/// Sends extracted document content to a VLM with a JSON schema,
/// returning structured data that conforms to the schema.
///
/// # Example
///
/// ```toml
/// [structured_extraction]
/// schema_name = "invoice_data"
/// strict = true
///
/// [structured_extraction.schema]
/// type = "object"
/// properties.vendor = { type = "string" }
/// properties.total = { type = "number" }
/// required = ["vendor", "total"]
///
/// [structured_extraction.llm]
/// model = "openai/gpt-4o"
/// ```
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "StructuredExtractionConfig")]
pub struct JsStructuredExtractionConfig {
    /// JSON Schema defining the desired output structure.
    pub schema: serde_json::Value,
    /// Schema name passed to the LLM's structured output mode.
    #[napi(js_name = "schemaName")]
    pub schema_name: String,
    /// Optional schema description for the LLM.
    #[napi(js_name = "schemaDescription")]
    pub schema_description: Option<String>,
    /// Enable strict mode — output must exactly match the schema.
    pub strict: bool,
    /// Custom Jinja2 extraction prompt template. When `None`, a default template is used.
    ///
    /// Available template variables:
    /// - `{{ content }}` — The extracted document text.
    /// - `{{ schema }}` — The JSON schema as a formatted string.
    /// - `{{ schema_name }}` — The schema name.
    /// - `{{ schema_description }}` — The schema description (may be empty).
    pub prompt: Option<String>,
    /// LLM configuration for the extraction.
    pub llm: JsLlmConfig,
}
impl<'de> serde::Deserialize<'de> for JsStructuredExtractionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::StructuredExtractionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for the summarisation post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "SummarizationConfig")]
pub struct JsSummarizationConfig {
    /// Summarisation strategy.
    #[napi(ts_type = "SummaryStrategy | 'extractive' | 'abstractive'")]
    pub strategy: Option<JsSummaryStrategy>,
    /// Maximum summary length in tokens. `None` lets the backend pick a default.
    #[napi(js_name = "maxTokens")]
    pub max_tokens: Option<u32>,
    /// LLM configuration for the abstractive backend. Ignored when
    /// `strategy = Extractive`. Required when `strategy = Abstractive`.
    pub llm: Option<JsLlmConfig>,
}
impl<'de> serde::Deserialize<'de> for JsSummarizationConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::SummarizationConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// A supported document format entry.
///
/// Represents a file extension and its corresponding MIME type that Xberg can process.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "SupportedFormat")]
pub struct JsSupportedFormat {
    /// File extension (without leading dot), e.g., "pdf", "docx"
    pub extension: String,
    /// MIME type string, e.g., "application/pdf"
    #[napi(js_name = "mimeType")]
    pub mime_type: String,
}

/// SVG-specific configuration for the image-encode pipeline.
///
/// Applies when the source image is SVG or when the output format is set to
/// `ImageOutputFormat.Svg`.  Available when the `svg` feature is active.
///
/// Used via `ImageExtractionConfig.svg`.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "SvgOptions")]
pub struct JsSvgOptions {
    /// Run SVG bytes through `usvg` sanitization (strips external `href` attributes,
    /// JavaScript event handlers, and `foreignObject` elements) even when the
    /// output format is `Native`.  Defaults to `true`.
    pub sanitize: Option<bool>,
    /// Target DPI when rasterizing SVG to a pixel-based format (PNG, JPEG, WebP,
    /// HEIF).  The tree's viewBox is scaled by `render_dpi / 96.0` before the
    /// pixel buffer is allocated.  Defaults to `96.0` (1× CSS pixel density).
    #[napi(js_name = "renderDpi")]
    pub render_dpi: Option<f64>,
}
impl<'de> serde::Deserialize<'de> for JsSvgOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::core::config::extraction::SvgOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[cfg(feature = "svg")]
#[napi(js_name = "svgOptionsDefault")]
pub fn svg_options_default() -> JsSvgOptions {
    xberg::core::config::extraction::SvgOptions::default().into()
}

/// Extracted table structure.
///
/// Represents a table detected and extracted from a document (PDF, image, etc.).
/// Tables are converted to both structured cell data and Markdown format.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "Table")]
pub struct JsTable {
    /// Table cells as a 2D vector (rows × columns)
    pub cells: Option<Vec<Vec<String>>>,
    /// Markdown representation of the table
    pub markdown: Option<String>,
    /// Page number where the table was found (1-indexed)
    #[napi(js_name = "pageNumber")]
    pub page_number: Option<u32>,
    /// Bounding box of the table's position. Only populated when position data is
    /// available from the producing extractor.
    ///
    /// The coordinate space depends on how the table was produced, and callers must
    /// know which route produced a given `Table` before interpreting this field:
    ///
    /// - Tables extracted from a PDF's native content, and tables recognized on a
    ///   scanned PDF page that went through xberg's OCR pipeline (`--force-ocr` /
    ///   `--ocr-scanned-pages`), are in **PDF points with a bottom-left origin**
    ///   (x0=left, y0=bottom, x1=right, y1=top; y increases upward). For the OCR
    ///   case, the pipeline rescales the backend's raw pixel output into this space
    ///   before it reaches `Table.bounding_box` — see
    ///   `rescale_ocr_bboxes_to_page_points` in `extractors.pdf.ocr`.
    /// - Tables detected by OCR on a standalone image with no backing PDF page (for
    ///   example extracting a bare PNG/JPEG/TIFF) are in **raster pixel coordinates
    ///   with a top-left origin** (x0=left, y0=top, x1=right, y1=bottom; y increases
    ///   downward) — the same convention the OCR backend (Tesseract, PaddleOCR,
    ///   candle-based backends) or the layout detector reported them in. There is no
    ///   PDF page geometry to rescale into for this case, so the raw pixel box is
    ///   passed through unchanged.
    #[napi(js_name = "boundingBox")]
    pub bounding_box: Option<JsBoundingBox>,
    /// Stable identifier shared by every `tables[]` entry that represents a
    /// fragment of the same physical table.
    ///
    /// Assigned deterministically by the extraction pipeline (e.g. a
    /// sequential `"table-N"` in document order); never derived from
    /// randomness or wall-clock time, so the same input document always
    /// produces the same ids. Consumers can use it to reconcile the markdown
    /// blocks in `content` / `pages[].content` / `chunks[].content` with the
    /// structured entries in `tables[]`. `None` when the extractor did not
    /// assign one.
    ///
    /// Today, same-page fragments of one physical table are already merged
    /// into a single `tables[]` entry before ids are assigned (see PDF table
    /// stitching), so in practice `table_id` is unique per entry rather than
    /// shared across several. A table split across a page boundary is
    /// intentionally *not* linked — its per-page pieces get separate ids.
    /// Sharing one id across page-boundary fragments is a known possible
    /// future extension, not implemented yet.
    #[napi(js_name = "tableId")]
    pub table_id: Option<String>,
    /// Paragraph styles carried by individual cells, for the cells that have one.
    ///
    /// Sparse and flat on purpose. A DOCX banner row -- row 0, one cell spanning the grid,
    /// styled `Heading1`..`Heading6` -- is what Word's navigation pane and a `TOC` field treat
    /// as the document outline, but as a table cell it reached consumers as anonymous text
    /// (GH#1587). `cells` keeps the bare text: prefixing it with `#` would put a markdown
    /// heading inside a table cell, which is invalid where it lands and would change text every
    /// existing consumer already reads. This list is the signal instead.
    ///
    /// Entries are only emitted for cells that actually carry a style, so an ordinary table
    /// serialises exactly as it did before. Indices are into `cells`.
    #[napi(js_name = "cellStyles")]
    pub cell_styles: Option<Vec<JsTableCellStyle>>,
    /// Header cells for this fragment, i.e. the first row of `cells`.
    ///
    /// Populated even when this fragment's own header row was merged away or
    /// physically lives in a sibling fragment (see `table_id`), so a single
    /// fragment is interpretable in isolation. `None` when no header row
    /// could be determined.
    pub columns: Option<Vec<String>>,
}
impl<'de> serde::Deserialize<'de> for JsTable {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::Table as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Individual table cell with content and optional styling.
///
/// Future extension point for rich table support with cell-level metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TableCell")]
pub struct JsTableCell {
    /// Cell content as text
    pub content: Option<String>,
    /// Row span (number of rows this cell spans)
    #[napi(js_name = "rowSpan")]
    pub row_span: Option<u32>,
    /// Column span (number of columns this cell spans)
    #[napi(js_name = "colSpan")]
    pub col_span: Option<u32>,
    /// Whether this is a header cell
    #[napi(js_name = "isHeader")]
    pub is_header: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsTableCell {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TableCell as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// The paragraph style a single table cell's text carries, located by grid position.
///
/// Flat rather than a nested `Vec<Vec<Option<..>>>`: the nested shape marshals badly across the
/// FFI bindings, and the data is sparse anyway. See `Table.cell_styles`.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TableCellStyle")]
pub struct JsTableCellStyle {
    /// Zero-indexed row of the cell this style belongs to.
    pub row: Option<u32>,
    /// Zero-indexed column of the cell this style belongs to.
    pub col: Option<u32>,
    /// Outline level 1-6 when the style resolves to a heading, otherwise `None`.
    #[napi(js_name = "headingLevel")]
    pub heading_level: Option<u8>,
    /// Human-readable style name, e.g. `heading 2`.
    #[napi(js_name = "styleName")]
    pub style_name: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsTableCellStyle {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TableCellStyle as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Cell-level changes for a pair of tables that share the same index.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TableDiff")]
pub struct JsTableDiff {
    /// Zero-based index of the table in both `a.tables` and `b.tables`.
    #[napi(js_name = "fromIndex")]
    pub from_index: i64,
    /// Zero-based index in `b.tables` (equal to `from_index` for same-dimension tables).
    #[napi(js_name = "toIndex")]
    pub to_index: i64,
    /// Cell-level changes within the table.
    #[napi(js_name = "cellChanges")]
    pub cell_changes: Vec<JsCellChange>,
}

/// Structured table grid with cell-level metadata.
///
/// Stores row/column dimensions and a flat list of cells with position info.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TableGrid")]
pub struct JsTableGrid {
    /// Number of rows in the table.
    pub rows: Option<u32>,
    /// Number of columns in the table.
    pub cols: Option<u32>,
    /// All cells in row-major order.
    pub cells: Option<Vec<JsGridCell>>,
}

/// Tesseract OCR configuration.
///
/// Provides fine-grained control over Tesseract OCR engine parameters.
/// Most users can use the defaults, but these settings allow optimization
/// for specific document types (invoices, handwriting, etc.).
///
/// **This is the public-facing counterpart of `ocr.types.TesseractConfig`
/// (the internal, engine-facing representation with `u8`/`String` fields instead of
/// `i32`/`Vec<String>`).** They are two independent struct definitions bridged only by
/// an explicit `From<&TesseractConfig> for crate.ocr.types.TesseractConfig` impl in
/// `ocr/types.rs` — that conversion carries field *values* across, but each struct keeps
/// its own `Default` impl, and the conversion does nothing to keep those two defaults in
/// sync. Several production call sites (`extractors.image.apply_default_tesseract_psm`,
/// `configured_region_ocr`, `sparse_image_ocr_fallback_config`) construct *this* struct's
/// default and convert it, bypassing the internal struct's `Default` entirely — so if the
/// two defaults disagree, standalone image OCR silently uses this struct's value while
/// PDF-embedded OCR (which can reach the internal `Default` directly when no
/// `tesseract_config` is set) uses the other. When changing a default here, also update
/// `ocr.types.TesseractConfig.default`, and vice versa.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TesseractConfig")]
pub struct JsTesseractConfig {
    /// Language code(s) for OCR recognition. For Tesseract, languages are joined with "+".
    ///
    /// A list is the canonical form and the only form accepted by the binding
    /// object APIs (Python, Node, PHP, WASM, etc.): `["eng", "deu"]`. When
    /// deserializing from a config file, JSON body, or the REST/MCP API, a
    /// single string is also accepted, either as one code ("eng") or
    /// "+"-joined ("eng+deu").
    pub language: Option<Vec<String>>,
    /// Page Segmentation Mode (1-13).
    ///
    /// PSM 0 is rejected: Tesseract's `PSM_OSD_ONLY` performs orientation and script
    /// detection with no character recognition, so it cannot satisfy a text-extraction
    /// request and previously yielded an empty document (GH#1586).
    ///
    /// `None` (the default) means the caller made no explicit choice: the extraction
    /// pipeline applies its own context-appropriate PSM (whole-image PSM 11, vertical-
    /// language PSM 5, layout-region PSM 6, or the sparse-text retry's PSM 3) exactly as
    /// it would with no `TesseractConfig` at all — see issue #1573. Setting any other
    /// field on this struct no longer changes that behaviour.
    ///
    /// Common explicit values:
    /// - 3: Fully automatic page segmentation (native engine default)
    /// - 6: Assume a single uniform block of text (WASM engine default — avoids
    ///   layout-analysis hang)
    /// - 11: Sparse text with no particular order
    pub psm: Option<i32>,
    /// Output format ("text" or "markdown")
    #[napi(js_name = "outputFormat")]
    pub output_format: Option<String>,
    /// OCR Engine Mode (0-3).
    ///
    /// - 0: Legacy engine only
    /// - 1: Neural nets (LSTM) only (usually best)
    /// - 2: Legacy + LSTM
    /// - 3: Default (based on what's available)
    pub oem: Option<i32>,
    /// Minimum confidence threshold (0.0-100.0).
    ///
    /// Words with confidence below this threshold may be rejected or flagged.
    #[napi(js_name = "minConfidence")]
    pub min_confidence: Option<f64>,
    /// Image preprocessing configuration.
    ///
    /// Controls how images are preprocessed before OCR. Can significantly
    /// improve quality for scanned documents or low-quality images.
    pub preprocessing: Option<JsImagePreprocessingConfig>,
    /// Enable automatic table detection and reconstruction
    #[napi(js_name = "enableTableDetection")]
    pub enable_table_detection: Option<bool>,
    /// Minimum confidence threshold for table detection (0.0-1.0)
    #[napi(js_name = "tableMinConfidence")]
    pub table_min_confidence: Option<f64>,
    /// Column threshold for table detection (pixels)
    #[napi(js_name = "tableColumnThreshold")]
    pub table_column_threshold: Option<i32>,
    /// Row threshold ratio for table detection (0.0-1.0)
    #[napi(js_name = "tableRowThresholdRatio")]
    pub table_row_threshold_ratio: Option<f64>,
    /// Enable OCR result caching
    #[napi(js_name = "useCache")]
    pub use_cache: Option<bool>,
    /// Use pre-adapted templates for character classification
    #[napi(js_name = "classifyUsePreAdaptedTemplates")]
    pub classify_use_pre_adapted_templates: Option<bool>,
    /// Enable N-gram language model.
    ///
    /// Kept on by default (see `Self.default` and
    /// `ocr.types.TesseractConfig.language_model_ngram_on` for the rationale);
    /// keep this field's default in sync with the internal struct's.
    #[napi(js_name = "languageModelNgramOn")]
    pub language_model_ngram_on: Option<bool>,
    /// Don't reject good words during block-level processing
    #[napi(js_name = "tesseditDontBlkrejGoodWds")]
    pub tessedit_dont_blkrej_good_wds: Option<bool>,
    /// Don't reject good words during row-level processing
    #[napi(js_name = "tesseditDontRowrejGoodWds")]
    pub tessedit_dont_rowrej_good_wds: Option<bool>,
    /// Enable dictionary correction
    #[napi(js_name = "tesseditEnableDictCorrection")]
    pub tessedit_enable_dict_correction: Option<bool>,
    /// Whitelist of allowed characters (empty = all allowed)
    #[napi(js_name = "tesseditCharWhitelist")]
    pub tessedit_char_whitelist: Option<String>,
    /// Blacklist of forbidden characters (empty = none forbidden)
    #[napi(js_name = "tesseditCharBlacklist")]
    pub tessedit_char_blacklist: Option<String>,
    /// Use primary language params model
    #[napi(js_name = "tesseditUsePrimaryParamsModel")]
    pub tessedit_use_primary_params_model: Option<bool>,
    /// Variable-width space detection
    #[napi(js_name = "textordSpaceSizeIsVariable")]
    pub textord_space_size_is_variable: Option<bool>,
    /// Use adaptive thresholding method
    #[napi(js_name = "thresholdingMethod")]
    pub thresholding_method: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsTesseractConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TesseractConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "tesseractConfigDefault")]
pub fn tesseract_config_default() -> JsTesseractConfig {
    xberg::TesseractConfig::default().into()
}

/// Inline text annotation — byte-range based formatting and links.
///
/// Annotations reference byte offsets into the node's text content,
/// enabling precise identification of formatted regions.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TextAnnotation")]
pub struct JsTextAnnotation {
    /// Start byte offset in the node's text content (inclusive).
    pub start: u32,
    /// End byte offset in the node's text content (exclusive).
    pub end: u32,
    /// Annotation type.
    pub kind: JsAnnotationKind,
}

/// Plain text and Markdown extraction result.
///
/// Contains the extracted text along with statistics and,
/// for Markdown files, structural elements like headers and links.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TextExtractionResult")]
pub struct JsTextExtractionResult {
    /// Extracted text content
    pub content: String,
    /// Number of lines
    #[napi(js_name = "lineCount")]
    pub line_count: i64,
    /// Number of words
    #[napi(js_name = "wordCount")]
    pub word_count: i64,
    /// Number of characters
    #[napi(js_name = "characterCount")]
    pub character_count: i64,
    /// Markdown headers (text only, Markdown files only)
    pub headers: Option<Vec<String>>,
    /// Markdown links (Markdown files only).
    pub links: Option<Vec<JsMarkdownLink>>,
    /// Code blocks (Markdown files only).
    #[napi(js_name = "codeBlocks")]
    pub code_blocks: Option<Vec<JsMarkdownCodeBlock>>,
}

/// Text/Markdown metadata.
///
/// Extracted from plain text and Markdown files. Includes word counts and,
/// for Markdown, structural elements like headers and links.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TextMetadata")]
pub struct JsTextMetadata {
    /// Number of lines in the document
    #[napi(js_name = "lineCount")]
    pub line_count: Option<u32>,
    /// Number of words
    #[napi(js_name = "wordCount")]
    pub word_count: Option<u32>,
    /// Number of characters
    #[napi(js_name = "characterCount")]
    pub character_count: Option<u32>,
    /// Markdown headers (headings text only, for Markdown files)
    pub headers: Option<Vec<String>>,
    /// Markdown links (for Markdown files).
    pub links: Option<Vec<JsMarkdownLink>>,
    /// Code blocks (for Markdown files).
    #[napi(js_name = "codeBlocks")]
    pub code_blocks: Option<Vec<JsMarkdownCodeBlock>>,
}

/// Per-category running counter for `RedactionStrategy.TokenReplace`.
#[derive(Clone)]
#[napi(js_name = "TokenCounter")]
pub struct JsTokenCounter {
    inner: Arc<xberg::TokenCounter>,
}

#[napi]
impl JsTokenCounter {
    /// Create a fresh counter with no previous state.
    #[napi]
    pub fn new() -> JsTokenCounter {
        Self {
            inner: Arc::new(xberg::TokenCounter::new()),
        }
    }
}

#[napi]
impl JsTokenCounter {
    #[napi(constructor)]
    pub fn new_constructor() -> Self {
        Self {
            inner: std::sync::Arc::new(xberg::TokenCounter::new()),
        }
    }
}

impl Default for JsTokenCounter {
    fn default() -> Self {
        Self::new()
    }
}

/// Configuration for the token-reduction pipeline.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "TokenReductionConfig")]
pub struct JsTokenReductionConfig {
    /// Reduction intensity level.
    #[napi(ts_type = "ReductionLevel | 'Off' | 'Light' | 'Moderate' | 'Aggressive' | 'Maximum'")]
    pub level: Option<JsReductionLevel>,
    /// ISO 639-1 language code hint for stopword selection (e.g. `"en"`, `"de"`).
    #[napi(js_name = "languageHint")]
    pub language_hint: Option<String>,
    /// Preserve Markdown formatting tokens during reduction.
    #[napi(js_name = "preserveMarkdown")]
    pub preserve_markdown: Option<bool>,
    /// Preserve code block contents unchanged.
    #[napi(js_name = "preserveCode")]
    pub preserve_code: Option<bool>,
    /// Cosine similarity threshold below which sentences are considered dissimilar.
    #[napi(js_name = "semanticThreshold")]
    pub semantic_threshold: Option<f64>,
    /// Use Rayon parallel iterators for multi-core processing.
    #[napi(js_name = "enableParallel")]
    pub enable_parallel: Option<bool>,
    /// Use SIMD-optimized text scanning where available.
    #[napi(js_name = "useSimd")]
    pub use_simd: Option<bool>,
    /// Per-language custom stopword lists (`language_code → stopword_list`).
    #[napi(js_name = "customStopwords")]
    pub custom_stopwords: Option<HashMap<String, Vec<String>>>,
    /// Regex patterns whose matched text is always preserved unchanged.
    #[napi(js_name = "preservePatterns")]
    pub preserve_patterns: Option<Vec<String>>,
    /// Target fraction of text to retain (0.0–1.0); `None` = no fixed target.
    #[napi(js_name = "targetReduction")]
    pub target_reduction: Option<f64>,
    /// Group semantically similar sentences and emit only one per cluster.
    #[napi(js_name = "enableSemanticClustering")]
    pub enable_semantic_clustering: Option<bool>,
    /// Skip removal of words with "important" characteristics (all-caps
    /// acronyms, words containing digits, mixed-case identifiers, very long
    /// words) during the `Aggressive`/`Maximum` common-word removal pass.
    ///
    /// `true` (the default) protects those words even when they would
    /// otherwise be dropped as low-value filler; `false` lets the frequency/
    /// length heuristics apply uniformly to every word, including ones that
    /// look like acronyms or technical terms (#269).
    #[napi(js_name = "preserveImportantWords")]
    pub preserve_important_words: Option<bool>,
}

#[napi(js_name = "tokenReductionConfigDefault")]
pub fn token_reduction_config_default() -> JsTokenReductionConfig {
    xberg::TokenReductionConfig::default().into()
}

/// Token reduction configuration.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TokenReductionOptions")]
pub struct JsTokenReductionOptions {
    /// Reduction mode: "off", "light", "moderate", "aggressive", "maximum"
    pub mode: Option<String>,
    /// Preserve important words (capitalized, technical terms)
    #[napi(js_name = "preserveImportantWords")]
    pub preserve_important_words: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsTokenReductionOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TokenReductionOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "tokenReductionOptionsDefault")]
pub fn token_reduction_options_default() -> JsTokenReductionOptions {
    xberg::TokenReductionOptions::default().into()
}

/// Configuration for audio/video transcription (speech-to-text).
///
/// When present and `enabled`, Xberg will route audio and video files
/// (mp3, mp4, m4a, wav, webm, etc.) through the transcription pipeline.
///
/// The heavy dependencies (ORT, hf-hub, symphonia) are only pulled when the
/// `transcription` feature is enabled. The config struct itself is available
/// under `transcription-types` so that `ExtractionConfig` round-trips on all
/// targets.
///
/// All fields have sensible defaults. The recommended starting point is:
///
/// ```toml
/// [extraction.transcription]
/// enabled = true
/// model = "tiny"
/// ```
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TranscriptionConfig")]
pub struct JsTranscriptionConfig {
    /// Master switch. When `false`, the transcription pipeline is not run.
    ///
    /// The extractor is registered for audio/video MIME types whenever the `transcription`
    /// feature is compiled in, independently of this flag, so an audio/video input with
    /// `enabled = false` fails with an `XbergError.Transcription` explaining how to turn
    /// transcription on — it does not fall through to another extractor.
    pub enabled: Option<bool>,
    /// Whisper model size to use.
    ///
    /// Smaller = faster + lower memory. `tiny` is the pragmatic default for
    /// first-time users and CI.
    #[napi(ts_type = "WhisperModel | 'tiny' | 'base' | 'small' | 'medium' | 'large_v_3'")]
    pub model: Option<JsWhisperModel>,
    /// Optional language hint (ISO-639-1 code, e.g. "en", "de").
    ///
    /// When `None` (default), the current engine falls back to English.
    /// For deterministic production output, always set this explicitly.
    pub language: Option<String>,
    /// Whether to request segment-level timestamps.
    ///
    /// When `true`, the decoder prompt omits `<|notimestamps|>` so the model emits
    /// `<|x.xx|>` tokens, and each transcript segment becomes its own paragraph element
    /// carrying `start_ms` / `end_ms` attributes. When `false` (default), all segment
    /// text is joined into a single flat paragraph with no timing attributes.
    pub timestamps: Option<bool>,
    /// Hard safety limit on input duration (milliseconds).
    ///
    /// Files longer than this are rejected after decode, before model work.
    /// Default: 30 minutes. Set to `None` to disable (not recommended for
    /// untrusted input).
    #[napi(js_name = "maxDurationMs")]
    pub max_duration_ms: Option<i64>,
    /// Hard safety limit on input size (bytes).
    ///
    /// Default: 512 MiB. Protects against pathological or malicious uploads.
    #[napi(js_name = "maxBytes")]
    pub max_bytes: Option<i64>,
    /// Wall-clock timeout for the entire transcription operation (ms).
    ///
    /// Bounds audio decode, model resolution/download, and inference together. On expiry
    /// the extraction fails with an `XbergError.Transcription`. `None` disables the bound
    /// and lets the operation run unbounded (not recommended for untrusted input).
    ///
    /// Enforced on the async extraction path only; the size and duration caps
    /// (`max_bytes`, `max_duration_ms`) are checked on every path.
    ///
    /// Default: 10 minutes.
    #[napi(js_name = "timeoutMs")]
    pub timeout_ms: Option<i64>,
    /// Optional alternate Hugging Face cache root for Whisper models.
    ///
    /// When unset, hf-hub follows `HF_HUB_CACHE`, `HUGGINGFACE_HUB_CACHE`,
    /// `HF_HOME`, XDG, and platform defaults. Files remain in the standard
    /// content-addressed snapshot layout and are not copied into an Xberg cache.
    #[napi(js_name = "modelCacheDir")]
    pub model_cache_dir: Option<String>,
    /// Allow network access to download models from Hugging Face Hub.
    ///
    /// When `false`, only previously cached models may be used. Useful for
    /// air-gapped or fully offline deployments.
    #[napi(js_name = "allowNetwork")]
    pub allow_network: Option<bool>,
    /// Request SHA256 verification of downloaded model files.
    ///
    /// Defaults to `false` because the resolver downloads from mutable Hugging
    /// Face refs unless callers pin and verify models out-of-band. Explicit
    /// `true` requests are rejected by the model resolver until pinned checksum
    /// metadata is available.
    #[napi(js_name = "verifyHash")]
    pub verify_hash: Option<bool>,
}
impl<'de> serde::Deserialize<'de> for JsTranscriptionConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TranscriptionConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "transcriptionConfigDefault")]
pub fn transcription_config_default() -> JsTranscriptionConfig {
    xberg::TranscriptionConfig::default().into()
}

/// Translation of the extracted content.
///
/// Holds the translated rendition of `ExtractedDocument.content` and (when
/// `preserve_markup` was requested) the translated `formatted_content`. Chunks
/// are translated in place inside `ExtractedDocument.chunks[*].content` rather
/// than duplicated here.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "Translation")]
pub struct JsTranslation {
    /// BCP-47 language tag the translation was produced into (e.g. `"de"`, `"fr-CA"`).
    #[napi(js_name = "targetLang")]
    pub target_lang: String,
    /// BCP-47 source language. `None` when the translation backend was asked to detect.
    #[napi(js_name = "sourceLang")]
    pub source_lang: Option<String>,
    /// Translated plain-text body. Matches the shape of `ExtractedDocument.content`.
    pub content: String,
    /// Translated markup body (Markdown / HTML / etc.) when `preserve_markup` was
    /// enabled on the config. `None` otherwise.
    #[napi(js_name = "formattedContent")]
    pub formatted_content: Option<String>,
}

/// Configuration for the translation post-processor.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TranslationConfig")]
pub struct JsTranslationConfig {
    /// BCP-47 language tag for the target language (e.g. `"de"`, `"fr-CA"`).
    #[napi(js_name = "targetLang")]
    pub target_lang: String,
    /// Optional explicit source language. `None` asks the backend to auto-detect.
    #[napi(js_name = "sourceLang")]
    pub source_lang: Option<String>,
    /// Translate the formatted (Markdown/HTML) rendition alongside plain text when
    /// `formatted_content` is present.
    #[napi(js_name = "preserveMarkup")]
    pub preserve_markup: bool,
    /// LLM configuration used for translation.
    pub llm: JsLlmConfig,
}
impl<'de> serde::Deserialize<'de> for JsTranslationConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TranslationConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Configuration for tree-sitter language pack integration.
///
/// Controls grammar download behavior and code analysis options.
///
/// # Example (TOML)
///
/// ```toml
/// [tree_sitter]
/// languages = ["python", "rust"]
/// groups = ["web"]
///
/// [tree_sitter.process]
/// structure = true
/// comments = true
/// docstrings = true
/// ```
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TreeSitterConfig")]
pub struct JsTreeSitterConfig {
    /// Enable code intelligence processing (default: true).
    ///
    /// When `false`, tree-sitter analysis is completely skipped even if
    /// the config section is present.
    pub enabled: Option<bool>,
    /// Custom cache directory for downloaded grammars.
    ///
    /// When `None`, uses the default: `~/.cache/tree-sitter-language-pack/v{version}/libs/`.
    ///
    /// Consumed both by the CLI (`tree-sitter download --from-config`,
    /// `cache warm`) and by `CodeExtractor` at
    /// extraction time, so that a configured cache directory is honoured
    /// wherever grammars are looked up or downloaded, not only during an
    /// explicit CLI download.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Languages to pre-download on init (e.g., `["python", "rust"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands as a pre-download hint. Extraction itself does
    /// not read this field: a given source file always processes with a
    /// single, already auto-detected language, so there is nothing for a
    /// language allowlist to gate at extraction time.
    pub languages: Option<Vec<String>>,
    /// Language groups to pre-download (e.g., `["web", "systems", "scripting"]`).
    ///
    /// Consumed only by the CLI's `tree-sitter download --from-config` and
    /// `cache warm` commands, for the same reason as `languages` above.
    pub groups: Option<Vec<String>>,
    /// Processing options for code analysis.
    pub process: Option<JsTreeSitterProcessConfig>,
}
impl<'de> serde::Deserialize<'de> for JsTreeSitterConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TreeSitterConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "treeSitterConfigDefault")]
pub fn tree_sitter_config_default() -> JsTreeSitterConfig {
    xberg::TreeSitterConfig::default().into()
}

/// Processing options for tree-sitter code analysis.
///
/// Controls which analysis features are enabled when extracting code files.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TreeSitterProcessConfig")]
pub struct JsTreeSitterProcessConfig {
    /// Extract structural items (functions, classes, structs, etc.). Default: true.
    pub structure: Option<bool>,
    /// Extract import statements. Default: true.
    pub imports: Option<bool>,
    /// Extract export statements. Default: true.
    pub exports: Option<bool>,
    /// Extract comments. Default: false.
    pub comments: Option<bool>,
    /// Extract docstrings. Default: false.
    pub docstrings: Option<bool>,
    /// Extract symbol definitions. Default: false.
    pub symbols: Option<bool>,
    /// Include parse diagnostics. Default: false.
    pub diagnostics: Option<bool>,
    /// Extract a hierarchical key/value data tree from data-format files
    /// (JSON, YAML, TOML, XML, CSV, etc.). Default: false.
    #[napi(js_name = "dataExtraction")]
    pub data_extraction: Option<bool>,
    /// Maximum chunk size in bytes. `None` disables chunking.
    #[napi(js_name = "chunkMaxSize")]
    pub chunk_max_size: Option<i64>,
    /// Content rendering mode for code extraction.
    #[napi(
        js_name = "contentMode",
        ts_type = "CodeContentMode | 'chunks' | 'raw' | 'structure'"
    )]
    pub content_mode: Option<JsCodeContentMode>,
}
impl<'de> serde::Deserialize<'de> for JsTreeSitterProcessConfig {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::TreeSitterProcessConfig as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

#[napi(js_name = "treeSitterProcessConfigDefault")]
pub fn tree_sitter_process_config_default() -> JsTreeSitterProcessConfig {
    xberg::TreeSitterProcessConfig::default().into()
}

/// Runtime options accepted by the `candle-trocr` backend.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "TrocrBackendOptions")]
pub struct JsTrocrBackendOptions {
    /// Optional model variant; the backend constructor's variant is used when absent.
    #[napi(
        ts_type = "CandleTrocrVariant | 'base-printed' | 'large-printed' | 'base-handwritten' | 'large-handwritten'"
    )]
    pub variant: Option<JsCandleTrocrVariant>,
    /// Optional per-call device override.
    #[napi(ts_type = "CandleDevicePreference | 'auto' | 'cpu' | 'cuda' | 'metal'")]
    pub device: Option<JsCandleDevicePreference>,
    /// Optional Hugging Face cache root.
    #[napi(js_name = "cacheDir")]
    pub cache_dir: Option<String>,
    /// Optional immutable Hugging Face model revision.
    #[napi(js_name = "hfRevision")]
    pub hf_revision: Option<String>,
}
impl<'de> serde::Deserialize<'de> for JsTrocrBackendOptions {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::candle_ocr::TrocrBackendOptions as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// URL ingestion and crawl configuration.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "UrlExtractionConfig")]
pub struct JsUrlExtractionConfig {
    /// URL extraction mode.
    #[napi(ts_type = "UrlExtractionMode | 'auto' | 'document' | 'crawl'")]
    pub mode: Option<JsUrlExtractionMode>,
    /// Crawlberg crawl configuration used for HTTP(S) URL extraction.
    #[serde(skip)]
    pub crawl: Option<JsCrawlConfig>,
    /// Optional regex filter for document-discovered URLs.
    #[napi(js_name = "documentUrlPattern")]
    pub document_url_pattern: Option<String>,
    /// Maximum URLs to follow per extraction result.
    #[napi(js_name = "maxDocumentUrlsPerResult")]
    pub max_document_urls_per_result: Option<u32>,
    /// Maximum URLs followed across the whole extraction call.
    #[napi(js_name = "maxTotalUrls")]
    pub max_total_urls: Option<u32>,
    /// Allow bare local filesystem path inputs.
    #[napi(js_name = "allowLocalFileInputs")]
    pub allow_local_file_inputs: Option<bool>,
    /// Allow local `file://` URI inputs.
    #[napi(js_name = "allowFileUris")]
    pub allow_file_uris: Option<bool>,
}

#[napi(js_name = "urlExtractionConfigDefault")]
pub fn url_extraction_config_default() -> JsUrlExtractionConfig {
    xberg::UrlExtractionConfig::default().into()
}

/// User-provided chunk configuration.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "UserChunkConfig")]
pub struct JsUserChunkConfig {
    /// User-specified page ranges (overrides automatic chunking).
    #[napi(js_name = "pageRanges")]
    pub page_ranges: Option<Vec<JsPageRange>>,
    /// User-specified pages per chunk (overrides automatic calculation).
    #[napi(js_name = "pagesPerChunk")]
    pub pages_per_chunk: Option<u32>,
    /// Force chunking even for small documents.
    #[napi(js_name = "forceChunking")]
    pub force_chunking: Option<bool>,
    /// Disable chunking even for large documents.
    #[napi(js_name = "disableChunking")]
    pub disable_chunking: Option<bool>,
}

/// Application properties from docProps/app.xml for XLSX
///
/// Contains Excel-specific document metadata.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "XlsxAppProperties")]
pub struct JsXlsxAppProperties {
    /// Application name (e.g., "Microsoft Excel")
    pub application: Option<String>,
    /// Application version
    #[napi(js_name = "appVersion")]
    pub app_version: Option<String>,
    /// Document security level
    #[napi(js_name = "docSecurity")]
    pub doc_security: Option<i32>,
    /// Scale crop flag
    #[napi(js_name = "scaleCrop")]
    pub scale_crop: Option<bool>,
    /// Links up to date flag
    #[napi(js_name = "linksUpToDate")]
    pub links_up_to_date: Option<bool>,
    /// Shared document flag
    #[napi(js_name = "sharedDoc")]
    pub shared_doc: Option<bool>,
    /// Hyperlinks changed flag
    #[napi(js_name = "hyperlinksChanged")]
    pub hyperlinks_changed: Option<bool>,
    /// Company name
    pub company: Option<String>,
    /// Worksheet names
    #[napi(js_name = "worksheetNames")]
    pub worksheet_names: Option<Vec<String>>,
}

/// XML extraction result.
///
/// Contains extracted text content from XML files along with
/// structural statistics about the XML document.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "XmlExtractionResult")]
pub struct JsXmlExtractionResult {
    /// Extracted text content (XML structure filtered out)
    pub content: String,
    /// Total number of XML elements processed
    #[napi(js_name = "elementCount")]
    pub element_count: i64,
    /// List of unique element names found (sorted)
    #[napi(js_name = "uniqueElements")]
    pub unique_elements: Vec<String>,
}

/// XML metadata extracted during XML parsing.
///
/// Provides statistics about XML document structure.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "XmlMetadata")]
pub struct JsXmlMetadata {
    /// Total number of XML elements processed
    #[napi(js_name = "elementCount")]
    pub element_count: Option<u32>,
    /// List of unique element tag names (sorted)
    #[napi(js_name = "uniqueElements")]
    pub unique_elements: Option<Vec<String>>,
}

/// YAKE-specific parameters.
#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "YakeParams")]
pub struct JsYakeParams {
    /// Window size for co-occurrence analysis (default: 2).
    ///
    /// Controls the context window for computing co-occurrence statistics.
    #[napi(js_name = "windowSize")]
    pub window_size: Option<i64>,
}

#[cfg(feature = "keywords-yake")]
#[napi(js_name = "yakeParamsDefault")]
pub fn yake_params_default() -> JsYakeParams {
    xberg::YakeParams::default().into()
}

/// Year range for bibliographic metadata.
#[derive(Clone, Default, serde::Serialize)]
#[napi(object, js_name = "YearRange")]
pub struct JsYearRange {
    /// Earliest (minimum) year in the range.
    pub min: Option<u32>,
    /// Latest (maximum) year in the range.
    pub max: Option<u32>,
    /// All individual years present in the collection.
    pub years: Vec<u32>,
}
impl<'de> serde::Deserialize<'de> for JsYearRange {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <xberg::YearRange as serde::Deserialize>::deserialize(deserializer).map(Into::into)
    }
}

/// Types of inline text annotations.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "AnnotationKind")]
pub struct JsAnnotationKind {
    #[napi(js_name = "annotation_type")]
    #[serde(rename = "annotation_type")]
    pub annotation_type_tag: String,
    pub url: Option<String>,
    pub title: Option<String>,
    pub value: Option<String>,
    pub name: Option<String>,
}

impl Default for JsAnnotationKind {
    fn default() -> Self {
        Self {
            annotation_type_tag: "bold".to_string(),
            name: None,
            title: None,
            url: None,
            value: None,
        }
    }
}

/// The category of a downloaded asset.
#[napi(string_enum = "snake_case", js_name = "AssetCategory")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsAssetCategory {
    /// A document file (PDF, DOC, etc.).
    Document,
    /// An image file.
    Image,
    /// An audio file.
    Audio,
    /// A video file.
    Video,
    /// A font file.
    Font,
    /// A CSS stylesheet.
    Stylesheet,
    /// A JavaScript file.
    Script,
    /// An archive file (ZIP, TAR, etc.).
    Archive,
    /// A data file (JSON, XML, CSV, etc.).
    Data,
    /// An unrecognized asset type.
    Other,
}

#[allow(clippy::derivable_impls)]
impl Default for JsAssetCategory {
    fn default() -> Self {
        Self::Document
    }
}

/// Authentication configuration.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "AuthConfig")]
pub struct JsAuthConfig {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub username: Option<String>,
    pub password: Option<String>,
    pub token: Option<String>,
    pub name: Option<String>,
    pub value: Option<String>,
}

impl Default for JsAuthConfig {
    fn default() -> Self {
        Self {
            type_tag: "basic".to_string(),
            name: None,
            password: None,
            token: None,
            username: None,
            value: None,
        }
    }
}

/// Types of block-level elements in Djot.
#[napi(string_enum = "snake_case", js_name = "BlockType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsBlockType {
    /// Standard prose paragraph.
    Paragraph,
    /// Section heading (level stored in `FormattedBlock.level`).
    Heading,
    /// Block quotation container.
    Blockquote,
    /// Fenced or indented code block.
    CodeBlock,
    /// Individual item within a list.
    ListItem,
    /// Numbered (ordered) list container.
    OrderedList,
    /// Unnumbered (bullet) list container.
    BulletList,
    /// Task / checkbox list container.
    TaskList,
    /// Definition list container.
    DefinitionList,
    /// Term part of a definition list entry.
    DefinitionTerm,
    /// Description / definition part of a definition list entry.
    DefinitionDescription,
    /// Generic `div` container with optional attributes.
    Div,
    /// Logical section container, often associated with a heading.
    Section,
    /// Horizontal rule / thematic break.
    ThematicBreak,
    /// Raw content block in a specified format (e.g. HTML, LaTeX).
    RawBlock,
    /// Display-mode mathematical expression.
    MathDisplay,
}

#[allow(clippy::derivable_impls)]
impl Default for JsBlockType {
    fn default() -> Self {
        Self::Paragraph
    }
}

/// Reason for boundary detection.
#[napi(string_enum = "snake_case", js_name = "BoundaryReason")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsBoundaryReason {
    /// Start of PDF.
    Start,
    /// Page-one marker ("Page 1", "1 of N") detected.
    PageOneMarker,
    /// Letterhead reset after signature block.
    LetterheadReset,
    /// Text density shift with low bigram overlap.
    DensityShift,
    /// End of PDF.
    End,
}

#[allow(clippy::derivable_impls)]
impl Default for JsBoundaryReason {
    fn default() -> Self {
        Self::Start
    }
}

/// Browser backend used for JavaScript rendering.
#[napi(string_enum = "snake_case", js_name = "BrowserBackend")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsBrowserBackend {
    /// Existing Chromium/CDP backend powered by chromiumoxide.
    Chromiumoxide,
    /// Crawlberg-owned native browser backend derived from Obscura.
    Native,
}

#[allow(clippy::derivable_impls)]
impl Default for JsBrowserBackend {
    fn default() -> Self {
        Self::Chromiumoxide
    }
}

/// When to use the headless browser fallback.
#[napi(string_enum = "snake_case", js_name = "BrowserMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsBrowserMode {
    /// Automatically detect when JS rendering is needed and fall back to browser.
    Auto,
    /// Always use the browser for every request.
    Always,
    /// Never use the browser fallback.
    Never,
    /// Always use the browser with all stealth surfaces enabled.
    ///
    /// Behaves like `Always` for escalation purposes
    /// (every request is routed through the browser tier), but additionally
    /// enables:
    ///
    /// - browser JavaScript stealth patches
    /// - native-backend TLS fingerprint spoofing
    /// - stealth-aware default user-agent when no explicit UA is set
    /// - 1920×1080 viewport override
    ///
    /// Use this instead of setting the now-removed `BrowserConfig.stealth`
    /// boolean field.
    Stealth,
}

#[allow(clippy::derivable_impls)]
impl Default for JsBrowserMode {
    fn default() -> Self {
        Self::Auto
    }
}

/// Wait strategy for browser page rendering.
#[napi(string_enum = "snake_case", js_name = "BrowserWait")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsBrowserWait {
    /// Wait until network activity is idle.
    NetworkIdle,
    /// Wait for a specific CSS selector to appear in the DOM.
    Selector,
    /// Wait for a fixed duration after navigation.
    Fixed,
}

#[allow(clippy::derivable_impls)]
impl Default for JsBrowserWait {
    fn default() -> Self {
        Self::NetworkIdle
    }
}

/// How a structured-extraction preset is dispatched to the model.
///
/// This is the preset-facing call mode (the `preferred_call_mode` field of a
/// `Preset`). The structured pipeline has a richer
/// runtime-only decision enum with skip and fallback states; this 3-variant
/// type is the stable, serializable surface presets and bindings depend on.
#[napi(string_enum = "snake_case", js_name = "CallMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCallMode {
    /// Use the extracted text only.
    TextOnly,
    /// Use rasterized page images only.
    VisionOnly,
    /// Provide both extracted text and page images to the model.
    TextPlusVision,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCallMode {
    fn default() -> Self {
        Self::TextOnly
    }
}

/// Device selection shared by the typed candle backend option objects.
#[napi(string_enum = "lowercase", js_name = "CandleDevicePreference")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCandleDevicePreference {
    Auto,
    Cpu,
    Cuda,
    Metal,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCandleDevicePreference {
    fn default() -> Self {
        Self::Auto
    }
}

/// TrOCR model variant accepted by `candle-trocr` backend options.
#[napi(string_enum = "kebab-case", js_name = "CandleTrocrVariant")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCandleTrocrVariant {
    BasePrinted,
    LargePrinted,
    BaseHandwritten,
    LargeHandwritten,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCandleTrocrVariant {
    fn default() -> Self {
        Self::BasePrinted
    }
}

/// How chunk size is measured.
///
/// Defaults to `Characters` (Unicode character count). When using token-based sizing,
/// chunks are sized by token count according to the specified tokenizer.
///
/// Token-based sizing uses HuggingFace tokenizers loaded at runtime, or a tokenizer
/// backend you register yourself. Any tokenizer available on HuggingFace Hub can be
/// used, including OpenAI-compatible tokenizers (e.g., `Xenova/gpt-4o`,
/// `Xenova/cl100k_base`). To size chunks with your own tokenizer instead (llama.cpp/GGUF
/// vocabularies, SentencePiece models, custom vocabs), register a `TokenizerBackend`
/// with `register_tokenizer_backend` and set `model` to the registered name.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ChunkSizing")]
pub struct JsChunkSizing {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub model: Option<String>,
    #[napi(js_name = "cacheDir")]
    #[serde(rename = "cacheDir")]
    pub cache_dir: Option<String>,
}

impl Default for JsChunkSizing {
    fn default() -> Self {
        Self {
            type_tag: "characters".to_string(),
            cache_dir: None,
            model: None,
        }
    }
}

/// Semantic structural classification of a text chunk.
///
/// Assigned by the heuristic classifier in `chunking.classifier`.
/// Defaults to `Unknown` when no rule matches.
/// Designed to be extended in future versions without breaking changes.
#[napi(string_enum = "snake_case", js_name = "ChunkType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsChunkType {
    /// Section heading or document title.
    Heading,
    /// Party list: names, addresses, and signatories.
    PartyList,
    /// Definition clause ("X means…", "X shall mean…").
    Definitions,
    /// Operative clause containing legal/contractual action verbs.
    OperativeClause,
    /// Signature block with signatures, names, and dates.
    SignatureBlock,
    /// Schedule, annex, appendix, or exhibit section.
    Schedule,
    /// Table-like content with aligned columns or repeated patterns.
    TableLike,
    /// Mathematical formula or equation.
    Formula,
    /// Code block or preformatted content.
    CodeBlock,
    /// Function or method definition (tree-sitter structured code chunking).
    Function,
    /// Class, struct, interface, or trait definition (tree-sitter structured code chunking).
    Class,
    /// Module, namespace, or top-level file scope (tree-sitter structured code chunking).
    Module,
    /// Embedded or referenced image content.
    Image,
    /// Organizational chart or hierarchy diagram.
    OrgChart,
    /// Diagram, figure, or visual illustration.
    Diagram,
    /// Unclassified or mixed content.
    Unknown,
}

#[allow(clippy::derivable_impls)]
impl Default for JsChunkType {
    fn default() -> Self {
        Self::Heading
    }
}

/// Type of text chunker to use.
///
/// # Variants
///
/// * `Text` - Generic text splitter, splits on whitespace and punctuation
/// * `Markdown` - Markdown-aware splitter, preserves formatting and structure
/// * `Yaml` - YAML-aware splitter, creates one chunk per top-level key
/// * `Semantic` - Topic-aware chunker. With an `EmbeddingConfig`, splits at
///   embedding-based topic shifts tuned by `topic_threshold` (default 0.75,
///   lower = more splits). Without an embedding, falls back to a
///   structural-boundary heuristic (ALL-CAPS headers, numbered sections,
///   blank-line paragraphs) and merges groups into chunks capped at
///   `max_characters` (default 1000). `topic_threshold` has no effect in the
///   fallback path. For best results, pair with an embedding model.
#[napi(string_enum = "lowercase", js_name = "ChunkerType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsChunkerType {
    /// Generic whitespace- and punctuation-aware text splitter (default).
    Text,
    /// Markdown-aware splitter that preserves heading and code-block boundaries.
    Markdown,
    /// YAML-aware splitter that creates one chunk per top-level key.
    Yaml,
    /// Topic-aware chunker that splits at embedding-based topic shifts.
    Semantic,
}

#[allow(clippy::derivable_impls)]
impl Default for JsChunkerType {
    fn default() -> Self {
        Self::Text
    }
}

/// Reason for chunking a document.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ChunkingReason")]
pub struct JsChunkingReason {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    #[napi(js_name = "sizeBytes")]
    #[serde(rename = "sizeBytes")]
    pub size_bytes: Option<i64>,
    #[napi(js_name = "thresholdBytes")]
    #[serde(rename = "thresholdBytes")]
    pub threshold_bytes: Option<i64>,
    #[napi(js_name = "pageCount")]
    #[serde(rename = "pageCount")]
    pub page_count: Option<u32>,
    pub threshold: Option<u32>,
    #[napi(js_name = "forceOcr")]
    #[serde(rename = "forceOcr")]
    pub force_ocr: Option<bool>,
}

impl Default for JsChunkingReason {
    fn default() -> Self {
        Self {
            type_tag: "LargeFile".to_string(),
            force_ocr: None,
            page_count: None,
            size_bytes: None,
            threshold: None,
            threshold_bytes: None,
        }
    }
}

/// Code block fence style in Markdown output.
///
/// Determines how code blocks (`<pre><code>`) are rendered in Markdown.
#[napi(string_enum, js_name = "CodeBlockStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCodeBlockStyle {
    /// Indented code blocks (4 spaces). `CommonMark` standard.
    Indented,
    /// Fenced code blocks with triple backticks. Default (GFM). Supports language hints.
    Backticks,
    /// Fenced code blocks with tildes (~~~). Supports language hints.
    Tildes,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCodeBlockStyle {
    fn default() -> Self {
        Self::Indented
    }
}

/// Content rendering mode for code extraction.
///
/// Controls how extracted code content is represented in the `content` field
/// of `ExtractedDocument`.
#[napi(string_enum = "snake_case", js_name = "CodeContentMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCodeContentMode {
    /// Use TSLP semantic chunks as content (default).
    Chunks,
    /// Use raw source code as content.
    Raw,
    /// Emit function/class headings + docstrings (no code bodies).
    Structure,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCodeContentMode {
    fn default() -> Self {
        Self::Chunks
    }
}

/// Discriminates the shape of a `CodeDataNode`.
///
/// Purpose-built mirror of `tree_sitter_language_pack.DataNodeKind` — kept as an
/// xberg-owned type so binding generators never need to resolve the upstream crate's
/// types across FFI/language boundaries.
#[napi(string_enum = "snake_case", js_name = "CodeDataNodeKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCodeDataNodeKind {
    /// A key/value pair or mapping (JSON/TOML/properties/YAML/HCL/CUE/KDL pair, or a
    /// wrapper "object"/"mapping" container).
    KeyValue,
    /// An XML element with a tag name in `key` and attributes in `attributes`.
    Element,
    /// A positional sequence item (JSON array element, YAML block sequence item,
    /// CSV/PSV row or cell).
    Sequence,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCodeDataNodeKind {
    fn default() -> Self {
        Self::KeyValue
    }
}

/// How a backend's reported page-level confidence must be interpreted.
///
/// Backend confidence scores are not interchangeable. Tesseract's mean word confidence is a
/// classifier score validated to track legibility on a 0-100 scale. Sceptre (EasyOCR-based)
/// reports a length-penalised `custom_mean` that is rescaled into the same 0-100 range but is
/// *not* comparable — its ordering can be inverted relative to legibility (a dense prose page
/// can score lower than a nearly-blank one). A page-rejection gate calibrated on Tesseract's
/// scale was once applied unconditionally to sceptre's output and rejected every page of a
/// 16-page document, emptying it. This descriptor exists so gating code can ask a backend what
/// its number means instead of assuming.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ConfidenceSemantics")]
pub struct JsConfidenceSemantics {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    #[napi(js_name = "scaleMax")]
    #[serde(rename = "scaleMax")]
    pub scale_max: Option<f64>,
}

impl Default for JsConfidenceSemantics {
    fn default() -> Self {
        Self {
            type_tag: "Uncalibrated".to_string(),
            scale_max: None,
        }
    }
}

/// Content filter applied to each crawled page before it reaches the result.
#[napi(string_enum = "snake_case", js_name = "ContentFilterKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsContentFilterKind {
    /// Keep only pages scoring at or above `bm25_threshold` for `bm25_query`.
    Bm25,
}

#[allow(clippy::derivable_impls)]
impl Default for JsContentFilterKind {
    fn default() -> Self {
        Self::Bm25
    }
}

/// Content layer classification for document nodes.
///
/// Replaces separate body/furniture arrays with per-node granularity.
#[napi(string_enum = "snake_case", js_name = "ContentLayer")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsContentLayer {
    /// Main document body content.
    Body,
    /// Page/section header (running header).
    Header,
    /// Page/section footer (running footer).
    Footer,
    /// Footnote content.
    Footnote,
}

#[allow(clippy::derivable_impls)]
impl Default for JsContentLayer {
    fn default() -> Self {
        Self::Body
    }
}

/// Traversal order for a crawl.
///
/// Selects both the queue discipline and the selection strategy, because global order is a
/// property of the frontier: the engine hands its bounded selection window to the strategy, so
/// a strategy alone can only reorder URLs that have already been dequeued.
#[napi(string_enum = "snake_case", js_name = "CrawlStrategyKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsCrawlStrategyKind {
    /// Breadth-first: a FIFO frontier visits every URL at one depth before the next.
    Bfs,
    /// Depth-first: a LIFO frontier descends into a page's children before its siblings.
    Dfs,
    /// Highest-priority-first within the selection window, scored by `CrawlStrategy.score_url`.
    BestFirst,
    /// Like `BestFirst`, but stops once newly crawled pages stop contributing new terms.
    Adaptive,
}

#[allow(clippy::derivable_impls)]
impl Default for JsCrawlStrategyKind {
    fn default() -> Self {
        Self::Bfs
    }
}

/// Managed credential-provider configuration for OAuth2/STS-based authentication modes liter-llm
/// cannot express via a static `api_key`. See `LlmConfig.credential_provider`.
///
/// `Debug` is implemented by hand: `CredentialProviderConfig.AzureAd`'s `client_secret` is a
/// credential and must never be printed, matching `LlmConfig`'s own redaction policy. The
/// other variants carry no secret material — `CredentialProviderConfig.VertexOauth2` and
/// `CredentialProviderConfig.BedrockWebIdentity` reference a *file path*, never the key or
/// token itself.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "CredentialProviderConfig")]
pub struct JsCredentialProviderConfig {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    #[napi(js_name = "tenantId")]
    #[serde(rename = "tenantId")]
    pub tenant_id: Option<String>,
    #[napi(js_name = "clientId")]
    #[serde(rename = "clientId")]
    pub client_id: Option<String>,
    #[napi(js_name = "clientSecret")]
    #[serde(rename = "clientSecret")]
    pub client_secret: Option<String>,
    pub scope: Option<String>,
    #[napi(js_name = "serviceAccountKeyFile")]
    #[serde(rename = "serviceAccountKeyFile")]
    pub service_account_key_file: Option<String>,
    #[napi(js_name = "roleArn")]
    #[serde(rename = "roleArn")]
    pub role_arn: Option<String>,
    #[napi(js_name = "tokenFile")]
    #[serde(rename = "tokenFile")]
    pub token_file: Option<String>,
    #[napi(js_name = "sessionName")]
    #[serde(rename = "sessionName")]
    pub session_name: Option<String>,
    pub region: Option<String>,
}

impl Default for JsCredentialProviderConfig {
    fn default() -> Self {
        Self {
            type_tag: "azure_ad".to_string(),
            client_id: None,
            client_secret: None,
            region: None,
            role_arn: None,
            scope: None,
            service_account_key_file: None,
            session_name: None,
            tenant_id: None,
            token_file: None,
        }
    }
}

/// A single line in a unified-diff hunk.
///
/// Defined here (rather than only in `crate.diff`) so `RevisionDelta` can
/// reference it unconditionally, without requiring the `diff` Cargo feature.
/// `crate.diff` re-exports this type verbatim.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "DiffLine")]
pub struct JsDiffLine {
    #[napi(js_name = "kind")]
    #[serde(rename = "kind")]
    pub kind_tag: String,
    pub text: Option<String>,
}

impl Default for JsDiffLine {
    fn default() -> Self {
        Self {
            kind_tag: "context".to_string(),
            text: None,
        }
    }
}

#[napi(namespace = "DiffLine", js_name = "Context")]
pub fn diff_line_context(text: String) -> JsDiffLine {
    JsDiffLine {
        kind_tag: "context".to_string(),
        text: Some(text),
    }
}
#[napi(namespace = "DiffLine", js_name = "Added")]
pub fn diff_line_added(text: String) -> JsDiffLine {
    JsDiffLine {
        kind_tag: "added".to_string(),
        text: Some(text),
    }
}
#[napi(namespace = "DiffLine", js_name = "Removed")]
pub fn diff_line_removed(text: String) -> JsDiffLine {
    JsDiffLine {
        kind_tag: "removed".to_string(),
        text: Some(text),
    }
}

/// Opt-in encoding applied to a downloaded document's bytes for callers who need the
/// content available in a serializable field rather than reading it from disk.
///
/// `None` (the `CrawlConfig.document_content_encoding` default) produces neither — unlike
/// screenshots, base64-encoding a document by default would duplicate an already
/// up-to-`document_max_size` buffer (50 MB default) in memory per document.
#[napi(string_enum = "snake_case", js_name = "DocumentContentEncoding")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsDocumentContentEncoding {
    /// Populate `DownloadedDocument.content_base64` with a base64-encoded copy.
    Base64,
}

#[allow(clippy::derivable_impls)]
impl Default for JsDocumentContentEncoding {
    fn default() -> Self {
        Self::Base64
    }
}

/// Semantic element type classification.
///
/// Categorizes text content into semantic units for downstream processing.
/// Supports the element types commonly found in Unstructured documents.
#[napi(string_enum = "snake_case", js_name = "ElementType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsElementType {
    /// Document title
    Title,
    /// Main narrative text body
    NarrativeText,
    /// Section heading
    Heading,
    /// List item (bullet, numbered, etc.)
    ListItem,
    /// Table element
    Table,
    /// Image element
    Image,
    /// Page break marker
    PageBreak,
    /// Code block
    CodeBlock,
    /// Mathematical formula (LaTeX source in `text`)
    Formula,
    /// Block quote
    BlockQuote,
    /// Footer text
    Footer,
    /// Header text
    Header,
}

#[allow(clippy::derivable_impls)]
impl Default for JsElementType {
    fn default() -> Self {
        Self::Title
    }
}

/// Embedding model types supported by Xberg.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EmbeddingModelType")]
pub struct JsEmbeddingModelType {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub name: Option<String>,
    #[napi(js_name = "modelId")]
    #[serde(rename = "modelId")]
    pub model_id: Option<String>,
    pub dimensions: Option<i64>,
    pub llm: Option<JsLlmConfig>,
}

impl Default for JsEmbeddingModelType {
    fn default() -> Self {
        Self {
            type_tag: "preset".to_string(),
            dimensions: None,
            llm: None,
            model_id: None,
            name: None,
        }
    }
}

/// Inference backend that an `EmbeddingPreset` runs on.
///
/// `Onnx` presets require the `embeddings` feature (ONNX Runtime, not available on
/// WASM/Android x86_64 emulator). `Static` presets require `static-embeddings`
/// (pure-Rust model2vec inference, no ORT — the only dense-embedding backend
/// available on `no-ort-target`).
///
/// Defaults to `Onnx` via `#[serde(default)]` so every existing preset payload
/// (which predates this field) keeps deserializing without change.
#[napi(string_enum = "snake_case", js_name = "EmbeddingsEmbeddingBackend")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsEmbeddingsEmbeddingBackend {
    /// ONNX Runtime transformer inference (the historical, default backend).
    Onnx,
    /// Pure-Rust static (model2vec) inference — no ONNX Runtime.
    Static,
}

#[allow(clippy::derivable_impls)]
impl Default for JsEmbeddingsEmbeddingBackend {
    fn default() -> Self {
        Self::Onnx
    }
}

/// Standard entity categories produced by built-in NER backends.
///
/// The `Custom(String)` variant lets caller-supplied categories (e.g. LLM
/// schemas) flow through without losing fidelity to the consumer.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "EntityCategory")]
pub struct JsEntityCategory {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub custom: Option<String>,
}

impl Default for JsEntityCategory {
    fn default() -> Self {
        Self {
            type_tag: "person".to_string(),
            custom: None,
        }
    }
}

/// ONNX Runtime execution provider type.
///
/// Determines which hardware backend is used for model inference.
/// `Auto` (default) selects the best available provider per platform.
#[napi(string_enum = "lowercase", js_name = "ExecutionProviderType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsExecutionProviderType {
    /// Auto-select: CoreML on macOS, CUDA on Linux, CPU elsewhere.
    Auto,
    /// CPU execution provider (always available).
    Cpu,
    /// Apple CoreML (macOS/iOS Neural Engine + GPU).
    CoreMl,
    /// NVIDIA CUDA GPU acceleration.
    Cuda,
    /// NVIDIA TensorRT (optimized CUDA inference).
    TensorRt,
}

#[allow(clippy::derivable_impls)]
impl Default for JsExecutionProviderType {
    fn default() -> Self {
        Self::Auto
    }
}

/// Source kind for `ExtractInput`.
#[napi(string_enum = "snake_case", js_name = "ExtractInputKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsExtractInputKind {
    /// Raw in-memory bytes.
    Bytes,
    /// A filesystem path, `file://` URI, or HTTP(S) URL.
    Uri,
}

#[allow(clippy::derivable_impls)]
impl Default for JsExtractInputKind {
    fn default() -> Self {
        Self::Bytes
    }
}

/// How the extracted text was produced.
#[napi(string_enum = "snake_case", js_name = "ExtractionMethod")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsExtractionMethod {
    /// Text extracted directly from the document's native format (no OCR).
    Native,
    /// All text was obtained via OCR (e.g. scanned image-only PDF).
    Ocr,
    /// Text came from a combination of native extraction and OCR.
    Mixed,
}

#[allow(clippy::derivable_impls)]
impl Default for JsExtractionMethod {
    fn default() -> Self {
        Self::Native
    }
}

/// Kind of a PDF form field.
///
/// Mirrors `xberg_native_pdf`'s widget field taxonomy without leaking the upstream
/// type across the binding surface.
#[napi(string_enum = "snake_case", js_name = "FormFieldType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsFormFieldType {
    /// Single- or multi-line text input.
    Text,
    /// Checkbox (on/off toggle).
    Checkbox,
    /// Radio-button group member.
    Radio,
    /// Choice field (dropdown or list box).
    Choice,
    /// Digital-signature field.
    Signature,
    /// Push button.
    Button,
    /// Field type that could not be classified.
    Unknown,
}

#[allow(clippy::derivable_impls)]
impl Default for JsFormFieldType {
    fn default() -> Self {
        Self::Text
    }
}

/// Format-specific metadata (discriminated union).
///
/// Only one format type can exist per extraction result. This provides
/// type-safe, clean metadata without nested optionals.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "FormatMetadata")]
pub struct JsFormatMetadata {
    #[napi(js_name = "format_type")]
    #[serde(rename = "format_type")]
    pub format_type_tag: String,
    pub pdf: Option<JsPdfMetadata>,
    pub docx: Option<JsDocxMetadata>,
    pub excel: Option<JsExcelMetadata>,
    pub email: Option<JsEmailMetadata>,
    pub pptx: Option<JsPptxMetadata>,
    pub archive: Option<JsArchiveMetadata>,
    pub image: Option<JsImageMetadata>,
    pub xml: Option<JsXmlMetadata>,
    pub text: Option<JsTextMetadata>,
    pub html: Option<JsHtmlMetadata>,
    pub ocr: Option<JsOcrMetadata>,
    pub csv: Option<JsCsvMetadata>,
    pub bibtex: Option<JsBibtexMetadata>,
    pub citation: Option<JsCitationMetadata>,
    #[napi(js_name = "fictionBook")]
    #[serde(rename = "fictionBook")]
    pub fiction_book: Option<JsFictionBookMetadata>,
    pub dbf: Option<JsDbfMetadata>,
    pub jats: Option<JsJatsMetadata>,
    pub epub: Option<JsEpubMetadata>,
    pub pst: Option<JsPstMetadata>,
    pub audio: Option<JsAudioMetadata>,
    pub code: Option<JsCodeMetadata>,
}

impl Default for JsFormatMetadata {
    fn default() -> Self {
        Self {
            format_type_tag: "pdf".to_string(),
            pdf: None,
            docx: None,
            excel: None,
            email: None,
            pptx: None,
            archive: None,
            image: None,
            xml: None,
            text: None,
            html: None,
            ocr: None,
            csv: None,
            bibtex: None,
            citation: None,
            fiction_book: None,
            dbf: None,
            jats: None,
            epub: None,
            pst: None,
            audio: None,
            code: None,
        }
    }
}

/// Formula recognition model selection.
#[napi(string_enum = "snake_case", js_name = "FormulaModel")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsFormulaModel {
    /// RapidLaTeXOCR (MIT, pix2tex-derived): resizer + encoder + decoder ONNX,
    /// ~180 MB total, downloaded on demand.
    LatexOcr,
}

#[allow(clippy::derivable_impls)]
impl Default for JsFormulaModel {
    fn default() -> Self {
        Self::LatexOcr
    }
}

/// Page layout mode accepted by `candle-glm-ocr` backend options.
#[napi(string_enum = "snake_case", js_name = "GlmOcrLayoutMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsGlmOcrLayoutMode {
    WholePage,
    Paired,
}

#[allow(clippy::derivable_impls)]
impl Default for JsGlmOcrLayoutMode {
    fn default() -> Self {
        Self::WholePage
    }
}

/// Task accepted by `candle-glm-ocr` backend options.
#[napi(string_enum = "lowercase", js_name = "GlmOcrTaskKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsGlmOcrTaskKind {
    Ocr,
    Table,
    Formula,
    Chart,
    Caption,
}

#[allow(clippy::derivable_impls)]
impl Default for JsGlmOcrTaskKind {
    fn default() -> Self {
        Self::Ocr
    }
}

/// Heading style options for Markdown output.
///
/// Controls how headings (h1-h6) are rendered in the output Markdown.
#[napi(string_enum, js_name = "HeadingStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsHeadingStyle {
    /// Underlined style (=== for h1, --- for h2).
    Underlined,
    /// ATX style (# for h1, ## for h2, etc.). Default.
    Atx,
    /// ATX closed style (# title #, with closing hashes).
    AtxClosed,
}

#[allow(clippy::derivable_impls)]
impl Default for JsHeadingStyle {
    fn default() -> Self {
        Self::Underlined
    }
}

/// Highlight rendering style for `<mark>` elements.
///
/// Controls how highlighted text is rendered in Markdown output.
#[napi(string_enum, js_name = "HighlightStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsHighlightStyle {
    /// Double equals syntax (==text==). Default. Pandoc-compatible.
    DoubleEqual,
    /// Preserve as HTML (==text==). Original HTML tag.
    Html,
    /// Render as bold (**text**). Uses strong emphasis.
    Bold,
    /// Strip formatting, render as plain text. No markup.
    None,
}

#[allow(clippy::derivable_impls)]
impl Default for JsHighlightStyle {
    fn default() -> Self {
        Self::DoubleEqual
    }
}

/// Hostname/IP allowlist matcher for SSRF policy.
///
/// Serializes as an internally-tagged object so each variant is distinguishable on the
/// wire and round-trips losslessly:
///
/// ```json
/// {"type": "exact",  "value": "api.example.com"}
/// {"type": "suffix", "value": ".example.com"}
/// {"type": "cidr",   "value": "10.0.0.0/8"}
/// ```
///
/// A bare JSON string is still accepted on deserialization and resolves to `Exact`,
/// preserving configs written against the previous untagged representation.
///
/// `Exact`: HostMatcher.Exact
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "HostMatcher")]
pub struct JsHostMatcher {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub value: Option<String>,
}

impl Default for JsHostMatcher {
    fn default() -> Self {
        Self {
            type_tag: "exact".to_string(),
            value: None,
        }
    }
}

/// Built-in HTML theme selection.
#[napi(string_enum = "lowercase", js_name = "HtmlTheme")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsHtmlTheme {
    /// Sensible defaults: system font stack, neutral colours, readable line
    /// measure. CSS custom properties (`--kb-*`) are all defined so user CSS
    /// can override individual values.
    Default,
    /// GitHub Markdown-inspired palette and spacing.
    GitHub,
    /// Dark background, light text.
    Dark,
    /// Minimal light theme with generous whitespace.
    Light,
    /// No built-in stylesheet emitted. CSS custom properties are still defined
    /// on `:root` so user stylesheets can reference `var(--kb-*)` tokens.
    Unstyled,
}

#[allow(clippy::derivable_impls)]
impl Default for JsHtmlTheme {
    fn default() -> Self {
        Self::Default
    }
}

/// Output format for conversion.
///
/// Specifies the target markup language format for the conversion output.
#[napi(string_enum, js_name = "HtmlToMarkdownRsOutputFormat")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsHtmlToMarkdownRsOutputFormat {
    /// Standard Markdown (`CommonMark` compatible). Default.
    Markdown,
    /// Djot lightweight markup language.
    Djot,
    /// Plain text output (no markup, visible text only).
    Plain,
}

#[allow(clippy::derivable_impls)]
impl Default for JsHtmlToMarkdownRsOutputFormat {
    fn default() -> Self {
        Self::Markdown
    }
}

/// Heuristic classification of what an image likely depicts.
#[napi(string_enum = "snake_case", js_name = "ImageKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsImageKind {
    /// Photographic image (natural scene, photograph)
    Photograph,
    /// Technical or schematic diagram
    Diagram,
    /// Chart, graph, or plot
    Chart,
    /// Freehand or technical drawing
    Drawing,
    /// Text-heavy image (scanned text, document)
    TextBlock,
    /// Decorative element or border
    Decoration,
    /// Logo or brand mark
    Logo,
    /// Small icon
    Icon,
    /// Fragment of a larger tiled image (tile of a technical drawing)
    TileFragment,
    /// Mask or transparency map
    Mask,
    /// Full-page render produced during OCR preprocessing; used as a citation thumbnail.
    PageRaster,
    /// Could not classify with reasonable confidence
    Unknown,
}

#[allow(clippy::derivable_impls)]
impl Default for JsImageKind {
    fn default() -> Self {
        Self::Photograph
    }
}

/// Target format for re-encoding extracted images.
///
/// Controls whether and how extracted images are normalised to a uniform
/// container format before being returned in `ExtractedDocument.images`.
/// The default (`Native`) preserves the format produced by each extractor
/// without any additional encode pass.
///
/// Callers that need uniform output — e.g. cloud pipelines that always store
/// WebP thumbnails — set this once on `ImageExtractionConfig.output_format`
/// rather than re-encoding downstream.
///
/// # Serde shape
///
/// Uses a tagged enum: `{"type": "native"}`, `{"type": "png"}`,
/// `{"type": "jpeg", "quality": 90}`, etc.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "ImageOutputFormat")]
pub struct JsImageOutputFormat {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub quality: Option<u8>,
}

impl Default for JsImageOutputFormat {
    fn default() -> Self {
        Self {
            type_tag: "native".to_string(),
            quality: None,
        }
    }
}

/// Image type classification.
#[napi(string_enum = "lowercase", js_name = "ImageType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsImageType {
    /// Data URI image
    #[napi(value = "data-uri")]
    DataUri,
    /// Inline SVG
    #[napi(value = "inline-svg")]
    InlineSvg,
    /// External image URL
    External,
    /// Relative path image
    Relative,
}

#[allow(clippy::derivable_impls)]
impl Default for JsImageType {
    fn default() -> Self {
        Self::DataUri
    }
}

/// Types of inline elements in Djot.
#[napi(string_enum = "snake_case", js_name = "InlineType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsInlineType {
    /// Plain text run.
    Text,
    /// Bold / strong emphasis.
    Strong,
    /// Italic / regular emphasis.
    Emphasis,
    /// Highlighted text (marker pen).
    Highlight,
    /// Subscript text.
    Subscript,
    /// Superscript text.
    Superscript,
    /// Inserted text (tracked change).
    Insert,
    /// Deleted text (tracked change).
    Delete,
    /// Inline code span.
    Code,
    /// Hyperlink with URL.
    Link,
    /// Inline image reference.
    Image,
    /// Generic inline span with optional attributes.
    Span,
    /// Inline mathematical expression.
    Math,
    /// Raw inline content in a specified format.
    RawInline,
    /// Footnote reference marker.
    FootnoteRef,
    /// Named symbol or emoji shortcode.
    Symbol,
}

#[allow(clippy::derivable_impls)]
impl Default for JsInlineType {
    fn default() -> Self {
        Self::Text
    }
}

/// Controls how Jupyter notebook code cells are rendered during extraction.
///
/// A code cell carries both its **source** and any **outputs** that were saved in
/// the notebook. Callers ingesting notebooks for AI agents want different slices of
/// this depending on the task. Xberg never executes cells — `Outputs` and `Both`
/// only surface outputs already stored in the `.ipynb`.
///
/// This toggle governs a code cell's **source body** and its **saved outputs**.
/// Markdown (prose) cells and structural markers (kernel language, cell id, tags,
/// execution count) are unaffected — prose always renders and markers orient the
/// reader regardless of mode.
#[napi(string_enum = "lowercase", js_name = "JupyterCellRendering")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsJupyterCellRendering {
    /// Render the code source as a fenced code block; omit saved outputs.
    Source,
    /// Omit the code source; render only the saved cell outputs.
    Outputs,
    /// Render both the code source and the saved outputs (default; preserves the
    /// historical behavior).
    Both,
}

#[allow(clippy::derivable_impls)]
impl Default for JsJupyterCellRendering {
    fn default() -> Self {
        Self::Source
    }
}

/// Keyword algorithm selection.
#[napi(string_enum = "lowercase", js_name = "KeywordAlgorithm")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsKeywordAlgorithm {
    /// YAKE (Yet Another Keyword Extractor) - statistical approach
    #[cfg(feature = "keywords-yake")]
    Yake,
    /// RAKE (Rapid Automatic Keyword Extraction) - co-occurrence based
    #[cfg(feature = "keywords-rake")]
    Rake,
}

#[cfg(feature = "keywords-yake")]
#[allow(clippy::derivable_impls)]
impl Default for JsKeywordAlgorithm {
    fn default() -> Self {
        Self::Yake
    }
}
#[cfg(all(feature = "keywords-rake", not(any(feature = "keywords-yake"))))]
#[allow(clippy::derivable_impls)]
impl Default for JsKeywordAlgorithm {
    fn default() -> Self {
        Self::Rake
    }
}

/// Late-interaction model types supported by Xberg.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "LateInteractionModelType")]
pub struct JsLateInteractionModelType {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub name: Option<String>,
    #[napi(js_name = "modelId")]
    #[serde(rename = "modelId")]
    pub model_id: Option<String>,
    #[napi(js_name = "modelFile")]
    #[serde(rename = "modelFile")]
    pub model_file: Option<String>,
    #[napi(js_name = "additionalFiles")]
    #[serde(rename = "additionalFiles")]
    pub additional_files: Option<Vec<String>>,
    #[napi(js_name = "maxLength")]
    #[serde(rename = "maxLength")]
    pub max_length: Option<i64>,
}

impl Default for JsLateInteractionModelType {
    fn default() -> Self {
        Self {
            type_tag: "preset".to_string(),
            additional_files: None,
            max_length: None,
            model_file: None,
            model_id: None,
            name: None,
        }
    }
}

/// The 18 canonical document layout classes.
///
/// All model backends (RT-DETR, YOLO, etc.) map their native class IDs
/// to this shared set. Models with fewer classes (DocLayNet: 11, PubLayNet: 5)
/// map to the closest equivalent.
///
/// Wire format is snake_case in all serializers (JSON, TOML, YAML).
#[napi(string_enum = "snake_case", js_name = "LayoutClass")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsLayoutClass {
    /// Figure or table caption text.
    Caption,
    /// Chart or graph visualization.
    Chart,
    /// Footnote or endnote text.
    Footnote,
    /// Mathematical formula or equation.
    Formula,
    /// A single item in a bulleted or numbered list.
    ListItem,
    /// Running footer at the bottom of a page.
    PageFooter,
    /// Running header at the top of a page.
    PageHeader,
    /// Image, chart, or other graphical element.
    Picture,
    /// Section heading.
    SectionHeader,
    /// Data table.
    Table,
    /// Body text paragraph.
    Text,
    /// Document or chapter title.
    Title,
    /// Table of contents or index.
    DocumentIndex,
    /// Source code block.
    Code,
    /// Checkbox in selected state.
    CheckboxSelected,
    /// Checkbox in unselected state.
    CheckboxUnselected,
    /// Form field or form element.
    Form,
    /// Key-value pair region (e.g. label + value in a form).
    KeyValueRegion,
}

#[allow(clippy::derivable_impls)]
impl Default for JsLayoutClass {
    fn default() -> Self {
        Self::Caption
    }
}

/// Which PDF pages the layout model runs on.
///
/// Layout detection renders each selected page to a raster and runs ONNX
/// inference on it, which dominates extraction cost. This controls page
/// selection; `LayoutStrategy.Always` preserves the historical behavior of
/// running on every page. Wire format is snake_case in all serializers
/// (JSON, TOML, YAML).
#[napi(string_enum = "snake_case", js_name = "LayoutStrategy")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsLayoutStrategy {
    /// Run layout detection unconditionally on every page.
    Always,
    /// Pre-screen each page with cheap geometry signals and run the model
    /// only on pages likely to benefit (multi-column, table-bearing,
    /// figure-heavy, form-like, or rotated pages).
    ///
    /// Pages the pre-screen skips are processed exactly like pages where the
    /// model ran and found no regions. On the OCR path only inference is
    /// skipped; page rasters are still produced because OCR consumes them.
    /// For non-PDF inputs `Auto` behaves as `LayoutStrategy.Always`.
    Auto,
}

#[allow(clippy::derivable_impls)]
impl Default for JsLayoutStrategy {
    fn default() -> Self {
        Self::Always
    }
}

/// Link rendering style in Markdown output.
///
/// Controls whether links and images use inline `[text](url)` syntax or
/// reference-style `[text][1]` syntax with definitions collected at the end.
#[napi(string_enum, js_name = "LinkStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsLinkStyle {
    /// Inline links: `[text](url)`. Default.
    Inline,
    /// Reference-style links: `[text][1]` with `[1]: url` at end of document.
    Reference,
}

#[allow(clippy::derivable_impls)]
impl Default for JsLinkStyle {
    fn default() -> Self {
        Self::Inline
    }
}

/// Link type classification.
#[napi(string_enum = "lowercase", js_name = "LinkType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsLinkType {
    /// Anchor link (#section)
    Anchor,
    /// Internal link (same domain)
    Internal,
    /// External link (different domain)
    External,
    /// Email link (mailto:)
    Email,
    /// Phone link (tel:)
    Phone,
    /// Other link type
    Other,
}

#[allow(clippy::derivable_impls)]
impl Default for JsLinkType {
    fn default() -> Self {
        Self::Anchor
    }
}

/// List indentation character type.
///
/// Controls whether list items are indented with spaces or tabs.
#[napi(string_enum, js_name = "ListIndentType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsListIndentType {
    /// Use spaces for indentation. Default. Width controlled by `list_indent_width`.
    Spaces,
    /// Use tabs for indentation.
    Tabs,
}

#[allow(clippy::derivable_impls)]
impl Default for JsListIndentType {
    fn default() -> Self {
        Self::Spaces
    }
}

/// Type of list detection.
#[napi(string_enum, js_name = "ListType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsListType {
    /// Bullet points (-, *, •, etc.)
    Bullet,
    /// Numbered lists (1., 2., etc.)
    Numbered,
    /// Lettered lists (a., b., A., B., etc.)
    Lettered,
    /// Indented items
    Indented,
}

#[allow(clippy::derivable_impls)]
impl Default for JsListType {
    fn default() -> Self {
        Self::Bullet
    }
}

/// How partial results from multiple model calls (e.g. per page batch) are combined.
///
/// Canonical home for the merge strategy referenced by presets and by the
/// structured pipeline's post-processing. There is intentionally only one merge
/// type across the crate — do not introduce a second.
#[napi(string_enum = "snake_case", js_name = "MergeMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsMergeMode {
    /// Deep-merge JSON objects field by field (later calls fill missing fields).
    ObjectMerge,
    /// Concatenate top-level arrays across calls.
    ArrayConcat,
    /// Keep the first non-empty result; ignore subsequent calls.
    ObjectFirst,
}

#[allow(clippy::derivable_impls)]
impl Default for JsMergeMode {
    fn default() -> Self {
        Self::ObjectMerge
    }
}

/// Selects which evidence is authoritative when Xberg infers a MIME type.
#[napi(string_enum = "snake_case", js_name = "MimeDetectionPolicy")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsMimeDetectionPolicy {
    /// Prefer a supported content signature, falling back to the filename extension.
    PreferContent,
    /// Trust a supported filename extension without reading content for detection.
    ///
    /// # Security
    ///
    /// Filenames are attacker-controlled input in uploads and downloads. Use this only for trusted sources;
    /// otherwise a misleading extension can route arbitrary content to the wrong extractor.
    TrustExtension,
    /// Ignore the filename extension and require content-based detection.
    ContentOnly,
}

#[allow(clippy::derivable_impls)]
impl Default for JsMimeDetectionPolicy {
    fn default() -> Self {
        Self::PreferContent
    }
}

/// NER backend selector.
#[napi(string_enum = "snake_case", js_name = "NerBackendKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsNerBackendKind {
    /// `xberg-gliner` ONNX inference. Requires `ner-onnx` feature. Models
    /// download lazily from `xberg-io/gliner-models`.
    Onnx,
    /// liter-llm zero-shot NER via structured-output prompts. Requires `ner-llm`
    /// feature. Useful when domain-specific categories outstrip the ONNX taxonomy.
    Llm,
}

#[allow(clippy::derivable_impls)]
impl Default for JsNerBackendKind {
    fn default() -> Self {
        Self::Onnx
    }
}

/// Line break syntax in Markdown output.
///
/// Controls how soft line breaks (from `<br>` or line breaks in source) are rendered.
#[napi(string_enum, js_name = "NewlineStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsNewlineStyle {
    /// Two trailing spaces at end of line. Default. Standard Markdown syntax.
    Spaces,
    /// Backslash at end of line. Alternative Markdown syntax.
    Backslash,
}

#[allow(clippy::derivable_impls)]
impl Default for JsNewlineStyle {
    fn default() -> Self {
        Self::Spaces
    }
}

/// Reason for not chunking a document.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "NoChunkingReason")]
pub struct JsNoChunkingReason {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    #[napi(js_name = "sizeBytes")]
    #[serde(rename = "sizeBytes")]
    pub size_bytes: Option<i64>,
    #[napi(js_name = "thresholdBytes")]
    #[serde(rename = "thresholdBytes")]
    pub threshold_bytes: Option<i64>,
    #[napi(js_name = "pageCount")]
    #[serde(rename = "pageCount")]
    pub page_count: Option<u32>,
    pub threshold: Option<u32>,
    #[napi(js_name = "textCoverage")]
    #[serde(rename = "textCoverage")]
    pub text_coverage: Option<f64>,
    #[napi(js_name = "avgCharsPerPage")]
    #[serde(rename = "avgCharsPerPage")]
    pub avg_chars_per_page: Option<u32>,
    #[napi(js_name = "mimeType")]
    #[serde(rename = "mimeType")]
    pub mime_type: Option<String>,
}

impl Default for JsNoChunkingReason {
    fn default() -> Self {
        Self {
            type_tag: "ChunkingDisabled".to_string(),
            avg_chars_per_page: None,
            mime_type: None,
            page_count: None,
            size_bytes: None,
            text_coverage: None,
            threshold: None,
            threshold_bytes: None,
        }
    }
}

/// Tagged enum for node content. Each variant carries only type-specific data.
///
/// Uses `#[serde(tag = "node_type")]` to avoid "type" keyword collision in
/// Go/Java/TypeScript bindings.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "NodeContent")]
pub struct JsNodeContent {
    #[napi(js_name = "node_type")]
    #[serde(rename = "node_type")]
    pub node_type_tag: String,
    pub text: Option<String>,
    pub level: Option<u8>,
    pub ordered: Option<bool>,
    pub grid: Option<JsTableGrid>,
    pub description: Option<String>,
    #[napi(js_name = "imageIndex")]
    #[serde(rename = "imageIndex")]
    pub image_index: Option<u32>,
    pub src: Option<String>,
    pub language: Option<String>,
    pub label: Option<String>,
    #[napi(js_name = "headingLevel")]
    #[serde(rename = "headingLevel")]
    pub heading_level: Option<u8>,
    #[napi(js_name = "headingText")]
    #[serde(rename = "headingText")]
    pub heading_text: Option<String>,
    pub number: Option<u32>,
    pub title: Option<String>,
    pub term: Option<String>,
    pub definition: Option<String>,
    pub key: Option<String>,
    pub kind: Option<String>,
    pub format: Option<String>,
    pub content: Option<String>,
    pub entries: Option<Vec<JsKeyValueAttribute>>,
}

impl Default for JsNodeContent {
    fn default() -> Self {
        Self {
            node_type_tag: "title".to_string(),
            content: None,
            definition: None,
            description: None,
            entries: None,
            format: None,
            grid: None,
            heading_level: None,
            heading_text: None,
            image_index: None,
            key: None,
            kind: None,
            label: None,
            language: None,
            level: None,
            number: None,
            ordered: None,
            src: None,
            term: None,
            text: None,
            title: None,
        }
    }
}

/// OCR backend types.
#[napi(string_enum, js_name = "OcrBackendType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsOcrBackendType {
    /// Tesseract OCR (native Rust binding)
    Tesseract,
    /// PaddleOCR (Python-based, via FFI)
    PaddleOCR,
    /// Candle-based VLM OCR (TrOCR, PaddleOCR-VL).
    Candle,
    /// Name-selected built-in or third-party OCR backend.
    Custom,
}

#[allow(clippy::derivable_impls)]
impl Default for JsOcrBackendType {
    fn default() -> Self {
        Self::Tesseract
    }
}

/// Bounding geometry for an OCR element.
///
/// Supports both axis-aligned rectangles (from Tesseract) and 4-point quadrilaterals
/// (from PaddleOCR and rotated text detection).
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrBoundingGeometry")]
pub struct JsOcrBoundingGeometry {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub left: Option<u32>,
    pub top: Option<u32>,
    pub width: Option<u32>,
    pub height: Option<u32>,
    pub points: Option<Vec<JsOcrPoint>>,
}

impl Default for JsOcrBoundingGeometry {
    fn default() -> Self {
        Self {
            type_tag: "rectangle".to_string(),
            height: None,
            left: None,
            points: None,
            top: None,
            width: None,
        }
    }
}

/// Hierarchical level of an OCR element.
///
/// Maps to Tesseract's page segmentation hierarchy and provides
/// equivalent semantics for PaddleOCR.
#[napi(string_enum = "snake_case", js_name = "OcrElementLevel")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsOcrElementLevel {
    /// Individual word
    Word,
    /// Line of text (default for PaddleOCR)
    Line,
    /// Paragraph or text block
    Block,
    /// Page-level element
    Page,
}

#[allow(clippy::derivable_impls)]
impl Default for JsOcrElementLevel {
    fn default() -> Self {
        Self::Word
    }
}

/// Which pages of a PDF get OCR'd when neither `force_ocr` nor `force_ocr_pages` applies.
///
/// # Examples
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "OcrStrategy")]
pub struct JsOcrStrategy {
    #[napi(js_name = "mode")]
    #[serde(rename = "mode")]
    pub mode_tag: String,
    #[napi(js_name = "minConfidence")]
    #[serde(rename = "minConfidence")]
    pub min_confidence: Option<f64>,
}

impl Default for JsOcrStrategy {
    fn default() -> Self {
        Self {
            mode_tag: "auto".to_string(),
            min_confidence: None,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(transparent)]
pub struct JsOutputFormat(pub serde_json::Value);

impl napi::bindgen_prelude::TypeName for JsOutputFormat {
    fn type_name() -> &'static str {
        "JsOutputFormat"
    }
    fn value_type() -> napi::ValueType {
        napi::ValueType::Unknown
    }
}

impl napi::bindgen_prelude::FromNapiValue for JsOutputFormat {
    unsafe fn from_napi_value(env: napi::sys::napi_env, val: napi::sys::napi_value) -> napi::Result<Self> {
        let v: serde_json::Value = unsafe { napi::bindgen_prelude::FromNapiValue::from_napi_value(env, val)? };
        Ok(Self(v))
    }
}

impl napi::bindgen_prelude::ToNapiValue for JsOutputFormat {
    unsafe fn to_napi_value(env: napi::sys::napi_env, val: Self) -> napi::Result<napi::sys::napi_value> {
        unsafe { napi::bindgen_prelude::ToNapiValue::to_napi_value(env, val.0) }
    }
}

impl napi::bindgen_prelude::ValidateNapiValue for JsOutputFormat {}

/// Page Segmentation Mode for Tesseract OCR.
#[napi(string_enum, js_name = "PSMMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPSMMode {
    /// Orientation and script detection only.
    OsdOnly,
    /// Automatic page segmentation with OSD.
    AutoOsd,
    /// Automatic page segmentation without OSD or OCR.
    AutoOnly,
    /// Fully automatic page segmentation with no OSD (default).
    Auto,
    /// Assume a single column of text of variable sizes.
    SingleColumn,
    /// Assume a single uniform block of vertically aligned text.
    SingleBlockVertical,
    /// Assume a single uniform block of text.
    SingleBlock,
    /// Treat the image as a single text line.
    SingleLine,
    /// Treat the image as a single word.
    SingleWord,
    /// Treat the image as a single word in a circle.
    CircleWord,
    /// Treat the image as a single character.
    SingleChar,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPSMMode {
    fn default() -> Self {
        Self::OsdOnly
    }
}

/// Which concrete ONNX inference engine PaddleOCR model loading uses.
///
/// Mirrors `sceptre.Backend` for the PaddleOCR backend: `Ort` is the native,
/// full-featured path (acceleration/execution-provider hook, ONNX-embedded
/// dictionary metadata); `Tract` is the pure-Rust, CPU-only path used on targets
/// where `ort` cannot link (Android x86_64 emulator, WASM once wired).
#[napi(string_enum = "snake_case", js_name = "PaddleInferenceBackend")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPaddleInferenceBackend {
    /// Native ONNX Runtime (requires the `paddle-ocr-ort` feature).
    Ort,
    /// Pure-Rust ONNX via `tract` (requires the `paddle-ocr-tract` feature).
    Tract,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPaddleInferenceBackend {
    fn default() -> Self {
        Self::Ort
    }
}

/// Supported languages in PaddleOCR.
///
/// Maps user-friendly language codes to paddle-ocr-rs language identifiers.
#[napi(string_enum, js_name = "PaddleLanguage")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPaddleLanguage {
    /// English
    English,
    /// Simplified Chinese
    Chinese,
    /// Japanese
    Japanese,
    /// Korean
    Korean,
    /// German
    German,
    /// French
    French,
    /// Latin script (covers most European languages)
    Latin,
    /// Cyrillic (Russian and related)
    Cyrillic,
    /// Traditional Chinese
    TraditionalChinese,
    /// Thai
    Thai,
    /// Greek
    Greek,
    /// East Slavic (Russian, Ukrainian, Belarusian)
    EastSlavic,
    /// Arabic (Arabic, Persian, Urdu)
    Arabic,
    /// Devanagari (Hindi, Marathi, Sanskrit, Nepali)
    Devanagari,
    /// Tamil
    Tamil,
    /// Telugu
    Telugu,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPaddleLanguage {
    fn default() -> Self {
        Self::English
    }
}

/// Task accepted by `candle-paddleocr-vl` backend options.
#[napi(string_enum = "lowercase", js_name = "PaddleOcrVlTaskKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPaddleOcrVlTaskKind {
    Ocr,
    Table,
    Formula,
    Chart,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPaddleOcrVlTaskKind {
    fn default() -> Self {
        Self::Ocr
    }
}

/// How a backend copes with a page raster whose text is not upright.
///
/// Rotated-page handling is a backend capability, not a universal guarantee. An A/B run this
/// session against `/Rotate 270` scanned pages showed the three handled cases genuinely differ:
/// Tesseract reconstructs correct reading order on a sideways raster outright; PaddleOCR
/// recognises the rotated text correctly (it warps each detected quad upright before running
/// recognition) but leaves its block list in raw raster `(y, x)` order, so the caller must
/// reorder; sceptre produces character garbage on the same sideways raster and only reads
/// correctly once the page is rendered upright first. A caller that skips an upright-render step
/// for a backend that actually needs one gets silent garbage, not an error.
///
/// # Only one variant is discriminated (#657)
///
/// Read this before "simplifying" the type. There is exactly one decision point in the
/// codebase that inspects this value: `upright_raster_for_backend`
/// (`crate.extractors.pdf.ocr`), which tests `orientation_handling != RequiresUpright` and
/// otherwise does nothing. Every other mention forwards the value to that test. So, *to that
/// codepath*, `SelfCorrecting` and `RecognisesRotatedText` are behaviourally identical — the
/// enum is a boolean at the point of use, and the three variants describe measured backend
/// behaviour rather than three dispatch paths.
///
/// `RecognisesRotatedText`'s actual remedy is not this enum. The block-order fix is the
/// `backend_options["page_rotation_degrees"]` hint injected by
/// `ocr_config_with_page_rotation_hint` (`crate.extractors.pdf.ocr`) **unconditionally, for
/// every backend**, which `PaddleOcrBackend.process_image` reads back
/// (`page_rotation_degrees_from_backend_options` -> `residual_rotation_for_reorder` ->
/// `reorder_blocks_for_page_rotation`, `crate.paddle_ocr.backend`) and applies internally.
/// Declaring `RecognisesRotatedText` therefore changes nothing on its own; a backend in that
/// class must also read the hint. Conversely, gating that hint on this enum would remove a
/// field from Tesseract's `OcrConfig` and hence from the OCR cache key
/// (`hash(image + language + config)`), invalidating every cached page — do not do it without
/// its own A/B.
///
/// # PDF-route-only
///
/// Only the PDF OCR routes call `OcrBackend.page_orientation_handling` (the `--force-ocr`
/// route via `extract_with_ocr` and the scanned-pages route via `extract_mixed_ocr_native`).
/// The raw-image route (`crate.extractors.image`) never calls it: there is no `/Rotate` to
/// consult, and orientation there is handled by the PP-LCNet document-orientation classifier
/// (`crate.doc_orientation`) gated on `OcrConfig.auto_rotate`.
///
/// # Cost of the default
///
/// The trait default is `RequiresUpright` (deliberately the least capable option, see
/// `OcrBackend.page_orientation_handling`). A backend that does not declare therefore pays,
/// on every page with `/Rotate != 0`, a re-encode plus rotation of the raster in
/// `upright_raster_for_backend` and a bounding-box round-trip back through
/// `undo_upright_raster_correction`. That is the safe direction to be wrong in, but it is not
/// free, and for a backend that never got measured it is not known to be necessary either.
#[napi(string_enum, js_name = "PageOrientationHandling")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPageOrientationHandling {
    /// Reconstructs reading order regardless of page rotation — safe to hand a
    /// raster in any orientation.
    SelfCorrecting,
    /// Recognises rotated text correctly but emits blocks in raw raster order,
    /// so the caller must reorder.
    RecognisesRotatedText,
    /// Requires an upright raster; rotated text produces garbage.
    RequiresUpright,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPageOrientationHandling {
    fn default() -> Self {
        Self::SelfCorrecting
    }
}

/// Type of paginated unit in a document.
///
/// Distinguishes between different types of "pages" (PDF pages, presentation slides, spreadsheet sheets).
#[napi(string_enum = "snake_case", js_name = "PageUnitType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPageUnitType {
    /// Standard document pages (PDF, DOCX, images)
    Page,
    /// Presentation slides (PPTX, ODP)
    Slide,
    /// Spreadsheet sheets (XLSX, ODS)
    Sheet,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPageUnitType {
    fn default() -> Self {
        Self::Page
    }
}

/// Type of PDF annotation.
#[napi(string_enum = "snake_case", js_name = "PdfAnnotationType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPdfAnnotationType {
    /// Sticky note / text annotation
    Text,
    /// Highlighted text region
    Highlight,
    /// Hyperlink annotation
    Link,
    /// Rubber stamp annotation
    Stamp,
    /// Underline text markup
    Underline,
    /// Strikeout text markup
    StrikeOut,
    /// Squiggly (wavy) underline text markup
    Squiggly,
    /// Freehand drawing (ink) annotation
    Ink,
    /// Rectangle/box shape annotation
    Square,
    /// Ellipse/oval shape annotation
    Circle,
    /// Closed polygon shape annotation
    Polygon,
    /// Open polyline shape annotation
    PolyLine,
    /// Line annotation
    Line,
    /// Caret (text-insertion marker) annotation
    Caret,
    /// Embedded file attachment annotation
    FileAttachment,
    /// Embedded sound annotation
    Sound,
    /// Embedded movie annotation
    Movie,
    /// Any other annotation type
    Other,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPdfAnnotationType {
    fn default() -> Self {
        Self::Text
    }
}

/// PDF extraction backend selection.
///
/// Controls which engine parses and renders PDF documents. Wire format is
/// snake_case in all serializers (JSON, TOML, YAML). Defaults to
/// `PdfBackend.Native` -- selecting anything else never changes behavior
/// for a caller who does not opt in.
#[napi(string_enum = "snake_case", js_name = "PdfBackend")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPdfBackend {
    /// xberg's own pure-Rust PDF engine (default), `crates/xberg-native-pdf`.
    Native,
    /// pdfium -- Google's PDFium engine, gated behind the `pdf-pdfium` Cargo
    /// feature (#700 added the selection level; #702 added the extraction
    /// engine, `extractors.pdf.pdfium_engine`, deliberately smaller in scope
    /// than `Native` -- see that module's doc comment for exactly what it
    /// extracts). A build without the `pdf-pdfium` feature rejects this at the
    /// CLI validation layer rather than silently falling back to `Native`.
    /// A build *with* the feature still enforces the selection at extraction
    /// time (`extractors.pdf.PdfExtractor.extract_core`), because that is
    /// the one dispatch point every caller -- CLI, library use, API/MCP
    /// servers, language bindings -- passes through; see that function's doc
    /// comment for why the enforcement lives there and not only in CLI
    /// validation.
    Pdfium,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPdfBackend {
    fn default() -> Self {
        Self::Native
    }
}

/// PII categories the pattern engine recognises.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "PiiCategory")]
pub struct JsPiiCategory {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub custom: Option<String>,
}

impl Default for JsPiiCategory {
    fn default() -> Self {
        Self {
            type_tag: "email".to_string(),
            custom: None,
        }
    }
}

/// HTML preprocessing aggressiveness level.
///
/// Controls the extent of cleanup performed before conversion. Higher levels remove more elements.
#[napi(string_enum, js_name = "PreprocessingPreset")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPreprocessingPreset {
    /// Minimal cleanup. Remove only essential noise (scripts, styles).
    Minimal,
    /// Standard cleanup. Default. Removes navigation, forms, and other auxiliary content.
    Standard,
    /// Aggressive cleanup. Remove extensive non-content elements and structure.
    Aggressive,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPreprocessingPreset {
    fn default() -> Self {
        Self::Minimal
    }
}

/// High-level category used to group presets in the registry UI.
#[napi(string_enum = "snake_case", js_name = "PresetCategory")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsPresetCategory {
    /// Invoices, receipts, statements, purchase orders, W-9.
    Finance,
    /// Passports, drivers licenses, insurance cards.
    Identity,
    /// Contracts, NDAs, agreements.
    Legal,
    /// Bills of lading, customs declarations, packing lists.
    Logistics,
    /// Clinical records, lab reports.
    Medical,
    /// Pay stubs, resumes, employment offers.
    Hr,
    /// Catch-all for documents that don't fit the other categories.
    Other,
}

#[allow(clippy::derivable_impls)]
impl Default for JsPresetCategory {
    fn default() -> Self {
        Self::Finance
    }
}

/// Outcome of a single doctor check.
#[napi(string_enum = "lowercase", js_name = "ProbeStatus")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsProbeStatus {
    /// The backend or setting will work as configured.
    Pass,
    /// The check ran and found something actionable, but nothing is broken
    /// (e.g. stray cache files, stale model revisions). Never fails the report.
    Warn,
    /// The configured setup will not work (or will silently degrade) on this host.
    Fail,
    /// The check cannot run locally (e.g. model not cached, feature not compiled in);
    /// first real use decides, possibly after a download.
    Skip,
}

#[allow(clippy::derivable_impls)]
impl Default for JsProbeStatus {
    fn default() -> Self {
        Self::Pass
    }
}

/// Processing stages for post-processors.
///
/// Post-processors are executed in stage order (Early → Middle → Late).
/// Use stages to control the order of post-processing operations.
#[napi(string_enum, js_name = "ProcessingStage")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsProcessingStage {
    /// Early stage - foundational processing.
    ///
    /// Use for:
    /// - Language detection
    /// - Character encoding normalization
    /// - Entity extraction (NER)
    /// - Text quality scoring
    Early,
    /// Middle stage - content transformation.
    ///
    /// Use for:
    /// - Keyword extraction
    /// - Token reduction
    /// - Text summarization
    /// - Semantic analysis
    Middle,
    /// Late stage - final enrichment.
    ///
    /// Use for:
    /// - Custom user hooks
    /// - Analytics/logging
    /// - Final validation
    /// - Output formatting
    Late,
}

#[allow(clippy::derivable_impls)]
impl Default for JsProcessingStage {
    fn default() -> Self {
        Self::Early
    }
}

/// Strategy applied when a PII match is rewritten.
#[napi(string_enum = "snake_case", js_name = "RedactionStrategy")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsRedactionStrategy {
    /// Replace the matched span with a fixed mask token (default `"[REDACTED]"`).
    Mask,
    /// Replace with a SHA-256 hash of the original value (truncated to 16 hex chars).
    /// Lets downstream consumers do equality joins without recovering the source.
    Hash,
    /// Replace with a per-category running token (`"[PERSON_1]"`, `"[PERSON_2]"`, …)
    /// so the same person referenced twice gets the same token within the document.
    TokenReplace,
    /// Delete the matched span entirely.
    Drop,
}

#[allow(clippy::derivable_impls)]
impl Default for JsRedactionStrategy {
    fn default() -> Self {
        Self::Mask
    }
}

/// Intensity level for the token-reduction pipeline.
#[napi(string_enum, js_name = "ReductionLevel")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsReductionLevel {
    /// No reduction applied; text is returned as-is.
    Off,
    /// Remove only the most common stopwords.
    Light,
    /// Balanced stopword removal and redundancy filtering.
    Moderate,
    /// Aggressive filtering; may remove less common content words.
    Aggressive,
    /// Maximum compression; prioritizes brevity over completeness.
    Maximum,
}

#[allow(clippy::derivable_impls)]
impl Default for JsReductionLevel {
    fn default() -> Self {
        Self::Off
    }
}

/// Classification of a detected layout region that warrants VLM extraction.
#[napi(string_enum, js_name = "RegionKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsRegionKind {
    /// A figure, diagram, chart, or image region.
    Figure,
    /// A densely formatted or complex table.
    DenseTable,
    /// A region with complex or mixed layout.
    ComplexLayout,
    /// A standalone image to caption.
    Caption,
}

#[allow(clippy::derivable_impls)]
impl Default for JsRegionKind {
    fn default() -> Self {
        Self::Figure
    }
}

/// Semantic kind of a relationship between document elements.
#[napi(string_enum = "snake_case", js_name = "RelationshipKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsRelationshipKind {
    /// Footnote marker -> footnote definition.
    FootnoteReference,
    /// Citation marker -> bibliography entry.
    CitationReference,
    /// Internal anchor link (`#id`) -> target heading/element.
    InternalLink,
    /// Caption paragraph -> figure/table it describes.
    Caption,
    /// Label -> labeled element (HTML `<label for>`, LaTeX `\label{}`).
    Label,
    /// TOC entry -> target section.
    TocEntry,
    /// Cross-reference (LaTeX `\ref{}`, DOCX cross-reference field).
    CrossReference,
}

#[allow(clippy::derivable_impls)]
impl Default for JsRelationshipKind {
    fn default() -> Self {
        Self::FootnoteReference
    }
}

/// Selects how a local ONNX reranker's raw output tensor is turned into a score.
///
/// - `RerankerHead.CrossEncoder` — classic single-logit cross-encoder head:
///   the model emits `[batch, 1]` (or `[batch]`) logits; the caller applies
///   sigmoid to get a `[0, 1]` score. This is the original, unchanged path.
/// - `RerankerHead.Qwen3Generative` — Qwen3 generative-reranker head: the
///   model emits `[batch, seq, vocab]` logits; the score is `P("yes")` read
///   from the last token's logits over the "yes"/"no" vocabulary entries,
///   via a softmax over those two logits. Already a `[0, 1]` probability —
///   no sigmoid is applied.
#[napi(string_enum = "snake_case", js_name = "RerankerHead")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsRerankerHead {
    /// Single-logit cross-encoder head (sigmoid applied by the caller).
    CrossEncoder,
    /// Qwen3 generative-reranker head (softmax over yes/no token logits).
    Qwen3Generative,
}

#[allow(clippy::derivable_impls)]
impl Default for JsRerankerHead {
    fn default() -> Self {
        Self::CrossEncoder
    }
}

/// Reranker model types supported by Xberg.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RerankerModelType")]
pub struct JsRerankerModelType {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub name: Option<String>,
    #[napi(js_name = "modelId")]
    #[serde(rename = "modelId")]
    pub model_id: Option<String>,
    #[napi(js_name = "modelFile")]
    #[serde(rename = "modelFile")]
    pub model_file: Option<String>,
    #[napi(js_name = "additionalFiles")]
    #[serde(rename = "additionalFiles")]
    pub additional_files: Option<Vec<String>>,
    #[napi(js_name = "maxLength")]
    #[serde(rename = "maxLength")]
    pub max_length: Option<i64>,
    pub head: Option<JsRerankerHead>,
    pub llm: Option<JsLlmConfig>,
}

impl Default for JsRerankerModelType {
    fn default() -> Self {
        Self {
            type_tag: "preset".to_string(),
            additional_files: None,
            head: None,
            llm: None,
            max_length: None,
            model_file: None,
            model_id: None,
            name: None,
        }
    }
}

/// Result-shape selection for extraction results.
///
/// Distinct from `OutputFormat` (which controls rendering — Plain, Markdown,
/// HTML, etc.). `ResultFormat` controls the *shape* of the result: a unified content
/// blob vs. an element-based decomposition.
#[napi(string_enum = "snake_case", js_name = "ResultFormat")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsResultFormat {
    /// Unified format with all content in `content` field
    Unified,
    /// Element-based format with semantic element extraction
    ElementBased,
}

#[allow(clippy::derivable_impls)]
impl Default for JsResultFormat {
    fn default() -> Self {
        Self::Unified
    }
}

/// Best-effort document location for a revision.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "RevisionAnchor")]
pub struct JsRevisionAnchor {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub index: Option<i64>,
    pub row: Option<i64>,
    pub col: Option<i64>,
    #[napi(js_name = "tableIndex")]
    #[serde(rename = "tableIndex")]
    pub table_index: Option<i64>,
    pub name: Option<String>,
}

impl Default for JsRevisionAnchor {
    fn default() -> Self {
        Self {
            type_tag: "paragraph".to_string(),
            col: None,
            index: None,
            name: None,
            row: None,
            table_index: None,
        }
    }
}

/// Semantic classification of a tracked change.
#[napi(string_enum = "snake_case", js_name = "RevisionKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsRevisionKind {
    /// Text or content was inserted.
    Insertion,
    /// Text or content was deleted.
    Deletion,
    /// Run-level formatting (font, size, colour, …) was changed.
    FormatChange,
    /// A reviewer comment or annotation.
    Comment,
}

#[allow(clippy::derivable_impls)]
impl Default for JsRevisionKind {
    fn default() -> Self {
        Self::Insertion
    }
}

/// Schema-validation outcome surfaced as one of three buckets.
///
/// Fold into the combined confidence score without leaking internal validation
/// error types.
#[napi(string_enum = "snake_case", js_name = "SchemaCompliance")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsSchemaCompliance {
    /// Every batch validated against the schema.
    AllValid,
    /// At least one batch validated; at least one did not.
    PartialValid,
    /// No batch validated.
    AllInvalid,
}

#[allow(clippy::derivable_impls)]
impl Default for JsSchemaCompliance {
    fn default() -> Self {
        Self::AllValid
    }
}

/// Sparse-embedding model types supported by Xberg.
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "SparseEmbeddingModelType")]
pub struct JsSparseEmbeddingModelType {
    #[napi(js_name = "type")]
    #[serde(rename = "type")]
    pub type_tag: String,
    pub name: Option<String>,
    #[napi(js_name = "modelId")]
    #[serde(rename = "modelId")]
    pub model_id: Option<String>,
    #[napi(js_name = "modelFile")]
    #[serde(rename = "modelFile")]
    pub model_file: Option<String>,
    #[napi(js_name = "additionalFiles")]
    #[serde(rename = "additionalFiles")]
    pub additional_files: Option<Vec<String>>,
    #[napi(js_name = "maxLength")]
    #[serde(rename = "maxLength")]
    pub max_length: Option<i64>,
}

impl Default for JsSparseEmbeddingModelType {
    fn default() -> Self {
        Self {
            type_tag: "preset".to_string(),
            additional_files: None,
            max_length: None,
            model_file: None,
            model_id: None,
            name: None,
        }
    }
}

/// Structured data type classification.
#[napi(string_enum = "lowercase", js_name = "StructuredDataType")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsStructuredDataType {
    /// JSON-LD structured data
    #[napi(value = "json-ld")]
    JsonLd,
    /// Microdata
    Microdata,
    /// RDFa
    #[napi(value = "rdfa")]
    RDFa,
}

#[allow(clippy::derivable_impls)]
impl Default for JsStructuredDataType {
    fn default() -> Self {
        Self::JsonLd
    }
}

/// Summarisation strategy.
#[napi(string_enum = "snake_case", js_name = "SummaryStrategy")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsSummaryStrategy {
    /// Pure-Rust extractive summary (TextRank over the chunk graph). Deterministic,
    /// fast, no external service required.
    Extractive,
    /// Abstractive summary produced by liter-llm. Requires `liter-llm` feature and
    /// a configured `LlmConfig`. Token usage is captured in
    /// `ExtractedDocument.llm_usage`.
    Abstractive,
}

#[allow(clippy::derivable_impls)]
impl Default for JsSummaryStrategy {
    fn default() -> Self {
        Self::Extractive
    }
}

/// Controls how markdown tables are handled when they exceed the chunk size limit.
///
/// Only applies when `chunker_type` is `Markdown`.
///
/// # Variants
///
/// * `Split` - Default behavior: tables are split at row boundaries like any
///   other block element. Continuation chunks contain only data rows without
///   the header, which can break downstream consumers that need column context.
/// * `RepeatHeader` - Prepend the table header (header row + separator row) to
///   every continuation chunk that contains data rows from the same table.
///   Adds a small amount of duplicate text but ensures each chunk is
///   self-contained for extraction, search, and LLM consumption.
#[napi(string_enum = "snake_case", js_name = "TableChunkingMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsTableChunkingMode {
    /// Split tables at row boundaries (default). Continuation chunks have no header.
    Split,
    /// Prepend the table header to every chunk that continues a split table.
    RepeatHeader,
}

#[allow(clippy::derivable_impls)]
impl Default for JsTableChunkingMode {
    fn default() -> Self {
        Self::Split
    }
}

/// Which table structure recognition model to use.
///
/// Controls the model used for table cell detection within layout-detected
/// table regions. Wire format is snake_case in all serializers (JSON, TOML,
/// YAML).
#[napi(string_enum = "snake_case", js_name = "TableModel")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsTableModel {
    /// TATR (Table Transformer) -- default, 30MB, DETR-based row/column detection.
    Tatr,
    /// SLANeXT wired variant -- 365MB, optimized for bordered tables.
    SlanetWired,
    /// SLANeXT wireless variant -- 365MB, optimized for borderless tables.
    SlanetWireless,
    /// SLANet-plus -- 7.78MB, lightweight general-purpose.
    SlanetPlus,
    /// Classifier-routed SLANeXT: auto-select wired/wireless per table.
    /// Uses PP-LCNet classifier (6.78MB) + both SLANeXT variants (730MB total).
    SlanetAuto,
    /// Disable table structure model inference entirely; use heuristic path only.
    Disabled,
}

#[allow(clippy::derivable_impls)]
impl Default for JsTableModel {
    fn default() -> Self {
        Self::Tatr
    }
}

/// How to resolve overlapping native vs layout (TATR/SLANeXT) tables.
///
/// When both native detection and the layout table model produce a table for
/// the same page region, one must be dropped. This controls which one wins. Wire
/// format is snake_case in all serializers (JSON, TOML, YAML).
#[napi(string_enum = "snake_case", js_name = "TableOverlapPreference")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsTableOverlapPreference {
    /// Keep whichever table carries more content (cell count + markdown length).
    /// This is the historical default. TATR/SLANeXT tables usually recognize more
    /// cells and therefore win, which maximizes table-structure F1 but can lower
    /// text F1 when the recognized cell reflow diverges from the source reading order.
    Content,
    /// Prefer the native table when it overlaps a layout table. Native tables
    /// preserve the source reading order, which scores higher on text F1 for
    /// documents where the layout model's cell reflow diverges from the ground truth.
    Native,
    /// Prefer the layout (TATR/SLANeXT) table when it overlaps a native table.
    Layout,
}

#[allow(clippy::derivable_impls)]
impl Default for JsTableOverlapPreference {
    fn default() -> Self {
        Self::Content
    }
}

/// Text direction enumeration for HTML documents.
#[napi(string_enum = "lowercase", js_name = "TextDirection")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsTextDirection {
    /// Left-to-right text direction
    #[napi(value = "ltr")]
    LeftToRight,
    /// Right-to-left text direction
    #[napi(value = "rtl")]
    RightToLeft,
    /// Automatic text direction detection
    #[napi(value = "auto")]
    Auto,
}

#[allow(clippy::derivable_impls)]
impl Default for JsTextDirection {
    fn default() -> Self {
        Self::LeftToRight
    }
}

/// Controls which conversion tier is used.
#[napi(string_enum = "snake_case", js_name = "TierStrategy")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsTierStrategy {
    /// Automatically pick the best tier for the input (default).
    ///
    /// Runs the classifier against the prescan report and uses Tier-1 when
    /// eligible; falls back to Tier-2 on bail or when the classifier routes
    /// to Tier-2.
    Auto,
    /// Always use the Tier-2 (`tl.parse` + walk) path, skipping Tier-1.
    Tier2,
}

#[allow(clippy::derivable_impls)]
impl Default for JsTierStrategy {
    fn default() -> Self {
        Self::Auto
    }
}

/// Semantic classification of an extracted URI.
#[napi(string_enum = "snake_case", js_name = "UriKind")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsUriKind {
    /// A clickable hyperlink (web URL, file link).
    Hyperlink,
    /// An image or media resource reference.
    Image,
    /// An internal anchor or cross-reference target.
    Anchor,
    /// A citation or bibliographic reference (DOI, academic ref).
    Citation,
    /// A general reference (e.g. `\ref{}` in LaTeX, `:ref:` in RST).
    Reference,
    /// An email address (`mailto:` link or bare email).
    Email,
}

#[allow(clippy::derivable_impls)]
impl Default for JsUriKind {
    fn default() -> Self {
        Self::Hyperlink
    }
}

/// URL encoding strategy for link and image destinations.
///
/// Controls how special characters in URL destinations are handled when they
/// require escaping to produce valid Markdown.
///
/// The `Angle` variant (default) wraps the destination in angle brackets:
/// `[text](<url with spaces>)`. This is the CommonMark-specified escape hatch
/// but breaks when the URL itself contains `>`.
///
/// The `Percent` variant percent-encodes every character that is not an RFC 3986
/// unreserved character or `/`, producing a destination safe for all Markdown
/// parsers: `[text](url%20with%20spaces)`.
#[napi(string_enum, js_name = "UrlEscapeStyle")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsUrlEscapeStyle {
    /// Wrap destinations that contain spaces or newlines in angle brackets. Default.
    Angle,
    /// Percent-encode all characters that are not RFC 3986 unreserved or `/`.
    Percent,
}

#[allow(clippy::derivable_impls)]
impl Default for JsUrlEscapeStyle {
    fn default() -> Self {
        Self::Angle
    }
}

/// URL extraction mode.
#[napi(string_enum = "snake_case", js_name = "UrlExtractionMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsUrlExtractionMode {
    /// Classify HTTP(S) resources after fetch.
    Auto,
    /// Treat the URI as a single remote document/page.
    Document,
    /// Crawl from the seed URI and extract discovered pages/documents.
    Crawl,
}

#[allow(clippy::derivable_impls)]
impl Default for JsUrlExtractionMode {
    fn default() -> Self {
        Self::Auto
    }
}

/// Policy controlling when VLM (Vision Language Model) OCR is used as a fallback.
///
/// This knob is syntactic sugar over the explicit `OcrPipelineConfig` stage
/// ordering. When `vlm_fallback` is set and `pipeline` is `None`, an equivalent
/// pipeline is synthesised at extraction time:
///
/// - `VlmFallbackPolicy.Disabled` — no synthesis; single-backend mode (default).
/// - `VlmFallbackPolicy.OnLowQuality` — tries the classical backend first; if the
///   result scores below `quality_threshold`, tries VLM.
/// - `VlmFallbackPolicy.Always` — skips the classical backend and sends every page
///   to the VLM.
///
/// When `OcrConfig.pipeline` is explicitly set, `vlm_fallback` is ignored — the
/// explicit pipeline takes precedence.
///
/// # Errors
///
/// Both `OnLowQuality` and `Always` require `OcrConfig.vlm_config` to be `Some`.
/// Constructing an `OcrConfig` with one of these policies but no `vlm_config` is
/// detected by `OcrConfig.validate` and will surface as a
/// `Validation` error at extraction time, not a panic.
///
/// # Example
#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[napi(object, js_name = "VlmFallbackPolicy")]
pub struct JsVlmFallbackPolicy {
    #[napi(js_name = "mode")]
    #[serde(rename = "mode")]
    pub mode_tag: String,
    #[napi(js_name = "qualityThreshold")]
    #[serde(rename = "qualityThreshold")]
    pub quality_threshold: Option<f64>,
}

impl Default for JsVlmFallbackPolicy {
    fn default() -> Self {
        Self {
            mode_tag: "disabled".to_string(),
            quality_threshold: None,
        }
    }
}

/// Supported Whisper model sizes.
///
/// These map to published ONNX exports on Hugging Face (onnx-community or
/// similar orgs). The actual filenames and repos are resolved inside the
/// transcription engine.
#[napi(string_enum = "snake_case", js_name = "WhisperModel")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsWhisperModel {
    /// Smallest, fastest, lowest quality. Good default for development and CI.
    Tiny,
    /// Reasonable quality/speed tradeoff.
    Base,
    /// Better accuracy with higher memory and cache use.
    Small,
    /// High quality; slower and more memory-intensive.
    Medium,
    /// Best quality (large-v3). Use only when latency and memory use are acceptable.
    LargeV3,
}

#[allow(clippy::derivable_impls)]
impl Default for JsWhisperModel {
    fn default() -> Self {
        Self::Tiny
    }
}

/// Whitespace handling strategy during conversion.
///
/// Determines how sequences of whitespace characters (spaces, tabs, newlines) are processed.
#[napi(string_enum, js_name = "WhitespaceMode")]
#[derive(Clone, serde::Serialize, serde::Deserialize)]
pub enum JsWhitespaceMode {
    /// Collapse multiple whitespace characters to single spaces. Default. Matches browser behavior.
    Normalized,
    /// Preserve all whitespace exactly as it appears in the HTML.
    Strict,
}

#[allow(clippy::derivable_impls)]
impl Default for JsWhitespaceMode {
    fn default() -> Self {
        Self::Normalized
    }
}

/// Classify a document's chunks and return the updated document.
///
/// This owned form preserves the mutations when the document crosses a language-binding boundary.
/// Rust callers that already own a mutable document can use `classify_chunks` to avoid moving it.
///
/// # Errors
///
/// Returns the same validation and LLM errors as `classify_chunks`.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "classifyChunksOwned")]
pub async fn classify_chunks_owned(
    result: Option<JsExtractedDocument>,
    config: Option<JsChunkClassificationConfig>,
) -> Result<JsExtractedDocument> {
    let result_core: xberg::ExtractedDocument = result.unwrap_or_default().into();

    let config_core: xberg::ChunkClassificationConfig = config.unwrap_or_default().into();

    let result = xberg::text::classification::classify_chunks_owned(result_core, &config_core)
        .await
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    Ok(result.into())
}

/// Probe the backends and settings in `config` and report what will actually
/// execute on this host.
///
/// Runs no downloads and no billable API calls. Backends that are not compiled
/// in or whose models are not cached report `Skip` rather than failing.
#[napi]
pub fn doctor(config: Option<JsExtractionConfig>) -> JsDoctorReport {
    let config_core: xberg::ExtractionConfig = config.unwrap_or_default().into();

    xberg::doctor(&config_core).into()
}

/// Extract content from a single bytes or URI input.
#[allow(clippy::missing_errors_doc)]
#[napi]
pub async fn extract(input: Option<JsExtractInput>, config: Option<JsExtractionConfig>) -> Result<JsExtractionResult> {
    let input_core: xberg::ExtractInput = input.unwrap_or_default().into();

    let config_core: xberg::ExtractionConfig = config.unwrap_or_default().into();

    let result = xberg::extract(input_core, &config_core)
        .await
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    Ok(result.into())
}

/// Extract content from multiple bytes or URI inputs.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "extractBatch")]
pub async fn extract_batch(
    inputs: Vec<JsExtractInput>,
    config: Option<JsExtractionConfig>,
) -> Result<JsExtractionResult> {
    let inputs_core: Vec<_> = inputs.into_iter().map(Into::into).collect();

    let config_core: xberg::ExtractionConfig = config.unwrap_or_default().into();

    let result = xberg::extract_batch(inputs_core, &config_core)
        .await
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    Ok(result.into())
}

#[cfg(feature = "markdown-footnotes")]
/// Find unmarked claims in markdown text.
///
/// Returns lines that assert a claim but carry neither a footnote citation anchor (`[^...]`)
/// nor an inference marker (`[*inference*]`).
///
/// The heuristic is simple: a line that contains alphabetic words, ends with sentence punctuation,
/// and is not a heading, blank line, or markup-only line is considered a claim.
/// Exclude lines that appear in the citation block (after `---` + `<!-- citations ... -->`).
///
/// # Arguments
///
/// * `markdown` - The markdown text to search
///
/// # Returns
///
/// A vector of trimmed line text strings for unmarked claims.
///
/// # Examples
#[napi(js_name = "findUnmarkedClaims")]
pub fn find_unmarked_claims(markdown: String) -> Vec<String> {
    xberg::find_unmarked_claims(&markdown)
}

/// List names of all registered document extractors.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listDocumentExtractors")]
pub fn list_document_extractors() -> Result<Vec<String>> {
    xberg::list_document_extractors().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List the names of all registered embedding backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listEmbeddingBackends")]
pub fn list_embedding_backends() -> Result<Vec<String>> {
    xberg::list_embedding_backends().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List all registered OCR backends.
///
/// Returns the names of all OCR backends currently registered in the global registry.
///
/// # Returns
///
/// A vector of OCR backend names.
///
/// # Example
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listOcrBackends")]
pub fn list_ocr_backends() -> Result<Vec<String>> {
    xberg::list_ocr_backends().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List all registered post-processor names.
///
/// Returns a vector of all post-processor names currently registered in the
/// global registry.
///
/// # Returns
///
/// - `Ok(Vec<String>)` - Vector of post-processor names
/// - `Err(...)` if the registry lock is poisoned
///
/// # Example
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listPostProcessors")]
pub fn list_post_processors() -> Result<Vec<String>> {
    xberg::list_post_processors().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List names of all registered renderers.
///
/// # Errors
///
/// Returns an error if the registry lock is poisoned.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listRenderers")]
pub fn list_renderers() -> Result<Vec<String>> {
    xberg::list_renderers().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List the names of all registered reranker backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listRerankerBackends")]
pub fn list_reranker_backends() -> Result<Vec<String>> {
    xberg::list_reranker_backends().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List all supported document formats.
///
/// Returns every file extension Xberg recognizes together with its
/// corresponding MIME type, derived from the central format registry.
/// Formats that have no registered file extension (such as source code,
/// which is detected dynamically) are not included.
///
/// The static `EXT_TO_MIME` table lists every format the *codebase* knows how
/// to describe, regardless of which Cargo features were compiled in. Advertising
/// that table directly would claim support for extractors that may not exist in
/// this build (see GH#1387). To keep the advertised catalogue honest, the table
/// is intersected with the document extractor registry: an extension is only
/// included if some registered extractor actually claims its MIME type in this
/// build. This can never drift from reality and automatically covers
/// third-party extractors registered at runtime.
///
/// The list is sorted alphabetically by file extension.
///
/// # Returns
///
/// A vector of `SupportedFormat` entries sorted by extension, limited to
/// formats with a registered extractor in this build.
///
/// # Example
#[napi(js_name = "listSupportedFormats")]
pub fn list_supported_formats() -> Vec<JsSupportedFormat> {
    xberg::list_supported_formats().into_iter().map(Into::into).collect()
}

/// List the names of all registered tokenizer backends.
///
/// Used by `xberg-cli`, the api/mcp endpoints, and generated language
/// bindings.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listTokenizerBackends")]
pub fn list_tokenizer_backends() -> Result<Vec<String>> {
    xberg::list_tokenizer_backends().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

/// List names of all registered validators.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "listValidators")]
pub fn list_validators() -> Result<Vec<String>> {
    xberg::list_validators().map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))
}

#[cfg(feature = "url-ingestion")]
/// Discover all pages and sitemaps reachable from `uri` without extracting document content.
///
/// Builds a `crawlberg.CrawlEngine` from `config.crawl`, calls
/// `CrawlEngine.map`, and returns the set of discovered URLs as a
/// `crawlberg.MapResult` (re-exported as `MapResult`).
///
/// Use this when you need the URL inventory of a site before committing to
/// full document extraction — e.g. to build a crawl queue or validate scope.
///
/// # Errors
///
/// Returns `Validation` if the crawl configuration fails
/// validation or if the map operation itself fails.
#[allow(clippy::missing_errors_doc)]
#[napi(js_name = "mapUrl")]
pub async fn map_url(uri: String, config: Option<JsUrlExtractionConfig>) -> Result<JsMapResult> {
    let config_core: xberg::UrlExtractionConfig = config.unwrap_or_default().into();

    let result = xberg::map_url(&uri, &config_core)
        .await
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, e.to_string()))?;
    Ok(result.into())
}

#[cfg(any(
    any(feature = "late-interaction-presets", feature = "late-interaction"),
    feature = "late-interaction-presets"
))]
/// Rank a set of documents against a query by MaxSim score, descending.
///
/// Mirrors the sort/truncate shape of `crate.reranking`'s `build_results`,
/// minus top-k truncation (callers slice the returned `Vec` themselves).
///
/// Pure CPU primitive — available without ONNX Runtime.
#[napi(js_name = "maxSimRank")]
pub fn max_sim_rank(query: JsMultiVectorEmbedding, docs: Vec<JsMultiVectorEmbedding>) -> Vec<JsLateInteractionMatch> {
    let query_core: xberg::MultiVectorEmbedding = query.into();

    let docs_core: Vec<_> = docs.into_iter().map(Into::into).collect();

    xberg::max_sim_rank(&query_core, &docs_core)
        .into_iter()
        .map(Into::into)
        .collect()
}

#[cfg(any(
    any(feature = "late-interaction-presets", feature = "late-interaction"),
    feature = "late-interaction-presets"
))]
/// Score a query against a document using ColBERT's MaxSim operator: for each
/// query token vector, take the maximum dot product against any document
/// token vector, then sum across query tokens.
///
/// Returns `0.0` if `query` and `doc` have mismatched dimensionality, if either
/// has zero tokens, or if either is not well-formed per
/// `MultiVectorEmbedding.is_well_formed` (its `data` length does not match
/// `num_tokens * dim`).
///
/// Pure CPU primitive — available without ONNX Runtime.
#[napi(js_name = "maxSimScore")]
pub fn max_sim_score(query: JsMultiVectorEmbedding, doc: JsMultiVectorEmbedding) -> f64 {
    let query_core: xberg::MultiVectorEmbedding = query.into();

    let doc_core: xberg::MultiVectorEmbedding = doc.into();

    xberg::max_sim_score(&query_core, &doc_core)
}

#[cfg(feature = "markdown-footnotes")]
/// Verify that an excerpt appears verbatim in source text.
///
/// Performs exact matching by default. Also tries whitespace-normalized matching
/// (collapsing runs of whitespace on both sides) since PDF-extracted text often
/// has irregular spacing.
///
/// # Arguments
///
/// * `excerpt` - The text snippet to find
/// * `source_text` - The full source text to search
///
/// # Returns
///
/// `true` if the excerpt appears (exactly or with normalized whitespace), `false` otherwise.
///
/// # Examples
#[napi(js_name = "verifyExcerpt")]
pub fn verify_excerpt(excerpt: String, source_text: String) -> bool {
    xberg::verify_excerpt(&excerpt, &source_text)
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsOcrBackendBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
    has_process_image_file: bool,
    has_supported_languages: bool,
    has_supports_table_detection: bool,
    has_supports_document_processing: bool,
    has_emits_structured_markdown: bool,
    has_confidence_semantics: bool,
    has_page_orientation_handling: bool,
    has_process_document: bool,
}

impl std::fmt::Debug for JsOcrBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsOcrBackendBridge")
    }
}

impl JsOcrBackendBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("processImage").unwrap_or(false)
            && !js_obj.has_named_property("process_image").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "processImage"),
            ));
        }
        if !js_obj.has_named_property("supportsLanguage").unwrap_or(false)
            && !js_obj.has_named_property("supports_language").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "supportsLanguage"),
            ));
        }
        if !js_obj.has_named_property("backendType").unwrap_or(false)
            && !js_obj.has_named_property("backend_type").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "backendType"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
            has_process_image_file: js_obj.has_named_property("processImageFile").unwrap_or(false)
                || js_obj.has_named_property("process_image_file").unwrap_or(false),
            has_supported_languages: js_obj.has_named_property("supportedLanguages").unwrap_or(false)
                || js_obj.has_named_property("supported_languages").unwrap_or(false),
            has_supports_table_detection: js_obj.has_named_property("supportsTableDetection").unwrap_or(false)
                || js_obj.has_named_property("supports_table_detection").unwrap_or(false),
            has_supports_document_processing: js_obj.has_named_property("supportsDocumentProcessing").unwrap_or(false)
                || js_obj
                    .has_named_property("supports_document_processing")
                    .unwrap_or(false),
            has_emits_structured_markdown: js_obj.has_named_property("emitsStructuredMarkdown").unwrap_or(false)
                || js_obj.has_named_property("emits_structured_markdown").unwrap_or(false),
            has_confidence_semantics: js_obj.has_named_property("confidenceSemantics").unwrap_or(false)
                || js_obj.has_named_property("confidence_semantics").unwrap_or(false),
            has_page_orientation_handling: js_obj.has_named_property("pageOrientationHandling").unwrap_or(false)
                || js_obj.has_named_property("page_orientation_handling").unwrap_or(false),
            has_process_document: js_obj.has_named_property("processDocument").unwrap_or(false)
                || js_obj.has_named_property("process_document").unwrap_or(false),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsOcrBackendBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsOcrBackendBridge {}
unsafe impl Sync for JsOcrBackendBridge {}

impl xberg::plugins::Plugin for JsOcrBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridge {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("processImage")
            .or_else(|_| __obj.get_named_property("process_image"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(&format!("{:?}", image_bytes)) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsOcrConfig::from((*config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'process_image' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                    .map_err(|e| {
                        xberg::XbergError::Other(format!(
                            "Failed to extract return value from method 'process_image': {}",
                            e
                        ))
                    })?;
                // null/empty is a protocol error for Result-returning methods: the JS
                // implementation must always return a serialisable value, never null.
                // Avoid requiring Default on return types that are intentionally
                // excluded from bindings.
                match s.as_str() {
                    "" | "null" => Err(xberg::XbergError::Other(
                        "Failed to parse return value for method 'process_image'".to_string(),
                    )),
                    _ => serde_json::from_str::<_>(&s).map_err(|_| {
                        xberg::XbergError::Other("Failed to parse return value for method 'process_image'".to_string())
                    }),
                }
            }
        }
    }

    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        if !(self.has_process_image_file) {
            return JsOcrBackendBridgeDefaultProcessImageFile(self)
                .process_image_file(path, config)
                .await;
        }
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("processImageFile")
            .or_else(|_| __obj.get_named_property("process_image_file"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(&format!("{:?}", path)) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsOcrConfig::from((*config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'process_image_file' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                    .map_err(|e| {
                        xberg::XbergError::Other(format!(
                            "Failed to extract return value from method 'process_image_file': {}",
                            e
                        ))
                    })?;
                // null/empty is a protocol error for Result-returning methods: the JS
                // implementation must always return a serialisable value, never null.
                // Avoid requiring Default on return types that are intentionally
                // excluded from bindings.
                match s.as_str() {
                    "" | "null" => Err(xberg::XbergError::Other(
                        "Failed to parse return value for method 'process_image_file'".to_string(),
                    )),
                    _ => serde_json::from_str::<_>(&s).map_err(|_| {
                        xberg::XbergError::Other(
                            "Failed to parse return value for method 'process_image_file'".to_string(),
                        )
                    }),
                }
            }
        }
    }

    fn supports_language(&self, lang: &str) -> bool {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown,)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("supportsLanguage")
            .or_else(|_| __obj.get_named_property("supports_language"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(lang) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
        )));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsLanguage", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsLanguage", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsLanguage", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn backend_type(&self) -> xberg::OcrBackendType {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("backendType")
            .or_else(|_| __obj.get_named_property("backend_type"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "backendType", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "backendType", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "backendType", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn supported_languages(&self) -> Vec<String> {
        if !(self.has_supported_languages) {
            return JsOcrBackendBridgeDefaultSupportedLanguages(self).supported_languages();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("supportedLanguages")
            .or_else(|_| __obj.get_named_property("supported_languages"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportedLanguages", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportedLanguages", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportedLanguages", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn supports_table_detection(&self) -> bool {
        if !(self.has_supports_table_detection) {
            return JsOcrBackendBridgeDefaultSupportsTableDetection(self).supports_table_detection();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("supportsTableDetection")
            .or_else(|_| __obj.get_named_property("supports_table_detection"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsTableDetection", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsTableDetection", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsTableDetection", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn supports_document_processing(&self) -> bool {
        if !(self.has_supports_document_processing) {
            return JsOcrBackendBridgeDefaultSupportsDocumentProcessing(self).supports_document_processing();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("supportsDocumentProcessing")
            .or_else(|_| __obj.get_named_property("supports_document_processing"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsDocumentProcessing", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsDocumentProcessing", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "supportsDocumentProcessing", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn emits_structured_markdown(&self) -> bool {
        if !(self.has_emits_structured_markdown) {
            return JsOcrBackendBridgeDefaultEmitsStructuredMarkdown(self).emits_structured_markdown();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("emitsStructuredMarkdown")
            .or_else(|_| __obj.get_named_property("emits_structured_markdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "emitsStructuredMarkdown", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "emitsStructuredMarkdown", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "emitsStructuredMarkdown", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        if !(self.has_confidence_semantics) {
            return JsOcrBackendBridgeDefaultConfidenceSemantics(self).confidence_semantics();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("confidenceSemantics")
            .or_else(|_| __obj.get_named_property("confidence_semantics"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "confidenceSemantics", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "confidenceSemantics", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "confidenceSemantics", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        if !(self.has_page_orientation_handling) {
            return JsOcrBackendBridgeDefaultPageOrientationHandling(self).page_orientation_handling();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("pageOrientationHandling")
            .or_else(|_| __obj.get_named_property("page_orientation_handling"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsOcrBackendBridge", method = "pageOrientationHandling", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsOcrBackendBridge", method = "pageOrientationHandling", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsOcrBackendBridge", method = "pageOrientationHandling", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        if !(self.has_process_document) {
            return JsOcrBackendBridgeDefaultProcessDocument(self)
                .process_document(_path, _config)
                .await;
        }
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("processDocument")
            .or_else(|_| __obj.get_named_property("process_document"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(&format!("{:?}", _path)) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsOcrConfig::from((*_config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'process_document' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                    .map_err(|e| {
                        xberg::XbergError::Other(format!(
                            "Failed to extract return value from method 'process_document': {}",
                            e
                        ))
                    })?;
                // null/empty is a protocol error for Result-returning methods: the JS
                // implementation must always return a serialisable value, never null.
                // Avoid requiring Default on return types that are intentionally
                // excluded from bindings.
                match s.as_str() {
                    "" | "null" => Err(xberg::XbergError::Other(
                        "Failed to parse return value for method 'process_document'".to_string(),
                    )),
                    _ => serde_json::from_str::<_>(&s).map_err(|_| {
                        xberg::XbergError::Other(
                            "Failed to parse return value for method 'process_document'".to_string(),
                        )
                    }),
                }
            }
        }
    }
}

/// Runs `process_image_file`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultProcessImageFile<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultProcessImageFile<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultProcessImageFile<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supported_languages`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultSupportedLanguages<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultSupportedLanguages<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultSupportedLanguages<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supports_table_detection`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultSupportsTableDetection<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultSupportsTableDetection<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultSupportsTableDetection<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `supports_document_processing`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultSupportsDocumentProcessing<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultSupportsDocumentProcessing<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultSupportsDocumentProcessing<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `emits_structured_markdown`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultEmitsStructuredMarkdown<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultEmitsStructuredMarkdown<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultEmitsStructuredMarkdown<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `confidence_semantics`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultConfidenceSemantics<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultConfidenceSemantics<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultConfidenceSemantics<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `page_orientation_handling`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultPageOrientationHandling<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultPageOrientationHandling<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultPageOrientationHandling<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    async fn process_document(
        &self,
        _path: &std::path::Path,
        _config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_document(self.0, _path, _config).await
    }
}

/// Runs `process_document`'s Rust default body for [`JsOcrBackendBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsOcrBackendBridgeDefaultProcessDocument<'a>(&'a JsOcrBackendBridge);
impl xberg::plugins::Plugin for JsOcrBackendBridgeDefaultProcessDocument<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::OcrBackend for JsOcrBackendBridgeDefaultProcessDocument<'_> {
    async fn process_image(
        &self,
        image_bytes: &[u8],
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image(self.0, image_bytes, config).await
    }
    async fn process_image_file(
        &self,
        path: &std::path::Path,
        config: &xberg::OcrConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::OcrBackend::process_image_file(self.0, path, config).await
    }
    fn supports_language(&self, lang: &str) -> bool {
        xberg::OcrBackend::supports_language(self.0, lang)
    }
    fn backend_type(&self) -> xberg::OcrBackendType {
        xberg::OcrBackend::backend_type(self.0)
    }
    fn supported_languages(&self) -> Vec<String> {
        xberg::OcrBackend::supported_languages(self.0)
    }
    fn supports_table_detection(&self) -> bool {
        xberg::OcrBackend::supports_table_detection(self.0)
    }
    fn supports_document_processing(&self) -> bool {
        xberg::OcrBackend::supports_document_processing(self.0)
    }
    fn emits_structured_markdown(&self) -> bool {
        xberg::OcrBackend::emits_structured_markdown(self.0)
    }
    fn confidence_semantics(&self) -> xberg::ConfidenceSemantics {
        xberg::OcrBackend::confidence_semantics(self.0)
    }
    fn page_orientation_handling(&self) -> xberg::PageOrientationHandling {
        xberg::OcrBackend::page_orientation_handling(self.0)
    }
}

#[napi]
pub fn register_ocr_backend(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsOcrBackendBridge::new(obj)?;
    let arc: Arc<dyn xberg::OcrBackend> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_ocr_backend_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterOcrBackend")]
pub fn _alef_unregister_ocr_backend(name: String) -> napi::Result<()> {
    xberg::plugins::ocr_backend::unregister_ocr_backend(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearOcrBackends")]
pub fn _alef_clear_ocr_backends() -> napi::Result<()> {
    xberg::plugins::ocr_backend::clear_ocr_backends()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsPostProcessorBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
    has_should_process: bool,
    has_estimated_duration_ms: bool,
    has_priority: bool,
}

impl std::fmt::Debug for JsPostProcessorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsPostProcessorBridge")
    }
}

impl JsPostProcessorBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("process").unwrap_or(false)
            && !js_obj.has_named_property("process").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "process"),
            ));
        }
        if !js_obj.has_named_property("processingStage").unwrap_or(false)
            && !js_obj.has_named_property("processing_stage").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "processingStage"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
            has_should_process: js_obj.has_named_property("shouldProcess").unwrap_or(false)
                || js_obj.has_named_property("should_process").unwrap_or(false),
            has_estimated_duration_ms: js_obj.has_named_property("estimatedDurationMs").unwrap_or(false)
                || js_obj.has_named_property("estimated_duration_ms").unwrap_or(false),
            has_priority: js_obj.has_named_property("priority").unwrap_or(false)
                || js_obj.has_named_property("priority").unwrap_or(false),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsPostProcessorBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsPostProcessorBridge {}
unsafe impl Sync for JsPostProcessorBridge {}

impl xberg::plugins::Plugin for JsPostProcessorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsPostProcessorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsPostProcessorBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsPostProcessorBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::PostProcessor for JsPostProcessorBridge {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("process")
            .or_else(|_| __obj.get_named_property("process"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractedDocument::from((*result).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractionConfig::from((*config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'process' failed: {}",
                cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn processing_stage(&self) -> xberg::ProcessingStage {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("processingStage")
            .or_else(|_| __obj.get_named_property("processing_stage"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsPostProcessorBridge", method = "processingStage", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsPostProcessorBridge", method = "processingStage", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsPostProcessorBridge", method = "processingStage", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        if !(self.has_should_process) {
            return JsPostProcessorBridgeDefaultShouldProcess(self).should_process(_result, _config);
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("shouldProcess")
            .or_else(|_| __obj.get_named_property("should_process"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractedDocument::from((*_result).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractionConfig::from((*_config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsPostProcessorBridge", method = "shouldProcess", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsPostProcessorBridge", method = "shouldProcess", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsPostProcessorBridge", method = "shouldProcess", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        if !(self.has_estimated_duration_ms) {
            return JsPostProcessorBridgeDefaultEstimatedDurationMs(self).estimated_duration_ms(_result);
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown,)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("estimatedDurationMs")
            .or_else(|_| __obj.get_named_property("estimated_duration_ms"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((unsafe {
            let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                self.env().raw(),
                JsExtractedDocument::from((*_result).clone()),
            )
            .unwrap_or(std::ptr::null_mut());
            napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
        },)));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsPostProcessorBridge", method = "estimatedDurationMs", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsPostProcessorBridge", method = "estimatedDurationMs", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsPostProcessorBridge", method = "estimatedDurationMs", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn priority(&self) -> i32 {
        if !(self.has_priority) {
            return JsPostProcessorBridgeDefaultPriority(self).priority();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("priority")
            .or_else(|_| __obj.get_named_property("priority"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsPostProcessorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsPostProcessorBridge", method = "priority", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsPostProcessorBridge", method = "priority", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }
}

/// Runs `should_process`'s Rust default body for [`JsPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsPostProcessorBridgeDefaultShouldProcess<'a>(&'a JsPostProcessorBridge);
impl xberg::plugins::Plugin for JsPostProcessorBridgeDefaultShouldProcess<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for JsPostProcessorBridgeDefaultShouldProcess<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        xberg::PostProcessor::estimated_duration_ms(self.0, _result)
    }
    fn priority(&self) -> i32 {
        xberg::PostProcessor::priority(self.0)
    }
}

/// Runs `estimated_duration_ms`'s Rust default body for [`JsPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsPostProcessorBridgeDefaultEstimatedDurationMs<'a>(&'a JsPostProcessorBridge);
impl xberg::plugins::Plugin for JsPostProcessorBridgeDefaultEstimatedDurationMs<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for JsPostProcessorBridgeDefaultEstimatedDurationMs<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::PostProcessor::should_process(self.0, _result, _config)
    }
    fn priority(&self) -> i32 {
        xberg::PostProcessor::priority(self.0)
    }
}

/// Runs `priority`'s Rust default body for [`JsPostProcessorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsPostProcessorBridgeDefaultPriority<'a>(&'a JsPostProcessorBridge);
impl xberg::plugins::Plugin for JsPostProcessorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::PostProcessor for JsPostProcessorBridgeDefaultPriority<'_> {
    async fn process(
        &self,
        result: &mut xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::PostProcessor::process(self.0, result, config).await
    }
    fn processing_stage(&self) -> xberg::ProcessingStage {
        xberg::PostProcessor::processing_stage(self.0)
    }
    fn should_process(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::PostProcessor::should_process(self.0, _result, _config)
    }
    fn estimated_duration_ms(&self, _result: &xberg::ExtractedDocument) -> u64 {
        xberg::PostProcessor::estimated_duration_ms(self.0, _result)
    }
}

#[napi]
pub fn register_post_processor(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsPostProcessorBridge::new(obj)?;
    let arc: Arc<dyn xberg::PostProcessor> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_post_processor_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterPostProcessor")]
pub fn _alef_unregister_post_processor(name: String) -> napi::Result<()> {
    xberg::plugins::post_processor::unregister_post_processor(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearPostProcessors")]
pub fn _alef_clear_post_processors() -> napi::Result<()> {
    xberg::plugins::post_processor::clear_post_processors()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsValidatorBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
    has_should_validate: bool,
    has_priority: bool,
}

impl std::fmt::Debug for JsValidatorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsValidatorBridge")
    }
}

impl JsValidatorBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("validate").unwrap_or(false)
            && !js_obj.has_named_property("validate").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "validate"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
            has_should_validate: js_obj.has_named_property("shouldValidate").unwrap_or(false)
                || js_obj.has_named_property("should_validate").unwrap_or(false),
            has_priority: js_obj.has_named_property("priority").unwrap_or(false)
                || js_obj.has_named_property("priority").unwrap_or(false),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsValidatorBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsValidatorBridge {}
unsafe impl Sync for JsValidatorBridge {}

impl xberg::plugins::Plugin for JsValidatorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsValidatorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsValidatorBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsValidatorBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::Validator for JsValidatorBridge {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("validate")
            .or_else(|_| __obj.get_named_property("validate"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractedDocument::from((*result).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractionConfig::from((*config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'validate' failed: {}",
                cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn should_validate(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        if !(self.has_should_validate) {
            return JsValidatorBridgeDefaultShouldValidate(self).should_validate(_result, _config);
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("shouldValidate")
            .or_else(|_| __obj.get_named_property("should_validate"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractedDocument::from((*_result).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractionConfig::from((*_config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsValidatorBridge", method = "shouldValidate", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsValidatorBridge", method = "shouldValidate", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsValidatorBridge", method = "shouldValidate", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn priority(&self) -> i32 {
        if !(self.has_priority) {
            return JsValidatorBridgeDefaultPriority(self).priority();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("priority")
            .or_else(|_| __obj.get_named_property("priority"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsValidatorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsValidatorBridge", method = "priority", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsValidatorBridge", method = "priority", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }
}

/// Runs `should_validate`'s Rust default body for [`JsValidatorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsValidatorBridgeDefaultShouldValidate<'a>(&'a JsValidatorBridge);
impl xberg::plugins::Plugin for JsValidatorBridgeDefaultShouldValidate<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::Validator for JsValidatorBridgeDefaultShouldValidate<'_> {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::Validator::validate(self.0, result, config).await
    }
    fn priority(&self) -> i32 {
        xberg::Validator::priority(self.0)
    }
}

/// Runs `priority`'s Rust default body for [`JsValidatorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsValidatorBridgeDefaultPriority<'a>(&'a JsValidatorBridge);
impl xberg::plugins::Plugin for JsValidatorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::Validator for JsValidatorBridgeDefaultPriority<'_> {
    async fn validate(
        &self,
        result: &xberg::ExtractedDocument,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<(), xberg::XbergError> {
        xberg::Validator::validate(self.0, result, config).await
    }
    fn should_validate(&self, _result: &xberg::ExtractedDocument, _config: &xberg::ExtractionConfig) -> bool {
        xberg::Validator::should_validate(self.0, _result, _config)
    }
}

#[napi]
pub fn register_validator(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsValidatorBridge::new(obj)?;
    let arc: Arc<dyn xberg::Validator> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_validator_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterValidator")]
pub fn _alef_unregister_validator(name: String) -> napi::Result<()> {
    xberg::plugins::validator::unregister_validator(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearValidators")]
pub fn _alef_clear_validators() -> napi::Result<()> {
    xberg::plugins::validator::clear_validators()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsDocumentExtractorBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
    has_priority: bool,
    has_can_handle: bool,
}

impl std::fmt::Debug for JsDocumentExtractorBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsDocumentExtractorBridge")
    }
}

impl JsDocumentExtractorBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("extract").unwrap_or(false)
            && !js_obj.has_named_property("extract").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "extract"),
            ));
        }
        if !js_obj.has_named_property("supportedMimeTypes").unwrap_or(false)
            && !js_obj.has_named_property("supported_mime_types").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "supportedMimeTypes"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
            has_priority: js_obj.has_named_property("priority").unwrap_or(false)
                || js_obj.has_named_property("priority").unwrap_or(false),
            has_can_handle: js_obj.has_named_property("canHandle").unwrap_or(false)
                || js_obj.has_named_property("can_handle").unwrap_or(false),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsDocumentExtractorBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsDocumentExtractorBridge {}
unsafe impl Sync for JsDocumentExtractorBridge {}

impl xberg::plugins::Plugin for JsDocumentExtractorBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::DocumentExtractor for JsDocumentExtractorBridge {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("extract")
            .or_else(|_| __obj.get_named_property("extract"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            unsafe {
                let r =
                    napi::bindgen_prelude::ToNapiValue::to_napi_value(self.env().raw(), JsExtractInput::from(input))
                        .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                    self.env().raw(),
                    JsExtractionConfig::from((*config).clone()),
                )
                .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'extract' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                    .map_err(|e| {
                        xberg::XbergError::Other(format!("Failed to extract return value from method 'extract': {}", e))
                    })?;
                // null/empty is a protocol error for Result-returning methods: the JS
                // implementation must always return a serialisable value, never null.
                // Avoid requiring Default on return types that are intentionally
                // excluded from bindings.
                match s.as_str() {
                    "" | "null" => Err(xberg::XbergError::Other(
                        "Failed to parse return value for method 'extract'".to_string(),
                    )),
                    _ => serde_json::from_str::<_>(&s).map_err(|_| {
                        xberg::XbergError::Other("Failed to parse return value for method 'extract'".to_string())
                    }),
                }
            }
        }
    }

    fn supported_mime_types(&self) -> &[&str] {
        let __types: Vec<String> = {
            let __env = self.env();
            let __obj = match self.obj(&__env) {
                Ok(o) => o,
                Err(_) => return Default::default(),
            };
            let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
                .get_named_property("supportedMimeTypes")
                .or_else(|_| __obj.get_named_property("supported_mime_types"))
            {
                Ok(f) => f,
                Err(_) => return Default::default(),
            };

            let result = func.call(());

            match result {
                // This method's Rust signature is infallible, so a JS exception cannot
                // propagate. Log before substituting the default — a silent default is
                // indistinguishable from a real result to the caller.
                Err(e) => {
                    tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "supportedMimeTypes", error = %e, "host callback threw; returning default");
                    Default::default()
                }
                Ok(val) => {
                    // Convert JS value to Rust type via string coercion
                    let s = match val
                        .coerce_to_string()
                        .and_then(|s| s.into_utf8())
                        .and_then(|s| s.into_owned())
                    {
                        Ok(s) => s,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "supportedMimeTypes", error = %e, "host returned a value that could not be coerced to a string; returning default");
                            return Default::default();
                        }
                    };
                    match s.as_str() {
                        // null/undefined is a legitimate "no value" answer for infallible
                        // returns (mirrors the extendr backend) — no log.
                        "" | "null" => Default::default(),
                        _ => match serde_json::from_str::<_>(&s) {
                            Ok(v) => v,
                            Err(e) => {
                                tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "supportedMimeTypes", error = %e, "host returned an unparseable value; returning default");
                                Default::default()
                            }
                        },
                    }
                }
            }
        };
        let __strs: Vec<&'static str> = __types
            .into_iter()
            .map(|s| -> &'static str { Box::leak(s.into_boxed_str()) })
            .collect();
        Box::leak(__strs.into_boxed_slice())
    }

    fn priority(&self) -> i32 {
        if !(self.has_priority) {
            return JsDocumentExtractorBridgeDefaultPriority(self).priority();
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("priority")
            .or_else(|_| __obj.get_named_property("priority"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "priority", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "priority", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "priority", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn can_handle(&self, _path: &std::path::Path, _mime_type: &str) -> bool {
        if !(self.has_can_handle) {
            return JsDocumentExtractorBridgeDefaultCanHandle(self).can_handle(_path, _mime_type);
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("canHandle")
            .or_else(|_| __obj.get_named_property("can_handle"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(&format!("{:?}", _path)) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
            match self.env().create_string(_mime_type) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
        )));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "canHandle", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "canHandle", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsDocumentExtractorBridge", method = "canHandle", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }
}

/// Runs `priority`'s Rust default body for [`JsDocumentExtractorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsDocumentExtractorBridgeDefaultPriority<'a>(&'a JsDocumentExtractorBridge);
impl xberg::plugins::Plugin for JsDocumentExtractorBridgeDefaultPriority<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::DocumentExtractor for JsDocumentExtractorBridgeDefaultPriority<'_> {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::DocumentExtractor::extract(self.0, input, config).await
    }
    fn supported_mime_types(&self) -> &[&str] {
        xberg::DocumentExtractor::supported_mime_types(self.0)
    }
    fn can_handle(&self, _path: &std::path::Path, _mime_type: &str) -> bool {
        xberg::DocumentExtractor::can_handle(self.0, _path, _mime_type)
    }
}

/// Runs `can_handle`'s Rust default body for [`JsDocumentExtractorBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsDocumentExtractorBridgeDefaultCanHandle<'a>(&'a JsDocumentExtractorBridge);
impl xberg::plugins::Plugin for JsDocumentExtractorBridgeDefaultCanHandle<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
#[async_trait::async_trait]
impl xberg::DocumentExtractor for JsDocumentExtractorBridgeDefaultCanHandle<'_> {
    async fn extract(
        &self,
        input: xberg::ExtractInput,
        config: &xberg::ExtractionConfig,
    ) -> std::result::Result<xberg::ExtractedDocument, xberg::XbergError> {
        xberg::DocumentExtractor::extract(self.0, input, config).await
    }
    fn supported_mime_types(&self) -> &[&str] {
        xberg::DocumentExtractor::supported_mime_types(self.0)
    }
    fn priority(&self) -> i32 {
        xberg::DocumentExtractor::priority(self.0)
    }
}

#[napi]
pub fn register_document_extractor(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsDocumentExtractorBridge::new(obj)?;
    let arc: Arc<dyn xberg::DocumentExtractor> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_document_extractor_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterDocumentExtractor")]
pub fn _alef_unregister_document_extractor(name: String) -> napi::Result<()> {
    xberg::plugins::document_extractor::unregister_document_extractor(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearDocumentExtractors")]
pub fn _alef_clear_document_extractors() -> napi::Result<()> {
    xberg::plugins::document_extractor::clear_document_extractors()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsEmbeddingBackendBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
}

impl std::fmt::Debug for JsEmbeddingBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsEmbeddingBackendBridge")
    }
}

impl JsEmbeddingBackendBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("dimensions").unwrap_or(false)
            && !js_obj.has_named_property("dimensions").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "dimensions"),
            ));
        }
        if !js_obj.has_named_property("embed").unwrap_or(false) && !js_obj.has_named_property("embed").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "embed"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsEmbeddingBackendBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsEmbeddingBackendBridge {}
unsafe impl Sync for JsEmbeddingBackendBridge {}

impl xberg::plugins::Plugin for JsEmbeddingBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::EmbeddingBackend for JsEmbeddingBackendBridge {
    fn dimensions(&self) -> usize {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("dimensions")
            .or_else(|_| __obj.get_named_property("dimensions"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "dimensions", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "dimensions", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsEmbeddingBackendBridge", method = "dimensions", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    async fn embed(&self, texts: Vec<String>) -> std::result::Result<Vec<Vec<f32>>, xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown,)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("embed")
            .or_else(|_| __obj.get_named_property("embed"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((unsafe {
            let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(self.env().raw(), texts.clone())
                .unwrap_or(std::ptr::null_mut());
            napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
        },)));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'embed' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // napi-rs has no `FromNapiValue for f32`; decode into the f64 analog (every JS
                // number already round-trips through f64) and cast element-wise back to f32.
                let __decoded: Vec<Vec<f64>> =
                    unsafe { napi::bindgen_prelude::FromNapiValue::from_napi_value(self.env().raw(), val.raw()) }
                        .map_err(|e| {
                            xberg::XbergError::Other(format!(
                                "Failed to extract return value from method 'embed': {}",
                                e
                            ))
                        })?;
                Ok(__decoded
                    .into_iter()
                    .map(|v| v.into_iter().map(|v| v as f32).collect())
                    .collect())
            }
        }
    }
}

#[napi]
pub fn register_embedding_backend(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsEmbeddingBackendBridge::new(obj)?;
    let arc: Arc<dyn xberg::EmbeddingBackend> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_embedding_backend_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterEmbeddingBackend")]
pub fn _alef_unregister_embedding_backend(name: String) -> napi::Result<()> {
    xberg::plugins::embedding_backend::unregister_embedding_backend(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearEmbeddingBackends")]
pub fn _alef_clear_embedding_backends() -> napi::Result<()> {
    xberg::plugins::embedding_backend::clear_embedding_backends()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsRendererBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
    has_render_result: bool,
}

impl std::fmt::Debug for JsRendererBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsRendererBridge")
    }
}

impl JsRendererBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
            has_render_result: js_obj.has_named_property("renderResult").unwrap_or(false)
                || js_obj.has_named_property("render_result").unwrap_or(false),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsRendererBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsRendererBridge {}
unsafe impl Sync for JsRendererBridge {}

impl xberg::plugins::Plugin for JsRendererBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsRendererBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsRendererBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsRendererBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

impl xberg::Renderer for JsRendererBridge {
    fn render_result(&self, result: &xberg::ExtractedDocument) -> std::result::Result<String, xberg::XbergError> {
        if !(self.has_render_result) {
            return JsRendererBridgeDefaultRenderResult(self).render_result(result);
        }
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(Default::default()),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown,)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("renderResult")
            .or_else(|_| __obj.get_named_property("render_result"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it). Falls back to Default::default().
            Err(_) => return Ok(Default::default()),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((unsafe {
            let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                self.env().raw(),
                JsExtractedDocument::from((*result).clone()),
            )
            .unwrap_or(std::ptr::null_mut());
            napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
        },)));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'render_result' failed: {}",
                self.cached_name, e
            ))),
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                    .map_err(|e| {
                        xberg::XbergError::Other(format!(
                            "Failed to extract return value from method 'render_result': {}",
                            e
                        ))
                    })?;
                match s.as_str() {
                    // null/empty is a protocol error for Result-returning methods: the JS
                    // implementation must always return a serialisable value, never null.
                    // Avoid requiring Default on return types that are intentionally
                    // excluded from bindings.
                    "" | "null" => Err(xberg::XbergError::Other(format!(
                        "Plugin '{}' failed to parse return value for method 'render_result'",
                        self.cached_name
                    ))),
                    _ => serde_json::from_str::<_>(&s).map_err(|_| {
                        xberg::XbergError::Other(format!(
                            "Plugin '{}' failed to parse return value for method 'render_result'",
                            self.cached_name
                        ))
                    }),
                }
            }
        }
    }
}

/// Runs `render_result`'s Rust default body for [`JsRendererBridge`] while routing
/// every other trait method back through the bridge, so the default body's `self.*`
/// calls still reach host overrides.
#[derive(Debug)]
struct JsRendererBridgeDefaultRenderResult<'a>(&'a JsRendererBridge);
impl xberg::plugins::Plugin for JsRendererBridgeDefaultRenderResult<'_> {
    fn name(&self) -> &str {
        xberg::plugins::Plugin::name(self.0)
    }

    fn version(&self) -> String {
        xberg::plugins::Plugin::version(self.0)
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::initialize(self.0)
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        xberg::plugins::Plugin::shutdown(self.0)
    }
}
impl xberg::Renderer for JsRendererBridgeDefaultRenderResult<'_> {}

#[napi]
pub fn register_renderer(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsRendererBridge::new(obj)?;
    let arc: Arc<dyn xberg::Renderer> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_renderer_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterRenderer")]
pub fn _alef_unregister_renderer(name: String) -> napi::Result<()> {
    xberg::plugins::renderer::unregister_renderer(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearRenderers")]
pub fn _alef_clear_renderers() -> napi::Result<()> {
    xberg::plugins::renderer::clear_renderers()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsRerankerBackendBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
}

impl std::fmt::Debug for JsRerankerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsRerankerBackendBridge")
    }
}

impl JsRerankerBackendBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("rerank").unwrap_or(false)
            && !js_obj.has_named_property("rerank").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "rerank"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsRerankerBackendBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsRerankerBackendBridge {}
unsafe impl Sync for JsRerankerBackendBridge {}

impl xberg::plugins::Plugin for JsRerankerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsRerankerBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsRerankerBackendBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsRerankerBackendBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

#[async_trait::async_trait]
impl xberg::RerankerBackend for JsRerankerBackendBridge {
    async fn rerank(&self, query: String, documents: Vec<String>) -> std::result::Result<Vec<f32>, xberg::XbergError> {
        let cached_name = self.cached_name.clone();
        let __env = self.env();

        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown, napi::bindgen_prelude::Unknown)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("rerank")
            .or_else(|_| __obj.get_named_property("rerank"))
        {
            Ok(f) => f,
            Err(e) => {
                return Err(xberg::XbergError::Other(format!(
                    "Method '{}' not found on bridge object: {}",
                    cached_name, e
                )));
            }
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(query.as_str()) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
            unsafe {
                let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(self.env().raw(), documents.clone())
                    .unwrap_or(std::ptr::null_mut());
                napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
            },
        )));

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'rerank' failed: {}",
                cached_name, e
            ))),
            Ok(val) => {
                // napi-rs has no `FromNapiValue for f32`; decode into the f64 analog (every JS
                // number already round-trips through f64) and cast element-wise back to f32.
                let __decoded: Vec<f64> =
                    unsafe { napi::bindgen_prelude::FromNapiValue::from_napi_value(self.env().raw(), val.raw()) }
                        .map_err(|e| {
                            xberg::XbergError::Other(format!(
                                "Failed to extract return value from method 'rerank': {}",
                                e
                            ))
                        })?;
                Ok(__decoded.into_iter().map(|v| v as f32).collect())
            }
        }
    }
}

#[napi]
pub fn register_reranker_backend(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsRerankerBackendBridge::new(obj)?;
    let arc: Arc<dyn xberg::RerankerBackend> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_reranker_backend_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterRerankerBackend")]
pub fn _alef_unregister_reranker_backend(name: String) -> napi::Result<()> {
    xberg::plugins::reranker_backend::unregister_reranker_backend(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearRerankerBackends")]
pub fn _alef_clear_reranker_backends() -> napi::Result<()> {
    xberg::plugins::reranker_backend::clear_reranker_backends()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

/// Wrapper that bridges a foreign JavaScript object to a Rust trait.
///
/// Holds the JS object via a persistent napi reference (`ObjectRef`) rather than
/// a borrowed `Object` transmuted to `'static`. The reference is released in
/// `dispose()` (and defensively in `Drop`) so the outstanding handle no longer
/// pins the libuv event loop, letting Node/worker threads exit cleanly.
pub struct JsTokenizerBackendBridge {
    env: napi::sys::napi_env,
    obj_ref: std::sync::Mutex<Option<napi::bindgen_prelude::ObjectRef<false>>>,
    cached_name: String,
    cancellation_token: std::sync::Arc<tokio_util::sync::CancellationToken>,
}

impl std::fmt::Debug for JsTokenizerBackendBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "JsTokenizerBackendBridge")
    }
}

impl JsTokenizerBackendBridge {
    /// Create a new bridge wrapping a NAPI Object.
    ///
    /// Validates that the object provides all required methods.
    pub fn new(js_obj: napi::bindgen_prelude::Object<'_>) -> napi::Result<Self> {
        if !js_obj.has_named_property("countTokens").unwrap_or(false)
            && !js_obj.has_named_property("count_tokens").unwrap_or(false)
        {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Object missing required method: {}", "countTokens"),
            ));
        }
        if !js_obj.has_named_property("name").unwrap_or(false) {
            return Err(napi::Error::new(
                napi::Status::GenericFailure,
                "Object missing required method: name".to_string(),
            ));
        } // Cache the plugin name while the borrowed object is still in scope. `name`
        // may be either a string property or a zero-arg function returning a string
        // (the trait method form). Try the function first, then fall back to a
        // string property.
        let cached_name = js_obj
            .get_named_property::<napi::bindgen_prelude::Function<(), String>>("name")
            .and_then(|f| f.call(()))
            .or_else(|_| js_obj.get_named_property::<String>("name"))
            .map_err(|e| {
                napi::Error::new(
                    napi::Status::GenericFailure,
                    format!("Object missing required method: name ({e})"),
                )
            })?;
        // Capture the raw napi_env that owns the object. It is stable for the
        // lifetime of the Node process and is used to reconstruct an `Env` when
        // materialising the object or building context arguments.
        // SAFETY: `Object<'_>` is laid out as 3 pointer-sized words whose first
        // word is the owning napi_env.
        let env = unsafe {
            let raw: [*mut std::ffi::c_void; 3] = std::mem::transmute_copy(&js_obj);
            raw[0] as napi::sys::napi_env
        };
        // Hold the JS object via a persistent napi reference instead of a borrowed
        // `Object` transmuted to `'static`. The reference keeps the value alive
        // across handle scopes and, crucially, is released in `dispose()`/`Drop`
        // so it no longer pins the event loop.
        let obj_ref = js_obj.create_ref::<false>()?;

        Ok(Self {
            env,
            obj_ref: std::sync::Mutex::new(Some(obj_ref)),
            cached_name,
            cancellation_token: std::sync::Arc::new(tokio_util::sync::CancellationToken::new()),
        })
    }

    /// Materialise a live `Object` from the persistent reference.
    ///
    /// The returned object is tied to the provided `Env` and kept alive by the
    /// underlying napi reference for the duration of the current event-loop turn.
    /// Returns an error once the reference has been released (after `dispose()`).
    fn obj<'env>(&self, env: &'env napi::Env) -> napi::Result<napi::bindgen_prelude::Object<'env>> {
        let guard = self.obj_ref.lock().unwrap_or_else(|e| e.into_inner());
        match guard.as_ref() {
            Some(obj_ref) => obj_ref.get_value(env),
            None => Err(napi::Error::new(
                napi::Status::GenericFailure,
                format!("Plugin '{}' object has been disposed", self.cached_name),
            )),
        }
    }

    /// Release the persistent reference, unpinning the event loop.
    ///
    /// Idempotent: safe to call multiple times. After disposal the bridge can no
    /// longer invoke the JS object.
    fn release_ref(&self) {
        if let Ok(mut guard) = self.obj_ref.lock()
            && let Some(obj_ref) = guard.take()
        {
            let env = napi::Env::from_raw(self.env);
            let _ = obj_ref.unref(&env);
        }
    }

    /// Clean up TSFN callbacks and release the event loop.
    pub async fn dispose(&self) -> napi::Result<()> {
        self.cancellation_token.cancel();
        self.release_ref();
        Ok(())
    }

    /// Extract napi::Env from the captured raw env pointer.
    fn env(&self) -> napi::Env {
        napi::Env::from_raw(self.env)
    }
}

impl Drop for JsTokenizerBackendBridge {
    fn drop(&mut self) {
        // Defensive release in case `dispose()` was never called. Releases the
        // napi reference so the JS object can be collected and the event loop is
        // no longer pinned by this bridge.
        self.release_ref();
    }
}

// SAFETY: The bridge is created from a NAPI Object that is pinned to the
// Node.js event loop thread. All napi access occurs on that thread; the stored
// reference (`ObjectRef`) is only dereferenced while reconstructing an `Env` for
// that same thread. Send+Sync are required by the Plugin trait but the bridge is
// never actually moved across threads.
unsafe impl Send for JsTokenizerBackendBridge {}
unsafe impl Sync for JsTokenizerBackendBridge {}

impl xberg::plugins::Plugin for JsTokenizerBackendBridge {
    fn name(&self) -> &str {
        &self.cached_name
    }

    fn version(&self) -> String {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("version")
            .or_else(|_| __obj.get_named_property("version"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(());

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "version", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "version", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "version", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }

    fn initialize(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("initialize")
            .or_else(|_| __obj.get_named_property("initialize"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'initialize' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }

    fn shutdown(&self) -> std::result::Result<(), xberg::XbergError> {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            // Object disposed/unavailable — treat like a missing method (no-op default).
            Err(_) => return Ok(()),
        };
        let func: napi::bindgen_prelude::Function<(), napi::bindgen_prelude::Unknown> = match __obj
            .get_named_property("shutdown")
            .or_else(|_| __obj.get_named_property("shutdown"))
        {
            Ok(f) => f,
            // Method has a default impl on the Rust trait — treat missing JS property as a no-op
            // (the foreign object opted out of overriding it).
            Err(_) => return Ok(()),
        };

        let result = func.call(());

        match result {
            Err(e) => Err(xberg::XbergError::Other(format!(
                "Plugin '{}' method 'shutdown' failed: {}",
                self.cached_name, e
            ))),
            Ok(_) => Ok(()),
        }
    }
}

impl xberg::TokenizerBackend for JsTokenizerBackendBridge {
    fn count_tokens(&self, text: &str) -> usize {
        let __env = self.env();
        let __obj = match self.obj(&__env) {
            Ok(o) => o,
            Err(_) => return Default::default(),
        };
        let func: napi::bindgen_prelude::Function<
            napi::bindgen_prelude::FnArgs<(napi::bindgen_prelude::Unknown,)>,
            napi::bindgen_prelude::Unknown,
        > = match __obj
            .get_named_property("countTokens")
            .or_else(|_| __obj.get_named_property("count_tokens"))
        {
            Ok(f) => f,
            Err(_) => return Default::default(),
        };

        let result = func.call(napi::bindgen_prelude::FnArgs::from((
            match self.env().create_string(text) {
                Ok(s) => s.to_unknown(),
                Err(_) => unsafe {
                    let r = napi::bindgen_prelude::ToNapiValue::to_napi_value(
                        self.env().raw(),
                        napi::bindgen_prelude::Null,
                    )
                    .unwrap_or(std::ptr::null_mut());
                    napi::bindgen_prelude::Unknown::from_raw_unchecked(self.env().raw(), r)
                },
            },
        )));

        match result {
            // This method's Rust signature is infallible, so a JS exception cannot
            // propagate. Log before substituting the default — a silent default is
            // indistinguishable from a real result to the caller.
            Err(e) => {
                tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "countTokens", error = %e, "host callback threw; returning default");
                Default::default()
            }
            Ok(val) => {
                // Convert JS value to Rust type via string coercion
                let s = match val
                    .coerce_to_string()
                    .and_then(|s| s.into_utf8())
                    .and_then(|s| s.into_owned())
                {
                    Ok(s) => s,
                    Err(e) => {
                        tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "countTokens", error = %e, "host returned a value that could not be coerced to a string; returning default");
                        return Default::default();
                    }
                };
                match s.as_str() {
                    // null/undefined is a legitimate "no value" answer for infallible
                    // returns (mirrors the extendr backend) — no log.
                    "" | "null" => Default::default(),
                    _ => match serde_json::from_str::<_>(&s) {
                        Ok(v) => v,
                        Err(e) => {
                            tracing::warn!(wrapper = "JsTokenizerBackendBridge", method = "countTokens", error = %e, "host returned an unparseable value; returning default");
                            Default::default()
                        }
                    },
                }
            }
        }
    }
}

#[napi]
pub fn register_tokenizer_backend(obj: napi::bindgen_prelude::Object) -> napi::Result<()> {
    let bridge = JsTokenizerBackendBridge::new(obj)?;
    let arc: Arc<dyn xberg::TokenizerBackend> = Arc::new(bridge);

    let registry = xberg::plugins::registry::get_tokenizer_backend_registry();
    let mut registry = registry.write();
    registry.register(arc).map_err(|e| {
        napi::Error::new(
            napi::Status::GenericFailure,
            format!("Failed to register backend: {}", e),
        )
    })
}

#[napi(js_name = "unregisterTokenizerBackend")]
pub fn _alef_unregister_tokenizer_backend(name: String) -> napi::Result<()> {
    xberg::plugins::tokenizer_backend::unregister_tokenizer_backend(&name)
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[napi(js_name = "clearTokenizerBackends")]
pub fn _alef_clear_tokenizer_backends() -> napi::Result<()> {
    xberg::plugins::tokenizer_backend::clear_tokenizer_backends()
        .map_err(|e| napi::Error::new(napi::Status::GenericFailure, format!("{}", e)))
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsAccelerationConfig> for xberg::AccelerationConfig {
    fn from(val: JsAccelerationConfig) -> Self {
        let mut __result = xberg::AccelerationConfig::default();
        if let Some(__v) = val.provider {
            __result.provider = __v.into();
        }
        if let Some(__v) = val.device_id {
            __result.device_id = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::AccelerationConfig> for JsAccelerationConfig {
    fn from(val: xberg::AccelerationConfig) -> Self {
        Self {
            provider: Some(val.provider.into()),
            device_id: Some(val.device_id),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsArchiveEntry> for xberg::ArchiveEntry {
    fn from(val: JsArchiveEntry) -> Self {
        Self {
            path: val.path,
            mime_type: val.mime_type,
            result: Box::new(val.result.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ArchiveEntry> for JsArchiveEntry {
    fn from(val: xberg::ArchiveEntry) -> Self {
        Self {
            path: val.path.to_string(),
            mime_type: val.mime_type.to_string(),
            result: (*val.result).into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsArchiveMetadata> for xberg::ArchiveMetadata {
    fn from(val: JsArchiveMetadata) -> Self {
        let mut __result = xberg::ArchiveMetadata::default();
        if let Some(__v) = val.format {
            __result.format = __v.into();
        }
        if let Some(__v) = val.file_count {
            __result.file_count = __v;
        }
        if let Some(__v) = val.file_list {
            __result.file_list = __v.into_iter().collect();
        }
        if let Some(__v) = val.total_size {
            __result.total_size = __v as u64;
        }
        __result.compressed_size = val.compressed_size.map(|v| v as u64);
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ArchiveMetadata> for JsArchiveMetadata {
    fn from(val: xberg::ArchiveMetadata) -> Self {
        Self {
            format: Some(val.format.to_string()),
            file_count: Some(val.file_count),
            file_list: Some(val.file_list.into_iter().collect()),
            total_size: Some(val.total_size as i64),
            compressed_size: val.compressed_size.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsAttributes> for xberg::Attributes {
    fn from(val: JsAttributes) -> Self {
        let mut __result = xberg::Attributes::default();
        __result.id = val.id;
        if let Some(__v) = val.classes {
            __result.classes = __v.into_iter().collect();
        }
        if let Some(__v) = val.key_values {
            __result.key_values = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Attributes> for JsAttributes {
    fn from(val: xberg::Attributes) -> Self {
        Self {
            id: val.id.map(|v| v.to_string()),
            classes: Some(val.classes.into_iter().collect()),
            key_values: Some(val.key_values.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsAudioMetadata> for xberg::AudioMetadata {
    fn from(val: JsAudioMetadata) -> Self {
        let mut __result = xberg::AudioMetadata::default();
        __result.duration_ms = val.duration_ms.map(|v| v as u64);
        __result.codec = val.codec;
        __result.container = val.container;
        __result.sample_rate_hz = val.sample_rate_hz;
        __result.channels = val.channels;
        __result.bitrate = val.bitrate;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::AudioMetadata> for JsAudioMetadata {
    fn from(val: xberg::AudioMetadata) -> Self {
        Self {
            duration_ms: val.duration_ms.map(|v| v as i64),
            codec: val.codec.map(|v| v.to_string()),
            container: val.container.map(|v| v.to_string()),
            sample_rate_hz: val.sample_rate_hz,
            channels: val.channels,
            bitrate: val.bitrate,
        }
    }
}

impl From<xberg::BBox> for JsBBox {
    fn from(val: xberg::BBox) -> Self {
        Self {
            x1: val.x1 as f64,
            y1: val.y1 as f64,
            x2: val.x2 as f64,
            y2: val.y2 as f64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsBedrockConfig> for xberg::BedrockConfig {
    fn from(val: JsBedrockConfig) -> Self {
        let mut __result = xberg::BedrockConfig::default();
        __result.region = val.region;
        __result.cross_region_prefix = val.cross_region_prefix;
        __result.access_key_id = val.access_key_id;
        __result.secret_access_key = val.secret_access_key;
        __result.session_token = val.session_token;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::BedrockConfig> for JsBedrockConfig {
    fn from(val: xberg::BedrockConfig) -> Self {
        Self {
            region: val.region.map(|v| v.to_string()),
            cross_region_prefix: val.cross_region_prefix.map(|v| v.to_string()),
            access_key_id: val.access_key_id.map(|v| v.to_string()),
            secret_access_key: val.secret_access_key.map(|v| v.to_string()),
            session_token: val.session_token.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsBibtexMetadata> for xberg::BibtexMetadata {
    fn from(val: JsBibtexMetadata) -> Self {
        let mut __result = xberg::BibtexMetadata::default();
        if let Some(__v) = val.entry_count {
            __result.entry_count = __v as usize;
        }
        if let Some(__v) = val.citation_keys {
            __result.citation_keys = __v.into_iter().collect();
        }
        if let Some(__v) = val.authors {
            __result.authors = __v.into_iter().collect();
        }
        __result.year_range = val.year_range.map(Into::into);
        __result.entry_types = val
            .entry_types
            .map(|m| m.into_iter().map(|(k, v)| (k, v as usize)).collect());
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::BibtexMetadata> for JsBibtexMetadata {
    fn from(val: xberg::BibtexMetadata) -> Self {
        Self {
            entry_count: Some(val.entry_count as i64),
            citation_keys: Some(val.citation_keys.into_iter().collect()),
            authors: Some(val.authors.into_iter().collect()),
            year_range: val.year_range.map(Into::into),
            entry_types: val
                .entry_types
                .as_ref()
                .map(|m| m.iter().map(|(k, v)| (k.clone(), *v as i64)).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsBoundingBox> for xberg::BoundingBox {
    fn from(val: JsBoundingBox) -> Self {
        let mut __result = xberg::BoundingBox::default();
        if let Some(__v) = val.x0 {
            __result.x0 = __v;
        }
        if let Some(__v) = val.y0 {
            __result.y0 = __v;
        }
        if let Some(__v) = val.x1 {
            __result.x1 = __v;
        }
        if let Some(__v) = val.y1 {
            __result.y1 = __v;
        }
        __result
    }
}

impl From<xberg::BoundingBox> for JsBoundingBox {
    fn from(val: xberg::BoundingBox) -> Self {
        Self {
            x0: Some(val.x0),
            y0: Some(val.y0),
            x1: Some(val.x1),
            y1: Some(val.y1),
        }
    }
}

#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsBrowserConfig> for crawlberg::BrowserConfig {
    fn from(val: JsBrowserConfig) -> Self {
        let mut __result = crawlberg::BrowserConfig::default();
        if let Some(__v) = val.mode {
            __result.mode = __v.into();
        }
        if let Some(__v) = val.backend {
            __result.backend = __v.into();
        }
        __result.endpoint = val.endpoint;
        if let Some(__v) = val.timeout {
            __result.timeout = std::time::Duration::from_millis(__v as u64);
        }
        if let Some(__v) = val.wait {
            __result.wait = __v.into();
        }
        __result.wait_selector = val.wait_selector;
        __result.extra_wait = val.extra_wait.map(|v| std::time::Duration::from_millis(v as u64));
        __result.proxy = val.proxy.map(Into::into);
        if let Some(__v) = val.block_url_patterns {
            __result.block_url_patterns = __v.into_iter().collect();
        }
        __result.eval_script = val.eval_script;
        __result.robots_user_agent = val.robots_user_agent;
        if let Some(__v) = val.capture_network_events {
            __result.capture_network_events = __v;
        }
        if let Some(__v) = val.session_affinity {
            __result.session_affinity = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<crawlberg::BrowserConfig> for JsBrowserConfig {
    fn from(val: crawlberg::BrowserConfig) -> Self {
        Self {
            mode: Some(val.mode.into()),
            backend: Some(val.backend.into()),
            endpoint: val.endpoint.map(|v| v.to_string()),
            timeout: Some(val.timeout.as_millis() as u64 as i64),
            wait: Some(val.wait.into()),
            wait_selector: val.wait_selector.map(|v| v.to_string()),
            extra_wait: val.extra_wait.map(|d| d.as_millis() as u64 as i64),
            proxy: val.proxy.map(Into::into),
            block_url_patterns: Some(val.block_url_patterns.into_iter().collect()),
            eval_script: val.eval_script.map(|v| v.to_string()),
            robots_user_agent: val.robots_user_agent.map(|v| v.to_string()),
            capture_network_events: Some(val.capture_network_events),
            session_affinity: Some(val.session_affinity),
        }
    }
}

impl From<xberg::CacheStats> for JsCacheStats {
    fn from(val: xberg::CacheStats) -> Self {
        Self {
            total_files: val.total_files as i64,
            total_size_mb: val.total_size_mb,
            available_space_mb: val.available_space_mb,
            oldest_file_age_days: val.oldest_file_age_days,
            newest_file_age_days: val.newest_file_age_days,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsCaptioningConfig> for xberg::CaptioningConfig {
    fn from(val: JsCaptioningConfig) -> Self {
        Self {
            llm: val.llm.into(),
            prompt: val.prompt,
            min_image_area: val.min_image_area,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CaptioningConfig> for JsCaptioningConfig {
    fn from(val: xberg::CaptioningConfig) -> Self {
        Self {
            llm: val.llm.into(),
            prompt: val.prompt.map(|v| v.to_string()),
            min_image_area: val.min_image_area,
        }
    }
}

impl From<JsCellChange> for xberg::CellChange {
    fn from(val: JsCellChange) -> Self {
        Self {
            row: val.row as usize,
            col: val.col as usize,
            from: val.from,
            to: val.to,
        }
    }
}

impl From<xberg::CellChange> for JsCellChange {
    fn from(val: xberg::CellChange) -> Self {
        Self {
            row: val.row as i64,
            col: val.col as i64,
            from: val.from.to_string(),
            to: val.to.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsChunk> for xberg::Chunk {
    fn from(val: JsChunk) -> Self {
        Self {
            content: val.content,
            chunk_type: val.chunk_type.into(),
            embedding: val.embedding.map(|v| v.into_iter().map(|x| x as f32).collect()),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Chunk> for JsChunk {
    fn from(val: xberg::Chunk) -> Self {
        Self {
            content: val.content.to_string(),
            chunk_type: val.chunk_type.into(),
            embedding: val.embedding.as_ref().map(|v| v.iter().map(|&x| x as f64).collect()),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsChunkClassificationConfig> for xberg::ChunkClassificationConfig {
    fn from(val: JsChunkClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template,
            definitions: val.definitions.into_iter().map(Into::into).collect(),
            llm: val.llm.into(),
            batch_size: val.batch_size as usize,
            max_concurrency: val.max_concurrency as usize,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkClassificationConfig> for JsChunkClassificationConfig {
    fn from(val: xberg::ChunkClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template.map(|v| v.to_string()),
            definitions: val.definitions.into_iter().map(Into::into).collect(),
            llm: val.llm.into(),
            batch_size: val.batch_size as i64,
            max_concurrency: val.max_concurrency as i64,
        }
    }
}

impl From<JsChunkClassificationDefinition> for xberg::ChunkClassificationDefinition {
    fn from(val: JsChunkClassificationDefinition) -> Self {
        Self {
            label: val.label,
            description: val.description,
        }
    }
}

impl From<xberg::ChunkClassificationDefinition> for JsChunkClassificationDefinition {
    fn from(val: xberg::ChunkClassificationDefinition) -> Self {
        Self {
            label: val.label.to_string(),
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ChunkClassificationEnrichmentConfig> for JsChunkClassificationEnrichmentConfig {
    fn from(val: xberg::ChunkClassificationEnrichmentConfig) -> Self {
        Self {
            config: val.config.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ChunkInfo> for JsChunkInfo {
    fn from(val: xberg::ChunkInfo) -> Self {
        Self {
            index: val.index,
            pages: val.pages.into(),
            estimated_time_ms: val.estimated_time_ms as i64,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsChunkMetadata> for xberg::ChunkMetadata {
    fn from(val: JsChunkMetadata) -> Self {
        Self {
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
            token_count: val.token_count.map(|v| v as usize),
            chunk_index: val.chunk_index as usize,
            total_chunks: val.total_chunks as usize,
            first_page: val.first_page,
            last_page: val.last_page,
            heading_context: val.heading_context.map(Into::into),
            heading_path: val.heading_path.into_iter().collect(),
            image_indices: val.image_indices.into_iter().collect(),
            node_ids: val.node_ids.into_iter().collect(),
            page_spans: val.page_spans.into_iter().map(Into::into).collect(),
            classifications: val.classifications.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkMetadata> for JsChunkMetadata {
    fn from(val: xberg::ChunkMetadata) -> Self {
        Self {
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
            token_count: val.token_count.map(|v| v as i64),
            chunk_index: val.chunk_index as i64,
            total_chunks: val.total_chunks as i64,
            first_page: val.first_page,
            last_page: val.last_page,
            heading_context: val.heading_context.map(Into::into),
            heading_path: val.heading_path.into_iter().collect(),
            image_indices: val.image_indices.into_iter().collect(),
            node_ids: val.node_ids.into_iter().collect(),
            page_spans: val.page_spans.into_iter().map(Into::into).collect(),
            classifications: val.classifications.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsChunkingConfig> for xberg::ChunkingConfig {
    fn from(val: JsChunkingConfig) -> Self {
        let mut __result = xberg::ChunkingConfig::default();
        if let Some(__v) = val.max_characters {
            __result.max_characters = __v as usize;
        }
        if let Some(__v) = val.overlap {
            __result.overlap = __v as usize;
        }
        if let Some(__v) = val.trim {
            __result.trim = __v;
        }
        if let Some(__v) = val.chunker_type {
            __result.chunker_type = __v.into();
        }
        __result.embedding = val.embedding.map(Into::into);
        __result.sparse_embedding = val.sparse_embedding.map(Into::into);
        __result.late_interaction = val.late_interaction.map(Into::into);
        __result.preset = val.preset;
        if let Some(__v) = val.sizing {
            __result.sizing = __v.into();
        }
        __result.topic_threshold = val.topic_threshold.map(|v| v as f32);
        if let Some(__v) = val.table_chunking {
            __result.table_chunking = __v.into();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ChunkingConfig> for JsChunkingConfig {
    fn from(val: xberg::ChunkingConfig) -> Self {
        Self {
            max_characters: Some(val.max_characters as i64),
            overlap: Some(val.overlap as i64),
            trim: Some(val.trim),
            chunker_type: Some(val.chunker_type.into()),
            embedding: val.embedding.map(Into::into),
            sparse_embedding: val.sparse_embedding.map(Into::into),
            late_interaction: val.late_interaction.map(Into::into),
            preset: val.preset.map(|v| v.to_string()),
            sizing: Some(val.sizing.into()),
            topic_threshold: val.topic_threshold.map(|v| v as f64),
            table_chunking: Some(val.table_chunking.into()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Citation> for JsCitation {
    fn from(val: xberg::Citation) -> Self {
        Self {
            label: val.label.to_string(),
            source: val.source.to_string(),
            locator: val.locator.map(|v| v.to_string()),
            excerpt: val.excerpt.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsCitationMetadata> for xberg::CitationMetadata {
    fn from(val: JsCitationMetadata) -> Self {
        let mut __result = xberg::CitationMetadata::default();
        if let Some(__v) = val.citation_count {
            __result.citation_count = __v as usize;
        }
        __result.format = val.format;
        if let Some(__v) = val.authors {
            __result.authors = __v.into_iter().collect();
        }
        __result.year_range = val.year_range.map(Into::into);
        if let Some(__v) = val.dois {
            __result.dois = __v.into_iter().collect();
        }
        if let Some(__v) = val.keywords {
            __result.keywords = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CitationMetadata> for JsCitationMetadata {
    fn from(val: xberg::CitationMetadata) -> Self {
        Self {
            citation_count: Some(val.citation_count as i64),
            format: val.format.map(|v| v.to_string()),
            authors: Some(val.authors.into_iter().collect()),
            year_range: val.year_range.map(Into::into),
            dois: Some(val.dois.into_iter().collect()),
            keywords: Some(val.keywords.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<JsClassificationLabel> for xberg::ClassificationLabel {
    fn from(val: JsClassificationLabel) -> Self {
        Self {
            label: val.label,
            confidence: val.confidence.map(|v| v as f32),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ClassificationLabel> for JsClassificationLabel {
    fn from(val: xberg::ClassificationLabel) -> Self {
        Self {
            label: val.label.to_string(),
            confidence: val.confidence.map(|v| v as f64),
        }
    }
}

impl From<JsCodeChunkInfo> for xberg::CodeChunkInfo {
    fn from(val: JsCodeChunkInfo) -> Self {
        Self {
            text: val.text,
            context_path: val.context_path.into_iter().collect(),
            node_types: val.node_types.into_iter().collect(),
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

impl From<xberg::CodeChunkInfo> for JsCodeChunkInfo {
    fn from(val: xberg::CodeChunkInfo) -> Self {
        Self {
            text: val.text.to_string(),
            context_path: val.context_path.into_iter().collect(),
            node_types: val.node_types.into_iter().collect(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

impl From<JsCodeDataAttribute> for xberg::CodeDataAttribute {
    fn from(val: JsCodeDataAttribute) -> Self {
        Self {
            name: val.name,
            value: val.value,
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

impl From<xberg::CodeDataAttribute> for JsCodeDataAttribute {
    fn from(val: xberg::CodeDataAttribute) -> Self {
        Self {
            name: val.name.to_string(),
            value: val.value.to_string(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsCodeDataNode> for xberg::CodeDataNode {
    fn from(val: JsCodeDataNode) -> Self {
        Self {
            kind: val.kind.into(),
            key: val.key,
            value: val.value,
            attributes: val.attributes.into_iter().map(Into::into).collect(),
            children: val.children.into_iter().map(Into::into).collect(),
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::CodeDataNode> for JsCodeDataNode {
    fn from(val: xberg::CodeDataNode) -> Self {
        Self {
            kind: val.kind.into(),
            key: val.key.map(|v| v.to_string()),
            value: val.value.map(|v| v.to_string()),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
            children: val.children.into_iter().map(Into::into).collect(),
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsCodeMetadata> for xberg::CodeMetadata {
    fn from(val: JsCodeMetadata) -> Self {
        let mut __result = xberg::CodeMetadata::default();
        if let Some(__v) = val.chunks {
            __result.chunks = __v.into_iter().map(Into::into).collect();
        }
        __result.data = val.data.map(Into::into);
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::CodeMetadata> for JsCodeMetadata {
    fn from(val: xberg::CodeMetadata) -> Self {
        Self {
            chunks: Some(val.chunks.into_iter().map(Into::into).collect()),
            data: val.data.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsConcurrencyConfig> for xberg::ConcurrencyConfig {
    fn from(val: JsConcurrencyConfig) -> Self {
        let mut __result = xberg::ConcurrencyConfig::default();
        __result.max_threads = val.max_threads.map(|v| v as usize);
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ConcurrencyConfig> for JsConcurrencyConfig {
    fn from(val: xberg::ConcurrencyConfig) -> Self {
        Self {
            max_threads: val.max_threads.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsContentConfig> for crawlberg::ContentConfig {
    fn from(val: JsContentConfig) -> Self {
        let mut __result = crawlberg::ContentConfig::default();
        if let Some(__v) = val.output_format {
            __result.output_format = __v;
        }
        if let Some(__v) = val.preprocessing_preset {
            __result.preprocessing_preset = __v;
        }
        if let Some(__v) = val.remove_navigation {
            __result.remove_navigation = __v;
        }
        if let Some(__v) = val.remove_forms {
            __result.remove_forms = __v;
        }
        if let Some(__v) = val.strip_tags {
            __result.strip_tags = __v.into_iter().collect();
        }
        if let Some(__v) = val.preserve_tags {
            __result.preserve_tags = __v.into_iter().collect();
        }
        if let Some(__v) = val.exclude_selectors {
            __result.exclude_selectors = __v.into_iter().collect();
        }
        if let Some(__v) = val.skip_images {
            __result.skip_images = __v;
        }
        __result.max_depth = val.max_depth.map(|v| v as usize);
        if let Some(__v) = val.wrap {
            __result.wrap = __v;
        }
        if let Some(__v) = val.wrap_width {
            __result.wrap_width = __v as usize;
        }
        if let Some(__v) = val.include_document_structure {
            __result.include_document_structure = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::ContentConfig> for JsContentConfig {
    fn from(val: crawlberg::ContentConfig) -> Self {
        Self {
            output_format: Some(val.output_format.to_string()),
            preprocessing_preset: Some(val.preprocessing_preset.to_string()),
            remove_navigation: Some(val.remove_navigation),
            remove_forms: Some(val.remove_forms),
            strip_tags: Some(val.strip_tags.into_iter().collect()),
            preserve_tags: Some(val.preserve_tags.into_iter().collect()),
            exclude_selectors: Some(val.exclude_selectors.into_iter().collect()),
            skip_images: Some(val.skip_images),
            max_depth: val.max_depth.map(|v| v as i64),
            wrap: Some(val.wrap),
            wrap_width: Some(val.wrap_width as i64),
            include_document_structure: Some(val.include_document_structure),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsContentFilterConfig> for xberg::ContentFilterConfig {
    fn from(val: JsContentFilterConfig) -> Self {
        let mut __result = xberg::ContentFilterConfig::default();
        if let Some(__v) = val.include_headers {
            __result.include_headers = __v;
        }
        if let Some(__v) = val.include_footers {
            __result.include_footers = __v;
        }
        if let Some(__v) = val.include_footnotes {
            __result.include_footnotes = __v;
        }
        if let Some(__v) = val.strip_repeating_text {
            __result.strip_repeating_text = __v;
        }
        if let Some(__v) = val.include_watermarks {
            __result.include_watermarks = __v;
        }
        __result
    }
}

impl From<xberg::ContentFilterConfig> for JsContentFilterConfig {
    fn from(val: xberg::ContentFilterConfig) -> Self {
        Self {
            include_headers: Some(val.include_headers),
            include_footers: Some(val.include_footers),
            include_footnotes: Some(val.include_footnotes),
            strip_repeating_text: Some(val.strip_repeating_text),
            include_watermarks: Some(val.include_watermarks),
        }
    }
}

impl From<JsContributorRole> for xberg::ContributorRole {
    fn from(val: JsContributorRole) -> Self {
        Self {
            name: val.name,
            role: val.role,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ContributorRole> for JsContributorRole {
    fn from(val: xberg::ContributorRole) -> Self {
        Self {
            name: val.name.to_string(),
            role: val.role.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsConversionOptions> for html_to_markdown_rs::ConversionOptions {
    fn from(val: JsConversionOptions) -> Self {
        let mut __result = html_to_markdown_rs::ConversionOptions::default();
        if let Some(__v) = val.heading_style {
            __result.heading_style = __v.into();
        }
        if let Some(__v) = val.list_indent_type {
            __result.list_indent_type = __v.into();
        }
        if let Some(__v) = val.list_indent_width {
            __result.list_indent_width = __v as usize;
        }
        if let Some(__v) = val.bullets {
            __result.bullets = __v;
        }
        if let Some(__v) = val.strong_em_symbol {
            __result.strong_em_symbol = __v.chars().next().unwrap_or('*');
        }
        if let Some(__v) = val.escape_asterisks {
            __result.escape_asterisks = __v;
        }
        if let Some(__v) = val.escape_underscores {
            __result.escape_underscores = __v;
        }
        if let Some(__v) = val.escape_misc {
            __result.escape_misc = __v;
        }
        if let Some(__v) = val.escape_ascii {
            __result.escape_ascii = __v;
        }
        if let Some(__v) = val.code_language {
            __result.code_language = __v;
        }
        if let Some(__v) = val.autolinks {
            __result.autolinks = __v;
        }
        if let Some(__v) = val.default_title {
            __result.default_title = __v;
        }
        if let Some(__v) = val.br_in_tables {
            __result.br_in_tables = __v;
        }
        if let Some(__v) = val.compact_tables {
            __result.compact_tables = __v;
        }
        if let Some(__v) = val.highlight_style {
            __result.highlight_style = __v.into();
        }
        if let Some(__v) = val.extract_metadata {
            __result.extract_metadata = __v;
        }
        if let Some(__v) = val.whitespace_mode {
            __result.whitespace_mode = __v.into();
        }
        if let Some(__v) = val.strip_newlines {
            __result.strip_newlines = __v;
        }
        if let Some(__v) = val.wrap {
            __result.wrap = __v;
        }
        if let Some(__v) = val.wrap_width {
            __result.wrap_width = __v as usize;
        }
        if let Some(__v) = val.convert_as_inline {
            __result.convert_as_inline = __v;
        }
        if let Some(__v) = val.sub_symbol {
            __result.sub_symbol = __v;
        }
        if let Some(__v) = val.sup_symbol {
            __result.sup_symbol = __v;
        }
        if let Some(__v) = val.newline_style {
            __result.newline_style = __v.into();
        }
        if let Some(__v) = val.code_block_style {
            __result.code_block_style = __v.into();
        }
        if let Some(__v) = val.keep_inline_images_in {
            __result.keep_inline_images_in = __v.into_iter().collect();
        }
        if let Some(__v) = val.preprocessing {
            __result.preprocessing = __v.into();
        }
        if let Some(__v) = val.encoding {
            __result.encoding = __v;
        }
        if let Some(__v) = val.debug {
            __result.debug = __v;
        }
        if let Some(__v) = val.strip_tags {
            __result.strip_tags = __v.into_iter().collect();
        }
        if let Some(__v) = val.preserve_tags {
            __result.preserve_tags = __v.into_iter().collect();
        }
        if let Some(__v) = val.skip_images {
            __result.skip_images = __v;
        }
        if let Some(__v) = val.url_escape_style {
            __result.url_escape_style = __v.into();
        }
        if let Some(__v) = val.link_style {
            __result.link_style = __v.into();
        }
        if let Some(__v) = val.output_format {
            __result.output_format = __v.into();
        }
        if let Some(__v) = val.include_document_structure {
            __result.include_document_structure = __v;
        }
        if let Some(__v) = val.extract_images {
            __result.extract_images = __v;
        }
        if let Some(__v) = val.max_image_size {
            __result.max_image_size = __v as u64;
        }
        if let Some(__v) = val.capture_svg {
            __result.capture_svg = __v;
        }
        if let Some(__v) = val.infer_dimensions {
            __result.infer_dimensions = __v;
        }
        __result.max_depth = val.max_depth.map(|v| v as usize);
        if let Some(__v) = val.exclude_selectors {
            __result.exclude_selectors = __v.into_iter().collect();
        }
        if let Some(__v) = val.tier_strategy {
            __result.tier_strategy = __v.into();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<html_to_markdown_rs::ConversionOptions> for JsConversionOptions {
    fn from(val: html_to_markdown_rs::ConversionOptions) -> Self {
        Self {
            heading_style: Some(val.heading_style.into()),
            list_indent_type: Some(val.list_indent_type.into()),
            list_indent_width: Some(val.list_indent_width as i64),
            bullets: Some(val.bullets.to_string()),
            strong_em_symbol: Some(val.strong_em_symbol.to_string()),
            escape_asterisks: Some(val.escape_asterisks),
            escape_underscores: Some(val.escape_underscores),
            escape_misc: Some(val.escape_misc),
            escape_ascii: Some(val.escape_ascii),
            code_language: Some(val.code_language.to_string()),
            autolinks: Some(val.autolinks),
            default_title: Some(val.default_title),
            br_in_tables: Some(val.br_in_tables),
            compact_tables: Some(val.compact_tables),
            highlight_style: Some(val.highlight_style.into()),
            extract_metadata: Some(val.extract_metadata),
            whitespace_mode: Some(val.whitespace_mode.into()),
            strip_newlines: Some(val.strip_newlines),
            wrap: Some(val.wrap),
            wrap_width: Some(val.wrap_width as i64),
            convert_as_inline: Some(val.convert_as_inline),
            sub_symbol: Some(val.sub_symbol.to_string()),
            sup_symbol: Some(val.sup_symbol.to_string()),
            newline_style: Some(val.newline_style.into()),
            code_block_style: Some(val.code_block_style.into()),
            keep_inline_images_in: Some(val.keep_inline_images_in.into_iter().collect()),
            preprocessing: Some(val.preprocessing.into()),
            encoding: Some(val.encoding.to_string()),
            debug: Some(val.debug),
            strip_tags: Some(val.strip_tags.into_iter().collect()),
            preserve_tags: Some(val.preserve_tags.into_iter().collect()),
            skip_images: Some(val.skip_images),
            url_escape_style: Some(val.url_escape_style.into()),
            link_style: Some(val.link_style.into()),
            output_format: Some(val.output_format.into()),
            include_document_structure: Some(val.include_document_structure),
            extract_images: Some(val.extract_images),
            max_image_size: Some(val.max_image_size as i64),
            capture_svg: Some(val.capture_svg),
            infer_dimensions: Some(val.infer_dimensions),
            max_depth: val.max_depth.map(|v| v as i64),
            exclude_selectors: Some(val.exclude_selectors.into_iter().collect()),
            tier_strategy: Some(val.tier_strategy.into()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsCoreProperties> for xberg::CoreProperties {
    fn from(val: JsCoreProperties) -> Self {
        let mut __result = xberg::CoreProperties::default();
        __result.title = val.title;
        __result.subject = val.subject;
        __result.creator = val.creator;
        __result.keywords = val.keywords;
        __result.description = val.description;
        __result.last_modified_by = val.last_modified_by;
        __result.revision = val.revision;
        __result.created = val.created;
        __result.modified = val.modified;
        __result.category = val.category;
        __result.content_status = val.content_status;
        __result.language = val.language;
        __result.identifier = val.identifier;
        __result.version = val.version;
        __result.last_printed = val.last_printed;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CoreProperties> for JsCoreProperties {
    fn from(val: xberg::CoreProperties) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            creator: val.creator.map(|v| v.to_string()),
            keywords: val.keywords.map(|v| v.to_string()),
            description: val.description.map(|v| v.to_string()),
            last_modified_by: val.last_modified_by.map(|v| v.to_string()),
            revision: val.revision.map(|v| v.to_string()),
            created: val.created.map(|v| v.to_string()),
            modified: val.modified.map(|v| v.to_string()),
            category: val.category.map(|v| v.to_string()),
            content_status: val.content_status.map(|v| v.to_string()),
            language: val.language.map(|v| v.to_string()),
            identifier: val.identifier.map(|v| v.to_string()),
            version: val.version.map(|v| v.to_string()),
            last_printed: val.last_printed.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsCrawlConfig> for crawlberg::CrawlConfig {
    fn from(val: JsCrawlConfig) -> Self {
        let mut __result = crawlberg::CrawlConfig::default();
        __result.max_depth = val.max_depth.map(|v| v as usize);
        __result.max_pages = val.max_pages.map(|v| v as usize);
        __result.max_links_per_page = val.max_links_per_page.map(|v| v as usize);
        __result.max_concurrent = val.max_concurrent.map(|v| v as usize);
        if let Some(__v) = val.crawl_strategy {
            __result.crawl_strategy = __v.into();
        }
        __result.content_filter = val.content_filter.map(Into::into);
        __result.bm25_query = val.bm25_query;
        __result.bm25_threshold = val.bm25_threshold;
        if let Some(__v) = val.respect_robots_txt {
            __result.respect_robots_txt = __v;
        }
        if let Some(__v) = val.soft_http_errors {
            __result.soft_http_errors = __v;
        }
        __result.user_agent = val.user_agent;
        if let Some(__v) = val.stay_on_domain {
            __result.stay_on_domain = __v;
        }
        if let Some(__v) = val.allow_subdomains {
            __result.allow_subdomains = __v;
        }
        if let Some(__v) = val.include_paths {
            __result.include_paths = __v.into_iter().collect();
        }
        if let Some(__v) = val.exclude_paths {
            __result.exclude_paths = __v.into_iter().collect();
        }
        if let Some(__v) = val.custom_headers {
            __result.custom_headers = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        if let Some(__v) = val.request_timeout {
            __result.request_timeout = std::time::Duration::from_millis(__v as u64);
        }
        __result.rate_limit_ms = val.rate_limit_ms.map(|v| v as u64);
        if let Some(__v) = val.max_redirects {
            __result.max_redirects = __v as usize;
        }
        if let Some(__v) = val.retry_count {
            __result.retry_count = __v as usize;
        }
        if let Some(__v) = val.retry_codes {
            __result.retry_codes = __v.into_iter().collect();
        }
        if let Some(__v) = val.cookies_enabled {
            __result.cookies_enabled = __v;
        }
        __result.auth = val.auth.map(Into::into);
        __result.max_body_size = val.max_body_size.map(|v| v as usize);
        if let Some(__v) = val.remove_tags {
            __result.remove_tags = __v.into_iter().collect();
        }
        if let Some(__v) = val.content {
            __result.content = __v.into();
        }
        __result.map_limit = val.map_limit.map(|v| v as usize);
        __result.map_search = val.map_search;
        if let Some(__v) = val.download_assets {
            __result.download_assets = __v;
        }
        if let Some(__v) = val.asset_types {
            __result.asset_types = __v.into_iter().map(Into::into).collect();
        }
        __result.max_asset_size = val.max_asset_size.map(|v| v as usize);
        if let Some(__v) = val.browser {
            __result.browser = __v.into();
        }
        __result.proxy = val.proxy.map(Into::into);
        if let Some(__v) = val.user_agents {
            __result.user_agents = __v.into_iter().collect();
        }
        if let Some(__v) = val.capture_screenshot {
            __result.capture_screenshot = __v;
        }
        if let Some(__v) = val.follow_document_urls {
            __result.follow_document_urls = __v;
        }
        __result.document_url_depth = val.document_url_depth;
        if let Some(__v) = val.download_documents {
            __result.download_documents = __v;
        }
        __result.document_max_size = val.document_max_size.map(|v| v as usize);
        if let Some(__v) = val.document_mime_types {
            __result.document_mime_types = __v.into_iter().collect();
        }
        __result.document_output_dir = val.document_output_dir.map(Into::into);
        __result.document_content_encoding = val.document_content_encoding.map(Into::into);
        __result.warc_output = val.warc_output.map(Into::into);
        __result.browser_profile = val.browser_profile;
        if let Some(__v) = val.save_browser_profile {
            __result.save_browser_profile = __v;
        }
        if let Some(__v) = val.ssrf {
            __result.ssrf = __v.into();
        }
        __result.ssrf_deny_private_explicit = val.ssrf_deny_private_explicit;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<crawlberg::CrawlConfig> for JsCrawlConfig {
    fn from(val: crawlberg::CrawlConfig) -> Self {
        Self {
            max_depth: val.max_depth.map(|v| v as i64),
            max_pages: val.max_pages.map(|v| v as i64),
            max_links_per_page: val.max_links_per_page.map(|v| v as i64),
            max_concurrent: val.max_concurrent.map(|v| v as i64),
            crawl_strategy: Some(val.crawl_strategy.into()),
            content_filter: val.content_filter.map(Into::into),
            bm25_query: val.bm25_query.map(|v| v.to_string()),
            bm25_threshold: val.bm25_threshold,
            respect_robots_txt: Some(val.respect_robots_txt),
            soft_http_errors: Some(val.soft_http_errors),
            user_agent: val.user_agent.map(|v| v.to_string()),
            stay_on_domain: Some(val.stay_on_domain),
            allow_subdomains: Some(val.allow_subdomains),
            include_paths: Some(val.include_paths.into_iter().collect()),
            exclude_paths: Some(val.exclude_paths.into_iter().collect()),
            custom_headers: Some(
                val.custom_headers
                    .into_iter()
                    .map(|(k, v)| (k.into(), v.into()))
                    .collect(),
            ),
            request_timeout: Some(val.request_timeout.as_millis() as u64 as i64),
            rate_limit_ms: val.rate_limit_ms.map(|v| v as i64),
            max_redirects: Some(val.max_redirects as i64),
            retry_count: Some(val.retry_count as i64),
            retry_codes: Some(val.retry_codes.into_iter().collect()),
            cookies_enabled: Some(val.cookies_enabled),
            auth: val.auth.map(Into::into),
            max_body_size: val.max_body_size.map(|v| v as i64),
            remove_tags: Some(val.remove_tags.into_iter().collect()),
            content: Some(val.content.into()),
            map_limit: val.map_limit.map(|v| v as i64),
            map_search: val.map_search.map(|v| v.to_string()),
            download_assets: Some(val.download_assets),
            asset_types: Some(val.asset_types.into_iter().map(Into::into).collect()),
            max_asset_size: val.max_asset_size.map(|v| v as i64),
            browser: Some(val.browser.into()),
            proxy: val.proxy.map(Into::into),
            user_agents: Some(val.user_agents.into_iter().collect()),
            capture_screenshot: Some(val.capture_screenshot),
            follow_document_urls: Some(val.follow_document_urls),
            document_url_depth: val.document_url_depth,
            download_documents: Some(val.download_documents),
            document_max_size: val.document_max_size.map(|v| v as i64),
            document_mime_types: Some(val.document_mime_types.into_iter().collect()),
            document_output_dir: val.document_output_dir.map(|p| p.to_string_lossy().to_string()),
            document_content_encoding: val.document_content_encoding.map(Into::into),
            warc_output: val.warc_output.map(|p| p.to_string_lossy().to_string()),
            browser_profile: val.browser_profile.map(|v| v.to_string()),
            save_browser_profile: Some(val.save_browser_profile),
            ssrf: Some(val.ssrf.into()),
            ssrf_deny_private_explicit: val.ssrf_deny_private_explicit,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsCsvConfig> for xberg::CsvConfig {
    fn from(val: JsCsvConfig) -> Self {
        let mut __result = xberg::CsvConfig::default();
        __result.delimiter = val.delimiter;
        if let Some(__v) = val.comment_prefixes {
            __result.comment_prefixes = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CsvConfig> for JsCsvConfig {
    fn from(val: xberg::CsvConfig) -> Self {
        Self {
            delimiter: val.delimiter.map(|v| v.to_string()),
            comment_prefixes: Some(val.comment_prefixes.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsCsvMetadata> for xberg::CsvMetadata {
    fn from(val: JsCsvMetadata) -> Self {
        let mut __result = xberg::CsvMetadata::default();
        if let Some(__v) = val.row_count {
            __result.row_count = __v;
        }
        if let Some(__v) = val.column_count {
            __result.column_count = __v;
        }
        __result.delimiter = val.delimiter;
        if let Some(__v) = val.has_header {
            __result.has_header = __v;
        }
        __result.column_types = val.column_types.map(|v| v.into_iter().collect());
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::CsvMetadata> for JsCsvMetadata {
    fn from(val: xberg::CsvMetadata) -> Self {
        Self {
            row_count: Some(val.row_count),
            column_count: Some(val.column_count),
            delimiter: val.delimiter.map(|v| v.to_string()),
            has_header: Some(val.has_header),
            column_types: val.column_types.map(|v| v.into_iter().collect()),
        }
    }
}

impl From<JsDbfFieldInfo> for xberg::DbfFieldInfo {
    fn from(val: JsDbfFieldInfo) -> Self {
        Self {
            name: val.name,
            field_type: val.field_type,
        }
    }
}

impl From<xberg::DbfFieldInfo> for JsDbfFieldInfo {
    fn from(val: xberg::DbfFieldInfo) -> Self {
        Self {
            name: val.name.to_string(),
            field_type: val.field_type.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsDbfMetadata> for xberg::DbfMetadata {
    fn from(val: JsDbfMetadata) -> Self {
        let mut __result = xberg::DbfMetadata::default();
        if let Some(__v) = val.record_count {
            __result.record_count = __v as usize;
        }
        if let Some(__v) = val.field_count {
            __result.field_count = __v as usize;
        }
        if let Some(__v) = val.fields {
            __result.fields = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DbfMetadata> for JsDbfMetadata {
    fn from(val: xberg::DbfMetadata) -> Self {
        Self {
            record_count: Some(val.record_count as i64),
            field_count: Some(val.field_count as i64),
            fields: Some(val.fields.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::candle_ocr::DeepseekOcrBackendOptions> for JsDeepseekOcrBackendOptions {
    fn from(val: xberg::candle_ocr::DeepseekOcrBackendOptions) -> Self {
        Self {
            model_path: val.model_path.map(|v| v.to_string()),
            device: val.device.map(Into::into),
            version: val.version,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::api::DetectResponse> for JsDetectResponse {
    fn from(val: xberg::api::DetectResponse) -> Self {
        Self {
            mime_type: val.mime_type.to_string(),
            filename: val.filename.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DetectionResult> for JsDetectionResult {
    fn from(val: xberg::DetectionResult) -> Self {
        Self {
            page_width: val.page_width,
            page_height: val.page_height,
            detections: val.detections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DiffHunk> for JsDiffHunk {
    fn from(val: xberg::DiffHunk) -> Self {
        Self {
            from_line: val.from_line as i64,
            from_count: val.from_count as i64,
            to_line: val.to_line as i64,
            to_count: val.to_count as i64,
            lines: val.lines.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsDiffOptions> for xberg::DiffOptions {
    fn from(val: JsDiffOptions) -> Self {
        let mut __result = xberg::DiffOptions::default();
        if let Some(__v) = val.include_metadata {
            __result.include_metadata = __v;
        }
        if let Some(__v) = val.include_embedded {
            __result.include_embedded = __v;
        }
        __result.max_content_chars = val.max_content_chars.map(|v| v as usize);
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DiffOptions> for JsDiffOptions {
    fn from(val: xberg::DiffOptions) -> Self {
        Self {
            include_metadata: Some(val.include_metadata),
            include_embedded: Some(val.include_embedded),
            max_content_chars: val.max_content_chars.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsDjotAttributeGroup> for xberg::DjotAttributeGroup {
    fn from(val: JsDjotAttributeGroup) -> Self {
        let mut __result = xberg::DjotAttributeGroup::default();
        if let Some(__v) = val.identifier {
            __result.identifier = __v;
        }
        if let Some(__v) = val.attributes {
            __result.attributes = __v.into();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DjotAttributeGroup> for JsDjotAttributeGroup {
    fn from(val: xberg::DjotAttributeGroup) -> Self {
        Self {
            identifier: Some(val.identifier.to_string()),
            attributes: Some(val.attributes.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDjotContent> for xberg::DjotContent {
    fn from(val: JsDjotContent) -> Self {
        Self {
            plain_text: val.plain_text,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into(),
            tables: val.tables.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            footnotes: val.footnotes.into_iter().map(Into::into).collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DjotContent> for JsDjotContent {
    fn from(val: xberg::DjotContent) -> Self {
        Self {
            plain_text: val.plain_text.to_string(),
            blocks: val.blocks.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into(),
            tables: val.tables.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            links: val.links.into_iter().map(Into::into).collect(),
            footnotes: val.footnotes.into_iter().map(Into::into).collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDjotImage> for xberg::DjotImage {
    fn from(val: JsDjotImage) -> Self {
        Self {
            src: val.src,
            alt: val.alt,
            title: val.title,
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DjotImage> for JsDjotImage {
    fn from(val: xberg::DjotImage) -> Self {
        Self {
            src: val.src.to_string(),
            alt: val.alt.to_string(),
            title: val.title.map(|v| v.to_string()),
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDjotLink> for xberg::DjotLink {
    fn from(val: JsDjotLink) -> Self {
        Self {
            url: val.url,
            text: val.text,
            title: val.title,
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DjotLink> for JsDjotLink {
    fn from(val: xberg::DjotLink) -> Self {
        Self {
            url: val.url.to_string(),
            text: val.text.to_string(),
            title: val.title.map(|v| v.to_string()),
            attributes: val.attributes.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDoctorCheck> for xberg::DoctorCheck {
    fn from(val: JsDoctorCheck) -> Self {
        Self {
            name: val.name,
            status: val.status.into(),
            message: val.message,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DoctorCheck> for JsDoctorCheck {
    fn from(val: xberg::DoctorCheck) -> Self {
        Self {
            name: val.name.to_string(),
            status: val.status.into(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsDoctorReport> for xberg::DoctorReport {
    fn from(val: JsDoctorReport) -> Self {
        let mut __result = xberg::DoctorReport::default();
        if let Some(__v) = val.checks {
            __result.checks = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DoctorReport> for JsDoctorReport {
    fn from(val: xberg::DoctorReport) -> Self {
        Self {
            checks: Some(val.checks.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DocumentBoundary> for JsDocumentBoundary {
    fn from(val: xberg::DocumentBoundary) -> Self {
        Self {
            start_page: val.start_page,
            end_page: val.end_page,
            confidence: val.confidence as f64,
            reason: val.reason.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsDocumentCounts> for xberg::DocumentCounts {
    fn from(val: JsDocumentCounts) -> Self {
        let mut __result = xberg::DocumentCounts::default();
        if let Some(__v) = val.pages {
            __result.pages = __v as usize;
        }
        if let Some(__v) = val.tables {
            __result.tables = __v as usize;
        }
        if let Some(__v) = val.images {
            __result.images = __v as usize;
        }
        __result
    }
}

impl From<xberg::DocumentCounts> for JsDocumentCounts {
    fn from(val: xberg::DocumentCounts) -> Self {
        Self {
            pages: Some(val.pages as i64),
            tables: Some(val.tables as i64),
            images: Some(val.images as i64),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DocumentMetadata> for JsDocumentMetadata {
    fn from(val: xberg::DocumentMetadata) -> Self {
        Self {
            mime_type: val.mime_type.to_string(),
            size_bytes: val.size_bytes as i64,
            page_count: val.page_count,
            force_ocr: val.force_ocr,
            user_chunk_config: val.user_chunk_config.map(Into::into),
            chunking_enabled: val.chunking_enabled,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsDocumentNode> for xberg::DocumentNode {
    fn from(val: JsDocumentNode) -> Self {
        Self {
            id: val.id,
            content: val.content.into(),
            parent: (val.parent).map(xberg::NodeIndex),
            children: (val.children.into_iter().collect::<Vec<_>>())
                .into_iter()
                .map(xberg::NodeIndex)
                .collect(),
            content_layer: val.content_layer.into(),
            page: val.page,
            page_end: val.page_end,
            bbox: val.bbox.map(Into::into),
            annotations: val.annotations.into_iter().map(Into::into).collect(),
            attributes: val
                .attributes
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentNode> for JsDocumentNode {
    fn from(val: xberg::DocumentNode) -> Self {
        Self {
            id: val.id.to_string(),
            content: val.content.into(),
            parent: val.parent.map(|v| v.0),
            children: val
                .children
                .iter()
                .map(|v| v.0)
                .collect::<Vec<_>>()
                .into_iter()
                .collect(),
            content_layer: val.content_layer.into(),
            page: val.page,
            page_end: val.page_end,
            bbox: val.bbox.map(Into::into),
            annotations: val.annotations.into_iter().map(Into::into).collect(),
            attributes: val
                .attributes
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDocumentRelationship> for xberg::DocumentRelationship {
    fn from(val: JsDocumentRelationship) -> Self {
        Self {
            source: xberg::NodeIndex(val.source),
            target: xberg::NodeIndex(val.target),
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DocumentRelationship> for JsDocumentRelationship {
    fn from(val: xberg::DocumentRelationship) -> Self {
        Self {
            source: val.source.0,
            target: val.target.0,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDocumentRevision> for xberg::DocumentRevision {
    fn from(val: JsDocumentRevision) -> Self {
        Self {
            revision_id: val.revision_id,
            author: val.author,
            timestamp: val.timestamp,
            kind: val.kind.into(),
            anchor: val.anchor.map(Into::into),
            delta: val.delta.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentRevision> for JsDocumentRevision {
    fn from(val: xberg::DocumentRevision) -> Self {
        Self {
            revision_id: val.revision_id.to_string(),
            author: val.author.map(|v| v.to_string()),
            timestamp: val.timestamp.map(|v| v.to_string()),
            kind: val.kind.into(),
            anchor: val.anchor.map(Into::into),
            delta: val.delta.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsDocumentStructure> for xberg::DocumentStructure {
    fn from(val: JsDocumentStructure) -> Self {
        let mut __result = xberg::DocumentStructure::default();
        if let Some(__v) = val.nodes {
            __result.nodes = __v.into_iter().map(Into::into).collect();
        }
        __result.source_format = val.source_format;
        if let Some(__v) = val.relationships {
            __result.relationships = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.node_types {
            __result.node_types = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocumentStructure> for JsDocumentStructure {
    fn from(val: xberg::DocumentStructure) -> Self {
        Self {
            nodes: Some(val.nodes.into_iter().map(Into::into).collect()),
            source_format: val.source_format.map(|v| v.to_string()),
            relationships: Some(val.relationships.into_iter().map(Into::into).collect()),
            node_types: Some(val.node_types.into_iter().collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDocumentSummary> for xberg::DocumentSummary {
    fn from(val: JsDocumentSummary) -> Self {
        Self {
            text: val.text,
            strategy: val.strategy.into(),
            token_count: val.token_count,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::DocumentSummary> for JsDocumentSummary {
    fn from(val: xberg::DocumentSummary) -> Self {
        Self {
            text: val.text.to_string(),
            strategy: val.strategy.into(),
            token_count: val.token_count,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsDocxAppProperties> for xberg::DocxAppProperties {
    fn from(val: JsDocxAppProperties) -> Self {
        let mut __result = xberg::DocxAppProperties::default();
        __result.application = val.application;
        __result.app_version = val.app_version;
        __result.template = val.template;
        __result.total_time = val.total_time;
        __result.pages = val.pages;
        __result.words = val.words;
        __result.characters = val.characters;
        __result.characters_with_spaces = val.characters_with_spaces;
        __result.lines = val.lines;
        __result.paragraphs = val.paragraphs;
        __result.company = val.company;
        __result.doc_security = val.doc_security;
        __result.scale_crop = val.scale_crop;
        __result.links_up_to_date = val.links_up_to_date;
        __result.shared_doc = val.shared_doc;
        __result.hyperlinks_changed = val.hyperlinks_changed;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::DocxAppProperties> for JsDocxAppProperties {
    fn from(val: xberg::DocxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            template: val.template.map(|v| v.to_string()),
            total_time: val.total_time,
            pages: val.pages,
            words: val.words,
            characters: val.characters,
            characters_with_spaces: val.characters_with_spaces,
            lines: val.lines,
            paragraphs: val.paragraphs,
            company: val.company.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsDocxMetadata> for xberg::DocxMetadata {
    fn from(val: JsDocxMetadata) -> Self {
        let mut __result = xberg::DocxMetadata::default();
        __result.core_properties = val.core_properties.map(Into::into);
        __result.app_properties = val.app_properties.map(Into::into);
        __result.custom_properties = val
            .custom_properties
            .map(|m| m.into_iter().map(|(k, v)| (k.into(), v)).collect());
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::DocxMetadata> for JsDocxMetadata {
    fn from(val: xberg::DocxMetadata) -> Self {
        Self {
            core_properties: val.core_properties.map(Into::into),
            app_properties: val.app_properties.map(Into::into),
            custom_properties: val
                .custom_properties
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v)).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsElement> for xberg::Element {
    fn from(val: JsElement) -> Self {
        Self {
            element_id: val.element_id,
            element_type: val.element_type.into(),
            text: val.text,
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::Element> for JsElement {
    fn from(val: xberg::Element) -> Self {
        Self {
            element_id: val.element_id.to_string(),
            element_type: val.element_type.into(),
            text: val.text.to_string(),
            metadata: val.metadata.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsElementMetadata> for xberg::ElementMetadata {
    fn from(val: JsElementMetadata) -> Self {
        Self {
            page_number: val.page_number,
            filename: val.filename,
            coordinates: val.coordinates.map(Into::into),
            element_index: val.element_index.map(|v| v as usize),
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ElementMetadata> for JsElementMetadata {
    fn from(val: xberg::ElementMetadata) -> Self {
        Self {
            page_number: val.page_number,
            filename: val.filename.map(|v| v.to_string()),
            coordinates: val.coordinates.map(Into::into),
            element_index: val.element_index.map(|v| v as i64),
            additional: val.additional.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmailAttachment> for JsEmailAttachment {
    fn from(val: xberg::EmailAttachment) -> Self {
        Self {
            name: val.name.map(|v| v.to_string()),
            filename: val.filename.map(|v| v.to_string()),
            mime_type: val.mime_type.map(|v| v.to_string()),
            size: val.size.map(|v| v as i64),
            is_image: val.is_image,
            data: val.data.map(|v| v.to_vec().into()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsEmailConfig> for xberg::EmailConfig {
    fn from(val: JsEmailConfig) -> Self {
        let mut __result = xberg::EmailConfig::default();
        __result.msg_fallback_codepage = val.msg_fallback_codepage;
        __result
    }
}

impl From<xberg::EmailConfig> for JsEmailConfig {
    fn from(val: xberg::EmailConfig) -> Self {
        Self {
            msg_fallback_codepage: val.msg_fallback_codepage,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmailExtractionResult> for JsEmailExtractionResult {
    fn from(val: xberg::EmailExtractionResult) -> Self {
        Self {
            subject: val.subject.map(|v| v.to_string()),
            from_email: val.from_email.map(|v| v.to_string()),
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            date: val.date.map(|v| v.to_string()),
            message_id: val.message_id.map(|v| v.to_string()),
            plain_text: val.plain_text.map(|v| v.to_string()),
            html_content: val.html_content.map(|v| v.to_string()),
            content: val.content.to_string(),
            attachments: val.attachments.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsEmailMetadata> for xberg::EmailMetadata {
    fn from(val: JsEmailMetadata) -> Self {
        let mut __result = xberg::EmailMetadata::default();
        __result.from_email = val.from_email;
        __result.from_name = val.from_name;
        if let Some(__v) = val.to_emails {
            __result.to_emails = __v.into_iter().collect();
        }
        if let Some(__v) = val.cc_emails {
            __result.cc_emails = __v.into_iter().collect();
        }
        if let Some(__v) = val.bcc_emails {
            __result.bcc_emails = __v.into_iter().collect();
        }
        __result.message_id = val.message_id;
        if let Some(__v) = val.attachments {
            __result.attachments = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::EmailMetadata> for JsEmailMetadata {
    fn from(val: xberg::EmailMetadata) -> Self {
        Self {
            from_email: val.from_email.map(|v| v.to_string()),
            from_name: val.from_name.map(|v| v.to_string()),
            to_emails: Some(val.to_emails.into_iter().collect()),
            cc_emails: Some(val.cc_emails.into_iter().collect()),
            bcc_emails: Some(val.bcc_emails.into_iter().collect()),
            message_id: val.message_id.map(|v| v.to_string()),
            attachments: Some(val.attachments.into_iter().collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::EmbeddedChanges> for JsEmbeddedChanges {
    fn from(val: xberg::EmbeddedChanges) -> Self {
        Self {
            added: Some(val.added.into_iter().map(Into::into).collect()),
            removed: Some(val.removed.into_iter().map(Into::into).collect()),
            changed: Some(val.changed.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::EmbeddedDiff> for JsEmbeddedDiff {
    fn from(val: xberg::EmbeddedDiff) -> Self {
        Self {
            path: val.path.to_string(),
            diff: (*val.diff).into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::pdf::embedded_files::EmbeddedFile> for JsEmbeddedFile {
    fn from(val: xberg::pdf::embedded_files::EmbeddedFile) -> Self {
        Self {
            name: val.name.to_string(),
            data: val.data.to_vec().into(),
            compressed_size: val.compressed_size as i64,
            mime_type: val.mime_type.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsEmbeddingConfig> for xberg::EmbeddingConfig {
    fn from(val: JsEmbeddingConfig) -> Self {
        let mut __result = xberg::EmbeddingConfig::default();
        if let Some(__v) = val.model {
            __result.model = __v.into();
        }
        if let Some(__v) = val.normalize {
            __result.normalize = __v;
        }
        if let Some(__v) = val.batch_size {
            __result.batch_size = __v as usize;
        }
        if let Some(__v) = val.show_download_progress {
            __result.show_download_progress = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        __result.acceleration = val.acceleration.map(Into::into);
        __result.max_embed_duration_secs = val.max_embed_duration_secs.map(|v| v as u64);
        __result.max_sequence_length = val.max_sequence_length.map(|v| v as usize);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::EmbeddingConfig> for JsEmbeddingConfig {
    fn from(val: xberg::EmbeddingConfig) -> Self {
        Self {
            model: Some(val.model.into()),
            normalize: Some(val.normalize),
            batch_size: Some(val.batch_size as i64),
            show_download_progress: Some(val.show_download_progress),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
            max_sequence_length: val.max_sequence_length.map(|v| v as i64),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsEntity> for xberg::Entity {
    fn from(val: JsEntity) -> Self {
        Self {
            category: val.category.into(),
            text: val.text,
            start: val.start,
            end: val.end,
            confidence: val.confidence.map(|v| v as f32),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Entity> for JsEntity {
    fn from(val: xberg::Entity) -> Self {
        Self {
            category: val.category.into(),
            text: val.text.to_string(),
            start: val.start,
            end: val.end,
            confidence: val.confidence.map(|v| v as f64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsEpubMetadata> for xberg::EpubMetadata {
    fn from(val: JsEpubMetadata) -> Self {
        let mut __result = xberg::EpubMetadata::default();
        __result.coverage = val.coverage;
        __result.dc_format = val.dc_format;
        __result.relation = val.relation;
        __result.source = val.source;
        __result.dc_type = val.dc_type;
        __result.cover_image = val.cover_image;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::EpubMetadata> for JsEpubMetadata {
    fn from(val: xberg::EpubMetadata) -> Self {
        Self {
            coverage: val.coverage.map(|v| v.to_string()),
            dc_format: val.dc_format.map(|v| v.to_string()),
            relation: val.relation.map(|v| v.to_string()),
            source: val.source.map(|v| v.to_string()),
            dc_type: val.dc_type.map(|v| v.to_string()),
            cover_image: val.cover_image.map(|v| v.to_string()),
        }
    }
}

impl From<JsErrorMetadata> for xberg::ErrorMetadata {
    fn from(val: JsErrorMetadata) -> Self {
        Self {
            error_type: val.error_type,
            message: val.message,
        }
    }
}

impl From<xberg::ErrorMetadata> for JsErrorMetadata {
    fn from(val: xberg::ErrorMetadata) -> Self {
        Self {
            error_type: val.error_type.to_string(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsExcelMetadata> for xberg::ExcelMetadata {
    fn from(val: JsExcelMetadata) -> Self {
        let mut __result = xberg::ExcelMetadata::default();
        __result.sheet_count = val.sheet_count;
        __result.sheet_names = val.sheet_names.map(|v| v.into_iter().collect());
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExcelMetadata> for JsExcelMetadata {
    fn from(val: xberg::ExcelMetadata) -> Self {
        Self {
            sheet_count: val.sheet_count,
            sheet_names: val.sheet_names.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::ExcelSheet> for JsExcelSheet {
    fn from(val: xberg::ExcelSheet) -> Self {
        Self {
            name: val.name.to_string(),
            markdown: val.markdown.to_string(),
            row_count: val.row_count as i64,
            col_count: val.col_count as i64,
            cell_count: val.cell_count as i64,
            table_cells: val.table_cells.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExcelWorkbook> for JsExcelWorkbook {
    fn from(val: xberg::ExcelWorkbook) -> Self {
        Self {
            sheets: val.sheets.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExtractInput> for xberg::ExtractInput {
    fn from(val: JsExtractInput) -> Self {
        let mut __result = xberg::ExtractInput::default();
        if let Some(__v) = val.kind {
            __result.kind = __v.into();
        }
        __result.bytes = val.bytes.map(|v| v.to_vec().into());
        __result.uri = val.uri;
        __result.mime_type = val.mime_type;
        __result.filename = val.filename;
        __result.config = val.config.map(Into::into);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractInput> for JsExtractInput {
    fn from(val: xberg::ExtractInput) -> Self {
        Self {
            kind: Some(val.kind.into()),
            bytes: val.bytes.map(|v| v.to_vec().into()),
            uri: val.uri.map(|v| v.to_string()),
            mime_type: val.mime_type.map(|v| v.to_string()),
            filename: val.filename.map(|v| v.to_string()),
            config: val.config.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExtractedDocument> for xberg::ExtractedDocument {
    fn from(val: JsExtractedDocument) -> Self {
        let mut __result = xberg::ExtractedDocument::default();
        if let Some(__v) = val.content {
            __result.content = __v;
        }
        if let Some(__v) = val.mime_type {
            __result.mime_type = __v.into();
        }
        if let Some(__v) = val.metadata {
            __result.metadata = __v.into();
        }
        __result.extraction_method = val.extraction_method.map(Into::into);
        if let Some(__v) = val.tables {
            __result.tables = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.counts {
            __result.counts = __v.into();
        }
        __result.detected_languages = val.detected_languages.map(|v| v.into_iter().collect());
        __result.detected_language_confidences = val
            .detected_language_confidences
            .map(|v| v.into_iter().map(Into::into).collect());
        __result.chunks = val.chunks.map(|v| v.into_iter().map(Into::into).collect());
        __result.images = val.images.map(|v| v.into_iter().map(Into::into).collect());
        __result.pages = val.pages.map(|v| v.into_iter().map(Into::into).collect());
        __result.elements = val.elements.map(|v| v.into_iter().map(Into::into).collect());
        __result.djot_content = val.djot_content.map(Into::into);
        __result.ocr_elements = val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect());
        __result.document = val.document.map(Into::into);
        #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
        {
            __result.extracted_keywords = val.extracted_keywords.map(|v| v.into_iter().map(Into::into).collect());
        }
        __result.quality_score = val.quality_score;
        if let Some(__v) = val.processing_warnings {
            __result.processing_warnings = __v.into_iter().map(Into::into).collect();
        }
        __result.annotations = val.annotations.map(|v| v.into_iter().map(Into::into).collect());
        __result.children = val.children.map(|v| v.into_iter().map(Into::into).collect());
        __result.uris = val.uris.map(|v| v.into_iter().map(Into::into).collect());
        __result.revisions = val.revisions.map(|v| v.into_iter().map(Into::into).collect());
        __result.structured_output = val.structured_output;
        #[cfg(feature = "tree-sitter")]
        {
            __result.code_intelligence = val.code_intelligence;
        }
        __result.llm_usage = val.llm_usage.map(|v| v.into_iter().map(Into::into).collect());
        __result.entities = val.entities.map(|v| v.into_iter().map(Into::into).collect());
        __result.summary = val.summary.map(Into::into);
        #[cfg(feature = "heuristics")]
        {
            __result.extraction_confidence = val.extraction_confidence.map(Into::into);
        }
        __result.translation = val.translation.map(Into::into);
        __result.page_classifications = val
            .page_classifications
            .map(|v| v.into_iter().map(Into::into).collect());
        __result.redaction_report = val.redaction_report.map(Into::into);
        if let Some(__v) = val.formulas {
            __result.formulas = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.form_fields {
            __result.form_fields = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractedDocument> for JsExtractedDocument {
    fn from(val: xberg::ExtractedDocument) -> Self {
        Self {
            content: Some(val.content.to_string()),
            mime_type: Some(val.mime_type.to_string()),
            metadata: Some(val.metadata.into()),
            extraction_method: val.extraction_method.map(Into::into),
            tables: Some(val.tables.into_iter().map(Into::into).collect()),
            counts: Some(val.counts.into()),
            detected_languages: val.detected_languages.map(|v| v.into_iter().collect()),
            detected_language_confidences: val
                .detected_language_confidences
                .map(|v| v.into_iter().map(Into::into).collect()),
            chunks: val.chunks.map(|v| v.into_iter().map(Into::into).collect()),
            images: val.images.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
            elements: val.elements.map(|v| v.into_iter().map(Into::into).collect()),
            djot_content: val.djot_content.map(Into::into),
            ocr_elements: val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            extracted_keywords: val.extracted_keywords.map(|v| v.into_iter().map(Into::into).collect()),
            quality_score: val.quality_score,
            processing_warnings: Some(val.processing_warnings.into_iter().map(Into::into).collect()),
            annotations: val.annotations.map(|v| v.into_iter().map(Into::into).collect()),
            children: val.children.map(|v| v.into_iter().map(Into::into).collect()),
            uris: val.uris.map(|v| v.into_iter().map(Into::into).collect()),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
            structured_output: val.structured_output,
            #[cfg(feature = "tree-sitter")]
            code_intelligence: val.code_intelligence,
            llm_usage: val.llm_usage.map(|v| v.into_iter().map(Into::into).collect()),
            entities: val.entities.map(|v| v.into_iter().map(Into::into).collect()),
            summary: val.summary.map(Into::into),
            #[cfg(feature = "heuristics")]
            extraction_confidence: val.extraction_confidence.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classifications: val
                .page_classifications
                .map(|v| v.into_iter().map(Into::into).collect()),
            redaction_report: val.redaction_report.map(Into::into),
            formulas: Some(val.formulas.into_iter().map(Into::into).collect()),
            form_fields: Some(val.form_fields.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExtractedImage> for xberg::ExtractedImage {
    fn from(val: JsExtractedImage) -> Self {
        let mut __result = xberg::ExtractedImage::default();
        if let Some(__v) = val.data {
            __result.data = __v.to_vec().into();
        }
        if let Some(__v) = val.format {
            __result.format = __v.into();
        }
        if let Some(__v) = val.image_index {
            __result.image_index = __v;
        }
        __result.page_number = val.page_number;
        __result.width = val.width;
        __result.height = val.height;
        __result.colorspace = val.colorspace;
        __result.bits_per_component = val.bits_per_component;
        if let Some(__v) = val.is_mask {
            __result.is_mask = __v;
        }
        __result.description = val.description;
        __result.ocr_result = val.ocr_result.map(Into::into).map(Box::new);
        __result.bounding_box = val.bounding_box.map(Into::into);
        __result.source_path = val.source_path;
        __result.image_kind = val.image_kind.map(Into::into);
        __result.kind_confidence = val.kind_confidence.map(|v| v as f32);
        __result.cluster_id = val.cluster_id;
        __result.caption = val.caption;
        __result.qr_codes = val.qr_codes.map(|v| v.into_iter().map(Into::into).collect());
        __result.data_base64 = val.data_base64;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractedImage> for JsExtractedImage {
    fn from(val: xberg::ExtractedImage) -> Self {
        Self {
            data: Some(val.data.to_vec().into()),
            format: Some(val.format.to_string()),
            image_index: Some(val.image_index),
            page_number: val.page_number,
            width: val.width,
            height: val.height,
            colorspace: val.colorspace.map(|v| v.to_string()),
            bits_per_component: val.bits_per_component,
            is_mask: Some(val.is_mask),
            description: val.description.map(|v| v.to_string()),
            ocr_result: val.ocr_result.map(|v| (*v).into()),
            bounding_box: val.bounding_box.map(Into::into),
            source_path: val.source_path.map(|v| v.to_string()),
            image_kind: val.image_kind.map(Into::into),
            kind_confidence: val.kind_confidence.map(|v| v as f64),
            cluster_id: val.cluster_id,
            caption: val.caption.map(|v| v.to_string()),
            qr_codes: val.qr_codes.map(|v| v.into_iter().map(Into::into).collect()),
            data_base64: val.data_base64.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsExtractedUri> for xberg::ExtractedUri {
    fn from(val: JsExtractedUri) -> Self {
        Self {
            url: val.url,
            label: val.label,
            page: val.page,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractedUri> for JsExtractedUri {
    fn from(val: xberg::ExtractedUri) -> Self {
        Self {
            url: val.url.to_string(),
            label: val.label.map(|v| v.to_string()),
            page: val.page,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExtractionConfidence> for xberg::ExtractionConfidence {
    fn from(val: JsExtractionConfidence) -> Self {
        Self {
            text_coverage: val.text_coverage as f32,
            ocr_aggregate: val.ocr_aggregate.map(|v| v as f32),
            schema_compliance: val.schema_compliance.into(),
            combined: val.combined as f32,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractionConfidence> for JsExtractionConfidence {
    fn from(val: xberg::ExtractionConfidence) -> Self {
        Self {
            text_coverage: val.text_coverage as f64,
            ocr_aggregate: val.ocr_aggregate.map(|v| v as f64),
            schema_compliance: val.schema_compliance.into(),
            combined: val.combined as f64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExtractionConfig> for xberg::ExtractionConfig {
    fn from(val: JsExtractionConfig) -> Self {
        let mut __result = xberg::ExtractionConfig::default();
        if let Some(__v) = val.mime_detection_policy {
            __result.mime_detection_policy = __v.into();
        }
        if let Some(__v) = val.use_cache {
            __result.use_cache = __v;
        }
        if let Some(__v) = val.enable_quality_processing {
            __result.enable_quality_processing = __v;
        }
        __result.ocr = val.ocr.map(Into::into);
        if let Some(__v) = val.force_ocr {
            __result.force_ocr = __v;
        }
        if let Some(__v) = val.ocr_strategy {
            __result.ocr_strategy = __v.into();
        }
        __result.force_ocr_pages = val.force_ocr_pages.map(|v| v.into_iter().collect());
        if let Some(__v) = val.disable_ocr {
            __result.disable_ocr = __v;
        }
        __result.chunking = val.chunking.map(Into::into);
        __result.content_filter = val.content_filter.map(Into::into);
        __result.images = val.images.map(Into::into);
        #[cfg(feature = "pdf")]
        {
            __result.pdf_options = val.pdf_options.map(Into::into);
        }
        __result.token_reduction = val.token_reduction.map(Into::into);
        __result.language_detection = val.language_detection.map(Into::into);
        __result.pages = val.pages.map(Into::into);
        #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
        {
            __result.keywords = val.keywords.map(Into::into);
        }
        __result.postprocessor = val.postprocessor.map(Into::into);
        #[cfg(feature = "html")]
        {
            __result.html_options = val.html_options.map(Into::into);
        }
        #[cfg(feature = "html")]
        {
            __result.html_output = val.html_output.map(Into::into);
        }
        __result.extraction_timeout_secs = val.extraction_timeout_secs.map(|v| v as u64);
        __result.max_concurrent_extractions = val.max_concurrent_extractions.map(|v| v as usize);
        if let Some(__v) = val.result_format {
            __result.result_format = __v.into();
        }
        __result.security_limits = val.security_limits.map(Into::into);
        __result.max_embedded_file_bytes = val.max_embedded_file_bytes.map(|v| v as u64);
        if let Some(__v) = val.output_format {
            __result.output_format = __v.into();
        }
        if let Some(__v) = val.escape_markdown {
            __result.escape_markdown = __v;
        }
        if let Some(__v) = val.table_anchors {
            __result.table_anchors = __v;
        }
        if let Some(__v) = val.jupyter_cell_rendering {
            __result.jupyter_cell_rendering = __v.into();
        }
        if let Some(__v) = val.apply_notebook_cell_tags {
            __result.apply_notebook_cell_tags = __v;
        }
        #[cfg(feature = "layout-types")]
        {
            __result.layout = val.layout.map(Into::into);
        }
        #[cfg(feature = "transcription-types")]
        {
            __result.transcription = val.transcription.map(Into::into);
        }
        if let Some(__v) = val.use_layout_for_markdown {
            __result.use_layout_for_markdown = __v;
        }
        if let Some(__v) = val.include_document_structure {
            __result.include_document_structure = __v;
        }
        __result.acceleration = val.acceleration.map(Into::into);
        __result.cache_namespace = val.cache_namespace;
        __result.cache_ttl_secs = val.cache_ttl_secs.map(|v| v as u64);
        __result.email = val.email.map(Into::into);
        __result.csv = val.csv.map(Into::into);
        __result.geojson = val.geojson.map(Into::into);
        __result.concurrency = val.concurrency.map(Into::into);
        if let Some(__v) = val.url {
            __result.url = __v.into();
        }
        if let Some(__v) = val.max_archive_depth {
            __result.max_archive_depth = __v as usize;
        }
        #[cfg(feature = "tree-sitter")]
        {
            __result.tree_sitter = val.tree_sitter.map(Into::into);
        }
        __result.structured_extraction = val.structured_extraction.map(Into::into);
        __result.ner = val.ner.map(Into::into);
        __result.redaction = val.redaction.map(Into::into);
        __result.summarization = val.summarization.map(Into::into);
        __result.translation = val.translation.map(Into::into);
        __result.page_classification = val.page_classification.map(Into::into);
        __result.chunk_classification = val.chunk_classification.map(Into::into);
        __result.captioning = val.captioning.map(Into::into);
        __result.qr_codes = val.qr_codes;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ExtractionConfig> for JsExtractionConfig {
    fn from(val: xberg::ExtractionConfig) -> Self {
        Self {
            mime_detection_policy: Some(val.mime_detection_policy.into()),
            use_cache: Some(val.use_cache),
            enable_quality_processing: Some(val.enable_quality_processing),
            ocr: val.ocr.map(Into::into),
            force_ocr: Some(val.force_ocr),
            ocr_strategy: Some(val.ocr_strategy.into()),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: Some(val.disable_ocr),
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            #[cfg(feature = "pdf")]
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            #[cfg(feature = "html")]
            html_options: val.html_options.map(Into::into),
            #[cfg(feature = "html")]
            html_output: val.html_output.map(Into::into),
            extraction_timeout_secs: val.extraction_timeout_secs.map(|v| v as i64),
            max_concurrent_extractions: val.max_concurrent_extractions.map(|v| v as i64),
            result_format: Some(val.result_format.into()),
            security_limits: val.security_limits.map(Into::into),
            max_embedded_file_bytes: val.max_embedded_file_bytes.map(|v| v as i64),
            output_format: Some(val.output_format.into()),
            escape_markdown: Some(val.escape_markdown),
            table_anchors: Some(val.table_anchors),
            jupyter_cell_rendering: Some(val.jupyter_cell_rendering.into()),
            apply_notebook_cell_tags: Some(val.apply_notebook_cell_tags),
            #[cfg(feature = "layout-types")]
            layout: val.layout.map(Into::into),
            #[cfg(feature = "transcription-types")]
            transcription: val.transcription.map(Into::into),
            use_layout_for_markdown: Some(val.use_layout_for_markdown),
            include_document_structure: Some(val.include_document_structure),
            acceleration: val.acceleration.map(Into::into),
            cache_namespace: val.cache_namespace.map(|v| v.to_string()),
            cache_ttl_secs: val.cache_ttl_secs.map(|v| v as i64),
            email: val.email.map(Into::into),
            csv: val.csv.map(Into::into),
            geojson: val.geojson.map(Into::into),
            concurrency: val.concurrency.map(Into::into),
            url: Some(val.url.into()),
            max_archive_depth: Some(val.max_archive_depth as i64),
            #[cfg(feature = "tree-sitter")]
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ExtractionDiff> for JsExtractionDiff {
    fn from(val: xberg::ExtractionDiff) -> Self {
        Self {
            content_diff: Some(val.content_diff.into_iter().map(Into::into).collect()),
            tables_added: Some(val.tables_added.into_iter().map(Into::into).collect()),
            tables_removed: Some(val.tables_removed.into_iter().map(Into::into).collect()),
            tables_changed: Some(val.tables_changed.into_iter().map(Into::into).collect()),
            metadata_changed: Some(val.metadata_changed),
            embedded_changes: Some(val.embedded_changes.into()),
        }
    }
}

impl From<JsExtractionErrorItem> for xberg::ExtractionErrorItem {
    fn from(val: JsExtractionErrorItem) -> Self {
        Self {
            index: val.index as usize,
            code: val.code,
            error_type: val.error_type,
            source: val.source,
            message: val.message,
        }
    }
}

impl From<xberg::ExtractionErrorItem> for JsExtractionErrorItem {
    fn from(val: xberg::ExtractionErrorItem) -> Self {
        Self {
            index: val.index as i64,
            code: val.code,
            error_type: val.error_type.to_string(),
            source: val.source.to_string(),
            message: val.message.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsExtractionResult> for xberg::ExtractionResult {
    fn from(val: JsExtractionResult) -> Self {
        let mut __result = xberg::ExtractionResult::default();
        if let Some(__v) = val.results {
            __result.results = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.errors {
            __result.errors = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.summary {
            __result.summary = __v.into();
        }
        if let Some(__v) = val.crawl_final_urls {
            __result.crawl_final_urls = __v.into_iter().collect();
        }
        if let Some(__v) = val.crawl_redirect_count {
            __result.crawl_redirect_count = __v as usize;
        }
        if let Some(__v) = val.crawl_unique_normalized_urls {
            __result.crawl_unique_normalized_urls = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ExtractionResult> for JsExtractionResult {
    fn from(val: xberg::ExtractionResult) -> Self {
        Self {
            results: Some(val.results.into_iter().map(Into::into).collect()),
            errors: Some(val.errors.into_iter().map(Into::into).collect()),
            summary: Some(val.summary.into()),
            crawl_final_urls: Some(val.crawl_final_urls.into_iter().collect()),
            crawl_redirect_count: Some(val.crawl_redirect_count as i64),
            crawl_unique_normalized_urls: Some(val.crawl_unique_normalized_urls.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsExtractionSummary> for xberg::ExtractionSummary {
    fn from(val: JsExtractionSummary) -> Self {
        let mut __result = xberg::ExtractionSummary::default();
        if let Some(__v) = val.inputs {
            __result.inputs = __v as usize;
        }
        if let Some(__v) = val.results {
            __result.results = __v as usize;
        }
        if let Some(__v) = val.errors {
            __result.errors = __v as usize;
        }
        if let Some(__v) = val.remote_urls {
            __result.remote_urls = __v as usize;
        }
        if let Some(__v) = val.pages_crawled {
            __result.pages_crawled = __v as usize;
        }
        if let Some(__v) = val.documents_downloaded {
            __result.documents_downloaded = __v as usize;
        }
        __result
    }
}

impl From<xberg::ExtractionSummary> for JsExtractionSummary {
    fn from(val: xberg::ExtractionSummary) -> Self {
        Self {
            inputs: Some(val.inputs as i64),
            results: Some(val.results as i64),
            errors: Some(val.errors as i64),
            remote_urls: Some(val.remote_urls as i64),
            pages_crawled: Some(val.pages_crawled as i64),
            documents_downloaded: Some(val.documents_downloaded as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsFictionBookMetadata> for xberg::FictionBookMetadata {
    fn from(val: JsFictionBookMetadata) -> Self {
        let mut __result = xberg::FictionBookMetadata::default();
        if let Some(__v) = val.genres {
            __result.genres = __v.into_iter().collect();
        }
        if let Some(__v) = val.sequences {
            __result.sequences = __v.into_iter().collect();
        }
        __result.annotation = val.annotation;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::FictionBookMetadata> for JsFictionBookMetadata {
    fn from(val: xberg::FictionBookMetadata) -> Self {
        Self {
            genres: Some(val.genres.into_iter().collect()),
            sequences: Some(val.sequences.into_iter().collect()),
            annotation: val.annotation.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsFileExtractionConfig> for xberg::FileExtractionConfig {
    fn from(val: JsFileExtractionConfig) -> Self {
        let mut __result = xberg::FileExtractionConfig::default();
        __result.mime_detection_policy = val.mime_detection_policy.map(Into::into);
        __result.enable_quality_processing = val.enable_quality_processing;
        __result.ocr = val.ocr.map(Into::into);
        __result.force_ocr = val.force_ocr;
        __result.ocr_strategy = val.ocr_strategy.map(Into::into);
        __result.force_ocr_pages = val.force_ocr_pages.map(|v| v.into_iter().collect());
        __result.disable_ocr = val.disable_ocr;
        __result.chunking = val.chunking.map(Into::into);
        __result.content_filter = val.content_filter.map(Into::into);
        __result.images = val.images.map(Into::into);
        #[cfg(feature = "pdf")]
        {
            __result.pdf_options = val.pdf_options.map(Into::into);
        }
        __result.token_reduction = val.token_reduction.map(Into::into);
        __result.language_detection = val.language_detection.map(Into::into);
        __result.pages = val.pages.map(Into::into);
        #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
        {
            __result.keywords = val.keywords.map(Into::into);
        }
        __result.postprocessor = val.postprocessor.map(Into::into);
        #[cfg(feature = "html")]
        {
            __result.html_options = val.html_options.map(Into::into);
        }
        #[cfg(feature = "html")]
        {
            __result.html_output = val.html_output.map(Into::into);
        }
        __result.result_format = val.result_format.map(Into::into);
        __result.output_format = val.output_format.map(Into::into);
        __result.include_document_structure = val.include_document_structure;
        #[cfg(feature = "layout-types")]
        {
            __result.layout = val.layout.map(Into::into);
        }
        #[cfg(feature = "transcription-types")]
        {
            __result.transcription = val.transcription.map(Into::into);
        }
        __result.timeout_secs = val.timeout_secs.map(|v| v as u64);
        #[cfg(feature = "tree-sitter")]
        {
            __result.tree_sitter = val.tree_sitter.map(Into::into);
        }
        __result.structured_extraction = val.structured_extraction.map(Into::into);
        __result.url = val.url.map(Into::into);
        __result.ner = val.ner.map(Into::into);
        __result.redaction = val.redaction.map(Into::into);
        __result.summarization = val.summarization.map(Into::into);
        __result.translation = val.translation.map(Into::into);
        __result.page_classification = val.page_classification.map(Into::into);
        __result.chunk_classification = val.chunk_classification.map(Into::into);
        __result.captioning = val.captioning.map(Into::into);
        __result.qr_codes = val.qr_codes;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::FileExtractionConfig> for JsFileExtractionConfig {
    fn from(val: xberg::FileExtractionConfig) -> Self {
        Self {
            mime_detection_policy: val.mime_detection_policy.map(Into::into),
            enable_quality_processing: val.enable_quality_processing,
            ocr: val.ocr.map(Into::into),
            force_ocr: val.force_ocr,
            ocr_strategy: val.ocr_strategy.map(Into::into),
            force_ocr_pages: val.force_ocr_pages.map(|v| v.into_iter().collect()),
            disable_ocr: val.disable_ocr,
            chunking: val.chunking.map(Into::into),
            content_filter: val.content_filter.map(Into::into),
            images: val.images.map(Into::into),
            #[cfg(feature = "pdf")]
            pdf_options: val.pdf_options.map(Into::into),
            token_reduction: val.token_reduction.map(Into::into),
            language_detection: val.language_detection.map(Into::into),
            pages: val.pages.map(Into::into),
            #[cfg(any(feature = "keywords-yake", feature = "keywords-rake"))]
            keywords: val.keywords.map(Into::into),
            postprocessor: val.postprocessor.map(Into::into),
            #[cfg(feature = "html")]
            html_options: val.html_options.map(Into::into),
            #[cfg(feature = "html")]
            html_output: val.html_output.map(Into::into),
            result_format: val.result_format.map(Into::into),
            output_format: val.output_format.map(Into::into),
            include_document_structure: val.include_document_structure,
            #[cfg(feature = "layout-types")]
            layout: val.layout.map(Into::into),
            #[cfg(feature = "transcription-types")]
            transcription: val.transcription.map(Into::into),
            timeout_secs: val.timeout_secs.map(|v| v as i64),
            #[cfg(feature = "tree-sitter")]
            tree_sitter: val.tree_sitter.map(Into::into),
            structured_extraction: val.structured_extraction.map(Into::into),
            url: val.url.map(Into::into),
            ner: val.ner.map(Into::into),
            redaction: val.redaction.map(Into::into),
            summarization: val.summarization.map(Into::into),
            translation: val.translation.map(Into::into),
            page_classification: val.page_classification.map(Into::into),
            chunk_classification: val.chunk_classification.map(Into::into),
            captioning: val.captioning.map(Into::into),
            qr_codes: val.qr_codes,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsFootnote> for xberg::Footnote {
    fn from(val: JsFootnote) -> Self {
        Self {
            label: val.label,
            content: val.content.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::Footnote> for JsFootnote {
    fn from(val: xberg::Footnote) -> Self {
        Self {
            label: val.label.to_string(),
            content: val.content.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<xberg::FootnoteAnchor> for JsFootnoteAnchor {
    fn from(val: xberg::FootnoteAnchor) -> Self {
        Self {
            label: val.label.to_string(),
            offset: val.offset as i64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsFootnoteConfig> for xberg::FootnoteConfig {
    fn from(val: JsFootnoteConfig) -> Self {
        let mut __result = xberg::FootnoteConfig::default();
        if let Some(__v) = val.parse_citations {
            __result.parse_citations = __v;
        }
        __result
    }
}

impl From<xberg::FootnoteConfig> for JsFootnoteConfig {
    fn from(val: xberg::FootnoteConfig) -> Self {
        Self {
            parse_citations: Some(val.parse_citations),
        }
    }
}

impl From<xberg::FootnoteDefinition> for JsFootnoteDefinition {
    fn from(val: xberg::FootnoteDefinition) -> Self {
        Self {
            label: val.label.to_string(),
            content: val.content.to_string(),
            offset: val.offset as i64,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsFormattedBlock> for xberg::FormattedBlock {
    fn from(val: JsFormattedBlock) -> Self {
        Self {
            block_type: val.block_type.into(),
            level: val.level.map(|v| v as usize),
            inline_content: val.inline_content.into_iter().map(Into::into).collect(),
            attributes: val.attributes.map(Into::into),
            language: val.language,
            code: val.code,
            children: val.children.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::FormattedBlock> for JsFormattedBlock {
    fn from(val: xberg::FormattedBlock) -> Self {
        Self {
            block_type: val.block_type.into(),
            level: val.level.map(|v| v as i64),
            inline_content: val.inline_content.into_iter().map(Into::into).collect(),
            attributes: val.attributes.map(Into::into),
            language: val.language.map(|v| v.to_string()),
            code: val.code.map(|v| v.to_string()),
            children: val.children.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsFormula> for xberg::Formula {
    fn from(val: JsFormula) -> Self {
        Self {
            latex: val.latex,
            bbox: val.bbox.map(Into::into),
            page: val.page,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::Formula> for JsFormula {
    fn from(val: xberg::Formula) -> Self {
        Self {
            latex: val.latex.to_string(),
            bbox: val.bbox.map(Into::into),
            page: val.page,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsGeoJsonExtractionConfig> for xberg::GeoJsonExtractionConfig {
    fn from(val: JsGeoJsonExtractionConfig) -> Self {
        let mut __result = xberg::GeoJsonExtractionConfig::default();
        if let Some(__v) = val.include_full_coordinates {
            __result.include_full_coordinates = __v;
        }
        __result
    }
}

impl From<xberg::GeoJsonExtractionConfig> for JsGeoJsonExtractionConfig {
    fn from(val: xberg::GeoJsonExtractionConfig) -> Self {
        Self {
            include_full_coordinates: Some(val.include_full_coordinates),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::candle_ocr::GlmOcrBackendOptions> for JsGlmOcrBackendOptions {
    fn from(val: xberg::candle_ocr::GlmOcrBackendOptions) -> Self {
        Self {
            task: val.task.map(Into::into),
            device: val.device.map(Into::into),
            layout_mode: val.layout_mode.map(Into::into),
            enable_chart_understanding: val.enable_chart_understanding,
            cache_dir: val.cache_dir.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsGridCell> for xberg::GridCell {
    fn from(val: JsGridCell) -> Self {
        Self {
            content: val.content,
            row: val.row,
            col: val.col,
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
            bbox: val.bbox.map(Into::into),
            heading_level: val.heading_level,
            style_name: val.style_name,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::GridCell> for JsGridCell {
    fn from(val: xberg::GridCell) -> Self {
        Self {
            content: val.content.to_string(),
            row: val.row,
            col: val.col,
            row_span: val.row_span,
            col_span: val.col_span,
            is_header: val.is_header,
            bbox: val.bbox.map(Into::into),
            heading_level: val.heading_level,
            style_name: val.style_name.map(|v| v.to_string()),
        }
    }
}

impl From<JsHeaderMetadata> for xberg::HeaderMetadata {
    fn from(val: JsHeaderMetadata) -> Self {
        Self {
            level: val.level,
            text: val.text,
            id: val.id,
            depth: val.depth,
            html_offset: val.html_offset,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::HeaderMetadata> for JsHeaderMetadata {
    fn from(val: xberg::HeaderMetadata) -> Self {
        Self {
            level: val.level,
            text: val.text.to_string(),
            id: val.id.map(|v| v.to_string()),
            depth: val.depth,
            html_offset: val.html_offset,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsHeadingContext> for xberg::HeadingContext {
    fn from(val: JsHeadingContext) -> Self {
        Self {
            headings: val.headings.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::HeadingContext> for JsHeadingContext {
    fn from(val: xberg::HeadingContext) -> Self {
        Self {
            headings: val.headings.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<JsHeadingLevel> for xberg::HeadingLevel {
    fn from(val: JsHeadingLevel) -> Self {
        Self {
            level: val.level,
            text: val.text,
        }
    }
}

impl From<xberg::HeadingLevel> for JsHeadingLevel {
    fn from(val: xberg::HeadingLevel) -> Self {
        Self {
            level: val.level,
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsHeuristicsConfig> for xberg::HeuristicsConfig {
    fn from(val: JsHeuristicsConfig) -> Self {
        let mut __result = xberg::HeuristicsConfig::default();
        if let Some(__v) = val.enable_pdf_text_heuristics {
            __result.enable_pdf_text_heuristics = __v;
        }
        if let Some(__v) = val.text_layer_threshold {
            __result.text_layer_threshold = __v as f32;
        }
        if let Some(__v) = val.file_size_threshold_bytes {
            __result.file_size_threshold_bytes = __v as u64;
        }
        if let Some(__v) = val.page_count_threshold {
            __result.page_count_threshold = __v;
        }
        if let Some(__v) = val.target_pages_per_chunk {
            __result.target_pages_per_chunk = __v;
        }
        if let Some(__v) = val.max_pages_per_chunk {
            __result.max_pages_per_chunk = __v;
        }
        if let Some(__v) = val.disk_processing_threshold_bytes {
            __result.disk_processing_threshold_bytes = __v as u64;
        }
        if let Some(__v) = val.min_chars_per_page {
            __result.min_chars_per_page = __v;
        }
        if let Some(__v) = val.max_xlsx_sheet_count {
            __result.max_xlsx_sheet_count = __v;
        }
        if let Some(__v) = val.max_xlsx_workbook_cells {
            __result.max_xlsx_workbook_cells = __v as u64;
        }
        if let Some(__v) = val.max_pptx_embedded_count {
            __result.max_pptx_embedded_count = __v;
        }
        __result
    }
}

impl From<xberg::HeuristicsConfig> for JsHeuristicsConfig {
    fn from(val: xberg::HeuristicsConfig) -> Self {
        Self {
            enable_pdf_text_heuristics: Some(val.enable_pdf_text_heuristics),
            text_layer_threshold: Some(val.text_layer_threshold as f64),
            file_size_threshold_bytes: Some(val.file_size_threshold_bytes as i64),
            page_count_threshold: Some(val.page_count_threshold),
            target_pages_per_chunk: Some(val.target_pages_per_chunk),
            max_pages_per_chunk: Some(val.max_pages_per_chunk),
            disk_processing_threshold_bytes: Some(val.disk_processing_threshold_bytes as i64),
            min_chars_per_page: Some(val.min_chars_per_page),
            max_xlsx_sheet_count: Some(val.max_xlsx_sheet_count),
            max_xlsx_workbook_cells: Some(val.max_xlsx_workbook_cells as i64),
            max_pptx_embedded_count: Some(val.max_pptx_embedded_count),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsHierarchicalBlock> for xberg::HierarchicalBlock {
    fn from(val: JsHierarchicalBlock) -> Self {
        Self {
            text: val.text,
            font_size: val.font_size as f32,
            level: val.level,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::HierarchicalBlock> for JsHierarchicalBlock {
    fn from(val: xberg::HierarchicalBlock) -> Self {
        Self {
            text: val.text.to_string(),
            font_size: val.font_size as f64,
            level: val.level.to_string(),
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsHierarchicalBoundingBox> for xberg::HierarchicalBoundingBox {
    fn from(val: JsHierarchicalBoundingBox) -> Self {
        let mut __result = xberg::HierarchicalBoundingBox::default();
        if let Some(__v) = val.left {
            __result.left = __v as f32;
        }
        if let Some(__v) = val.top {
            __result.top = __v as f32;
        }
        if let Some(__v) = val.right {
            __result.right = __v as f32;
        }
        if let Some(__v) = val.bottom {
            __result.bottom = __v as f32;
        }
        __result
    }
}

impl From<xberg::HierarchicalBoundingBox> for JsHierarchicalBoundingBox {
    fn from(val: xberg::HierarchicalBoundingBox) -> Self {
        Self {
            left: Some(val.left as f64),
            top: Some(val.top as f64),
            right: Some(val.right as f64),
            bottom: Some(val.bottom as f64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsHierarchyConfig> for xberg::HierarchyConfig {
    fn from(val: JsHierarchyConfig) -> Self {
        let mut __result = xberg::HierarchyConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        if let Some(__v) = val.k_clusters {
            __result.k_clusters = __v as usize;
        }
        if let Some(__v) = val.include_bbox {
            __result.include_bbox = __v;
        }
        __result
    }
}

impl From<xberg::HierarchyConfig> for JsHierarchyConfig {
    fn from(val: xberg::HierarchyConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            k_clusters: Some(val.k_clusters as i64),
            include_bbox: Some(val.include_bbox),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsHtmlMetadata> for xberg::HtmlMetadata {
    fn from(val: JsHtmlMetadata) -> Self {
        let mut __result = xberg::HtmlMetadata::default();
        __result.title = val.title;
        __result.description = val.description;
        if let Some(__v) = val.keywords {
            __result.keywords = __v.into_iter().collect();
        }
        __result.author = val.author;
        __result.canonical_url = val.canonical_url;
        __result.base_href = val.base_href;
        __result.language = val.language;
        __result.text_direction = val.text_direction.map(Into::into);
        if let Some(__v) = val.open_graph {
            __result.open_graph = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        if let Some(__v) = val.twitter_card {
            __result.twitter_card = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        if let Some(__v) = val.meta_tags {
            __result.meta_tags = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        if let Some(__v) = val.headers {
            __result.headers = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.links {
            __result.links = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.images {
            __result.images = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.structured_data {
            __result.structured_data = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::HtmlMetadata> for JsHtmlMetadata {
    fn from(val: xberg::HtmlMetadata) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            description: val.description.map(|v| v.to_string()),
            keywords: Some(val.keywords.into_iter().collect()),
            author: val.author.map(|v| v.to_string()),
            canonical_url: val.canonical_url.map(|v| v.to_string()),
            base_href: val.base_href.map(|v| v.to_string()),
            language: val.language.map(|v| v.to_string()),
            text_direction: val.text_direction.map(Into::into),
            open_graph: Some(val.open_graph.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            twitter_card: Some(
                val.twitter_card
                    .into_iter()
                    .map(|(k, v)| (k.into(), v.into()))
                    .collect(),
            ),
            meta_tags: Some(val.meta_tags.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            headers: Some(val.headers.into_iter().map(Into::into).collect()),
            links: Some(val.links.into_iter().map(Into::into).collect()),
            images: Some(val.images.into_iter().map(Into::into).collect()),
            structured_data: Some(val.structured_data.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsHtmlOutputConfig> for xberg::HtmlOutputConfig {
    fn from(val: JsHtmlOutputConfig) -> Self {
        let mut __result = xberg::HtmlOutputConfig::default();
        __result.css = val.css;
        __result.css_file = val.css_file.map(Into::into);
        if let Some(__v) = val.theme {
            __result.theme = __v.into();
        }
        if let Some(__v) = val.class_prefix {
            __result.class_prefix = __v;
        }
        if let Some(__v) = val.embed_css {
            __result.embed_css = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::HtmlOutputConfig> for JsHtmlOutputConfig {
    fn from(val: xberg::HtmlOutputConfig) -> Self {
        Self {
            css: val.css.map(|v| v.to_string()),
            css_file: val.css_file.map(|p| p.to_string_lossy().to_string()),
            theme: Some(val.theme.into()),
            class_prefix: Some(val.class_prefix.to_string()),
            embed_css: Some(val.embed_css),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsImageDimensions> for xberg::ImageDimensions {
    fn from(val: JsImageDimensions) -> Self {
        let mut __result = xberg::ImageDimensions::default();
        if let Some(__v) = val.width {
            __result.width = __v;
        }
        if let Some(__v) = val.height {
            __result.height = __v;
        }
        __result
    }
}

impl From<xberg::ImageDimensions> for JsImageDimensions {
    fn from(val: xberg::ImageDimensions) -> Self {
        Self {
            width: Some(val.width),
            height: Some(val.height),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsImageDpi> for xberg::ImageDpi {
    fn from(val: JsImageDpi) -> Self {
        let mut __result = xberg::ImageDpi::default();
        if let Some(__v) = val.horizontal {
            __result.horizontal = __v;
        }
        if let Some(__v) = val.vertical {
            __result.vertical = __v;
        }
        __result
    }
}

impl From<xberg::ImageDpi> for JsImageDpi {
    fn from(val: xberg::ImageDpi) -> Self {
        Self {
            horizontal: Some(val.horizontal),
            vertical: Some(val.vertical),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsImageExtractionConfig> for xberg::ImageExtractionConfig {
    fn from(val: JsImageExtractionConfig) -> Self {
        let mut __result = xberg::ImageExtractionConfig::default();
        if let Some(__v) = val.extract_images {
            __result.extract_images = __v;
        }
        if let Some(__v) = val.target_dpi {
            __result.target_dpi = __v;
        }
        if let Some(__v) = val.max_image_dimension {
            __result.max_image_dimension = __v;
        }
        if let Some(__v) = val.inject_placeholders {
            __result.inject_placeholders = __v;
        }
        if let Some(__v) = val.auto_adjust_dpi {
            __result.auto_adjust_dpi = __v;
        }
        if let Some(__v) = val.min_dpi {
            __result.min_dpi = __v;
        }
        if let Some(__v) = val.max_dpi {
            __result.max_dpi = __v;
        }
        __result.max_images_per_page = val.max_images_per_page;
        if let Some(__v) = val.classify {
            __result.classify = __v;
        }
        if let Some(__v) = val.include_page_rasters {
            __result.include_page_rasters = __v;
        }
        if let Some(__v) = val.run_ocr_on_images {
            __result.run_ocr_on_images = __v;
        }
        if let Some(__v) = val.ocr_text_only {
            __result.ocr_text_only = __v;
        }
        if let Some(__v) = val.append_ocr_text {
            __result.append_ocr_text = __v;
        }
        if let Some(__v) = val.output_format {
            __result.output_format = __v.into();
        }
        #[cfg(feature = "svg")]
        {
            if let Some(__v) = val.svg {
                __result.svg = __v.into();
            }
        }
        if let Some(__v) = val.include_data_base64 {
            __result.include_data_base64 = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ImageExtractionConfig> for JsImageExtractionConfig {
    fn from(val: xberg::ImageExtractionConfig) -> Self {
        Self {
            extract_images: Some(val.extract_images),
            target_dpi: Some(val.target_dpi),
            max_image_dimension: Some(val.max_image_dimension),
            inject_placeholders: Some(val.inject_placeholders),
            auto_adjust_dpi: Some(val.auto_adjust_dpi),
            min_dpi: Some(val.min_dpi),
            max_dpi: Some(val.max_dpi),
            max_images_per_page: val.max_images_per_page,
            classify: Some(val.classify),
            include_page_rasters: Some(val.include_page_rasters),
            run_ocr_on_images: Some(val.run_ocr_on_images),
            ocr_text_only: Some(val.ocr_text_only),
            append_ocr_text: Some(val.append_ocr_text),
            output_format: Some(val.output_format.into()),
            #[cfg(feature = "svg")]
            svg: Some(val.svg.into()),
            include_data_base64: Some(val.include_data_base64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsImageMetadata> for xberg::ImageMetadata {
    fn from(val: JsImageMetadata) -> Self {
        let mut __result = xberg::ImageMetadata::default();
        if let Some(__v) = val.width {
            __result.width = __v;
        }
        if let Some(__v) = val.height {
            __result.height = __v;
        }
        if let Some(__v) = val.format {
            __result.format = __v;
        }
        if let Some(__v) = val.exif {
            __result.exif = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImageMetadata> for JsImageMetadata {
    fn from(val: xberg::ImageMetadata) -> Self {
        Self {
            width: Some(val.width),
            height: Some(val.height),
            format: Some(val.format.to_string()),
            exif: Some(val.exif.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsImageMetadataType> for xberg::ImageMetadataType {
    fn from(val: JsImageMetadataType) -> Self {
        Self {
            src: val.src,
            alt: val.alt,
            title: val.title,
            dimensions: val.dimensions.map(Into::into),
            image_type: val.image_type.into(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImageMetadataType> for JsImageMetadataType {
    fn from(val: xberg::ImageMetadataType) -> Self {
        Self {
            src: val.src.to_string(),
            alt: val.alt.map(|v| v.to_string()),
            title: val.title.map(|v| v.to_string()),
            dimensions: val.dimensions.map(Into::into),
            image_type: val.image_type.into(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsImagePreprocessingConfig> for xberg::ImagePreprocessingConfig {
    fn from(val: JsImagePreprocessingConfig) -> Self {
        let mut __result = xberg::ImagePreprocessingConfig::default();
        if let Some(__v) = val.target_dpi {
            __result.target_dpi = __v;
        }
        if let Some(__v) = val.auto_rotate {
            __result.auto_rotate = __v;
        }
        if let Some(__v) = val.deskew {
            __result.deskew = __v;
        }
        if let Some(__v) = val.denoise {
            __result.denoise = __v;
        }
        if let Some(__v) = val.contrast_enhance {
            __result.contrast_enhance = __v;
        }
        if let Some(__v) = val.binarization_method {
            __result.binarization_method = __v;
        }
        if let Some(__v) = val.invert_colors {
            __result.invert_colors = __v;
        }
        __result
    }
}

impl From<xberg::ImagePreprocessingConfig> for JsImagePreprocessingConfig {
    fn from(val: xberg::ImagePreprocessingConfig) -> Self {
        Self {
            target_dpi: Some(val.target_dpi),
            auto_rotate: Some(val.auto_rotate),
            deskew: Some(val.deskew),
            denoise: Some(val.denoise),
            contrast_enhance: Some(val.contrast_enhance),
            binarization_method: Some(val.binarization_method.to_string()),
            invert_colors: Some(val.invert_colors),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsImagePreprocessingMetadata> for xberg::ImagePreprocessingMetadata {
    fn from(val: JsImagePreprocessingMetadata) -> Self {
        Self {
            original_dimensions: val.original_dimensions.into(),
            original_dpi: val.original_dpi.into(),
            target_dpi: val.target_dpi,
            scale_factor: val.scale_factor,
            auto_adjusted: val.auto_adjusted,
            final_dpi: val.final_dpi,
            new_dimensions: val.new_dimensions.map(Into::into),
            resample_method: val.resample_method,
            dimension_clamped: val.dimension_clamped,
            calculated_dpi: val.calculated_dpi,
            skipped_resize: val.skipped_resize,
            resize_error: val.resize_error,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::ImagePreprocessingMetadata> for JsImagePreprocessingMetadata {
    fn from(val: xberg::ImagePreprocessingMetadata) -> Self {
        Self {
            original_dimensions: val.original_dimensions.into(),
            original_dpi: val.original_dpi.into(),
            target_dpi: val.target_dpi,
            scale_factor: val.scale_factor,
            auto_adjusted: val.auto_adjusted,
            final_dpi: val.final_dpi,
            new_dimensions: val.new_dimensions.map(Into::into),
            resample_method: val.resample_method.to_string(),
            dimension_clamped: val.dimension_clamped,
            calculated_dpi: val.calculated_dpi,
            skipped_resize: val.skipped_resize,
            resize_error: val.resize_error.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsInlineElement> for xberg::InlineElement {
    fn from(val: JsInlineElement) -> Self {
        Self {
            element_type: val.element_type.into(),
            content: val.content,
            attributes: val.attributes.map(Into::into),
            metadata: val
                .metadata
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::InlineElement> for JsInlineElement {
    fn from(val: xberg::InlineElement) -> Self {
        Self {
            element_type: val.element_type.into(),
            content: val.content.to_string(),
            attributes: val.attributes.map(Into::into),
            metadata: val
                .metadata
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsJatsMetadata> for xberg::JatsMetadata {
    fn from(val: JsJatsMetadata) -> Self {
        let mut __result = xberg::JatsMetadata::default();
        __result.copyright = val.copyright;
        __result.license = val.license;
        if let Some(__v) = val.history_dates {
            __result.history_dates = __v.into_iter().map(|(k, v)| (k.into(), v.into())).collect();
        }
        if let Some(__v) = val.contributor_roles {
            __result.contributor_roles = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::JatsMetadata> for JsJatsMetadata {
    fn from(val: xberg::JatsMetadata) -> Self {
        Self {
            copyright: val.copyright.map(|v| v.to_string()),
            license: val.license.map(|v| v.to_string()),
            history_dates: Some(
                val.history_dates
                    .into_iter()
                    .map(|(k, v)| (k.into(), v.into()))
                    .collect(),
            ),
            contributor_roles: Some(val.contributor_roles.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsKeyValueAttribute> for xberg::KeyValueAttribute {
    fn from(val: JsKeyValueAttribute) -> Self {
        let mut __result = xberg::KeyValueAttribute::default();
        if let Some(__v) = val.key {
            __result.key = __v;
        }
        if let Some(__v) = val.value {
            __result.value = __v;
        }
        __result
    }
}

impl From<xberg::KeyValueAttribute> for JsKeyValueAttribute {
    fn from(val: xberg::KeyValueAttribute) -> Self {
        Self {
            key: Some(val.key.to_string()),
            value: Some(val.value.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsKeyword> for xberg::Keyword {
    fn from(val: JsKeyword) -> Self {
        Self {
            text: val.text,
            score: val.score as f32,
            algorithm: val.algorithm.into(),
            positions: val.positions.map(|v| v.into_iter().map(|x| x as usize).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Keyword> for JsKeyword {
    fn from(val: xberg::Keyword) -> Self {
        Self {
            text: val.text.to_string(),
            score: val.score as f64,
            algorithm: val.algorithm.into(),
            positions: val.positions.as_ref().map(|v| v.iter().map(|&x| x as i64).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsKeywordConfig> for xberg::KeywordConfig {
    fn from(val: JsKeywordConfig) -> Self {
        let mut __result = xberg::KeywordConfig::default();
        if let Some(__v) = val.algorithm {
            __result.algorithm = __v.into();
        }
        if let Some(__v) = val.max_keywords {
            __result.max_keywords = __v as usize;
        }
        if let Some(__v) = val.min_score {
            __result.min_score = __v as f32;
        }
        if let Some(__v) = val.ngram_range {
            __result.ngram_range = __v.into();
        }
        __result.language = val.language;
        #[cfg(feature = "keywords-yake")]
        {
            __result.yake_params = val.yake_params.map(Into::into);
        }
        #[cfg(feature = "keywords-rake")]
        {
            __result.rake_params = val.rake_params.map(Into::into);
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::KeywordConfig> for JsKeywordConfig {
    fn from(val: xberg::KeywordConfig) -> Self {
        Self {
            algorithm: Some(val.algorithm.into()),
            max_keywords: Some(val.max_keywords as i64),
            min_score: Some(val.min_score as f64),
            ngram_range: Some(val.ngram_range.into()),
            language: val.language.map(|v| v.to_string()),
            #[cfg(feature = "keywords-yake")]
            yake_params: val.yake_params.map(Into::into),
            #[cfg(feature = "keywords-rake")]
            rake_params: val.rake_params.map(Into::into),
        }
    }
}

impl From<JsLanguageConfidence> for xberg::LanguageConfidence {
    fn from(val: JsLanguageConfidence) -> Self {
        Self {
            language: val.language,
            confidence: val.confidence,
            proportion: val.proportion,
            script: val.script,
            reliable: val.reliable,
        }
    }
}

impl From<xberg::LanguageConfidence> for JsLanguageConfidence {
    fn from(val: xberg::LanguageConfidence) -> Self {
        Self {
            language: val.language.to_string(),
            confidence: val.confidence,
            proportion: val.proportion,
            script: val.script.to_string(),
            reliable: val.reliable,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsLanguageDetectionConfig> for xberg::LanguageDetectionConfig {
    fn from(val: JsLanguageDetectionConfig) -> Self {
        let mut __result = xberg::LanguageDetectionConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        if let Some(__v) = val.min_confidence {
            __result.min_confidence = __v;
        }
        if let Some(__v) = val.detect_multiple {
            __result.detect_multiple = __v;
        }
        __result
    }
}

impl From<xberg::LanguageDetectionConfig> for JsLanguageDetectionConfig {
    fn from(val: xberg::LanguageDetectionConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            min_confidence: Some(val.min_confidence),
            detect_multiple: Some(val.detect_multiple),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsLateInteractionConfig> for xberg::LateInteractionConfig {
    fn from(val: JsLateInteractionConfig) -> Self {
        let mut __result = xberg::LateInteractionConfig::default();
        if let Some(__v) = val.model {
            __result.model = __v.into();
        }
        if let Some(__v) = val.batch_size {
            __result.batch_size = __v as usize;
        }
        if let Some(__v) = val.max_length {
            __result.max_length = __v as usize;
        }
        if let Some(__v) = val.query_max_length {
            __result.query_max_length = __v as usize;
        }
        if let Some(__v) = val.show_download_progress {
            __result.show_download_progress = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        __result.acceleration = val.acceleration.map(Into::into);
        __result.max_embed_duration_secs = val.max_embed_duration_secs.map(|v| v as u64);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LateInteractionConfig> for JsLateInteractionConfig {
    fn from(val: xberg::LateInteractionConfig) -> Self {
        Self {
            model: Some(val.model.into()),
            batch_size: Some(val.batch_size as i64),
            max_length: Some(val.max_length as i64),
            query_max_length: Some(val.query_max_length as i64),
            show_download_progress: Some(val.show_download_progress),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
        }
    }
}

impl From<JsLateInteractionMatch> for xberg::LateInteractionMatch {
    fn from(val: JsLateInteractionMatch) -> Self {
        Self {
            index: val.index as usize,
            score: val.score as f32,
        }
    }
}

impl From<xberg::LateInteractionMatch> for JsLateInteractionMatch {
    fn from(val: xberg::LateInteractionMatch) -> Self {
        Self {
            index: val.index as i64,
            score: val.score as f64,
        }
    }
}

impl From<xberg::LateInteractionPreset> for JsLateInteractionPreset {
    fn from(val: xberg::LateInteractionPreset) -> Self {
        Self {
            name: val.name.to_string(),
            model_repo: val.model_repo.to_string(),
            model_file: val.model_file.to_string(),
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as i64,
            query_max_length: val.query_max_length as i64,
            dim: val.dim as i64,
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::LayoutDetection> for JsLayoutDetection {
    fn from(val: xberg::LayoutDetection) -> Self {
        Self {
            class_name: val.class_name.into(),
            confidence: val.confidence as f64,
            bbox: val.bbox.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsLayoutDetectionConfig> for xberg::LayoutDetectionConfig {
    fn from(val: JsLayoutDetectionConfig) -> Self {
        let mut __result = xberg::LayoutDetectionConfig::default();
        if let Some(__v) = val.strategy {
            __result.strategy = __v.into();
        }
        __result.confidence_threshold = val.confidence_threshold.map(|v| v as f32);
        if let Some(__v) = val.apply_heuristics {
            __result.apply_heuristics = __v;
        }
        if let Some(__v) = val.table_model {
            __result.table_model = __v.into();
        }
        __result.formula_model = val.formula_model.map(Into::into);
        if let Some(__v) = val.table_overlap_preference {
            __result.table_overlap_preference = __v.into();
        }
        __result.acceleration = val.acceleration.map(Into::into);
        if let Some(__v) = val.enable_chart_understanding {
            __result.enable_chart_understanding = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LayoutDetectionConfig> for JsLayoutDetectionConfig {
    fn from(val: xberg::LayoutDetectionConfig) -> Self {
        Self {
            strategy: Some(val.strategy.into()),
            confidence_threshold: val.confidence_threshold.map(|v| v as f64),
            apply_heuristics: Some(val.apply_heuristics),
            table_model: Some(val.table_model.into()),
            formula_model: val.formula_model.map(Into::into),
            table_overlap_preference: Some(val.table_overlap_preference.into()),
            acceleration: val.acceleration.map(Into::into),
            enable_chart_understanding: Some(val.enable_chart_understanding),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsLayoutRegion> for xberg::LayoutRegion {
    fn from(val: JsLayoutRegion) -> Self {
        let mut __result = xberg::LayoutRegion::default();
        if let Some(__v) = val.class_name {
            __result.class_name = __v;
        }
        if let Some(__v) = val.confidence {
            __result.confidence = __v;
        }
        if let Some(__v) = val.bounding_box {
            __result.bounding_box = __v.into();
        }
        if let Some(__v) = val.area_fraction {
            __result.area_fraction = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::LayoutRegion> for JsLayoutRegion {
    fn from(val: xberg::LayoutRegion) -> Self {
        Self {
            class_name: Some(val.class_name.to_string()),
            confidence: Some(val.confidence),
            bounding_box: Some(val.bounding_box.into()),
            area_fraction: Some(val.area_fraction),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsLinkMetadata> for xberg::LinkMetadata {
    fn from(val: JsLinkMetadata) -> Self {
        Self {
            href: val.href,
            text: val.text,
            title: val.title,
            link_type: val.link_type.into(),
            rel: val.rel.into_iter().collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LinkMetadata> for JsLinkMetadata {
    fn from(val: xberg::LinkMetadata) -> Self {
        Self {
            href: val.href.to_string(),
            text: val.text.to_string(),
            title: val.title.map(|v| v.to_string()),
            link_type: val.link_type.into(),
            rel: val.rel.into_iter().collect(),
            attributes: val.attributes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsLlmBudgetConfig> for xberg::LlmBudgetConfig {
    fn from(val: JsLlmBudgetConfig) -> Self {
        let mut __result = xberg::LlmBudgetConfig::default();
        __result.global_limit = val.global_limit;
        __result.model_limits = val.model_limits.map(|m| m.into_iter().collect());
        __result.enforcement = val.enforcement;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmBudgetConfig> for JsLlmBudgetConfig {
    fn from(val: xberg::LlmBudgetConfig) -> Self {
        Self {
            global_limit: val.global_limit,
            model_limits: val.model_limits.map(|m| m.into_iter().collect()),
            enforcement: val.enforcement.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsLlmCacheConfig> for xberg::LlmCacheConfig {
    fn from(val: JsLlmCacheConfig) -> Self {
        let mut __result = xberg::LlmCacheConfig::default();
        __result.max_entries = val.max_entries.map(|v| v as usize);
        __result.ttl_seconds = val.ttl_seconds.map(|v| v as u64);
        __result.backend = val.backend;
        __result.backend_config = val
            .backend_config
            .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect());
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LlmCacheConfig> for JsLlmCacheConfig {
    fn from(val: xberg::LlmCacheConfig) -> Self {
        Self {
            max_entries: val.max_entries.map(|v| v as i64),
            ttl_seconds: val.ttl_seconds.map(|v| v as i64),
            backend: val.backend.map(|v| v.to_string()),
            backend_config: val
                .backend_config
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsLlmConfig> for xberg::LlmConfig {
    fn from(val: JsLlmConfig) -> Self {
        let mut __result = xberg::LlmConfig::default();
        if let Some(__v) = val.model {
            __result.model = __v;
        }
        __result.api_key = val.api_key;
        __result.base_url = val.base_url;
        __result.timeout_secs = val.timeout_secs.map(|v| v as u64);
        __result.max_retries = val.max_retries;
        __result.temperature = val.temperature;
        __result.max_tokens = val.max_tokens.map(|v| v as u64);
        __result.top_p = val.top_p;
        __result.stop = val.stop.map(|v| v.into_iter().collect());
        __result.seed = val.seed;
        __result.presence_penalty = val.presence_penalty;
        __result.frequency_penalty = val.frequency_penalty;
        __result.reasoning_effort = val.reasoning_effort;
        __result.extra_body = val.extra_body;
        __result.load_env = val.load_env;
        __result.headers = val
            .headers
            .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect());
        __result.providers = val.providers.map(|v| v.into_iter().map(Into::into).collect());
        __result.cache = val.cache.map(Into::into).map(Box::new);
        __result.budget = val.budget.map(Into::into).map(Box::new);
        __result.rate_limit = val.rate_limit.map(Into::into).map(Box::new);
        __result.cost_tracking = val.cost_tracking;
        __result.tracing = val.tracing;
        __result.cooldown_secs = val.cooldown_secs.map(|v| v as u64);
        __result.health_check_secs = val.health_check_secs.map(|v| v as u64);
        __result.bedrock = val.bedrock.map(Into::into).map(Box::new);
        __result.credential_provider = val.credential_provider.map(Into::into).map(Box::new);
        __result.max_concurrency = val.max_concurrency.map(|v| v as usize);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::LlmConfig> for JsLlmConfig {
    fn from(val: xberg::LlmConfig) -> Self {
        Self {
            model: Some(val.model.to_string()),
            api_key: val.api_key.map(|v| v.to_string()),
            base_url: val.base_url.map(|v| v.to_string()),
            timeout_secs: val.timeout_secs.map(|v| v as i64),
            max_retries: val.max_retries,
            temperature: val.temperature,
            max_tokens: val.max_tokens.map(|v| v as i64),
            top_p: val.top_p,
            stop: val.stop.map(|v| v.into_iter().collect()),
            seed: val.seed,
            presence_penalty: val.presence_penalty,
            frequency_penalty: val.frequency_penalty,
            reasoning_effort: val.reasoning_effort.map(|v| v.to_string()),
            extra_body: val.extra_body,
            load_env: val.load_env,
            headers: val
                .headers
                .map(|m| m.into_iter().map(|(k, v)| (k.into(), v.into())).collect()),
            providers: val.providers.map(|v| v.into_iter().map(Into::into).collect()),
            cache: val.cache.map(|v| (*v).into()),
            budget: val.budget.map(|v| (*v).into()),
            rate_limit: val.rate_limit.map(|v| (*v).into()),
            cost_tracking: val.cost_tracking,
            tracing: val.tracing,
            cooldown_secs: val.cooldown_secs.map(|v| v as i64),
            health_check_secs: val.health_check_secs.map(|v| v as i64),
            bedrock: val.bedrock.map(|v| (*v).into()),
            credential_provider: val.credential_provider.map(|v| (*v).into()),
            max_concurrency: val.max_concurrency.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsLlmProviderConfig> for xberg::LlmProviderConfig {
    fn from(val: JsLlmProviderConfig) -> Self {
        let mut __result = xberg::LlmProviderConfig::default();
        if let Some(__v) = val.name {
            __result.name = __v;
        }
        if let Some(__v) = val.base_url {
            __result.base_url = __v;
        }
        __result.auth_header = val.auth_header;
        if let Some(__v) = val.model_prefixes {
            __result.model_prefixes = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmProviderConfig> for JsLlmProviderConfig {
    fn from(val: xberg::LlmProviderConfig) -> Self {
        Self {
            name: Some(val.name.to_string()),
            base_url: Some(val.base_url.to_string()),
            auth_header: val.auth_header.map(|v| v.to_string()),
            model_prefixes: Some(val.model_prefixes.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsLlmRateLimitConfig> for xberg::LlmRateLimitConfig {
    fn from(val: JsLlmRateLimitConfig) -> Self {
        let mut __result = xberg::LlmRateLimitConfig::default();
        __result.rpm = val.rpm;
        __result.tpm = val.tpm.map(|v| v as u64);
        __result.window_seconds = val.window_seconds.map(|v| v as u64);
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmRateLimitConfig> for JsLlmRateLimitConfig {
    fn from(val: xberg::LlmRateLimitConfig) -> Self {
        Self {
            rpm: val.rpm,
            tpm: val.tpm.map(|v| v as i64),
            window_seconds: val.window_seconds.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsLlmUsage> for xberg::LlmUsage {
    fn from(val: JsLlmUsage) -> Self {
        let mut __result = xberg::LlmUsage::default();
        if let Some(__v) = val.model {
            __result.model = __v;
        }
        if let Some(__v) = val.source {
            __result.source = __v;
        }
        __result.input_tokens = val.input_tokens.map(|v| v as u64);
        __result.output_tokens = val.output_tokens.map(|v| v as u64);
        __result.total_tokens = val.total_tokens.map(|v| v as u64);
        __result.estimated_cost = val.estimated_cost;
        __result.finish_reason = val.finish_reason;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::LlmUsage> for JsLlmUsage {
    fn from(val: xberg::LlmUsage) -> Self {
        Self {
            model: Some(val.model.to_string()),
            source: Some(val.source.to_string()),
            input_tokens: val.input_tokens.map(|v| v as i64),
            output_tokens: val.output_tokens.map(|v| v as i64),
            total_tokens: val.total_tokens.map(|v| v as i64),
            estimated_cost: val.estimated_cost,
            finish_reason: val.finish_reason.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsMapResult> for crawlberg::MapResult {
    fn from(val: JsMapResult) -> Self {
        let mut __result = crawlberg::MapResult::default();
        if let Some(__v) = val.urls {
            __result.urls = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<crawlberg::MapResult> for JsMapResult {
    fn from(val: crawlberg::MapResult) -> Self {
        Self {
            urls: Some(val.urls.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsMarkdownCodeBlock> for xberg::MarkdownCodeBlock {
    fn from(val: JsMarkdownCodeBlock) -> Self {
        let mut __result = xberg::MarkdownCodeBlock::default();
        if let Some(__v) = val.language {
            __result.language = __v;
        }
        if let Some(__v) = val.code {
            __result.code = __v;
        }
        __result
    }
}

impl From<xberg::MarkdownCodeBlock> for JsMarkdownCodeBlock {
    fn from(val: xberg::MarkdownCodeBlock) -> Self {
        Self {
            language: Some(val.language.to_string()),
            code: Some(val.code.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsMarkdownLink> for xberg::MarkdownLink {
    fn from(val: JsMarkdownLink) -> Self {
        let mut __result = xberg::MarkdownLink::default();
        if let Some(__v) = val.text {
            __result.text = __v;
        }
        if let Some(__v) = val.url {
            __result.url = __v;
        }
        __result
    }
}

impl From<xberg::MarkdownLink> for JsMarkdownLink {
    fn from(val: xberg::MarkdownLink) -> Self {
        Self {
            text: Some(val.text.to_string()),
            url: Some(val.url.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsMetadata> for xberg::Metadata {
    fn from(val: JsMetadata) -> Self {
        let mut __result = xberg::Metadata::default();
        __result.title = val.title;
        __result.subject = val.subject;
        __result.authors = val.authors.map(|v| v.into_iter().collect());
        __result.keywords = val.keywords.map(|v| v.into_iter().collect());
        __result.language = val.language;
        __result.created_at = val.created_at;
        __result.modified_at = val.modified_at;
        __result.created_by = val.created_by;
        __result.modified_by = val.modified_by;
        __result.pages = val.pages.map(Into::into);
        __result.format = val.format.map(Into::into);
        __result.image_preprocessing = val.image_preprocessing.map(Into::into);
        __result.json_schema = val.json_schema;
        __result.error = val.error.map(Into::into);
        __result.extraction_duration_ms = val.extraction_duration_ms.map(|v| v as u64);
        __result.category = val.category;
        __result.tags = val.tags.map(|v| v.into_iter().collect());
        __result.document_version = val.document_version;
        __result.abstract_text = val.abstract_text;
        __result.output_format = val.output_format;
        if let Some(__v) = val.ocr_used {
            __result.ocr_used = __v;
        }
        if let Some(__v) = val.additional {
            __result.additional = __v.into_iter().map(|(k, v)| (k.into(), v)).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Metadata> for JsMetadata {
    fn from(val: xberg::Metadata) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            authors: val.authors.map(|v| v.into_iter().collect()),
            keywords: val.keywords.map(|v| v.into_iter().collect()),
            language: val.language.map(|v| v.to_string()),
            created_at: val.created_at.map(|v| v.to_string()),
            modified_at: val.modified_at.map(|v| v.to_string()),
            created_by: val.created_by.map(|v| v.to_string()),
            modified_by: val.modified_by.map(|v| v.to_string()),
            pages: val.pages.map(Into::into),
            format: val.format.map(Into::into),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            json_schema: val.json_schema,
            error: val.error.map(Into::into),
            extraction_duration_ms: val.extraction_duration_ms.map(|v| v as i64),
            category: val.category.map(|v| v.to_string()),
            tags: val.tags.map(|v| v.into_iter().collect()),
            document_version: val.document_version.map(|v| v.to_string()),
            abstract_text: val.abstract_text.map(|v| v.to_string()),
            output_format: val.output_format.map(|v| v.to_string()),
            ocr_used: Some(val.ocr_used),
            additional: Some(val.additional.into_iter().map(|(k, v)| (k.into(), v)).collect()),
        }
    }
}

impl From<xberg::ModelPaths> for JsModelPaths {
    fn from(val: xberg::ModelPaths) -> Self {
        Self {
            det_model: val.det_model.to_string_lossy().to_string(),
            cls_model: val.cls_model.to_string_lossy().to_string(),
            rec_model: val.rec_model.to_string_lossy().to_string(),
            dict_file: val.dict_file.to_string_lossy().to_string(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<JsMultiVectorEmbedding> for xberg::MultiVectorEmbedding {
    fn from(val: JsMultiVectorEmbedding) -> Self {
        Self {
            num_tokens: val.num_tokens,
            dim: val.dim,
            data: val.data.into_iter().map(|v| v as f32).collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::MultiVectorEmbedding> for JsMultiVectorEmbedding {
    fn from(val: xberg::MultiVectorEmbedding) -> Self {
        Self {
            num_tokens: val.num_tokens,
            dim: val.dim,
            data: val.data.iter().map(|&v| v as f64).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::MultidocInput> for JsMultidocInput {
    fn from(val: xberg::MultidocInput) -> Self {
        Self {
            page_count: val.page_count,
            pages: val.pages.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsMultidocThresholds> for xberg::MultidocThresholds {
    fn from(val: JsMultidocThresholds) -> Self {
        let mut __result = xberg::MultidocThresholds::default();
        if let Some(__v) = val.density_shift_threshold {
            __result.density_shift_threshold = __v as f32;
        }
        if let Some(__v) = val.bigram_overlap_min {
            __result.bigram_overlap_min = __v as f32;
        }
        __result
    }
}

impl From<xberg::MultidocThresholds> for JsMultidocThresholds {
    fn from(val: xberg::MultidocThresholds) -> Self {
        Self {
            density_shift_threshold: Some(val.density_shift_threshold as f64),
            bigram_overlap_min: Some(val.bigram_overlap_min as f64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsNerConfig> for xberg::NerConfig {
    fn from(val: JsNerConfig) -> Self {
        let mut __result = xberg::NerConfig::default();
        if let Some(__v) = val.backend {
            __result.backend = __v.into();
        }
        if let Some(__v) = val.categories {
            __result.categories = __v.into_iter().map(Into::into).collect();
        }
        __result.model = val.model;
        __result.llm = val.llm.map(Into::into);
        if let Some(__v) = val.custom_labels {
            __result.custom_labels = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::NerConfig> for JsNerConfig {
    fn from(val: xberg::NerConfig) -> Self {
        Self {
            backend: Some(val.backend.into()),
            categories: Some(val.categories.into_iter().map(Into::into).collect()),
            model: val.model.map(|v| v.to_string()),
            llm: val.llm.map(Into::into),
            custom_labels: Some(val.custom_labels.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsNgramRange> for xberg::NgramRange {
    fn from(val: JsNgramRange) -> Self {
        let mut __result = xberg::NgramRange::default();
        if let Some(__v) = val.min {
            __result.min = __v as usize;
        }
        if let Some(__v) = val.max {
            __result.max = __v as usize;
        }
        __result
    }
}

impl From<xberg::NgramRange> for JsNgramRange {
    fn from(val: xberg::NgramRange) -> Self {
        Self {
            min: Some(val.min as i64),
            max: Some(val.max as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsOcrConfidence> for xberg::OcrConfidence {
    fn from(val: JsOcrConfidence) -> Self {
        let mut __result = xberg::OcrConfidence::default();
        __result.detection = val.detection;
        if let Some(__v) = val.recognition {
            __result.recognition = __v;
        }
        __result
    }
}

impl From<xberg::OcrConfidence> for JsOcrConfidence {
    fn from(val: xberg::OcrConfidence) -> Self {
        Self {
            detection: val.detection,
            recognition: Some(val.recognition),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsOcrConfig> for xberg::OcrConfig {
    fn from(val: JsOcrConfig) -> Self {
        let mut __result = xberg::OcrConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        if let Some(__v) = val.backend {
            __result.backend = __v;
        }
        if let Some(__v) = val.language {
            __result.language = __v.into_iter().collect();
        }
        __result.tesseract_config = val.tesseract_config.map(Into::into);
        __result.output_format = val.output_format.map(Into::into);
        __result.paddle_ocr_config = val.paddle_ocr_config;
        __result.backend_options = val.backend_options;
        __result.element_config = val.element_config.map(Into::into);
        __result.quality_thresholds = val.quality_thresholds.map(Into::into);
        __result.pipeline = val.pipeline.map(Into::into);
        if let Some(__v) = val.auto_rotate {
            __result.auto_rotate = __v;
        }
        if let Some(__v) = val.vlm_fallback {
            __result.vlm_fallback = __v.into();
        }
        __result.vlm_config = val.vlm_config.map(Into::into);
        __result.vlm_prompt = val.vlm_prompt;
        __result.acceleration = val.acceleration.map(Into::into);
        __result.security_limits = val.security_limits.map(Into::into);
        __result.tessdata_bytes = val
            .tessdata_bytes
            .map(|m| m.into_iter().map(|(k, v)| (k, v.to_vec().into())).collect());
        __result.tessdata_path = val.tessdata_path.map(Into::into);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrConfig> for JsOcrConfig {
    fn from(val: xberg::OcrConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            backend: Some(val.backend.to_string()),
            language: Some(val.language.into_iter().collect()),
            tesseract_config: val.tesseract_config.map(Into::into),
            output_format: val.output_format.map(Into::into),
            paddle_ocr_config: val.paddle_ocr_config,
            backend_options: val.backend_options,
            element_config: val.element_config.map(Into::into),
            quality_thresholds: val.quality_thresholds.map(Into::into),
            pipeline: val.pipeline.map(Into::into),
            auto_rotate: Some(val.auto_rotate),
            vlm_fallback: Some(val.vlm_fallback.into()),
            vlm_config: val.vlm_config.map(Into::into),
            vlm_prompt: val.vlm_prompt.map(|v| v.to_string()),
            acceleration: val.acceleration.map(Into::into),
            security_limits: val.security_limits.map(Into::into),
            tessdata_bytes: val
                .tessdata_bytes
                .map(|m| m.into_iter().map(|(k, v)| (k, v.to_vec().into())).collect()),
            tessdata_path: val.tessdata_path.map(|p| p.to_string_lossy().to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsOcrElement> for xberg::OcrElement {
    fn from(val: JsOcrElement) -> Self {
        let mut __result = xberg::OcrElement::default();
        if let Some(__v) = val.text {
            __result.text = __v;
        }
        if let Some(__v) = val.geometry {
            __result.geometry = __v.into();
        }
        if let Some(__v) = val.confidence {
            __result.confidence = __v.into();
        }
        if let Some(__v) = val.level {
            __result.level = __v.into();
        }
        __result.rotation = val.rotation.map(Into::into);
        if let Some(__v) = val.page_number {
            __result.page_number = __v;
        }
        __result.parent_id = val.parent_id;
        if let Some(__v) = val.backend_metadata {
            __result.backend_metadata = __v.into_iter().map(|(k, v)| (k.into(), v)).collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrElement> for JsOcrElement {
    fn from(val: xberg::OcrElement) -> Self {
        Self {
            text: Some(val.text.to_string()),
            geometry: Some(val.geometry.into()),
            confidence: Some(val.confidence.into()),
            level: Some(val.level.into()),
            rotation: val.rotation.map(Into::into),
            page_number: Some(val.page_number),
            parent_id: val.parent_id.map(|v| v.to_string()),
            backend_metadata: Some(val.backend_metadata.into_iter().map(|(k, v)| (k.into(), v)).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsOcrElementConfig> for xberg::OcrElementConfig {
    fn from(val: JsOcrElementConfig) -> Self {
        let mut __result = xberg::OcrElementConfig::default();
        if let Some(__v) = val.include_elements {
            __result.include_elements = __v;
        }
        if let Some(__v) = val.min_level {
            __result.min_level = __v.into();
        }
        if let Some(__v) = val.min_confidence {
            __result.min_confidence = __v;
        }
        if let Some(__v) = val.build_hierarchy {
            __result.build_hierarchy = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::OcrElementConfig> for JsOcrElementConfig {
    fn from(val: xberg::OcrElementConfig) -> Self {
        Self {
            include_elements: Some(val.include_elements),
            min_level: Some(val.min_level.into()),
            min_confidence: Some(val.min_confidence),
            build_hierarchy: Some(val.build_hierarchy),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrExtractionResult> for JsOcrExtractionResult {
    fn from(val: xberg::OcrExtractionResult) -> Self {
        Self {
            content: Some(val.content.to_string()),
            mime_type: Some(val.mime_type.to_string()),
            metadata: Some(val.metadata.into_iter().map(|(k, v)| (k.into(), v)).collect()),
            tables: Some(val.tables.into_iter().map(Into::into).collect()),
            ocr_elements: val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsOcrMetadata> for xberg::OcrMetadata {
    fn from(val: JsOcrMetadata) -> Self {
        let mut __result = xberg::OcrMetadata::default();
        if let Some(__v) = val.language {
            __result.language = __v;
        }
        if let Some(__v) = val.psm {
            __result.psm = __v;
        }
        if let Some(__v) = val.output_format {
            __result.output_format = __v;
        }
        if let Some(__v) = val.table_count {
            __result.table_count = __v;
        }
        __result.table_rows = val.table_rows;
        __result.table_cols = val.table_cols;
        __result
    }
}

impl From<xberg::OcrMetadata> for JsOcrMetadata {
    fn from(val: xberg::OcrMetadata) -> Self {
        Self {
            language: Some(val.language.to_string()),
            psm: Some(val.psm),
            output_format: Some(val.output_format.to_string()),
            table_count: Some(val.table_count),
            table_rows: val.table_rows,
            table_cols: val.table_cols,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsOcrPipelineConfig> for xberg::OcrPipelineConfig {
    fn from(val: JsOcrPipelineConfig) -> Self {
        Self {
            stages: val.stages.into_iter().map(Into::into).collect(),
            quality_thresholds: val.quality_thresholds.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::OcrPipelineConfig> for JsOcrPipelineConfig {
    fn from(val: xberg::OcrPipelineConfig) -> Self {
        Self {
            stages: val.stages.into_iter().map(Into::into).collect(),
            quality_thresholds: val.quality_thresholds.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsOcrPipelineStage> for xberg::OcrPipelineStage {
    fn from(val: JsOcrPipelineStage) -> Self {
        Self {
            backend: val.backend,
            priority: val.priority,
            language: val.language.map(|v| v.into_iter().collect()),
            tesseract_config: val.tesseract_config.map(Into::into),
            paddle_ocr_config: val.paddle_ocr_config,
            vlm_config: val.vlm_config.map(Into::into),
            backend_options: val.backend_options,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::OcrPipelineStage> for JsOcrPipelineStage {
    fn from(val: xberg::OcrPipelineStage) -> Self {
        Self {
            backend: val.backend.to_string(),
            priority: val.priority,
            language: val.language.map(|v| v.into_iter().collect()),
            tesseract_config: val.tesseract_config.map(Into::into),
            paddle_ocr_config: val.paddle_ocr_config,
            vlm_config: val.vlm_config.map(Into::into),
            backend_options: val.backend_options,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsOcrPoint> for xberg::OcrPoint {
    fn from(val: JsOcrPoint) -> Self {
        let mut __result = xberg::OcrPoint::default();
        if let Some(__v) = val.x {
            __result.x = __v;
        }
        if let Some(__v) = val.y {
            __result.y = __v;
        }
        __result
    }
}

impl From<xberg::OcrPoint> for JsOcrPoint {
    fn from(val: xberg::OcrPoint) -> Self {
        Self {
            x: Some(val.x),
            y: Some(val.y),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsOcrQualityThresholds> for xberg::OcrQualityThresholds {
    fn from(val: JsOcrQualityThresholds) -> Self {
        let mut __result = xberg::OcrQualityThresholds::default();
        if let Some(__v) = val.min_total_non_whitespace {
            __result.min_total_non_whitespace = __v as usize;
        }
        if let Some(__v) = val.min_non_whitespace_per_page {
            __result.min_non_whitespace_per_page = __v;
        }
        if let Some(__v) = val.min_meaningful_word_len {
            __result.min_meaningful_word_len = __v as usize;
        }
        if let Some(__v) = val.min_meaningful_words {
            __result.min_meaningful_words = __v as usize;
        }
        if let Some(__v) = val.min_alnum_ratio {
            __result.min_alnum_ratio = __v;
        }
        if let Some(__v) = val.min_garbage_chars {
            __result.min_garbage_chars = __v as usize;
        }
        if let Some(__v) = val.max_fragmented_word_ratio {
            __result.max_fragmented_word_ratio = __v;
        }
        if let Some(__v) = val.critical_fragmented_word_ratio {
            __result.critical_fragmented_word_ratio = __v;
        }
        if let Some(__v) = val.max_ocr_output_fragmented_word_ratio {
            __result.max_ocr_output_fragmented_word_ratio = __v;
        }
        if let Some(__v) = val.min_ocr_mean_confidence {
            __result.min_ocr_mean_confidence = __v;
        }
        if let Some(__v) = val.min_words_for_ocr_output_check {
            __result.min_words_for_ocr_output_check = __v as usize;
        }
        if let Some(__v) = val.max_ocr_output_dict_invalid_word_ratio {
            __result.max_ocr_output_dict_invalid_word_ratio = __v;
        }
        if let Some(__v) = val.discard_suspected_ocr_noise {
            __result.discard_suspected_ocr_noise = __v;
        }
        if let Some(__v) = val.min_avg_word_length {
            __result.min_avg_word_length = __v;
        }
        if let Some(__v) = val.min_words_for_avg_length_check {
            __result.min_words_for_avg_length_check = __v as usize;
        }
        if let Some(__v) = val.min_consecutive_repeat_ratio {
            __result.min_consecutive_repeat_ratio = __v;
        }
        if let Some(__v) = val.min_words_for_repeat_check {
            __result.min_words_for_repeat_check = __v as usize;
        }
        if let Some(__v) = val.substantive_min_chars {
            __result.substantive_min_chars = __v as usize;
        }
        if let Some(__v) = val.non_text_min_chars {
            __result.non_text_min_chars = __v as usize;
        }
        if let Some(__v) = val.alnum_ws_ratio_threshold {
            __result.alnum_ws_ratio_threshold = __v;
        }
        if let Some(__v) = val.pipeline_min_quality {
            __result.pipeline_min_quality = __v;
        }
        if let Some(__v) = val.min_undecodable_ratio {
            __result.min_undecodable_ratio = __v;
        }
        if let Some(__v) = val.enable_provenance_ocr_routing {
            __result.enable_provenance_ocr_routing = __v;
        }
        if let Some(__v) = val.min_provenance_fallback_ratio {
            __result.min_provenance_fallback_ratio = __v;
        }
        __result
    }
}

impl From<xberg::OcrQualityThresholds> for JsOcrQualityThresholds {
    fn from(val: xberg::OcrQualityThresholds) -> Self {
        Self {
            min_total_non_whitespace: Some(val.min_total_non_whitespace as i64),
            min_non_whitespace_per_page: Some(val.min_non_whitespace_per_page),
            min_meaningful_word_len: Some(val.min_meaningful_word_len as i64),
            min_meaningful_words: Some(val.min_meaningful_words as i64),
            min_alnum_ratio: Some(val.min_alnum_ratio),
            min_garbage_chars: Some(val.min_garbage_chars as i64),
            max_fragmented_word_ratio: Some(val.max_fragmented_word_ratio),
            critical_fragmented_word_ratio: Some(val.critical_fragmented_word_ratio),
            max_ocr_output_fragmented_word_ratio: Some(val.max_ocr_output_fragmented_word_ratio),
            min_ocr_mean_confidence: Some(val.min_ocr_mean_confidence),
            min_words_for_ocr_output_check: Some(val.min_words_for_ocr_output_check as i64),
            max_ocr_output_dict_invalid_word_ratio: Some(val.max_ocr_output_dict_invalid_word_ratio),
            discard_suspected_ocr_noise: Some(val.discard_suspected_ocr_noise),
            min_avg_word_length: Some(val.min_avg_word_length),
            min_words_for_avg_length_check: Some(val.min_words_for_avg_length_check as i64),
            min_consecutive_repeat_ratio: Some(val.min_consecutive_repeat_ratio),
            min_words_for_repeat_check: Some(val.min_words_for_repeat_check as i64),
            substantive_min_chars: Some(val.substantive_min_chars as i64),
            non_text_min_chars: Some(val.non_text_min_chars as i64),
            alnum_ws_ratio_threshold: Some(val.alnum_ws_ratio_threshold),
            pipeline_min_quality: Some(val.pipeline_min_quality),
            min_undecodable_ratio: Some(val.min_undecodable_ratio),
            enable_provenance_ocr_routing: Some(val.enable_provenance_ocr_routing),
            min_provenance_fallback_ratio: Some(val.min_provenance_fallback_ratio),
        }
    }
}

impl From<JsOcrRotation> for xberg::OcrRotation {
    fn from(val: JsOcrRotation) -> Self {
        Self {
            angle_degrees: val.angle_degrees,
            confidence: val.confidence,
        }
    }
}

impl From<xberg::OcrRotation> for JsOcrRotation {
    fn from(val: xberg::OcrRotation) -> Self {
        Self {
            angle_degrees: val.angle_degrees,
            confidence: val.confidence,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::OcrTable> for JsOcrTable {
    fn from(val: xberg::OcrTable) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown.to_string(),
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
        }
    }
}

impl From<xberg::OcrTableBoundingBox> for JsOcrTableBoundingBox {
    fn from(val: xberg::OcrTableBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
        }
    }
}

impl From<xberg::OrientationResult> for JsOrientationResult {
    fn from(val: xberg::OrientationResult) -> Self {
        Self {
            degrees: val.degrees,
            confidence: val.confidence as f64,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsPaddleOcrConfig> for xberg::PaddleOcrConfig {
    fn from(val: JsPaddleOcrConfig) -> Self {
        let mut __result = xberg::PaddleOcrConfig::default();
        if let Some(__v) = val.language {
            __result.language = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        if let Some(__v) = val.use_angle_cls {
            __result.use_angle_cls = __v;
        }
        if let Some(__v) = val.enable_table_detection {
            __result.enable_table_detection = __v;
        }
        if let Some(__v) = val.det_db_thresh {
            __result.det_db_thresh = __v as f32;
        }
        if let Some(__v) = val.det_db_box_thresh {
            __result.det_db_box_thresh = __v as f32;
        }
        if let Some(__v) = val.det_db_unclip_ratio {
            __result.det_db_unclip_ratio = __v as f32;
        }
        if let Some(__v) = val.det_limit_side_len {
            __result.det_limit_side_len = __v;
        }
        if let Some(__v) = val.rec_batch_num {
            __result.rec_batch_num = __v;
        }
        if let Some(__v) = val.padding {
            __result.padding = __v;
        }
        if let Some(__v) = val.drop_score {
            __result.drop_score = __v as f32;
        }
        if let Some(__v) = val.model_tier {
            __result.model_tier = __v;
        }
        if let Some(__v) = val.model_version {
            __result.model_version = __v;
        }
        __result.inference_backend = val.inference_backend.map(Into::into);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PaddleOcrConfig> for JsPaddleOcrConfig {
    fn from(val: xberg::PaddleOcrConfig) -> Self {
        Self {
            language: Some(val.language.to_string()),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            use_angle_cls: Some(val.use_angle_cls),
            enable_table_detection: Some(val.enable_table_detection),
            det_db_thresh: Some(val.det_db_thresh as f64),
            det_db_box_thresh: Some(val.det_db_box_thresh as f64),
            det_db_unclip_ratio: Some(val.det_db_unclip_ratio as f64),
            det_limit_side_len: Some(val.det_limit_side_len),
            rec_batch_num: Some(val.rec_batch_num),
            padding: Some(val.padding),
            drop_score: Some(val.drop_score as f64),
            model_tier: Some(val.model_tier.to_string()),
            model_version: Some(val.model_version.to_string()),
            inference_backend: val.inference_backend.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::candle_ocr::PaddleOcrVlBackendOptions> for JsPaddleOcrVlBackendOptions {
    fn from(val: xberg::candle_ocr::PaddleOcrVlBackendOptions) -> Self {
        Self {
            task: val.task.map(Into::into),
            model_path: val.model_path.map(|v| v.to_string()),
            model_id: val.model_id.map(|v| v.to_string()),
            hf_revision: val.hf_revision.map(|v| v.to_string()),
            cache_dir: val.cache_dir.map(|v| v.to_string()),
            device: val.device.map(Into::into),
        }
    }
}

impl From<JsPageBoundary> for xberg::PageBoundary {
    fn from(val: JsPageBoundary) -> Self {
        Self {
            byte_start: val.byte_start as usize,
            byte_end: val.byte_end as usize,
            page_number: val.page_number,
        }
    }
}

impl From<xberg::PageBoundary> for JsPageBoundary {
    fn from(val: xberg::PageBoundary) -> Self {
        Self {
            byte_start: val.byte_start as i64,
            byte_end: val.byte_end as i64,
            page_number: val.page_number,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPageClassification> for xberg::PageClassification {
    fn from(val: JsPageClassification) -> Self {
        Self {
            page_number: val.page_number,
            labels: val.labels.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageClassification> for JsPageClassification {
    fn from(val: xberg::PageClassification) -> Self {
        Self {
            page_number: val.page_number,
            labels: val.labels.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPageClassificationConfig> for xberg::PageClassificationConfig {
    fn from(val: JsPageClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template,
            labels: val.labels.into_iter().collect(),
            multi_label: val.multi_label,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageClassificationConfig> for JsPageClassificationConfig {
    fn from(val: xberg::PageClassificationConfig) -> Self {
        Self {
            prompt_template: val.prompt_template.map(|v| v.to_string()),
            labels: val.labels.into_iter().collect(),
            multi_label: val.multi_label,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPageConfig> for xberg::PageConfig {
    fn from(val: JsPageConfig) -> Self {
        let mut __result = xberg::PageConfig::default();
        if let Some(__v) = val.extract_pages {
            __result.extract_pages = __v;
        }
        if let Some(__v) = val.insert_page_markers {
            __result.insert_page_markers = __v;
        }
        if let Some(__v) = val.marker_format {
            __result.marker_format = __v;
        }
        __result
    }
}

impl From<xberg::PageConfig> for JsPageConfig {
    fn from(val: xberg::PageConfig) -> Self {
        Self {
            extract_pages: Some(val.extract_pages),
            insert_page_markers: Some(val.insert_page_markers),
            marker_format: Some(val.marker_format.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsPageContent> for xberg::PageContent {
    fn from(val: JsPageContent) -> Self {
        Self {
            page_number: val.page_number,
            content: val.content,
            tables: val.tables.into_iter().map(|v| std::sync::Arc::new(v.into())).collect(),
            image_indices: val.image_indices.into_iter().collect(),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            hierarchy: val.hierarchy.map(Into::into),
            is_blank: val.is_blank,
            layout_regions: val.layout_regions.map(|v| v.into_iter().map(Into::into).collect()),
            speaker_notes: val.speaker_notes,
            section_name: val.section_name,
            sheet_name: val.sheet_name,
            ocr_confidence: val.ocr_confidence.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageContent> for JsPageContent {
    fn from(val: xberg::PageContent) -> Self {
        Self {
            page_number: val.page_number,
            content: val.content.to_string(),
            tables: val.tables.into_iter().map(|v| (*v).clone().into()).collect(),
            image_indices: val.image_indices.into_iter().collect(),
            image_preprocessing: val.image_preprocessing.map(Into::into),
            hierarchy: val.hierarchy.map(Into::into),
            is_blank: val.is_blank,
            layout_regions: val.layout_regions.map(|v| v.into_iter().map(Into::into).collect()),
            speaker_notes: val.speaker_notes.map(|v| v.to_string()),
            section_name: val.section_name.map(|v| v.to_string()),
            sheet_name: val.sheet_name.map(|v| v.to_string()),
            ocr_confidence: val.ocr_confidence.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPageDimensions> for xberg::PageDimensions {
    fn from(val: JsPageDimensions) -> Self {
        let mut __result = xberg::PageDimensions::default();
        if let Some(__v) = val.width {
            __result.width = __v;
        }
        if let Some(__v) = val.height {
            __result.height = __v;
        }
        __result
    }
}

impl From<xberg::PageDimensions> for JsPageDimensions {
    fn from(val: xberg::PageDimensions) -> Self {
        Self {
            width: Some(val.width),
            height: Some(val.height),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPageHierarchy> for xberg::PageHierarchy {
    fn from(val: JsPageHierarchy) -> Self {
        Self {
            block_count: val.block_count,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageHierarchy> for JsPageHierarchy {
    fn from(val: xberg::PageHierarchy) -> Self {
        Self {
            block_count: val.block_count,
            blocks: val.blocks.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPageInfo> for xberg::PageInfo {
    fn from(val: JsPageInfo) -> Self {
        Self {
            number: val.number,
            title: val.title,
            dimensions: val.dimensions.map(Into::into),
            image_count: val.image_count,
            table_count: val.table_count,
            hidden: val.hidden,
            is_blank: val.is_blank,
            has_vector_graphics: val.has_vector_graphics,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageInfo> for JsPageInfo {
    fn from(val: xberg::PageInfo) -> Self {
        Self {
            number: val.number,
            title: val.title.map(|v| v.to_string()),
            dimensions: val.dimensions.map(Into::into),
            image_count: val.image_count,
            table_count: val.table_count,
            hidden: val.hidden,
            is_blank: val.is_blank,
            has_vector_graphics: val.has_vector_graphics,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPageOcrConfidence> for xberg::PageOcrConfidence {
    fn from(val: JsPageOcrConfidence) -> Self {
        let mut __result = xberg::PageOcrConfidence::default();
        __result.score = val.score;
        if let Some(__v) = val.word_count {
            __result.word_count = __v;
        }
        if let Some(__v) = val.backend {
            __result.backend = __v;
        }
        __result
    }
}

impl From<xberg::PageOcrConfidence> for JsPageOcrConfidence {
    fn from(val: xberg::PageOcrConfidence) -> Self {
        Self {
            score: val.score,
            word_count: Some(val.word_count),
            backend: Some(val.backend.to_string()),
        }
    }
}

impl From<xberg::PageRange> for JsPageRange {
    fn from(val: xberg::PageRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

impl From<JsPageSignals> for xberg::PageSignals {
    fn from(val: JsPageSignals) -> Self {
        Self {
            page_number: val.page_number,
            text_excerpt: val.text_excerpt,
            starts_with_letterhead_like: val.starts_with_letterhead_like,
            has_page_number_one_marker: val.has_page_number_one_marker,
            has_signature_block: val.has_signature_block,
            layout_text_density: val.layout_text_density as f32,
        }
    }
}

impl From<xberg::PageSignals> for JsPageSignals {
    fn from(val: xberg::PageSignals) -> Self {
        Self {
            page_number: val.page_number,
            text_excerpt: val.text_excerpt.to_string(),
            starts_with_letterhead_like: val.starts_with_letterhead_like,
            has_page_number_one_marker: val.has_page_number_one_marker,
            has_signature_block: val.has_signature_block,
            layout_text_density: val.layout_text_density as f64,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPageSpan> for xberg::PageSpan {
    fn from(val: JsPageSpan) -> Self {
        Self {
            page: val.page,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PageSpan> for JsPageSpan {
    fn from(val: xberg::PageSpan) -> Self {
        Self {
            page: val.page,
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsPageStructure> for xberg::PageStructure {
    fn from(val: JsPageStructure) -> Self {
        Self {
            total_count: val.total_count,
            unit_type: val.unit_type.into(),
            boundaries: val.boundaries.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PageStructure> for JsPageStructure {
    fn from(val: xberg::PageStructure) -> Self {
        Self {
            total_count: val.total_count,
            unit_type: val.unit_type.into(),
            boundaries: val.boundaries.map(|v| v.into_iter().map(Into::into).collect()),
            pages: val.pages.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::text::redaction::patterns::PatternMatch> for JsPatternMatch {
    fn from(val: xberg::text::redaction::patterns::PatternMatch) -> Self {
        Self {
            start: val.start as i64,
            end: val.end as i64,
            category: val.category.into(),
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsPdfAnnotation> for xberg::PdfAnnotation {
    fn from(val: JsPdfAnnotation) -> Self {
        Self {
            annotation_type: val.annotation_type.into(),
            content: val.content,
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            author: val.author,
            modified: val.modified,
            color: val.color,
            subject: val.subject,
            quad_points: val.quad_points.map(|v| v.into_iter().map(Into::into).collect()),
            marked_text: val.marked_text,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfAnnotation> for JsPdfAnnotation {
    fn from(val: xberg::PdfAnnotation) -> Self {
        Self {
            annotation_type: val.annotation_type.into(),
            content: val.content.map(|v| v.to_string()),
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
            author: val.author.map(|v| v.to_string()),
            modified: val.modified.map(|v| v.to_string()),
            color: val.color.map(|v| v.to_string()),
            subject: val.subject.map(|v| v.to_string()),
            quad_points: val.quad_points.map(|v| v.into_iter().map(Into::into).collect()),
            marked_text: val.marked_text.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsPdfConfig> for xberg::PdfConfig {
    fn from(val: JsPdfConfig) -> Self {
        let mut __result = xberg::PdfConfig::default();
        if let Some(__v) = val.extract_images {
            __result.extract_images = __v;
        }
        if let Some(__v) = val.extract_tables {
            __result.extract_tables = __v;
        }
        __result.passwords = val.passwords.map(|v| v.into_iter().collect());
        if let Some(__v) = val.extract_metadata {
            __result.extract_metadata = __v;
        }
        __result.hierarchy = val.hierarchy.map(Into::into);
        if let Some(__v) = val.extract_annotations {
            __result.extract_annotations = __v;
        }
        __result.top_margin_fraction = val.top_margin_fraction.map(|v| v as f32);
        __result.bottom_margin_fraction = val.bottom_margin_fraction.map(|v| v as f32);
        if let Some(__v) = val.allow_single_column_tables {
            __result.allow_single_column_tables = __v;
        }
        if let Some(__v) = val.ocr_inline_images {
            __result.ocr_inline_images = __v;
        }
        if let Some(__v) = val.extract_form_fields {
            __result.extract_form_fields = __v;
        }
        if let Some(__v) = val.reading_order {
            __result.reading_order = __v;
        }
        if let Some(__v) = val.backend {
            __result.backend = __v.into();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfConfig> for JsPdfConfig {
    fn from(val: xberg::PdfConfig) -> Self {
        Self {
            extract_images: Some(val.extract_images),
            extract_tables: Some(val.extract_tables),
            passwords: val.passwords.map(|v| v.into_iter().collect()),
            extract_metadata: Some(val.extract_metadata),
            hierarchy: val.hierarchy.map(Into::into),
            extract_annotations: Some(val.extract_annotations),
            top_margin_fraction: val.top_margin_fraction.map(|v| v as f64),
            bottom_margin_fraction: val.bottom_margin_fraction.map(|v| v as f64),
            allow_single_column_tables: Some(val.allow_single_column_tables),
            ocr_inline_images: Some(val.ocr_inline_images),
            extract_form_fields: Some(val.extract_form_fields),
            reading_order: Some(val.reading_order),
            backend: Some(val.backend.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPdfFormField> for xberg::PdfFormField {
    fn from(val: JsPdfFormField) -> Self {
        Self {
            name: val.name,
            full_name: val.full_name,
            field_type: val.field_type.into(),
            value: val.value,
            default_value: val.default_value,
            flags: val.flags,
            page: val.page,
            bbox: val.bbox.map(Into::into),
            max_length: val.max_length,
            tooltip: val.tooltip,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PdfFormField> for JsPdfFormField {
    fn from(val: xberg::PdfFormField) -> Self {
        Self {
            name: val.name.to_string(),
            full_name: val.full_name.to_string(),
            field_type: val.field_type.into(),
            value: val.value.map(|v| v.to_string()),
            default_value: val.default_value.map(|v| v.to_string()),
            flags: val.flags,
            page: val.page,
            bbox: val.bbox.map(Into::into),
            max_length: val.max_length,
            tooltip: val.tooltip.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsPdfMetadata> for xberg::pdf::metadata::PdfMetadata {
    fn from(val: JsPdfMetadata) -> Self {
        let mut __result = xberg::pdf::metadata::PdfMetadata::default();
        __result.pdf_version = val.pdf_version;
        __result.producer = val.producer;
        __result.is_encrypted = val.is_encrypted;
        __result.width = val.width;
        __result.height = val.height;
        __result.page_count = val.page_count;
        __result.scanned_confidence = val.scanned_confidence.map(|v| v as f32);
        __result.scanned_pages = val.scanned_pages.map(|v| v.into_iter().collect());
        __result.layout_gated_pages = val.layout_gated_pages.map(|v| v.into_iter().collect());
        __result.layout_gate_reasons = val.layout_gate_reasons.map(|v| v.into_iter().collect());
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::pdf::metadata::PdfMetadata> for JsPdfMetadata {
    fn from(val: xberg::pdf::metadata::PdfMetadata) -> Self {
        Self {
            pdf_version: val.pdf_version.map(|v| v.to_string()),
            producer: val.producer.map(|v| v.to_string()),
            is_encrypted: val.is_encrypted,
            width: val.width,
            height: val.height,
            page_count: val.page_count,
            scanned_confidence: val.scanned_confidence.map(|v| v as f64),
            scanned_pages: val.scanned_pages.map(|v| v.into_iter().collect()),
            layout_gated_pages: val.layout_gated_pages.map(|v| v.into_iter().collect()),
            layout_gate_reasons: val.layout_gate_reasons.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPixelDimensions> for xberg::PixelDimensions {
    fn from(val: JsPixelDimensions) -> Self {
        let mut __result = xberg::PixelDimensions::default();
        if let Some(__v) = val.width {
            __result.width = __v as usize;
        }
        if let Some(__v) = val.height {
            __result.height = __v as usize;
        }
        __result
    }
}

impl From<xberg::PixelDimensions> for JsPixelDimensions {
    fn from(val: xberg::PixelDimensions) -> Self {
        Self {
            width: Some(val.width as i64),
            height: Some(val.height as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsPostProcessorConfig> for xberg::PostProcessorConfig {
    fn from(val: JsPostProcessorConfig) -> Self {
        let mut __result = xberg::PostProcessorConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        __result.enabled_processors = val.enabled_processors.map(|v| v.into_iter().collect());
        __result.disabled_processors = val.disabled_processors.map(|v| v.into_iter().collect());
        __result.enabled_set = val.enabled_set.map(|v| v.into_iter().collect());
        __result.disabled_set = val.disabled_set.map(|v| v.into_iter().collect());
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PostProcessorConfig> for JsPostProcessorConfig {
    fn from(val: xberg::PostProcessorConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            enabled_processors: val.enabled_processors.map(|v| v.into_iter().collect()),
            disabled_processors: val.disabled_processors.map(|v| v.into_iter().collect()),
            enabled_set: val.enabled_set.map(|v| v.into_iter().collect()),
            disabled_set: val.disabled_set.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::extraction::office_metadata::app_properties::PptxAppProperties> for JsPptxAppProperties {
    fn from(val: xberg::extraction::office_metadata::app_properties::PptxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            total_time: val.total_time,
            company: val.company.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            slides: val.slides,
            notes: val.notes,
            hidden_slides: val.hidden_slides,
            multimedia_clips: val.multimedia_clips,
            presentation_format: val.presentation_format.map(|v| v.to_string()),
            slide_titles: Some(val.slide_titles.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::PptxExtractionResult> for JsPptxExtractionResult {
    fn from(val: xberg::PptxExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            metadata: val.metadata.into(),
            slide_count: val.slide_count as i64,
            image_count: val.image_count as i64,
            table_count: val.table_count as i64,
            images: val.images.into_iter().map(Into::into).collect(),
            page_structure: val.page_structure.map(Into::into),
            page_contents: val.page_contents.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            hyperlinks: val.hyperlinks.into_iter().map(Into::into).collect(),
            office_metadata: val
                .office_metadata
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPptxMetadata> for xberg::PptxMetadata {
    fn from(val: JsPptxMetadata) -> Self {
        let mut __result = xberg::PptxMetadata::default();
        if let Some(__v) = val.slide_count {
            __result.slide_count = __v;
        }
        if let Some(__v) = val.slide_names {
            __result.slide_names = __v.into_iter().collect();
        }
        __result.image_count = val.image_count;
        __result.table_count = val.table_count;
        __result
    }
}

impl From<xberg::PptxMetadata> for JsPptxMetadata {
    fn from(val: xberg::PptxMetadata) -> Self {
        Self {
            slide_count: Some(val.slide_count),
            slide_names: Some(val.slide_names.into_iter().collect()),
            image_count: val.image_count,
            table_count: val.table_count,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsPreprocessingOptions> for html_to_markdown_rs::PreprocessingOptions {
    fn from(val: JsPreprocessingOptions) -> Self {
        let mut __result = html_to_markdown_rs::PreprocessingOptions::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        if let Some(__v) = val.preset {
            __result.preset = __v.into();
        }
        if let Some(__v) = val.remove_navigation {
            __result.remove_navigation = __v;
        }
        if let Some(__v) = val.remove_forms {
            __result.remove_forms = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<html_to_markdown_rs::PreprocessingOptions> for JsPreprocessingOptions {
    fn from(val: html_to_markdown_rs::PreprocessingOptions) -> Self {
        Self {
            enabled: Some(val.enabled),
            preset: Some(val.preset.into()),
            remove_navigation: Some(val.remove_navigation),
            remove_forms: Some(val.remove_forms),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PresentationHyperlink> for JsPresentationHyperlink {
    fn from(val: xberg::PresentationHyperlink) -> Self {
        Self {
            url: Some(val.url.to_string()),
            label: val.label.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPreset> for xberg::Preset {
    fn from(val: JsPreset) -> Self {
        Self {
            id: val.id,
            version: val.version,
            schema_name: val.schema_name,
            description: val.description,
            category: val.category.into(),
            tags: val.tags.into_iter().collect(),
            schema: val.schema,
            system_prompt: val.system_prompt,
            context_template: val.context_template,
            merge_mode: val.merge_mode.into(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
            sample: val.sample.map(Into::into),
            fingerprint: val.fingerprint,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Preset> for JsPreset {
    fn from(val: xberg::Preset) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            schema_name: val.schema_name.to_string(),
            description: val.description.to_string(),
            category: val.category.into(),
            tags: val.tags.into_iter().collect(),
            schema: val.schema,
            system_prompt: val.system_prompt.to_string(),
            context_template: val.context_template.map(|v| v.to_string()),
            merge_mode: val.merge_mode.into(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
            sample: val.sample.map(Into::into),
            fingerprint: val.fingerprint.to_string(),
        }
    }
}

impl From<JsPresetSample> for xberg::PresetSample {
    fn from(val: JsPresetSample) -> Self {
        Self {
            input_path: val.input_path,
            output_path: val.output_path,
        }
    }
}

impl From<xberg::PresetSample> for JsPresetSample {
    fn from(val: xberg::PresetSample) -> Self {
        Self {
            input_path: val.input_path.to_string(),
            output_path: val.output_path.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPresetSummary> for xberg::PresetSummary {
    fn from(val: JsPresetSummary) -> Self {
        Self {
            id: val.id,
            version: val.version,
            schema_name: val.schema_name,
            description: val.description,
            category: val.category.into(),
            tags: val.tags.into_iter().collect(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
            fingerprint: val.fingerprint,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::PresetSummary> for JsPresetSummary {
    fn from(val: xberg::PresetSummary) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            schema_name: val.schema_name.to_string(),
            description: val.description.to_string(),
            category: val.category.into(),
            tags: val.tags.into_iter().collect(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
            fingerprint: val.fingerprint.to_string(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsProcessingWarning> for xberg::ProcessingWarning {
    fn from(val: JsProcessingWarning) -> Self {
        Self {
            source: val.source.into(),
            message: val.message.into(),
        }
    }
}

impl From<xberg::ProcessingWarning> for JsProcessingWarning {
    fn from(val: xberg::ProcessingWarning) -> Self {
        Self {
            source: val.source.to_string(),
            message: val.message.to_string(),
        }
    }
}

impl From<JsPropertyChange> for xberg::PropertyChange {
    fn from(val: JsPropertyChange) -> Self {
        Self {
            name: val.name,
            from: val.from,
            to: val.to,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::PropertyChange> for JsPropertyChange {
    fn from(val: xberg::PropertyChange) -> Self {
        Self {
            name: val.name.to_string(),
            from: val.from.map(|v| v.to_string()),
            to: val.to.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsProxyConfig> for crawlberg::ProxyConfig {
    fn from(val: JsProxyConfig) -> Self {
        let mut __result = crawlberg::ProxyConfig::default();
        if let Some(__v) = val.url {
            __result.url = __v;
        }
        __result.username = val.username;
        __result.password = val.password;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::ProxyConfig> for JsProxyConfig {
    fn from(val: crawlberg::ProxyConfig) -> Self {
        Self {
            url: Some(val.url.to_string()),
            username: val.username.map(|v| v.to_string()),
            password: val.password.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPstMetadata> for xberg::PstMetadata {
    fn from(val: JsPstMetadata) -> Self {
        let mut __result = xberg::PstMetadata::default();
        if let Some(__v) = val.message_count {
            __result.message_count = __v as usize;
        }
        __result
    }
}

impl From<xberg::PstMetadata> for JsPstMetadata {
    fn from(val: xberg::PstMetadata) -> Self {
        Self {
            message_count: Some(val.message_count as i64),
        }
    }
}

impl From<JsQrBoundingBox> for xberg::QrBoundingBox {
    fn from(val: JsQrBoundingBox) -> Self {
        Self {
            x: val.x,
            y: val.y,
            width: val.width,
            height: val.height,
        }
    }
}

impl From<xberg::QrBoundingBox> for JsQrBoundingBox {
    fn from(val: xberg::QrBoundingBox) -> Self {
        Self {
            x: val.x,
            y: val.y,
            width: val.width,
            height: val.height,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsQrCode> for xberg::QrCode {
    fn from(val: JsQrCode) -> Self {
        Self {
            payload: val.payload,
            confidence: val.confidence.map(|v| v as f32),
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::QrCode> for JsQrCode {
    fn from(val: xberg::QrCode) -> Self {
        Self {
            payload: val.payload.to_string(),
            confidence: val.confidence.map(|v| v as f64),
            bbox: val.bbox.map(Into::into),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsRakeParams> for xberg::RakeParams {
    fn from(val: JsRakeParams) -> Self {
        let mut __result = xberg::RakeParams::default();
        if let Some(__v) = val.min_word_length {
            __result.min_word_length = __v as usize;
        }
        if let Some(__v) = val.max_words_per_phrase {
            __result.max_words_per_phrase = __v as usize;
        }
        __result
    }
}

impl From<xberg::RakeParams> for JsRakeParams {
    fn from(val: xberg::RakeParams) -> Self {
        Self {
            min_word_length: Some(val.min_word_length as i64),
            max_words_per_phrase: Some(val.max_words_per_phrase as i64),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RecognizedTable> for JsRecognizedTable {
    fn from(val: xberg::RecognizedTable) -> Self {
        Self {
            detection_bbox: val.detection_bbox.into(),
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsRedactionConfig> for xberg::RedactionConfig {
    fn from(val: JsRedactionConfig) -> Self {
        let mut __result = xberg::RedactionConfig::default();
        if let Some(__v) = val.categories {
            __result.categories = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.strategy {
            __result.strategy = __v.into();
        }
        __result.ner = val.ner.map(Into::into);
        if let Some(__v) = val.preserve_offsets {
            __result.preserve_offsets = __v;
        }
        if let Some(__v) = val.custom_terms {
            __result.custom_terms = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.custom_patterns {
            __result.custom_patterns = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RedactionConfig> for JsRedactionConfig {
    fn from(val: xberg::RedactionConfig) -> Self {
        Self {
            categories: Some(val.categories.into_iter().map(Into::into).collect()),
            strategy: Some(val.strategy.into()),
            ner: val.ner.map(Into::into),
            preserve_offsets: Some(val.preserve_offsets),
            custom_terms: Some(val.custom_terms.into_iter().map(Into::into).collect()),
            custom_patterns: Some(val.custom_patterns.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsRedactionFinding> for xberg::RedactionFinding {
    fn from(val: JsRedactionFinding) -> Self {
        Self {
            start: val.start,
            end: val.end,
            category: val.category.into(),
            strategy: val.strategy.into(),
            replacement_token: val.replacement_token,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RedactionFinding> for JsRedactionFinding {
    fn from(val: xberg::RedactionFinding) -> Self {
        Self {
            start: val.start,
            end: val.end,
            category: val.category.into(),
            strategy: val.strategy.into(),
            replacement_token: val.replacement_token.to_string(),
        }
    }
}

impl From<JsRedactionPattern> for xberg::RedactionPattern {
    fn from(val: JsRedactionPattern) -> Self {
        Self {
            label: val.label,
            pattern: val.pattern,
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RedactionPattern> for JsRedactionPattern {
    fn from(val: xberg::RedactionPattern) -> Self {
        Self {
            label: val.label.to_string(),
            pattern: val.pattern.to_string(),
            case_sensitive: val.case_sensitive,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsRedactionReport> for xberg::RedactionReport {
    fn from(val: JsRedactionReport) -> Self {
        Self {
            findings: val.findings.into_iter().map(Into::into).collect(),
            total_redacted: val.total_redacted,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RedactionReport> for JsRedactionReport {
    fn from(val: xberg::RedactionReport) -> Self {
        Self {
            findings: val.findings.into_iter().map(Into::into).collect(),
            total_redacted: val.total_redacted,
        }
    }
}

impl From<JsRedactionTerm> for xberg::RedactionTerm {
    fn from(val: JsRedactionTerm) -> Self {
        Self {
            label: val.label,
            value: val.value,
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RedactionTerm> for JsRedactionTerm {
    fn from(val: xberg::RedactionTerm) -> Self {
        Self {
            label: val.label.to_string(),
            value: val.value.to_string(),
            case_sensitive: val.case_sensitive,
        }
    }
}

impl From<xberg::RerankedDocument> for JsRerankedDocument {
    fn from(val: xberg::RerankedDocument) -> Self {
        Self {
            index: val.index as i64,
            score: val.score as f64,
            document: val.document.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsRerankerConfig> for xberg::RerankerConfig {
    fn from(val: JsRerankerConfig) -> Self {
        let mut __result = xberg::RerankerConfig::default();
        if let Some(__v) = val.model {
            __result.model = __v.into();
        }
        __result.top_k = val.top_k.map(|v| v as usize);
        if let Some(__v) = val.batch_size {
            __result.batch_size = __v as usize;
        }
        if let Some(__v) = val.show_download_progress {
            __result.show_download_progress = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        __result.acceleration = val.acceleration.map(Into::into);
        __result.max_rerank_duration_secs = val.max_rerank_duration_secs.map(|v| v as u64);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::RerankerConfig> for JsRerankerConfig {
    fn from(val: xberg::RerankerConfig) -> Self {
        Self {
            model: Some(val.model.into()),
            top_k: val.top_k.map(|v| v as i64),
            batch_size: Some(val.batch_size as i64),
            show_download_progress: Some(val.show_download_progress),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_rerank_duration_secs: val.max_rerank_duration_secs.map(|v| v as i64),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::ResolvedPreset> for JsResolvedPreset {
    fn from(val: xberg::ResolvedPreset) -> Self {
        Self {
            id: val.id.to_string(),
            version: val.version.to_string(),
            fingerprint: val.fingerprint.to_string(),
            schema_name: val.schema_name.to_string(),
            schema: val.schema,
            system_prompt: val.system_prompt.to_string(),
            merge_mode: val.merge_mode.into(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsRevisionDelta> for xberg::RevisionDelta {
    fn from(val: JsRevisionDelta) -> Self {
        let mut __result = xberg::RevisionDelta::default();
        if let Some(__v) = val.content {
            __result.content = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.table_changes {
            __result.table_changes = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.property_changes {
            __result.property_changes = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::RevisionDelta> for JsRevisionDelta {
    fn from(val: xberg::RevisionDelta) -> Self {
        Self {
            content: Some(val.content.into_iter().map(Into::into).collect()),
            table_changes: Some(val.table_changes.into_iter().map(Into::into).collect()),
            property_changes: Some(val.property_changes.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure)]
impl From<JsSecurityLimits> for xberg::SecurityLimits {
    fn from(val: JsSecurityLimits) -> Self {
        let mut __result = xberg::SecurityLimits::default();
        if let Some(__v) = val.max_archive_size {
            __result.max_archive_size = __v as usize;
        }
        if let Some(__v) = val.max_compression_ratio {
            __result.max_compression_ratio = __v as usize;
        }
        if let Some(__v) = val.max_files_in_archive {
            __result.max_files_in_archive = __v as usize;
        }
        if let Some(__v) = val.max_nesting_depth {
            __result.max_nesting_depth = __v as usize;
        }
        if let Some(__v) = val.max_entity_length {
            __result.max_entity_length = __v as usize;
        }
        if let Some(__v) = val.max_content_size {
            __result.max_content_size = __v as usize;
        }
        if let Some(__v) = val.max_iterations {
            __result.max_iterations = __v as usize;
        }
        if let Some(__v) = val.max_xml_depth {
            __result.max_xml_depth = __v as usize;
        }
        if let Some(__v) = val.max_table_cells {
            __result.max_table_cells = __v as usize;
        }
        __result.max_pages = val.max_pages.map(|v| v as usize);
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::SecurityLimits> for JsSecurityLimits {
    fn from(val: xberg::SecurityLimits) -> Self {
        Self {
            max_archive_size: Some(val.max_archive_size as i64),
            max_compression_ratio: Some(val.max_compression_ratio as i64),
            max_files_in_archive: Some(val.max_files_in_archive as i64),
            max_nesting_depth: Some(val.max_nesting_depth as i64),
            max_entity_length: Some(val.max_entity_length as i64),
            max_content_size: Some(val.max_content_size as i64),
            max_iterations: Some(val.max_iterations as i64),
            max_xml_depth: Some(val.max_xml_depth as i64),
            max_table_cells: Some(val.max_table_cells as i64),
            max_pages: val.max_pages.map(|v| v as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsServerConfig> for xberg::ServerConfig {
    fn from(val: JsServerConfig) -> Self {
        let mut __result = xberg::ServerConfig::default();
        if let Some(__v) = val.host {
            __result.host = __v;
        }
        if let Some(__v) = val.port {
            __result.port = __v;
        }
        if let Some(__v) = val.cors_origins {
            __result.cors_origins = __v.into_iter().collect();
        }
        if let Some(__v) = val.max_request_body_bytes {
            __result.max_request_body_bytes = __v as usize;
        }
        if let Some(__v) = val.max_multipart_field_bytes {
            __result.max_multipart_field_bytes = __v as usize;
        }
        if let Some(__v) = val.job_timeout_secs {
            __result.job_timeout_secs = __v as u64;
        }
        __result
    }
}

impl From<xberg::ServerConfig> for JsServerConfig {
    fn from(val: xberg::ServerConfig) -> Self {
        Self {
            host: Some(val.host.to_string()),
            port: Some(val.port),
            cors_origins: Some(val.cors_origins.into_iter().collect()),
            max_request_body_bytes: Some(val.max_request_body_bytes as i64),
            max_multipart_field_bytes: Some(val.max_multipart_field_bytes as i64),
            job_timeout_secs: Some(val.job_timeout_secs as i64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsSitemapUrl> for crawlberg::SitemapUrl {
    fn from(val: JsSitemapUrl) -> Self {
        let mut __result = crawlberg::SitemapUrl::default();
        if let Some(__v) = val.url {
            __result.url = __v;
        }
        __result.lastmod = val.lastmod;
        __result.changefreq = val.changefreq;
        __result.priority = val.priority;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<crawlberg::SitemapUrl> for JsSitemapUrl {
    fn from(val: crawlberg::SitemapUrl) -> Self {
        Self {
            url: Some(val.url.to_string()),
            lastmod: val.lastmod.map(|v| v.to_string()),
            changefreq: val.changefreq.map(|v| v.to_string()),
            priority: val.priority.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<JsSparseEmbedding> for xberg::SparseEmbedding {
    fn from(val: JsSparseEmbedding) -> Self {
        Self {
            indices: val.indices.into_iter().collect(),
            values: val.values.into_iter().map(|v| v as f32).collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::SparseEmbedding> for JsSparseEmbedding {
    fn from(val: xberg::SparseEmbedding) -> Self {
        Self {
            indices: val.indices.into_iter().collect(),
            values: val.values.iter().map(|&v| v as f64).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsSparseEmbeddingConfig> for xberg::SparseEmbeddingConfig {
    fn from(val: JsSparseEmbeddingConfig) -> Self {
        let mut __result = xberg::SparseEmbeddingConfig::default();
        if let Some(__v) = val.model {
            __result.model = __v.into();
        }
        if let Some(__v) = val.batch_size {
            __result.batch_size = __v as usize;
        }
        if let Some(__v) = val.max_length {
            __result.max_length = __v as usize;
        }
        if let Some(__v) = val.show_download_progress {
            __result.show_download_progress = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        __result.acceleration = val.acceleration.map(Into::into);
        __result.max_embed_duration_secs = val.max_embed_duration_secs.map(|v| v as u64);
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::SparseEmbeddingConfig> for JsSparseEmbeddingConfig {
    fn from(val: xberg::SparseEmbeddingConfig) -> Self {
        Self {
            model: Some(val.model.into()),
            batch_size: Some(val.batch_size as i64),
            max_length: Some(val.max_length as i64),
            show_download_progress: Some(val.show_download_progress),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            acceleration: val.acceleration.map(Into::into),
            max_embed_duration_secs: val.max_embed_duration_secs.map(|v| v as i64),
        }
    }
}

impl From<xberg::SparseEmbeddingPreset> for JsSparseEmbeddingPreset {
    fn from(val: xberg::SparseEmbeddingPreset) -> Self {
        Self {
            name: val.name.to_string(),
            model_repo: val.model_repo.to_string(),
            model_file: val.model_file.to_string(),
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as i64,
            description: val.description.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsSsrfPolicy> for crawlberg::SsrfPolicy {
    fn from(val: JsSsrfPolicy) -> Self {
        let mut __result = crawlberg::SsrfPolicy::default();
        if let Some(__v) = val.deny_private {
            __result.deny_private = __v;
        }
        if let Some(__v) = val.allowlist {
            __result.allowlist = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.max_redirects {
            __result.max_redirects = __v;
        }
        if let Some(__v) = val.scheme_allowlist {
            __result.scheme_allowlist = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<crawlberg::SsrfPolicy> for JsSsrfPolicy {
    fn from(val: crawlberg::SsrfPolicy) -> Self {
        Self {
            deny_private: Some(val.deny_private),
            allowlist: Some(val.allowlist.into_iter().map(Into::into).collect()),
            max_redirects: Some(val.max_redirects),
            scheme_allowlist: Some(val.scheme_allowlist.into_iter().collect()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsStructuredData> for xberg::StructuredData {
    fn from(val: JsStructuredData) -> Self {
        Self {
            data_type: val.data_type.into(),
            raw_json: val.raw_json,
            schema_type: val.schema_type,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::StructuredData> for JsStructuredData {
    fn from(val: xberg::StructuredData) -> Self {
        Self {
            data_type: val.data_type.into(),
            raw_json: val.raw_json.to_string(),
            schema_type: val.schema_type.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::extraction::structured::StructuredDataResult> for JsStructuredDataResult {
    fn from(val: xberg::extraction::structured::StructuredDataResult) -> Self {
        Self {
            content: val.content.to_string(),
            format: val.format.to_string(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            text_fields: val.text_fields.into_iter().collect(),
            value: val.value,
            flattened: val.flattened.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsStructuredExtractionConfig> for xberg::StructuredExtractionConfig {
    fn from(val: JsStructuredExtractionConfig) -> Self {
        Self {
            schema: val.schema,
            schema_name: val.schema_name,
            schema_description: val.schema_description,
            strict: val.strict,
            prompt: val.prompt,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::StructuredExtractionConfig> for JsStructuredExtractionConfig {
    fn from(val: xberg::StructuredExtractionConfig) -> Self {
        Self {
            schema: val.schema,
            schema_name: val.schema_name.to_string(),
            schema_description: val.schema_description.map(|v| v.to_string()),
            strict: val.strict,
            prompt: val.prompt.map(|v| v.to_string()),
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsSummarizationConfig> for xberg::SummarizationConfig {
    fn from(val: JsSummarizationConfig) -> Self {
        let mut __result = xberg::SummarizationConfig::default();
        if let Some(__v) = val.strategy {
            __result.strategy = __v.into();
        }
        __result.max_tokens = val.max_tokens;
        __result.llm = val.llm.map(Into::into);
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::SummarizationConfig> for JsSummarizationConfig {
    fn from(val: xberg::SummarizationConfig) -> Self {
        Self {
            strategy: Some(val.strategy.into()),
            max_tokens: val.max_tokens,
            llm: val.llm.map(Into::into),
        }
    }
}

impl From<JsSupportedFormat> for xberg::SupportedFormat {
    fn from(val: JsSupportedFormat) -> Self {
        Self {
            extension: val.extension,
            mime_type: val.mime_type,
        }
    }
}

impl From<xberg::SupportedFormat> for JsSupportedFormat {
    fn from(val: xberg::SupportedFormat) -> Self {
        Self {
            extension: val.extension.to_string(),
            mime_type: val.mime_type.to_string(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsSvgOptions> for xberg::core::config::extraction::SvgOptions {
    fn from(val: JsSvgOptions) -> Self {
        let mut __result = xberg::core::config::extraction::SvgOptions::default();
        if let Some(__v) = val.sanitize {
            __result.sanitize = __v;
        }
        if let Some(__v) = val.render_dpi {
            __result.render_dpi = __v as f32;
        }
        __result
    }
}

impl From<xberg::core::config::extraction::SvgOptions> for JsSvgOptions {
    fn from(val: xberg::core::config::extraction::SvgOptions) -> Self {
        Self {
            sanitize: Some(val.sanitize),
            render_dpi: Some(val.render_dpi as f64),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTable> for xberg::Table {
    fn from(val: JsTable) -> Self {
        let mut __result = xberg::Table::default();
        if let Some(__v) = val.cells {
            __result.cells = __v.into_iter().collect();
        }
        if let Some(__v) = val.markdown {
            __result.markdown = __v;
        }
        if let Some(__v) = val.page_number {
            __result.page_number = __v;
        }
        __result.bounding_box = val.bounding_box.map(Into::into);
        __result.table_id = val.table_id;
        if let Some(__v) = val.cell_styles {
            __result.cell_styles = __v.into_iter().map(Into::into).collect();
        }
        __result.columns = val.columns.map(|v| v.into_iter().collect());
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::Table> for JsTable {
    fn from(val: xberg::Table) -> Self {
        Self {
            cells: Some(val.cells.into_iter().collect()),
            markdown: Some(val.markdown.to_string()),
            page_number: Some(val.page_number),
            bounding_box: val.bounding_box.map(Into::into),
            table_id: val.table_id.map(|v| v.to_string()),
            cell_styles: Some(val.cell_styles.into_iter().map(Into::into).collect()),
            columns: val.columns.map(|v| v.into_iter().collect()),
        }
    }
}

impl From<xberg::TableCell> for JsTableCell {
    fn from(val: xberg::TableCell) -> Self {
        Self {
            content: Some(val.content.to_string()),
            row_span: Some(val.row_span),
            col_span: Some(val.col_span),
            is_header: Some(val.is_header),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsTableCellStyle> for xberg::TableCellStyle {
    fn from(val: JsTableCellStyle) -> Self {
        let mut __result = xberg::TableCellStyle::default();
        if let Some(__v) = val.row {
            __result.row = __v;
        }
        if let Some(__v) = val.col {
            __result.col = __v;
        }
        __result.heading_level = val.heading_level;
        __result.style_name = val.style_name;
        __result
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::TableCellStyle> for JsTableCellStyle {
    fn from(val: xberg::TableCellStyle) -> Self {
        Self {
            row: Some(val.row),
            col: Some(val.col),
            heading_level: val.heading_level,
            style_name: val.style_name.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TableDiff> for JsTableDiff {
    fn from(val: xberg::TableDiff) -> Self {
        Self {
            from_index: val.from_index as i64,
            to_index: val.to_index as i64,
            cell_changes: val.cell_changes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsTableGrid> for xberg::TableGrid {
    fn from(val: JsTableGrid) -> Self {
        let mut __result = xberg::TableGrid::default();
        if let Some(__v) = val.rows {
            __result.rows = __v;
        }
        if let Some(__v) = val.cols {
            __result.cols = __v;
        }
        if let Some(__v) = val.cells {
            __result.cells = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TableGrid> for JsTableGrid {
    fn from(val: xberg::TableGrid) -> Self {
        Self {
            rows: Some(val.rows),
            cols: Some(val.cols),
            cells: Some(val.cells.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsTesseractConfig> for xberg::TesseractConfig {
    fn from(val: JsTesseractConfig) -> Self {
        let mut __result = xberg::TesseractConfig::default();
        if let Some(__v) = val.language {
            __result.language = __v.into_iter().collect();
        }
        __result.psm = val.psm;
        if let Some(__v) = val.output_format {
            __result.output_format = __v;
        }
        if let Some(__v) = val.oem {
            __result.oem = __v;
        }
        if let Some(__v) = val.min_confidence {
            __result.min_confidence = __v;
        }
        __result.preprocessing = val.preprocessing.map(Into::into);
        if let Some(__v) = val.enable_table_detection {
            __result.enable_table_detection = __v;
        }
        if let Some(__v) = val.table_min_confidence {
            __result.table_min_confidence = __v;
        }
        if let Some(__v) = val.table_column_threshold {
            __result.table_column_threshold = __v;
        }
        if let Some(__v) = val.table_row_threshold_ratio {
            __result.table_row_threshold_ratio = __v;
        }
        if let Some(__v) = val.use_cache {
            __result.use_cache = __v;
        }
        if let Some(__v) = val.classify_use_pre_adapted_templates {
            __result.classify_use_pre_adapted_templates = __v;
        }
        if let Some(__v) = val.language_model_ngram_on {
            __result.language_model_ngram_on = __v;
        }
        if let Some(__v) = val.tessedit_dont_blkrej_good_wds {
            __result.tessedit_dont_blkrej_good_wds = __v;
        }
        if let Some(__v) = val.tessedit_dont_rowrej_good_wds {
            __result.tessedit_dont_rowrej_good_wds = __v;
        }
        if let Some(__v) = val.tessedit_enable_dict_correction {
            __result.tessedit_enable_dict_correction = __v;
        }
        if let Some(__v) = val.tessedit_char_whitelist {
            __result.tessedit_char_whitelist = __v;
        }
        if let Some(__v) = val.tessedit_char_blacklist {
            __result.tessedit_char_blacklist = __v;
        }
        if let Some(__v) = val.tessedit_use_primary_params_model {
            __result.tessedit_use_primary_params_model = __v;
        }
        if let Some(__v) = val.textord_space_size_is_variable {
            __result.textord_space_size_is_variable = __v;
        }
        if let Some(__v) = val.thresholding_method {
            __result.thresholding_method = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TesseractConfig> for JsTesseractConfig {
    fn from(val: xberg::TesseractConfig) -> Self {
        Self {
            language: Some(val.language.into_iter().collect()),
            psm: val.psm,
            output_format: Some(val.output_format.to_string()),
            oem: Some(val.oem),
            min_confidence: Some(val.min_confidence),
            preprocessing: val.preprocessing.map(Into::into),
            enable_table_detection: Some(val.enable_table_detection),
            table_min_confidence: Some(val.table_min_confidence),
            table_column_threshold: Some(val.table_column_threshold),
            table_row_threshold_ratio: Some(val.table_row_threshold_ratio),
            use_cache: Some(val.use_cache),
            classify_use_pre_adapted_templates: Some(val.classify_use_pre_adapted_templates),
            language_model_ngram_on: Some(val.language_model_ngram_on),
            tessedit_dont_blkrej_good_wds: Some(val.tessedit_dont_blkrej_good_wds),
            tessedit_dont_rowrej_good_wds: Some(val.tessedit_dont_rowrej_good_wds),
            tessedit_enable_dict_correction: Some(val.tessedit_enable_dict_correction),
            tessedit_char_whitelist: Some(val.tessedit_char_whitelist.to_string()),
            tessedit_char_blacklist: Some(val.tessedit_char_blacklist.to_string()),
            tessedit_use_primary_params_model: Some(val.tessedit_use_primary_params_model),
            textord_space_size_is_variable: Some(val.textord_space_size_is_variable),
            thresholding_method: Some(val.thresholding_method),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsTextAnnotation> for xberg::TextAnnotation {
    fn from(val: JsTextAnnotation) -> Self {
        Self {
            start: val.start,
            end: val.end,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<xberg::TextAnnotation> for JsTextAnnotation {
    fn from(val: xberg::TextAnnotation) -> Self {
        Self {
            start: val.start,
            end: val.end,
            kind: val.kind.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TextExtractionResult> for JsTextExtractionResult {
    fn from(val: xberg::TextExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            line_count: val.line_count as i64,
            word_count: val.word_count as i64,
            character_count: val.character_count as i64,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTextMetadata> for xberg::TextMetadata {
    fn from(val: JsTextMetadata) -> Self {
        let mut __result = xberg::TextMetadata::default();
        if let Some(__v) = val.line_count {
            __result.line_count = __v;
        }
        if let Some(__v) = val.word_count {
            __result.word_count = __v;
        }
        if let Some(__v) = val.character_count {
            __result.character_count = __v;
        }
        __result.headers = val.headers.map(|v| v.into_iter().collect());
        __result.links = val.links.map(|v| v.into_iter().map(Into::into).collect());
        __result.code_blocks = val.code_blocks.map(|v| v.into_iter().map(Into::into).collect());
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TextMetadata> for JsTextMetadata {
    fn from(val: xberg::TextMetadata) -> Self {
        Self {
            line_count: Some(val.line_count),
            word_count: Some(val.word_count),
            character_count: Some(val.character_count),
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTokenReductionConfig> for xberg::TokenReductionConfig {
    fn from(val: JsTokenReductionConfig) -> Self {
        let mut __result = xberg::TokenReductionConfig::default();
        if let Some(__v) = val.level {
            __result.level = __v.into();
        }
        __result.language_hint = val.language_hint;
        if let Some(__v) = val.preserve_markdown {
            __result.preserve_markdown = __v;
        }
        if let Some(__v) = val.preserve_code {
            __result.preserve_code = __v;
        }
        if let Some(__v) = val.semantic_threshold {
            __result.semantic_threshold = __v as f32;
        }
        if let Some(__v) = val.enable_parallel {
            __result.enable_parallel = __v;
        }
        if let Some(__v) = val.use_simd {
            __result.use_simd = __v;
        }
        __result.custom_stopwords = val.custom_stopwords.map(|m| m.into_iter().collect());
        if let Some(__v) = val.preserve_patterns {
            __result.preserve_patterns = __v.into_iter().collect();
        }
        __result.target_reduction = val.target_reduction.map(|v| v as f32);
        if let Some(__v) = val.enable_semantic_clustering {
            __result.enable_semantic_clustering = __v;
        }
        if let Some(__v) = val.preserve_important_words {
            __result.preserve_important_words = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TokenReductionConfig> for JsTokenReductionConfig {
    fn from(val: xberg::TokenReductionConfig) -> Self {
        Self {
            level: Some(val.level.into()),
            language_hint: val.language_hint.map(|v| v.to_string()),
            preserve_markdown: Some(val.preserve_markdown),
            preserve_code: Some(val.preserve_code),
            semantic_threshold: Some(val.semantic_threshold as f64),
            enable_parallel: Some(val.enable_parallel),
            use_simd: Some(val.use_simd),
            custom_stopwords: val.custom_stopwords.map(|m| m.into_iter().collect()),
            preserve_patterns: Some(val.preserve_patterns.into_iter().collect()),
            target_reduction: val.target_reduction.map(|v| v as f64),
            enable_semantic_clustering: Some(val.enable_semantic_clustering),
            preserve_important_words: Some(val.preserve_important_words),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsTokenReductionOptions> for xberg::TokenReductionOptions {
    fn from(val: JsTokenReductionOptions) -> Self {
        let mut __result = xberg::TokenReductionOptions::default();
        if let Some(__v) = val.mode {
            __result.mode = __v;
        }
        if let Some(__v) = val.preserve_important_words {
            __result.preserve_important_words = __v;
        }
        __result
    }
}

impl From<xberg::TokenReductionOptions> for JsTokenReductionOptions {
    fn from(val: xberg::TokenReductionOptions) -> Self {
        Self {
            mode: Some(val.mode.to_string()),
            preserve_important_words: Some(val.preserve_important_words),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTranscriptionConfig> for xberg::TranscriptionConfig {
    fn from(val: JsTranscriptionConfig) -> Self {
        let mut __result = xberg::TranscriptionConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        if let Some(__v) = val.model {
            __result.model = __v.into();
        }
        __result.language = val.language;
        if let Some(__v) = val.timestamps {
            __result.timestamps = __v;
        }
        __result.max_duration_ms = val.max_duration_ms.map(|v| v as u64);
        __result.max_bytes = val.max_bytes.map(|v| v as u64);
        __result.timeout_ms = val.timeout_ms.map(|v| v as u64);
        __result.model_cache_dir = val.model_cache_dir.map(Into::into);
        if let Some(__v) = val.allow_network {
            __result.allow_network = __v;
        }
        if let Some(__v) = val.verify_hash {
            __result.verify_hash = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TranscriptionConfig> for JsTranscriptionConfig {
    fn from(val: xberg::TranscriptionConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            model: Some(val.model.into()),
            language: val.language.map(|v| v.to_string()),
            timestamps: Some(val.timestamps),
            max_duration_ms: val.max_duration_ms.map(|v| v as i64),
            max_bytes: val.max_bytes.map(|v| v as i64),
            timeout_ms: val.timeout_ms.map(|v| v as i64),
            model_cache_dir: val.model_cache_dir.map(|p| p.to_string_lossy().to_string()),
            allow_network: Some(val.allow_network),
            verify_hash: Some(val.verify_hash),
        }
    }
}

impl From<JsTranslation> for xberg::Translation {
    fn from(val: JsTranslation) -> Self {
        Self {
            target_lang: val.target_lang,
            source_lang: val.source_lang,
            content: val.content,
            formatted_content: val.formatted_content,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::Translation> for JsTranslation {
    fn from(val: xberg::Translation) -> Self {
        Self {
            target_lang: val.target_lang.to_string(),
            source_lang: val.source_lang.map(|v| v.to_string()),
            content: val.content.to_string(),
            formatted_content: val.formatted_content.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsTranslationConfig> for xberg::TranslationConfig {
    fn from(val: JsTranslationConfig) -> Self {
        Self {
            target_lang: val.target_lang,
            source_lang: val.source_lang,
            preserve_markup: val.preserve_markup,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TranslationConfig> for JsTranslationConfig {
    fn from(val: xberg::TranslationConfig) -> Self {
        Self {
            target_lang: val.target_lang.to_string(),
            source_lang: val.source_lang.map(|v| v.to_string()),
            preserve_markup: val.preserve_markup,
            llm: val.llm.into(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTreeSitterConfig> for xberg::TreeSitterConfig {
    fn from(val: JsTreeSitterConfig) -> Self {
        let mut __result = xberg::TreeSitterConfig::default();
        if let Some(__v) = val.enabled {
            __result.enabled = __v;
        }
        __result.cache_dir = val.cache_dir.map(Into::into);
        __result.languages = val.languages.map(|v| v.into_iter().collect());
        __result.groups = val.groups.map(|v| v.into_iter().collect());
        if let Some(__v) = val.process {
            __result.process = __v.into();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TreeSitterConfig> for JsTreeSitterConfig {
    fn from(val: xberg::TreeSitterConfig) -> Self {
        Self {
            enabled: Some(val.enabled),
            cache_dir: val.cache_dir.map(|p| p.to_string_lossy().to_string()),
            languages: val.languages.map(|v| v.into_iter().collect()),
            groups: val.groups.map(|v| v.into_iter().collect()),
            process: Some(val.process.into()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTreeSitterProcessConfig> for xberg::TreeSitterProcessConfig {
    fn from(val: JsTreeSitterProcessConfig) -> Self {
        let mut __result = xberg::TreeSitterProcessConfig::default();
        if let Some(__v) = val.structure {
            __result.structure = __v;
        }
        if let Some(__v) = val.imports {
            __result.imports = __v;
        }
        if let Some(__v) = val.exports {
            __result.exports = __v;
        }
        if let Some(__v) = val.comments {
            __result.comments = __v;
        }
        if let Some(__v) = val.docstrings {
            __result.docstrings = __v;
        }
        if let Some(__v) = val.symbols {
            __result.symbols = __v;
        }
        if let Some(__v) = val.diagnostics {
            __result.diagnostics = __v;
        }
        if let Some(__v) = val.data_extraction {
            __result.data_extraction = __v;
        }
        __result.chunk_max_size = val.chunk_max_size.map(|v| v as usize);
        if let Some(__v) = val.content_mode {
            __result.content_mode = __v.into();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::TreeSitterProcessConfig> for JsTreeSitterProcessConfig {
    fn from(val: xberg::TreeSitterProcessConfig) -> Self {
        Self {
            structure: Some(val.structure),
            imports: Some(val.imports),
            exports: Some(val.exports),
            comments: Some(val.comments),
            docstrings: Some(val.docstrings),
            symbols: Some(val.symbols),
            diagnostics: Some(val.diagnostics),
            data_extraction: Some(val.data_extraction),
            chunk_max_size: val.chunk_max_size.map(|v| v as i64),
            content_mode: Some(val.content_mode.into()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::candle_ocr::TrocrBackendOptions> for JsTrocrBackendOptions {
    fn from(val: xberg::candle_ocr::TrocrBackendOptions) -> Self {
        Self {
            variant: val.variant.map(Into::into),
            device: val.device.map(Into::into),
            cache_dir: val.cache_dir.map(|v| v.to_string()),
            hf_revision: val.hf_revision.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsUrlExtractionConfig> for xberg::UrlExtractionConfig {
    fn from(val: JsUrlExtractionConfig) -> Self {
        let mut __result = xberg::UrlExtractionConfig::default();
        if let Some(__v) = val.mode {
            __result.mode = __v.into();
        }
        #[cfg(any(feature = "url-ingestion", feature = "url-config-types"))]
        {
            if let Some(__v) = val.crawl {
                __result.crawl = __v.into();
            }
        }
        __result.document_url_pattern = val.document_url_pattern;
        __result.max_document_urls_per_result = val.max_document_urls_per_result;
        __result.max_total_urls = val.max_total_urls;
        if let Some(__v) = val.allow_local_file_inputs {
            __result.allow_local_file_inputs = __v;
        }
        if let Some(__v) = val.allow_file_uris {
            __result.allow_file_uris = __v;
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::UrlExtractionConfig> for JsUrlExtractionConfig {
    fn from(val: xberg::UrlExtractionConfig) -> Self {
        Self {
            mode: Some(val.mode.into()),
            #[cfg(any(feature = "url-ingestion", feature = "url-config-types"))]
            crawl: Some(val.crawl.into()),
            document_url_pattern: val.document_url_pattern.map(|v| v.to_string()),
            max_document_urls_per_result: val.max_document_urls_per_result,
            max_total_urls: val.max_total_urls,
            allow_local_file_inputs: Some(val.allow_local_file_inputs),
            allow_file_uris: Some(val.allow_file_uris),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<xberg::UserChunkConfig> for JsUserChunkConfig {
    fn from(val: xberg::UserChunkConfig) -> Self {
        Self {
            page_ranges: val.page_ranges.map(|v| v.into_iter().map(Into::into).collect()),
            pages_per_chunk: val.pages_per_chunk,
            force_chunking: Some(val.force_chunking),
            disable_chunking: Some(val.disable_chunking),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<xberg::extraction::office_metadata::app_properties::XlsxAppProperties> for JsXlsxAppProperties {
    fn from(val: xberg::extraction::office_metadata::app_properties::XlsxAppProperties) -> Self {
        Self {
            application: val.application.map(|v| v.to_string()),
            app_version: val.app_version.map(|v| v.to_string()),
            doc_security: val.doc_security,
            scale_crop: val.scale_crop,
            links_up_to_date: val.links_up_to_date,
            shared_doc: val.shared_doc,
            hyperlinks_changed: val.hyperlinks_changed,
            company: val.company.map(|v| v.to_string()),
            worksheet_names: Some(val.worksheet_names.into_iter().collect()),
        }
    }
}

impl From<xberg::XmlExtractionResult> for JsXmlExtractionResult {
    fn from(val: xberg::XmlExtractionResult) -> Self {
        Self {
            content: val.content.to_string(),
            element_count: val.element_count as i64,
            unique_elements: val.unique_elements.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsXmlMetadata> for xberg::XmlMetadata {
    fn from(val: JsXmlMetadata) -> Self {
        let mut __result = xberg::XmlMetadata::default();
        if let Some(__v) = val.element_count {
            __result.element_count = __v;
        }
        if let Some(__v) = val.unique_elements {
            __result.unique_elements = __v.into_iter().collect();
        }
        __result
    }
}

impl From<xberg::XmlMetadata> for JsXmlMetadata {
    fn from(val: xberg::XmlMetadata) -> Self {
        Self {
            element_count: Some(val.element_count),
            unique_elements: Some(val.unique_elements.into_iter().collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsYakeParams> for xberg::YakeParams {
    fn from(val: JsYakeParams) -> Self {
        let mut __result = xberg::YakeParams::default();
        if let Some(__v) = val.window_size {
            __result.window_size = __v as usize;
        }
        __result
    }
}

impl From<xberg::YakeParams> for JsYakeParams {
    fn from(val: xberg::YakeParams) -> Self {
        Self {
            window_size: Some(val.window_size as i64),
        }
    }
}

impl From<JsYearRange> for xberg::YearRange {
    fn from(val: JsYearRange) -> Self {
        Self {
            min: val.min,
            max: val.max,
            years: val.years.into_iter().collect(),
        }
    }
}

impl From<xberg::YearRange> for JsYearRange {
    fn from(val: xberg::YearRange) -> Self {
        Self {
            min: val.min,
            max: val.max,
            years: val.years.into_iter().collect(),
        }
    }
}

impl From<JsAnnotationKind> for xberg::AnnotationKind {
    fn from(val: JsAnnotationKind) -> Self {
        match val.annotation_type_tag.as_str() {
            "bold" => Self::Bold,
            "italic" => Self::Italic,
            "underline" => Self::Underline,
            "strikethrough" => Self::Strikethrough,
            "code" => Self::Code,
            "subscript" => Self::Subscript,
            "superscript" => Self::Superscript,
            "link" => Self::Link {
                url: val.url.unwrap_or_default(),
                title: val.title,
            },
            "highlight" => Self::Highlight,
            "color" => Self::Color {
                value: val.value.unwrap_or_default(),
            },
            "font_size" => Self::FontSize {
                value: val.value.unwrap_or_default(),
            },
            "custom" => Self::Custom {
                name: val.name.unwrap_or_default(),
                value: val.value,
            },
            _ => Self::Bold,
        }
    }
}

impl From<xberg::AnnotationKind> for JsAnnotationKind {
    fn from(val: xberg::AnnotationKind) -> Self {
        match val {
            xberg::AnnotationKind::Bold => Self {
                annotation_type_tag: "bold".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Italic => Self {
                annotation_type_tag: "italic".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Underline => Self {
                annotation_type_tag: "underline".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Strikethrough => Self {
                annotation_type_tag: "strikethrough".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Code => Self {
                annotation_type_tag: "code".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Subscript => Self {
                annotation_type_tag: "subscript".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Superscript => Self {
                annotation_type_tag: "superscript".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Link { url, title } => Self {
                annotation_type_tag: "link".to_string(),
                name: None,
                title,
                url: Some(url),
                value: None,
            },
            xberg::AnnotationKind::Highlight => Self {
                annotation_type_tag: "highlight".to_string(),
                name: None,
                title: None,
                url: None,
                value: None,
            },
            xberg::AnnotationKind::Color { value } => Self {
                annotation_type_tag: "color".to_string(),
                name: None,
                title: None,
                url: None,
                value: Some(value),
            },
            xberg::AnnotationKind::FontSize { value } => Self {
                annotation_type_tag: "font_size".to_string(),
                name: None,
                title: None,
                url: None,
                value: Some(value),
            },
            xberg::AnnotationKind::Custom { name, value } => Self {
                annotation_type_tag: "custom".to_string(),
                name: Some(name),
                title: None,
                url: None,
                value,
            },
        }
    }
}

impl From<JsAssetCategory> for crawlberg::AssetCategory {
    fn from(val: JsAssetCategory) -> Self {
        match val {
            JsAssetCategory::Document => Self::Document,
            JsAssetCategory::Image => Self::Image,
            JsAssetCategory::Audio => Self::Audio,
            JsAssetCategory::Video => Self::Video,
            JsAssetCategory::Font => Self::Font,
            JsAssetCategory::Stylesheet => Self::Stylesheet,
            JsAssetCategory::Script => Self::Script,
            JsAssetCategory::Archive => Self::Archive,
            JsAssetCategory::Data => Self::Data,
            JsAssetCategory::Other => Self::Other,
        }
    }
}

impl From<crawlberg::AssetCategory> for JsAssetCategory {
    fn from(val: crawlberg::AssetCategory) -> Self {
        match val {
            crawlberg::AssetCategory::Document => Self::Document,
            crawlberg::AssetCategory::Image => Self::Image,
            crawlberg::AssetCategory::Audio => Self::Audio,
            crawlberg::AssetCategory::Video => Self::Video,
            crawlberg::AssetCategory::Font => Self::Font,
            crawlberg::AssetCategory::Stylesheet => Self::Stylesheet,
            crawlberg::AssetCategory::Script => Self::Script,
            crawlberg::AssetCategory::Archive => Self::Archive,
            crawlberg::AssetCategory::Data => Self::Data,
            crawlberg::AssetCategory::Other => Self::Other,
        }
    }
}

impl From<JsAuthConfig> for crawlberg::AuthConfig {
    fn from(val: JsAuthConfig) -> Self {
        match val.type_tag.as_str() {
            "basic" => Self::Basic {
                username: val.username.unwrap_or_default(),
                password: val.password.unwrap_or_default(),
            },
            "bearer" => Self::Bearer {
                token: val.token.unwrap_or_default(),
            },
            "header" => Self::Header {
                name: val.name.unwrap_or_default(),
                value: val.value.unwrap_or_default(),
            },
            _ => Self::Basic {
                username: Default::default(),
                password: Default::default(),
            },
        }
    }
}

impl From<crawlberg::AuthConfig> for JsAuthConfig {
    fn from(val: crawlberg::AuthConfig) -> Self {
        match val {
            crawlberg::AuthConfig::Basic { username, password } => Self {
                type_tag: "basic".to_string(),
                name: None,
                password: Some(password),
                token: None,
                username: Some(username),
                value: None,
            },
            crawlberg::AuthConfig::Bearer { token } => Self {
                type_tag: "bearer".to_string(),
                name: None,
                password: None,
                token: Some(token),
                username: None,
                value: None,
            },
            crawlberg::AuthConfig::Header { name, value } => Self {
                type_tag: "header".to_string(),
                name: Some(name),
                password: None,
                token: None,
                username: None,
                value: Some(value),
            },
        }
    }
}

impl From<JsBlockType> for xberg::BlockType {
    fn from(val: JsBlockType) -> Self {
        match val {
            JsBlockType::Paragraph => Self::Paragraph,
            JsBlockType::Heading => Self::Heading,
            JsBlockType::Blockquote => Self::Blockquote,
            JsBlockType::CodeBlock => Self::CodeBlock,
            JsBlockType::ListItem => Self::ListItem,
            JsBlockType::OrderedList => Self::OrderedList,
            JsBlockType::BulletList => Self::BulletList,
            JsBlockType::TaskList => Self::TaskList,
            JsBlockType::DefinitionList => Self::DefinitionList,
            JsBlockType::DefinitionTerm => Self::DefinitionTerm,
            JsBlockType::DefinitionDescription => Self::DefinitionDescription,
            JsBlockType::Div => Self::Div,
            JsBlockType::Section => Self::Section,
            JsBlockType::ThematicBreak => Self::ThematicBreak,
            JsBlockType::RawBlock => Self::RawBlock,
            JsBlockType::MathDisplay => Self::MathDisplay,
        }
    }
}

impl From<xberg::BlockType> for JsBlockType {
    fn from(val: xberg::BlockType) -> Self {
        match val {
            xberg::BlockType::Paragraph => Self::Paragraph,
            xberg::BlockType::Heading => Self::Heading,
            xberg::BlockType::Blockquote => Self::Blockquote,
            xberg::BlockType::CodeBlock => Self::CodeBlock,
            xberg::BlockType::ListItem => Self::ListItem,
            xberg::BlockType::OrderedList => Self::OrderedList,
            xberg::BlockType::BulletList => Self::BulletList,
            xberg::BlockType::TaskList => Self::TaskList,
            xberg::BlockType::DefinitionList => Self::DefinitionList,
            xberg::BlockType::DefinitionTerm => Self::DefinitionTerm,
            xberg::BlockType::DefinitionDescription => Self::DefinitionDescription,
            xberg::BlockType::Div => Self::Div,
            xberg::BlockType::Section => Self::Section,
            xberg::BlockType::ThematicBreak => Self::ThematicBreak,
            xberg::BlockType::RawBlock => Self::RawBlock,
            xberg::BlockType::MathDisplay => Self::MathDisplay,
        }
    }
}

impl From<xberg::BoundaryReason> for JsBoundaryReason {
    fn from(val: xberg::BoundaryReason) -> Self {
        match val {
            xberg::BoundaryReason::Start => Self::Start,
            xberg::BoundaryReason::PageOneMarker => Self::PageOneMarker,
            xberg::BoundaryReason::LetterheadReset => Self::LetterheadReset,
            xberg::BoundaryReason::DensityShift => Self::DensityShift,
            xberg::BoundaryReason::End => Self::End,
        }
    }
}

impl From<JsBrowserBackend> for crawlberg::BrowserBackend {
    fn from(val: JsBrowserBackend) -> Self {
        match val {
            JsBrowserBackend::Chromiumoxide => Self::Chromiumoxide,
            JsBrowserBackend::Native => Self::Native,
        }
    }
}

impl From<crawlberg::BrowserBackend> for JsBrowserBackend {
    fn from(val: crawlberg::BrowserBackend) -> Self {
        match val {
            crawlberg::BrowserBackend::Chromiumoxide => Self::Chromiumoxide,
            crawlberg::BrowserBackend::Native => Self::Native,
        }
    }
}

impl From<JsBrowserMode> for crawlberg::BrowserMode {
    fn from(val: JsBrowserMode) -> Self {
        match val {
            JsBrowserMode::Auto => Self::Auto,
            JsBrowserMode::Always => Self::Always,
            JsBrowserMode::Never => Self::Never,
            JsBrowserMode::Stealth => Self::Stealth,
        }
    }
}

impl From<crawlberg::BrowserMode> for JsBrowserMode {
    fn from(val: crawlberg::BrowserMode) -> Self {
        match val {
            crawlberg::BrowserMode::Auto => Self::Auto,
            crawlberg::BrowserMode::Always => Self::Always,
            crawlberg::BrowserMode::Never => Self::Never,
            crawlberg::BrowserMode::Stealth => Self::Stealth,
        }
    }
}

impl From<JsBrowserWait> for crawlberg::BrowserWait {
    fn from(val: JsBrowserWait) -> Self {
        match val {
            JsBrowserWait::NetworkIdle => Self::NetworkIdle,
            JsBrowserWait::Selector => Self::Selector,
            JsBrowserWait::Fixed => Self::Fixed,
        }
    }
}

impl From<crawlberg::BrowserWait> for JsBrowserWait {
    fn from(val: crawlberg::BrowserWait) -> Self {
        match val {
            crawlberg::BrowserWait::NetworkIdle => Self::NetworkIdle,
            crawlberg::BrowserWait::Selector => Self::Selector,
            crawlberg::BrowserWait::Fixed => Self::Fixed,
        }
    }
}

impl From<JsCallMode> for xberg::CallMode {
    fn from(val: JsCallMode) -> Self {
        match val {
            JsCallMode::TextOnly => Self::TextOnly,
            JsCallMode::VisionOnly => Self::VisionOnly,
            JsCallMode::TextPlusVision => Self::TextPlusVision,
        }
    }
}

impl From<xberg::CallMode> for JsCallMode {
    fn from(val: xberg::CallMode) -> Self {
        match val {
            xberg::CallMode::TextOnly => Self::TextOnly,
            xberg::CallMode::VisionOnly => Self::VisionOnly,
            xberg::CallMode::TextPlusVision => Self::TextPlusVision,
        }
    }
}

impl From<xberg::candle_ocr::CandleDevicePreference> for JsCandleDevicePreference {
    fn from(val: xberg::candle_ocr::CandleDevicePreference) -> Self {
        match val {
            xberg::candle_ocr::CandleDevicePreference::Auto => Self::Auto,
            xberg::candle_ocr::CandleDevicePreference::Cpu => Self::Cpu,
            xberg::candle_ocr::CandleDevicePreference::Cuda => Self::Cuda,
            xberg::candle_ocr::CandleDevicePreference::Metal => Self::Metal,
        }
    }
}

impl From<xberg::candle_ocr::CandleTrocrVariant> for JsCandleTrocrVariant {
    fn from(val: xberg::candle_ocr::CandleTrocrVariant) -> Self {
        match val {
            xberg::candle_ocr::CandleTrocrVariant::BasePrinted => Self::BasePrinted,
            xberg::candle_ocr::CandleTrocrVariant::LargePrinted => Self::LargePrinted,
            xberg::candle_ocr::CandleTrocrVariant::BaseHandwritten => Self::BaseHandwritten,
            xberg::candle_ocr::CandleTrocrVariant::LargeHandwritten => Self::LargeHandwritten,
        }
    }
}

impl From<JsChunkSizing> for xberg::ChunkSizing {
    fn from(val: JsChunkSizing) -> Self {
        match val.type_tag.as_str() {
            "characters" => Self::Characters,
            #[cfg(feature = "chunking-tokenizers")]
            "tokenizer" => Self::Tokenizer {
                model: val.model.unwrap_or_default(),
                cache_dir: val.cache_dir.map(std::path::PathBuf::from),
            },
            _ => Self::Characters,
        }
    }
}

impl From<xberg::ChunkSizing> for JsChunkSizing {
    fn from(val: xberg::ChunkSizing) -> Self {
        match val {
            xberg::ChunkSizing::Characters => Self {
                type_tag: "characters".to_string(),
                cache_dir: None,
                model: None,
            },
            #[cfg(feature = "chunking-tokenizers")]
            xberg::ChunkSizing::Tokenizer { model, cache_dir } => Self {
                type_tag: "tokenizer".to_string(),
                cache_dir: cache_dir.map(|p| p.to_string_lossy().to_string()),
                model: Some(model),
            },
        }
    }
}

impl From<JsChunkType> for xberg::ChunkType {
    fn from(val: JsChunkType) -> Self {
        match val {
            JsChunkType::Heading => Self::Heading,
            JsChunkType::PartyList => Self::PartyList,
            JsChunkType::Definitions => Self::Definitions,
            JsChunkType::OperativeClause => Self::OperativeClause,
            JsChunkType::SignatureBlock => Self::SignatureBlock,
            JsChunkType::Schedule => Self::Schedule,
            JsChunkType::TableLike => Self::TableLike,
            JsChunkType::Formula => Self::Formula,
            JsChunkType::CodeBlock => Self::CodeBlock,
            JsChunkType::Function => Self::Function,
            JsChunkType::Class => Self::Class,
            JsChunkType::Module => Self::Module,
            JsChunkType::Image => Self::Image,
            JsChunkType::OrgChart => Self::OrgChart,
            JsChunkType::Diagram => Self::Diagram,
            JsChunkType::Unknown => Self::Unknown,
        }
    }
}

impl From<xberg::ChunkType> for JsChunkType {
    fn from(val: xberg::ChunkType) -> Self {
        match val {
            xberg::ChunkType::Heading => Self::Heading,
            xberg::ChunkType::PartyList => Self::PartyList,
            xberg::ChunkType::Definitions => Self::Definitions,
            xberg::ChunkType::OperativeClause => Self::OperativeClause,
            xberg::ChunkType::SignatureBlock => Self::SignatureBlock,
            xberg::ChunkType::Schedule => Self::Schedule,
            xberg::ChunkType::TableLike => Self::TableLike,
            xberg::ChunkType::Formula => Self::Formula,
            xberg::ChunkType::CodeBlock => Self::CodeBlock,
            xberg::ChunkType::Function => Self::Function,
            xberg::ChunkType::Class => Self::Class,
            xberg::ChunkType::Module => Self::Module,
            xberg::ChunkType::Image => Self::Image,
            xberg::ChunkType::OrgChart => Self::OrgChart,
            xberg::ChunkType::Diagram => Self::Diagram,
            xberg::ChunkType::Unknown => Self::Unknown,
        }
    }
}

impl From<JsChunkerType> for xberg::ChunkerType {
    fn from(val: JsChunkerType) -> Self {
        match val {
            JsChunkerType::Text => Self::Text,
            JsChunkerType::Markdown => Self::Markdown,
            JsChunkerType::Yaml => Self::Yaml,
            JsChunkerType::Semantic => Self::Semantic,
        }
    }
}

impl From<xberg::ChunkerType> for JsChunkerType {
    fn from(val: xberg::ChunkerType) -> Self {
        match val {
            xberg::ChunkerType::Text => Self::Text,
            xberg::ChunkerType::Markdown => Self::Markdown,
            xberg::ChunkerType::Yaml => Self::Yaml,
            xberg::ChunkerType::Semantic => Self::Semantic,
        }
    }
}

impl From<JsChunkingReason> for xberg::ChunkingReason {
    fn from(val: JsChunkingReason) -> Self {
        match val.type_tag.as_str() {
            "LargeFile" => Self::LargeFile {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                threshold_bytes: val.threshold_bytes.map(|v| v as u64).unwrap_or_default(),
            },
            "ManyPages" => Self::ManyPages {
                page_count: val.page_count.unwrap_or_default(),
                threshold: val.threshold.unwrap_or_default(),
            },
            "OcrRequired" => Self::OcrRequired {
                page_count: val.page_count.unwrap_or_default(),
                force_ocr: val.force_ocr.unwrap_or_default(),
            },
            "LargeAndManyPages" => Self::LargeAndManyPages {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                page_count: val.page_count.unwrap_or_default(),
            },
            _ => Self::LargeFile {
                size_bytes: Default::default(),
                threshold_bytes: Default::default(),
            },
        }
    }
}

impl From<xberg::ChunkingReason> for JsChunkingReason {
    fn from(val: xberg::ChunkingReason) -> Self {
        match val {
            xberg::ChunkingReason::LargeFile {
                size_bytes,
                threshold_bytes,
            } => Self {
                type_tag: "LargeFile".to_string(),
                force_ocr: None,
                page_count: None,
                size_bytes: Some(size_bytes as i64),
                threshold: None,
                threshold_bytes: Some(threshold_bytes as i64),
            },
            xberg::ChunkingReason::ManyPages { page_count, threshold } => Self {
                type_tag: "ManyPages".to_string(),
                force_ocr: None,
                page_count: Some(page_count),
                size_bytes: None,
                threshold: Some(threshold),
                threshold_bytes: None,
            },
            xberg::ChunkingReason::OcrRequired { page_count, force_ocr } => Self {
                type_tag: "OcrRequired".to_string(),
                force_ocr: Some(force_ocr),
                page_count: Some(page_count),
                size_bytes: None,
                threshold: None,
                threshold_bytes: None,
            },
            xberg::ChunkingReason::LargeAndManyPages { size_bytes, page_count } => Self {
                type_tag: "LargeAndManyPages".to_string(),
                force_ocr: None,
                page_count: Some(page_count),
                size_bytes: Some(size_bytes as i64),
                threshold: None,
                threshold_bytes: None,
            },
        }
    }
}

impl From<JsCodeBlockStyle> for html_to_markdown_rs::CodeBlockStyle {
    fn from(val: JsCodeBlockStyle) -> Self {
        match val {
            JsCodeBlockStyle::Indented => Self::Indented,
            JsCodeBlockStyle::Backticks => Self::Backticks,
            JsCodeBlockStyle::Tildes => Self::Tildes,
        }
    }
}

impl From<html_to_markdown_rs::CodeBlockStyle> for JsCodeBlockStyle {
    fn from(val: html_to_markdown_rs::CodeBlockStyle) -> Self {
        match val {
            html_to_markdown_rs::CodeBlockStyle::Indented => Self::Indented,
            html_to_markdown_rs::CodeBlockStyle::Backticks => Self::Backticks,
            html_to_markdown_rs::CodeBlockStyle::Tildes => Self::Tildes,
        }
    }
}

impl From<JsCodeContentMode> for xberg::CodeContentMode {
    fn from(val: JsCodeContentMode) -> Self {
        match val {
            JsCodeContentMode::Chunks => Self::Chunks,
            JsCodeContentMode::Raw => Self::Raw,
            JsCodeContentMode::Structure => Self::Structure,
        }
    }
}

impl From<xberg::CodeContentMode> for JsCodeContentMode {
    fn from(val: xberg::CodeContentMode) -> Self {
        match val {
            xberg::CodeContentMode::Chunks => Self::Chunks,
            xberg::CodeContentMode::Raw => Self::Raw,
            xberg::CodeContentMode::Structure => Self::Structure,
        }
    }
}

impl From<JsCodeDataNodeKind> for xberg::CodeDataNodeKind {
    fn from(val: JsCodeDataNodeKind) -> Self {
        match val {
            JsCodeDataNodeKind::KeyValue => Self::KeyValue,
            JsCodeDataNodeKind::Element => Self::Element,
            JsCodeDataNodeKind::Sequence => Self::Sequence,
        }
    }
}

impl From<xberg::CodeDataNodeKind> for JsCodeDataNodeKind {
    fn from(val: xberg::CodeDataNodeKind) -> Self {
        match val {
            xberg::CodeDataNodeKind::KeyValue => Self::KeyValue,
            xberg::CodeDataNodeKind::Element => Self::Element,
            xberg::CodeDataNodeKind::Sequence => Self::Sequence,
        }
    }
}

impl From<JsConfidenceSemantics> for xberg::ConfidenceSemantics {
    fn from(val: JsConfidenceSemantics) -> Self {
        match val.type_tag.as_str() {
            "Legibility" => Self::Legibility {
                scale_max: val.scale_max.unwrap_or_default(),
            },
            "Uncalibrated" => Self::Uncalibrated,
            "None" => Self::None,
            _ => Self::Legibility {
                scale_max: Default::default(),
            },
        }
    }
}

impl From<xberg::ConfidenceSemantics> for JsConfidenceSemantics {
    fn from(val: xberg::ConfidenceSemantics) -> Self {
        match val {
            xberg::ConfidenceSemantics::Legibility { scale_max } => Self {
                type_tag: "Legibility".to_string(),
                scale_max: Some(scale_max),
            },
            xberg::ConfidenceSemantics::Uncalibrated => Self {
                type_tag: "Uncalibrated".to_string(),
                scale_max: None,
            },
            xberg::ConfidenceSemantics::None => Self {
                type_tag: "None".to_string(),
                scale_max: None,
            },
        }
    }
}

impl From<JsContentFilterKind> for crawlberg::ContentFilterKind {
    fn from(val: JsContentFilterKind) -> Self {
        match val {
            JsContentFilterKind::Bm25 => Self::Bm25,
        }
    }
}

impl From<crawlberg::ContentFilterKind> for JsContentFilterKind {
    fn from(val: crawlberg::ContentFilterKind) -> Self {
        match val {
            crawlberg::ContentFilterKind::Bm25 => Self::Bm25,
        }
    }
}

impl From<JsContentLayer> for xberg::ContentLayer {
    fn from(val: JsContentLayer) -> Self {
        match val {
            JsContentLayer::Body => Self::Body,
            JsContentLayer::Header => Self::Header,
            JsContentLayer::Footer => Self::Footer,
            JsContentLayer::Footnote => Self::Footnote,
        }
    }
}

impl From<xberg::ContentLayer> for JsContentLayer {
    fn from(val: xberg::ContentLayer) -> Self {
        match val {
            xberg::ContentLayer::Body => Self::Body,
            xberg::ContentLayer::Header => Self::Header,
            xberg::ContentLayer::Footer => Self::Footer,
            xberg::ContentLayer::Footnote => Self::Footnote,
        }
    }
}

impl From<JsCrawlStrategyKind> for crawlberg::CrawlStrategyKind {
    fn from(val: JsCrawlStrategyKind) -> Self {
        match val {
            JsCrawlStrategyKind::Bfs => Self::Bfs,
            JsCrawlStrategyKind::Dfs => Self::Dfs,
            JsCrawlStrategyKind::BestFirst => Self::BestFirst,
            JsCrawlStrategyKind::Adaptive => Self::Adaptive,
        }
    }
}

impl From<crawlberg::CrawlStrategyKind> for JsCrawlStrategyKind {
    fn from(val: crawlberg::CrawlStrategyKind) -> Self {
        match val {
            crawlberg::CrawlStrategyKind::Bfs => Self::Bfs,
            crawlberg::CrawlStrategyKind::Dfs => Self::Dfs,
            crawlberg::CrawlStrategyKind::BestFirst => Self::BestFirst,
            crawlberg::CrawlStrategyKind::Adaptive => Self::Adaptive,
        }
    }
}

impl From<JsCredentialProviderConfig> for xberg::CredentialProviderConfig {
    fn from(val: JsCredentialProviderConfig) -> Self {
        match val.type_tag.as_str() {
            "azure_ad" => Self::AzureAd {
                tenant_id: val.tenant_id.unwrap_or_default(),
                client_id: val.client_id.unwrap_or_default(),
                client_secret: val.client_secret.unwrap_or_default(),
                scope: val.scope,
            },
            "vertex_oauth2" => Self::VertexOauth2 {
                service_account_key_file: val.service_account_key_file.unwrap_or_default(),
                scope: val.scope,
            },
            "vertex_adc" => Self::VertexAdc { scope: val.scope },
            "bedrock_web_identity" => Self::BedrockWebIdentity {
                role_arn: val.role_arn.unwrap_or_default(),
                token_file: val.token_file.unwrap_or_default(),
                session_name: val.session_name,
                region: val.region,
            },
            _ => Self::AzureAd {
                tenant_id: Default::default(),
                client_id: Default::default(),
                client_secret: Default::default(),
                scope: Default::default(),
            },
        }
    }
}

impl From<xberg::CredentialProviderConfig> for JsCredentialProviderConfig {
    fn from(val: xberg::CredentialProviderConfig) -> Self {
        match val {
            xberg::CredentialProviderConfig::AzureAd {
                tenant_id,
                client_id,
                client_secret,
                scope,
            } => Self {
                type_tag: "azure_ad".to_string(),
                client_id: Some(client_id),
                client_secret: Some(client_secret),
                region: None,
                role_arn: None,
                scope,
                service_account_key_file: None,
                session_name: None,
                tenant_id: Some(tenant_id),
                token_file: None,
            },
            xberg::CredentialProviderConfig::VertexOauth2 {
                service_account_key_file,
                scope,
            } => Self {
                type_tag: "vertex_oauth2".to_string(),
                client_id: None,
                client_secret: None,
                region: None,
                role_arn: None,
                scope,
                service_account_key_file: Some(service_account_key_file),
                session_name: None,
                tenant_id: None,
                token_file: None,
            },
            xberg::CredentialProviderConfig::VertexAdc { scope } => Self {
                type_tag: "vertex_adc".to_string(),
                client_id: None,
                client_secret: None,
                region: None,
                role_arn: None,
                scope,
                service_account_key_file: None,
                session_name: None,
                tenant_id: None,
                token_file: None,
            },
            xberg::CredentialProviderConfig::BedrockWebIdentity {
                role_arn,
                token_file,
                session_name,
                region,
            } => Self {
                type_tag: "bedrock_web_identity".to_string(),
                client_id: None,
                client_secret: None,
                region,
                role_arn: Some(role_arn),
                scope: None,
                service_account_key_file: None,
                session_name,
                tenant_id: None,
                token_file: Some(token_file),
            },
        }
    }
}

impl From<JsDiffLine> for xberg::DiffLine {
    fn from(val: JsDiffLine) -> Self {
        match val.kind_tag.as_str() {
            "context" => Self::Context(val.text.unwrap_or_default()),
            "added" => Self::Added(val.text.unwrap_or_default()),
            "removed" => Self::Removed(val.text.unwrap_or_default()),
            _ => Self::Context(Default::default()),
        }
    }
}

impl From<xberg::DiffLine> for JsDiffLine {
    fn from(val: xberg::DiffLine) -> Self {
        match val {
            xberg::DiffLine::Context(text) => Self {
                kind_tag: "context".to_string(),
                text: Some(text),
            },
            xberg::DiffLine::Added(text) => Self {
                kind_tag: "added".to_string(),
                text: Some(text),
            },
            xberg::DiffLine::Removed(text) => Self {
                kind_tag: "removed".to_string(),
                text: Some(text),
            },
        }
    }
}

impl From<JsDocumentContentEncoding> for crawlberg::DocumentContentEncoding {
    fn from(val: JsDocumentContentEncoding) -> Self {
        match val {
            JsDocumentContentEncoding::Base64 => Self::Base64,
        }
    }
}

impl From<crawlberg::DocumentContentEncoding> for JsDocumentContentEncoding {
    fn from(val: crawlberg::DocumentContentEncoding) -> Self {
        match val {
            crawlberg::DocumentContentEncoding::Base64 => Self::Base64,
        }
    }
}

impl From<JsElementType> for xberg::ElementType {
    fn from(val: JsElementType) -> Self {
        match val {
            JsElementType::Title => Self::Title,
            JsElementType::NarrativeText => Self::NarrativeText,
            JsElementType::Heading => Self::Heading,
            JsElementType::ListItem => Self::ListItem,
            JsElementType::Table => Self::Table,
            JsElementType::Image => Self::Image,
            JsElementType::PageBreak => Self::PageBreak,
            JsElementType::CodeBlock => Self::CodeBlock,
            JsElementType::Formula => Self::Formula,
            JsElementType::BlockQuote => Self::BlockQuote,
            JsElementType::Footer => Self::Footer,
            JsElementType::Header => Self::Header,
        }
    }
}

impl From<xberg::ElementType> for JsElementType {
    fn from(val: xberg::ElementType) -> Self {
        match val {
            xberg::ElementType::Title => Self::Title,
            xberg::ElementType::NarrativeText => Self::NarrativeText,
            xberg::ElementType::Heading => Self::Heading,
            xberg::ElementType::ListItem => Self::ListItem,
            xberg::ElementType::Table => Self::Table,
            xberg::ElementType::Image => Self::Image,
            xberg::ElementType::PageBreak => Self::PageBreak,
            xberg::ElementType::CodeBlock => Self::CodeBlock,
            xberg::ElementType::Formula => Self::Formula,
            xberg::ElementType::BlockQuote => Self::BlockQuote,
            xberg::ElementType::Footer => Self::Footer,
            xberg::ElementType::Header => Self::Header,
        }
    }
}

impl From<JsEmbeddingModelType> for xberg::EmbeddingModelType {
    fn from(val: JsEmbeddingModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => Self::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => Self::Custom {
                model_id: val.model_id.unwrap_or_default(),
                dimensions: val.dimensions.map(|v| v as usize).unwrap_or_default(),
            },
            "llm" => Self::Llm {
                llm: Box::new(val.llm.map(|v| v.into()).unwrap_or_default()),
            },
            "plugin" => Self::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => Self::Preset {
                name: Default::default(),
            },
        }
    }
}

impl From<xberg::EmbeddingModelType> for JsEmbeddingModelType {
    fn from(val: xberg::EmbeddingModelType) -> Self {
        match val {
            xberg::EmbeddingModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                dimensions: None,
                llm: None,
                model_id: None,
                name: Some(name),
            },
            xberg::EmbeddingModelType::Custom { model_id, dimensions } => Self {
                type_tag: "custom".to_string(),
                dimensions: Some(dimensions as i64),
                llm: None,
                model_id: Some(model_id),
                name: None,
            },
            xberg::EmbeddingModelType::Llm { llm } => Self {
                type_tag: "llm".to_string(),
                dimensions: None,
                llm: Some((*llm).into()),
                model_id: None,
                name: None,
            },
            xberg::EmbeddingModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                dimensions: None,
                llm: None,
                model_id: None,
                name: Some(name),
            },
        }
    }
}

impl From<xberg::embeddings::EmbeddingBackend> for JsEmbeddingsEmbeddingBackend {
    fn from(val: xberg::embeddings::EmbeddingBackend) -> Self {
        match val {
            xberg::embeddings::EmbeddingBackend::Onnx => Self::Onnx,
            xberg::embeddings::EmbeddingBackend::Static => Self::Static,
        }
    }
}

impl From<JsEntityCategory> for xberg::EntityCategory {
    fn from(val: JsEntityCategory) -> Self {
        match val.type_tag.as_str() {
            "person" => Self::Person,
            "organization" => Self::Organization,
            "location" => Self::Location,
            "date" => Self::Date,
            "time" => Self::Time,
            "money" => Self::Money,
            "percent" => Self::Percent,
            "email" => Self::Email,
            "phone" => Self::Phone,
            "url" => Self::Url,
            "custom" => Self::Custom(val.custom.unwrap_or_default()),
            _ => Self::Person,
        }
    }
}

impl From<xberg::EntityCategory> for JsEntityCategory {
    fn from(val: xberg::EntityCategory) -> Self {
        match val {
            xberg::EntityCategory::Person => Self {
                type_tag: "person".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Organization => Self {
                type_tag: "organization".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Location => Self {
                type_tag: "location".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Date => Self {
                type_tag: "date".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Time => Self {
                type_tag: "time".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Money => Self {
                type_tag: "money".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Percent => Self {
                type_tag: "percent".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Email => Self {
                type_tag: "email".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Phone => Self {
                type_tag: "phone".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Url => Self {
                type_tag: "url".to_string(),
                custom: None,
            },
            xberg::EntityCategory::Custom(custom) => Self {
                type_tag: "custom".to_string(),
                custom: Some(custom),
            },
        }
    }
}

impl From<JsExecutionProviderType> for xberg::ExecutionProviderType {
    fn from(val: JsExecutionProviderType) -> Self {
        match val {
            JsExecutionProviderType::Auto => Self::Auto,
            JsExecutionProviderType::Cpu => Self::Cpu,
            JsExecutionProviderType::CoreMl => Self::CoreMl,
            JsExecutionProviderType::Cuda => Self::Cuda,
            JsExecutionProviderType::TensorRt => Self::TensorRt,
        }
    }
}

impl From<xberg::ExecutionProviderType> for JsExecutionProviderType {
    fn from(val: xberg::ExecutionProviderType) -> Self {
        match val {
            xberg::ExecutionProviderType::Auto => Self::Auto,
            xberg::ExecutionProviderType::Cpu => Self::Cpu,
            xberg::ExecutionProviderType::CoreMl => Self::CoreMl,
            xberg::ExecutionProviderType::Cuda => Self::Cuda,
            xberg::ExecutionProviderType::TensorRt => Self::TensorRt,
        }
    }
}

impl From<JsExtractInputKind> for xberg::ExtractInputKind {
    fn from(val: JsExtractInputKind) -> Self {
        match val {
            JsExtractInputKind::Bytes => Self::Bytes,
            JsExtractInputKind::Uri => Self::Uri,
        }
    }
}

impl From<xberg::ExtractInputKind> for JsExtractInputKind {
    fn from(val: xberg::ExtractInputKind) -> Self {
        match val {
            xberg::ExtractInputKind::Bytes => Self::Bytes,
            xberg::ExtractInputKind::Uri => Self::Uri,
        }
    }
}

impl From<JsExtractionMethod> for xberg::ExtractionMethod {
    fn from(val: JsExtractionMethod) -> Self {
        match val {
            JsExtractionMethod::Native => Self::Native,
            JsExtractionMethod::Ocr => Self::Ocr,
            JsExtractionMethod::Mixed => Self::Mixed,
        }
    }
}

impl From<xberg::ExtractionMethod> for JsExtractionMethod {
    fn from(val: xberg::ExtractionMethod) -> Self {
        match val {
            xberg::ExtractionMethod::Native => Self::Native,
            xberg::ExtractionMethod::Ocr => Self::Ocr,
            xberg::ExtractionMethod::Mixed => Self::Mixed,
        }
    }
}

impl From<JsFormFieldType> for xberg::FormFieldType {
    fn from(val: JsFormFieldType) -> Self {
        match val {
            JsFormFieldType::Text => Self::Text,
            JsFormFieldType::Checkbox => Self::Checkbox,
            JsFormFieldType::Radio => Self::Radio,
            JsFormFieldType::Choice => Self::Choice,
            JsFormFieldType::Signature => Self::Signature,
            JsFormFieldType::Button => Self::Button,
            JsFormFieldType::Unknown => Self::Unknown,
        }
    }
}

impl From<xberg::FormFieldType> for JsFormFieldType {
    fn from(val: xberg::FormFieldType) -> Self {
        match val {
            xberg::FormFieldType::Text => Self::Text,
            xberg::FormFieldType::Checkbox => Self::Checkbox,
            xberg::FormFieldType::Radio => Self::Radio,
            xberg::FormFieldType::Choice => Self::Choice,
            xberg::FormFieldType::Signature => Self::Signature,
            xberg::FormFieldType::Button => Self::Button,
            xberg::FormFieldType::Unknown => Self::Unknown,
        }
    }
}

impl From<JsFormatMetadata> for xberg::FormatMetadata {
    fn from(val: JsFormatMetadata) -> Self {
        match val.format_type_tag.as_str() {
            #[cfg(feature = "pdf")]
            "pdf" => Self::Pdf(val.pdf.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "docx" => Self::Docx(Box::new(val.docx.map(|v| v.into()).unwrap_or_default())),
            "excel" => Self::Excel(val.excel.map(|v| v.into()).unwrap_or_default()),
            "email" => Self::Email(val.email.map(|v| v.into()).unwrap_or_default()),
            "pptx" => Self::Pptx(val.pptx.map(|v| v.into()).unwrap_or_default()),
            "archive" => Self::Archive(val.archive.map(|v| v.into()).unwrap_or_default()),
            "image" => Self::Image(val.image.map(|v| v.into()).unwrap_or_default()),
            "xml" => Self::Xml(val.xml.map(|v| v.into()).unwrap_or_default()),
            "text" => Self::Text(val.text.map(|v| v.into()).unwrap_or_default()),
            "html" => Self::Html(Box::new(val.html.map(|v| v.into()).unwrap_or_default())),
            "ocr" => Self::Ocr(val.ocr.map(|v| v.into()).unwrap_or_default()),
            "csv" => Self::Csv(val.csv.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "bibtex" => Self::Bibtex(val.bibtex.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "citation" => Self::Citation(val.citation.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "fiction_book" => Self::FictionBook(val.fiction_book.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "dbf" => Self::Dbf(val.dbf.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "xml")]
            "jats" => Self::Jats(val.jats.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "office")]
            "epub" => Self::Epub(val.epub.map(|v| v.into()).unwrap_or_default()),
            "pst" => Self::Pst(val.pst.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "transcription-types")]
            "audio" => Self::Audio(val.audio.map(|v| v.into()).unwrap_or_default()),
            #[cfg(feature = "tree-sitter")]
            "code" => Self::Code(val.code.map(|v| v.into()).unwrap_or_default()),
            _ => Self::Excel(Default::default()),
        }
    }
}

impl From<xberg::FormatMetadata> for JsFormatMetadata {
    fn from(val: xberg::FormatMetadata) -> Self {
        match val {
            #[cfg(feature = "pdf")]
            xberg::FormatMetadata::Pdf(pdf) => Self {
                format_type_tag: "pdf".to_string(),
                pdf: Some(pdf.into()),
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::Docx(docx) => Self {
                format_type_tag: "docx".to_string(),
                pdf: None,
                docx: Some((*docx).into()),
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Excel(excel) => Self {
                format_type_tag: "excel".to_string(),
                pdf: None,
                docx: None,
                excel: Some(excel.into()),
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Email(email) => Self {
                format_type_tag: "email".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: Some(email.into()),
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Pptx(pptx) => Self {
                format_type_tag: "pptx".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: Some(pptx.into()),
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Archive(archive) => Self {
                format_type_tag: "archive".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: Some(archive.into()),
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Image(image) => Self {
                format_type_tag: "image".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: Some(image.into()),
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Xml(xml) => Self {
                format_type_tag: "xml".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: Some(xml.into()),
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Text(text) => Self {
                format_type_tag: "text".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: Some(text.into()),
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Html(html) => Self {
                format_type_tag: "html".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: Some((*html).into()),
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Ocr(ocr) => Self {
                format_type_tag: "ocr".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: Some(ocr.into()),
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Csv(csv) => Self {
                format_type_tag: "csv".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: Some(csv.into()),
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::Bibtex(bibtex) => Self {
                format_type_tag: "bibtex".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: Some(bibtex.into()),
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::Citation(citation) => Self {
                format_type_tag: "citation".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: Some(citation.into()),
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::FictionBook(fiction_book) => Self {
                format_type_tag: "fiction_book".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: Some(fiction_book.into()),
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::Dbf(dbf) => Self {
                format_type_tag: "dbf".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: Some(dbf.into()),
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "xml")]
            xberg::FormatMetadata::Jats(jats) => Self {
                format_type_tag: "jats".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: Some(jats.into()),
                epub: None,
                pst: None,
                audio: None,
                code: None,
            },
            #[cfg(feature = "office")]
            xberg::FormatMetadata::Epub(epub) => Self {
                format_type_tag: "epub".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: Some(epub.into()),
                pst: None,
                audio: None,
                code: None,
            },
            xberg::FormatMetadata::Pst(pst) => Self {
                format_type_tag: "pst".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: Some(pst.into()),
                audio: None,
                code: None,
            },
            #[cfg(feature = "transcription-types")]
            xberg::FormatMetadata::Audio(audio) => Self {
                format_type_tag: "audio".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: Some(audio.into()),
                code: None,
            },
            #[cfg(feature = "tree-sitter")]
            xberg::FormatMetadata::Code(code) => Self {
                format_type_tag: "code".to_string(),
                pdf: None,
                docx: None,
                excel: None,
                email: None,
                pptx: None,
                archive: None,
                image: None,
                xml: None,
                text: None,
                html: None,
                ocr: None,
                csv: None,
                bibtex: None,
                citation: None,
                fiction_book: None,
                dbf: None,
                jats: None,
                epub: None,
                pst: None,
                audio: None,
                code: Some(code.into()),
            },
        }
    }
}

impl From<JsFormulaModel> for xberg::core::config::layout::FormulaModel {
    fn from(val: JsFormulaModel) -> Self {
        match val {
            JsFormulaModel::LatexOcr => Self::LatexOcr,
        }
    }
}

impl From<xberg::core::config::layout::FormulaModel> for JsFormulaModel {
    fn from(val: xberg::core::config::layout::FormulaModel) -> Self {
        match val {
            xberg::core::config::layout::FormulaModel::LatexOcr => Self::LatexOcr,
        }
    }
}

impl From<xberg::candle_ocr::GlmOcrLayoutMode> for JsGlmOcrLayoutMode {
    fn from(val: xberg::candle_ocr::GlmOcrLayoutMode) -> Self {
        match val {
            xberg::candle_ocr::GlmOcrLayoutMode::WholePage => Self::WholePage,
            xberg::candle_ocr::GlmOcrLayoutMode::Paired => Self::Paired,
        }
    }
}

impl From<xberg::candle_ocr::GlmOcrTaskKind> for JsGlmOcrTaskKind {
    fn from(val: xberg::candle_ocr::GlmOcrTaskKind) -> Self {
        match val {
            xberg::candle_ocr::GlmOcrTaskKind::Ocr => Self::Ocr,
            xberg::candle_ocr::GlmOcrTaskKind::Table => Self::Table,
            xberg::candle_ocr::GlmOcrTaskKind::Formula => Self::Formula,
            xberg::candle_ocr::GlmOcrTaskKind::Chart => Self::Chart,
            xberg::candle_ocr::GlmOcrTaskKind::Caption => Self::Caption,
        }
    }
}

impl From<JsHeadingStyle> for html_to_markdown_rs::HeadingStyle {
    fn from(val: JsHeadingStyle) -> Self {
        match val {
            JsHeadingStyle::Underlined => Self::Underlined,
            JsHeadingStyle::Atx => Self::Atx,
            JsHeadingStyle::AtxClosed => Self::AtxClosed,
        }
    }
}

impl From<html_to_markdown_rs::HeadingStyle> for JsHeadingStyle {
    fn from(val: html_to_markdown_rs::HeadingStyle) -> Self {
        match val {
            html_to_markdown_rs::HeadingStyle::Underlined => Self::Underlined,
            html_to_markdown_rs::HeadingStyle::Atx => Self::Atx,
            html_to_markdown_rs::HeadingStyle::AtxClosed => Self::AtxClosed,
        }
    }
}

impl From<JsHighlightStyle> for html_to_markdown_rs::HighlightStyle {
    fn from(val: JsHighlightStyle) -> Self {
        match val {
            JsHighlightStyle::DoubleEqual => Self::DoubleEqual,
            JsHighlightStyle::Html => Self::Html,
            JsHighlightStyle::Bold => Self::Bold,
            JsHighlightStyle::None => Self::None,
        }
    }
}

impl From<html_to_markdown_rs::HighlightStyle> for JsHighlightStyle {
    fn from(val: html_to_markdown_rs::HighlightStyle) -> Self {
        match val {
            html_to_markdown_rs::HighlightStyle::DoubleEqual => Self::DoubleEqual,
            html_to_markdown_rs::HighlightStyle::Html => Self::Html,
            html_to_markdown_rs::HighlightStyle::Bold => Self::Bold,
            html_to_markdown_rs::HighlightStyle::None => Self::None,
        }
    }
}

impl From<JsHostMatcher> for crawlberg::HostMatcher {
    fn from(val: JsHostMatcher) -> Self {
        match val.type_tag.as_str() {
            "exact" => Self::Exact {
                value: val.value.unwrap_or_default(),
            },
            "suffix" => Self::Suffix {
                value: val.value.unwrap_or_default(),
            },
            "cidr" => Self::Cidr {
                value: val.value.unwrap_or_default(),
            },
            _ => Self::Exact {
                value: Default::default(),
            },
        }
    }
}

impl From<crawlberg::HostMatcher> for JsHostMatcher {
    fn from(val: crawlberg::HostMatcher) -> Self {
        match val {
            crawlberg::HostMatcher::Exact { value } => Self {
                type_tag: "exact".to_string(),
                value: Some(value),
            },
            crawlberg::HostMatcher::Suffix { value } => Self {
                type_tag: "suffix".to_string(),
                value: Some(value),
            },
            crawlberg::HostMatcher::Cidr { value } => Self {
                type_tag: "cidr".to_string(),
                value: Some(value),
            },
        }
    }
}

impl From<JsHtmlTheme> for xberg::HtmlTheme {
    fn from(val: JsHtmlTheme) -> Self {
        match val {
            JsHtmlTheme::Default => Self::Default,
            JsHtmlTheme::GitHub => Self::GitHub,
            JsHtmlTheme::Dark => Self::Dark,
            JsHtmlTheme::Light => Self::Light,
            JsHtmlTheme::Unstyled => Self::Unstyled,
        }
    }
}

impl From<xberg::HtmlTheme> for JsHtmlTheme {
    fn from(val: xberg::HtmlTheme) -> Self {
        match val {
            xberg::HtmlTheme::Default => Self::Default,
            xberg::HtmlTheme::GitHub => Self::GitHub,
            xberg::HtmlTheme::Dark => Self::Dark,
            xberg::HtmlTheme::Light => Self::Light,
            xberg::HtmlTheme::Unstyled => Self::Unstyled,
        }
    }
}

impl From<JsHtmlToMarkdownRsOutputFormat> for html_to_markdown_rs::OutputFormat {
    fn from(val: JsHtmlToMarkdownRsOutputFormat) -> Self {
        match val {
            JsHtmlToMarkdownRsOutputFormat::Markdown => Self::Markdown,
            JsHtmlToMarkdownRsOutputFormat::Djot => Self::Djot,
            JsHtmlToMarkdownRsOutputFormat::Plain => Self::Plain,
        }
    }
}

impl From<html_to_markdown_rs::OutputFormat> for JsHtmlToMarkdownRsOutputFormat {
    fn from(val: html_to_markdown_rs::OutputFormat) -> Self {
        match val {
            html_to_markdown_rs::OutputFormat::Markdown => Self::Markdown,
            html_to_markdown_rs::OutputFormat::Djot => Self::Djot,
            html_to_markdown_rs::OutputFormat::Plain => Self::Plain,
        }
    }
}

impl From<JsImageKind> for xberg::ImageKind {
    fn from(val: JsImageKind) -> Self {
        match val {
            JsImageKind::Photograph => Self::Photograph,
            JsImageKind::Diagram => Self::Diagram,
            JsImageKind::Chart => Self::Chart,
            JsImageKind::Drawing => Self::Drawing,
            JsImageKind::TextBlock => Self::TextBlock,
            JsImageKind::Decoration => Self::Decoration,
            JsImageKind::Logo => Self::Logo,
            JsImageKind::Icon => Self::Icon,
            JsImageKind::TileFragment => Self::TileFragment,
            JsImageKind::Mask => Self::Mask,
            JsImageKind::PageRaster => Self::PageRaster,
            JsImageKind::Unknown => Self::Unknown,
        }
    }
}

impl From<xberg::ImageKind> for JsImageKind {
    fn from(val: xberg::ImageKind) -> Self {
        match val {
            xberg::ImageKind::Photograph => Self::Photograph,
            xberg::ImageKind::Diagram => Self::Diagram,
            xberg::ImageKind::Chart => Self::Chart,
            xberg::ImageKind::Drawing => Self::Drawing,
            xberg::ImageKind::TextBlock => Self::TextBlock,
            xberg::ImageKind::Decoration => Self::Decoration,
            xberg::ImageKind::Logo => Self::Logo,
            xberg::ImageKind::Icon => Self::Icon,
            xberg::ImageKind::TileFragment => Self::TileFragment,
            xberg::ImageKind::Mask => Self::Mask,
            xberg::ImageKind::PageRaster => Self::PageRaster,
            xberg::ImageKind::Unknown => Self::Unknown,
        }
    }
}

impl From<JsImageOutputFormat> for xberg::core::config::extraction::ImageOutputFormat {
    fn from(val: JsImageOutputFormat) -> Self {
        match val.type_tag.as_str() {
            "native" => Self::Native,
            "png" => Self::Png,
            "jpeg" => Self::Jpeg {
                quality: val.quality.unwrap_or_default(),
            },
            "webp" => Self::Webp {
                quality: val.quality.unwrap_or_default(),
            },
            "heif" => Self::Heif {
                quality: val.quality.unwrap_or_default(),
            },
            #[cfg(feature = "svg")]
            "svg" => Self::Svg,
            _ => Self::Native,
        }
    }
}

impl From<xberg::core::config::extraction::ImageOutputFormat> for JsImageOutputFormat {
    fn from(val: xberg::core::config::extraction::ImageOutputFormat) -> Self {
        match val {
            xberg::core::config::extraction::ImageOutputFormat::Native => Self {
                type_tag: "native".to_string(),
                quality: None,
            },
            xberg::core::config::extraction::ImageOutputFormat::Png => Self {
                type_tag: "png".to_string(),
                quality: None,
            },
            xberg::core::config::extraction::ImageOutputFormat::Jpeg { quality } => Self {
                type_tag: "jpeg".to_string(),
                quality: Some(quality),
            },
            xberg::core::config::extraction::ImageOutputFormat::Webp { quality } => Self {
                type_tag: "webp".to_string(),
                quality: Some(quality),
            },
            xberg::core::config::extraction::ImageOutputFormat::Heif { quality } => Self {
                type_tag: "heif".to_string(),
                quality: Some(quality),
            },
            #[cfg(feature = "svg")]
            xberg::core::config::extraction::ImageOutputFormat::Svg => Self {
                type_tag: "svg".to_string(),
                quality: None,
            },
        }
    }
}

impl From<JsImageType> for xberg::ImageType {
    fn from(val: JsImageType) -> Self {
        match val {
            JsImageType::DataUri => Self::DataUri,
            JsImageType::InlineSvg => Self::InlineSvg,
            JsImageType::External => Self::External,
            JsImageType::Relative => Self::Relative,
        }
    }
}

impl From<xberg::ImageType> for JsImageType {
    fn from(val: xberg::ImageType) -> Self {
        match val {
            xberg::ImageType::DataUri => Self::DataUri,
            xberg::ImageType::InlineSvg => Self::InlineSvg,
            xberg::ImageType::External => Self::External,
            xberg::ImageType::Relative => Self::Relative,
        }
    }
}

impl From<JsInlineType> for xberg::InlineType {
    fn from(val: JsInlineType) -> Self {
        match val {
            JsInlineType::Text => Self::Text,
            JsInlineType::Strong => Self::Strong,
            JsInlineType::Emphasis => Self::Emphasis,
            JsInlineType::Highlight => Self::Highlight,
            JsInlineType::Subscript => Self::Subscript,
            JsInlineType::Superscript => Self::Superscript,
            JsInlineType::Insert => Self::Insert,
            JsInlineType::Delete => Self::Delete,
            JsInlineType::Code => Self::Code,
            JsInlineType::Link => Self::Link,
            JsInlineType::Image => Self::Image,
            JsInlineType::Span => Self::Span,
            JsInlineType::Math => Self::Math,
            JsInlineType::RawInline => Self::RawInline,
            JsInlineType::FootnoteRef => Self::FootnoteRef,
            JsInlineType::Symbol => Self::Symbol,
        }
    }
}

impl From<xberg::InlineType> for JsInlineType {
    fn from(val: xberg::InlineType) -> Self {
        match val {
            xberg::InlineType::Text => Self::Text,
            xberg::InlineType::Strong => Self::Strong,
            xberg::InlineType::Emphasis => Self::Emphasis,
            xberg::InlineType::Highlight => Self::Highlight,
            xberg::InlineType::Subscript => Self::Subscript,
            xberg::InlineType::Superscript => Self::Superscript,
            xberg::InlineType::Insert => Self::Insert,
            xberg::InlineType::Delete => Self::Delete,
            xberg::InlineType::Code => Self::Code,
            xberg::InlineType::Link => Self::Link,
            xberg::InlineType::Image => Self::Image,
            xberg::InlineType::Span => Self::Span,
            xberg::InlineType::Math => Self::Math,
            xberg::InlineType::RawInline => Self::RawInline,
            xberg::InlineType::FootnoteRef => Self::FootnoteRef,
            xberg::InlineType::Symbol => Self::Symbol,
        }
    }
}

impl From<JsJupyterCellRendering> for xberg::JupyterCellRendering {
    fn from(val: JsJupyterCellRendering) -> Self {
        match val {
            JsJupyterCellRendering::Source => Self::Source,
            JsJupyterCellRendering::Outputs => Self::Outputs,
            JsJupyterCellRendering::Both => Self::Both,
        }
    }
}

impl From<xberg::JupyterCellRendering> for JsJupyterCellRendering {
    fn from(val: xberg::JupyterCellRendering) -> Self {
        match val {
            xberg::JupyterCellRendering::Source => Self::Source,
            xberg::JupyterCellRendering::Outputs => Self::Outputs,
            xberg::JupyterCellRendering::Both => Self::Both,
        }
    }
}

impl From<JsKeywordAlgorithm> for xberg::KeywordAlgorithm {
    fn from(val: JsKeywordAlgorithm) -> Self {
        match val {
            #[cfg(feature = "keywords-yake")]
            JsKeywordAlgorithm::Yake => Self::Yake,
            #[cfg(feature = "keywords-rake")]
            JsKeywordAlgorithm::Rake => Self::Rake,
        }
    }
}

impl From<xberg::KeywordAlgorithm> for JsKeywordAlgorithm {
    fn from(val: xberg::KeywordAlgorithm) -> Self {
        match val {
            #[cfg(feature = "keywords-yake")]
            xberg::KeywordAlgorithm::Yake => Self::Yake,
            #[cfg(feature = "keywords-rake")]
            xberg::KeywordAlgorithm::Rake => Self::Rake,
        }
    }
}

impl From<JsLateInteractionModelType> for xberg::LateInteractionModelType {
    fn from(val: JsLateInteractionModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => Self::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => Self::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.unwrap_or_default(),
                max_length: val.max_length,
            },
            "plugin" => Self::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => Self::Preset {
                name: Default::default(),
            },
        }
    }
}

impl From<xberg::LateInteractionModelType> for JsLateInteractionModelType {
    fn from(val: xberg::LateInteractionModelType) -> Self {
        match val {
            xberg::LateInteractionModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                additional_files: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
            xberg::LateInteractionModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
            } => Self {
                type_tag: "custom".to_string(),
                additional_files: Some(additional_files),
                max_length,
                model_file,
                model_id: Some(model_id),
                name: None,
            },
            xberg::LateInteractionModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                additional_files: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
        }
    }
}

impl From<xberg::LayoutClass> for JsLayoutClass {
    fn from(val: xberg::LayoutClass) -> Self {
        match val {
            xberg::LayoutClass::Caption => Self::Caption,
            xberg::LayoutClass::Chart => Self::Chart,
            xberg::LayoutClass::Footnote => Self::Footnote,
            xberg::LayoutClass::Formula => Self::Formula,
            xberg::LayoutClass::ListItem => Self::ListItem,
            xberg::LayoutClass::PageFooter => Self::PageFooter,
            xberg::LayoutClass::PageHeader => Self::PageHeader,
            xberg::LayoutClass::Picture => Self::Picture,
            xberg::LayoutClass::SectionHeader => Self::SectionHeader,
            xberg::LayoutClass::Table => Self::Table,
            xberg::LayoutClass::Text => Self::Text,
            xberg::LayoutClass::Title => Self::Title,
            xberg::LayoutClass::DocumentIndex => Self::DocumentIndex,
            xberg::LayoutClass::Code => Self::Code,
            xberg::LayoutClass::CheckboxSelected => Self::CheckboxSelected,
            xberg::LayoutClass::CheckboxUnselected => Self::CheckboxUnselected,
            xberg::LayoutClass::Form => Self::Form,
            xberg::LayoutClass::KeyValueRegion => Self::KeyValueRegion,
        }
    }
}

impl From<JsLayoutStrategy> for xberg::LayoutStrategy {
    fn from(val: JsLayoutStrategy) -> Self {
        match val {
            JsLayoutStrategy::Always => Self::Always,
            JsLayoutStrategy::Auto => Self::Auto,
        }
    }
}

impl From<xberg::LayoutStrategy> for JsLayoutStrategy {
    fn from(val: xberg::LayoutStrategy) -> Self {
        match val {
            xberg::LayoutStrategy::Always => Self::Always,
            xberg::LayoutStrategy::Auto => Self::Auto,
        }
    }
}

impl From<JsLinkStyle> for html_to_markdown_rs::LinkStyle {
    fn from(val: JsLinkStyle) -> Self {
        match val {
            JsLinkStyle::Inline => Self::Inline,
            JsLinkStyle::Reference => Self::Reference,
        }
    }
}

impl From<html_to_markdown_rs::LinkStyle> for JsLinkStyle {
    fn from(val: html_to_markdown_rs::LinkStyle) -> Self {
        match val {
            html_to_markdown_rs::LinkStyle::Inline => Self::Inline,
            html_to_markdown_rs::LinkStyle::Reference => Self::Reference,
        }
    }
}

impl From<JsLinkType> for xberg::LinkType {
    fn from(val: JsLinkType) -> Self {
        match val {
            JsLinkType::Anchor => Self::Anchor,
            JsLinkType::Internal => Self::Internal,
            JsLinkType::External => Self::External,
            JsLinkType::Email => Self::Email,
            JsLinkType::Phone => Self::Phone,
            JsLinkType::Other => Self::Other,
        }
    }
}

impl From<xberg::LinkType> for JsLinkType {
    fn from(val: xberg::LinkType) -> Self {
        match val {
            xberg::LinkType::Anchor => Self::Anchor,
            xberg::LinkType::Internal => Self::Internal,
            xberg::LinkType::External => Self::External,
            xberg::LinkType::Email => Self::Email,
            xberg::LinkType::Phone => Self::Phone,
            xberg::LinkType::Other => Self::Other,
        }
    }
}

impl From<JsListIndentType> for html_to_markdown_rs::ListIndentType {
    fn from(val: JsListIndentType) -> Self {
        match val {
            JsListIndentType::Spaces => Self::Spaces,
            JsListIndentType::Tabs => Self::Tabs,
        }
    }
}

impl From<html_to_markdown_rs::ListIndentType> for JsListIndentType {
    fn from(val: html_to_markdown_rs::ListIndentType) -> Self {
        match val {
            html_to_markdown_rs::ListIndentType::Spaces => Self::Spaces,
            html_to_markdown_rs::ListIndentType::Tabs => Self::Tabs,
        }
    }
}

impl From<xberg::extraction::transform::ListType> for JsListType {
    fn from(val: xberg::extraction::transform::ListType) -> Self {
        match val {
            xberg::extraction::transform::ListType::Bullet => Self::Bullet,
            xberg::extraction::transform::ListType::Numbered => Self::Numbered,
            xberg::extraction::transform::ListType::Lettered => Self::Lettered,
            xberg::extraction::transform::ListType::Indented => Self::Indented,
        }
    }
}

impl From<JsMergeMode> for xberg::MergeMode {
    fn from(val: JsMergeMode) -> Self {
        match val {
            JsMergeMode::ObjectMerge => Self::ObjectMerge,
            JsMergeMode::ArrayConcat => Self::ArrayConcat,
            JsMergeMode::ObjectFirst => Self::ObjectFirst,
        }
    }
}

impl From<xberg::MergeMode> for JsMergeMode {
    fn from(val: xberg::MergeMode) -> Self {
        match val {
            xberg::MergeMode::ObjectMerge => Self::ObjectMerge,
            xberg::MergeMode::ArrayConcat => Self::ArrayConcat,
            xberg::MergeMode::ObjectFirst => Self::ObjectFirst,
        }
    }
}

impl From<JsMimeDetectionPolicy> for xberg::MimeDetectionPolicy {
    fn from(val: JsMimeDetectionPolicy) -> Self {
        match val {
            JsMimeDetectionPolicy::PreferContent => Self::PreferContent,
            JsMimeDetectionPolicy::TrustExtension => Self::TrustExtension,
            JsMimeDetectionPolicy::ContentOnly => Self::ContentOnly,
        }
    }
}

impl From<xberg::MimeDetectionPolicy> for JsMimeDetectionPolicy {
    fn from(val: xberg::MimeDetectionPolicy) -> Self {
        match val {
            xberg::MimeDetectionPolicy::PreferContent => Self::PreferContent,
            xberg::MimeDetectionPolicy::TrustExtension => Self::TrustExtension,
            xberg::MimeDetectionPolicy::ContentOnly => Self::ContentOnly,
        }
    }
}

impl From<JsNerBackendKind> for xberg::NerBackendKind {
    fn from(val: JsNerBackendKind) -> Self {
        match val {
            JsNerBackendKind::Onnx => Self::Onnx,
            JsNerBackendKind::Llm => Self::Llm,
        }
    }
}

impl From<xberg::NerBackendKind> for JsNerBackendKind {
    fn from(val: xberg::NerBackendKind) -> Self {
        match val {
            xberg::NerBackendKind::Onnx => Self::Onnx,
            xberg::NerBackendKind::Llm => Self::Llm,
        }
    }
}

impl From<JsNewlineStyle> for html_to_markdown_rs::NewlineStyle {
    fn from(val: JsNewlineStyle) -> Self {
        match val {
            JsNewlineStyle::Spaces => Self::Spaces,
            JsNewlineStyle::Backslash => Self::Backslash,
        }
    }
}

impl From<html_to_markdown_rs::NewlineStyle> for JsNewlineStyle {
    fn from(val: html_to_markdown_rs::NewlineStyle) -> Self {
        match val {
            html_to_markdown_rs::NewlineStyle::Spaces => Self::Spaces,
            html_to_markdown_rs::NewlineStyle::Backslash => Self::Backslash,
        }
    }
}

impl From<JsNoChunkingReason> for xberg::NoChunkingReason {
    fn from(val: JsNoChunkingReason) -> Self {
        match val.type_tag.as_str() {
            "SmallFile" => Self::SmallFile {
                size_bytes: val.size_bytes.map(|v| v as u64).unwrap_or_default(),
                threshold_bytes: val.threshold_bytes.map(|v| v as u64).unwrap_or_default(),
            },
            "FewPages" => Self::FewPages {
                page_count: val.page_count.unwrap_or_default(),
                threshold: val.threshold.unwrap_or_default(),
            },
            "TextLayerDetected" => Self::TextLayerDetected {
                text_coverage: val.text_coverage.map(|v| v as f32).unwrap_or_default(),
                avg_chars_per_page: val.avg_chars_per_page.unwrap_or_default(),
            },
            "FormatNotChunkable" => Self::FormatNotChunkable {
                mime_type: val.mime_type.unwrap_or_default(),
            },
            "ChunkingDisabled" => Self::ChunkingDisabled,
            "FastTextExtraction" => Self::FastTextExtraction,
            _ => Self::SmallFile {
                size_bytes: Default::default(),
                threshold_bytes: Default::default(),
            },
        }
    }
}

impl From<xberg::NoChunkingReason> for JsNoChunkingReason {
    fn from(val: xberg::NoChunkingReason) -> Self {
        match val {
            xberg::NoChunkingReason::SmallFile {
                size_bytes,
                threshold_bytes,
            } => Self {
                type_tag: "SmallFile".to_string(),
                avg_chars_per_page: None,
                mime_type: None,
                page_count: None,
                size_bytes: Some(size_bytes as i64),
                text_coverage: None,
                threshold: None,
                threshold_bytes: Some(threshold_bytes as i64),
            },
            xberg::NoChunkingReason::FewPages { page_count, threshold } => Self {
                type_tag: "FewPages".to_string(),
                avg_chars_per_page: None,
                mime_type: None,
                page_count: Some(page_count),
                size_bytes: None,
                text_coverage: None,
                threshold: Some(threshold),
                threshold_bytes: None,
            },
            xberg::NoChunkingReason::TextLayerDetected {
                text_coverage,
                avg_chars_per_page,
            } => Self {
                type_tag: "TextLayerDetected".to_string(),
                avg_chars_per_page: Some(avg_chars_per_page),
                mime_type: None,
                page_count: None,
                size_bytes: None,
                text_coverage: Some(text_coverage as f64),
                threshold: None,
                threshold_bytes: None,
            },
            xberg::NoChunkingReason::FormatNotChunkable { mime_type } => Self {
                type_tag: "FormatNotChunkable".to_string(),
                avg_chars_per_page: None,
                mime_type: Some(mime_type),
                page_count: None,
                size_bytes: None,
                text_coverage: None,
                threshold: None,
                threshold_bytes: None,
            },
            xberg::NoChunkingReason::ChunkingDisabled => Self {
                type_tag: "ChunkingDisabled".to_string(),
                avg_chars_per_page: None,
                mime_type: None,
                page_count: None,
                size_bytes: None,
                text_coverage: None,
                threshold: None,
                threshold_bytes: None,
            },
            xberg::NoChunkingReason::FastTextExtraction => Self {
                type_tag: "FastTextExtraction".to_string(),
                avg_chars_per_page: None,
                mime_type: None,
                page_count: None,
                size_bytes: None,
                text_coverage: None,
                threshold: None,
                threshold_bytes: None,
            },
        }
    }
}

impl From<JsNodeContent> for xberg::NodeContent {
    fn from(val: JsNodeContent) -> Self {
        match val.node_type_tag.as_str() {
            "title" => Self::Title {
                text: val.text.unwrap_or_default(),
            },
            "heading" => Self::Heading {
                level: val.level.unwrap_or_default(),
                text: val.text.unwrap_or_default(),
            },
            "paragraph" => Self::Paragraph {
                text: val.text.unwrap_or_default(),
            },
            "list" => Self::List {
                ordered: val.ordered.unwrap_or_default(),
            },
            "list_item" => Self::ListItem {
                text: val.text.unwrap_or_default(),
            },
            "table" => Self::Table {
                grid: val.grid.map(|v| v.into()).unwrap_or_default(),
            },
            "image" => Self::Image {
                description: val.description,
                image_index: val.image_index,
                src: val.src,
            },
            "code" => Self::Code {
                text: val.text.unwrap_or_default(),
                language: val.language,
            },
            "quote" => Self::Quote,
            "formula" => Self::Formula {
                text: val.text.unwrap_or_default(),
            },
            "footnote" => Self::Footnote {
                text: val.text.unwrap_or_default(),
            },
            "comment" => Self::Comment {
                text: val.text.unwrap_or_default(),
            },
            "group" => Self::Group {
                label: val.label,
                heading_level: val.heading_level,
                heading_text: val.heading_text,
            },
            "page_break" => Self::PageBreak,
            "slide" => Self::Slide {
                number: val.number.unwrap_or_default(),
                title: val.title,
            },
            "definition_list" => Self::DefinitionList,
            "definition_item" => Self::DefinitionItem {
                term: val.term.unwrap_or_default(),
                definition: val.definition.unwrap_or_default(),
            },
            "citation" => Self::Citation {
                key: val.key.unwrap_or_default(),
                text: val.text.unwrap_or_default(),
            },
            "admonition" => Self::Admonition {
                kind: val.kind.unwrap_or_default(),
                title: val.title,
            },
            "raw_block" => Self::RawBlock {
                format: val.format.unwrap_or_default(),
                content: val.content.unwrap_or_default(),
            },
            "metadata_block" => Self::MetadataBlock {
                entries: val
                    .entries
                    .map(|v| v.into_iter().map(Into::into).collect())
                    .unwrap_or_default(),
            },
            _ => Self::Title {
                text: Default::default(),
            },
        }
    }
}

impl From<xberg::NodeContent> for JsNodeContent {
    fn from(val: xberg::NodeContent) -> Self {
        match val {
            xberg::NodeContent::Title { text } => Self {
                node_type_tag: "title".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Heading { level, text } => Self {
                node_type_tag: "heading".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: Some(level),
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Paragraph { text } => Self {
                node_type_tag: "paragraph".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::List { ordered } => Self {
                node_type_tag: "list".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: Some(ordered),
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::ListItem { text } => Self {
                node_type_tag: "list_item".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Table { grid } => Self {
                node_type_tag: "table".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: Some((grid).into()),
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::Image {
                description,
                image_index,
                src,
            } => Self {
                node_type_tag: "image".to_string(),
                content: None,
                definition: None,
                description,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::Code { text, language } => Self {
                node_type_tag: "code".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Quote => Self {
                node_type_tag: "quote".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::Formula { text } => Self {
                node_type_tag: "formula".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Footnote { text } => Self {
                node_type_tag: "footnote".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Comment { text } => Self {
                node_type_tag: "comment".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Group {
                label,
                heading_level,
                heading_text,
            } => Self {
                node_type_tag: "group".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level,
                heading_text,
                image_index: None,
                key: None,
                kind: None,
                label,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::PageBreak => Self {
                node_type_tag: "page_break".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::Slide { number, title } => Self {
                node_type_tag: "slide".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: Some(number),
                ordered: None,
                src: None,
                term: None,
                text: None,
                title,
            },
            xberg::NodeContent::DefinitionList => Self {
                node_type_tag: "definition_list".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::DefinitionItem { term, definition } => Self {
                node_type_tag: "definition_item".to_string(),
                content: None,
                definition: Some(definition),
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: Some(term),
                text: None,
                title: None,
            },
            xberg::NodeContent::Citation { key, text } => Self {
                node_type_tag: "citation".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: Some(key),
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: Some(text),
                title: None,
            },
            xberg::NodeContent::Admonition { kind, title } => Self {
                node_type_tag: "admonition".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: None,
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: Some(kind),
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title,
            },
            xberg::NodeContent::RawBlock { format, content } => Self {
                node_type_tag: "raw_block".to_string(),
                content: Some(content),
                definition: None,
                description: None,
                entries: None,
                format: Some(format),
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
            xberg::NodeContent::MetadataBlock { entries } => Self {
                node_type_tag: "metadata_block".to_string(),
                content: None,
                definition: None,
                description: None,
                entries: Some(entries.into_iter().map(Into::into).collect()),
                format: None,
                grid: None,
                heading_level: None,
                heading_text: None,
                image_index: None,
                key: None,
                kind: None,
                label: None,
                language: None,
                level: None,
                number: None,
                ordered: None,
                src: None,
                term: None,
                text: None,
                title: None,
            },
        }
    }
}

impl From<xberg::OcrBackendType> for JsOcrBackendType {
    fn from(val: xberg::OcrBackendType) -> Self {
        match val {
            xberg::OcrBackendType::Tesseract => Self::Tesseract,
            xberg::OcrBackendType::PaddleOCR => Self::PaddleOCR,
            xberg::OcrBackendType::Candle => Self::Candle,
            xberg::OcrBackendType::Custom => Self::Custom,
        }
    }
}

impl From<JsOcrBoundingGeometry> for xberg::OcrBoundingGeometry {
    fn from(val: JsOcrBoundingGeometry) -> Self {
        match val.type_tag.as_str() {
            "rectangle" => Self::Rectangle {
                left: val.left.unwrap_or_default(),
                top: val.top.unwrap_or_default(),
                width: val.width.unwrap_or_default(),
                height: val.height.unwrap_or_default(),
            },
            "quadrilateral" => Self::Quadrilateral {
                points: val
                    .points
                    .map(|v| v.into_iter().map(Into::into).collect())
                    .unwrap_or_default(),
            },
            _ => Self::Rectangle {
                left: Default::default(),
                top: Default::default(),
                width: Default::default(),
                height: Default::default(),
            },
        }
    }
}

impl From<xberg::OcrBoundingGeometry> for JsOcrBoundingGeometry {
    fn from(val: xberg::OcrBoundingGeometry) -> Self {
        match val {
            xberg::OcrBoundingGeometry::Rectangle {
                left,
                top,
                width,
                height,
            } => Self {
                type_tag: "rectangle".to_string(),
                height: Some(height),
                left: Some(left),
                points: None,
                top: Some(top),
                width: Some(width),
            },
            xberg::OcrBoundingGeometry::Quadrilateral { points } => Self {
                type_tag: "quadrilateral".to_string(),
                height: None,
                left: None,
                points: Some(points.into_iter().map(Into::into).collect()),
                top: None,
                width: None,
            },
        }
    }
}

impl From<JsOcrElementLevel> for xberg::OcrElementLevel {
    fn from(val: JsOcrElementLevel) -> Self {
        match val {
            JsOcrElementLevel::Word => Self::Word,
            JsOcrElementLevel::Line => Self::Line,
            JsOcrElementLevel::Block => Self::Block,
            JsOcrElementLevel::Page => Self::Page,
        }
    }
}

impl From<xberg::OcrElementLevel> for JsOcrElementLevel {
    fn from(val: xberg::OcrElementLevel) -> Self {
        match val {
            xberg::OcrElementLevel::Word => Self::Word,
            xberg::OcrElementLevel::Line => Self::Line,
            xberg::OcrElementLevel::Block => Self::Block,
            xberg::OcrElementLevel::Page => Self::Page,
        }
    }
}

impl From<JsOcrStrategy> for xberg::OcrStrategy {
    fn from(val: JsOcrStrategy) -> Self {
        match val.mode_tag.as_str() {
            "auto" => Self::Auto,
            "scanned_pages" => Self::ScannedPages {
                min_confidence: val.min_confidence.unwrap_or_default(),
            },
            _ => Self::Auto,
        }
    }
}

impl From<xberg::OcrStrategy> for JsOcrStrategy {
    fn from(val: xberg::OcrStrategy) -> Self {
        match val {
            xberg::OcrStrategy::Auto => Self {
                mode_tag: "auto".to_string(),
                min_confidence: None,
            },
            xberg::OcrStrategy::ScannedPages { min_confidence } => Self {
                mode_tag: "scanned_pages".to_string(),
                min_confidence: Some(min_confidence),
            },
        }
    }
}

impl From<JsOutputFormat> for xberg::OutputFormat {
    fn from(val: JsOutputFormat) -> Self {
        serde_json::from_value(val.0).unwrap_or_default()
    }
}

impl From<xberg::OutputFormat> for JsOutputFormat {
    fn from(val: xberg::OutputFormat) -> Self {
        Self(serde_json::to_value(val).unwrap_or_default())
    }
}

impl From<xberg::PSMMode> for JsPSMMode {
    fn from(val: xberg::PSMMode) -> Self {
        match val {
            xberg::PSMMode::OsdOnly => Self::OsdOnly,
            xberg::PSMMode::AutoOsd => Self::AutoOsd,
            xberg::PSMMode::AutoOnly => Self::AutoOnly,
            xberg::PSMMode::Auto => Self::Auto,
            xberg::PSMMode::SingleColumn => Self::SingleColumn,
            xberg::PSMMode::SingleBlockVertical => Self::SingleBlockVertical,
            xberg::PSMMode::SingleBlock => Self::SingleBlock,
            xberg::PSMMode::SingleLine => Self::SingleLine,
            xberg::PSMMode::SingleWord => Self::SingleWord,
            xberg::PSMMode::CircleWord => Self::CircleWord,
            xberg::PSMMode::SingleChar => Self::SingleChar,
        }
    }
}

impl From<JsPaddleInferenceBackend> for xberg::PaddleInferenceBackend {
    fn from(val: JsPaddleInferenceBackend) -> Self {
        match val {
            JsPaddleInferenceBackend::Ort => Self::Ort,
            JsPaddleInferenceBackend::Tract => Self::Tract,
        }
    }
}

impl From<xberg::PaddleInferenceBackend> for JsPaddleInferenceBackend {
    fn from(val: xberg::PaddleInferenceBackend) -> Self {
        match val {
            xberg::PaddleInferenceBackend::Ort => Self::Ort,
            xberg::PaddleInferenceBackend::Tract => Self::Tract,
        }
    }
}

impl From<xberg::PaddleLanguage> for JsPaddleLanguage {
    fn from(val: xberg::PaddleLanguage) -> Self {
        match val {
            xberg::PaddleLanguage::English => Self::English,
            xberg::PaddleLanguage::Chinese => Self::Chinese,
            xberg::PaddleLanguage::Japanese => Self::Japanese,
            xberg::PaddleLanguage::Korean => Self::Korean,
            xberg::PaddleLanguage::German => Self::German,
            xberg::PaddleLanguage::French => Self::French,
            xberg::PaddleLanguage::Latin => Self::Latin,
            xberg::PaddleLanguage::Cyrillic => Self::Cyrillic,
            xberg::PaddleLanguage::TraditionalChinese => Self::TraditionalChinese,
            xberg::PaddleLanguage::Thai => Self::Thai,
            xberg::PaddleLanguage::Greek => Self::Greek,
            xberg::PaddleLanguage::EastSlavic => Self::EastSlavic,
            xberg::PaddleLanguage::Arabic => Self::Arabic,
            xberg::PaddleLanguage::Devanagari => Self::Devanagari,
            xberg::PaddleLanguage::Tamil => Self::Tamil,
            xberg::PaddleLanguage::Telugu => Self::Telugu,
        }
    }
}

impl From<xberg::candle_ocr::PaddleOcrVlTaskKind> for JsPaddleOcrVlTaskKind {
    fn from(val: xberg::candle_ocr::PaddleOcrVlTaskKind) -> Self {
        match val {
            xberg::candle_ocr::PaddleOcrVlTaskKind::Ocr => Self::Ocr,
            xberg::candle_ocr::PaddleOcrVlTaskKind::Table => Self::Table,
            xberg::candle_ocr::PaddleOcrVlTaskKind::Formula => Self::Formula,
            xberg::candle_ocr::PaddleOcrVlTaskKind::Chart => Self::Chart,
        }
    }
}

impl From<xberg::PageOrientationHandling> for JsPageOrientationHandling {
    fn from(val: xberg::PageOrientationHandling) -> Self {
        match val {
            xberg::PageOrientationHandling::SelfCorrecting => Self::SelfCorrecting,
            xberg::PageOrientationHandling::RecognisesRotatedText => Self::RecognisesRotatedText,
            xberg::PageOrientationHandling::RequiresUpright => Self::RequiresUpright,
        }
    }
}

impl From<JsPageUnitType> for xberg::PageUnitType {
    fn from(val: JsPageUnitType) -> Self {
        match val {
            JsPageUnitType::Page => Self::Page,
            JsPageUnitType::Slide => Self::Slide,
            JsPageUnitType::Sheet => Self::Sheet,
        }
    }
}

impl From<xberg::PageUnitType> for JsPageUnitType {
    fn from(val: xberg::PageUnitType) -> Self {
        match val {
            xberg::PageUnitType::Page => Self::Page,
            xberg::PageUnitType::Slide => Self::Slide,
            xberg::PageUnitType::Sheet => Self::Sheet,
        }
    }
}

impl From<JsPdfAnnotationType> for xberg::PdfAnnotationType {
    fn from(val: JsPdfAnnotationType) -> Self {
        match val {
            JsPdfAnnotationType::Text => Self::Text,
            JsPdfAnnotationType::Highlight => Self::Highlight,
            JsPdfAnnotationType::Link => Self::Link,
            JsPdfAnnotationType::Stamp => Self::Stamp,
            JsPdfAnnotationType::Underline => Self::Underline,
            JsPdfAnnotationType::StrikeOut => Self::StrikeOut,
            JsPdfAnnotationType::Squiggly => Self::Squiggly,
            JsPdfAnnotationType::Ink => Self::Ink,
            JsPdfAnnotationType::Square => Self::Square,
            JsPdfAnnotationType::Circle => Self::Circle,
            JsPdfAnnotationType::Polygon => Self::Polygon,
            JsPdfAnnotationType::PolyLine => Self::PolyLine,
            JsPdfAnnotationType::Line => Self::Line,
            JsPdfAnnotationType::Caret => Self::Caret,
            JsPdfAnnotationType::FileAttachment => Self::FileAttachment,
            JsPdfAnnotationType::Sound => Self::Sound,
            JsPdfAnnotationType::Movie => Self::Movie,
            JsPdfAnnotationType::Other => Self::Other,
        }
    }
}

impl From<xberg::PdfAnnotationType> for JsPdfAnnotationType {
    fn from(val: xberg::PdfAnnotationType) -> Self {
        match val {
            xberg::PdfAnnotationType::Text => Self::Text,
            xberg::PdfAnnotationType::Highlight => Self::Highlight,
            xberg::PdfAnnotationType::Link => Self::Link,
            xberg::PdfAnnotationType::Stamp => Self::Stamp,
            xberg::PdfAnnotationType::Underline => Self::Underline,
            xberg::PdfAnnotationType::StrikeOut => Self::StrikeOut,
            xberg::PdfAnnotationType::Squiggly => Self::Squiggly,
            xberg::PdfAnnotationType::Ink => Self::Ink,
            xberg::PdfAnnotationType::Square => Self::Square,
            xberg::PdfAnnotationType::Circle => Self::Circle,
            xberg::PdfAnnotationType::Polygon => Self::Polygon,
            xberg::PdfAnnotationType::PolyLine => Self::PolyLine,
            xberg::PdfAnnotationType::Line => Self::Line,
            xberg::PdfAnnotationType::Caret => Self::Caret,
            xberg::PdfAnnotationType::FileAttachment => Self::FileAttachment,
            xberg::PdfAnnotationType::Sound => Self::Sound,
            xberg::PdfAnnotationType::Movie => Self::Movie,
            xberg::PdfAnnotationType::Other => Self::Other,
        }
    }
}

impl From<JsPdfBackend> for xberg::PdfBackend {
    fn from(val: JsPdfBackend) -> Self {
        match val {
            JsPdfBackend::Native => Self::Native,
            JsPdfBackend::Pdfium => Self::Pdfium,
        }
    }
}

impl From<xberg::PdfBackend> for JsPdfBackend {
    fn from(val: xberg::PdfBackend) -> Self {
        match val {
            xberg::PdfBackend::Native => Self::Native,
            xberg::PdfBackend::Pdfium => Self::Pdfium,
        }
    }
}

impl From<JsPiiCategory> for xberg::PiiCategory {
    fn from(val: JsPiiCategory) -> Self {
        match val.type_tag.as_str() {
            "email" => Self::Email,
            "phone" => Self::Phone,
            "ssn" => Self::Ssn,
            "credit_card" => Self::CreditCard,
            "postal_code" => Self::PostalCode,
            "ip_address" => Self::IpAddress,
            "iban" => Self::Iban,
            "swift_bic" => Self::SwiftBic,
            "date_of_birth" => Self::DateOfBirth,
            "person" => Self::Person,
            "organization" => Self::Organization,
            "location" => Self::Location,
            "custom" => Self::Custom(val.custom.unwrap_or_default()),
            _ => Self::Email,
        }
    }
}

impl From<xberg::PiiCategory> for JsPiiCategory {
    fn from(val: xberg::PiiCategory) -> Self {
        match val {
            xberg::PiiCategory::Email => Self {
                type_tag: "email".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Phone => Self {
                type_tag: "phone".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Ssn => Self {
                type_tag: "ssn".to_string(),
                custom: None,
            },
            xberg::PiiCategory::CreditCard => Self {
                type_tag: "credit_card".to_string(),
                custom: None,
            },
            xberg::PiiCategory::PostalCode => Self {
                type_tag: "postal_code".to_string(),
                custom: None,
            },
            xberg::PiiCategory::IpAddress => Self {
                type_tag: "ip_address".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Iban => Self {
                type_tag: "iban".to_string(),
                custom: None,
            },
            xberg::PiiCategory::SwiftBic => Self {
                type_tag: "swift_bic".to_string(),
                custom: None,
            },
            xberg::PiiCategory::DateOfBirth => Self {
                type_tag: "date_of_birth".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Person => Self {
                type_tag: "person".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Organization => Self {
                type_tag: "organization".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Location => Self {
                type_tag: "location".to_string(),
                custom: None,
            },
            xberg::PiiCategory::Custom(custom) => Self {
                type_tag: "custom".to_string(),
                custom: Some(custom),
            },
        }
    }
}

impl From<JsPreprocessingPreset> for html_to_markdown_rs::PreprocessingPreset {
    fn from(val: JsPreprocessingPreset) -> Self {
        match val {
            JsPreprocessingPreset::Minimal => Self::Minimal,
            JsPreprocessingPreset::Standard => Self::Standard,
            JsPreprocessingPreset::Aggressive => Self::Aggressive,
        }
    }
}

impl From<html_to_markdown_rs::PreprocessingPreset> for JsPreprocessingPreset {
    fn from(val: html_to_markdown_rs::PreprocessingPreset) -> Self {
        match val {
            html_to_markdown_rs::PreprocessingPreset::Minimal => Self::Minimal,
            html_to_markdown_rs::PreprocessingPreset::Standard => Self::Standard,
            html_to_markdown_rs::PreprocessingPreset::Aggressive => Self::Aggressive,
        }
    }
}

impl From<JsPresetCategory> for xberg::PresetCategory {
    fn from(val: JsPresetCategory) -> Self {
        match val {
            JsPresetCategory::Finance => Self::Finance,
            JsPresetCategory::Identity => Self::Identity,
            JsPresetCategory::Legal => Self::Legal,
            JsPresetCategory::Logistics => Self::Logistics,
            JsPresetCategory::Medical => Self::Medical,
            JsPresetCategory::Hr => Self::Hr,
            JsPresetCategory::Other => Self::Other,
        }
    }
}

impl From<xberg::PresetCategory> for JsPresetCategory {
    fn from(val: xberg::PresetCategory) -> Self {
        match val {
            xberg::PresetCategory::Finance => Self::Finance,
            xberg::PresetCategory::Identity => Self::Identity,
            xberg::PresetCategory::Legal => Self::Legal,
            xberg::PresetCategory::Logistics => Self::Logistics,
            xberg::PresetCategory::Medical => Self::Medical,
            xberg::PresetCategory::Hr => Self::Hr,
            xberg::PresetCategory::Other => Self::Other,
        }
    }
}

impl From<JsProbeStatus> for xberg::ProbeStatus {
    fn from(val: JsProbeStatus) -> Self {
        match val {
            JsProbeStatus::Pass => Self::Pass,
            JsProbeStatus::Warn => Self::Warn,
            JsProbeStatus::Fail => Self::Fail,
            JsProbeStatus::Skip => Self::Skip,
        }
    }
}

impl From<xberg::ProbeStatus> for JsProbeStatus {
    fn from(val: xberg::ProbeStatus) -> Self {
        match val {
            xberg::ProbeStatus::Pass => Self::Pass,
            xberg::ProbeStatus::Warn => Self::Warn,
            xberg::ProbeStatus::Fail => Self::Fail,
            xberg::ProbeStatus::Skip => Self::Skip,
        }
    }
}

impl From<xberg::ProcessingStage> for JsProcessingStage {
    fn from(val: xberg::ProcessingStage) -> Self {
        match val {
            xberg::ProcessingStage::Early => Self::Early,
            xberg::ProcessingStage::Middle => Self::Middle,
            xberg::ProcessingStage::Late => Self::Late,
        }
    }
}

impl From<JsRedactionStrategy> for xberg::RedactionStrategy {
    fn from(val: JsRedactionStrategy) -> Self {
        match val {
            JsRedactionStrategy::Mask => Self::Mask,
            JsRedactionStrategy::Hash => Self::Hash,
            JsRedactionStrategy::TokenReplace => Self::TokenReplace,
            JsRedactionStrategy::Drop => Self::Drop,
        }
    }
}

impl From<xberg::RedactionStrategy> for JsRedactionStrategy {
    fn from(val: xberg::RedactionStrategy) -> Self {
        match val {
            xberg::RedactionStrategy::Mask => Self::Mask,
            xberg::RedactionStrategy::Hash => Self::Hash,
            xberg::RedactionStrategy::TokenReplace => Self::TokenReplace,
            xberg::RedactionStrategy::Drop => Self::Drop,
        }
    }
}

impl From<JsReductionLevel> for xberg::ReductionLevel {
    fn from(val: JsReductionLevel) -> Self {
        match val {
            JsReductionLevel::Off => Self::Off,
            JsReductionLevel::Light => Self::Light,
            JsReductionLevel::Moderate => Self::Moderate,
            JsReductionLevel::Aggressive => Self::Aggressive,
            JsReductionLevel::Maximum => Self::Maximum,
        }
    }
}

impl From<xberg::ReductionLevel> for JsReductionLevel {
    fn from(val: xberg::ReductionLevel) -> Self {
        match val {
            xberg::ReductionLevel::Off => Self::Off,
            xberg::ReductionLevel::Light => Self::Light,
            xberg::ReductionLevel::Moderate => Self::Moderate,
            xberg::ReductionLevel::Aggressive => Self::Aggressive,
            xberg::ReductionLevel::Maximum => Self::Maximum,
        }
    }
}

impl From<xberg::RegionKind> for JsRegionKind {
    fn from(val: xberg::RegionKind) -> Self {
        match val {
            xberg::RegionKind::Figure => Self::Figure,
            xberg::RegionKind::DenseTable => Self::DenseTable,
            xberg::RegionKind::ComplexLayout => Self::ComplexLayout,
            xberg::RegionKind::Caption => Self::Caption,
        }
    }
}

impl From<JsRelationshipKind> for xberg::RelationshipKind {
    fn from(val: JsRelationshipKind) -> Self {
        match val {
            JsRelationshipKind::FootnoteReference => Self::FootnoteReference,
            JsRelationshipKind::CitationReference => Self::CitationReference,
            JsRelationshipKind::InternalLink => Self::InternalLink,
            JsRelationshipKind::Caption => Self::Caption,
            JsRelationshipKind::Label => Self::Label,
            JsRelationshipKind::TocEntry => Self::TocEntry,
            JsRelationshipKind::CrossReference => Self::CrossReference,
        }
    }
}

impl From<xberg::RelationshipKind> for JsRelationshipKind {
    fn from(val: xberg::RelationshipKind) -> Self {
        match val {
            xberg::RelationshipKind::FootnoteReference => Self::FootnoteReference,
            xberg::RelationshipKind::CitationReference => Self::CitationReference,
            xberg::RelationshipKind::InternalLink => Self::InternalLink,
            xberg::RelationshipKind::Caption => Self::Caption,
            xberg::RelationshipKind::Label => Self::Label,
            xberg::RelationshipKind::TocEntry => Self::TocEntry,
            xberg::RelationshipKind::CrossReference => Self::CrossReference,
        }
    }
}

impl From<JsRerankerHead> for xberg::RerankerHead {
    fn from(val: JsRerankerHead) -> Self {
        match val {
            JsRerankerHead::CrossEncoder => Self::CrossEncoder,
            JsRerankerHead::Qwen3Generative => Self::Qwen3Generative,
        }
    }
}

impl From<xberg::RerankerHead> for JsRerankerHead {
    fn from(val: xberg::RerankerHead) -> Self {
        match val {
            xberg::RerankerHead::CrossEncoder => Self::CrossEncoder,
            xberg::RerankerHead::Qwen3Generative => Self::Qwen3Generative,
        }
    }
}

impl From<JsRerankerModelType> for xberg::RerankerModelType {
    fn from(val: JsRerankerModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => Self::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => Self::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.unwrap_or_default(),
                max_length: val.max_length,
                head: val.head.map(|v| v.into()).unwrap_or_default(),
            },
            "llm" => Self::Llm {
                llm: Box::new(val.llm.map(|v| v.into()).unwrap_or_default()),
            },
            "plugin" => Self::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => Self::Preset {
                name: Default::default(),
            },
        }
    }
}

impl From<xberg::RerankerModelType> for JsRerankerModelType {
    fn from(val: xberg::RerankerModelType) -> Self {
        match val {
            xberg::RerankerModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                additional_files: None,
                head: None,
                llm: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
            xberg::RerankerModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
                head,
            } => Self {
                type_tag: "custom".to_string(),
                additional_files: Some(additional_files),
                head: Some((head).into()),
                llm: None,
                max_length,
                model_file,
                model_id: Some(model_id),
                name: None,
            },
            xberg::RerankerModelType::Llm { llm } => Self {
                type_tag: "llm".to_string(),
                additional_files: None,
                head: None,
                llm: Some((*llm).into()),
                max_length: None,
                model_file: None,
                model_id: None,
                name: None,
            },
            xberg::RerankerModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                additional_files: None,
                head: None,
                llm: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
        }
    }
}

impl From<JsResultFormat> for xberg::ResultFormat {
    fn from(val: JsResultFormat) -> Self {
        match val {
            JsResultFormat::Unified => Self::Unified,
            JsResultFormat::ElementBased => Self::ElementBased,
        }
    }
}

impl From<xberg::ResultFormat> for JsResultFormat {
    fn from(val: xberg::ResultFormat) -> Self {
        match val {
            xberg::ResultFormat::Unified => Self::Unified,
            xberg::ResultFormat::ElementBased => Self::ElementBased,
        }
    }
}

impl From<JsRevisionAnchor> for xberg::RevisionAnchor {
    fn from(val: JsRevisionAnchor) -> Self {
        match val.type_tag.as_str() {
            "paragraph" => Self::Paragraph {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "table_cell" => Self::TableCell {
                row: val.row.map(|v| v as usize).unwrap_or_default(),
                col: val.col.map(|v| v as usize).unwrap_or_default(),
                table_index: val.table_index.map(|v| v as usize).unwrap_or_default(),
            },
            "page" => Self::Page {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "slide" => Self::Slide {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
            },
            "sheet" => Self::Sheet {
                index: val.index.map(|v| v as usize).unwrap_or_default(),
                name: val.name,
            },
            _ => Self::Paragraph {
                index: Default::default(),
            },
        }
    }
}

impl From<xberg::RevisionAnchor> for JsRevisionAnchor {
    fn from(val: xberg::RevisionAnchor) -> Self {
        match val {
            xberg::RevisionAnchor::Paragraph { index } => Self {
                type_tag: "paragraph".to_string(),
                col: None,
                index: Some(index as i64),
                name: None,
                row: None,
                table_index: None,
            },
            xberg::RevisionAnchor::TableCell { row, col, table_index } => Self {
                type_tag: "table_cell".to_string(),
                col: Some(col as i64),
                index: None,
                name: None,
                row: Some(row as i64),
                table_index: Some(table_index as i64),
            },
            xberg::RevisionAnchor::Page { index } => Self {
                type_tag: "page".to_string(),
                col: None,
                index: Some(index as i64),
                name: None,
                row: None,
                table_index: None,
            },
            xberg::RevisionAnchor::Slide { index } => Self {
                type_tag: "slide".to_string(),
                col: None,
                index: Some(index as i64),
                name: None,
                row: None,
                table_index: None,
            },
            xberg::RevisionAnchor::Sheet { index, name } => Self {
                type_tag: "sheet".to_string(),
                col: None,
                index: Some(index as i64),
                name,
                row: None,
                table_index: None,
            },
        }
    }
}

impl From<JsRevisionKind> for xberg::RevisionKind {
    fn from(val: JsRevisionKind) -> Self {
        match val {
            JsRevisionKind::Insertion => Self::Insertion,
            JsRevisionKind::Deletion => Self::Deletion,
            JsRevisionKind::FormatChange => Self::FormatChange,
            JsRevisionKind::Comment => Self::Comment,
        }
    }
}

impl From<xberg::RevisionKind> for JsRevisionKind {
    fn from(val: xberg::RevisionKind) -> Self {
        match val {
            xberg::RevisionKind::Insertion => Self::Insertion,
            xberg::RevisionKind::Deletion => Self::Deletion,
            xberg::RevisionKind::FormatChange => Self::FormatChange,
            xberg::RevisionKind::Comment => Self::Comment,
        }
    }
}

impl From<JsSchemaCompliance> for xberg::SchemaCompliance {
    fn from(val: JsSchemaCompliance) -> Self {
        match val {
            JsSchemaCompliance::AllValid => Self::AllValid,
            JsSchemaCompliance::PartialValid => Self::PartialValid,
            JsSchemaCompliance::AllInvalid => Self::AllInvalid,
        }
    }
}

impl From<xberg::SchemaCompliance> for JsSchemaCompliance {
    fn from(val: xberg::SchemaCompliance) -> Self {
        match val {
            xberg::SchemaCompliance::AllValid => Self::AllValid,
            xberg::SchemaCompliance::PartialValid => Self::PartialValid,
            xberg::SchemaCompliance::AllInvalid => Self::AllInvalid,
        }
    }
}

impl From<JsSparseEmbeddingModelType> for xberg::SparseEmbeddingModelType {
    fn from(val: JsSparseEmbeddingModelType) -> Self {
        match val.type_tag.as_str() {
            "preset" => Self::Preset {
                name: val.name.unwrap_or_default(),
            },
            "custom" => Self::Custom {
                model_id: val.model_id.unwrap_or_default(),
                model_file: val.model_file,
                additional_files: val.additional_files.unwrap_or_default(),
                max_length: val.max_length,
            },
            "plugin" => Self::Plugin {
                name: val.name.unwrap_or_default(),
            },
            _ => Self::Preset {
                name: Default::default(),
            },
        }
    }
}

impl From<xberg::SparseEmbeddingModelType> for JsSparseEmbeddingModelType {
    fn from(val: xberg::SparseEmbeddingModelType) -> Self {
        match val {
            xberg::SparseEmbeddingModelType::Preset { name } => Self {
                type_tag: "preset".to_string(),
                additional_files: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
            xberg::SparseEmbeddingModelType::Custom {
                model_id,
                model_file,
                additional_files,
                max_length,
            } => Self {
                type_tag: "custom".to_string(),
                additional_files: Some(additional_files),
                max_length,
                model_file,
                model_id: Some(model_id),
                name: None,
            },
            xberg::SparseEmbeddingModelType::Plugin { name } => Self {
                type_tag: "plugin".to_string(),
                additional_files: None,
                max_length: None,
                model_file: None,
                model_id: None,
                name: Some(name),
            },
        }
    }
}

impl From<JsStructuredDataType> for xberg::StructuredDataType {
    fn from(val: JsStructuredDataType) -> Self {
        match val {
            JsStructuredDataType::JsonLd => Self::JsonLd,
            JsStructuredDataType::Microdata => Self::Microdata,
            JsStructuredDataType::RDFa => Self::RDFa,
        }
    }
}

impl From<xberg::StructuredDataType> for JsStructuredDataType {
    fn from(val: xberg::StructuredDataType) -> Self {
        match val {
            xberg::StructuredDataType::JsonLd => Self::JsonLd,
            xberg::StructuredDataType::Microdata => Self::Microdata,
            xberg::StructuredDataType::RDFa => Self::RDFa,
        }
    }
}

impl From<JsSummaryStrategy> for xberg::SummaryStrategy {
    fn from(val: JsSummaryStrategy) -> Self {
        match val {
            JsSummaryStrategy::Extractive => Self::Extractive,
            JsSummaryStrategy::Abstractive => Self::Abstractive,
        }
    }
}

impl From<xberg::SummaryStrategy> for JsSummaryStrategy {
    fn from(val: xberg::SummaryStrategy) -> Self {
        match val {
            xberg::SummaryStrategy::Extractive => Self::Extractive,
            xberg::SummaryStrategy::Abstractive => Self::Abstractive,
        }
    }
}

impl From<JsTableChunkingMode> for xberg::TableChunkingMode {
    fn from(val: JsTableChunkingMode) -> Self {
        match val {
            JsTableChunkingMode::Split => Self::Split,
            JsTableChunkingMode::RepeatHeader => Self::RepeatHeader,
        }
    }
}

impl From<xberg::TableChunkingMode> for JsTableChunkingMode {
    fn from(val: xberg::TableChunkingMode) -> Self {
        match val {
            xberg::TableChunkingMode::Split => Self::Split,
            xberg::TableChunkingMode::RepeatHeader => Self::RepeatHeader,
        }
    }
}

impl From<JsTableModel> for xberg::TableModel {
    fn from(val: JsTableModel) -> Self {
        match val {
            JsTableModel::Tatr => Self::Tatr,
            JsTableModel::SlanetWired => Self::SlanetWired,
            JsTableModel::SlanetWireless => Self::SlanetWireless,
            JsTableModel::SlanetPlus => Self::SlanetPlus,
            JsTableModel::SlanetAuto => Self::SlanetAuto,
            JsTableModel::Disabled => Self::Disabled,
        }
    }
}

impl From<xberg::TableModel> for JsTableModel {
    fn from(val: xberg::TableModel) -> Self {
        match val {
            xberg::TableModel::Tatr => Self::Tatr,
            xberg::TableModel::SlanetWired => Self::SlanetWired,
            xberg::TableModel::SlanetWireless => Self::SlanetWireless,
            xberg::TableModel::SlanetPlus => Self::SlanetPlus,
            xberg::TableModel::SlanetAuto => Self::SlanetAuto,
            xberg::TableModel::Disabled => Self::Disabled,
        }
    }
}

impl From<JsTableOverlapPreference> for xberg::core::config::layout::TableOverlapPreference {
    fn from(val: JsTableOverlapPreference) -> Self {
        match val {
            JsTableOverlapPreference::Content => Self::Content,
            JsTableOverlapPreference::Native => Self::Native,
            JsTableOverlapPreference::Layout => Self::Layout,
        }
    }
}

impl From<xberg::core::config::layout::TableOverlapPreference> for JsTableOverlapPreference {
    fn from(val: xberg::core::config::layout::TableOverlapPreference) -> Self {
        match val {
            xberg::core::config::layout::TableOverlapPreference::Content => Self::Content,
            xberg::core::config::layout::TableOverlapPreference::Native => Self::Native,
            xberg::core::config::layout::TableOverlapPreference::Layout => Self::Layout,
        }
    }
}

impl From<JsTextDirection> for xberg::TextDirection {
    fn from(val: JsTextDirection) -> Self {
        match val {
            JsTextDirection::LeftToRight => Self::LeftToRight,
            JsTextDirection::RightToLeft => Self::RightToLeft,
            JsTextDirection::Auto => Self::Auto,
        }
    }
}

impl From<xberg::TextDirection> for JsTextDirection {
    fn from(val: xberg::TextDirection) -> Self {
        match val {
            xberg::TextDirection::LeftToRight => Self::LeftToRight,
            xberg::TextDirection::RightToLeft => Self::RightToLeft,
            xberg::TextDirection::Auto => Self::Auto,
        }
    }
}

impl From<JsTierStrategy> for html_to_markdown_rs::TierStrategy {
    fn from(val: JsTierStrategy) -> Self {
        match val {
            JsTierStrategy::Auto => Self::Auto,
            JsTierStrategy::Tier2 => Self::Tier2,
        }
    }
}

impl From<html_to_markdown_rs::TierStrategy> for JsTierStrategy {
    fn from(val: html_to_markdown_rs::TierStrategy) -> Self {
        match val {
            html_to_markdown_rs::TierStrategy::Auto => Self::Auto,
            html_to_markdown_rs::TierStrategy::Tier2 => Self::Tier2,
        }
    }
}

impl From<JsUriKind> for xberg::UriKind {
    fn from(val: JsUriKind) -> Self {
        match val {
            JsUriKind::Hyperlink => Self::Hyperlink,
            JsUriKind::Image => Self::Image,
            JsUriKind::Anchor => Self::Anchor,
            JsUriKind::Citation => Self::Citation,
            JsUriKind::Reference => Self::Reference,
            JsUriKind::Email => Self::Email,
        }
    }
}

impl From<xberg::UriKind> for JsUriKind {
    fn from(val: xberg::UriKind) -> Self {
        match val {
            xberg::UriKind::Hyperlink => Self::Hyperlink,
            xberg::UriKind::Image => Self::Image,
            xberg::UriKind::Anchor => Self::Anchor,
            xberg::UriKind::Citation => Self::Citation,
            xberg::UriKind::Reference => Self::Reference,
            xberg::UriKind::Email => Self::Email,
        }
    }
}

impl From<JsUrlEscapeStyle> for html_to_markdown_rs::UrlEscapeStyle {
    fn from(val: JsUrlEscapeStyle) -> Self {
        match val {
            JsUrlEscapeStyle::Angle => Self::Angle,
            JsUrlEscapeStyle::Percent => Self::Percent,
        }
    }
}

impl From<html_to_markdown_rs::UrlEscapeStyle> for JsUrlEscapeStyle {
    fn from(val: html_to_markdown_rs::UrlEscapeStyle) -> Self {
        match val {
            html_to_markdown_rs::UrlEscapeStyle::Angle => Self::Angle,
            html_to_markdown_rs::UrlEscapeStyle::Percent => Self::Percent,
        }
    }
}

impl From<JsUrlExtractionMode> for xberg::UrlExtractionMode {
    fn from(val: JsUrlExtractionMode) -> Self {
        match val {
            JsUrlExtractionMode::Auto => Self::Auto,
            JsUrlExtractionMode::Document => Self::Document,
            JsUrlExtractionMode::Crawl => Self::Crawl,
        }
    }
}

impl From<xberg::UrlExtractionMode> for JsUrlExtractionMode {
    fn from(val: xberg::UrlExtractionMode) -> Self {
        match val {
            xberg::UrlExtractionMode::Auto => Self::Auto,
            xberg::UrlExtractionMode::Document => Self::Document,
            xberg::UrlExtractionMode::Crawl => Self::Crawl,
        }
    }
}

impl From<JsVlmFallbackPolicy> for xberg::VlmFallbackPolicy {
    fn from(val: JsVlmFallbackPolicy) -> Self {
        match val.mode_tag.as_str() {
            "disabled" => Self::Disabled,
            "on_low_quality" => Self::OnLowQuality {
                quality_threshold: val.quality_threshold.unwrap_or_default(),
            },
            "always" => Self::Always,
            _ => Self::Disabled,
        }
    }
}

impl From<xberg::VlmFallbackPolicy> for JsVlmFallbackPolicy {
    fn from(val: xberg::VlmFallbackPolicy) -> Self {
        match val {
            xberg::VlmFallbackPolicy::Disabled => Self {
                mode_tag: "disabled".to_string(),
                quality_threshold: None,
            },
            xberg::VlmFallbackPolicy::OnLowQuality { quality_threshold } => Self {
                mode_tag: "on_low_quality".to_string(),
                quality_threshold: Some(quality_threshold),
            },
            xberg::VlmFallbackPolicy::Always => Self {
                mode_tag: "always".to_string(),
                quality_threshold: None,
            },
        }
    }
}

impl From<JsWhisperModel> for xberg::WhisperModel {
    fn from(val: JsWhisperModel) -> Self {
        match val {
            JsWhisperModel::Tiny => Self::Tiny,
            JsWhisperModel::Base => Self::Base,
            JsWhisperModel::Small => Self::Small,
            JsWhisperModel::Medium => Self::Medium,
            JsWhisperModel::LargeV3 => Self::LargeV3,
        }
    }
}

impl From<xberg::WhisperModel> for JsWhisperModel {
    fn from(val: xberg::WhisperModel) -> Self {
        match val {
            xberg::WhisperModel::Tiny => Self::Tiny,
            xberg::WhisperModel::Base => Self::Base,
            xberg::WhisperModel::Small => Self::Small,
            xberg::WhisperModel::Medium => Self::Medium,
            xberg::WhisperModel::LargeV3 => Self::LargeV3,
        }
    }
}

impl From<JsWhitespaceMode> for html_to_markdown_rs::WhitespaceMode {
    fn from(val: JsWhitespaceMode) -> Self {
        match val {
            JsWhitespaceMode::Normalized => Self::Normalized,
            JsWhitespaceMode::Strict => Self::Strict,
        }
    }
}

impl From<html_to_markdown_rs::WhitespaceMode> for JsWhitespaceMode {
    fn from(val: html_to_markdown_rs::WhitespaceMode) -> Self {
        match val {
            html_to_markdown_rs::WhitespaceMode::Normalized => Self::Normalized,
            html_to_markdown_rs::WhitespaceMode::Strict => Self::Strict,
        }
    }
}

impl From<JsBBox> for xberg::BBox {
    fn from(val: JsBBox) -> Self {
        Self {
            x1: val.x1 as f32,
            y1: val.y1 as f32,
            x2: val.x2 as f32,
            y2: val.y2 as f32,
        }
    }
}

impl From<JsCacheStats> for xberg::CacheStats {
    fn from(val: JsCacheStats) -> Self {
        Self {
            total_files: val.total_files as usize,
            total_size_mb: val.total_size_mb,
            available_space_mb: val.available_space_mb,
            oldest_file_age_days: val.oldest_file_age_days,
            newest_file_age_days: val.newest_file_age_days,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsChunkClassificationEnrichmentConfig> for xberg::ChunkClassificationEnrichmentConfig {
    fn from(val: JsChunkClassificationEnrichmentConfig) -> Self {
        Self {
            config: val.config.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsChunkInfo> for xberg::ChunkInfo {
    fn from(val: JsChunkInfo) -> Self {
        Self {
            index: val.index,
            pages: val.pages.into(),
            estimated_time_ms: val.estimated_time_ms as u64,
        }
    }
}

impl From<JsCitation> for xberg::Citation {
    fn from(val: JsCitation) -> Self {
        Self {
            label: val.label,
            source: val.source,
            locator: val.locator,
            excerpt: val.excerpt,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsDeepseekOcrBackendOptions> for xberg::candle_ocr::DeepseekOcrBackendOptions {
    fn from(val: JsDeepseekOcrBackendOptions) -> Self {
        let mut __result = xberg::candle_ocr::DeepseekOcrBackendOptions::default();
        __result.model_path = val.model_path;
        __result.device = val.device.map(Into::into);
        __result.version = val.version;
        __result
    }
}

impl From<JsDetectResponse> for xberg::api::DetectResponse {
    fn from(val: JsDetectResponse) -> Self {
        Self {
            mime_type: val.mime_type,
            filename: val.filename,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDetectionResult> for xberg::DetectionResult {
    fn from(val: JsDetectionResult) -> Self {
        Self {
            page_width: val.page_width,
            page_height: val.page_height,
            detections: val.detections.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDiffHunk> for xberg::DiffHunk {
    fn from(val: JsDiffHunk) -> Self {
        Self {
            from_line: val.from_line as usize,
            from_count: val.from_count as usize,
            to_line: val.to_line as usize,
            to_count: val.to_count as usize,
            lines: val.lines.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDocumentBoundary> for xberg::DocumentBoundary {
    fn from(val: JsDocumentBoundary) -> Self {
        Self {
            start_page: val.start_page,
            end_page: val.end_page,
            confidence: val.confidence as f32,
            reason: val.reason.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsDocumentMetadata> for xberg::DocumentMetadata {
    fn from(val: JsDocumentMetadata) -> Self {
        Self {
            mime_type: val.mime_type,
            size_bytes: val.size_bytes as u64,
            page_count: val.page_count,
            force_ocr: val.force_ocr,
            user_chunk_config: val.user_chunk_config.map(Into::into),
            chunking_enabled: val.chunking_enabled,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsEmailAttachment> for xberg::EmailAttachment {
    fn from(val: JsEmailAttachment) -> Self {
        Self {
            name: val.name,
            filename: val.filename,
            mime_type: val.mime_type,
            size: val.size.map(|v| v as usize),
            is_image: val.is_image,
            data: val.data.map(|v| v.to_vec().into()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsEmailExtractionResult> for xberg::EmailExtractionResult {
    fn from(val: JsEmailExtractionResult) -> Self {
        Self {
            subject: val.subject,
            from_email: val.from_email,
            to_emails: val.to_emails.into_iter().collect(),
            cc_emails: val.cc_emails.into_iter().collect(),
            bcc_emails: val.bcc_emails.into_iter().collect(),
            date: val.date,
            message_id: val.message_id,
            plain_text: val.plain_text,
            html_content: val.html_content,
            content: val.content,
            attachments: val.attachments.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsEmbeddedChanges> for xberg::EmbeddedChanges {
    fn from(val: JsEmbeddedChanges) -> Self {
        let mut __result = xberg::EmbeddedChanges::default();
        if let Some(__v) = val.added {
            __result.added = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.removed {
            __result.removed = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.changed {
            __result.changed = __v.into_iter().map(Into::into).collect();
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsEmbeddedDiff> for xberg::EmbeddedDiff {
    fn from(val: JsEmbeddedDiff) -> Self {
        Self {
            path: val.path,
            diff: Box::new(val.diff.into()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsEmbeddedFile> for xberg::pdf::embedded_files::EmbeddedFile {
    fn from(val: JsEmbeddedFile) -> Self {
        Self {
            name: val.name,
            data: val.data.to_vec().into(),
            compressed_size: val.compressed_size as usize,
            mime_type: val.mime_type,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<JsExcelSheet> for xberg::ExcelSheet {
    fn from(val: JsExcelSheet) -> Self {
        Self {
            name: val.name,
            markdown: val.markdown,
            row_count: val.row_count as usize,
            col_count: val.col_count as usize,
            cell_count: val.cell_count as usize,
            table_cells: val.table_cells.map(|v| v.into_iter().collect()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsExcelWorkbook> for xberg::ExcelWorkbook {
    fn from(val: JsExcelWorkbook) -> Self {
        Self {
            sheets: val.sheets.into_iter().map(Into::into).collect(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsExtractionDiff> for xberg::ExtractionDiff {
    fn from(val: JsExtractionDiff) -> Self {
        let mut __result = xberg::ExtractionDiff::default();
        if let Some(__v) = val.content_diff {
            __result.content_diff = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.tables_added {
            __result.tables_added = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.tables_removed {
            __result.tables_removed = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.tables_changed {
            __result.tables_changed = __v.into_iter().map(Into::into).collect();
        }
        if let Some(__v) = val.metadata_changed {
            __result.metadata_changed = __v;
        }
        if let Some(__v) = val.embedded_changes {
            __result.embedded_changes = __v.into();
        }
        __result
    }
}

impl From<JsFootnoteAnchor> for xberg::FootnoteAnchor {
    fn from(val: JsFootnoteAnchor) -> Self {
        Self {
            label: val.label,
            offset: val.offset as usize,
        }
    }
}

impl From<JsFootnoteDefinition> for xberg::FootnoteDefinition {
    fn from(val: JsFootnoteDefinition) -> Self {
        Self {
            label: val.label,
            content: val.content,
            offset: val.offset as usize,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsGlmOcrBackendOptions> for xberg::candle_ocr::GlmOcrBackendOptions {
    fn from(val: JsGlmOcrBackendOptions) -> Self {
        let mut __result = xberg::candle_ocr::GlmOcrBackendOptions::default();
        __result.task = val.task.map(Into::into);
        __result.device = val.device.map(Into::into);
        __result.layout_mode = val.layout_mode.map(Into::into);
        __result.enable_chart_understanding = val.enable_chart_understanding;
        __result.cache_dir = val.cache_dir;
        __result
    }
}

impl From<JsLateInteractionPreset> for xberg::LateInteractionPreset {
    fn from(val: JsLateInteractionPreset) -> Self {
        Self {
            name: val.name,
            model_repo: val.model_repo,
            model_file: val.model_file,
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as usize,
            query_max_length: val.query_max_length as usize,
            dim: val.dim as usize,
            description: val.description,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsLayoutDetection> for xberg::LayoutDetection {
    fn from(val: JsLayoutDetection) -> Self {
        Self {
            class_name: val.class_name.into(),
            confidence: val.confidence as f32,
            bbox: val.bbox.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsModelPaths> for xberg::ModelPaths {
    fn from(val: JsModelPaths) -> Self {
        Self {
            det_model: val.det_model.into(),
            cls_model: val.cls_model.into(),
            rec_model: val.rec_model.into(),
            dict_file: val.dict_file.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsMultidocInput> for xberg::MultidocInput {
    fn from(val: JsMultidocInput) -> Self {
        Self {
            page_count: val.page_count,
            pages: val.pages.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsOcrExtractionResult> for xberg::OcrExtractionResult {
    fn from(val: JsOcrExtractionResult) -> Self {
        let mut __result = xberg::OcrExtractionResult::default();
        if let Some(__v) = val.content {
            __result.content = __v;
        }
        if let Some(__v) = val.mime_type {
            __result.mime_type = __v;
        }
        if let Some(__v) = val.metadata {
            __result.metadata = __v.into_iter().map(|(k, v)| (k.into(), v)).collect();
        }
        if let Some(__v) = val.tables {
            __result.tables = __v.into_iter().map(Into::into).collect();
        }
        __result.ocr_elements = val.ocr_elements.map(|v| v.into_iter().map(Into::into).collect());
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsOcrTable> for xberg::OcrTable {
    fn from(val: JsOcrTable) -> Self {
        Self {
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown,
            page_number: val.page_number,
            bounding_box: val.bounding_box.map(Into::into),
        }
    }
}

impl From<JsOcrTableBoundingBox> for xberg::OcrTableBoundingBox {
    fn from(val: JsOcrTableBoundingBox) -> Self {
        Self {
            left: val.left,
            top: val.top,
            right: val.right,
            bottom: val.bottom,
        }
    }
}

impl From<JsOrientationResult> for xberg::OrientationResult {
    fn from(val: JsOrientationResult) -> Self {
        Self {
            degrees: val.degrees,
            confidence: val.confidence as f32,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsPaddleOcrVlBackendOptions> for xberg::candle_ocr::PaddleOcrVlBackendOptions {
    fn from(val: JsPaddleOcrVlBackendOptions) -> Self {
        let mut __result = xberg::candle_ocr::PaddleOcrVlBackendOptions::default();
        __result.task = val.task.map(Into::into);
        __result.model_path = val.model_path;
        __result.model_id = val.model_id;
        __result.hf_revision = val.hf_revision;
        __result.cache_dir = val.cache_dir;
        __result.device = val.device.map(Into::into);
        __result
    }
}

impl From<JsPageRange> for xberg::PageRange {
    fn from(val: JsPageRange) -> Self {
        Self {
            start: val.start,
            end: val.end,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsPatternMatch> for xberg::text::redaction::patterns::PatternMatch {
    fn from(val: JsPatternMatch) -> Self {
        Self {
            start: val.start as usize,
            end: val.end as usize,
            category: val.category.into(),
            text: val.text,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPptxAppProperties> for xberg::extraction::office_metadata::app_properties::PptxAppProperties {
    fn from(val: JsPptxAppProperties) -> Self {
        let mut __result = xberg::extraction::office_metadata::app_properties::PptxAppProperties::default();
        __result.application = val.application;
        __result.app_version = val.app_version;
        __result.total_time = val.total_time;
        __result.company = val.company;
        __result.doc_security = val.doc_security;
        __result.scale_crop = val.scale_crop;
        __result.links_up_to_date = val.links_up_to_date;
        __result.shared_doc = val.shared_doc;
        __result.hyperlinks_changed = val.hyperlinks_changed;
        __result.slides = val.slides;
        __result.notes = val.notes;
        __result.hidden_slides = val.hidden_slides;
        __result.multimedia_clips = val.multimedia_clips;
        __result.presentation_format = val.presentation_format;
        if let Some(__v) = val.slide_titles {
            __result.slide_titles = __v.into_iter().collect();
        }
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsPptxExtractionResult> for xberg::PptxExtractionResult {
    fn from(val: JsPptxExtractionResult) -> Self {
        Self {
            content: val.content,
            metadata: val.metadata.into(),
            slide_count: val.slide_count as usize,
            image_count: val.image_count as usize,
            table_count: val.table_count as usize,
            images: val.images.into_iter().map(Into::into).collect(),
            page_structure: val.page_structure.map(Into::into),
            page_contents: val.page_contents.map(|v| v.into_iter().map(Into::into).collect()),
            document: val.document.map(Into::into),
            hyperlinks: val.hyperlinks.into_iter().map(Into::into).collect(),
            office_metadata: val
                .office_metadata
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            revisions: val.revisions.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsPresentationHyperlink> for xberg::PresentationHyperlink {
    fn from(val: JsPresentationHyperlink) -> Self {
        let mut __result = xberg::PresentationHyperlink::default();
        if let Some(__v) = val.url {
            __result.url = __v;
        }
        __result.label = val.label;
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsRecognizedTable> for xberg::RecognizedTable {
    fn from(val: JsRecognizedTable) -> Self {
        Self {
            detection_bbox: val.detection_bbox.into(),
            cells: val.cells.into_iter().collect(),
            markdown: val.markdown,
        }
    }
}

impl From<JsRerankedDocument> for xberg::RerankedDocument {
    fn from(val: JsRerankedDocument) -> Self {
        Self {
            index: val.index as usize,
            score: val.score as f32,
            document: val.document,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsResolvedPreset> for xberg::ResolvedPreset {
    fn from(val: JsResolvedPreset) -> Self {
        Self {
            id: val.id,
            version: val.version,
            fingerprint: val.fingerprint,
            schema_name: val.schema_name,
            schema: val.schema,
            system_prompt: val.system_prompt,
            merge_mode: val.merge_mode.into(),
            preferred_call_mode: val.preferred_call_mode.into(),
            emit_citations: val.emit_citations,
        }
    }
}

impl From<JsSparseEmbeddingPreset> for xberg::SparseEmbeddingPreset {
    fn from(val: JsSparseEmbeddingPreset) -> Self {
        Self {
            name: val.name,
            model_repo: val.model_repo,
            model_file: val.model_file,
            additional_files: val.additional_files.into_iter().collect(),
            max_length: val.max_length as usize,
            description: val.description,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsStructuredDataResult> for xberg::extraction::structured::StructuredDataResult {
    fn from(val: JsStructuredDataResult) -> Self {
        Self {
            content: val.content,
            format: val.format.into(),
            metadata: val.metadata.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
            text_fields: val.text_fields.into_iter().collect(),
            value: val.value,
            flattened: val.flattened.into_iter().collect(),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsTableCell> for xberg::TableCell {
    fn from(val: JsTableCell) -> Self {
        let mut __result = xberg::TableCell::default();
        if let Some(__v) = val.content {
            __result.content = __v;
        }
        if let Some(__v) = val.row_span {
            __result.row_span = __v;
        }
        if let Some(__v) = val.col_span {
            __result.col_span = __v;
        }
        if let Some(__v) = val.is_header {
            __result.is_header = __v;
        }
        __result
    }
}

#[allow(clippy::useless_conversion)]
impl From<JsTableDiff> for xberg::TableDiff {
    fn from(val: JsTableDiff) -> Self {
        Self {
            from_index: val.from_index as usize,
            to_index: val.to_index as usize,
            cell_changes: val.cell_changes.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsTextExtractionResult> for xberg::TextExtractionResult {
    fn from(val: JsTextExtractionResult) -> Self {
        Self {
            content: val.content,
            line_count: val.line_count as usize,
            word_count: val.word_count as usize,
            character_count: val.character_count as usize,
            headers: val.headers.map(|v| v.into_iter().collect()),
            links: val.links.map(|v| v.into_iter().map(Into::into).collect()),
            code_blocks: val.code_blocks.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::useless_conversion)]
impl From<JsTrocrBackendOptions> for xberg::candle_ocr::TrocrBackendOptions {
    fn from(val: JsTrocrBackendOptions) -> Self {
        let mut __result = xberg::candle_ocr::TrocrBackendOptions::default();
        __result.variant = val.variant.map(Into::into);
        __result.device = val.device.map(Into::into);
        __result.cache_dir = val.cache_dir;
        __result.hf_revision = val.hf_revision;
        __result
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsUserChunkConfig> for xberg::UserChunkConfig {
    fn from(val: JsUserChunkConfig) -> Self {
        let mut __result = xberg::UserChunkConfig::default();
        __result.page_ranges = val.page_ranges.map(|v| v.into_iter().map(Into::into).collect());
        __result.pages_per_chunk = val.pages_per_chunk;
        if let Some(__v) = val.force_chunking {
            __result.force_chunking = __v;
        }
        if let Some(__v) = val.disable_chunking {
            __result.disable_chunking = __v;
        }
        __result
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
impl From<JsXlsxAppProperties> for xberg::extraction::office_metadata::app_properties::XlsxAppProperties {
    fn from(val: JsXlsxAppProperties) -> Self {
        let mut __result = xberg::extraction::office_metadata::app_properties::XlsxAppProperties::default();
        __result.application = val.application;
        __result.app_version = val.app_version;
        __result.doc_security = val.doc_security;
        __result.scale_crop = val.scale_crop;
        __result.links_up_to_date = val.links_up_to_date;
        __result.shared_doc = val.shared_doc;
        __result.hyperlinks_changed = val.hyperlinks_changed;
        __result.company = val.company;
        if let Some(__v) = val.worksheet_names {
            __result.worksheet_names = __v.into_iter().collect();
        }
        __result
    }
}

impl From<JsXmlExtractionResult> for xberg::XmlExtractionResult {
    fn from(val: JsXmlExtractionResult) -> Self {
        Self {
            content: val.content,
            element_count: val.element_count as usize,
            unique_elements: val.unique_elements.into_iter().collect(),
        }
    }
}

impl From<JsCandleDevicePreference> for xberg::candle_ocr::CandleDevicePreference {
    fn from(val: JsCandleDevicePreference) -> Self {
        match val {
            JsCandleDevicePreference::Auto => Self::Auto,
            JsCandleDevicePreference::Cpu => Self::Cpu,
            JsCandleDevicePreference::Cuda => Self::Cuda,
            JsCandleDevicePreference::Metal => Self::Metal,
        }
    }
}

impl From<JsBoundaryReason> for xberg::BoundaryReason {
    fn from(val: JsBoundaryReason) -> Self {
        match val {
            JsBoundaryReason::Start => Self::Start,
            JsBoundaryReason::PageOneMarker => Self::PageOneMarker,
            JsBoundaryReason::LetterheadReset => Self::LetterheadReset,
            JsBoundaryReason::DensityShift => Self::DensityShift,
            JsBoundaryReason::End => Self::End,
        }
    }
}

impl From<JsGlmOcrTaskKind> for xberg::candle_ocr::GlmOcrTaskKind {
    fn from(val: JsGlmOcrTaskKind) -> Self {
        match val {
            JsGlmOcrTaskKind::Ocr => Self::Ocr,
            JsGlmOcrTaskKind::Table => Self::Table,
            JsGlmOcrTaskKind::Formula => Self::Formula,
            JsGlmOcrTaskKind::Chart => Self::Chart,
            JsGlmOcrTaskKind::Caption => Self::Caption,
        }
    }
}

impl From<JsGlmOcrLayoutMode> for xberg::candle_ocr::GlmOcrLayoutMode {
    fn from(val: JsGlmOcrLayoutMode) -> Self {
        match val {
            JsGlmOcrLayoutMode::WholePage => Self::WholePage,
            JsGlmOcrLayoutMode::Paired => Self::Paired,
        }
    }
}

impl From<JsLayoutClass> for xberg::LayoutClass {
    fn from(val: JsLayoutClass) -> Self {
        match val {
            JsLayoutClass::Caption => Self::Caption,
            JsLayoutClass::Chart => Self::Chart,
            JsLayoutClass::Footnote => Self::Footnote,
            JsLayoutClass::Formula => Self::Formula,
            JsLayoutClass::ListItem => Self::ListItem,
            JsLayoutClass::PageFooter => Self::PageFooter,
            JsLayoutClass::PageHeader => Self::PageHeader,
            JsLayoutClass::Picture => Self::Picture,
            JsLayoutClass::SectionHeader => Self::SectionHeader,
            JsLayoutClass::Table => Self::Table,
            JsLayoutClass::Text => Self::Text,
            JsLayoutClass::Title => Self::Title,
            JsLayoutClass::DocumentIndex => Self::DocumentIndex,
            JsLayoutClass::Code => Self::Code,
            JsLayoutClass::CheckboxSelected => Self::CheckboxSelected,
            JsLayoutClass::CheckboxUnselected => Self::CheckboxUnselected,
            JsLayoutClass::Form => Self::Form,
            JsLayoutClass::KeyValueRegion => Self::KeyValueRegion,
        }
    }
}

impl From<JsPaddleOcrVlTaskKind> for xberg::candle_ocr::PaddleOcrVlTaskKind {
    fn from(val: JsPaddleOcrVlTaskKind) -> Self {
        match val {
            JsPaddleOcrVlTaskKind::Ocr => Self::Ocr,
            JsPaddleOcrVlTaskKind::Table => Self::Table,
            JsPaddleOcrVlTaskKind::Formula => Self::Formula,
            JsPaddleOcrVlTaskKind::Chart => Self::Chart,
        }
    }
}

impl From<JsCandleTrocrVariant> for xberg::candle_ocr::CandleTrocrVariant {
    fn from(val: JsCandleTrocrVariant) -> Self {
        match val {
            JsCandleTrocrVariant::BasePrinted => Self::BasePrinted,
            JsCandleTrocrVariant::LargePrinted => Self::LargePrinted,
            JsCandleTrocrVariant::BaseHandwritten => Self::BaseHandwritten,
            JsCandleTrocrVariant::LargeHandwritten => Self::LargeHandwritten,
        }
    }
}

// Error variant name constants
pub const HEURISTICS_ERROR_ERROR_CONFIG_ERROR: &str = "ConfigError";
pub const HEURISTICS_ERROR_ERROR_CONFIG_ERROR_CODE: u32 = 1100;
pub const HEURISTICS_ERROR_ERROR_PDF_ANALYSIS_ERROR: &str = "PdfAnalysisError";
pub const HEURISTICS_ERROR_ERROR_PDF_ANALYSIS_ERROR_CODE: u32 = 1101;

/// Convert a `xberg::heuristics::error::HeuristicsError` error to a NAPI error.
///
/// The stable numeric error code is deliberately not interpolated into this message —
/// `napi::Error` has no per-variant subclass to attach it to. When available, it is exposed
/// as a structured `code` field on the `*Info` companion class instead (see
/// `gen_napi_error_class`), never smuggled into human-facing text.
#[allow(dead_code)]
fn heuristics_error_to_napi_err(e: xberg::heuristics::error::HeuristicsError) -> napi::Error {
    napi::Error::new(napi::Status::GenericFailure, e.to_string())
}

// Error variant name constants
pub const LOAD_ERROR_ERROR_PARSE: &str = "Parse";
pub const LOAD_ERROR_ERROR_PARSE_CODE: u32 = 1200;
pub const LOAD_ERROR_ERROR_SCHEMA_VALIDATION: &str = "SchemaValidation";
pub const LOAD_ERROR_ERROR_SCHEMA_VALIDATION_CODE: u32 = 1201;
pub const LOAD_ERROR_ERROR_DESERIALIZE: &str = "Deserialize";
pub const LOAD_ERROR_ERROR_DESERIALIZE_CODE: u32 = 1202;
pub const LOAD_ERROR_ERROR_ID_MISMATCH: &str = "IdMismatch";
pub const LOAD_ERROR_ERROR_ID_MISMATCH_CODE: u32 = 1203;
pub const LOAD_ERROR_ERROR_BAD_META_SCHEMA: &str = "BadMetaSchema";
pub const LOAD_ERROR_ERROR_BAD_META_SCHEMA_CODE: u32 = 1204;
pub const LOAD_ERROR_ERROR_IO: &str = "Io";
pub const LOAD_ERROR_ERROR_IO_CODE: u32 = 1205;

/// Convert a `xberg::presets::loader::LoadError` error to a NAPI error.
///
/// The stable numeric error code is deliberately not interpolated into this message —
/// `napi::Error` has no per-variant subclass to attach it to. When available, it is exposed
/// as a structured `code` field on the `*Info` companion class instead (see
/// `gen_napi_error_class`), never smuggled into human-facing text.
#[allow(dead_code)]
fn load_error_to_napi_err(e: xberg::presets::loader::LoadError) -> napi::Error {
    napi::Error::new(napi::Status::GenericFailure, e.to_string())
}

// Error variant name constants
pub const RESOLVE_ERROR_ERROR_SCHEMA_NOT_OBJECT: &str = "SchemaNotObject";
pub const RESOLVE_ERROR_ERROR_SCHEMA_NOT_OBJECT_CODE: u32 = 1300;

/// Convert a `xberg::presets::resolve::ResolveError` error to a NAPI error.
///
/// The stable numeric error code is deliberately not interpolated into this message —
/// `napi::Error` has no per-variant subclass to attach it to. When available, it is exposed
/// as a structured `code` field on the `*Info` companion class instead (see
/// `gen_napi_error_class`), never smuggled into human-facing text.
#[allow(dead_code)]
fn resolve_error_to_napi_err(e: xberg::presets::resolve::ResolveError) -> napi::Error {
    napi::Error::new(napi::Status::GenericFailure, e.to_string())
}

// Error variant name constants
pub const XBERG_ERROR_ERROR_IO: &str = "Io";
pub const XBERG_ERROR_ERROR_IO_CODE: u32 = 1000;
pub const XBERG_ERROR_ERROR_PARSING: &str = "Parsing";
pub const XBERG_ERROR_ERROR_PARSING_CODE: u32 = 1001;
pub const XBERG_ERROR_ERROR_OCR: &str = "Ocr";
pub const XBERG_ERROR_ERROR_OCR_CODE: u32 = 1002;
pub const XBERG_ERROR_ERROR_VALIDATION: &str = "Validation";
pub const XBERG_ERROR_ERROR_VALIDATION_CODE: u32 = 1003;
pub const XBERG_ERROR_ERROR_CACHE: &str = "Cache";
pub const XBERG_ERROR_ERROR_CACHE_CODE: u32 = 1004;
pub const XBERG_ERROR_ERROR_IMAGE_PROCESSING: &str = "ImageProcessing";
pub const XBERG_ERROR_ERROR_IMAGE_PROCESSING_CODE: u32 = 1005;
pub const XBERG_ERROR_ERROR_SERIALIZATION: &str = "Serialization";
pub const XBERG_ERROR_ERROR_SERIALIZATION_CODE: u32 = 1006;
pub const XBERG_ERROR_ERROR_MISSING_DEPENDENCY: &str = "MissingDependency";
pub const XBERG_ERROR_ERROR_MISSING_DEPENDENCY_CODE: u32 = 1007;
pub const XBERG_ERROR_ERROR_PLUGIN: &str = "Plugin";
pub const XBERG_ERROR_ERROR_PLUGIN_CODE: u32 = 1008;
pub const XBERG_ERROR_ERROR_LOCK_POISONED: &str = "LockPoisoned";
pub const XBERG_ERROR_ERROR_LOCK_POISONED_CODE: u32 = 1009;
pub const XBERG_ERROR_ERROR_UNSUPPORTED_FORMAT: &str = "UnsupportedFormat";
pub const XBERG_ERROR_ERROR_UNSUPPORTED_FORMAT_CODE: u32 = 1010;
pub const XBERG_ERROR_ERROR_EMBEDDING: &str = "Embedding";
pub const XBERG_ERROR_ERROR_EMBEDDING_CODE: u32 = 1011;
pub const XBERG_ERROR_ERROR_RERANKING: &str = "Reranking";
pub const XBERG_ERROR_ERROR_RERANKING_CODE: u32 = 1012;
pub const XBERG_ERROR_ERROR_TRANSCRIPTION: &str = "Transcription";
pub const XBERG_ERROR_ERROR_TRANSCRIPTION_CODE: u32 = 1013;
pub const XBERG_ERROR_ERROR_TIMEOUT: &str = "Timeout";
pub const XBERG_ERROR_ERROR_TIMEOUT_CODE: u32 = 1014;
pub const XBERG_ERROR_ERROR_CANCELLED: &str = "Cancelled";
pub const XBERG_ERROR_ERROR_CANCELLED_CODE: u32 = 1015;
pub const XBERG_ERROR_ERROR_SECURITY: &str = "Security";
pub const XBERG_ERROR_ERROR_SECURITY_CODE: u32 = 1016;
pub const XBERG_ERROR_ERROR_OTHER: &str = "Other";
pub const XBERG_ERROR_ERROR_OTHER_CODE: u32 = 1017;

/// Convert a `xberg::error::XbergError` error to a NAPI error.
///
/// The stable numeric error code is deliberately not interpolated into this message —
/// `napi::Error` has no per-variant subclass to attach it to. When available, it is exposed
/// as a structured `code` field on the `*Info` companion class instead (see
/// `gen_napi_error_class`), never smuggled into human-facing text.
#[allow(dead_code)]
fn xberg_error_to_napi_err(e: xberg::error::XbergError) -> napi::Error {
    napi::Error::new(napi::Status::GenericFailure, e.to_string())
}
