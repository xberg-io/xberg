{
  "id": "rerank_basic",
  "category": "rerank",
  "description": "rerank: plugin model with no backend registered returns an error",
  "call": "rerank",
  "input": {
    "query": "What is machine learning?",
    "documents": [
      "Machine learning is a subset of artificial intelligence focused on learning from data.",
      "Cats are independent animals that prefer to play alone.",
      "Deep learning uses neural networks with multiple layers."
    ],
    "config": {
      "model": {
        "type": "plugin",
        "name": "rerank_basic"
      }
    }
  },
  "assertions": [
    {
      "type": "error"
    }
  ]
}
