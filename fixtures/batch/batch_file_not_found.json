{
	"id": "batch_file_not_found",
	"category": "batch",
	"description": "extract_batch nonexistent",
	"tags": [
		"batch",
		"error"
	],
	"call": "extract_batch",
	"input": {
		"inputs": [
			{
				"kind": "uri",
				"uri": "/nonexistent/a.pdf"
			},
			{
				"kind": "uri",
				"uri": "/nonexistent/b.txt"
			}
		]
	},
	"assertions": [
		{
			"type": "not_error"
		}
	],
	"skip": {
		"languages": [
			"wasm"
		],
		"reason": "WASM cannot access filesystem; use bytes input with file content instead"
	}
}
