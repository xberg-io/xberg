{
	"id": "batch_file_async_not_found",
	"category": "batch",
	"description": "extract_batch async nonexistent",
	"tags": [
		"batch",
		"async"
	],
	"call": "extract_batch",
	"input": {
		"inputs": [
			{
				"kind": "uri",
				"uri": "/nonexistent/a.pdf"
			}
		]
	},
	"assertions": [
		{
			"type": "not_error"
		}
	],
	"skip": {
		"languages": [
			"wasm"
		],
		"reason": "WASM cannot access filesystem; use bytes input with file content instead"
	}
}
