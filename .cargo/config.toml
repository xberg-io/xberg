# Local dev override: build kreuzberg-dart without ONNX Runtime (embeddings)
# ONNX Runtime has no prebuilt binaries for x86_64-apple-darwin.
# GitHub CI uses kreuzberg-dev/actions/setup-onnx-runtime for full builds.
[target.x86_64-apple-darwin]
rustflags = ["--cfg", "feature=\"no-ort\""]
