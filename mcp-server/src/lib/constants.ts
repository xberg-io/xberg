/**
 * Centralized constants for xberg-mcp-server.
 * EMBEDDING_DIM must match the runtime's default embedder output.
 * Current default embedder: Xenova/bge-m3 → 1024 dimensions.
 */
export const EMBEDDING_DIM = 1024;
export const EMBEDDER_MODEL_ID = "Xenova/bge-m3";